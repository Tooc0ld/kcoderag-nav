{
  "author": {
    "name": "KCodeRag"
  },
  "description": "Graph-first structural code navigation with the KCodeRag QA service.",
  "keywords": [
    "code-navigation",
    "knowledge-graph",
    "mcp"
  ],
  "mcpServers": "./mcp.json",
  "name": "kcoderag-nav",
  "rules": "./rules/",
  "skills": "./skills/",
  "variables": {
    "properties": {
      "KCODERAG_BEARER_TOKEN": {
        "default": "kcoderag-qa-token-2026",
        "description": "Internal KCodeRag QA bearer credential.",
        "title": "KCodeRag bearer token",
        "type": "string"
      },
      "KCODERAG_MCP_URL": {
        "default": "http://10.11.184.71:8010/mcp/",
        "description": "Internal KCodeRag QA MCP endpoint.",
        "title": "KCodeRag MCP URL",
        "type": "string"
      }
    },
    "required": [
      "KCODERAG_MCP_URL",
      "KCODERAG_BEARER_TOKEN"
    ],
    "type": "object"
  },
  "version": "0.2.2"
}

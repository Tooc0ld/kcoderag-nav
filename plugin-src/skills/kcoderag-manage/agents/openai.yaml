interface:
  display_name: "KCodeRag Manage"
  short_description: "Inspect and safely update KCodeRag Nav installations"
  default_prompt: "Use $kcoderag-manage to check this project's installation health."
policy:
  allow_implicit_invocation: true

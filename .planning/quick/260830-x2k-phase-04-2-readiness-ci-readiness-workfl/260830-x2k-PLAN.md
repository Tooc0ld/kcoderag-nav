---
quick_id: 260830-x2k
status: complete
mode: quick
---

# Quick Task 260830-x2k: Optimize Phase 04.2 readiness CI latency

## Task 1: Reuse one verified content-addressed invocation tarball and npm cache per smoke run

**Files:**
- `src/smoke/host-smoke.cts`
- `tests/smoke/host-smoke.test.cts`

**Action:** Replace command-specific temporary tgz copies with one stable SHA-named tgz and one npm execution cache shared by the isolated host runtimes in a smoke run. Continue validating the canonical lease and the invocation file before and after every npm exec, preserve symlink/special-file rejection, and keep mutation failures normalized across all five hosts. Add regression assertions that successful lifecycle calls reuse both stable paths while host homes/configs remain isolated.

**Verify:**
- `npm run build`
- `npm run test:smoke`

**Done:** The same five-host packaged lifecycle and drift evidence pass while Windows can reuse npm's execution cache for a stable package spec.

## Task 2: Make artifact failure tests fail fast without weakening the real package job

**Files:**
- `src/maintainer/github-artifact-upload.cts`
- `src/maintainer/readiness-workflow.cts`
- `tests/maintainer/readiness-workflow.test.cts`

**Action:** Export a secret-safe GitHub artifact runtime preflight and run it before package creation/smoke. Add narrow dependency injection to the workflow command so the stdout-normalization test can stub the already-covered smoke/upload layers instead of executing five real hosts. Production defaults must remain the real pack, scan, smoke, and upload functions.

**Verify:**
- `npm run build`
- `npm run test:github-artifact-upload`
- `npm run test:readiness-workflow`
- `npm run test:ci-contract`

**Done:** Missing artifact credentials fail before expensive work, safe error serialization remains covered, and production readiness still audits, scans, smokes, and uploads one exact artifact.

## Task 3: Prove correctness and compare latency

**Files:**
- `.planning/quick/260830-x2k-phase-04-2-readiness-ci-readiness-workfl/260830-x2k-SUMMARY.md`
- `.planning/STATE.md`

**Action:** Re-run changed suites after the final edit, compare with the captured baselines (`readiness-workflow` 121059 ms; `smoke` 114325 ms), run the full serialized test suite, generation check, and diff hygiene, then record the measured result without claiming a hosted timing that was not rerun.

**Verify:**
- `npm test`
- `npm run generate:check`
- `git diff --check`

**Done:** Fresh evidence shows no gate loss and quantifies local speedup; hosted Windows improvement is described as expected until a new hosted receipt is authorized and run.

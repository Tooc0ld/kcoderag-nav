---
quick_id: 260903-x20
phase: quick-260903-x20
plan: "01"
type: execute
status: complete
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - .github/workflows/acceptance.yml
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - src/maintainer/acceptance-workflow.cts
  - tests/maintainer/acceptance-workflow.test.cts
  - tests/maintainer/ci-contract.test.cts
  - tests/maintainer/release-workflow.test.cts
  - .planning/STATE.md
  - .planning/quick/260903-x20-optimize-ci-by-skipping-documentation-on/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pushes that change only README.md, docs/, or .planning/ do not trigger exact-candidate acceptance, while any product or workflow path still does."
    - "Ordinary required CI retains Windows/Linux and Node 22/24 coverage, but the heavy five-host packaged test runs in exactly one Windows Node 22 lane per CI event."
    - "Exact-candidate acceptance builds one artifact, consumes it in exactly one Windows Node 22 PACKAGED lane, and preserves the protected self-hosted LIVE lane and final fail-closed gate."
    - "Release CI retains the four-platform ordinary matrix, runs required five-host smoke once in an independent Windows Node 22 job, and publishes only after both gates pass."
    - "All workflow actions remain immutable, permissions stay read-only except the scoped npm credential, and no required result becomes optional."
  artifacts:
    - path: .github/workflows/acceptance.yml
      provides: Documentation-aware trigger and single heavy PACKAGED lane
    - path: .github/workflows/ci.yml
      provides: Four-platform ordinary contracts plus one Windows packaged job
    - path: .github/workflows/release.yml
      provides: Parallel four-platform contracts and one release smoke gate
    - path: src/maintainer/acceptance-workflow.cts
      provides: Fail-closed singleton PACKAGED lane contract
  key_links:
    - from: .github/workflows/acceptance.yml
      to: src/maintainer/acceptance-workflow.cts
      via: exact trigger, topology, artifact, and lane validation
    - from: .github/workflows/ci.yml
      to: package.json
      via: test:ci matrix and one test:ci:packaged invocation
    - from: .github/workflows/release.yml
      to: npm publish
      via: publish needs both required-contracts and packaged-contracts
---

# Quick Task 260903-x20: optimize CI packaged acceptance

## Goal

Remove waste observed in the public-release follow-up runs without weakening ordinary OS/Node coverage, exact artifact binding, protected LIVE admission, or release publication gates.

## Task 1: Lock the optimized topology with failing contracts

**Files:** `tests/maintainer/ci-contract.test.cts`, `tests/maintainer/acceptance-workflow.test.cts`, `tests/maintainer/release-workflow.test.cts`

**Action:** Update contract expectations first: the acceptance push trigger ignores only the closed documentation set; CI and acceptance each have one Windows Node 22 packaged lane; release has a separate single Windows Node 22 packaged job and publish depends on both required jobs.

**Verify:** Build and run the three targeted compiled test files before workflow changes; require failures that identify the old four-lane/inline-smoke topology.

**Done:** RED evidence proves the tests detect every intended scheduling change.

## Task 2: Implement the single-smoke workflow topology

**Files:** `.github/workflows/acceptance.yml`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `src/maintainer/acceptance-workflow.cts`

**Action:** Add exact `paths-ignore` entries for `README.md`, `docs/**`, and `.planning/**` to the acceptance push trigger. Replace the CI and acceptance four-lane packaged matrices with one explicit Windows Node 22 job. Move release `smoke:required` out of the four-platform ordinary matrix into an independent Windows Node 22 packaged job, then require both jobs before publish. Preserve artifact identity, fail-closed verification, action pins, permissions, and protected LIVE behavior.

**Verify:** Rebuild and run acceptance-workflow, CI-contract, and release-workflow tests; run the checked-in acceptance validator and action-pin checks.

**Done:** The workflow contracts report one heavy packaged execution per event while retaining the ordinary four-platform matrix and every security boundary.

## Task 3: Run regression gates and record measured evidence

**Files:** quick SUMMARY/VERIFICATION and `.planning/STATE.md`

**Action:** Run local build, scoped maintainer tests, dependency/generation/package gates, and `git diff --check`. Commit the product/workflow change, push it, and monitor hosted CI/acceptance to prove documentation-only routing and the optimized product-change topology as applicable. Record honest durations without promoting PACKAGED evidence to LIVE.

**Verify:** Fresh tests pass, GitHub workflow syntax/action checks pass, hosted required gates succeed, GSD verification status is `passed`, and the worktree is clean.

**Done:** The optimization is committed, pushed, evidence-backed, and leaves Phase 05 LIVE work unchanged.

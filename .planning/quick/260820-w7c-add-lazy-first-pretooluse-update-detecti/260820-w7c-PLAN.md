---
phase: quick
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugin-src/version.txt
  - plugin-src/hooks/grep_nudge.py
  - plugin-src/hooks/update_check.py
  - plugin-src/hooks/test_grep_nudge.py
  - plugin-src/README.md.tmpl
  - scripts/generate_plugins.py
  - scripts/manage_project_install.py
  - scripts/update_plugin.py
  - tests/test_generation.py
  - tests/test_routing_and_hooks.py
  - tests/test_project_install.py
  - tests/test_update_check.py
  - tests/test_plugin_update.py
  - kcoderag-update.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/hooks/grep_nudge.py
  - kcoderag-qa/hooks/update_check.py
  - kcoderag-qa/hooks/test_grep_nudge.py
  - kcoderag-qa/README.md
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
  - kcoderag-dev/hooks/grep_nudge.py
  - kcoderag-dev/hooks/update_check.py
  - kcoderag-dev/hooks/test_grep_nudge.py
  - kcoderag-dev/README.md
  - README.md
  - MCP_QA_EXPERIENCE_GUIDE.md
autonomous: true
requirements:
  - UPDATE-ID-01
  - UPDATE-CHECK-01
  - UPDATE-CHECK-02
  - UPDATE-CMD-01
  - UPDATE-DOCS-01
  - DELIVERY-01
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "QA 与 Dev 的 Claude Code/Codex 插件以及 Codex 项目安装都只经现有 PreToolUse 路径，在每个可识别 session 的第一次 Grep/Glob/Bash payload 上延迟检查更新；不注册其他 lifecycle hook。"
    - "同一 session 的后续 PreToolUse 不重复网络检查或更新提示；缺 session id 时按项目/环境和固定时间桶形成有界 fallback throttle，而不是每次工具调用重试。"
    - "远端检查只访问固定 HTTPS raw GitHub 版本文档，带短超时、响应大小上限、严格 schema/版本校验、24 小时缓存与并发锁；任何 I/O、解析、锁或缓存异常都静默 fail-open。"
    - "远端数据只能决定 validated version equality；AI 上下文中的提示完全由本地固定模板生成，不回显任意远端文本、endpoint、header、credential、异常或 subprocess 输出。"
    - "每个环境 manifest 的完整 `+codex.<cachebuster>` 由该环境可分发 bytes 的规范化内容指纹确定；相同输入重复生成完全一致，任一有效插件内容变化会改变对应版本且不依赖时钟。"
    - "检测到新版本时只提示当前版本、新版本和文档化更新命令，并要求先取得用户确认；hook 本身绝不 fetch marketplace、重装插件或改写项目。"
    - "项目安装器提供保留当前单一环境的 `update --target PATH`；Codex/Claude marketplace updater 使用宿主现有刷新/更新合同，步骤失败时停止并输出稳定、脱敏 reason。"
    - "QA/Dev 继续互斥，两个生成包仍各自自包含 update checker；现有 75f0037/1119e67 环境互斥提交和项目边界不能被回退。"
    - "所有 task 改动原子提交到当前本地分支但不 push；origin/master 之前已有的两提交 ahead 状态与未跟踪 `.gsd/` 不被覆盖、暂存或清理。"
  artifacts:
    - path: "kcoderag-update.json"
      provides: "固定 raw GitHub URL 上的严格、无凭据、确定性环境版本文档"
    - path: "plugin-src/hooks/update_check.py"
      provides: "PreToolUse session dedup、bounded fetch、缓存/锁与固定模板更新提示"
    - path: "plugin-src/hooks/grep_nudge.py"
      provides: "现有结构检索 nudge 与可选更新 notice 的单一 advisory output 组合入口"
    - path: "scripts/generate_plugins.py"
      provides: "按环境两阶段渲染和 content-sensitive Codex cachebuster"
    - path: "scripts/manage_project_install.py"
      provides: "保留当前互斥环境且拒绝 drift/invalid 的项目级 update"
    - path: "scripts/update_plugin.py"
      provides: "Codex/Claude marketplace 更新编排和稳定失败结果"
    - path: "tests/test_update_check.py"
      provides: "session、并发、缓存、超时、schema 与 fail-open 行为回归"
    - path: "tests/test_plugin_update.py"
      provides: "宿主 updater argv、停止点与 credential-safe failure 回归"
    - path: "MCP_QA_EXPERIENCE_GUIDE.md"
      provides: "QA 默认安装的更新感知、命令、首次手动升级和新会话说明"
  key_links:
    - from: "scripts/generate_plugins.py"
      to: "kcoderag-update.json and QA/Dev manifests/hooks"
      via: "先以 base version 渲染规范化 package bytes 求 SHA-256 identity，再以完整有效版本重渲染 manifests 与 hook constant"
      pattern: "effective_version|kcoderag-update"
    - from: "plugin-src/hooks/grep_nudge.py"
      to: "plugin-src/hooks/update_check.py"
      via: "main 对已验证的 PreToolUse payload 请求一次 optional notice，再与现有 structural NUDGE 合并为 additionalContext"
      pattern: "maybe_update_notice|additionalContext"
    - from: "scripts/generate_plugins.py"
      to: "kcoderag-qa/hooks and kcoderag-dev/hooks"
      via: "update_check.py 进入自包含生成 inventory，grep_nudge.py 获得环境与完整当前版本模板值"
      pattern: "SHARED_FILES|plugin_version"
    - from: "scripts/manage_project_install.py"
      to: "<target>/.codex/kcoderag-nav/<env>/hooks"
      via: "update checker 加入 HOOK_PAYLOAD_FILENAMES、state originals/digests/status ownership 和 repeat install/update transaction"
      pattern: "HOOK_PAYLOAD_FILENAMES|update"
    - from: "scripts/update_plugin.py"
      to: "codex/claude plugin marketplaces"
      via: "Codex 执行 marketplace upgrade 后 add reinstall；Claude 执行 marketplace update 后 plugin update，任一步失败即停止"
      pattern: "marketplace|plugin"
---

<objective>
为 QA/Dev 在 Claude Code 与 Codex 中统一增加“首次相关 PreToolUse 才检查”的低打扰更新感知，并提供确定性版本身份和显式、可诊断的更新命令。

Purpose: 自定义 Git marketplace 不会因 master push 自动通知已有安装；当前时间戳 cachebuster 也没有随近期内容变化。该 quick 让安装后的 AI 在首次代码搜索工具事件上安全感知新版，同时把实际更新保留为用户确认后的显式动作。

Output: content-sensitive manifests、固定版本文档、自包含 update checker、项目/Codex/Claude 更新命令、QA/Dev 生成产物、完整回归和更新后的 README/QA 指南。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
@gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.agents/skills/kcoderag-nav/SKILL.md
@plugin-src/version.txt
@plugin-src/hooks/grep_nudge.py
@plugin-src/hooks/hooks.json
@plugin-src/hooks/test_grep_nudge.py
@plugin-src/README.md.tmpl
@scripts/generate_plugins.py
@scripts/manage_project_install.py
@tests/test_generation.py
@tests/test_routing_and_hooks.py
@tests/test_project_install.py
@tests/test_hook_runtime.py
@README.md
@MCP_QA_EXPERIENCE_GUIDE.md

<interfaces>
Preserve these existing seams and extend them rather than creating a second hook path:
- `scripts.generate_plugins.load_inputs(root: Path) -> CanonicalInputs`
- `scripts.generate_plugins.render_outputs(inputs: CanonicalInputs) -> dict[str, bytes]`
- `scripts.generate_plugins.compare_outputs(root, outputs) -> list[str]`
- `scripts.manage_project_install.install(target, source_root, environments) -> None`
- `scripts.manage_project_install.inspect_status(target, source_root) -> StatusResult`
- `plugin-src/hooks/grep_nudge.py: hook_output(data: Mapping[str, Any]) -> dict[str, Any] | None`
- Existing registration: one `hooks.PreToolUse` entry matching `^(Grep|Glob|Bash)$`, with a 5-second host timeout and 600-character additionalContext limit.

Create these explicit, test-injectable seams:
- `effective_version(inputs: CanonicalInputs, environment: str) -> str`: base SemVer plus `+codex.` and a fixed-length lowercase SHA-256 prefix derived from normalized provisional package bytes.
- `maybe_update_notice(data, environment, current_version, *, cache_root=None, now=None, opener=None) -> str | None`: never raises; production uses fixed constants, tests inject clock/cache/opener without overriding the trusted production URL.
- `update_project(target: Path, source_root: Path) -> str`: returns the active environment/result only after state and digest validation; it never accepts a new environment.
- `run_marketplace_update(host: str, environment: str, *, scope="project", runner=None) -> result`: exact argv orchestration with captured output and stable safe reason codes.

Verified host CLI contracts at planning time:
- Codex 0.146.1: `codex plugin marketplace upgrade kcoderag-nav` then `codex plugin add kcoderag-<env>@kcoderag-nav`; no native `plugin update` subcommand.
- Claude Code: `claude plugin marketplace update kcoderag-nav` then `claude plugin update kcoderag-<env>@kcoderag-nav --scope project`; restart/new session is required to apply.
</interfaces>
</context>

<source_coverage>

| Source | Item | Task | Status | Notes |
|---|---|---:|---|---|
| GOAL | 首次相关 PreToolUse 延迟更新检测 | 1 | COVERED | tracer 贯穿固定版本文档→生成包/项目安装→hook notice |
| GOAL | session dedup、无 session fallback、远端缓存 | 2 | COVERED | exclusive marker/refresh lock + 24h cache + bounded pruning |
| GOAL | 超时、网络/缓存/解析错误完全 fail-open | 2 | COVERED | 所有边界用 deterministic fake opener/clock/cache 测试，不访问真实网络 |
| GOAL | same version 静默/new version notice | 1, 2 | COVERED | 完整字符串相等比较；只渲染本地固定模板 |
| GOAL | QA/Dev、Claude/Codex host payload parity | 1 | COVERED | 两环境生成 hook × 两类代表 payload 行为级回归 |
| GOAL | content-sensitive deterministic cachebuster | 1 | COVERED | 两阶段 provisional render 避免 self-reference/time nondeterminism |
| GOAL | Codex marketplace 与项目级更新命令 | 3 | COVERED | Codex upgrade+add；项目 update 保留当前单环境 |
| GOAL | Claude Code 明确更新路径 | 3 | COVERED | 已验证的 marketplace update + plugin update project scope |
| GOAL | update command failure/result 不泄密 | 3 | COVERED | 捕获原始输出，仅返回 stable code/stage/environment |
| GOAL | README 与 MCP_QA_EXPERIENCE_GUIDE.md | 3 | COVERED | 包括旧版首次手动更新、新会话和不自动修改边界 |
| GOAL | generator/unittest/QA+Dev hook/diff 全验证 | 3 | COVERED | 无授权 push，因此不触发/宣称新 GitHub run |
| CONTEXT | LOCKED-01：禁止 SessionStart，四条宿主/环境路径全部复用 PreToolUse | 1, 2 | COVERED | hooks.json 继续只有 PreToolUse registration |
| CONTEXT | LOCKED-02：QA/Dev 互斥且各自独立 | 1, 3 | COVERED | 不恢复 both/routing/dedup 旧语义；updater 不切换环境 |
| CONTEXT | LOCKED-03：只 advisory/fail-open，不自动更新 | 1, 2, 3 | COVERED | notice only；实际 mutation 只能由用户显式 CLI 命令触发 |
| REQ | Roadmap 未给本 quick 分配 requirement IDs | — | N/A | frontmatter 使用 quick-specific contract IDs |
| RESEARCH | 明确不执行 research/plan-checker | — | N/A | 只用 Python 标准库和本机已验证 CLI surface，不新增依赖 |

本 quick 不增加 release ZIP/Tag/发布流水线，不建立后台 daemon，不轮询内部 QA/Dev MCP，不更改或输出 Bearer/endpoint，也不把现有互斥环境重新解释为可双装。
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED→GREEN 打通确定性版本文档到首次 PreToolUse 更新提示</name>
  <reversibility rating="costly">固定 raw URL、版本文档 schema 和 cachebuster 形状会被已安装 hook 与两个 marketplace 消费；后续修改需 schema/version 兼容，但不需要数据迁移。</reversibility>
  <files>plugin-src/version.txt, plugin-src/hooks/grep_nudge.py, plugin-src/hooks/update_check.py, plugin-src/hooks/test_grep_nudge.py, scripts/generate_plugins.py, scripts/manage_project_install.py, tests/test_generation.py, tests/test_routing_and_hooks.py, tests/test_project_install.py, tests/test_update_check.py, kcoderag-update.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/hooks/grep_nudge.py, kcoderag-qa/hooks/update_check.py, kcoderag-qa/hooks/test_grep_nudge.py, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json, kcoderag-dev/hooks/grep_nudge.py, kcoderag-dev/hooks/update_check.py, kcoderag-dev/hooks/test_grep_nudge.py</files>
  <behavior>
    - RED 1: 两次 render 相同规范源得到 byte-identical QA/Dev manifests 和 kcoderag-update.json；复制规范源后只改变共享可分发 hook byte 时两个环境完整版本都改变，只改变 QA MCP byte 时只有 QA 版本改变；当前固定时间戳版本合同必须失败。
    - GREEN 1: plugin-src/version.txt 只保存 base SemVer；每个环境先用 base version 渲染 provisional self-contained package bytes，按有序 path + length-prefixed bytes 求 SHA-256，再以 `<base>+codex.<固定长度小写十六进制>` 重渲染最终 manifest/hook。指纹不得读取时钟、git commit、absolute path 或环境变量。
    - RED 2: 将严格合法的“QA newer”文档作为 fake response，第一次 Claude `session_id` + Grep payload 和第一次 Codex `thread_id` + Bash payload都产生固定模板 notice；当前 hook 没有此路径，测试因缺 notice 失败。
    - GREEN 2: 同一入口仍输出 `hookSpecificOutput.hookEventName=PreToolUse`；结构 nudge 与 update notice 同时存在时按固定顺序合并且总长不超过 600，机械搜索只有 update notice 时也保持 advisory。
    - Characterization: 远端版本与 CURRENT_VERSION 完整字符串相等时不产生 notice；缺失/无效 tool_input 或不属于 Grep/Glob/Bash 的 payload 不发网络请求并静默。
    - Distribution: QA/Dev package 和 project-installed QA/Dev hook 都携带 update_check.py、环境 id 与各自 effective version；两个包仍可脱离 canonical parent 单独运行。
  </behavior>
  <action>
严格按一个 observable contract 一次 RED→GREEN：先只提交 deterministic identity 的失败测试，确认失败原因是旧 timestamp/version 输出而非 import/syntax；实现并生成后转绿再提交。然后只添加“合法 newer document 使首次 Claude/QA PreToolUse 出 notice”的失败测试，打通 fixed document→checker→grep_nudge main→JSON stdout 后转绿；再逐个加入 Codex/Dev parity 与 same-version silence，用例若直接通过则记录 characterization，不制造假 RED。每个真实 RED 必须在对应生产改动前有独立 `test(260820-w7c): ...` commit，GREEN 用后续 `feat(260820-w7c): ...` commit。

把 `plugin-src/version.txt` 从当前完整时间戳改为 base SemVer。重构 generator 为每环境两阶段渲染：provisional 包使用 base version，content identity 覆盖该环境最终会携带的所有自包含文件（包括选择的 MCP bytes、hook、skill、agent、README 与两个 host manifests），但不把最终自引用 cachebuster 再喂回 hash；最终版本用固定长度 SHA-256 prefix。生成根 `kcoderag-update.json`，exact schema 固定为 `schema_version: 1`、`repository: "Tooc0ld/kcoderag-nav"`、`channel: "master"`、`versions: {qa, dev}`，无时间、URL、commit、credential 或自由文本。`--check` 必须把该文件纳入 drift，测试不得保留 `\d{14}` timestamp 假设。

新增标准库 update_check.py，并在 grep_nudge.py 模板写入 `ENVIRONMENT`/`CURRENT_VERSION`，由 main 在 JSON boundary 调用；保持 `hook_output` 可纯函数测试，可通过显式 optional notice 合并，不让现有直接 helper 测试触发网络。生产 URL 只能是 `https://raw.githubusercontent.com/Tooc0ld/kcoderag-nav/master/kcoderag-update.json`：HTTPS scheme、host、owner/repo/branch/path 和 response final URL 都必须精确校验；timeout 不超过 1.5 秒，最多读取 8 KiB，JSON exact keys/type/version regex 不符即 None。远端值只参与 full-string equality，notice 使用本地固定模板拼接已验证 environment/version，并明确“ask user / do not update automatically”和文档化 updater 名称；绝不拼入远端 repository/channel/extra/error body。

`maybe_update_notice` 接受 test-only cache_root/clock/opener injection；生产 URL不可由参数或环境变量替换。允许 `KCODERAG_NAV_UPDATE_CHECK=0` 完全禁用检查，使离线 regression 明确 opt out；不得提供任意 URL override。更新 canonical hook regression/subprocess helpers 显式禁用网络，专项 tests 才使用 fake opener，整个 required suite 不访问 GitHub。

把 update_check.py 纳入 generator self-contained inventory，也加入 project installer `HOOK_PAYLOAD_FILENAMES`、`_private_payloads`、original/digest/status ownership。已有 state 的 same-environment repeat install/update要安全捕获新文件 original；同名未受管文件必须拒绝。不要修改 hooks.json event map：QA/Dev、plugin/project 全部继续只有现有 PreToolUse matcher，不创建 SessionStart registration。运行 generator 写出两包，禁止手改 generated files；保留 75f0037/1119e67 的 single-environment state 和 cross-environment conflict。
  </action>
  <verify>
    <automated>python scripts/generate_plugins.py --check &amp;&amp; python -m unittest tests.test_generation tests.test_update_check tests.test_routing_and_hooks tests.test_project_install -v</automated>
  </verify>
  <done>同一规范源产生稳定、content-sensitive QA/Dev 完整版本与固定版本文档；两环境、两宿主和项目安装的首次相关 PreToolUse 能对 fake newer 文档输出本地固定 advisory notice，同版本或无效 payload 静默，生成/ownership 无 drift。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 逐条锁定 session 去重、并发缓存和所有失败静默</name>
  <files>plugin-src/hooks/update_check.py, plugin-src/hooks/grep_nudge.py, plugin-src/hooks/test_grep_nudge.py, tests/test_update_check.py, tests/test_routing_and_hooks.py, scripts/generate_plugins.py, kcoderag-qa/hooks/grep_nudge.py, kcoderag-qa/hooks/update_check.py, kcoderag-qa/hooks/test_grep_nudge.py, kcoderag-dev/hooks/grep_nudge.py, kcoderag-dev/hooks/update_check.py, kcoderag-dev/hooks/test_grep_nudge.py</files>
  <behavior>
    - Session once: 同一个明确 session id 的并发 first PreToolUse 最多一个 caller获得 notice，后续事件不重复 notice/fetch；不同新 session 可各自消费同一 cached available update 一次。
    - Missing session: 无 session id 时使用 environment + normalized cwd + 固定一小时时间桶的 hash identity；同桶只消费一次，下一时间桶允许重新消费，任何 raw cwd/session id 不写入文件名或缓存 JSON。
    - Fresh cache: 24 小时内复用严格验证过的 cached versions，新的 session 可通知但不访问网络；same-version cache 对新 session继续静默。
    - Stale cache: 第一个成功取得 exclusive refresh lock 的 caller 最多刷新一次；lock loser 或 refresh error 可使用旧的 validated cached available version，仍只对其新 session 通知一次。
    - Failure: timeout、DNS/OSError、HTTP status、redirect mismatch、oversize、malformed JSON、wrong schema/version、unwritable cache、stale/corrupt lock/cache、atomic replace error全部 exit 0/None，不污染 hook stdout/stderr，不在同一 session 重试。
    - Bounded state: session markers只用 hash key，best-effort prune 后有固定上限；lock 有短 stale TTL，cache 写入 temp+fsync+replace，任何清理失败不影响原工具。
  </behavior>
  <action>
继续一次只引入一个失败行为。顺序固定为：同 session sequential dedup → 同 session ThreadPoolExecutor concurrency → fresh cache 跨 session → stale refresh lock → timeout/network failure consumed once → missing-session fallback bucket → corrupt/unwritable/oversize fail-open。每一步先加一个最小 observable test并运行到因该缺失能力 RED，再实现到 GREEN后提交；前一步未 GREEN 不得堆下一组失败。若某后续用例被已有通用实现自然满足，记录为 GREEN characterization，不为追求 commit 数量破坏实现。

session identity 只接受有界字符串/整数形式的顶层 `session_id`、`thread_id` 或 `conversation_id`，先截断/规范化再 SHA-256；不信任对象/list 或把原值用于路径。无 id fallback 使用环境、规范化 cwd 和一小时 epoch bucket 的 hash，避免每次工具调用访问网络，同时明确它是 throttle 而非可靠会话标识。对 valid Grep/Glob/Bash + Mapping tool_input，先以 exclusive-create 写入 session-consumed marker，再读取/刷新 remote cache：这样首次事件无论成功、超时或错误都已消费，后续同 session 不重试。marker/cache root使用标准库的用户 cache 位置并允许 test override；目录或权限不可用时完全静默。

remote result cache 只保存 schema_version、checked_at 和 qa/dev validated version strings，TTL 固定 24 小时。刷新用 O_CREAT|O_EXCL lock 和 bounded stale-lock recovery；lock loser不等待网络，可读取旧的 strict-valid cache。写入用同目录 temporary file、flush/fsync、os.replace；读取有 byte cap。保留 stale available cache作为 refresh failure 的安全 fallback，因为输出只是本地固定提示。session marker best-effort按mtime裁剪到固定上限（例如128），所有路径分量来自常量或 SHA-256，不允许 remote/session/cwd 原文。

main boundary继续捕获所有 BaseException 之外的普通异常、返回 0且只在有 advisory output 时写一个 JSON object；不要把 update failure附加到 structural NUDGE。专项测试捕获 stdout/stderr，并扫描 timeout body、fake credential、remote arbitrary text、URL 和 raw session id 都未出现。每次 canonical 修改后 generator `--write` 同步两包；两份 generated regression显式 offline，现有 parser/nudge行为不得因更新检查变慢或改变分类。
  </action>
  <verify>
    <automated>python -m unittest tests.test_update_check tests.test_routing_and_hooks -v &amp;&amp; python kcoderag-qa/hooks/test_grep_nudge.py &amp;&amp; python kcoderag-dev/hooks/test_grep_nudge.py &amp;&amp; python scripts/generate_plugins.py --check</automated>
  </verify>
  <done>明确 session、并发 caller、缓存新旧状态和无 session fallback 都满足一次消费/有界访问；所有网络、schema、锁和文件异常保持 silent fail-open，专项与两包离线回归全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 提供显式 project/marketplace updater、更新文档并完成本地交付</name>
  <files>scripts/manage_project_install.py, scripts/update_plugin.py, tests/test_project_install.py, tests/test_plugin_update.py, plugin-src/README.md.tmpl, tests/test_generation.py, README.md, MCP_QA_EXPERIENCE_GUIDE.md, kcoderag-qa/README.md, kcoderag-dev/README.md</files>
  <behavior>
    - Project healthy/update_available: `update --target PATH` 从 install-state 取得唯一 active environment并用当前 checkout source重渲染；不接受 `--environment`、不切换 QA/Dev、成功后 status=healthy且 unrelated bytes不变。
    - Project refusal: not_installed、drifted、invalid、双环境 legacy/conflict、symlink/path escape均在任何写入前稳定失败；输出只含 reason code/relative path，不含 state bytes、digest、MCP 或 credential。
    - Codex marketplace: updater严格依次运行 `codex plugin marketplace upgrade kcoderag-nav --json` 与 `codex plugin add kcoderag-<env>@kcoderag-nav --json`；full cachebuster 未变化时报告 `already_current` 或明确 reinstall result，不宣称 undocumented overwrite。
    - Claude marketplace: updater严格依次运行 `claude plugin marketplace update kcoderag-nav` 与 `claude plugin update kcoderag-<env>@kcoderag-nav --scope project`；成功提示重启/新 session，scope可显式选择但文档默认 project。
    - Failure: CLI missing、timeout、marketplace refresh nonzero、plugin step nonzero分别有稳定 stage/reason/exit code；第一失败后不执行下一步，捕获的 stdout/stderr不透传到用户或 hook context。
    - Notice boundary: update checker只命名上述文档化命令并要求用户确认；测试证明调用 hook不会触发 subprocess、installer或文件 mutation。
    - Documentation: root/package README 与 QA guide解释 master push不会主动替换旧 cache、首次相关 PreToolUse 检查、24h cache/once-per-session、旧版需要先手动更新一次、项目/Codex/Claude精确命令和新 session requirement。
  </behavior>
  <action>
先为项目 update 的“保持当前 QA、应用 current source update并回到 healthy”写一个失败测试，确认失败于缺 subcommand；实现后转 GREEN再加入 drift/not-installed无写入测试。`manage_project_install.py update --target PATH` 不提供 environment参数：调用 `_safe_target`/`_load_state`/`_verify_digests`，要求 exactly-one active environment，再复用 `_desired_install` 与事务写入；source与安装一致时输出 `already current: <env>`，bytes不同才输出 `updated: <env>`。不要把远程 fetch塞进项目 installer；文档要求先对 kcoderag-nav checkout执行 `git pull --ff-only`，这样不会让 updater擅自修改用户 Git 工作树。

再为 scripts/update_plugin.py 逐宿主写 fake executable/runner RED→GREEN。CLI contract为 `--host codex|claude --environment qa|dev`，Claude接受 `--scope project|local|user`且默认project；不得提供 both。Codex按已验证合同先 `marketplace upgrade ... --json` 再 `plugin add ... --json`，依赖 Task 1 的 full content cachebuster触发重装识别；Claude先 marketplace update再原生 plugin update。使用 `shutil.which` 或注入 executable/runner，设置短但足够的 subprocess timeout，stdin DEVNULL，capture stdout/stderr；成功只打印 host/environment/result/new-session提示，失败只打印 `update failed (<stable_reason>)` 和 stage，不回显捕获内容、环境变量或命令中的潜在配置。任一步失败即停止；hook本身永不调用此脚本。

更新 plugin-src/README.md.tmpl 后由 generator同步 QA/Dev README，根 README 与 MCP_QA_EXPERIENCE_GUIDE.md 增加“更新感知与应用”章节。精确说明：旧安装不含 checker，必须先手动刷新一次；新安装在每个 session 第一次 Grep/Glob/Bash payload才检查；24h remote cache不等于每个session只提示一次；检查失败不会阻断工具；提示不自动更新。项目命令写为 `git pull --ff-only` 后 `python scripts/manage_project_install.py update --target PATH`；Codex/Claude写为 `python scripts/update_plugin.py --host ... --environment ...` 并列出脚本内部原生命令；两者应用后开启新 thread/session。继续说明QA默认、QA/Dev互斥，切环境仍须卸载后安装。文档与测试不得出现 Authorization、Bearer值或内部 endpoint。

完成后按顺序运行全套 generator、unittest、QA/Dev hook 与 diff gates。只显式 stage本计划 source/generated/test/docs文件；不得 `git add -A`，不得暂存/删除 `.gsd/` 或修改现有两个 ahead commits。每个 RED/GREEN切片按 quick TDD 原子提交，最后可有独立 docs/test commit；创建 SUMMARY前确认 `git status --short --branch` 只剩预期 planning/runtime状态。本 turn没有 push授权：绝不运行 git push，也不监控/宣称新的 GitHub Actions run；SUMMARY记录 `push: NOT_RUN (not authorized)` 并把最终本地 commit交给 orchestrator/user决定。
  </action>
  <verify>
    <automated>python scripts/generate_plugins.py --check &amp;&amp; python -m unittest discover -s tests -p "test_*.py" -v &amp;&amp; python kcoderag-qa/hooks/test_grep_nudge.py &amp;&amp; python kcoderag-dev/hooks/test_grep_nudge.py &amp;&amp; git diff --check 75f0037..HEAD &amp;&amp; git status --short --branch</automated>
  </verify>
  <done>三种安装路径均有显式、失败可诊断且 credential-safe 的更新步骤；文档说明检测/缓存/首次手动升级/新会话边界；所有本地 gates通过并原子提交，未 push且未触碰现有ahead commits或`.gsd/`。</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| Host PreToolUse payload → session/cache identity | payload、session id、cwd均不可信，可能超长、异型或试图构造路径；只能规范化后hash并有界存储。 |
| GitHub raw response → AI additionalContext | remote内容可能损坏、被替换或含prompt injection；只能接受固定URL上exact schema/version，并用本地模板输出。 |
| Concurrent hook processes → user cache | 多个首次tool call可能竞争marker/cache/lock；必须atomic、bounded且任何失败都放行。 |
| User-confirmed updater → host CLI/project files | updater会修改插件缓存或受管项目文件；必须保持环境互斥、已有ownership/path guards并在失败时停止。 |
| Generator → public manifest cache key | cachebuster控制宿主是否看到新bytes；遗漏内容或引入时间会造成旧缓存/不可重复构建。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-W7C-01 | Spoofing | remote version document | high | mitigate | 固定HTTPS raw URL并校验final scheme/host/path；exact repository/channel/schema与version regex；不接受env URL override。 |
| T-W7C-02 | Tampering | remote JSON → notice | high | mitigate | 8KiB cap、exact keys/type、full version equality；additionalContext只由本地常量模板和validated版本组成，不回显remote自由文本。 |
| T-W7C-03 | Denial of Service | first PreToolUse network/cache | medium | mitigate | 1.5s内timeout、24h cache、session-consumed marker、exclusive refresh lock、无锁等待和所有异常silent fail-open。 |
| T-W7C-04 | Tampering | concurrent cache files | medium | mitigate | hash-only filenames、O_EXCL marker/lock、temp+fsync+replace、strict cache schema、stale-lock TTL与固定marker上限。 |
| T-W7C-05 | Information Disclosure | hook/updater diagnostics | high | mitigate | 不打印response/error/subprocess stdout/stderr、auth/header/endpoint/session/cwd；测试用synthetic secrets扫描stdout/stderr/JSON。 |
| T-W7C-06 | Elevation of Privilege | automatic update | high | mitigate | hook只返回advisory notice；没有subprocess/install调用；实际更新只在用户显式运行CLI后发生且保留project path/ownership guards。 |
| T-W7C-07 | Tampering | deterministic cachebuster | medium | mitigate | 两阶段normalized provisional render、length-prefixed ordered bytes、SHA-256固定prefix；tests覆盖repeatability和单环境/共享内容敏感性。 |
| T-W7C-08 | Tampering | QA/Dev environment state | high | mitigate | project update从已验证state读取唯一active环境且不接受environment；marketplace updater只接受qa/dev，文档继续要求互斥。 |
| T-W7C-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划不新增或执行package-manager install，只使用Python标准库和已安装host CLI，因此package legitimacy gate不适用。 |
</threat_model>

<verification>
1. TDD history显示每个真实新行为先有因目标行为缺失而失败的RED，再有对应GREEN；不会用network/import/syntax偶发失败冒充RED，也不会同时堆多个未实现合同。
2. `python scripts/generate_plugins.py --check` 返回0；重复render byte-identical，有效shared/QA-only内容改变按合同改变完整版本，kcoderag-update.json与两个manifest一致。
3. update checker全部使用fake opener/clock/cache；required tests不访问GitHub。Claude/Codex、QA/Dev、明确session/无session、fresh/stale/concurrent/failure矩阵全绿。
4. hooks.json继续只有PreToolUse；first tool notice只advisory，same session不重复，same version与所有错误静默，现有structural/local parser回归不变。
5. project updater保持当前单环境且拒绝drift/invalid；Codex/Claude fake CLI验证exact argv、失败停止与safe output，hook测试证明不会自动调用更新。
6. README、generated package README和MCP_QA_EXPERIENCE_GUIDE.md给出准确命令、首次手动升级和新会话说明，不包含canonical MCP URL/header/credential。
7. 全套unittest、两个generated hook回归和`git diff --check 75f0037..HEAD`通过；变更已本地原子提交，origin未push、`.gsd/`未staged或删除。
</verification>

<success_criteria>
- master出现content-changing提交后，已具备checker的QA/Dev插件可在Claude Code/Codex第一次相关PreToolUse上低频感知新完整版本。
- 无论session id是否存在、并发多少、GitHub或cache是否失败，hook都不会阻断、重复轰炸或泄露远端/认证内容。
- Codex cachebuster确定、可重复且真正随可分发内容变化，不再依赖人工时间戳。
- 项目级、Codex marketplace和Claude marketplace均有明确更新命令、稳定失败语义和新会话提示；更新永不由hook自动执行。
- QA/Dev仍互斥且独立自包含，75f0037当前行为和所有既有测试保持。
- 文档与本地验证完成，原子提交保留在当前分支并等待用户决定push。
</success_criteria>

<output>
完成后创建 `.planning/quick/260820-w7c-add-lazy-first-pretooluse-update-detecti/260820-w7c-SUMMARY.md`。SUMMARY必须记录每个RED/GREEN证据、effective QA/Dev版本、cache/timeout/concurrency测试、完整本地gate、commits，以及`push: NOT_RUN (not authorized)`；不得包含remote response body、host auth、Bearer、Authorization header、内部endpoint、raw session/cwd或完整敏感日志。
</output>

---
quick_id: 260831-nb1
phase: quick-260831-nb1
plan: "01"
type: execute
status: planned
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - docs/MCP_QA_EXPERIENCE_GUIDE.md
  - README.md
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - tests/maintainer/ci-contract.test.cts
  - tests/maintainer/release-workflow.test.cts
  - .planning/quick/260831-nb1-update-the-local-authoritative-mcp-qa-gu/260831-nb1-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [BRAND-04, TEST-10]
must_haves:
  truths:
    - "The authoritative local guide distinguishes Phase 04.2 readiness-only work from the later separately authorized v0.3.0 publication."
    - "Ordinary CI runs for branch pushes and pull requests but does not duplicate itself for release tag pushes."
    - "The Release workflow keeps the complete Ubuntu/Windows and Node 22/24 required matrix while its publish job avoids repeating gates already proven by that matrix."
    - "Release publication remains tag-bound, dependency-gated, immutable-action-pinned, and limited to one npm publish step."
  artifacts:
    - path: "docs/MCP_QA_EXPERIENCE_GUIDE.md"
      provides: "Current authoritative v0.3.0 publication status without rewriting the Phase 04.2 boundary"
    - path: ".github/workflows/ci.yml"
      provides: "Branch-only push trigger plus pull-request and manual CI"
    - path: ".github/workflows/release.yml"
      provides: "Four-lane required verification followed by a lean publish job"
    - path: "tests/maintainer/release-workflow.test.cts"
      provides: "Static proof that full gates remain in required-contracts and are not duplicated in publish"
---

<objective>
Bring the local authoritative documentation up to date after the separately authorized `v0.3.0` release, then remove safe CI duplication without weakening the release evidence boundary.

Output: synchronized guide/README release status, a tag-excluding ordinary CI trigger, a lean publish job behind the unchanged four-platform release matrix, and updated workflow contract tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update the authoritative local release guidance</name>
  <files>docs/MCP_QA_EXPERIENCE_GUIDE.md, README.md</files>
  <action>Preserve the historical fact that Phase 04.2 itself was readiness-only. Add the later, separately authorized result: annotated tag `v0.3.0` was released, npm exact `0.3.0` is public, and `latest` points to `0.3.0`. Keep Phase 05-08 deferrals and forward-fix policy explicit.</action>
  <verify>`npm run guide:check`; `npm run docs:check`</verify>
  <done>The local authority and public overview describe the current release state consistently without claiming deferred phases are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate hosted verification while preserving release gates</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, tests/maintainer/ci-contract.test.cts, tests/maintainer/release-workflow.test.cts</files>
  <action>Limit the CI push trigger to branch refs so a release tag does not start both CI and Release. Keep the full Release `required-contracts` matrix unchanged. In the dependent publish job, retain checkout/setup, clean install, tag-version binding, build, dependency audit, pack audit, and the single publish step; remove only the complete tests, generation, docs, retirement, and packaged smoke gates already run by every required lane. Update static contracts to assert both sides of this boundary.</action>
  <verify>`npm run build`; `npm run test:ci-contract`; `npm run test:release-workflow`; `npm test`; `npm run pack:audit`; `git diff --check`</verify>
  <done>Tag pushes avoid duplicate ordinary CI, all four required release lanes remain, publish waits for them, and the publish job repeats only package-finalization checks.</done>
</task>

</tasks>

<scope_boundary>
No workflow dispatch, Git push, new tag, npm publication, matrix reduction, Action-pin change, credential-scope change, or product-runtime change is authorized by this quick task. Preserve all unrelated dirty and untracked work.
</scope_boundary>

<success_criteria>
- Guide and README accurately report the completed `0.3.0` release and retain the Phase 04.2 readiness-only history.
- CI workflow contract proves branch pushes do not include tag refs.
- Release workflow contract proves full validation remains in `required-contracts`, publish depends on it, and duplicate heavy gates are absent from publish.
- Fresh relevant and full local test evidence passes before completion.
</success_criteria>

---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin-src/version.txt
  - kcoderag-update.json
  - kcoderag-qa/
  - kcoderag-dev/
  - kcoderag-cursor/
  - .agents/plugins/marketplace.json
  - .claude-plugin/marketplace.json
  - .cursor-plugin/marketplace.json
  - .planning/STATE.md
autonomous: true
requirements:
  - RELEASE-VERSION-01
estimate:
  tokens: 3500
  raw_tokens: 3500
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "基础 SemVer 从 0.1.2 显式升级到 0.1.3。"
    - "QA、Dev 与 Cursor 自包含生成包都使用 0.1.3 加各自确定性内容哈希。"
    - "完整本地门禁通过后，kcoderag-nav 的 origin/master 与本地 HEAD 一致。"
    - "不得把 KCodeRag 仓库领先远端的 673 个无关本地提交混入本次推送。"
  artifacts:
    - path: "plugin-src/version.txt"
      provides: "0.1.3 规范基础版本"
    - path: "kcoderag-update.json"
      provides: "远端更新检查使用的新 QA/Dev 版本"
  key_links:
    - from: "plugin-src/version.txt"
      to: "kcoderag-qa/.codex-plugin/plugin.json"
      via: "scripts/generate_plugins.py --write"
      pattern: "0.1.3\\+codex"
    - from: "plugin-src/version.txt"
      to: "kcoderag-cursor/.cursor-plugin/plugin.json"
      via: "scripts/generate_plugins.py --write"
      pattern: "0.1.3\\+cursor"
---

<objective>
把 kcoderag-nav 的基础版本升级到 0.1.3，确定性刷新 QA、Dev、Cursor 全部发布产物，完成验证和 GSD 记录后把本仓库累计 24 个本地提交及本次版本提交推送到 origin/master。KCodeRag 是独立且严重领先远端的脏仓库，本 quick 不提交或推送它。
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@plugin-src/version.txt
@scripts/generate_plugins.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级基础版本并刷新三套生成包</name>
  <files>plugin-src/version.txt, kcoderag-update.json, kcoderag-qa/, kcoderag-dev/, kcoderag-cursor/, .agents/plugins/marketplace.json, .claude-plugin/marketplace.json, .cursor-plugin/marketplace.json</files>
  <action>把规范基础版本从 0.1.2 修改为 0.1.3，运行生成器；验证 QA/Dev/Cursor manifest 与更新元数据全部以 0.1.3 开头，内容哈希仍各自确定且生成器 check 无漂移。提交规范源与全部生成产物。</action>
  <verify>python scripts/generate_plugins.py --write &amp;&amp; python scripts/generate_plugins.py --check</verify>
  <done>三套插件和 marketplace/update 元数据均使用 0.1.3 基础版本，无遗漏或手工版本漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 全量验证、记录并推送 kcoderag-nav</name>
  <files>.planning/quick/260821-g07-bump-plugin-base-version-to-0-1-3-regene/260821-g07-SUMMARY.md, .planning/STATE.md</files>
  <action>运行生成、全量 unittest、QA/Dev hook、pre-commit 和 diff 门禁；写 SUMMARY、更新 STATE、提交 GSD 记录。普通 push 到 origin/master，随后确认 origin/master 与本地 HEAD 完全相同，并观察该 push 触发的 required GitHub Actions 结果。不得暂存、提交或推送 KCodeRag 的 673 个本地提交和脏工作树。</action>
  <verify>python -m unittest discover -s tests -p "test_*.py" -q &amp;&amp; python kcoderag-qa/hooks/test_grep_nudge.py &amp;&amp; python kcoderag-dev/hooks/test_grep_nudge.py &amp;&amp; git hook run pre-commit &amp;&amp; git diff --check &amp;&amp; git push origin master</verify>
  <done>本地门禁全绿，kcoderag-nav 工作树干净，origin/master 等于 HEAD；KCodeRag 保持原状。</done>
</task>

</tasks>

---
quick_id: 260830-t09
status: complete
mode: quick-full
must_haves:
  truths:
    - Generated Windows hook registrations cannot invoke PowerShell or an interactive/new-window cmd path.
    - Codex and Claude Windows hooks exit through cmd /c, while their internal child process is hidden and bounded by a timeout.
    - Canonical launcher scripts remain non-interactive and generated artifacts remain synchronized.
  artifacts:
    - tests/hooks/launcher.test.cts
  key_links:
    - tests/hooks/launcher.test.cts -> src/core/project-root.cts
    - tests/hooks/launcher.test.cts -> plugin-src/hooks/run_hook.cmd
    - tests/hooks/launcher.test.cts -> kcoderag-qa/hooks/hooks.json
---

# Quick Task 260830-t09: Guard generated Windows hooks against popup windows

## Task 1: Add and verify a no-popup regression contract

**Files:**
- `tests/hooks/launcher.test.cts`

**Action:** Add assertions covering the canonical launcher templates, rendered Codex/Claude commands, and generated hook registrations. Reject PowerShell, `cmd /k`, `start`, prompt commands, `%CD%`, `$repo`, and `Join-Path`; require synchronous five-second hooks, `cmd /c`, and a decoded bootstrap that uses `windowsHide: true` plus a finite timeout.

**Verify:**
- `npm run build`
- `npm run test:launcher`
- `npm run generate:check`
- Run the Codex, Claude, Cursor, OpenCode, and ZCode adapter test files.

**Done:** A regression that reintroduces an interactive shell, a popup-capable Windows command, an asynchronous hook, or an unbounded/visible bootstrap process fails the test suite before packaging.

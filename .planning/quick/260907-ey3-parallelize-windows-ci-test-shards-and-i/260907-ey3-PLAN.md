---
status: complete
quick_id: 260907-ey3
description: Parallelize Windows CI test shards and isolated packaged host groups
---

# Plan

User approved two-way parallelism with complete platform and host coverage. Execute inline under the GSD quick workflow. Preserve unrelated 260904-mh0 release artifacts.

1. Split each Windows Node 22/24 ordinary CI and release lane into two independent jobs, retaining serial execution inside each shard and full Linux lanes. Prove disjoint, exhaustive test selection and failure propagation.
2. Run real packaged smoke in two isolated child processes, each with private temporary directories, npm cache, environment and MCP stub receipts. Preserve the original exact artifact, all five host results, integrity checks, failure semantics and aggregate receipt. Keep dependency-injected tests deterministic.
3. Run build, focused and full tests, generation/docs/package gates. Commit and push the verified changes, inspect actual hosted CI and acceptance durations, then record evidence in the quick summary and STATE. Do not create a release tag or publish a version.

## Acceptance

- No ordinary test disappears from any existing OS/Node combination.
- Windows shards execute concurrently on separate hosted runners.
- Five-host packaged output remains one complete, secret-free aggregate for one exact package identity.
- Worker failures, missing/duplicate hosts, tampered artifacts and nonzero exits cannot yield PASS.
- Hosted results determine the speedup claim; 5–6 minutes is a target, not a promise.

Completed in 2325bf4 and 1b7c394. Hosted evidence selected two independent acceptance runners after same-runner worker parallelism gave negligible hosted improvement. Final ordinary CI: 347 s; acceptance: 481 s. Both PASS. The acceptance 5–6 minute target was not reached; the three-host group remains the critical path.

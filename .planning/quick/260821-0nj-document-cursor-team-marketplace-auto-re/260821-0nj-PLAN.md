---
quick_id: 260821-0nj
status: complete
description: Document Cursor Team Marketplace Auto Refresh updates, manual refresh fallback, and local plugin sync behavior
---

# Quick Task 260821-0nj

## Goal

Make the Cursor update path explicit: Team Marketplace installations update through Cursor's
Auto Refresh (or an administrator's manual Refresh), while local development installations must
be resynced or linked and followed by a window reload.

## Tasks

1. Add regression assertions for the Cursor update contract in the root and generated package docs.
2. Update the root README and Cursor canonical README template, then regenerate the package.
3. Run generation and full test verification, commit the documentation change, and record completion.

## Verification

```text
python -B scripts/generate_plugins.py --write
python -B scripts/generate_plugins.py --check
python -B -m unittest tests.test_generation -v
python -B -m unittest discover -s tests -p "test_*.py" -v
git diff --check
```

---
quick_id: 260831-s98
phase: quick-260831-s98
plan: "01"
type: execute
status: planned
mode: quick
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/acceptance.yml
  - tests/maintainer/ci-contract.test.cts
  - .planning/quick/260831-s98-automate-self-hosted-live-acceptance-and/260831-s98-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [TEST-10]
must_haves:
  truths:
    - "Candidate branch pushes and explicit manual dispatches can run acceptance only on the dedicated kcoderag-nav self-hosted Windows runner."
    - "Each acceptance run creates a unique temporary root below R:\\actions-runner\\kcoderag-nav\\_temp, exports TEMP/TMP only for that job, and removes the root even after failures."
    - "The required five-host packaged lifecycle runs before optional real-host smoke; Cursor and ZCode headless gaps remain honest NOT_RUN evidence."
    - "Ordinary CI no longer embeds the self-hosted acceptance job or spends hosted lanes merely to request live acceptance."
    - "Documentation-only commits cannot cancel or replace a different product commit's full CI receipt."
---

<objective>
Replace the old manually maintained acceptance projects with a repeatable, isolated self-hosted GitHub Actions acceptance workflow and remove the obsolete local projects.

Output: a dedicated candidate/manual acceptance workflow, static workflow contracts, an R-drive job-temporary lifecycle, and verified cleanup of the legacy acceptance directories.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract and harden self-hosted acceptance</name>
  <files>.github/workflows/ci.yml, .github/workflows/acceptance.yml, tests/maintainer/ci-contract.test.cts</files>
  <action>Create a minimal read-only workflow triggered by readiness candidate pushes or workflow_dispatch. Pin it to self-hosted/Windows/X64/kcoderag-live, prepare a validated per-run R-drive TEMP/TMP directory, build the repository, run dependency audit, then run required five-host packaged smoke followed by optional real-host smoke. Always clean the bounded per-run directory. Remove the old manual-only job from ordinary CI, bind ordinary CI concurrency to the exact commit SHA so a later docs-only commit cannot cancel prior product evidence, and update static contracts for triggers, pins, isolation, ordering, and non-publication.</action>
  <verify>`npm run build`; `npm run test:ci-contract`; action-pin audit; `git diff --check`</verify>
  <done>Acceptance is event-driven, isolated on the named runner class, independently testable, and ordinary CI retains only its documentation/full-contract jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy projects and exercise the automated path</name>
  <files>D:\AIProgram\*-acceptance*</files>
  <action>Resolve and validate the exact seven direct-child acceptance directories under D:\AIProgram, reject reparse points or any path outside that root, then delete only those directories. Create the R-drive acceptance temp root, verify no old directory remains, run focused/local smoke evidence, and dispatch the dedicated workflow when the committed workflow is remotely available.</action>
  <verify>Exact-path absence check; R-drive temp-root check; `npm run smoke:required`; `npm run smoke:live` or remote workflow evidence</verify>
  <done>Manual acceptance state is gone, new runs use disposable projects, and PASS/FAIL/NOT_RUN remains explicit per host.</done>
</task>

</tasks>

<scope_boundary>
Do not weaken required CI/readiness/release gates, expose credentials, enable publication, claim Cursor/ZCode headless live support, delete unrelated D:\AIProgram projects, or modify unrelated dirty/untracked work.
</scope_boundary>

<success_criteria>
- Dedicated acceptance triggers only on readiness candidate pushes or explicit dispatch and uses the kcoderag-live self-hosted Windows runner.
- Job-local TEMP/TMP lives beneath the approved R-drive runner directory and is always cleaned.
- Required packaged smoke runs before optional live smoke, with no PASS promotion of NOT_RUN.
- All seven enumerated legacy acceptance directories are absent and no other D:\AIProgram directory is removed.
- Workflow contracts, build, action pins, smoke evidence, and diff hygiene pass.
</success_criteria>

---
quick_id: 260821-0r6
status: complete
description: Synchronize Cursor update guidance into MCP_QA_EXPERIENCE_GUIDE.md and enforce the guide as a required release documentation surface
---

# Quick Task 260821-0r6

## Goal

Bring the Cursor marketplace and local update instructions into
`MCP_QA_EXPERIENCE_GUIDE.md`, then record a durable project rule that operational changes must
update the guide in the same change.

## Tasks

1. Add regression assertions requiring the Cursor update contract and synchronization policy in
   the experience guide.
2. Update the experience guide and the project instruction source, then synchronize `AGENTS.md`.
3. Run the targeted and complete test suites, commit the documentation change, and record task
   completion in GSD state.

## Verification

```text
python -B -m unittest tests.test_generation.GenerationTests.test_manifest_and_install_documentation_contracts -v
python -B scripts/generate_plugins.py --check
python -B -m unittest discover -s tests -p "test_*.py" -v
git diff --check
```

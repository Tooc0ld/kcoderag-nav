---
quick_id: 260902-kn6
status: complete
---

# Replace the Head project legacy Codex installation

## Task 1: Establish a bounded removal plan

- Confirm the exact legacy state, managed paths, and drift without exposing MCP configuration values.
- Create a recoverable backup containing only the legacy state and files that will be changed.

## Task 2: Remove the legacy contribution and install the current release

- Prefer the `0.2.2` uninstaller when its integrity gate permits it.
- If drift prevents uninstall, remove only the legacy kcoderag-nav-owned files and structured Codex configuration entries while preserving unrelated project configuration.
- Install the latest `kcoderag-navigation` capability for Codex into `I:\JX3_SVN\Head`.

## Task 3: Verify

- Run current `status --json` and confirm the installation is healthy, version-current, and scoped to the Head project.
- Confirm unrelated Codex configuration remains present and report the backup location.

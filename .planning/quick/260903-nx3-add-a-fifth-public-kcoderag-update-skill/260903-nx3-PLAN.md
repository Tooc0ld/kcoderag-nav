---
quick_id: 260903-nx3
phase: quick-260903-nx3
plan: "01"
type: execute
status: complete
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - plugin-src/skills/kcoderag-update/SKILL.md
  - plugin-src/skills/kcoderag-update/agents/openai.yaml
  - plugin-src/skills/kcoderag-manage/SKILL.md
  - src/capabilities/navigation.cts
  - src/generator/index.cts
  - src/hosts/codex.cts
  - src/hosts/claude.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - src/hosts/zcode.cts
  - src/hosts/user-sources.cts
  - src/maintainer/pack-audit.cts
  - tests/skills/public-skills.test.cts
  - tests/hosts/public-skill-source-gate.test.cts
  - package.json
  - package-lock.json
  - plugin-src/README.md.tmpl
  - docs/MCP_QA_EXPERIENCE_GUIDE.md
  - .planning/PROJECT.md
  - generated QA and Cursor product trees
autonomous: true
requirements: []
must_haves:
  truths:
    - "The public interface contains exactly five Skills: $kcoderag, $kcoderag-manage, $kcoderag-update, $kcoderag-feedback, and $kcoderag-code-style."
    - "$kcoderag-update performs only an explicitly requested, single-host project update through npx kcoderag-nav@latest and never bypasses lifecycle refusals or exposes secrets."
    - "$kcoderag-manage remains diagnostic-first and routes update requests to $kcoderag-update instead of duplicating mutation instructions."
    - "Fresh install, update, selective uninstall, and package smoke converge on the fifth Skill for Codex, Claude Code, Cursor, OpenCode, and ZCode."
    - "The immutable published 0.3.3 package is not overwritten; release dry-run verifies 0.3.4 as the next candidate without committing, tagging, or publishing."
  artifacts:
    - path: plugin-src/skills/kcoderag-update/SKILL.md
      provides: Canonical concise update-only Skill contract
    - path: plugin-src/skills/kcoderag-update/agents/openai.yaml
      provides: Codex discovery metadata and explicit update prompt
    - path: src/capabilities/navigation.cts
      provides: Navigation capability ownership of the new public contributor
    - path: src/generator/index.cts
      provides: Deterministic QA and Cursor projection
    - path: src/hosts
      provides: Five-host project-native installation and lifecycle ownership
    - path: tests/skills/public-skills.test.cts
      provides: Public identity and authority-boundary regression coverage
    - path: package.json
      provides: Exact package inventory including the fifth public Skill
  key_links:
    - from: plugin-src/skills/kcoderag-update/SKILL.md
      to: kcoderag-qa/skills/kcoderag-update/SKILL.md
      via: deterministic generator projection
    - from: generated kcoderag-update assets
      to: five host-native Skill roots
      via: host adapter DesiredState composition and shared transaction
    - from: package.json files inventory
      to: next 0.3.4 release candidate
      via: pack audit, acquired-package smoke, and release dry-run
---

# Quick Task 260903-nx3: Add the public KCodeRag update Skill

## Goal

Add a dedicated fifth public `$kcoderag-update` Skill across all five hosts, separate update mutation guidance from diagnostic management, and prepare a verified `0.3.4` candidate without publishing it.

## Task 1: Define the update-only public contract

**Files:** `plugin-src/skills/kcoderag-update/SKILL.md`, `plugin-src/skills/kcoderag-update/agents/openai.yaml`, `plugin-src/skills/kcoderag-manage/SKILL.md`, `tests/skills/public-skills.test.cts`, current public documentation

**Action:** Create a short, pure-XML task-execution Skill whose invocation authorizes only the requested single-host update. It must confirm the explicit project and host, use `npx kcoderag-nav@latest update --host <host>`, respect every ownership/source/drift/transaction refusal, validate with read-only status, and never expose secret-bearing configuration. Remove duplicated update execution guidance from `$kcoderag-manage` and route explicit update intent to the new Skill. Update current product decisions from four to five while preserving Phase 06 historical records.

**Verify:** Run the public-Skill contract tests and a structural check for matching frontmatter/directory identity, required XML tags, metadata fields, mutation boundaries, and absence of destructive cleanup authority.

**Done:** The fifth Skill has one clear purpose, management remains diagnostic-first, and current documentation describes the same five-entry interface.

## Task 2: Project the fifth Skill through the complete lifecycle

**Files:** `src/capabilities/navigation.cts`, `src/generator/index.cts`, `src/hosts/{codex,claude,cursor,opencode,zcode}.cts`, `src/hosts/user-sources.cts`, `src/maintainer/pack-audit.cts`, generator/host/smoke tests, generated `kcoderag-qa/` and `kcoderag-cursor/` trees

**Action:** Add the canonical Skill and Codex metadata as navigation contributors, deterministically generate appropriate QA/Cursor assets, install the Skill into each host's native project root, include its identity in selected-host source conflict gates, and extend exact package/lifecycle expectations. Preserve the two internal capability IDs and the shared transaction boundary.

**Verify:** Build, run targeted Skill/generator/host/pack tests, run generation twice with the second pass reporting zero drift, and exercise an acquired local package across all five host adapters.

**Done:** Every lifecycle path owns and reconciles `$kcoderag-update` exactly like the other navigation-family Skills, with no new capability or host-global mutation.

## Task 3: Verify the forward release path

**Files:** `package.json`, `package-lock.json`, versioned manifests/generated assets, release-readiness expectations, `.planning/quick/260903-nx3-add-a-fifth-public-kcoderag-update-skill/260903-nx3-SUMMARY.md`

**Action:** Keep immutable `0.3.3` unchanged, run the complete local CI, pack audit, required five-host acquired-package smoke, and a release dry-run that derives `0.3.4` as the next version. Keep the actual version commit, tag, dist-tag, and publish operations outside this task.

**Verify:** `npm run ci:local`, release/readiness gates, pack audit, and required smoke all pass; `npm run release:patch -- --dry-run --json` reports `0.3.4`, tag `v0.3.4`, `commit:null`, and leaves product paths clean.

**Done:** The five-Skill product surface and forward `0.3.4` release path are verified and ready for an explicit publication decision.

---
quick_id: 260831-0ei
phase: quick-260831-0ei
plan: "01"
type: execute
status: planned
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/workflows/readiness.yml
  - tests/maintainer/readiness-workflow.test.cts
  - .planning/WINDOWS.md
  - .planning/phases/03.1-javascript-npx/deferred-items.md
  - .planning/phases/04.2-public-debranding/04.2-VERIFICATION.md
  - .planning/quick/260831-0ei-close-phase-04-2-maintenance-debt-audit-/260831-0ei-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [BRAND-04, TEST-10]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every checkout/setup-node use in CI, release, and readiness is pinned to a currently verified official full commit SHA whose action.yml declares the Node 24 runtime."
    - "Broken-window entries 10 and 16-21 are marked fixed only after their implementation or workflow repair is demonstrated, leaving zero open windows."
    - "The duplicated Phase 03.1 action-pin deferred item is resolved and the cross-phase UAT audit reports zero outstanding items."
    - "Phase 04.2 remains passed after fresh local build, full regression, package, smoke, identity, documentation, and readiness-contract gates."
    - "The evidence states that hosted readiness lanes were not rerun and makes no publish, tag, push, or hosted-timing claim."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Node-24-backed immutable official Action pins for required and optional CI jobs"
    - path: ".github/workflows/release.yml"
      provides: "Node-24-backed immutable official Action pins without changing the tag-only publish boundary"
    - path: ".github/workflows/readiness.yml"
      provides: "Node-24-backed immutable official Action pins without changing the dedicated readiness trigger or lane topology"
    - path: ".planning/WINDOWS.md"
      provides: "Machine-managed zero-open broken-windows ledger"
    - path: ".planning/phases/03.1-javascript-npx/deferred-items.md"
      provides: "Resolved action-pin maintenance record"
    - path: ".planning/phases/04.2-public-debranding/04.2-VERIFICATION.md"
      provides: "Honest post-maintenance local revalidation addendum"
    - path: ".planning/quick/260831-0ei-close-phase-04-2-maintenance-debt-audit-/260831-0ei-SUMMARY.md"
      provides: "Sanitized live-pin, ledger, audit, and local gate evidence"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/maintainer/ci-contract.test.cts"
      via: "full-SHA, exact matrix, test-only, and no-publish workflow assertions"
      pattern: "actions/(checkout|setup-node)@[0-9a-f]{40}"
    - from: ".github/workflows/release.yml"
      to: "tests/maintainer/release-workflow.test.cts"
      via: "tag subject, ordered gates, scoped npm credential, and immutable Action assertions"
      pattern: "actions/(checkout|setup-node)@[0-9a-f]{40}"
    - from: ".github/workflows/readiness.yml"
      to: "tests/maintainer/readiness-workflow.test.cts"
      via: "exact pin constants plus one-producer/four-consumer/one-verifier workflow contract"
      pattern: "actions/(checkout|setup-node)@[0-9a-f]{40}"
    - from: ".planning/WINDOWS.md"
      to: ".planning/phases/03.1-javascript-npx/deferred-items.md"
      via: "one demonstrated action-pin repair closes window 10 and its duplicate deferred record"
      pattern: "open_count: 0"
    - from: ".planning/phases/04.2-public-debranding/04.2-VERIFICATION.md"
      to: ".planning/quick/260831-0ei-close-phase-04-2-maintenance-debt-audit-/260831-0ei-SUMMARY.md"
      via: "the same fresh local gate results and explicit hosted-not-rerun boundary"
      pattern: "NOT_RUN_BY_SCOPE"
---

<objective>
Close Phase 04.2 maintenance debt without changing product semantics or making a new release claim.

Purpose: Replace the two deprecated-runtime Action pins with live-verified official immutable commits, close stale bookkeeping only after current evidence proves it, and refresh Phase 04.2's local completion evidence while preserving the already-accepted hosted candidate as historical evidence.
Output: Three updated workflows, their exact readiness pin contract, a zero-open ledger, a resolved deferred record, and an honest local maintenance revalidation.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/03.1-javascript-npx/deferred-items.md
@.planning/phases/04.1-code-style/04.1-06-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-07-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-VERIFICATION.md
@.planning/phases/04.2-public-debranding/04.2-VERIFICATION.md
@.planning/quick/260830-x2k-phase-04-2-readiness-ci-readiness-workfl/260830-x2k-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.github/workflows/readiness.yml
@tests/maintainer/ci-contract.test.cts
@tests/maintainer/release-workflow.test.cts
@tests/maintainer/readiness-workflow.test.cts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Replace deprecated-runtime Action pins through all workflow contracts</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .github/workflows/readiness.yml, tests/maintainer/readiness-workflow.test.cts</files>
  <action>
Use the GitHub-workflows fallback required by this repository because `scripts/ci_monitor.cjs` is absent: query only the official `actions/checkout` and `actions/setup-node` repositories through read-only GitHub REST metadata. Resolve each latest non-draft, non-prerelease stable release tag to its Git ref, peel an annotated tag if needed, fetch `action.yml` at the resolved commit, and require a lowercase 40-character commit SHA plus `runs.using: node24` before editing. The planning-time live result was checkout `v7.0.1` at `3d3c42e5aac5ba805825da76410c181273ba90b1` and setup-node `v7.0.0` at `820762786026740c76f36085b0efc47a31fe5020`; re-resolve immediately before the edit and record only repository, stable tag, commit SHA, and runtime in the quick summary.

Replace every checkout/setup-node occurrence in `ci.yml`, `release.yml`, and `readiness.yml` with the verified full SHA, retaining a human-readable stable-tag comment. Update the exact checkout/setup-node constants in `readiness-workflow.test.cts`; the CI and release tests already enforce full-SHA immutability generically. Preserve every trigger, permission, concurrency rule, runner, Node 22/24 matrix entry, cache setting, `persist-credentials: false`, job dependency, local readiness action, download-artifact pin, command ordering, artifact identity, and publish credential boundary. Do not invoke, dispatch, push to, or monitor a hosted workflow, and do not install a package.
  </action>
  <verify>
    <automated>
Read-only official-source check: `Invoke-RestMethod` against `https://api.github.com/repos/actions/{checkout|setup-node}/releases/latest` and `/git/ref/tags/{tag}`, followed by the official raw `action.yml` at the resolved SHA; assert stable tag, exact workflow SHA equality, 40 lowercase hex characters, and `using: node24` for both actions.
`npm run build`
`npm run test:ci-contract`
`npm run test:release-workflow`
`npm run test:readiness-workflow`
`git diff --check -- .github/workflows/ci.yml .github/workflows/release.yml .github/workflows/readiness.yml tests/maintainer/readiness-workflow.test.cts`
    </automated>
  </verify>
  <done>All three workflows use the same live-verified official Node-24-backed immutable checkout/setup-node commits, all workflow contract suites pass, and no trigger, release authority, lane, or artifact contract changed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove and close all seven windows plus the duplicate deferred item</name>
  <files>.planning/WINDOWS.md, .planning/phases/03.1-javascript-npx/deferred-items.md</files>
  <action>
Treat ledger closure as an evidence operation, not a bulk status rewrite. Before closing windows 16-19, rerun the capability and Codex/Claude/Cursor/OpenCode host projections plus cross-host tests and confirm `04.1-06-SUMMARY.md` and `04.1-VERIFICATION.md` identify no remaining stub. Before closing window 20, assert `package.json.files` contains `dist/capabilities/compose.cjs` and rerun the package contract. Before closing window 21, rerun build and CLI lifecycle/source-gate coverage and confirm `04.1-07-SUMMARY.md` reports no known stub or legacy cleanup authority. Resolve the canonical GSD tools shim as `$GSD_TOOLS`, then close each demonstrated entry with `node "$GSD_TOOLS" windows fixed <id>` so counts, timestamps, the Markdown table, and embedded JSON remain synchronized.

Close window 10 only after Task 1's live official-source check and all three workflow contract suites pass. Change the Phase 03.1 deferred item from open to resolved and record the verified official tags/SHAs, Node 24 runtime declaration, three updated workflow paths, and this quick task as the closure evidence. Do not rewrite the historical `v0.1.6` result. Finish by running the GSD cross-phase audit and require zero outstanding items, then require the windows status JSON to report `open_count: 0`, `fixed_count: 24`, and no waived entries. Preserve every unrelated dirty or untracked path and stage only the two declared planning files for this task.
  </action>
  <verify>
    <automated>
`npm run build`
`npm run test:capabilities`
`npm run test:host:codex`
`npm run test:host:claude`
`npm run test:host:cursor`
`npm run test:host:opencode`
`npm run test:cross-host`
`npm run test:cli`
`npm run test:pack`
`node -e "const p=require('./package.json'); if(!p.files.includes('dist/capabilities/compose.cjs')) process.exit(1)"`
`node "$GSD_TOOLS" query audit-uat --raw` returns `summary.total_items: 0`.
`node "$GSD_TOOLS" windows status --raw` returns `ledger.open_count: 0`, `ledger.fixed_count: 24`, and `ledger.waived_count: 0`.
    </automated>
  </verify>
  <done>Windows 16-21 are evidence-backed stale closures, window 10 is closed by the Node-24 pin repair, the duplicate deferred record is resolved, and both GSD audits are clean.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh Phase 04.2 local completion evidence without a hosted or release claim</name>
  <files>.planning/phases/04.2-public-debranding/04.2-VERIFICATION.md, .planning/quick/260831-0ei-close-phase-04-2-maintenance-debt-audit-/260831-0ei-SUMMARY.md, .planning/STATE.md</files>
  <action>
After Tasks 1 and 2 are committed, run the complete local maintenance proof from the resulting committed HEAD: dependency audit, full serialized tests, deterministic generation, docs and retirement checks, current Git brand audit, real pack audit, five-host packaged smoke, CI/release/readiness workflow contracts, release-readiness and seal unit suites, artifact-upload tests, ledger status, UAT audit, and diff hygiene. Capture actual test counts, durations, HEAD, official pin metadata, and zero-open audit results; do not copy the previous 429 count unless the fresh suite reports it.

Append a clearly labeled post-maintenance revalidation section to `04.2-VERIFICATION.md` and add non-destructive maintenance metadata in its frontmatter rather than replacing the original verification timestamp or immutable hosted candidate/run/receipt facts. State that the previously accepted hosted evidence remains historical Phase 04.2 evidence, while the current HEAD's action-pin and CI-latency maintenance changes were re-proved locally. Record hosted readiness, publish, tag, push, registry mutation, and hosted timing as `NOT_RUN_BY_SCOPE`; do not run `readiness:workflow-upload`, a lane command against hosted artifacts, `npm publish`, `git tag`, `git push`, or any GitHub workflow dispatch. Write the quick summary with the same boundary and update STATE activity while leaving current phase 05 unchanged. Recheck the final committed documentation child with docs, brand, ledger, audit-UAT, and diff gates.
  </action>
  <verify>
    <automated>
`npm run build`
`npm run deps:audit`
`npm test`
`npm run generate:check`
`npm run docs:check`
`npm run audit:retirement`
`npm run audit:brand -- git --subject HEAD`
`npm run pack:audit`
`npm run test:smoke`
`npm run test:ci-contract`
`npm run test:release-workflow`
`npm run test:readiness-workflow`
`npm run test:release-readiness`
`npm run test:readiness-seal`
`npm run test:github-artifact-upload`
`node "$GSD_TOOLS" query audit-uat --raw` returns zero items.
`node "$GSD_TOOLS" windows status --raw` returns zero open windows.
`git diff --check`
    </automated>
  </verify>
  <done>Phase 04.2 still has passed status with fresh local post-maintenance evidence, the report and summary explicitly distinguish historical hosted evidence from current local verification, STATE remains positioned at Phase 05, and all prohibited external mutations remain unperformed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository to GitHub API/raw official repositories | Network metadata determines executable third-party Action commits and must be authenticated by repository identity, stable tag, immutable SHA, and action runtime content. |
| Workflow source to GitHub-hosted runner | Pinned external Action code executes with workflow permissions; full-SHA immutability and minimal permissions bound this supply-chain surface. |
| Implementation evidence to planning ledgers | A status-only edit could falsely erase debt, so every closure is gated by current code/tests and machine-managed ledger commands. |
| Historical hosted receipt to current maintenance HEAD | Prior hosted evidence must not be presented as a new run or as timing evidence for the modified HEAD. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q260831-01 | Spoofing | Action pin resolution | high | mitigate | Accept only official `actions/*` repositories, GitHub release/tag metadata, the resolved immutable commit, and official raw `action.yml`; record only non-secret metadata. |
| T-Q260831-02 | Tampering | CI/release/readiness workflows | high | mitigate | Preserve 40-character full-SHA pins, validate Node 24 runtime at that exact commit, and rerun all three workflow contract suites. |
| T-Q260831-03 | Elevation of privilege | Release workflow | high | mitigate | Preserve contents-read permissions, tag/version binding, scoped `NPM_TOKEN`, ordered gates, and the no-dispatch/no-publish task boundary. |
| T-Q260831-04 | Repudiation | Windows/deferred closure | medium | mitigate | Run focused implementation evidence before each ledger closure, use GSD's `windows fixed` command, and require zero-open machine-readable audit results. |
| T-Q260831-05 | Information disclosure | GitHub metadata and summaries | low | mitigate | Store only repository, public tag, public SHA, runtime, counts, and safe paths; do not capture headers, tokens, raw logs, artifact bodies, or private paths. |
| T-Q260831-06 | Tampering | Phase 04.2 verification | medium | mitigate | Preserve original candidate/run/receipt facts, append a separate local maintenance result, and mark hosted/release actions `NOT_RUN_BY_SCOPE`. |
| T-Q260831-07 | Denial of service | Full local regression | low | accept | The serialized suite and real five-host smoke are intentionally comprehensive; bounded local runtime is acceptable for final closure evidence. |
</threat_model>

<verification>
After all three task commits, repeat the Task 3 final documentation-child checks from committed HEAD. The final verdict requires: official Node-24 pin resolution still matches every checkout/setup-node workflow use; all local build/full/focused/readiness gates pass; `audit-uat` reports zero items; windows reports 0 open, 24 fixed, 0 waived; current Git identity and docs checks pass; unrelated pre-existing work remains unchanged; and no hosted workflow, publish, tag, push, registry mutation, or hosted-timing claim occurred.
</verification>

<success_criteria>
- All checkout/setup-node uses across the three workflows are official, full-SHA, Node-24-backed pins with passing static contracts.
- All seven former windows are fixed through demonstrated evidence, not waived, and the duplicate deferred item is resolved.
- Fresh local Phase 04.2 maintenance evidence passes and is recorded without replacing the prior hosted candidate authority.
- The repository remains release-inactive: no hosted workflow trigger, publish, tag, push, or registry change.
- Only declared task files are committed; unrelated dirty and untracked work is byte-preserved.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Close Phase 04.2 maintenance debt and re-prove local completion without external release actions | 1-3 | COVERED | Workflow repair, ledger closure, and honest local revalidation form one sequential closure path. |
| REQ | BRAND-04 | Preserve exact readiness/release boundary without publish, tag, push, or registry mutation | 1, 3 | COVERED | Workflow topology and release authority remain unchanged; external actions are explicitly not run. |
| REQ | TEST-10 | Preserve executable host/package/CI evidence | 1-3 | COVERED | Workflow contracts, focused host/package suites, full tests, and five-host smoke are rerun. |
| RESEARCH | — | No RESEARCH.md supplied for this quick task | — | COVERED | No new dependency, syntax, or architecture choice is introduced; official live metadata resolves the only time-sensitive input. |
| CONTEXT | — | Close windows 10 and 16-21, resolve the duplicated deferred item, preserve unrelated work, and avoid hosted/release mutations | 1-3 | COVERED | Every supplied constraint appears in task actions and final acceptance. |

<output>
Create `.planning/quick/260831-0ei-close-phase-04-2-maintenance-debt-audit-/260831-0ei-SUMMARY.md` when done.
</output>

---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin-src/version.txt
  - kcoderag-update.json
  - kcoderag-qa/
  - kcoderag-dev/
  - cursor-plugin/
  - .planning/STATE.md
autonomous: true
requirements:
  - RELEASE-VERSION-01
---

<objective>
Publish the accumulated local plugin work as base version 0.1.2.

Purpose: give the current QA, Dev, and Cursor distribution changes an explicit patch release and make them available from origin/master.
Output: regenerated deterministic plugin packages, verified commits, GSD summary/state record, and a successful push.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Bump and regenerate plugin packages</name>
  <files>plugin-src/version.txt, kcoderag-update.json, kcoderag-qa/, kcoderag-dev/, cursor-plugin/</files>
  <action>Change the canonical base version from 0.1.1 to 0.1.2, run the repository generator, and commit only generated/source release changes. Preserve content-derived +codex cachebusters and all accumulated local commits.</action>
  <verify>python scripts/generate_plugins.py --check</verify>
  <done>Every generated manifest/version document uses base version 0.1.2 with deterministic environment-specific cachebusters.</done>
</task>

<task type="auto">
  <name>Task 2: Verify, record, and push</name>
  <files>.planning/quick/260821-07f-bump-plugin-base-version-from-0-1-1-to-0/260821-07f-SUMMARY.md, .planning/STATE.md</files>
  <action>Run the complete local unit/hook/generation/diff gates, create the quick summary, update STATE, commit planning artifacts, then push the complete local master history to origin/master. Confirm the remote tracking ref equals HEAD.</action>
  <verify>python -m unittest discover -s tests -p "test_*.py"; python kcoderag-qa/hooks/test_grep_nudge.py; python kcoderag-dev/hooks/test_grep_nudge.py; git status; git push origin master</verify>
  <done>All gates pass, the working tree is clean, and origin/master equals local HEAD.</done>
</task>

</tasks>

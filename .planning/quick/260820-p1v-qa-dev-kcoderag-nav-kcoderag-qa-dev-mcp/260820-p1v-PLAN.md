---
quick_id: 260820-p1v
status: in_progress
---

# 将项目级插件迁移到 kcoderag-nav

## Task 1: 建立目标状态

- 确认 `kcoderag-nav` 当前未安装项目级插件，`KCodeRag` 当前只有 QA/Dev MCP 配置且没有 KCodeRag hook/skill。
- 使用仓库安装器将 QA 与 Dev 项目级插件安装到 `D:\AIProgram\kcoderag-nav`。
- 不修改或输出任一 MCP Bearer 值。

## Task 2: 验证隔离边界

- 验证 `kcoderag-nav` 拥有 QA/Dev MCP、两个 advisory hook、共享 skill 与安装状态。
- 验证 `KCodeRag` 仍只有 QA/Dev MCP，不存在 KCodeRag hook、skill 或安装器状态。
- 运行安装器相关测试与生成漂移检查。

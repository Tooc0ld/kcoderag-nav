---
quick_id: 260904-l6r
phase: quick-260904-l6r
plan: "01"
type: execute
status: complete
mode: quick
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/MCP_QA_EXPERIENCE_GUIDE.md
  - plugin-src/README.md.tmpl
  - plugin-src/cursor/README.md.tmpl
  - kcoderag-qa/README.md
  - kcoderag-cursor/README.md
  - src/maintainer/docs-check.cts
  - src/maintainer/local-guide-audit.cts
  - tests/maintainer/docs-check.test.cts
  - tests/maintainer/local-guide-audit.test.cts
  - plugin-src/skills/kcoderag/SKILL.md
  - plugin-src/skills/kcoderag/agents/openai.yaml
  - kcoderag-qa/skills/kcoderag/SKILL.md
  - kcoderag-qa/skills/kcoderag/agents/openai.yaml
  - kcoderag-cursor/skills/kcoderag/SKILL.md
  - tests/skills/public-skills.test.cts
autonomous: true
requirements: []
---

# Quick Task 260904-l6r: Update user documentation from recent releases and update-skill commits

## Goal

Give a fresh user one accurate path to install KCodeRag Nav, confirm installed/latest versions, invoke all five public Skills, understand host-specific update notifications, explicitly update one host, and recognize the current public `0.3.5` release.

## Task 1: Lock the current user-facing contract

**Files:** `src/maintainer/docs-check.cts`, `tests/maintainer/docs-check.test.cts`

**Action:** Extend the user-guide documentation contract to require all five public Skill identities, installed/latest/version-status guidance, explicit single-host update authority, and an honest Cursor no-automatic-notice boundary.

**Verify:** Build and run the focused docs-check test; prove the new contract fails against incomplete fixtures and passes after fixture updates.

**Done:** Future documentation drift cannot silently remove the fifth Skill, version interpretation, or Cursor notification limitation.

## Task 2: Make the navigation Skill self-explanatory

**Files:** `plugin-src/skills/kcoderag/SKILL.md`, `plugin-src/skills/kcoderag/agents/openai.yaml`, `tests/skills/public-skills.test.cts`

**Action:** Keep `$kcoderag` as the single read-only navigation entry, add a no-input/help response, document the `find`, `context`, `callers`, `callees`, `impact`, and `indexes` intent forms, and surface a help-oriented Codex default prompt. Do not add a redundant `$kcoderag-find` Skill.

**Verify:** Run the focused public-skills test and prove the source Skill contains an immediate usage contract without acquiring lifecycle or write authority.

**Done:** Selecting or invoking `$kcoderag` without a target teaches the user what to type, while direct natural-language navigation remains supported.

## Task 3: Rewrite the usage path from recent product changes

**Files:** `README.md`, `docs/MCP_QA_EXPERIENCE_GUIDE.md`, `plugin-src/README.md.tmpl`, `plugin-src/cursor/README.md.tmpl`

**Action:** Update the quick start, post-install verification, five public Skills, update workflow, cache/notification timing, host differences, and current npm release. Remove stale `0.3.1` and false Cursor `postToolUse` update-notice claims while preserving security and Phase 05 evidence boundaries.

**Verify:** Cold-read the guide as a new end user and walk the exact install → status/doctor → use → update → reopen flow without relying on repository implementation details.

**Done:** A user can act correctly from the docs alone, and README plus the focused guide agree on every public command and host boundary.

## Task 4: Regenerate and verify public documentation

**Files:** `kcoderag-qa/README.md`, `kcoderag-cursor/README.md`

**Action:** Regenerate the checked-in QA/Cursor documents from canonical templates, then run documentation, deterministic generation, build, focused tests, and package gates.

**Verify:** `npm run docs:check`, `npm run generate:check`, the focused compiled docs/Skill/adapter tests, and `npm run pack:audit` all pass after the final edit; `git diff --check` is clean.

**Done:** Canonical templates and generated package documentation are byte-aligned and safe to publish.

---
quick_id: 260820-t66
status: complete
---

# Fix installer ownership and cross-host versioning

## Task 1: Lock the regressions with tests

- Add project-installer coverage proving an unowned legacy `hooks.json` is never deleted when no prior install state proves ownership.
- Replace the version assertion that bans build metadata with a contract for a real patch release plus the Codex cachebuster suffix.

## Task 2: Correct ownership and version behavior

- Retire legacy payloads only when an existing verified installer state owns them; otherwise preserve the normal unmanaged-content conflict.
- Publish the repair as `0.1.1+codex.20260820112121` and regenerate both QA/Dev packages deterministically.

## Task 3: Verify and record completion

- Run generation drift checks, all repository unit tests, both hook regression suites, strict Claude plugin validation, and Git whitespace checks.
- Commit source changes atomically, then write the quick-task summary and update `.planning/STATE.md` in a separate documentation commit.

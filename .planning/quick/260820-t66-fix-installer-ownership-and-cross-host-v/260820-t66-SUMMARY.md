---
phase: quick
plan: 260820-t66
subsystem: plugin-distribution
tags: [python, installer, ownership, codex, claude-code, semver]
provides:
  - Unowned legacy hook payloads are preserved through conflict refusal
  - Verified legacy installer payloads still migrate without resurrection
  - QA and Dev packages use a patch release with a Codex cachebuster
key-files:
  modified:
    - scripts/manage_project_install.py
    - tests/test_project_install.py
    - tests/test_generation.py
    - plugin-src/version.txt
    - kcoderag-qa/.claude-plugin/plugin.json
    - kcoderag-qa/.codex-plugin/plugin.json
    - kcoderag-dev/.claude-plugin/plugin.json
    - kcoderag-dev/.codex-plugin/plugin.json
completed: 2026-08-20
status: complete
---

# Quick Task 260820-t66 Summary

## Accomplishments

- Removed the installer bypass that treated every legacy private `hooks.json` as owned.
- Limited legacy retirement to paths proven by the verified install-state digest; unowned files now fail before writes with `unmanaged_name_conflict`.
- Changed the shared release from the semantic downgrade `0.1.0-codex...` to `0.1.1+codex.20260820112121` and regenerated both packages.
- Added regression coverage for unowned legacy files both before the first install and after a current install.

## Commit

- `0b478fc` — `fix: preserve unowned legacy hooks during install`

## Verification

- `python scripts/generate_plugins.py --check` — PASS.
- `python -m unittest discover -s tests -p "test_*.py" -v` — PASS, 20/20.
- QA and Dev hook regressions — PASS, 53/53 each.
- `claude plugin validate ... --strict` — PASS for QA and Dev.
- `codex plugin list --json` — both packages recognized at `0.1.1+codex.20260820112121`.
- `git diff --check` — PASS.

## Security Boundary

No MCP credential value was printed or added outside the existing accepted package configuration.

---
phase: 05-hook-precision
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/smoke/acceptance-receipt.cts
  - src/smoke/host-smoke.cts
  - tests/smoke/acceptance-receipt.test.cts
  - tests/smoke/host-smoke.test.cts
  - src/smoke/live-host-coordinator.cts
  - src/maintainer/acceptance-workflow.cts
  - .github/workflows/acceptance.yml
  - tests/smoke/live-host-coordinator.test.cts
  - tests/maintainer/acceptance-workflow.test.cts
  - package.json
  - docs/MCP_QA_EXPERIENCE_GUIDE.md
  - plugin-src/skills/code-lookup-discipline/SKILL.md
  - tests/maintainer/ci-contract.test.cts
  - .planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json
autonomous: true
requirements: [ROUT-05, TEST-07, TEST-08, TEST-09, TEST-11]
user_setup:
  - service: "Protected Windows real-host acceptance"
    why: "D-14 through D-20 require native five-host events and authenticated KCodeRag MCP calls that hosted runners cannot provide"
    dashboard_config:
      - task: "Keep one Windows X64 self-hosted runner labeled kcoderag-live attached to a protected GitHub environment with manual approval or explicit workflow_dispatch admission"
        location: "GitHub repository Settings -> Actions -> Runners and Settings -> Environments"
      - task: "Install and authenticate the frozen Codex, Claude Code, Cursor, OpenCode, and ZCode versions; approve only the disposable Cursor/ZCode acceptance workspaces when the LIVE run requests trust"
        location: "The protected self-hosted Windows runner"

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14/D-15: PACKAGED PASS and LIVE PASS are different evidence levels; native-host absence is NOT_RUN with a specific reasonCode, executed behavioral breach is FAIL, and direct launcher execution cannot satisfy LIVE."
    - "D-16: ordinary hosted Windows/Linux Node 22/24 lanes produce five-host PACKAGED evidence, while the only required LIVE platform is protected Windows self-hosted Node 22 with liveOs and packagedOs recorded honestly."
    - "D-17: every real-host lane proves its honest session baseline, list_indexes negotiation, authenticated SyntheticSymbol search_code structured result, feedback reminder, test-purpose submit_feedback success and suppression, plus malformed/failure fail-open behavior."
    - "D-18: receipts contain only exact tgz hash, host/version/OS/Node metadata, logical event/tool names, booleans, status/stage/reasonCode/evidenceLevel, durations, counts, and marker states; receipt/log/artifact secret scans pass."
    - "D-19: a hosted producer builds one exact tgz, every packaged/live consumer verifies its SHA, self-hosted execution is unavailable to untrusted forks, and any current FAIL/NOT_RUN/missing receipt/hash mismatch fails the required LIVE gate."
    - "D-20: one Windows job runs isolated Codex/Claude/OpenCode lanes in parallel, then Cursor and ZCode serially, with per-host timeout, complete process-tree cleanup, and independent receipts."
    - "TEST-07/08/09/11: each named host completes clean-project install/status/authenticated MCP/native delivery/update/uninstall against the same exact candidate, including idempotent repeat and interruption recovery evidence."
  artifacts:
    - path: "src/smoke/acceptance-receipt.cts"
      provides: "Closed Phase 5 PACKAGED/LIVE receipt schema, parser, validator, atomic writer, and secret scanner"
      exports: ["AcceptanceStatus", "AcceptanceStage", "AcceptanceReasonCode", "EvidenceLevel", "AcceptanceReceipt", "parseAcceptanceReceipt", "writeAcceptanceReceipt"]
    - path: "src/smoke/live-host-coordinator.cts"
      provides: "Mixed Windows coordinator with three parallel CLI lanes and two serial desktop lanes"
      exports: ["LiveLaneSpec", "LiveCoordinatorResult", "runLiveHostCoordinator"]
    - path: "src/maintainer/acceptance-workflow.cts"
      provides: "Exact artifact handoff, current-candidate receipt-set verification, and secret-safe aggregation"
      exports: ["verifyAcceptanceArtifact", "verifyLiveReceiptSet", "runAcceptanceWorkflowCommand"]
    - path: ".github/workflows/acceptance.yml"
      provides: "One-package hosted PACKAGED matrix and protected Windows Node 22 LIVE gate"
    - path: ".planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json"
      provides: "Current exact-candidate five-host metadata-only LIVE receipt set"
    - path: "docs/MCP_QA_EXPERIENCE_GUIDE.md"
      provides: "Authoritative host-honest prompt, routing, marker, PACKAGED/LIVE, and D-19 reset guidance"
  key_links:
    - from: ".github/workflows/acceptance.yml"
      to: "src/maintainer/acceptance-workflow.cts"
      via: "hosted producer outputs artifact id, SHA-256, member count, and candidate SHA consumed without rebuild"
      pattern: "artifact-sha256|acceptance:workflow"
    - from: "src/maintainer/acceptance-workflow.cts"
      to: "src/smoke/live-host-coordinator.cts"
      via: "verified exact tgz and protected live admission passed into the Windows Node 22 coordinator"
      pattern: "runLiveHostCoordinator"
    - from: "src/smoke/live-host-coordinator.cts"
      to: "src/smoke/acceptance-receipt.cts"
      via: "each lane writes one closed atomic metadata-only receipt after cleanup"
      pattern: "writeAcceptanceReceipt"
    - from: "plugin-src/skills/code-lookup-discipline/SKILL.md"
      to: "src/hooks/mcp-call-marker.cts"
      via: "ROUT-05 guidance matches the same reliable list_indexes availability fact used by runtime prompts"
      pattern: "list_indexes|semantic|hybrid"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] A launcher, handler, loopback stub, simulated callback, copied historical receipt, or unavailable/skipped host must not produce or satisfy LIVE PASS."
    - "[FLAGGED-UNVERIFIED] The persistent Windows runner must not execute pull-request code from an untrusted fork or any ref that bypasses the protected environment/manual workflow admission policy."
    - "[FLAGGED-UNVERIFIED] The LIVE job must not rebuild, repack, refetch, publish, tag, unpublish, or change a dist-tag after downloading the hosted producer's exact tgz."
    - "[FLAGGED-UNVERIFIED] Receipt, log, job summary, uploaded artifact, and tracked evidence must not contain queries, code results, model replies, configuration bodies, URL, Header, Bearer, tool arguments, response bodies, raw stderr, or private credential paths."
    - "[FLAGGED-UNVERIFIED] Linux PACKAGED coverage must not be presented as Linux native-host LIVE evidence, and Cursor/ZCode desktop lanes must not run concurrently."
    - "[FLAGGED-UNVERIFIED] Historical receipts or a receipt bound to a different candidate/package hash must not complete the current phase."
---

<objective>
Replace optional observation with a strict exact-artifact acceptance system and collect the current five-host authenticated LIVE evidence required to complete Phase 5.

Purpose: Make every host claim falsifiable, secret-safe, current-candidate-bound, and honest about environment absence versus behavioral failure.
Output: Closed receipt taxonomy, mixed Windows coordinator, one-package hosted/LIVE workflow, authoritative guidance, and a current five-host LIVE receipt set.
</objective>

## Phase Goal

**As a** 用户, **I want to** 在 Codex、Claude Code、Cursor、OpenCode 与 ZCode 中获得统一但宿主诚实的低打扰导航策略, **so that** 每一种项目级提示、成功 marker、更新行为和 MCP 路径都由同一 exact package 的契约测试与真实宿主事件留下可审计证据。

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hook-precision/05-CONTEXT.md
@.planning/phases/05-hook-precision/05-PATTERNS.md
@.planning/phases/05-hook-precision/05-01-SUMMARY.md
@.planning/phases/05-hook-precision/05-02-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-45-SUMMARY.md
@.planning/quick/260831-s98-automate-self-hosted-live-acceptance-and/260831-s98-SUMMARY.md
@src/smoke/host-smoke.cts
@src/fixtures/host-delivery.cts
@src/maintainer/readiness-workflow.cts
@.github/workflows/acceptance.yml
@.github/workflows/ci.yml
@docs/MCP_QA_EXPERIENCE_GUIDE.md

<interfaces>
Existing migration inputs:
- host-smoke.cts: SmokeMode, SmokeStatus, HostSmokeResult, PackageProvenance, evaluateHostEvidence, smokeExitCode, LIVE_PROMPT, liveCommandSpec.
- readiness-workflow.cts: PlatformLaneReceipt, openDownloadedLease, verifyPlatformLaneSet and exact downloaded artifact SHA/member verification.
- host-delivery.cts: strict exact-key parse/freeze/digest/atomic-write pattern; its code-style delivery schema remains separate from the new Phase 5 acceptance schema.
- acceptance.yml currently runs one Windows Node 24 self-hosted job and treats live failure as optional; this is a migration source, not a contract to preserve.
Plan 05-01/02 provide normalized events, governor marker states, five honest host projections, and a deterministic exact package inventory.
</interfaces>
</context>

## Flagged Assumptions — Specless Edge Fallback

- **A-06 — TEST-07 idempotency [FLAGGED-UNVERIFIED]:** two Codex install/status/update passes over the exact tgz yield identical managed bytes/state and one successful uninstall restores the clean project.
- **A-07 — TEST-07 concurrency/interruption [FLAGGED-UNVERIFIED]:** Codex may run concurrently only with isolated Claude/OpenCode lanes; timeout/cancellation kills its complete process tree, closes handles, emits one non-PASS receipt, and leaves no reusable project/cache/marker lane.
- **A-08 — TEST-08 idempotency [FLAGGED-UNVERIFIED]:** two Claude Code install/status/update passes are byte-idempotent and uninstall restores unrelated project settings/MCP bytes.
- **A-09 — TEST-08 concurrency/interruption [FLAGGED-UNVERIFIED]:** Claude Code runs in an isolated parallel lane and interruption cleanup cannot terminate or read another lane's process, cwd, temp, npm cache, host home, or markers.
- **A-10 — TEST-09 idempotency [FLAGGED-UNVERIFIED]:** Cursor reload plus repeated install/status/update preserves one Rule/skill/MCP/after-event projection and uninstall restores unrelated entries.
- **A-11 — TEST-09 concurrency/interruption [FLAGGED-UNVERIFIED]:** the coordinator refuses concurrent Cursor desktop lanes, runs one foreground workspace at a time, and kills the exact interrupted process tree before cleanup and receipt finalization.
- **A-12 — TEST-11 idempotency [FLAGGED-UNVERIFIED]:** after user-approved disposable workspace trust, ZCode reload plus repeated lifecycle preserves one MCP/Skill/native Hook projection and uninstall restores unrelated config.
- **A-13 — TEST-11 concurrency/interruption [FLAGGED-UNVERIFIED]:** ZCode is serialized after Cursor, a trust/admission timeout is NOT_RUN with a specific reasonCode, and an admitted execution timeout is FAIL with exact process-tree cleanup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define closed PACKAGED/LIVE receipt taxonomy and migrate smoke evaluation</name>
  <reversibility rating="costly">The receipt schema becomes the workflow, tracked evidence, and support-audit contract, so changes require coordinated migration of every producer and verifier.</reversibility>
  <files>src/smoke/acceptance-receipt.cts, src/smoke/host-smoke.cts, tests/smoke/acceptance-receipt.test.cts, tests/smoke/host-smoke.test.cts</files>
  <read_first>
    <file>src/smoke/host-smoke.cts</file>
    <file>tests/smoke/host-smoke.test.cts</file>
    <file>src/fixtures/host-delivery.cts</file>
    <file>src/maintainer/readiness-workflow.cts</file>
    <file>.planning/phases/05-hook-precision/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: exact keys and enum values are required for status, stage, reasonCode, host, hostVersion, os, nodeVersion, evidenceLevel, artifact SHA, candidate, event/tool booleans, counts, durations, and marker state per D-15/D-18.
    - Test 2: PASS requires stage complete, reasonCode verified, exact current artifact/candidate, and every evidence-level-specific observation; NOT_RUN requires an environment/admission reason and FAIL requires an executed-stage behavioral/evidence reason.
    - Test 3: direct launcher/handler/stub execution can create PACKAGED observations only and parse/serialization rejects LIVE PASS unless nativeHostProcess, nativeEventObserved, and current receipt facts are true per D-14.
    - Test 4: secret canaries in queries, results, config, URL, headers, Bearer, arguments, response, stderr, and private paths are absent from receipt serialization, error messages, and summaries per D-18.
  </behavior>
  <action>Create an independent schema-versioned `acceptance-receipt.cts`; do not repurpose the existing code-style delivery receipt. Define `AcceptanceStatus` as PASS/FAIL/NOT_RUN, `EvidenceLevel` as PACKAGED/LIVE, and independent `AcceptanceStage` values environment, admission, package, install, native-event, prompt-semantics, mcp, feedback, evidence, complete. Define a closed `AcceptanceReasonCode` vocabulary covering runner/host binary/version/login/trust absence; artifact missing/hash/member mismatch; install/status/update/uninstall failure; native event/session identity absence; prompt semantic/fail-open violation; MCP registration/auth/list_indexes/search/result-shape failure; feedback reminder/submit/suppression failure; timeout/process cleanup; receipt missing/schema/secret/current-candidate failure; and verified. Enforce D-14/D-15 PASS implications in the parser, use atomic mode-0600 writes, cap counts/strings/file size, and scan serialized receipts plus supplied metadata-only summaries. Migrate host-smoke result evaluation so required PACKAGED evidence and strict LIVE evidence are distinct; remove the behavior where an optional live NOT_RUN can satisfy a phase gate, while retaining a nonzero result for every incomplete required level.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/smoke/acceptance-receipt.test.cjs dist-tests/smoke/host-smoke.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>D-14: direct launcher, handler, loopback, and simulated callback cases are explicitly incapable of producing LIVE PASS.</criterion>
    <criterion>D-15: status, stage, reasonCode, and evidenceLevel remain independent closed fields; missing environment and executed failure have different valid combinations.</criterion>
    <criterion>D-18: the exact allow-list excludes all payload/config/result/stderr/private-path bodies and secret-canary tests cover serializer, validator, error, and summary paths.</criterion>
    <criterion>Current required smoke continues to fail on incomplete PACKAGED evidence and the new strict LIVE evaluator fails on PASS precondition gaps.</criterion>
  </acceptance_criteria>
  <done>Every host result has a closed, stage-specific, evidence-level-specific, secret-safe receipt contract that cannot confuse unavailable, failed, packaged, and live states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build exact-artifact workflow handoff and the mixed Windows live-host coordinator</name>
  <reversibility rating="costly">The coordinator ordering and protected workflow topology encode persistent-runner isolation and current-candidate evidence authority across all five hosts.</reversibility>
  <files>src/smoke/live-host-coordinator.cts, src/maintainer/acceptance-workflow.cts, .github/workflows/acceptance.yml, tests/smoke/live-host-coordinator.test.cts, tests/maintainer/acceptance-workflow.test.cts</files>
  <read_first>
    <file>src/smoke/host-smoke.cts</file>
    <file>src/maintainer/readiness-workflow.cts</file>
    <file>.github/workflows/acceptance.yml</file>
    <file>.github/workflows/ci.yml</file>
    <file>tests/maintainer/ci-contract.test.cts</file>
    <file>.planning/debug/resolved/real-host-smoke-deadlock.md</file>
  </read_first>
  <behavior>
    - Test 1: a hosted producer creates one actual tgz and outputs its SHA/member count/candidate; every Windows/Linux Node 22/24 PACKAGED lane and the Windows Node 22 LIVE job downloads and verifies those values without rebuilding per D-16/D-19.
    - Test 2: self-hosted LIVE is admitted only for trusted same-repository protected/manual workflow execution; fork pull requests and unapproved refs cannot schedule the persistent runner per D-19.
    - Test 3: Codex, Claude Code, and OpenCode start concurrently with disjoint project/cwd/temp/npm-cache/host-home/marker roots; after all settle, Cursor and ZCode execute one at a time per D-20/A-07/A-09/A-11/A-13.
    - Test 4: each lane timeout/cancellation closes stdin, terminates the complete process tree, releases handles, removes only its validated disposable root, and emits an independent metadata-only non-PASS receipt without affecting sibling lanes.
    - Test 5: missing binary/login/trust is NOT_RUN; admitted native behavioral failure is FAIL; neither is hidden by continue-on-error or an aggregate generic reason.
  </behavior>
  <action>Implement `runLiveHostCoordinator` with explicit `LiveLaneSpec` values, three Promise-based isolated CLI lanes for Codex/Claude/OpenCode, then separate awaited Cursor and ZCode desktop lanes. Use injected spawn/clock/kill-tree/filesystem seams, per-host deadlines, Windows-hidden processes where applicable, unique validated roots under the job temp directory, and finally-block cleanup; never retain raw stdout/stderr beyond in-memory bounded classification. Build `acceptance-workflow.cts` by reusing the existing no-follow singleton artifact open/hash/member checks and by verifying one current receipt per host, exact candidate/artifact equality, liveOs windows, packagedOs windows/linux, and all PASS. Replace the optional self-hosted observation workflow with a hosted package producer, four exact-artifact PACKAGED consumers, and one protected Windows Node 22 LIVE consumer. Pin actions by immutable SHA, use least permissions, an approved environment/workflow_dispatch guard, no fork path, no rebuild in LIVE, and no continue-on-error. Keep cursor/zcode trust as explicit admission state rather than CLI preauthorization.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/smoke/live-host-coordinator.test.cjs dist-tests/maintainer/acceptance-workflow.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>D-16/D-19: workflow topology has one hosted tgz producer, four hosted PACKAGED lanes, and one protected Windows Node 22 LIVE job that verifies the same SHA.</criterion>
    <criterion>D-19: untrusted fork code has no path to the self-hosted label; no required live step uses continue-on-error; stale/missing/non-PASS receipts fail aggregation.</criterion>
    <criterion>D-20/A-07/A-09: three CLI lanes overlap in time while every root/cache/home/marker path and cleanup target is disjoint.</criterion>
    <criterion>D-20/A-11/A-13: Cursor finishes cleanup before ZCode starts; tests reject desktop overlap and distinguish admission timeout from post-admission failure.</criterion>
    <criterion>Process-tree cleanup and receipt finalization run for success, failure, timeout, cancellation, and injected cleanup error without deleting outside the validated job root.</criterion>
  </acceptance_criteria>
  <done>The workflow passes one verified artifact through honest hosted and protected LIVE lanes, and the Windows coordinator isolates/cleans all five hosts with the locked mixed parallel order.</done>
</task>

<task type="auto">
  <name>Task 3: Run five-host authenticated acceptance, seal current receipts, and align authoritative guidance</name>
  <precondition>The protected Windows X64 `kcoderag-live` runner is online with Node 22 and the frozen five host binaries installed; required host logins are active, and disposable Cursor/ZCode workspace trust can be approved without exposing any credential value.</precondition>
  <reversibility rating="costly">The checked receipt set becomes the current exact-candidate authority; replacing it requires a new complete five-host run rather than reusing historical evidence.</reversibility>
  <files>package.json, docs/MCP_QA_EXPERIENCE_GUIDE.md, plugin-src/skills/code-lookup-discipline/SKILL.md, tests/maintainer/ci-contract.test.cts, .planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json</files>
  <read_first>
    <file>package.json</file>
    <file>docs/MCP_QA_EXPERIENCE_GUIDE.md</file>
    <file>plugin-src/skills/code-lookup-discipline/SKILL.md</file>
    <file>tests/maintainer/ci-contract.test.cts</file>
    <file>.github/workflows/acceptance.yml</file>
    <file>src/smoke/live-host-coordinator.cts</file>
    <file>src/smoke/acceptance-receipt.cts</file>
  </read_first>
  <action>Add focused build/test/packaged/live/workflow npm scripts and the new compiled runtime files to the exact package allow-list. Update the shared navigation Skill and repository-owned guide so ROUT-05 uses semantic/hybrid only after reliable list_indexes availability and otherwise names keyword/context/call-chain fallback; document D-01/D-02 host-honest baseline semantics, D-06/D-07 epoch behavior, D-12/D-13 feedback rules, D-14 PACKAGED versus LIVE, D-15 reason taxonomy, D-16 platform boundary, D-19 protected exact-artifact gate, D-20 lane order, and the existing manual D-19 marker reset. Before editing the already-dirty guide, capture its private path/hunk/digest baseline and preserve all unrelated user hunks with scoped additions; do not replace the file wholesale. Extend the CI contract test to require the ordinary hosted Windows/Linux Node 22/24 PACKAGED matrix and the protected acceptance topology. Build and audit one current candidate, dispatch the protected acceptance workflow for that exact ref, and collect only the five closed receipt objects into `05-LIVE-RECEIPTS.json`; host processes must make real authenticated list_indexes, SyntheticSymbol search_code, and test-purpose submit_feedback calls through their own opaque config. Verify native SessionStart sources where supported and the honest Rule/plugin session baseline elsewhere, feedback reminder/submission suppression, fail-open malformed/failure paths, repeat lifecycle idempotency, interruption cleanup, current hash, and secret scans. If any host is FAIL, NOT_RUN, missing, stale, or hash-mismatched, do not write a passing aggregate and leave the task incomplete.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:ci-contract &amp;&amp; npm run test:pack &amp;&amp; npm run smoke:required &amp;&amp; npm run acceptance:verify -- --receipts .planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json</automated>
    <human-check>Approve only the protected disposable Cursor and ZCode workspaces if the LIVE workflow requests native trust; confirm the workflow summary reports five current LIVE PASS receipts for the exact artifact and contains metadata only.</human-check>
  </verify>
  <acceptance_criteria>
    <criterion>D-17: Codex, Claude Code, Cursor, OpenCode, and ZCode each prove honest session baseline, list_indexes, authenticated structured SyntheticSymbol search_code, feedback reminder, test-purpose submit_feedback, suppression, and fail-open behavior through the real host.</criterion>
    <criterion>D-18: receipt, workflow summary, uploaded artifacts, bounded logs, and tracked JSON pass secret scan and contain only the allowed metadata fields.</criterion>
    <criterion>D-19: all five current LIVE receipts bind the same producer tgz hash/current candidate and any non-PASS or missing result fails `acceptance:verify`.</criterion>
    <criterion>TEST-07 A-06/A-07: Codex exact-package lifecycle is idempotent, overlaps only isolated CLI lanes, and interruption cleanup is complete.</criterion>
    <criterion>TEST-08 A-08/A-09: Claude Code exact-package lifecycle is idempotent and concurrent isolation prevents cross-lane reads or kills.</criterion>
    <criterion>TEST-09 A-10/A-11: Cursor reload/lifecycle is idempotent and desktop overlap is refused with exact cleanup.</criterion>
    <criterion>TEST-11 A-12/A-13: ZCode trust/reload/lifecycle is idempotent and admission absence differs from admitted execution failure.</criterion>
    <criterion>ROUT-05: nudge runtime, shared Skill, and authoritative guide use the same index-availability gate and fallback vocabulary.</criterion>
    <criterion>No tag, publish, registry refetch, unpublish, dist-tag mutation, global GSD Hook work, production identity, HTTPS, or token rotation occurs.</criterion>
  </acceptance_criteria>
  <done>The current candidate has five exact-hash authenticated LIVE PASS receipts, the protected gate is blocking, and public guidance matches the tested host-honest policy without exposing secrets.</done>
</task>

</tasks>

## Artifacts this phase produces

- New `src/smoke/acceptance-receipt.cts` exports `AcceptanceStatus`, `AcceptanceStage`, `AcceptanceReasonCode`, `EvidenceLevel`, `AcceptanceReceipt`, `parseAcceptanceReceipt`, and `writeAcceptanceReceipt`.
- Receipt fields `schemaVersion`, `status`, `stage`, `reasonCode`, `host`, `hostVersion`, `os`, `nodeVersion`, `evidenceLevel`, `candidateSha`, `artifactSha256`, `liveOs`, `packagedOs`, `nativeEvents`, `logicalTools`, `protocolOk`, `authenticated`, `structuredResult`, `durationsMs`, `counts`, and `markerStates`.
- New `src/smoke/live-host-coordinator.cts` exports `LiveLaneSpec`, `LiveCoordinatorResult`, and `runLiveHostCoordinator` with lane groups `parallel-cli` and `serial-desktop`.
- New `src/maintainer/acceptance-workflow.cts` exports `verifyAcceptanceArtifact`, `verifyLiveReceiptSet`, and `runAcceptanceWorkflowCommand`.
- CLI/script flags `--mode packaged|live`, `--artifact-path`, `--artifact-sha256`, `--candidate-sha`, `--receipt-dir`, and `--host`; npm scripts `acceptance:packaged`, `acceptance:live`, `acceptance:workflow`, and `acceptance:verify`.
- Workflow outputs `artifact-id`, `artifact-sha256`, `artifact-member-count`, and `candidate-sha`; explicit receipt metadata `liveOs: windows` and `packagedOs: [windows, linux]`.
- New test files `tests/smoke/acceptance-receipt.test.cts`, `tests/smoke/live-host-coordinator.test.cts`, and `tests/maintainer/acceptance-workflow.test.cts`.
- Current aggregate evidence `.planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json` with exactly five closed LIVE receipt objects and no raw payload/config/output fields.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Five-host unified but native-honest prompt, marker, update, MCP, exact-package, and auditable event evidence | 05-01..03 | COVERED | Tracer → projections/package → strict LIVE evidence. |
| REQ | HOOK-06 | Fixed/multi-file/single-file/log/generated local checks remain silent | 05-01 | COVERED | Task 3 classifier table plus A-01/A-02. |
| REQ | HOOK-07 | Deep narrow/Lua globals local; unique C++/qualified Lua structural | 05-01 | COVERED | Task 3 plus A-03. |
| REQ | HOOK-08 | Post-KCodeRag exact local recheck dedupe is bounded and fail-open | 05-01 | COVERED | Task 3 plus A-04. |
| REQ | ROUT-05 | semantic/hybrid only after real list_indexes availability | 05-01, 05-03 | COVERED | Runtime, Skill, guide, and LIVE proof plus A-05. |
| REQ | TEST-07 | Exact-package Codex lifecycle/native/MCP evidence | 05-02, 05-03 | COVERED | A-06/A-07 idempotency and concurrency/interruption included. |
| REQ | TEST-08 | Exact-package Claude lifecycle/native/MCP evidence | 05-02, 05-03 | COVERED | A-08/A-09 included. |
| REQ | TEST-09 | Exact-package Cursor lifecycle/Rule/skill/MCP evidence | 05-02, 05-03 | COVERED | A-10/A-11 included. |
| REQ | TEST-11 | Exact-package ZCode lifecycle/trust/Hook/MCP receipt | 05-02, 05-03 | COVERED | A-12/A-13 included. |
| RESEARCH | — | No RESEARCH.md; use current code and PATTERNS analog map | 05-01..03 | COVERED | Pattern guardrails and three no-analog files are assigned directly. |
| CONTEXT | D-01 | Contributor registry and host-honest normalization | 05-01, 05-02 | COVERED | Tracer and adapter projection. |
| CONTEXT | D-02 | Same bounded baseline for startup/resume/clear/compact | 05-01, 05-02 | COVERED | SessionStart matrix. |
| CONTEXT | D-03 | Receipt/version/integrity-gated code-style summary | 05-01, 05-02 | COVERED | Pre-claim integrity tests. |
| CONTEXT | D-04 | Fresh newer cache; detached bounded worker | 05-01, 05-02 | COVERED | SessionStart/update task and projections. |
| CONTEXT | D-05 | Explicit command update; no automatic project mutation | 05-01..03 | COVERED | Hook, plugin, workflow, and guide prohibitions. |
| CONTEXT | D-06 | Host/root/capability/session/epoch isolation | 05-01 | COVERED | Governor contract/tracer. |
| CONTEXT | D-07 | New session/clear/compact rearm; resume/no-source no reset | 05-01, 05-02 | COVERED | Epoch tests and native payloads. |
| CONTEXT | D-08 | No time/tool-count/violation correction | 05-01 | COVERED | Locked no-rearm cases. |
| CONTEXT | D-09 | Receipt-proven SessionEnd cleanup only | 05-01, 05-02 | COVERED | Cleanup gate and host registration. |
| CONTEXT | D-10 | Broad matcher, narrow structural intent | 05-01 | COVERED | Precision matrix. |
| CONTEXT | D-11 | Structured source writes only; integrity before claim | 05-01 | COVERED | Write classifier matrix. |
| CONTEXT | D-12 | Feedback only after reliable result success | 05-01, 05-02 | COVERED | Five-host outcome normalizer. |
| CONTEXT | D-13 | Epoch reminder plus session submitted state | 05-01, 05-03 | COVERED | Governor implementation and LIVE proof. |
| CONTEXT | D-14 | Native real-host exact artifact required for LIVE | 05-01..03 | COVERED | Falsifiable tracer and strict receipt implications. |
| CONTEXT | D-15 | Independent status/stage/reasonCode/evidenceLevel | 05-03 | COVERED | Closed schema and tests. |
| CONTEXT | D-16 | Hosted dual-OS 22/24 PACKAGED; Windows 22 LIVE | 05-03 | COVERED | Workflow topology and explicit OS metadata. |
| CONTEXT | D-17 | Five-host authenticated MCP/session/feedback/fail-open | 05-03 | COVERED | Protected LIVE run. |
| CONTEXT | D-18 | Metadata-only evidence and secret scan | 05-03 | COVERED | Schema, serialization, summary, artifact scans. |
| CONTEXT | D-19 | Protected fork-safe exact-artifact blocking gate | 05-03 | COVERED | Workflow admission and current receipt verifier. |
| CONTEXT | D-20 | Three parallel CLI lanes then two serial desktop lanes | 05-03 | COVERED | Coordinator and timing/cleanup tests. |

Deferred automatic background update, Linux native LIVE, Cursor/ZCode desktop parallelism, global GSD Hooks, and production identity/HTTPS/rotation are exclusions rather than gaps and do not appear as implementation tasks.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hosted producer → artifact consumers | One tgz crosses GitHub artifact storage into four packaged lanes and a persistent Windows runner. |
| GitHub event/ref → self-hosted runner | Repository events determine whether untrusted code can reach a credentialed persistent machine. |
| real host → authenticated MCP | Each installed host reads opaque credentials and emits native events around service calls. |
| host process/log → receipt/artifact | Potentially sensitive output is reduced to closed metadata and uploaded/committed evidence. |
| current candidate → historical receipt store | Stale evidence could be replayed as current acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Elevation of Privilege | persistent self-hosted Windows runner admission | critical | mitigate | Require same-repository workflow_dispatch/protected environment approval, least permissions, immutable actions, exact ref, and static tests proving no fork pull-request path to the self-hosted label. |
| T-05-12 | Tampering | downloaded tgz and candidate binding | high | mitigate | Producer builds once; consumers open no-follow singleton file and verify SHA-256, member count, candidate, and closed inventory before host startup; LIVE never rebuilds. |
| T-05-13 | Information Disclosure | host logs, receipts, summaries, uploaded artifacts | high | mitigate | Keep raw output bounded in memory, serialize only closed metadata, scan every persisted/uploaded channel with secret canaries, and reject a receipt/artifact on any hit. |
| T-05-14 | Repudiation | stale/missing or cross-candidate evidence | high | mitigate | Require one current atomic receipt per host with exact candidate/artifact identity and reject historical, duplicate, missing, NOT_RUN, or FAIL entries. |
| T-05-15 | Denial of Service | host deadlock, timeout, desktop focus | medium | mitigate | Use three isolated parallel CLI lanes, serialized desktop lanes, per-host deadlines, complete process-tree termination, finally cleanup, and independent failure receipts. |
| T-05-16 | Spoofing | PACKAGED observation promoted to LIVE | high | mitigate | LIVE PASS parser requires native process/event booleans and protected workflow provenance; direct launchers and stubs can emit only PACKAGED evidence. |
| T-05-17 | Tampering | MCP result/feedback acceptance | medium | mitigate | Host itself reads opaque config; coordinator accepts only logical tool name, protocol/auth/result-shape booleans, and marker transitions for the fixed canary. |
| T-05-SC | Tampering | exact package used by real hosts | high | mitigate | Use the audited zero-runtime-dependency graph, one producer tgz, immutable artifact SHA, no install-script trust expansion, and no registry rebuild/refetch in LIVE. |
</threat_model>

<verification>
- `npm run build`
- `node --test dist-tests/smoke/acceptance-receipt.test.cjs dist-tests/smoke/live-host-coordinator.test.cjs`
- `node --test dist-tests/maintainer/acceptance-workflow.test.cjs dist-tests/maintainer/ci-contract.test.cjs`
- `npm run test:pack`
- `npm run smoke:required`
- `npm run acceptance:verify -- --receipts .planning/phases/05-hook-precision/05-LIVE-RECEIPTS.json`
</verification>

<success_criteria>
- The exact same tgz is SHA-verified by Windows/Linux Node 22/24 PACKAGED lanes and the protected Windows Node 22 LIVE job.
- Five current LIVE PASS receipts prove native/honest session delivery, authenticated list/search/feedback behavior, fail-open paths, lifecycle idempotency, and interruption cleanup.
- Direct launcher/stub observations remain PACKAGED, and any FAIL/NOT_RUN/missing/stale/hash-mismatched receipt fails the live gate.
- Receipt, log, summary, artifact, and tracked evidence scans find no query/result/config/URL/header/Bearer/argument/response/stderr/private-path material.
- All eight Phase 5 requirement IDs and D-01 through D-20 are covered with no deferred idea implemented.
</success_criteria>

<output>
Create `.planning/phases/05-hook-precision/05-03-SUMMARY.md` when done.
</output>

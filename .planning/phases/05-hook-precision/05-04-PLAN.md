---
phase: 05-hook-precision
plan: "04"
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/smoke/acceptance-receipt.cts
  - tests/smoke/acceptance-receipt.test.cts
  - src/smoke/host-smoke.cts
  - src/smoke/live-host-coordinator.cts
  - tests/smoke/host-smoke.test.cts
  - tests/smoke/live-host-coordinator.test.cts
  - src/maintainer/pack-audit.cts
  - package.json
  - kcoderag-cursor/rules/kcoderag-navigation.mdc
  - kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
  - tests/maintainer/pack-audit.test.cts
autonomous: true
requirements: [HOOK-06, HOOK-07, HOOK-08, ROUT-05, TEST-07, TEST-08, TEST-09, TEST-11]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-14/D-15: each host lane emits exactly one receipt status PASS|FAIL|NOT_RUN; INCOMPLETE is exclusively an aggregate verdict and is rejected as a receipt status."
    - "D-15: receipt stage is exactly environment, admission, package, install, native_event, prompt_semantics, mcp, feedback or evidence_integrity, with a closed reasonCode allow-list and exhaustive valid-combination tests."
    - "D-15: environment/admission absence produces NOT_RUN plus a matching reasonCode; attempted execution failures produce FAIL; PASS requires reasonCode=none and every required observation true."
    - "D-16/D-17/D-18/D-20: the actual package contains the closed metadata-only receipt runtime and deterministic three-parallel-then-two-serial coordinator; PACKAGED cannot satisfy LIVE."
    - "All Phase 05 public compiled/generated/config assets are declared in package.json, pack audit and actual tgz before candidate/workflow work begins."
  artifacts:
    - path: src/smoke/acceptance-receipt.cts
      provides: "ReceiptStatus PASS|FAIL|NOT_RUN, aggregate-only INCOMPLETE, closed stage/reasonCode table and host observation schema"
    - path: src/smoke/live-host-coordinator.cts
      provides: "Codex/Claude/OpenCode parallel then Cursor/ZCode serial runner with per-lane receipt and cleanup"
    - path: package.json
      provides: "Closed public compiled/generated/config allow-list"
    - path: src/maintainer/pack-audit.cts
      provides: "Exact member/hash/package closure including receipt/coordinator and generated families"
  key_links:
    - from: src/smoke/host-smoke.cts
      to: src/smoke/acceptance-receipt.cts
      via: "every terminal lane fact maps through the valid status/stage/reasonCode table"
    - from: src/smoke/live-host-coordinator.cts
      to: src/smoke/acceptance-receipt.cts
      via: "five lanes emit one closed receipt each; aggregate derives PASS|FAIL|INCOMPLETE separately"
    - from: package.json
      to: src/maintainer/pack-audit.cts
      via: "root allow-list equals actual tgz member inventory and digests"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] INCOMPLETE must never serialize as a per-host receipt status; NOT_RUN without environment/admission reasonCode is invalid."
    - "[FLAGGED-UNVERIFIED] PACKAGED/direct-launcher/mock/synthetic evidence must not set nativeHostProcess or produce LIVE PASS."
    - "[FLAGGED-UNVERIFIED] Receipt/package/log bytes must not contain query/result/config/URL/Header/Bearer/token/credential content."
    - "[FLAGGED-UNVERIFIED] No later candidate/evidence plan may modify a public packaged runtime/config/generated member."
---

<objective>
Close the exact public tgz around a strict receipt state machine, native-host coordinator and every generated/runtime/config member.

Purpose: Give candidate preparation one independently passing package whose receipt statuses cannot blur absence, failure or aggregate incompleteness.
Output: Closed receipt/coordinator runtime, Cursor generated family, package allow-list, pack audit and deterministic actual tgz proof.
</objective>

## User Story

**As a** KCodeRag Nav maintainer, **I want to** package one strict evidence runtime with the installed assets, **so that** later real-host runs classify outcomes consistently without rebuilding.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hook-precision/05-CONTEXT.md
@.planning/phases/05-hook-precision/05-PATTERNS.md
@.planning/phases/05-hook-precision/05-03-SUMMARY.md
@package.json
@src/maintainer/pack-audit.cts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the per-host receipt status/stage/reasonCode state machine</name>
  <files>src/smoke/acceptance-receipt.cts, tests/smoke/acceptance-receipt.test.cts</files>
  <read_first>
    <file>.planning/phases/05-hook-precision/05-CONTEXT.md</file>
    <file>.planning/phases/05-hook-precision/05-PATTERNS.md</file>
    <file>src/smoke/host-smoke.cts</file>
    <file>tests/smoke/host-smoke.test.cts</file>
  </read_first>
  <behavior>
    - "D-15 ReceiptStatus is exactly PASS|FAIL|NOT_RUN; parsing INCOMPLETE as a receipt status fails. AggregateVerdict may be PASS|FAIL|INCOMPLETE and is a separate type."
    - "ReceiptStage is exactly environment|admission|package|install|native_event|prompt_semantics|mcp|feedback|evidence_integrity."
    - "Environment reasonCodes: host_unavailable, runner_unavailable, node_version_unsupported. Admission: host_version_unsupported, workspace_trust_missing, protected_environment_denied, untrusted_ref. Package: package_acquisition_failed, package_hash_mismatch, package_inventory_mismatch. Install: install_failed, status_unhealthy, update_failed, uninstall_failed."
    - "Native event reasonCodes: native_event_missing, native_event_failed. Prompt semantics: prompt_missing, prompt_unexpected, prompt_dedupe_failed. MCP: mcp_registration_missing, list_indexes_unavailable, mcp_call_failed, structured_result_invalid. Feedback: feedback_reminder_missing, submit_feedback_failed, feedback_suppression_failed. Evidence integrity: candidate_mismatch, receipt_invalid, secret_detected, cleanup_failed."
    - "Valid combinations: PASS requires reasonCode=none and all required booleans true; FAIL requires an attempted lane plus a non-none reasonCode valid for its stage; NOT_RUN is valid only for environment/admission with the corresponding absence reasonCode and no execution/native success claims."
    - "D-17/D-18 metadata allow-list and common/per-host boolean schema reject missing/unknown fields and secret-shaped names/values."
  </behavior>
  <action>Implement the receipt and aggregate types test-first. Use separate discriminated unions for `HostReceipt` and `AggregateVerdict`; never share an enum containing INCOMPLETE. Encode the nine exact stage categories and closed reasonCode-to-stage map above. Implement an exhaustive valid-combination table and tests for every accepted pair plus cross-product rejection: NOT_RUN only at environment/admission, FAIL only after attempted execution, PASS only with reasonCode none and closed required booleans. Keep evidenceLevel PACKAGED|LIVE independent from status; LIVE requires native host/process and exact host booleans. Serialize bounded metadata only and reject secret-shaped material before output.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/smoke/acceptance-receipt.test.cjs</automated></verify>
  <acceptance_criteria>
    <criterion>D-15 tests prove the exact PASS|FAIL|NOT_RUN receipt enum and that INCOMPLETE is aggregate-only.</criterion>
    <criterion>All nine stage categories and every listed reasonCode have one valid mapping; invalid status/stage/reason combinations fail.</criterion>
    <criterion>Environment/admission absence yields NOT_RUN with reasonCode; no missing host/trust/runner condition becomes FAIL or PASS.</criterion>
    <criterion>D-17/D-18 closed observations and metadata reject omissions, unknown fields and secret canaries.</criterion>
  </acceptance_criteria>
  <done>One falsifiable receipt state machine distinguishes absence, attempted failure, success and aggregate incompleteness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire packaged smoke and five-host coordinator to closed receipts</name>
  <files>src/smoke/host-smoke.cts, src/smoke/live-host-coordinator.cts, tests/smoke/host-smoke.test.cts, tests/smoke/live-host-coordinator.test.cts</files>
  <read_first>
    <file>src/smoke/acceptance-receipt.cts</file>
    <file>src/smoke/host-smoke.cts</file>
    <file>src/smoke/live-host-coordinator.cts</file>
    <file>.planning/phases/05-hook-precision/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: PACKAGED direct launcher can emit PASS/FAIL/NOT_RUN but cannot satisfy LIVE or native observations per D-14/D-15.
    - Test 2: missing executable/runner/version/trust produces environment/admission NOT_RUN with exact reasonCode; attempted native/MCP/feedback failures produce FAIL at the matching stage.
    - Test 3: Codex/Claude/OpenCode lanes run isolated in parallel, then Cursor/ZCode serially; every lane emits one receipt and performs process/project/cache cleanup per D-20.
    - Test 4: aggregate is PASS only for five valid PASS receipts, FAIL for an attempted failure, and INCOMPLETE for missing/invalid/current-candidate-incomplete or required NOT_RUN evidence; no receipt status is INCOMPLETE.
  </behavior>
  <action>Integrate the closed receipt constructors into actual-package smoke and the injected Windows coordinator. Normalize environment/admission checks before execution so missing host/version/trust/runner yields NOT_RUN with the exact D-15 category. Once admitted, map package/install/native event/prompt/MCP/feedback/evidence failures to FAIL. Preserve D-20's three-parallel barrier then Cursor/ZCode serialization and finally cleanup for success/failure/interruption. Aggregate receipts separately and never rewrite receipt statuses to force completion. Keep direct launcher evidence PACKAGED.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/smoke/acceptance-receipt.test.cjs dist-tests/smoke/host-smoke.test.cjs dist-tests/smoke/live-host-coordinator.test.cjs</automated></verify>
  <acceptance_criteria>
    <criterion>Every lane terminal path emits exactly one valid PASS, FAIL or NOT_RUN receipt.</criterion>
    <criterion>NOT_RUN+reasonCode covers environment/admission absence; attempted failures use the matching later stage.</criterion>
    <criterion>Aggregate INCOMPLETE never appears inside a host receipt and cannot be promoted to PASS.</criterion>
    <criterion>D-20 ordering/isolation and cleanup pass under success, failure, concurrency and interruption.</criterion>
  </acceptance_criteria>
  <done>Packaged smoke and coordinator produce closed receipts and a separate aggregate verdict for every terminal state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close Cursor generated family and actual-tgz inventory</name>
  <files>src/maintainer/pack-audit.cts, package.json, kcoderag-cursor/rules/kcoderag-navigation.mdc, kcoderag-cursor/skills/code-lookup-discipline/SKILL.md, tests/maintainer/pack-audit.test.cts</files>
  <read_first>
    <file>package.json</file>
    <file>src/maintainer/pack-audit.cts</file>
    <file>plugin-src/cursor/rules/kcoderag-navigation.mdc</file>
    <file>src/generator/index.cts</file>
    <file>tests/maintainer/pack-audit.test.cts</file>
  </read_first>
  <behavior>
    - Test 1: Cursor Rule/Skill generated bytes match canonical generator routes and ROUT-05 guidance.
    - Test 2: package.json, pack audit and actual tgz contain every declared Phase 05 public runtime/config/generated member, including feedback-nudge, acceptance-receipt, updated host-smoke and live-host-coordinator.
    - Test 3: two actual packs from the same tree have identical SHA/member digest; five clean-host required PACKAGED lifecycles consume that tgz.
  </behavior>
  <action>Generate the two declared Cursor outputs from the canonical Rule/Skill sources, then update `package.json` and pack-audit allow-lists for the complete public inventory. Explicitly add `dist/hooks/feedback-nudge.cjs`, `dist/smoke/acceptance-receipt.cjs`, updated `dist/smoke/host-smoke.cjs` and `dist/smoke/live-host-coordinator.cjs`; retain all Plan 05-03 QA generated members. Cross-check generator declarations, root files list, tar members and orphan rejection. Pack twice, compare hashes/inventory, install into clean projects for all five hosts and run required PACKAGED lifecycle. This plan must close independently before Plan 05-05; no later plan may change a public member.</action>
  <verify><automated>npm run build &amp;&amp; npm run generate &amp;&amp; npm run generate:check &amp;&amp; node --test dist-tests/maintainer/pack-audit.test.cjs &amp;&amp; npm run smoke:required</automated></verify>
  <acceptance_criteria>
    <criterion>The two Cursor outputs are generator-derived and the task owns exactly five modified files.</criterion>
    <criterion>package.json and pack audit enumerate every compiled/generated/config member required by the actual tgz.</criterion>
    <criterion>Two packs are hash/member-identical and the one exact tgz passes five-host PACKAGED lifecycle.</criterion>
    <criterion>Plan 05-04 closes without candidate/workflow/evidence artifacts and forbids later package-member mutation.</criterion>
  </acceptance_criteria>
  <done>One deterministic actual tgz contains the complete Phase 05 runtime and independently passes PACKAGED gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/runtime facts → receipt | Untrusted terminal outcomes enter a closed classification state machine. |
| coordinator → persistent runner | Five host lanes share protected machine resources. |
| repository tree → actual tgz | Compiled/generated/config bytes cross into the exact distributable artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Spoofing | receipt status | high | mitigate | Exhaustive PASS/FAIL/NOT_RUN and status/stage/reason combination table; INCOMPLETE rejected in receipts. |
| T-05-04-02 | Tampering | actual tgz | high | mitigate | Closed root/pack/tar inventories, generate check and double-pack digest equality. |
| T-05-04-03 | Information Disclosure | receipts/artifacts | high | mitigate | Closed metadata/boolean schema and secret-shaped field/value rejection. |
| T-05-04-04 | Denial of Service | coordinator processes | medium | mitigate | Bounded lanes, deterministic barrier/serialization and finally process/project/cache cleanup. |
| T-05-04-SC | Tampering | npm package | high | mitigate | No dependency install; audited lockfile and exact member allow-list. |
</threat_model>

## Artifacts this phase produces

- `ReceiptStatus`, `ReceiptStage`, closed `ReceiptReasonCode`, `HostReceipt`, separate `AggregateVerdict` and exhaustive valid-combination table.
- Packaged `acceptance-receipt`, updated `host-smoke` and `live-host-coordinator` CJS runtime.
- Generated Cursor Rule/Skill and closed package.json/pack-audit/actual-tgz inventory.

<verification>
- Plan declares 11 files; task ownership is 2/4/5 files.
- Receipt tests reject INCOMPLETE host status and every invalid status/stage/reasonCode pair.
- Actual tgz closes twice identically and passes required five-host PACKAGED smoke before Plan 05-05.
</verification>

<success_criteria>
- Per-host absence/failure/success is unambiguous and aggregate incompleteness is a separate concept.
- The complete package is immutable before candidate/workflow and real-host evidence work.
</success_criteria>

<output>Create `.planning/phases/05-hook-precision/05-04-SUMMARY.md` when done.</output>

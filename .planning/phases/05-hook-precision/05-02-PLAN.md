---
phase: 05-hook-precision
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/hosts/claude.cts
  - src/hosts/zcode.cts
  - plugin-src/hooks/hooks.json
  - tests/hosts/native-lifecycle.test.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - plugin-src/opencode/kcoderag-nav.js
  - tests/hosts/honest-events.test.cts
  - src/capabilities/contracts.cts
  - src/generator/index.cts
  - src/maintainer/pack-audit.cts
  - kcoderag-qa/hooks/hooks.json
  - tests/generator/repository-generation.test.cts
autonomous: true
requirements: [HOOK-06, HOOK-07, HOOK-08, ROUT-05, TEST-07, TEST-08, TEST-09, TEST-11]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01/D-02: Claude Code and any receipt-proven native lifecycle host project the shared SessionStart policy without duplicating the state machine; startup/resume/clear/compact retain their locked semantics."
    - "D-03/D-04/D-05: each supported host delivers only integrity-gated code-style and fresh-newer update fragments, and no Hook/Rule/plugin path modifies the project automatically."
    - "D-06/D-07/D-09: host projections pass stable native session/source fields to the shared governor and register SessionEnd cleanup only behind the frozen LIVE proof gate."
    - "D-10/D-11/D-12/D-13: all five host payload shapes reach the same narrow lookup/write/result/feedback policy while Cursor Rule and OpenCode plugin semantics remain explicit."
    - "D-14: the canonical generator and pack audit contain the exact handlers, native registrations, Rule/skill/plugin assets, and host lifecycle files required for PACKAGED evidence; they make no LIVE claim."
    - "TEST-07/08/09/11: clean-project install/status/update/uninstall remains idempotent for Codex, Claude Code, Cursor, and ZCode while native hook/plugin merges preserve unrelated entries and interruption leaves transaction ownership intact."
  artifacts:
    - path: "plugin-src/hooks/hooks.json"
      provides: "Canonical native SessionStart/PreToolUse/PostToolUse/receipt-gated SessionEnd registration template"
    - path: "plugin-src/opencode/kcoderag-nav.js"
      provides: "Isolated OpenCode project-plugin callbacks for actual supported events"
    - path: "src/capabilities/contracts.cts"
      provides: "Lifecycle section kinds used by capability contributions"
      contains: "session-start"
    - path: "src/generator/index.cts"
      provides: "Closed deterministic projection of every Phase 5 runtime and registration asset"
    - path: "kcoderag-qa/hooks/hooks.json"
      provides: "Generated canonical native Hook registration bytes"
    - path: "tests/hosts/native-lifecycle.test.cts"
      provides: "Native event merge, session-source, cleanup-gate, idempotency, and interruption matrix"
    - path: "tests/hosts/honest-events.test.cts"
      provides: "Cursor Rule/event and OpenCode plugin honesty matrix"
  key_links:
    - from: "src/hosts/claude.cts"
      to: "plugin-src/hooks/hooks.json"
      via: "unrelated-entry-preserving render of managed native lifecycle entries"
      pattern: "SessionStart|SessionEnd|PreToolUse|PostToolUse"
    - from: "src/hosts/cursor.cts"
      to: "src/hooks/mcp-call-marker.cts"
      via: "actual afterMCPExecution success event, not a fabricated PreToolUse path"
      pattern: "afterMCPExecution"
    - from: "src/hosts/opencode.cts"
      to: "plugin-src/opencode/kcoderag-nav.js"
      via: "project plugin projection with isolated callback failures"
      pattern: "tool.execute.after"
    - from: "src/generator/index.cts"
      to: "src/maintainer/pack-audit.cts"
      via: "one closed asset inventory rendered and checked in the actual tgz"
      pattern: "NAVIGATION_QA_GROUPS|EXPECTED"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Cursor and OpenCode must not receive fabricated SessionStart, SessionEnd, or PreToolUse registrations to imitate another host."
    - "[FLAGGED-UNVERIFIED] Host adapters and generated assets must not read, compare, print, snapshot, or serialize MCP URL, headers, Bearer, configuration body, query, or result content."
    - "[FLAGGED-UNVERIFIED] Generated CJS and generated host trees must not become independent maintenance sources or be hand-edited outside the canonical generator task."
    - "[FLAGGED-UNVERIFIED] Unrelated host-native hook, Rule, plugin, skill, MCP, or configuration entries must not be replaced or reordered by install/update/uninstall."
---

<objective>
Project the shared Phase 5 policy into five host-native, capability-honest project integrations and close the deterministic generator/package inventory.

Purpose: Users must get one policy without false event equivalence, and the exact tgz must contain every handler and registration consumed by the later LIVE gate.
Output: Native Claude/ZCode lifecycle entries where proven, honest Cursor/OpenCode projections, deterministic generated bytes, actual-pack inventory enforcement, and clean-project lifecycle tests.
</objective>

## Phase Goal

**As a** 用户, **I want to** 在 Codex、Claude Code、Cursor、OpenCode 与 ZCode 中获得统一但宿主诚实的低打扰导航策略, **so that** 每一种项目级提示、成功 marker、更新行为和 MCP 路径都由同一 exact package 的契约测试与真实宿主事件留下可审计证据。

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hook-precision/05-CONTEXT.md
@.planning/phases/05-hook-precision/05-PATTERNS.md
@.planning/phases/05-hook-precision/05-01-SUMMARY.md
@src/capabilities/contracts.cts
@src/capabilities/navigation.cts
@src/hosts/claude.cts
@src/hosts/cursor.cts
@src/hosts/opencode.cts
@src/hosts/zcode.cts
@src/generator/index.cts
@src/maintainer/pack-audit.cts
@plugin-src/hooks/hooks.json
@plugin-src/opencode/kcoderag-nav.js

<interfaces>
Contracts created by Plan 05-01 and consumed here:
- NormalizedHookEvent and dispatchHookEvent carry host, managed root, stable session identity, source, context epoch, logical tool, and reliable outcome.
- ReminderKind and claimReminder own bounded epoch/session claims; adapters pass facts and never implement their own marker policy.
Existing adapter/generator contracts to preserve:
- CapabilitySectionKind is currently mcp | pre-tool | post-tool and may be extended only for genuinely managed native lifecycle sections.
- createClaudeAdapter, createCursorAdapter, createOpenCodeAdapter, and createZCodeAdapter return read/render-only HostAdapter values.
- CAPABILITY_PROJECTION_PATHS, ASSET_GROUP_PATHS, checkGenerated, and generatePackage define deterministic product inventory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project proven native lifecycle events into Claude Code and ZCode without widening cleanup authority</name>
  <files>src/hosts/claude.cts, src/hosts/zcode.cts, plugin-src/hooks/hooks.json, tests/hosts/native-lifecycle.test.cts</files>
  <read_first>
    <file>src/hosts/claude.cts</file>
    <file>src/hosts/zcode.cts</file>
    <file>src/hosts/codex.cts</file>
    <file>plugin-src/hooks/hooks.json</file>
    <file>src/hooks/session-cleanup.cts</file>
    <file>tests/hosts/claude.test.cts</file>
    <file>tests/hosts/zcode.test.cts</file>
  </read_first>
  <behavior>
    - Test 1: Claude Code install renders native SessionStart sources startup/resume/clear/compact through the shared dispatcher and preserves unrelated event entries per D-01/D-02.
    - Test 2: ZCode registers only lifecycle events supported by the frozen target version/fixture; unsupported source or event shapes remain absent and no false equivalence is claimed per D-01.
    - Test 3: SessionEnd invokes cleanup only when the frozen LIVE receipt proves a stable accurate identity field; otherwise the handler exits 0 and leaves markers intact per D-09.
    - Test 4: repeated install/update is byte-idempotent, uninstall restores unrelated entries, and injected interruption restores the complete host-local tree for TEST-08/TEST-11.
  </behavior>
  <action>Use the Plan 05-01 normalized dispatcher for every proven native SessionStart source and existing Pre/Post tool event. Update the canonical hooks template and the Claude/ZCode read/render merges together, preserving unrelated arrays and refusing unmanaged name collisions. Per D-01, do not register a lifecycle event solely to achieve parity: ZCode receives SessionStart/SessionEnd only if the frozen native fixture proves the exact event name, payload source, stable session field, and model-context response contract. Per D-02/D-03/D-04, route all startup/resume/clear/compact baseline and conditional fragments through the shared implementation. Register SessionEnd as a fail-open call whose cleanup remains gated by `sessionEndCleanupProven` per D-09. Add temporary-project tests for exact merge/restore bytes, idempotent update, injected transaction interruption, and secret canaries without reading MCP values.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/hosts/native-lifecycle.test.cjs dist-tests/hosts/claude.test.cjs dist-tests/hosts/zcode.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>D-01/D-02: each registered native source maps to one shared policy path and all four source semantics remain test-locked where the host exposes them.</criterion>
    <criterion>D-09: no SessionEnd deletion can occur from host shape inference or an unsupported identity field.</criterion>
    <criterion>TEST-08/TEST-11: install/status/update/uninstall idempotency and interruption rollback are exercised in isolated clean projects.</criterion>
    <criterion>Unrelated native entries and opaque MCP values remain unchanged and absent from diagnostics/snapshots.</criterion>
  </acceptance_criteria>
  <done>Claude Code and ZCode use only proven native lifecycle entries, share the governor policy, preserve unrelated configuration, and retain host-local transactional recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep Cursor Rule/events and OpenCode plugin callbacks semantically honest</name>
  <files>src/hosts/cursor.cts, src/hosts/opencode.cts, plugin-src/opencode/kcoderag-nav.js, tests/hosts/honest-events.test.cts</files>
  <read_first>
    <file>src/hosts/cursor.cts</file>
    <file>src/hosts/opencode.cts</file>
    <file>plugin-src/opencode/kcoderag-nav.js</file>
    <file>src/hooks/mcp-call-marker.cts</file>
    <file>src/hooks/feedback-nudge.cts</file>
    <file>tests/hosts/cursor.test.cts</file>
    <file>tests/hosts/opencode.test.cts</file>
  </read_first>
  <behavior>
    - Test 1: Cursor retains always-on Rule/skill/MCP plus actual afterMCPExecution/postToolUse-style events and contains no fabricated SessionStart/PreToolUse registration per D-01/D-14.
    - Test 2: OpenCode project plugin isolates marker, feedback, and update callbacks; one exception cannot suppress another callback or reject the host tool per D-04/D-12/D-13.
    - Test 3: Cursor and OpenCode normalize successful KCodeRag logical tool names and reliable outcomes into the same feedback state machine without retaining payload bodies.
    - Test 4: repeated install/update is byte-idempotent, uninstall restores unrelated native entries, and interruption leaves the prior complete tree for TEST-09 and the five-host phase goal.
  </behavior>
  <action>Preserve the Cursor product as Rule, shared Skill, MCP, and only its documented actual after-events. Feed afterMCPExecution into the shared logical-tool/success normalizer and keep update notification on its existing real callback; do not claim SessionStart or PreToolUse. Extend the OpenCode project plugin callback map only with actual plugin events available in the frozen version, isolating recordKCodeRagCall, feedback state, update notice, detached scheduling, and optional toast in independent try/catch boundaries. Pass project directory and stable host session facts to the shared governor without persisting raw callback input. Preserve the JSON/JSONC exclusive project-config rule, unrelated plugin entries, project-only scope, D-05 no-auto-update behavior, and fail-open host operation. Test repeated lifecycle, interruption, and secret canaries.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/hosts/honest-events.test.cjs dist-tests/hosts/cursor.test.cjs dist-tests/hosts/opencode.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>D-01/D-14: tests assert explicit Rule/plugin semantics and reject a fabricated native SessionStart/PreToolUse or LIVE-equivalence claim.</criterion>
    <criterion>D-04/D-05: update reads local state, schedules detached refresh only when eligible, and never mutates the project automatically.</criterion>
    <criterion>D-12/D-13: reliable result and feedback transitions use logical tool metadata only, with callback failures isolated.</criterion>
    <criterion>TEST-09 and OpenCode phase coverage: idempotent lifecycle, unrelated-entry preservation, rollback after interruption, and secret-safe outputs are automated.</criterion>
  </acceptance_criteria>
  <done>Cursor and OpenCode deliver the same policy through their true Rule/plugin events without invented hook equivalence or shared-callback failure coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close lifecycle section contracts, deterministic generation, and actual-pack inventory</name>
  <reversibility rating="costly">The closed capability and package inventories are consumed by installer ownership, generated products, smoke, and release gates; changing them requires coordinated updates across those consumers.</reversibility>
  <files>src/capabilities/contracts.cts, src/generator/index.cts, src/maintainer/pack-audit.cts, kcoderag-qa/hooks/hooks.json, tests/generator/repository-generation.test.cts</files>
  <read_first>
    <file>src/capabilities/contracts.cts</file>
    <file>src/capabilities/navigation.cts</file>
    <file>src/generator/index.cts</file>
    <file>src/maintainer/pack-audit.cts</file>
    <file>package.json</file>
    <file>tests/generator/repository-generation.test.cts</file>
    <file>tests/maintainer/pack-audit.test.cts</file>
  </read_first>
  <behavior>
    - Test 1: the navigation provider's lifecycle requirements resolve to exact canonical source and installed paths with no duplicate contributor/section identifiers.
    - Test 2: generation writes the exact expected QA registration bytes once, a second generation is a no-op, and check mode reports zero drift without mutation.
    - Test 3: an actual npm tgz contains every declared Phase 5 handler/registration/Rule/skill/plugin file and no undeclared runtime, unresolved token, Python path, or extra capability.
    - Test 4: disabling navigation removes its lifecycle, result, feedback, update, and guidance contributions without damaging code-style or unrelated host entries.
  </behavior>
  <action>Extend `CapabilitySectionKind` only for separately owned native lifecycle sections actually emitted by Task 1; keep Cursor/OpenCode Rule/plugin contributions under their true kinds. Update generator asset groups and render routing for the complete Phase 5 navigation inventory, including the Plan 05-01 feedback handler when declared by the provider, the canonical hook registration, and the OpenCode plugin callback. Update the pack allow-list in the same task and regenerate `kcoderag-qa/hooks/hooks.json` from canonical TypeScript/templates; never hand-maintain other generated output. Make the repository-generation test compare provider declarations, generator groups, generated bytes, root package allow-list, and pack-audit membership so missing or orphaned assets fail together. Keep byte ordering deterministic and diagnostics path-only.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run generate &amp;&amp; npm run generate:check &amp;&amp; node --test dist-tests/generator/repository-generation.test.cjs dist-tests/maintainer/pack-audit.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every Phase 5 source handler and native registration has one provider requirement, generator route, installed projection, package member, and drift assertion.</criterion>
    <criterion>Generated `kcoderag-qa/hooks/hooks.json` is derived output whose second generation and check mode are mutation-free.</criterion>
    <criterion>D-14: actual-pack tests label launcher/handler/runtime checks PACKAGED and contain no path that can issue a LIVE PASS.</criterion>
    <criterion>Capability removal and cross-capability composition preserve independent ownership and unrelated bytes.</criterion>
    <criterion>No new runtime dependency, runtime compiler, Python path, marketplace source, or third public capability enters the package.</criterion>
  </acceptance_criteria>
  <done>The exact package inventory deterministically contains every honest host projection and rejects generated or pack drift as one closed contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- `session-start` and, only where separately managed, `session-end` values in `CapabilitySectionKind`.
- Canonical `SessionStart`/`SessionEnd` registrations in `plugin-src/hooks/hooks.json` for proven native hosts and their generated bytes in `kcoderag-qa/hooks/hooks.json`.
- Claude Code and ZCode native lifecycle projections that preserve unrelated entries and delegate to the shared dispatcher/governor.
- Cursor actual event projection retaining Rule/skill/MCP semantics and OpenCode isolated project callbacks in `plugin-src/opencode/kcoderag-nav.js`.
- Closed Phase 5 runtime/registration/guidance groups in `src/generator/index.cts` and matching actual-tgz members in `src/maintainer/pack-audit.cts`.
- New test files `tests/hosts/native-lifecycle.test.cts` and `tests/hosts/honest-events.test.cts`; expanded repository generation coverage.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native host config → adapter | Existing user-managed JSON/TOML/Rule/plugin entries are untrusted inputs to read/render merges. |
| canonical source → generated products | TypeScript/templates become installed CJS/configuration and npm members. |
| host callback → shared policy | Five distinct payload shapes cross into logical event/success normalization. |
| package MCP config → diagnostics/tests | Opaque credential-bearing native configuration must remain unread and unreported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | host-native merge and uninstall restore | high | mitigate | Preserve unrelated entries, reject unmanaged collisions/type conflicts, record exact originals/digests, and rely on one state-last transaction with injected rollback tests. |
| T-05-07 | Spoofing | native event capability claims | medium | mitigate | Register only documented/frozen fixture events and assert absent registrations for unsupported Cursor/OpenCode equivalence. |
| T-05-08 | Information Disclosure | MCP configuration and plugin callback payloads | high | mitigate | Treat values as opaque bytes, test only names/booleans/paths, use secret canaries, and forbid raw callback or config serialization. |
| T-05-09 | Denial of Service | plugin callback and native hook failures | medium | mitigate | Isolate each callback/contributor, bound processing, catch all exceptions, suppress stderr/protocol noise, and leave the host operation successful. |
| T-05-10 | Tampering | generator and actual tgz inventory | high | mitigate | Cross-check provider declarations, canonical groups, generated bytes, package allow-list, and tar membership; run generation check as a read-only drift gate. |
| T-05-SC | Tampering | npm package composition | high | mitigate | Add no third-party install; enforce the audited dependency graph, exact member allow-list, deterministic bytes, and package hash handoff to Plan 05-03. |
</threat_model>

<verification>
- `npm run build`
- `node --test dist-tests/hosts/native-lifecycle.test.cjs dist-tests/hosts/honest-events.test.cjs`
- `npm run test:cross-host`
- `npm run generate:check`
- `npm run test:pack`
</verification>

<success_criteria>
- All five hosts project the same D-01 through D-13 policy only through events they truly support.
- Codex/Claude/ZCode native entries and Cursor/OpenCode Rule/plugin paths survive repeat lifecycle and interruption without touching unrelated state.
- The canonical source, generated QA tree, package allow-list, and actual tgz form one closed drift-detecting inventory.
- PACKAGED evidence remains explicitly distinct from native-host LIVE evidence required by Plan 05-03.
</success_criteria>

<output>
Create `.planning/phases/05-hook-precision/05-02-SUMMARY.md` when done.
</output>

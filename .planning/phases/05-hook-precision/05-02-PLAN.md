---
phase: 05-hook-precision
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/hosts/claude.cts
  - src/hosts/zcode.cts
  - plugin-src/hooks/hooks.json
  - tests/hosts/native-lifecycle.test.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - plugin-src/opencode/kcoderag-nav.js
  - tests/hosts/honest-events.test.cts
autonomous: true
requirements: [HOOK-06, HOOK-07, HOOK-08, ROUT-05, TEST-08, TEST-09, TEST-11]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-06/D-07/D-09: Claude Code and ZCode consume the already-valid session-start/session-end contract and project only proven native lifecycle events through the shared governor."
    - "D-03/D-04/D-05: code-style and update fragments retain integrity/version gates and every host callback remains advisory, secret-safe and fail-open."
    - "D-10/D-11/D-12/D-13: five host payloads normalize to the same lookup/write/result/feedback semantics while Cursor remains Rule/Skill/event based and OpenCode remains project-plugin based."
    - "D-14/D-15: host adapters emit closed observation facts only; they cannot issue a receipt PASS or LIVE claim, and unsupported native surfaces remain explicit."
  artifacts:
    - path: plugin-src/hooks/hooks.json
      provides: "Canonical Claude/ZCode native SessionStart/PreToolUse/PostToolUse and receipt-gated SessionEnd registrations"
    - path: plugin-src/opencode/kcoderag-nav.js
      provides: "Project-only OpenCode callbacks for supported real events"
    - path: tests/hosts/native-lifecycle.test.cts
      provides: "Native lifecycle/source/merge/idempotency/interruption matrix"
    - path: tests/hosts/honest-events.test.cts
      provides: "Cursor Rule/event and OpenCode plugin honesty matrix"
  key_links:
    - from: src/capabilities/contracts.cts
      to: src/hosts/claude.cts
      via: "Plan 05-01 session-start/session-end section kinds are consumed without extension"
    - from: plugin-src/hooks/hooks.json
      to: src/hooks/pre-tool-dispatcher.cts
      via: "native payloads delegate to the shared normalized dispatcher"
    - from: plugin-src/opencode/kcoderag-nav.js
      to: src/hooks/mcp-call-marker.cts
      via: "supported plugin result callback records a bounded successful-call marker"
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Cursor Rule/Skill and OpenCode plugin presence must not be described as equivalent PreToolUse or SessionStart hooks."
    - "[FLAGGED-UNVERIFIED] Adapter callbacks must not write project state, expose MCP/config values, consume markers on ambiguous outcomes or invent unsupported lifecycle events."
    - "[FLAGGED-UNVERIFIED] Host projections must not emit PASS/FAIL/NOT_RUN or aggregate verdicts; receipt classification belongs to the later closed schema."
---

<objective>
Project the Plan 05-01 shared governor into Claude Code, Cursor, OpenCode and ZCode using only each host's real project-native surfaces.

Purpose: Complete host delivery semantics before deterministic generation/package closure without duplicating policy or pretending Rule/plugin behavior is a hook.
Output: Four host projections plus falsifiable native-lifecycle and honest-event tests.
</objective>

## User Story

**As a** KCodeRag Nav user, **I want to** receive the shared navigation policy through my host's real capabilities, **so that** guidance is consistent without false cross-host claims.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hook-precision/05-CONTEXT.md
@.planning/phases/05-hook-precision/05-PATTERNS.md
@.planning/phases/05-hook-precision/05-01-SUMMARY.md
@src/capabilities/contracts.cts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Project proven native lifecycle events into Claude Code and ZCode</name>
  <files>src/hosts/claude.cts, src/hosts/zcode.cts, plugin-src/hooks/hooks.json, tests/hosts/native-lifecycle.test.cts</files>
  <read_first>
    <file>src/capabilities/contracts.cts</file>
    <file>src/hosts/claude.cts</file>
    <file>src/hosts/zcode.cts</file>
    <file>plugin-src/hooks/hooks.json</file>
    <file>.planning/phases/05-hook-precision/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: Claude and ZCode merge project-native SessionStart startup/resume/clear/compact registrations without duplicate sections or unrelated-entry loss per D-01/D-02.
    - Test 2: native source/session identity reaches the shared governor; unsupported or malformed sources fail open without marker consumption per D-06/D-07.
    - Test 3: SessionEnd cleanup is registered only behind the frozen receipt proof and never broadens D-19 manual-reset authority per D-09.
    - Test 4: install/update/uninstall reruns and interrupted transaction fixtures preserve exact ownership and originals for TEST-08/TEST-11.
  </behavior>
  <action>Consume the `session-start`/`session-end` kinds added in 05-01; do not extend the contract here. Render Claude Code and ZCode project-native lifecycle entries that invoke the shared dispatcher/governor, preserve unrelated entries, use relative installed paths and fail open. Pass only stable host/session/source metadata. Keep ZCode workspace trust a user-controlled admission boundary and register cleanup only when the exact frozen receipt proves native identity semantics. Add idempotency, collision, malformed payload and interruption tests without persisting config or credentials.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hosts/native-lifecycle.test.cjs dist-tests/hosts/claude.test.cjs dist-tests/hosts/zcode.test.cjs</automated></verify>
  <acceptance_criteria>
    <criterion>The task builds using the 05-01 section-kind contract and does not modify `src/capabilities/contracts.cts`.</criterion>
    <criterion>D-01/D-02/D-06/D-07/D-09 native events and source/reset behavior are explicit and falsifiable for Claude/ZCode.</criterion>
    <criterion>TEST-08/TEST-11 lifecycle repetition/interruption preserves unrelated bytes, rollback and fail-open behavior.</criterion>
    <criterion>ZCode trust absence remains an admission fact for later NOT_RUN classification, never an adapter PASS/FAIL.</criterion>
  </acceptance_criteria>
  <done>Claude and ZCode honestly deliver the shared lifecycle policy through their supported native events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project honest Cursor Rule/events and OpenCode plugin callbacks</name>
  <files>src/hosts/cursor.cts, src/hosts/opencode.cts, plugin-src/opencode/kcoderag-nav.js, tests/hosts/honest-events.test.cts</files>
  <read_first>
    <file>src/hosts/cursor.cts</file>
    <file>src/hosts/opencode.cts</file>
    <file>plugin-src/opencode/kcoderag-nav.js</file>
    <file>src/hooks/mcp-call-marker.cts</file>
    <file>.planning/phases/05-hook-precision/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: Cursor renders Rule, Skill, MCP and supported afterMCPExecution observation wiring without SessionStart/PreToolUse claims.
    - Test 2: OpenCode installs project config/plugin only, rejects JSON/JSONC ambiguity, and records only real supported callback/tool outcomes.
    - Test 3: D-10 through D-13 normalized lookup/write/result/feedback facts match hook hosts without duplicated reminder policy.
    - Test 4: TEST-09/TEST-11 rerun/interruption fixtures preserve ownership, marker bounds and unrelated project content.
  </behavior>
  <action>Keep Cursor always-on navigation in its Rule/Skill/MCP projection and limit event wiring to the proven afterMCPExecution surface. Keep OpenCode project-only and isolate callback failure so tool success is unaffected. Both projections pass closed booleans/metadata to shared marker logic but never create receipt status. Enforce D-10–D-13 routing, structured-result and feedback eligibility through shared contracts. Test reload/plugin load, JSON/JSONC conflict, idempotency, interruption and secret canaries while explicitly rejecting hook-equivalence claims.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hosts/honest-events.test.cjs dist-tests/hosts/cursor.test.cjs dist-tests/hosts/opencode.test.cjs</automated></verify>
  <acceptance_criteria>
    <criterion>Cursor exposes Rule/Skill/MCP/afterMCPExecution only; OpenCode exposes project lifecycle and real plugin callbacks only.</criterion>
    <criterion>D-10/D-11/D-12/D-13 semantics reuse shared eligibility/governor logic and stay fail-open.</criterion>
    <criterion>TEST-09/TEST-11 idempotency and concurrency/interruption are covered without duplicate managed assets or leaked state.</criterion>
    <criterion>No projection writes receipt status, captures tool bodies or claims unsupported native events.</criterion>
  </acceptance_criteria>
  <done>Cursor and OpenCode deliver host-honest observations that later evidence can classify without inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native host config → adapter | Existing project configuration and plugin entries are untrusted merge inputs. |
| host callback → shared policy | Host-specific payloads cross into normalized event/result facts. |
| workspace trust → ZCode execution | User admission controls whether native ZCode evidence can run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | host-native merges | high | mitigate | Preserve unrelated entries, reject collisions/type conflicts and verify digest-backed rollback/idempotency. |
| T-05-02-02 | Spoofing | host capability claims | high | mitigate | Fixture-test exact native surfaces and explicitly prohibit Cursor/OpenCode hook equivalence. |
| T-05-02-03 | Information Disclosure | callback/config payloads | high | mitigate | Pass closed facts only, keep MCP/config values opaque and use secret-canary tests. |
| T-05-02-04 | Denial of Service | native callbacks | medium | mitigate | Bound parsing, catch all callback failures and leave original host operations successful. |
| T-05-02-SC | Tampering | dependencies | high | mitigate | Add no package install or runtime dependency; use Node built-ins and audited lockfile only. |
</threat_model>

## Artifacts this phase produces

- Claude Code and ZCode native lifecycle projections consuming the 05-01 section contract.
- Cursor Rule/Skill/MCP/afterMCPExecution and OpenCode project-plugin projections with explicit semantic limits.
- `plugin-src/hooks/hooks.json` and `plugin-src/opencode/kcoderag-nav.js` canonical inputs for Plan 05-03 generation.
- Native lifecycle and honest-event test matrices covering idempotency and interruption.

<verification>
- `npm run build` succeeds without any 05-02 change to `src/capabilities/contracts.cts`.
- Native lifecycle and honest-event suites pass with secret canaries and unsupported-event negatives.
- Plan declares 8 files; each task owns 4 modified files.
</verification>

<success_criteria>
- Four non-Codex hosts consume the shared policy without duplicating its state machine.
- Host projections are explicit facts, never receipt statuses or false LIVE claims.
- Canonical host sources are ready for deterministic generation in the next wave.
</success_criteria>

<output>Create `.planning/phases/05-hook-precision/05-02-SUMMARY.md` when done.</output>

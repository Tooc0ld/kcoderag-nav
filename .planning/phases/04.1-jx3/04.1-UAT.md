---
status: complete
phase: 04.1-jx3
source: [04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md, 04.1-04-SUMMARY.md, 04.1-05-SUMMARY.md, 04.1-06-SUMMARY.md, 04.1-07-SUMMARY.md, 04.1-08-SUMMARY.md, 04.1-09-SUMMARY.md, 04.1-10-SUMMARY.md, 04.1-11-SUMMARY.md, 04.1-12-SUMMARY.md, 04.1-13-SUMMARY.md]
started: 2026-08-27T04:38:43Z
updated: 2026-08-27T07:05:10Z
---

## Current Test

[testing complete]

## Tests

### 1. Current HEAD release-readiness gate
expected: Current committed implementation builds, has no dependency or generated-product drift, packs the closed npm artifact, and passes the required five-host lifecycle matrix.
result: pass
evidence: `npm run ci:local` on `0c60870` exited 0; 332/332 tests passed, generation changed/written paths were empty, pack audit reported 73 entries, and all five packed host lanes passed.

### 2. Frozen four-host native delivery baseline
expected: Claude Code 2.1.241 proves all 13 native pre-write observations; Codex 0.146.1, Cursor 3.17.8, and OpenCode 1.18.23 remain honest UNSUPPORTED lanes after successful native install/probe.
result: pass
evidence: Fresh local capture produced Claude PASS/verified with 13/13 observations. Codex and OpenCode returned UNSUPPORTED/native_context_unproved; Cursor returned UNSUPPORTED/headless_host_unsupported. A first Claude attempt missed only sentinel visibility, and an independent retry passed all fields, so the transient is retained in the acceptance record rather than hidden.

### 3. Repeatable native capture command
expected: The current capability-selecting CLI can still be invoked by the native delivery fixture instead of failing before host launch.
result: pass
evidence: Reproduction initially failed with `capability_selection_required`; commit `0c60870` adds explicit `kcoderag-navigation` selection and a regression assertion. Build, fixture tests 8/8, pack audit 73, real native capture, and the full gate passed afterward.

### 4. ZCode project lifecycle on the installed desktop machine
expected: A current local npm tarball installs project-scoped navigation into the acceptance project; update, status, and doctor remain healthy; JX3 is rejected with zero writes.
result: pass
evidence: ZCode 3.9.2.6069 is installed and responsive. The real project contains only the managed workspace config, Skill, and state; update/status/doctor exited 0, and JX3 returned `host_version_unsupported` with all three managed SHA-256 values unchanged.

### 5. ZCode 3.9.2 workspace discovery and real MCP call
expected: Open the acceptance project in ZCode; `kcoderag-qa` is connected, the `kcoderag-nav` workspace Skill is discoverable, and one read-only `search_code` call completes without file edits.
result: pass
initial_result: issue
reported: "The initial ZCode session discovered the Skill but did not mount the KCodeRag MCP tools."
severity: major
root_cause: The ZCode adapter copied the shared endpoint unchanged, but ZCode 3.9.2 does not follow the terminal-slash MCP path.
fix: Commit `f893ed9` removes only the final slash from a terminal `/mcp/` path in the ZCode projection. Full local CI passed with 332/332 tests, deterministic generation, a 73-entry pack audit, and five-host smoke PASS.
retest: pass
evidence: After a full workspace reload, the user confirmed the project MCP is connected through a successful read-only tool call, all six expected tools including `search_code` are exposed, and the workspace Skill is registered in the active session. This complements the earlier direct read-only `search_code` success without exposing endpoint or credential values.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "ZCode loads project-scoped kcoderag-qa and exposes search_code"
  status: resolved
  reason: "The initial session failed because the generated endpoint retained a terminal slash; the fixed workspace now mounts the MCP and exposes the expected tools."
  severity: major
  test: 5
  root_cause: "The ZCode adapter copied the shared endpoint unchanged although ZCode 3.9.2 requires the slash-free terminal MCP path."
  artifacts:
    - path: src/hosts/zcode.cts
      issue: "ZCode endpoint projection lacked host-specific terminal-slash normalization."
    - path: tests/hosts/zcode.test.cts
      issue: "The host projection test asserted only that the endpoint was a string, not that it had the native-compatible shape."
  missing: []
  debug_session: ".planning/debug/resolved/zcode-mcp-trailing-slash.md"
  resolved_by: "f893ed9"
  resolved_at: 2026-08-27T07:05:10Z

---
phase: 04.1-jx3
plan: "13"
type: execute
wave: 9
depends_on: ["04.1-12"]
files_modified:
  - src/core/contracts.cts
  - src/hosts/user-sources.cts
  - src/hosts/host-adapter.cts
  - src/cli/commands.cts
  - src/maintainer/retirement-audit.cts
  - tests/cli/commands.test.cts
  - tests/hosts/cursor.test.cts
  - tests/hosts/opencode.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/hooks/launcher.test.cts
  - tests/maintainer/retirement-audit.test.cts
  - tests/hosts/user-sources.test.cts
  - tests/hosts/codex.test.cts
  - tests/hosts/claude.test.cts
autonomous: true
gap_closure: true
requirements: [LEG-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-07 / LEG-01: canonical lifecycle contracts, adapter contexts, source-scan results, and CLI callers contain no legacy migration, takeover, or owned-cleanup authority seam."
    - "Selected-host source discovery remains secret-safe and read-only: every SourceFinding exposes exactly the required code, severity, sourceType, scope, and safePath metadata fields, and every scan result exposes only mode, findings, and hasConflict."
    - "Install, update, and uninstall still run the selected-host source gate before desired-state rendering or transaction entry; any manual, active, ambiguous, or retired source conflict produces zero writes."
    - "Status and doctor remain read-only, bounded, and secret-safe while reporting source_conflict as ok:false without configuration bodies, URLs, headers, or Bearer values."
    - "The retirement auditor rejects legacy authority expressed as interface members, callback declarations, object properties, or false-valued caller flags, preventing the representation-level false negative that allowed this gap."
    - "Current capability-scoped state parsing, host support policy, transaction ownership, rollback, and generated host assets retain their verified behavior."
  artifacts:
    - path: "src/core/contracts.cts"
      provides: "Exact conflict-only SourceFinding contract with required code, severity, sourceType, scope, and safePath fields"
      exports: ["SourceFinding"]
    - path: "src/hosts/user-sources.cts"
      provides: "Bounded read-only source scan with conflict-only SourceScanResult"
      exports: ["SourceScanResult", "scanSelectedHostSources", "createSourceFinding", "createSourceScanResult"]
    - path: "src/hosts/host-adapter.cts"
      provides: "Current-only observation and install/uninstall contexts without cleanup callbacks"
      exports: ["HostObservation", "HostInstallContext", "HostUninstallContext", "HostAdapter"]
    - path: "src/cli/commands.cts"
      provides: "Mutation orchestration that passes only current lifecycle context and keeps source gating before render/write"
      exports: ["executeCommand"]
    - path: "src/maintainer/retirement-audit.cts"
      provides: "Representation-aware rejection of legacy migration and cleanup authority seams"
    - path: "tests/maintainer/retirement-audit.test.cts"
      provides: "Regression fixtures for declaration-, callback-, and object-property-shaped authority seams"
  key_links:
    - from: "src/cli/commands.cts"
      to: "src/hosts/user-sources.cts"
      via: "Every mutating command runs the selected-host gate before adapter render and applyTransaction"
      pattern: "runMutationSourceGate"
    - from: "src/hosts/user-sources.cts"
      to: "src/core/contracts.cts"
      via: "Source findings are projected into the exact secret-safe metadata contract"
      pattern: "SourceFinding"
    - from: "src/cli/commands.cts"
      to: "src/hosts/host-adapter.cts"
      via: "Current-only install/update/uninstall contexts cross the controller-to-adapter boundary"
      pattern: "HostInstallContext|HostUninstallContext"
    - from: "src/maintainer/retirement-audit.cts"
      to: "src/core|src/hosts|src/cli"
      via: "Active-source retirement scan rejects authority-bearing declarations and caller properties"
      pattern: "legacy_authority_remains|cleanup_authority_remains"
---

## Phase Goal

**As a** 使用受支持 AI 编码宿主的开发者，**I want to** 按项目选择并管理导航与 JX3 提示能力，**so that** 我能在保持能力隔离和低打扰的前提下，于写代码前获得所需规范提示。

<objective>
Close the sole Phase 04.1 verification blocker by deleting the remaining compile-time legacy migration and owned-cleanup authority seams while preserving the current read-only conflict detector and zero-write hard stop per D-07 / LEG-01.

Purpose: Make the canonical TypeScript surface match the product that already ships: detect selected-host duplicates safely, report them without secrets, and refuse mutation without any migration, takeover, or cleanup capability.
Output: Narrowed core/adapter/controller contracts, representation-aware retirement enforcement, focused lifecycle regressions, and a complete local-gate proof.
</objective>

## Artifacts this phase produces

This gap plan creates no new public files, public symbols, CLI flags, schema versions, runtime dependencies, generated host assets, or migration/cleanup entry points. It narrows existing internal contracts and strengthens existing tests and the maintainer retirement audit. Build outputs may be regenerated by the normal build, but checked-in generated products must remain byte-identical unless an executor can first prove that a canonical generator input in this plan changed.

## Scope partition rationale

The fourteen declared files form one mechanically coupled deletion seam and remain inside the 52,000-token execution estimate. Task 1 owns the five production contract/audit files, Task 2 owns five direct CLI and host-fixture consumers, and Task 3 owns four exact-surface and adapter regression files. Splitting this 5/5/4 partition into separate plans would leave the narrowed interfaces and their required compile/test consumers temporarily inconsistent and duplicate the same retirement-gate context, so the single-plan boundary is intentional.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-RESEARCH.md
@.planning/phases/04.1-jx3/04.1-PATTERNS.md
@.planning/phases/04.1-jx3/04.1-VERIFICATION.md
@.planning/phases/04.1-jx3/04.1-04-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-07-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-08-SUMMARY.md
@src/core/contracts.cts
@src/hosts/user-sources.cts
@src/hosts/host-adapter.cts
@src/cli/commands.cts
@src/maintainer/retirement-audit.cts
@tests/cli/commands.test.cts
@tests/hosts/user-sources.test.cts
@tests/maintainer/retirement-audit.test.cts

<interfaces>
- `SourceFinding` is the secret-safe diagnostic boundary: required code, severity, sourceType, scope, and safePath fields only.
- `SourceScanResult` is the read-only gate result: mode, findings, and hasConflict only.
- `HostAdapter` detects and renders immutable desired state; it never owns filesystem cleanup or transaction commits.
- `executeCommand` must complete `runMutationSourceGate` before any install/update/uninstall render or `applyTransaction` call.
- `scanActiveRetirementPolicy` is the source-level backstop and must recognize authority by declaration/property shape, not merely executable function syntax.
</interfaces>
</context>

<gap_analysis>
The runtime path already refuses source conflicts without cleanup, but Plan 04 intentionally left compile-only seams for the later CLI migration work, Plan 07 removed the executable flow without deleting those seams, and Plan 08's retirement patterns matched only exported functions or assignments. The gap is therefore an execution hand-off miss plus a representation-level audit false negative, not missing architecture or missing product behavior.
</gap_analysis>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the current-only lifecycle surface pass a representation-aware retirement gate</name>
  <read_first>
    .planning/phases/04.1-jx3/04.1-VERIFICATION.md
    .planning/phases/04.1-jx3/04.1-04-SUMMARY.md
    .planning/phases/04.1-jx3/04.1-07-SUMMARY.md
    .planning/phases/04.1-jx3/04.1-08-SUMMARY.md
    src/core/contracts.cts
    src/hosts/user-sources.cts
    src/hosts/host-adapter.cts
    src/cli/commands.cts
    src/maintainer/retirement-audit.cts
  </read_first>
  <reversibility rating="reversible">The removed APIs are unpublished internal compile-time seams with no permitted callers; Git history can restore them if the locked D-07 product boundary is ever explicitly changed.</reversibility>
  <files>src/core/contracts.cts, src/hosts/user-sources.cts, src/hosts/host-adapter.cts, src/cli/commands.cts, src/maintainer/retirement-audit.cts</files>
  <behavior>
    - RED: after strengthening the active-source retirement scan but before contract cleanup, the current tree fails on legacy interface members, cleanup types/callbacks, and false-valued CLI context properties.
    - GREEN: production TypeScript compiles after those authority seams are removed, and the strengthened retirement scan passes the canonical source tree.
    - GREEN: every source finding retains exactly the five required code, severity, sourceType, scope, and safePath fields; result surfaces retain only mode, findings, and hasConflict, with no operation that can migrate, adopt, take over, delete, or clean a detected source.
    - GREEN: install/update/uninstall still reach one common pre-render source gate, while status/doctor continue to consume the same read-only findings.
  </behavior>
  <action>
    Start by extending the active-source retirement policy so it detects authority-bearing interface members, optional callback declarations, type/interface names, and object-property caller shapes, including false-valued properties; run the auditor once and retain the expected RED evidence against the current tree. Keep the auditor scoped to executable canonical source shapes so frozen historical receipts, explicit rejection tests, and read-only legacy-detection vocabulary remain valid evidence rather than false positives.

    Then implement D-07 / LEG-01 at the source boundary. Remove cleanupEligible, cleanupCommand, and cleanupFingerprint from SourceFinding while preserving required safePath alongside required code, severity, sourceType, and scope. Remove cleanupPlans, NativeCleanupPlan, and OwnedCleanupAuthority from user-sources. Remove the legacy observation members, allowLegacyUserRemoval and allowLegacyDevMigration context members, their imports, and the optional cleanupOwnedSource callback from host-adapter. Remove the two false-valued legacy properties from the CLI's shared mutation context and every production caller. Preserve createSourceFinding's exact five-required-field secret-safe projection, createSourceScanResult's mode/findings/hasConflict output, the fast/deep/gate scan modes, no-follow bounded reads, selected-host filtering, stable source_conflict diagnostics, the mutation source gate's position before render/write, current state rejection, and transaction ownership. Do not introduce a decoder, migration branch, adoption/takeover API, cleanup plan, compatibility wrapper, filesystem write, dependency, or new public flag.

    Do not edit historical Phase 04 acceptance evidence solely because it records old metadata, and do not change generated inputs or checked-in generated assets. If `generate:check` later reports drift, stop and identify a canonical changed generator input before considering regeneration; unrelated drift must remain untouched.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/bin/tsc -p tsconfig.json &amp;&amp; npm run audit:retirement</automated>
  </verify>
  <acceptance_criteria>
    - Production TypeScript compiles with the exact current-only controller, adapter, observation, source-finding, and source-result contracts; SourceFinding.safePath is required on every finding and the exact key set is code, severity, sourceType, scope, and safePath.
    - The canonical core/host/CLI surface contains none of the retired cleanup fields, cleanup authority types, legacy context members, or cleanup callback contract named by the verification gap.
    - The strengthened audit passes this tree and would fail if any named seam returned as an interface member, callback, declaration, assignment, or object property even when its value is false.
    - Read-only source classification and the common pre-render hard-stop remain reachable for every mutating command; no filesystem authority moves into an adapter or scanner.
  </acceptance_criteria>
  <done>The production contract graph is current-only from CLI through adapter and scanner, and the retirement gate can see the representation that previously escaped it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebase CLI and direct lifecycle fixtures onto the narrowed contract</name>
  <read_first>
    src/hosts/host-adapter.cts
    src/cli/commands.cts
    tests/cli/commands.test.cts
    tests/hosts/cursor.test.cts
    tests/hosts/opencode.test.cts
    tests/hosts/cross-host.test.cts
    tests/hooks/launcher.test.cts
  </read_first>
  <files>tests/cli/commands.test.cts, tests/hosts/cursor.test.cts, tests/hosts/opencode.test.cts, tests/hosts/cross-host.test.cts, tests/hooks/launcher.test.cts</files>
  <behavior>
    - CLI fake adapters receive only the current render context; install/update/uninstall happy paths still render exactly once and enter the transaction exactly once.
    - Manual, active, ambiguous, and retired selected-host findings still stop install/update/uninstall before render and before filesystem mutation.
    - Public retired authority flags remain rejected before host detection without creating an alternate compatibility route.
    - Cursor, OpenCode, cross-host coexistence, and Codex/Claude launcher setup compile and execute with current-only contexts and unchanged installed bytes.
  </behavior>
  <action>
    Update the CLI test adapter and call traces to model the narrowed HostObservation and render contexts directly, removing its unused legacy option branches and false-valued context assertions. Keep the existing retired-flag rejection cases as negative public-policy coverage, and preserve all source-gate event-order and zero-write assertions for install, update, and uninstall. Remove obsolete context properties from direct Cursor, OpenCode, cross-host uninstall, and launcher install fixtures without changing their capability selections, support receipts, expected managed paths, merge behavior, or transaction assertions.

    Add or tighten the focused CLI assertions so a conflict trace ends after selected-host scanning and never records render or transaction entry, while a clean trace records the existing render/transaction sequence using only current fields. Diagnostics must continue asserting stable codes and the required safePath on every finding rather than source contents. Do not weaken JX3's exact Claude Code support boundary, cross-host coexistence, or the advisory fail-open launcher contract.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:cli &amp;&amp; npm run test:host:cursor &amp;&amp; npm run test:host:opencode &amp;&amp; npm run test:cross-host &amp;&amp; npm run test:launcher</automated>
  </verify>
  <acceptance_criteria>
    - The complete source and test TypeScript build succeeds after all direct caller fixtures adopt the current-only interfaces.
    - Clean install/update/uninstall paths retain their existing render and atomic transaction behavior without any legacy boolean or cleanup callback.
    - Every selected-host conflict path remains secret-safe and performs zero desired-state renders and zero writes.
    - Cursor/OpenCode lifecycle behavior, four-host coexistence, and generated Codex/Claude launcher behavior remain byte- and policy-compatible with the verified phase baseline.
  </acceptance_criteria>
  <done>All direct lifecycle callers compile against the narrowed contract and prove that deletion of dead authority does not change current host behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock exact source surfaces and close LEG-01 under the full local gates</name>
  <read_first>
    src/core/contracts.cts
    src/hosts/user-sources.cts
    src/hosts/host-adapter.cts
    src/maintainer/retirement-audit.cts
    tests/maintainer/retirement-audit.test.cts
    tests/hosts/user-sources.test.cts
    tests/hosts/codex.test.cts
    tests/hosts/claude.test.cts
    package.json
  </read_first>
  <files>tests/maintainer/retirement-audit.test.cts, tests/hosts/user-sources.test.cts, tests/hosts/codex.test.cts, tests/hosts/claude.test.cts</files>
  <behavior>
    - Retirement fixtures independently fail for every removed field/type/context/callback shape and for false-valued object properties, while historical-data and rejection-test controls still pass.
    - The compiled user-source module exports no cleanup planner/runner and returns exact secret-safe finding/result keys for fast, deep, and gate scans.
    - Codex and Claude adapters expose no cleanup callback and accept only current install/uninstall contexts while preserving navigation/JX3 support behavior.
    - Full generation, real-pack, dependency, test, and required loopback smoke gates remain green without checked-in generated drift.
  </behavior>
  <action>
    Expand the retirement-audit fixture table with direct examples of each authority representation identified by verification: finding cleanup members, scan cleanup collections, cleanup plan/authority type declarations, legacy observation/context members, false-valued CLI properties, and an optional adapter cleanup callback. Assert stable legacy_authority_remains or cleanup_authority_remains codes for each fixture, plus false-positive controls for frozen evidence labels, read-only detection/rejection text, and test-only negative assertions.

    Update user-sources tests to type the compiled module from its current exports, prove every SourceFinding requires safePath and has the exact code/severity/sourceType/scope/safePath key set, prove the exact SourceScanResult key set, prove the old planner/runner exports are absent, and retain bounded no-follow, selected-host, severity, and secret-canary coverage. Remove obsolete context fields from Codex and Claude fixtures while keeping the explicit no-cleanup-callback assertion and all current capability, support receipt, section merge, digest, and rollback expectations.

    Run the focused suite, retirement audit, and `npm run ci:local`. The latter must cover the full Node tests, deterministic `generate:check`, real `pack:audit`, and `smoke:required`; record each component in the SUMMARY. Do not run the generator just to make a drift check pass. Regeneration is permitted only if a canonical generator input listed in this plan actually changed and the SUMMARY identifies that input plus every resulting checked-in path; otherwise stop on drift and preserve the user's unrelated worktree.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; node --test dist-tests/hosts/user-sources.test.cjs &amp;&amp; npm run test:host:codex &amp;&amp; npm run test:host:claude &amp;&amp; npm run audit:retirement &amp;&amp; npm run ci:local</automated>
  </verify>
  <acceptance_criteria>
    - Focused regressions prove exact current-only contracts, required safePath on every exact five-key SourceFinding, no compiled cleanup authority export/callback, conflict-only scan output, and representation-aware retirement failures.
    - `npm run audit:retirement` passes the repository and rejects every interface-, callback-, declaration-, assignment-, and object-property-shaped legacy authority fixture.
    - `npm run ci:local` passes, including deterministic generation check, real npm pack audit, and required loopback smoke; no runtime dependency, package allow-list member, or checked-in generated byte changes.
    - Git status after verification differs from the pre-task baseline only at the fourteen declared source/test paths plus expected ignored build outputs and the executor's plan SUMMARY; all unrelated dirty/untracked work remains intact.
    - The sole LEG-01 blocker is resolved without claiming Phase 05, 06, 07, or 08 behavior and without modifying plans 04.1-01 through 04.1-12.
  </acceptance_criteria>
  <done>LEG-01 is closed by source deletion, exact runtime-surface tests, a non-bypassable retirement audit, and the complete local assurance suite.</done>
</task>

</tasks>

<threat_model>
## Security profile

OWASP ASVS Level 1 applies. The blocking threshold is high: every high-severity threat below must be mitigated and proven by automated tests before this plan may complete.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project/user host configuration → source scanner | Potentially secret-bearing, malformed, oversized, linked, or special-file inputs cross into bounded read-only classification. |
| CLI controller → selected HostAdapter | Mutation authorization and current-only context cross into host-specific desired-state rendering. |
| HostAdapter desired state → shared transaction | The only permitted filesystem mutation boundary; conflicts must stop before it is reached. |
| Canonical source tree → retirement auditor | Source representations are treated as untrusted with respect to accidentally reintroducing retired authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-13-01 | Elevation of Privilege | `HostAdapter` and mutation contexts | high | mitigate | Delete migration/cleanup members and callbacks; compile all callers against the closed current-only types; make the retirement audit reject declaration and property representations. |
| T-04.1-13-02 | Tampering | CLI mutation source gate | high | mitigate | Preserve one selected-host gate before every render/transaction entry and retain install/update/uninstall zero-render, zero-write conflict tests. |
| T-04.1-13-03 | Information Disclosure | `SourceFinding` / `SourceScanResult` | high | mitigate | Enforce the exact five required SourceFinding keys including safePath, exact result keys, bounded no-follow reads, and secret-canary tests; never return config bodies, URLs, headers, Bearer values, or subprocess output. |
| T-04.1-13-04 | Repudiation | retirement audit evidence | medium | mitigate | Add independent fixtures for interface, callback, type, assignment, and object-property shapes plus false-positive controls so a green audit is attributable to closed representation coverage. |
| T-04.1-13-05 | Denial of Service | selected-host source scanning | medium | mitigate | Retain bounded file/directory/TOML/JSON inspection, stable severity classification, no-follow behavior, and fail-closed mutation gating without adding filesystem cleanup. |
| T-04.1-13-06 | Spoofing | host/capability support context | low | accept | This plan does not change host identity or receipt verification; the existing exact host-version and capability tests remain part of `ci:local`. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|--------|------|----------|--------|
| GOAL | Phase 04.1 project-scoped capability management with isolation and low interruption | Plans 01-12 remain the feature owners; Plan 13 restores the no-authority source boundary without changing behavior | COVERED |
| REQ | LEG-01 deletes old state/migration/cleanup authority while retaining read-only detection and hard-stop | Tasks 1-3 | COVERED |
| RESEARCH | Legacy map and retirement/redetection pattern: recognize safe indicators, never decode or mutate old sources | Tasks 1 and 3 | COVERED |
| CONTEXT | D-07 deletes unpublished compatibility seams and permits only read-only pre-npm source detection plus mutation refusal | Tasks 1-3 | COVERED |
| VERIFICATION | Single blocker: compile-only fields/types/callbacks/caller flags plus retirement-audit false negative | Tasks 1-3 | COVERED |

All other Phase 04.1 requirements and locked decisions retain their existing coverage in plans 04.1-01 through 04.1-12 and their passing verification truths. Deferred scanner expansion, remote registries, migration/takeover/cleanup, and later Phase 05-08 capabilities are excluded rather than gaps. No source item relevant to this gap remains unplanned.

<verification>
1. Capture `git status --short` before implementation and compare it after every task; never stage, overwrite, revert, or commit unrelated dirty/untracked paths.
2. Prove the strengthened source gate directly with `npm run audit:retirement` and its focused fixture suite.
3. Build and run the focused CLI, source scanner, four-host, cross-host, and launcher regressions named in the tasks.
4. Run `npm run ci:local`; confirm its `generate:check`, `pack:audit`, and `smoke:required` sub-gates all pass. Run no generator unless a changed canonical input from this plan first justifies it.
5. Re-run the LEG-01 checks from `04.1-VERIFICATION.md` against canonical source and compiled exports; confirm the only previously failed truth is now satisfied without weakening any already-passing truth.
</verification>

<success_criteria>
- D-07 / LEG-01 has no compile-time or runtime migration, takeover, owned-cleanup, or caller-flag authority seam in canonical product source.
- Secret-safe read-only detection retains exactly required code, severity, sourceType, scope, and safePath on every SourceFinding; selected-host conflict reporting and pre-render zero-write refusal remain unchanged for install/update/uninstall, and status/doctor remain read-only.
- Representation-aware retirement fixtures prevent the prior false negative, and all focused plus complete local gates pass.
- No new public artifact, dependency, schema, flag, generated asset, or later-phase behavior is introduced; plans 01-12 and unrelated worktree changes are untouched.
</success_criteria>

<output>
Create `.planning/phases/04.1-jx3/04.1-13-SUMMARY.md` when done.
</output>

---
phase: 04.1-jx3
plan: "07"
type: execute
wave: 4
depends_on: ["04.1-04", "04.1-05", "04.1-06"]
files_modified:
  - src/bin/kcoderag-nav.cts
  - src/cli/commands.cts
  - src/core/mutation-lock.cts
  - src/hosts/user-sources.cts
  - tests/cli/commands.test.cts
  - tests/core/mutation-lock.test.cts
  - tests/hosts/user-sources.test.cts
  - tests/migration/manual-source-conflict.test.cts
autonomous: true
requirements: [PLAT-02, LEG-01, JX3-01, TEST-10]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The first real CLI tracer runs only now, after canonical Skill, runtime/marker, capability core/state, and host projections exist."
    - "D-02 additive install computes `target = installed ∪ selected`, preflights the whole target, and commits once; both addition orders, duplicate no-op, and failed second add are proven."
    - "D-07/D-23: install, update, and uninstall all run the same selected-host read-only secret-safe manual-source gate before render/transaction and hard-stop with zero writes."
    - "Status/doctor are read-only across all capabilities; update defaults installed set; uninstall selection is always explicit/interactive."
  artifacts:
    - {path: "src/cli/commands.cts", provides: "five-command additive capability lifecycle"}
    - {path: "src/core/mutation-lock.cts", provides: "host-target concurrent mutation exclusion"}
    - {path: "tests/migration/manual-source-conflict.test.cts", provides: "negative legacy/manual lifecycle matrix"}
  key_links:
    - {from: "src/cli/commands.cts", to: "src/capabilities/compose.cts", via: "installed union selected whole-target preflight"}
    - {from: "src/cli/commands.cts", to: "src/core/transaction.cts", via: "one lock and one transaction"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "JX3 support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Expose the finished platform through the five-command CLI and execute the first real JX3 install tracer.
Purpose: Prove additive lifecycle semantics, concurrency/idempotency boundaries, and the common manual-source hard stop.
Output: Public CLI grammar/prompts/orchestration, mutation lock, source gate, and real temp-project lifecycle tests.</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-RESEARCH.md
@src/bin/kcoderag-nav.cts
@src/cli/commands.cts
@src/hosts/user-sources.cts
@tests/cli/commands.test.cts
@.planning/phases/04.1-jx3/04.1-04-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-05-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-06-SUMMARY.md
</context>

<planner_flagged_assumptions>
- PLAT-02 idempotency was unresolved. Acceptance boundary: identical canonical target returns `changed:false`, calls no transaction write, and preserves file/state bytes and mtimes.
- PLAT-02 concurrency/interruption was unresolved. Acceptance boundary: exclusive host-target lock rejects overlap with `target_busy` before writes; caught/injected failures roll back. Forced termination may leave a stale owned lock/drift that doctor reports and that blocks mutation until documented operator recovery; no stronger crash-atomicity claim is made.
</planner_flagged_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Perform the first real CLI additive JX3 install</name>
  <read_first>src/cli/commands.cts, src/bin/kcoderag-nav.cts, src/capabilities/compose.cts, src/hosts/claude.cts, tests/cli/commands.test.cts</read_first>
  <files>src/bin/kcoderag-nav.cts, src/cli/commands.cts, src/core/mutation-lock.cts, tests/cli/commands.test.cts, tests/core/mutation-lock.test.cts</files>
  <behavior>
    - Run compiled public CLI against a real temporary Claude project whose exact version is Plan 01 PASS; install navigation, then `--capability jx3-style-nudge`.
    - Additive target is exactly `installed ∪ selected`; the whole target passes support/source/ownership/drift preflight before one transaction.
    - Reverse order JX3→navigation also preserves the first capability; repeated add is no-op; failed second add makes zero writes and leaves first capability/state unchanged.
  </behavior>
  <action>Implement repeatable `--capability`, interactive host then multiselect, and additive install orchestration. Acquire the exclusive host-target lock, detect installed IDs, compute canonical union, run all gates for the complete target, render once, and call one transaction. Execute the real compiled CLI tracer only after Plans 02-06 dependencies. Test navigation→JX3, JX3→navigation, duplicate selection/no-op, unsupported/conflicting second addition, and exact original capability preservation.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:cli &amp;&amp; node --test dist-tests/core/mutation-lock.test.cjs</automated></verify>
  <acceptance_criteria>
    - Real temporary Claude CLI install contains canonical Skill/runtime/state and reports both capabilities.
    - Both additive orders use one transaction per invocation and preserve installed contributors.
    - Duplicate add is byte/mtime-stable; failed second add is zero-write with original capability healthy.</acceptance_criteria>
  <done>The actual public CLI installs JX3 additively on the mandatory supported host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same source hard-stop to every mutating command</name>
  <read_first>src/hosts/user-sources.cts, tests/hosts/user-sources.test.cts, src/cli/commands.cts, src/hosts/codex.cts, src/hosts/claude.cts, src/hosts/cursor.cts, src/hosts/opencode.cts</read_first>
  <files>src/hosts/user-sources.cts, src/cli/commands.cts, tests/hosts/user-sources.test.cts, tests/migration/manual-source-conflict.test.cts</files>
  <behavior>
    - Install, update, and uninstall each deep-scan the selected host before render/transaction and stop on manual MCP, plugin, Python hook, old state, or ambiguous/multiple sources.
    - Uninstall conflict specifically proves zero render, zero transaction, and unchanged installed state/files.
    - Findings contain only stable code/type/safe path metadata and never a cleanup command/plan/body/URL/Header/Bearer/token.
  </behavior>
  <action>Centralize one read-only selected-host source gate and invoke it from all three mutating commands, including uninstall. Use narrow known path/structural-key signatures; incomplete history becomes generic ambiguity without body inspection or ownership guesses. Never migrate, adopt, take over, delete, or auto-clean. Replace the old migration test with per-host/per-command conflict fixtures and secret canaries.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hosts/user-sources.test.cjs dist-tests/migration/manual-source-conflict.test.cjs dist-tests/cli/commands.test.cjs</automated></verify>
  <acceptance_criteria>
    - Install/update/uninstall each stop before render/transaction on every conflict class.
    - Uninstall conflict is zero-write and retains the exact existing capability state.
    - Status/doctor can report the same findings read-only without exposing config or secrets.</acceptance_criteria>
  <done>Every mutation shares one conservative manual-source authority boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete update, status, doctor, uninstall, and concurrency semantics</name>
  <read_first>src/cli/commands.cts, src/bin/kcoderag-nav.cts, tests/cli/commands.test.cts, src/core/state.cts, src/core/mutation-lock.cts</read_first>
  <files>src/bin/kcoderag-nav.cts, src/cli/commands.cts, src/core/mutation-lock.cts, tests/cli/commands.test.cts, tests/core/mutation-lock.test.cts</files>
  <behavior>
    - Update defaults to all installed capabilities and may filter repeatable IDs; it does not implicitly install an absent ID.
    - Status/doctor inspect all capabilities and are read-only, including `capability_drift` and stale marker-lock/manual-cleanup guidance data.
    - Uninstall requires explicit repeatable/all selection in automation or an interactive checklist and recomposes remaining contributors.
    - Parallel loser is `target_busy` before writes; injected failures rollback; JSON is one secret-safe document.
  </behavior>
  <action>Finish the D-02 five-command truth table, remove old environment/migration/cleanup flags/prompts, and add stable human/JSON per-capability results. Keep marker cache cleanup out of the public command set. Test all subsets/defaults, partial/final uninstall, no-op update, lock contention, stale lock reporting, caught interruption points, drift, and exact one-value JSON output.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:cli &amp;&amp; node --test dist-tests/core/mutation-lock.test.cjs</automated></verify>
  <acceptance_criteria>
    - Public command set remains exactly install/status/doctor/update/uninstall.
    - Defaults and explicit destructive selection match D-02.
    - Idempotency/concurrency/interruption boundaries are tested and no stronger guarantee is claimed.</acceptance_criteria>
  <done>The complete capability CLI is safe, additive, explicit, and deterministic.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-07-01 | Tampering | CLI selection/addition | high | mitigate | closed IDs, union target, whole-target preflight, one transaction |
| T-04.1-07-02 | Elevation/Disclosure | manual source | critical | mitigate | same read-only hard-stop for all mutations; secret canary tests |
| T-04.1-07-03 | Tampering/DoS | concurrent mutation | high | mitigate | exclusive lock, zero-write loser, rollback/stale-lock diagnostics |
</threat_model>
<verification>Run CLI, lock, source, and manual-conflict suites including the real temporary Claude install tracer.</verification>
<success_criteria>PLAT-02 additive lifecycle works and every mutating command respects the same LEG-01 hard stop.</success_criteria>

## Artifacts this phase produces
- Five-command additive capability CLI
- Host-target mutation lock
- Per-command manual-source zero-write matrix

<output>Create `.planning/phases/04.1-jx3/04.1-07-SUMMARY.md`.</output>

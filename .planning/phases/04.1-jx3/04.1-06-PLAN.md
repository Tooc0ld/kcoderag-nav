---
phase: 04.1-jx3
plan: "06"
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02", "04.1-03", "04.1-04", "04.1-05"]
files_modified:
  - src/hosts/codex.cts
  - src/hosts/claude.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - src/core/project-root.cts
  - tests/hosts/codex.test.cts
  - tests/hosts/claude.test.cts
  - tests/hosts/cursor.test.cts
  - tests/hosts/opencode.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/hooks/launcher.test.cts
autonomous: true
requirements: [PLAT-03, JX3-01, TEST-10]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-04/D-05/D-21-D-24: each host purely renders the selected capability contributions into native project roots and enables JX3 only for its exact receipt-supported version; Claude's current PASS lane is mandatory."
    - "Navigation remains installable when JX3 is unsupported; Cursor Rule/Skill and OpenCode after-event plugin are never treated as equivalent pre-write evidence."
    - "D-15 drift in SKILL.md, any of four references, handler, dispatcher, or an extra override yields silent hooks plus read-only status/doctor `capability_drift` with `ok:false`."
  artifacts:
    - {path: "src/hosts/codex.cts", provides: "Codex capability projection and structured patch normalization"}
    - {path: "src/hosts/claude.cts", provides: "mandatory-PASS Claude projection"}
    - {path: "src/hosts/cursor.cts", provides: "receipt-gated Cursor projection"}
    - {path: "src/hosts/opencode.cts", provides: "receipt-gated OpenCode projection"}
  key_links:
    - {from: "src/hosts/host-version-support.cts", to: "src/hosts/*.cts", via: "exact version and receipt digest"}
    - {from: "src/core/project-root.cts", to: "src/hooks/pre-tool-dispatcher.cts", via: "nearest exact managed state/digests"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "JX3 support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Project the complete capability set into four native hosts with exact support and integrity gates.
Purpose: Make adapters transaction-ready before the first real CLI install while preserving honest unsupported behavior.
Output: Four host projections, nearest runtime discovery, source metadata observations, and cross-host tests.</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-PATTERNS.md
@src/hosts/codex.cts
@src/hosts/claude.cts
@src/hosts/cursor.cts
@src/hosts/opencode.cts
@src/core/project-root.cts
@tests/hosts/cross-host.test.cts
@.planning/phases/04.1-jx3/04.1-01-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-05-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Render complete Codex and mandatory Claude capability states</name>
  <read_first>src/hosts/codex.cts, src/hosts/claude.cts, tests/hosts/codex.test.cts, tests/hosts/claude.test.cts, src/hosts/host-version-support.cts, src/capabilities/compose.cts</read_first>
  <files>src/hosts/codex.cts, src/hosts/claude.cts, tests/hosts/codex.test.cts, tests/hosts/claude.test.cts</files>
  <behavior>
    - Claude current PASS version renders MCP/navigation plus canonical JX3 Skill tree, one dispatcher/handler/marker, and native registration.
    - Codex JX3 renders only if its exact receipt passes; otherwise navigation-only remains valid.
    - Both adapters expose metadata/path-only manual MCP/plugin/Python/ambiguous source observations and never cleanup.
  </behavior>
  <action>Implement capability-aware detect/render/status for Codex and Claude using pure contributions and one complete desired state. Enforce Plan 01 exact support, complete D-15 asset digests, native structured target mapping, unrelated config preservation, per-capability ownership, partial removal, and safe source metadata. Require Claude's current lane to remain enabled; any loss of its PASS binding fails tests rather than degrading.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hosts/codex.test.cjs dist-tests/hosts/claude.test.cjs</automated></verify>
  <acceptance_criteria>
    - Claude both-capability desired state is deterministic and complete.
    - Codex support matches its exact receipt; unsupported JX3 preserves navigation.
    - Missing/edited each Skill reference/handler/dispatcher and extra override is `capability_drift`, hook-silent, `ok:false`.</acceptance_criteria>
  <done>Both hook-capable adapters render exact capability states and honest diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Cursor and OpenCode without false pre-write parity</name>
  <read_first>src/hosts/cursor.cts, src/hosts/opencode.cts, tests/hosts/cursor.test.cts, tests/hosts/opencode.test.cts, src/hosts/host-version-support.cts</read_first>
  <files>src/hosts/cursor.cts, src/hosts/opencode.cts, tests/hosts/cursor.test.cts, tests/hosts/opencode.test.cts</files>
  <behavior>
    - Cursor/OpenCode JX3 is enabled only by their exact PASS native pre-write receipt; Rule, Skill, plugin presence, toast, `afterMCPExecution`, and `tool.execute.after` do not qualify.
    - Project-only config selection/JSON-JSONC ambiguity and unrelated keys are preserved.
    - Unsupported JX3 returns `host_version_unsupported` before desired state while navigation remains independent.
  </behavior>
  <action>Implement capability projections and read-only status/source observations for Cursor and OpenCode. Keep success-call after events separate from pre-write delivery. If either current receipt is not PASS, render no JX3 contribution/fallback and assert rejection. Test both/single selections, partial removal, drift, project-only roots, JSON/JSONC ambiguity, and unrelated config preservation.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hosts/cursor.test.cjs dist-tests/hosts/opencode.test.cjs</automated></verify>
  <acceptance_criteria>
    - Each JX3 verdict exactly matches Plan 01 evidence.
    - Instruction/post-event-only fixtures stay unsupported.
    - Navigation and other host installations remain unaffected by JX3 rejection/removal.</acceptance_criteria>
  <done>Cursor and OpenCode projections are native, project-scoped, and evidence-honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify nearest runtime integrity and four-host coexistence</name>
  <read_first>src/core/project-root.cts, tests/hooks/launcher.test.cts, tests/hosts/cross-host.test.cts, src/core/state.cts, src/hooks/pre-tool-dispatcher.cts</read_first>
  <files>src/core/project-root.cts, tests/hooks/launcher.test.cts, tests/hosts/cross-host.test.cts</files>
  <behavior>
    - Nearest current capability state and complete managed digests resolve relative runtime paths after project move.
    - Damaged nearest state is a silent boundary and never falls through to an outer project.
    - Four hosts can coexist in one target; one-host/one-capability removal never changes another host or contributor.
  </behavior>
  <action>Update project-root bootstrap for the exact capability schema, complete Skill/runtime digest set, shared dispatcher, and optional receipt-proven SessionEnd registration. Add the four-host selection × support × drift matrix and moved/nested project fixtures. Ensure diagnostics contain only stable code/capability/safe relative path.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hooks/launcher.test.cjs dist-tests/hosts/cross-host.test.cjs</automated></verify>
  <acceptance_criteria>
    - All registry capability/host/support combinations are present.
    - Every D-15 drift fixture is silent at hook time and visible read-only in status/doctor data.
    - Relocation and nested invalid boundaries preserve project isolation.</acceptance_criteria>
  <done>Host projections are complete inputs for a real CLI lifecycle.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-06-01 | Spoofing | host version/event | high | mitigate | exact receipt-bound support and mandatory Claude PASS |
| T-04.1-06-02 | Tampering/Elevation | host config/root | critical | mitigate | declared project roots, structured merges, ambiguity/symlink/drift stops |
| T-04.1-06-03 | Disclosure | MCP config/status | high | mitigate | opaque values and path/code-only diagnostics |
</threat_model>
<verification>Run all four host, cross-host, and launcher suites against exact support receipts and complete integrity fixtures.</verification>
<success_criteria>Four native adapters are transaction-ready; unsupported JX3 never gains fallback parity.</success_criteria>

## Artifacts this phase produces
- Four capability-aware host adapters
- Capability-aware nearest project discovery
- Full support/drift/coexistence test matrix

<output>Create `.planning/phases/04.1-jx3/04.1-06-SUMMARY.md`.</output>

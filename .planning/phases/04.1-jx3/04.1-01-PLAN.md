---
phase: 04.1-jx3
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/fixtures/host-delivery.cts
  - src/hosts/host-version-support.cts
  - tests/fixtures/host-delivery.test.cts
  - fixtures/host-delivery/claude-2.1.241.json
  - fixtures/host-delivery/codex-0.146.1.json
  - fixtures/host-delivery/cursor-3.17.8.json
  - fixtures/host-delivery/opencode-1.18.23.json
autonomous: true
requirements: [PLAT-03, TEST-10]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-21/D-22/D-23: a real temporary native fixture install on Claude Code 2.1.241 reaches the first relevant C/C++/Lua write, injects model-visible context, does not block the write, and records PASS; failure blocks Phase 04.1 completion."
    - "D-24: Codex, Cursor, and OpenCode JX3 support is enabled only for an exact version with a checked-in PASS receipt; failed or unproved lanes remain unsupported while navigation remains independent."
    - "Receipts and support rows are metadata-only, exact-version, digest-bound, and never infer a minimum version."
  artifacts:
    - {path: "src/fixtures/host-delivery.cts", provides: "bounded real-host fixture runner and closed receipt validator"}
    - {path: "src/hosts/host-version-support.cts", provides: "receipt-derived exact support rows"}
    - {path: "fixtures/host-delivery/*.json", provides: "four host/version evidence verdicts"}
  key_links:
    - {from: "fixtures/host-delivery/*.json", to: "src/hosts/host-version-support.cts", via: "PASS verdict plus frozen receipt digest"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "JX3 support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>
Freeze real native delivery evidence before any host projection claims JX3 support (D-21-D-24).
Purpose: Make Claude Code the required working baseline and make all other support claims evidence-derived.
Output: Fixture runner, closed receipts, and an exact-version support table.
</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-RESEARCH.md
@src/smoke/host-smoke.cts
@tests/smoke/host-smoke.test.cts
@src/hosts/codex.cts
@src/hosts/claude.cts
@src/hosts/cursor.cts
@src/hosts/opencode.cts
</context>

<planner_flagged_assumptions>
- PLAT-03 was unclassified. Acceptance boundary: exact PASS receipts enable JX3; no documentation, current-version observation, Rule, Skill, toast, or after-event substitute is sufficient.
- TEST-10 was unclassified. Acceptance boundary: Claude is required PASS; every other host has a deterministic PASS or UNSUPPORTED/FAIL receipt, and every enabled row is receipt-bound.
</planner_flagged_assumptions>

<api_coverage>API-surface disposition is recorded in @.planning/phases/04.1-jx3/COVERAGE.md.</api_coverage>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove the current Claude Code native pre-write contract</name>
  <read_first>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts, src/hosts/claude.cts, .planning/phases/04.1-jx3/04.1-RESEARCH.md</read_first>
  <files>src/fixtures/host-delivery.cts, tests/fixtures/host-delivery.test.cts, fixtures/host-delivery/claude-2.1.241.json, src/hosts/host-version-support.cts</files>
  <behavior>
    - D-21/D-22: install a fixture-only constant advisory hook into a real temporary Claude project, start a real Claude 2.1.241 session, perform the first structured C++ create/modify and a later Lua write in the same stable session, and prove the first relevant write receives context once.
    - The receipt proves a stable session field repeats, the constant sentinel is visible in model context, and the write completes when the hook is valid, empty, malformed, nonzero, and timed out.
    - Claude receipt verdict must be PASS; fixture failure is a phase blocker, not an unsupported downgrade.
  </behavior>
  <action>Implement `src/fixtures/host-delivery.cts` as a real `--capture|--verify` CLI. For `--capture`, require the requested project path to be absent, create a fresh temporary project, locally pack the current tree, invoke the packed public `kcoderag-nav install --host claude --target &lt;project&gt; --yes` through npx to establish the native host configuration, then install only the fixture constant probe registration inside that temporary project. Launch the real host subprocess from the temp project with exactly `claude -p --output-format stream-json --include-hook-events --permission-mode acceptEdits --allowed-tools Write,Edit --no-session-persistence`; do not use `--bare`, safe-mode, or any option that disables hooks. Feed a deterministic transient prompt that performs the first C++ create/modify and a later Lua write in the same process/session. Parse stream JSON in memory to prove structured target paths, repeated stable-session field, first sentinel delivery confirmed by the model's subsequent response, no second sentinel, and completed writes under valid/empty/malformed/nonzero/timeout hook cases. Write only observed booleans, host/version, stable field names, fixture/provenance digests, timestamp, and verdict; never receipt-store prompt/output/code/path/raw session/config/subprocess data. Any real install, subprocess, observation, or cleanup failure exits nonzero. `--require-pass` makes every non-PASS Claude result nonzero.</action>
  <verify><automated>npm run build &amp;&amp; node dist/fixtures/host-delivery.cjs --capture --host claude --expected-version 2.1.241 --project .tmp/host-delivery/claude --receipt fixtures/host-delivery/claude-2.1.241.json --require-pass &amp;&amp; node --test dist-tests/fixtures/host-delivery.test.cjs &amp;&amp; node dist/fixtures/host-delivery.cjs --verify --receipt fixtures/host-delivery/claude-2.1.241.json --require-pass</automated></verify>
  <acceptance_criteria>
    - Real temp install → first C++ write → one model-visible nonblocking context, followed by silent nonblocking Lua write in the same stable session, is PASS for Claude 2.1.241.
    - Missing stable session, missing sentinel, blocked write, or any hook-failure case that blocks produces FAIL and blocks completion.
    - Receipt contains only the closed metadata schema and binds the exact support row digest.</acceptance_criteria>
  <done>Claude Code is a mandatory, evidence-backed JX3 delivery baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve Codex, Cursor, and OpenCode support without guessing</name>
  <read_first>src/fixtures/host-delivery.cts, tests/fixtures/host-delivery.test.cts, src/hosts/codex.cts, src/hosts/cursor.cts, src/hosts/opencode.cts</read_first>
  <files>src/fixtures/host-delivery.cts, tests/fixtures/host-delivery.test.cts, src/hosts/host-version-support.cts, fixtures/host-delivery/codex-0.146.1.json, fixtures/host-delivery/cursor-3.17.8.json, fixtures/host-delivery/opencode-1.18.23.json</files>
  <behavior>
    - Each host uses a real fixture and the same target/session/context/nonblocking criteria.
    - D-24: an absent, malformed, failed, unknown, lower, or digest-mismatched lane returns `host_version_unsupported` for JX3 only.
    - No lowest supported version is emitted unless that exact released version itself has a PASS receipt.
  </behavior>
  <action>Use the same capture wrapper for Codex 0.146.1, Cursor 3.17.8, and OpenCode 1.18.23. For each lane, create a fresh target, invoke the locally packed public CLI to configure that host, then invoke the real host through one adapter-owned fixed noninteractive command with `shell:false`, hidden Windows window, bounded output, and timeouts. The wrapper must actually probe/invoke the host; when no reliable headless path exists or target/session/model-context invariants cannot be directly observed, record a bounded UNSUPPORTED receipt with a stable reason code rather than PASS. Never hand-author or infer PASS from Rule/Skill/config/post-event presence. Run the exact capture commands `node dist/fixtures/host-delivery.cjs --capture --host codex --expected-version 0.146.1 --project .tmp/host-delivery/codex --receipt fixtures/host-delivery/codex-0.146.1.json`, the same form for Cursor 3.17.8 and OpenCode 1.18.23, then closed-schema verification. Generate immutable support rows solely from validated PASS receipts and retain navigation eligibility independently.</action>
  <verify><automated>npm run build &amp;&amp; node dist/fixtures/host-delivery.cjs --capture --host codex --expected-version 0.146.1 --project .tmp/host-delivery/codex --receipt fixtures/host-delivery/codex-0.146.1.json &amp;&amp; node dist/fixtures/host-delivery.cjs --capture --host cursor --expected-version 3.17.8 --project .tmp/host-delivery/cursor --receipt fixtures/host-delivery/cursor-3.17.8.json &amp;&amp; node dist/fixtures/host-delivery.cjs --capture --host opencode --expected-version 1.18.23 --project .tmp/host-delivery/opencode --receipt fixtures/host-delivery/opencode-1.18.23.json &amp;&amp; node --test dist-tests/fixtures/host-delivery.test.cjs &amp;&amp; node dist/fixtures/host-delivery.cjs --verify --receipt fixtures/host-delivery/codex-0.146.1.json &amp;&amp; node dist/fixtures/host-delivery.cjs --verify --receipt fixtures/host-delivery/cursor-3.17.8.json &amp;&amp; node dist/fixtures/host-delivery.cjs --verify --receipt fixtures/host-delivery/opencode-1.18.23.json</automated></verify>
  <acceptance_criteria>
    - All four candidate versions have a validated bounded receipt; Claude is PASS.
    - Every enabled row points to an exact PASS receipt digest; no range or inferred minimum exists.
    - Cursor/OpenCode instruction or after-event surfaces cannot satisfy native pre-write support.</acceptance_criteria>
  <done>The host support table is complete, conservative, and secret-safe.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-01-01 | Spoofing/Tampering | host executable/version | high | mitigate | fixed argv, strict parse, timeout/output bounds, exact receipt digest |
| T-04.1-01-02 | Information Disclosure | session/transcript receipt | critical | mitigate | closed metadata schema and secret/path/raw-ID canary tests |
| T-04.1-01-03 | Repudiation | support claim | high | mitigate | Claude required PASS; all enabled rows require exact native receipt |
</threat_model>

<verification>Run the compiled fixture suite and require the Claude receipt PASS.</verification>
<success_criteria>Claude native delivery is proved; every other JX3 support decision is exact-receipt-derived.</success_criteria>

## Artifacts this phase produces
- Real-host fixture runner and closed validator
- Required Claude PASS receipt and three explicit evidence receipts
- Exact-version support table

<output>Create `.planning/phases/04.1-jx3/04.1-01-SUMMARY.md`.</output>

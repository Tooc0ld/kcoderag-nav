---
phase: 04.1-jx3
plan: "03"
type: execute
wave: 1
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - src/capabilities/contracts.cts
  - src/capabilities/registry.cts
  - src/capabilities/navigation.cts
  - src/capabilities/jx3-style-nudge.cts
  - src/hosts/host-adapter.cts
  - tests/capabilities/registry.test.cts
  - tests/capabilities/providers.test.cts
autonomous: true
requirements: [PLAT-01, JX3-01, TEST-10]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-02: one frozen package-internal registry contains exactly navigation and JX3, canonicalizes selections, and rejects unknown IDs before provider/host access."
    - "D-04: providers return immutable host-neutral contribution requirements and never write; JX3 eligibility delegates to exact receipt support."
    - "Registry/provider tests are explicitly synthetic contracts; they do not perform or claim a real CLI install and do not require generated host assets."
  artifacts:
    - {path: "src/capabilities/registry.cts", provides: "closed built-in manifest and selection resolver"}
    - {path: "src/capabilities/navigation.cts", provides: "navigation contribution provider"}
    - {path: "src/capabilities/jx3-style-nudge.cts", provides: "JX3 contribution requirements and support binding"}
  key_links:
    - {from: "src/capabilities/jx3-style-nudge.cts", to: "src/hosts/host-version-support.cts", via: "exact support decision"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "JX3 support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Create the closed capability registry and pure providers without prematurely exercising the CLI.
Purpose: Establish deterministic contracts consumed by state composition, runtime, and host projection.
Output: Registry/providers, adapter contribution interface, and synthetic contract tests.</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-PATTERNS.md
@src/hosts/index.cts
@src/hosts/host-adapter.cts
@src/core/contracts.cts
@.planning/phases/04.1-jx3/04.1-01-SUMMARY.md
@.planning/phases/04.1-jx3/04.1-02-SUMMARY.md
</context>

<planner_flagged_assumptions>
- PLAT-01 was unclassified. Acceptance boundary: exact two-item frozen manifest, duplicate-free canonical selection, immutable contributions, deterministic collision inputs, and unknown-ID rejection before any host/project access.
</planner_flagged_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resolve a synthetic navigation contribution through the frozen registry</name>
  <read_first>src/hosts/index.cts, src/hosts/host-adapter.cts, src/core/contracts.cts, tests/hosts/cross-host.test.cts</read_first>
  <files>src/capabilities/contracts.cts, src/capabilities/registry.cts, src/capabilities/navigation.cts, src/hosts/host-adapter.cts, tests/capabilities/registry.test.cts</files>
  <behavior>
    - Exact IDs are frozen, lookup results are immutable copies, and order/duplicates canonicalize deterministically.
    - Unknown/empty-invalid selection fails before provider/host/project access.
    - The tracer is labeled synthetic contract coverage and returns a navigation contribution object without reading/generated installed assets.
  </behavior>
  <action>Define capability/selection/contribution/support/file/section contracts and implement the registry by the existing frozen host-registry pattern. Route one synthetic navigation selection into an immutable provider result. Keep host paths/merges in adapters and filesystem mutation outside providers. Do not invoke the public CLI or call this a real install.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/capabilities/registry.test.cjs</automated></verify>
  <acceptance_criteria>
    - Exactly two immutable built-ins exist and unknown IDs cause zero provider/host calls.
    - Synthetic navigation resolution passes without requiring generated product assets.
    - Tests clearly distinguish contract proof from later real CLI lifecycle proof.</acceptance_criteria>
  <done>The frozen registry resolves one pure provider contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind both providers to canonical assets and exact support requirements</name>
  <read_first>src/capabilities/contracts.cts, src/capabilities/registry.cts, plugin-src/capabilities/jx3-style-nudge/skill/SKILL.md, src/hosts/host-version-support.cts</read_first>
  <files>src/capabilities/navigation.cts, src/capabilities/jx3-style-nudge.cts, src/capabilities/registry.cts, tests/capabilities/providers.test.cts</files>
  <behavior>
    - Navigation declares existing MCP/Skill/hook needs independently.
    - JX3 declares canonical Skill tree plus handler/dispatcher/marker needs and requires an exact PASS support row.
    - Providers return declarations only and cannot read/write installation files or weaken unsupported results.
  </behavior>
  <action>Complete the two provider definitions using canonical source paths and receipt-bound support. Freeze/copy all arrays/maps and reject caller mutation. Test supported/unsupported decisions, manifest/provider identity, canonical Skill tree membership, and absence of write/network authority. These remain synthetic provider tests; real install is reserved for Plan 07 after runtime and host projection exist.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/capabilities/registry.test.cjs dist-tests/capabilities/providers.test.cjs</automated></verify>
  <acceptance_criteria>
    - Provider assets match the canonical sources from Plan 02.
    - JX3 cannot turn an unsupported receipt row into an eligible contribution.
    - No provider imports transaction/filesystem/network mutation APIs.</acceptance_criteria>
  <done>Both built-in providers are deterministic inputs for later composition.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-03-01 | Tampering | registry/provider inputs | high | mitigate | frozen exact IDs, copies, unknown rejection, mutation tests |
| T-04.1-03-02 | Elevation | provider authority | high | mitigate | read/render declaration interface only; no write/network imports |
| T-04.1-03-03 | Spoofing | support decision | high | mitigate | exact receipt-bound table and unsupported-default tests |
</threat_model>
<verification>Run registry and provider synthetic contract suites.</verification>
<success_criteria>Two immutable providers exist without premature CLI/install claims.</success_criteria>

## Artifacts this phase produces
- Capability contracts, frozen registry, and two pure providers
- Synthetic registry/provider test suites

<output>Create `.planning/phases/04.1-jx3/04.1-03-SUMMARY.md`.</output>

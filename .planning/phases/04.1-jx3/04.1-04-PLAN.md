---
phase: 04.1-jx3
plan: "04"
type: execute
wave: 2
depends_on: ["04.1-03"]
files_modified:
  - src/capabilities/compose.cts
  - src/core/contracts.cts
  - src/core/state.cts
  - src/core/transaction.cts
  - src/hosts/host-adapter.cts
  - src/hosts/user-sources.cts
  - src/hosts/codex.cts
  - src/hosts/claude.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - tests/capabilities/compose.test.cts
  - tests/core/transaction.test.cts
  - tests/hosts/user-sources.test.cts
  - tests/migration/legacy-state.test.cts
autonomous: true
requirements: [PLAT-01, LEG-01, TEST-10]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-03-D-06: a canonical selected capability set is fully preflighted and composed into one desired state/transaction with capability-scoped files, sections, digests, and shared contributors."
    - "Single-capability removal recomposes shared files from remaining contributors and restores originals only after the final contributor is removed."
    - "D-07: old state/QA-Dev/Python migration decoders and owned cleanup authority are deleted, not adapted; legacy/manual presence becomes conflict-only metadata."
  artifacts:
    - {path: "src/capabilities/compose.cts", provides: "deterministic contributor-aware composer"}
    - {path: "src/core/state.cts", provides: "exact capability state schema"}
    - {path: "src/core/transaction.cts", provides: "single state-last rollback boundary"}
  key_links:
    - {from: "src/capabilities/compose.cts", to: "src/core/transaction.cts", via: "one immutable DesiredState"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "JX3 support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Implement capability-scoped state/composition/transaction and delete unpublished legacy authority (D-03-D-07).
Purpose: Make later host/CLI lifecycle independently composable and set-atomic with one current schema.
Output: Composer, exact state, transaction invariants, current-only adapters, and retired legacy code/tests.</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/phases/04.1-jx3/04.1-CONTEXT.md
@.planning/phases/04.1-jx3/04.1-PATTERNS.md
@src/core/contracts.cts
@src/core/state.cts
@src/core/transaction.cts
@src/hosts/host-adapter.cts
@src/hosts/user-sources.cts
@tests/core/transaction.test.cts
@tests/migration/legacy-state.test.cts
@.planning/phases/04.1-jx3/04.1-03-SUMMARY.md
</context>

<planner_flagged_assumptions>
- LEG-01 was unclassified. Acceptance boundary: old schema, environment migration, Python assets, owned cleanup/fingerprint authority, and executable cleanup callbacks are removed; only read-only path/metadata conflict recognition survives.
</planner_flagged_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Compose two synthetic contributions into one exact transaction input</name>
  <read_first>src/core/contracts.cts, src/core/state.cts, src/core/transaction.cts, src/hosts/opencode.cts, tests/core/transaction.test.cts</read_first>
  <files>src/capabilities/compose.cts, src/core/contracts.cts, src/core/state.cts, src/core/transaction.cts, src/hosts/host-adapter.cts, tests/capabilities/compose.test.cts, tests/core/transaction.test.cts</files>
  <behavior>
    - Composition resolves all support/assets/collisions before returning and stable-sorts the canonical selected set.
    - State records capability-owned files/sections/digests and shared-file contributor IDs plus one composite digest.
    - One transaction spy sees exactly one call for the whole selected set and zero calls on collision/drift/preflight failure.
    - This tracer uses synthetic contributions and does not claim a real CLI install or require generated assets.
  </behavior>
  <action>Replace product-shaped contracts/state with an exact current capability schema and remove the core `LegacyInstallState`/decoder plus legacy/cleanup fields from `HostAdapter`. Compose whole files and structured sections deterministically, reject ambiguous collisions/path escapes/mutable inputs, and preserve opaque secret-bearing bytes. Adapt the existing transaction to contributor-aware entries without weakening lstat/symlink/special-file validation, state-last ordering, staging, or rollback. Test both/single selections, one-contributor recomposition, final restoration, and injected failure at every commit boundary.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/capabilities/compose.test.cjs dist-tests/core/transaction.test.cjs</automated></verify>
  <acceptance_criteria>
    - Synthetic two-capability composition produces one immutable desired state and transaction call.
    - Removing either contributor preserves the other and unrelated bytes.
    - Every preflight/commit failure is zero-write or exact rollback for the whole set.</acceptance_criteria>
  <done>State, composition, and transactionality are proven without premature install assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy schema, migration, and cleanup authority from all consumers</name>
  <read_first>src/core/state.cts, src/core/contracts.cts, src/hosts/host-adapter.cts, src/hosts/user-sources.cts, src/hosts/codex.cts, src/hosts/claude.cts, src/hosts/cursor.cts, src/hosts/opencode.cts</read_first>
  <files>src/hosts/user-sources.cts, src/hosts/codex.cts, src/hosts/claude.cts, src/hosts/cursor.cts, src/hosts/opencode.cts, tests/hosts/user-sources.test.cts, tests/migration/legacy-state.test.cts</files>
  <behavior>
    - `LegacyInstallState`, `parseLegacyInstallState`, QA/Dev/Python migration branches, cleanup plans/commands/fingerprints, and cleanup callbacks/flags are absent.
    - Old/manual records never become ownership and expose no deletion/adoption authority.
    - Negative publication guards for Dev/Python survive even though compatibility code is deleted.
  </behavior>
  <action>After Task 1 removes the core schema/adapter authority, delete the obsolete migration suite and remove every remaining legacy/owned-cleanup consumer branch from user-source helpers plus four adapters. Narrow observations/findings to current capability state and safe conflict metadata. Do not read config bodies, migrate, take over, delete, or produce executable cleanup guidance. Leave detailed mutation source-gate orchestration to Plan 07 after host projection is complete.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/capabilities/compose.test.cjs dist-tests/core/transaction.test.cjs dist-tests/hosts/user-sources.test.cjs</automated></verify>
  <acceptance_criteria>
    - Legacy decoder/test and all cleanup authority exports/callers are absent.
    - All four adapters compile against current capability state only.
    - Old/manual fixtures are conflict-only, immutable, sorted, and secret-safe.</acceptance_criteria>
  <done>The runtime has one state model and no legacy mutation authority.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-04-01 | Tampering/Elevation | state/contribution paths | critical | mitigate | exact schema, declared roots, lstat/symlink/special-file checks |
| T-04.1-04-02 | Tampering | shared ownership | high | mitigate | contributor IDs, composite digest, collision/recomposition tests |
| T-04.1-04-03 | Elevation/Disclosure | legacy/manual source | high | mitigate | no decoder/cleanup authority; path/code-only findings |
</threat_model>
<verification>Run composer, transaction, and source tests; verify removed legacy symbols are absent from source/compiled output.</verification>
<success_criteria>PLAT-01 atomic ownership works and LEG-01 compatibility authority is deleted.</success_criteria>

## Artifacts this phase produces
- Capability-aware state/composer/transaction
- Current-only adapter contract and conflict-only findings
- Removed obsolete migration suite and authority

<output>Create `.planning/phases/04.1-jx3/04.1-04-SUMMARY.md`.</output>

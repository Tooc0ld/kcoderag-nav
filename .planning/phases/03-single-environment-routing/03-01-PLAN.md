---
phase: 03-single-environment-routing
plan: 01
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - .planning/phases/03-single-environment-routing/03-01-SUMMARY.md
  - .planning/phases/03-single-environment-routing/03-VERIFICATION.md
  - .planning/phases/03-single-environment-routing/03-VALIDATION.md
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - HOOK-02
  - TEST-04
must_haves:
  truths:
    - "Only one installed environment is routed and QA remains the default."
    - "An unreachable target is reported explicitly and never silently falls through to the other environment."
    - "Unavailable or stale indexes permit explicit local fallback while advisory hooks remain non-blocking."
    - "Navigation ownership does not use cross-environment markers; asynchronous update/session state is bounded and fail-open."
  artifacts:
    - path: "plugin-src/routing.json"
      provides: "Single-environment routing policy"
    - path: "plugin-src/hooks/grep_nudge.py"
      provides: "Host-neutral advisory lookup behavior"
    - path: "plugin-src/hooks/update_check.py"
      provides: "Bounded asynchronous update state"
    - path: "tests/test_routing_and_hooks.py"
      provides: "Routing, unreachable, fallback, and hook behavior evidence"
    - path: "tests/test_update_check.py"
      provides: "Bounded cache/session/lock fail-open evidence"
  key_links:
    - from: "active environment"
      to: "generated skill and MCP namespace"
      via: "single-environment metadata rendering"
      pattern: "qa|dev"
    - from: "PreToolUse"
      to: "local search"
      via: "advisory context only"
      pattern: "additionalContext"
---

<objective>
Retrospectively verify the single-environment routing MVP and reconcile the original no-marker
wording with the later bounded asynchronous update cache. Keep hook precision and real-host
behavior in Phase 5–6 rather than expanding the original routing goal.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit routing, failure, and fallback semantics</name>
  <files>plugin-src/routing.json, plugin-src/hooks/grep_nudge.py, tests/test_routing_and_hooks.py</files>
  <action>Verify default QA, explicit Dev, mutual exclusion, no dual-environment routing, explicit unreachable behavior, and local fallback when indexes are unavailable or stale.</action>
  <verify>python -m unittest tests.test_routing_and_hooks -q</verify>
  <done>All single-environment and failure-routing requirements have current automated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile marker semantics and record validation</name>
  <files>plugin-src/hooks/update_check.py, tests/test_update_check.py, .planning/phases/03-single-environment-routing/03-01-SUMMARY.md, .planning/phases/03-single-environment-routing/03-VERIFICATION.md, .planning/phases/03-single-environment-routing/03-VALIDATION.md</files>
  <action>Confirm navigation nudge has no cross-environment ownership marker and asynchronous update/session markers are bounded, pruned, lock-safe, and fail-open. Record false-positive and semantic-index refinements as Phase 5 hardening.</action>
  <verify>python -m unittest tests.test_routing_and_hooks tests.test_update_check -q</verify>
  <done>Phase 3 is verified under the clarified marker contract and all six requirements are ready to mark complete.</done>
</task>

</tasks>

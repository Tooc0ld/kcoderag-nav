---
phase: 06-skill
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/skills/code-style-correction.test.cts
  - tests/skills/kcoderag-code-style.test.cts
  - dist-tests/skills/code-style-correction.test.cjs
  - tests/skills/code-style-correction.behavior.test.cts
  - tests/skills/kcoderag-code-style.behavior.test.cts
  - dist-tests/skills/code-style-correction.behavior.test.cjs
  - plugin-src/capabilities/code-style-nudge/skill/SKILL.md
  - plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml
autonomous: true
requirements: []
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: The style Skill and both source/compiled tests use only the kcoderag-code-style identity."
    - "D-06: The public grammar supports preparation and review but defines no apply operation."
    - "D-10/D-11: Codex metadata is valid and the concise Skill selectively loads exactly four existing references."
  artifacts:
    - path: "tests/skills/kcoderag-code-style.test.cts"
      provides: "Sole current style contract test"
    - path: "tests/skills/kcoderag-code-style.behavior.test.cts"
      provides: "Sole current style behavior test"
    - path: "plugin-src/capabilities/code-style-nudge/skill/SKILL.md"
      provides: "Canonical prepare/review Skill"
  key_links:
    - from: "plugin-src/capabilities/code-style-nudge/skill/SKILL.md"
      to: "plugin-src/capabilities/code-style-nudge/skill/references/"
      via: "risk-based selective reference routing"
      pattern: "cpp-lifetime-control-flow|protocol-serialization-data|lua-contracts|change-hygiene-self-review"
---

<objective>
Rename the style test surface exactly once, clean only its two obsolete compiled outputs, and define $kcoderag-code-style.

Purpose: Prevent the Node test glob from executing stale compiled tests while preserving every existing behavior assertion.
Output: Two renamed tests, two exact compiled-output removals, canonical style instructions, and Codex metadata.

Requirement fallback: D-01/D-02/D-06/D-10/D-11 are authoritative because Phase 06 has no mapped requirement IDs.
</objective>

## Artifacts this phase produces

- Sole kcoderag-code-style contract and behavior test destinations.
- Exact cross-platform cleanup of two obsolete dist-tests files.
- Canonical prepare/review Skill plus agents/openai.yaml.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-01-SUMMARY.md
@plugin-src/capabilities/code-style-nudge/skill/SKILL.md
</context>

<interfaces>
- Public name is exactly kcoderag-code-style; internal capability and handler identities are unchanged per D-01/D-07.
- Review accepts a file or current changes; actual edits remain ordinary host operations per D-06.
- The four existing reference files remain the only long-form style payload per D-11.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rename the style contract test and remove its stale compiled output</name>
  <reversibility rating="costly">The test destination becomes the sole current public contract identity.</reversibility>
  <read_first>
    <file>tests/skills/code-style-correction.test.cts</file>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
  </read_first>
  <files>tests/skills/code-style-correction.test.cts, tests/skills/kcoderag-code-style.test.cts, dist-tests/skills/code-style-correction.test.cjs</files>
  <behavior>
    - D-01/D-02: The source test moves to the exact current destination with all assertions preserved.
    - The obsolete compiled contract test is absent before any glob-based test command is allowed.
  </behavior>
  <action>Move the source test exactly, preserving all assertions. Before building, run a Node built-in fs.rmSync call with force=true against only dist-tests/skills/code-style-correction.test.cjs. Do not clean dist-tests broadly and do not run npm test. Build, then invoke only dist-tests/skills/kcoderag-code-style.test.cjs explicitly.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');fs.rmSync('dist-tests/skills/code-style-correction.test.cjs',{force:true})" &amp;&amp; npm run build &amp;&amp; node --test dist-tests/skills/kcoderag-code-style.test.cjs</automated>
  </verify>
  <done>The contract test exists only under its current source/compiled name and the obsolete compiled file is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename the behavior test and remove its stale compiled output</name>
  <read_first>
    <file>tests/skills/code-style-correction.behavior.test.cts</file>
    <file>tests/skills/kcoderag-code-style.test.cts</file>
  </read_first>
  <files>tests/skills/code-style-correction.behavior.test.cts, tests/skills/kcoderag-code-style.behavior.test.cts, dist-tests/skills/code-style-correction.behavior.test.cjs</files>
  <behavior>
    - D-02/D-11: All rule, evidence-boundary, and reference-routing assertions survive at the sole current destination.
    - Both obsolete compiled style tests are absent before later npm test glob execution.
  </behavior>
  <action>Move the behavior test exactly. Remove only dist-tests/skills/code-style-correction.behavior.test.cjs using Node fs.rmSync with force=true before building. Invoke both current compiled style tests by exact path; do not use a glob or broad output cleanup.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs');fs.rmSync('dist-tests/skills/code-style-correction.behavior.test.cjs',{force:true})" &amp;&amp; npm run build &amp;&amp; node --test dist-tests/skills/kcoderag-code-style.test.cjs dist-tests/skills/kcoderag-code-style.behavior.test.cjs</automated>
  </verify>
  <done>Both source tests and compiled executions use only the current public identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define the prepare/review Skill and Codex metadata</name>
  <read_first>
    <file>plugin-src/capabilities/code-style-nudge/skill/SKILL.md</file>
    <file>tests/skills/kcoderag-code-style.test.cts</file>
    <file>tests/skills/kcoderag-code-style.behavior.test.cts</file>
  </read_first>
  <files>plugin-src/capabilities/code-style-nudge/skill/SKILL.md, plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml, tests/skills/kcoderag-code-style.test.cts, tests/skills/kcoderag-code-style.behavior.test.cts</files>
  <behavior>
    - D-01/D-02/D-06: Frontmatter and metadata expose only kcoderag-code-style; preparation and review are supported without a public apply command.
    - D-10/D-11: Metadata satisfies the locked schema and the short entry routes selectively to four retained references.
  </behavior>
  <action>Extend the renamed tests first, then update the canonical Skill and add metadata. Keep the four reference bodies unchanged, require inspected evidence for findings, and avoid README, alias, extra references, or edit authority. Continue to invoke only the two exact current compiled tests.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/skills/kcoderag-code-style.test.cjs dist-tests/skills/kcoderag-code-style.behavior.test.cjs &amp;&amp; node -e "const fs=require('node:fs');const p=['dist-tests/skills/code-style-correction.test.cjs','dist-tests/skills/code-style-correction.behavior.test.cjs'];if(p.some(fs.existsSync))process.exit(1)"</automated>
  </verify>
  <done>$kcoderag-code-style is the sole prepare/review identity and no stale compiled test can be discovered later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source-test rename to compiled glob | Stale CJS can silently preserve retired behavior. |
| Style guidance to editing authority | Advice/review must not grant mutation authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-02-01 | Tampering | dist-tests/skills | high | mitigate | Exact Node cleanup and explicit-path tests remove only the two obsolete outputs. |
| T-06-02-02 | Elevation of Privilege | style Skill | high | mitigate | D-06 tests separate advice/review from actual edits. |
| T-06-02-03 | Information Disclosure | review output | medium | mitigate | D-06/D-11 require bounded inspected evidence and secret-safe output. |
</threat_model>

<verification>
Run both renamed tests by exact compiled path, assert both obsolete CJS files are absent, and confirm no glob-based repository test ran.
</verification>

<source_audit>
D-01/D-02/D-06/D-10/D-11 and the style portion of the phase goal are covered. Delivery support, diagnostics, generation, hosts, lifecycle, docs, and distribution are covered by Plans 06-03 through 06-13.
</source_audit>

<success_criteria>
- Both renamed tests preserve prior assertions at one current destination each.
- Exactly two obsolete compiled outputs are removed cross-platform.
- The canonical style Skill and metadata implement prepare/review without a public apply operation.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-02-SUMMARY.md when done.
</output>

---
phase: 06-skill
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - plugin-src/capabilities/code-style-nudge/skill/SKILL.md
  - plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml
  - src/capabilities/contracts.cts
  - src/capabilities/code-style-nudge.cts
  - src/hosts/host-version-support.cts
  - tests/capabilities/providers.test.cts
  - tests/skills/code-style-correction.test.cts
  - tests/skills/code-style-correction.behavior.test.cts
  - tests/skills/kcoderag-code-style.test.cts
  - tests/skills/kcoderag-code-style.behavior.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-06: The code-style public contract is named $kcoderag-code-style, supports natural-language preparation and review, defines no public apply operation, and retains no old public test identity."
    - "D-07/D-08: code-style-nudge remains the internal capability; manual Skill delivery is eligible for all five hosts while native pre-write delivery requires exact Claude Code 2.1.241 receipt evidence."
    - "D-10/D-11: The style entry has valid Codex metadata and selectively loads the four existing substantial references."
  artifacts:
    - path: "plugin-src/capabilities/code-style-nudge/skill/SKILL.md"
      provides: "Canonical prepare/review code-style contract"
    - path: "plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml"
      provides: "Codex discovery metadata for $kcoderag-code-style"
    - path: "src/hosts/host-version-support.cts"
      provides: "Single manual-versus-native delivery evidence authority"
    - path: "tests/skills/kcoderag-code-style.behavior.test.cts"
      provides: "Renamed behavior contract preserving the prior rule coverage"
  key_links:
    - from: "src/capabilities/code-style-nudge.cts"
      to: "src/hosts/host-version-support.cts"
      via: "provider consumes one immutable delivery decision"
      pattern: "manual_skill|native_pre_write|receiptDigest"
    - from: "plugin-src/capabilities/code-style-nudge/skill/SKILL.md"
      to: "plugin-src/capabilities/code-style-nudge/skill/references/"
      via: "risk-based selective loading of four retained references"
      pattern: "cpp-lifetime-control-flow|protocol-serialization-data|lua-contracts|change-hygiene-self-review"
---

<objective>
Define the fourth public Skill and separate its all-host manual delivery from receipt-gated native pre-write delivery.

Purpose: Make code-style guidance manually callable on every host without changing internal capability identity or overstating automatic support.
Output: Renamed style Skill and tests, Codex metadata, delivery decision contracts, and provider/support policy tests.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-01/D-02, D-06 through D-08, D-10, and D-11 govern this plan.
</objective>

## Artifacts this phase produces

- Canonical `$kcoderag-code-style` Skill and Codex metadata.
- Renamed style contract/behavior tests with no duplicate destinations.
- Host-neutral manual/native delivery decision while preserving the exact receipt table.
- `code-style-nudge` provider output for the manual base and optional native overlay.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-01-SUMMARY.md
@src/capabilities/contracts.cts
@src/capabilities/code-style-nudge.cts
@src/hosts/host-version-support.cts
</context>

<interfaces>
- `CapabilityId` remains exactly `"kcoderag-navigation" | "code-style-nudge"`; public Skill names are file assets (D-07).
- The support result distinguishes `deliveryMode: "manual_skill" | "native_pre_write"`; every host can receive the manual base, and only the exact frozen receipt may select the native overlay (D-08).
- Only the native branch carries `receiptDigest`; no second host/version policy or parallel state file is introduced (D-08).
- The style metadata follows the same exact schema established in Plan 06-01 and names `$kcoderag-code-style` (D-10).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the code-style contract tests to their sole destinations</name>
  <reversibility rating="costly">The public path and test identity are coordinated package contracts across every host.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>tests/skills/code-style-correction.test.cts</file>
    <file>tests/skills/code-style-correction.behavior.test.cts</file>
  </read_first>
  <files>tests/skills/code-style-correction.test.cts, tests/skills/code-style-correction.behavior.test.cts, tests/skills/kcoderag-code-style.test.cts, tests/skills/kcoderag-code-style.behavior.test.cts</files>
  <behavior>
    - D-01/D-02: Both destination filenames use `kcoderag-code-style`; both source files are removed in the same explicit rename and no duplicate test remains.
    - D-06/D-11: All prior rule, evidence-boundary, and reference-routing assertions survive the move before the new public grammar is added.
  </behavior>
  <action>Move the two existing style tests to the two `kcoderag-code-style` destination paths without dropping any existing rule, evidence, or reference assertions. These four source/destination paths are owned only by this task; every later plan must read the destinations rather than recreate or overwrite them. Remove the source files through the exact rename and do not retain alias tests or duplicate copies per D-02.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-02: Only the two renamed destination files remain in the current style-test surface.
    - D-06/D-11: Existing behavior and reference assertions remain present and passing after the move.
  </acceptance_criteria>
  <done>The old style test files have been moved exactly once to their sole current destinations with prior coverage intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the $kcoderag-code-style entry and metadata</name>
  <read_first>
    <file>plugin-src/capabilities/code-style-nudge/skill/SKILL.md</file>
    <file>plugin-src/capabilities/code-style-nudge/skill/references/cpp-lifetime-control-flow.md</file>
    <file>plugin-src/capabilities/code-style-nudge/skill/references/protocol-serialization-data.md</file>
    <file>plugin-src/capabilities/code-style-nudge/skill/references/lua-contracts.md</file>
    <file>plugin-src/capabilities/code-style-nudge/skill/references/change-hygiene-self-review.md</file>
    <file>tests/skills/kcoderag-code-style.test.cts</file>
    <file>tests/skills/kcoderag-code-style.behavior.test.cts</file>
  </read_first>
  <files>plugin-src/capabilities/code-style-nudge/skill/SKILL.md, plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml, tests/skills/kcoderag-code-style.test.cts, tests/skills/kcoderag-code-style.behavior.test.cts</files>
  <behavior>
    - D-01/D-02: Frontmatter and metadata expose only `kcoderag-code-style` and no alias.
    - D-06: Natural-language preparation and `review &lt;file-or-current-changes&gt;` are supported; actual edits remain normal host operations and no public apply subcommand is defined.
    - D-10: Metadata uses the exact public name, valid short description length, matching default prompt, and implicit invocation policy.
    - D-11: The short entry routes selectively to exactly the four existing references and reports only inspected evidence.
  </behavior>
  <action>Extend the renamed tests with failing assertions for exact frontmatter, preparation/review grammar, missing public apply command, evidence bounds, four reference routes, and metadata schema. Update the canonical Skill and add its Codex metadata. Preserve the four reference files and their substantive rule content unchanged. Do not add a README, alias, extra reference tree, or edit operation per D-02/D-06/D-10/D-11.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-02: The canonical entry and tests use only the current public identity.
    - D-06: Tests distinguish preparation, bounded review, and ordinary host editing authority.
    - D-10/D-11: Metadata is exact and the entry selectively loads all four retained references without duplication.
  </acceptance_criteria>
  <done>The canonical style entry exposes only `$kcoderag-code-style` with prepare/review behavior and exact metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Split provider support into manual base and exact-receipt native overlay</name>
  <reversibility rating="costly">The delivery union is consumed by all adapters and diagnostics, so changing it requires coordinated updates.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-PATTERNS.md</file>
    <file>src/capabilities/contracts.cts</file>
    <file>src/capabilities/code-style-nudge.cts</file>
    <file>src/hosts/host-version-support.cts</file>
    <file>tests/capabilities/providers.test.cts</file>
  </read_first>
  <files>src/capabilities/contracts.cts, src/capabilities/code-style-nudge.cts, src/hosts/host-version-support.cts, tests/capabilities/providers.test.cts</files>
  <behavior>
    - D-07: Provider and registry assertions expose only the two existing capability IDs and retain internal handler filenames.
    - D-08: Every supported host/version decision permits the manual style tree; exact Claude Code 2.1.241 alone returns `native_pre_write` and the frozen receipt digest.
    - D-08: Unknown versions and Codex/Cursor/OpenCode/ZCode return manual delivery without `host_version_unsupported` install refusal or native assets.
    - D-11: Provider inventory always contains the Skill plus four references and adds native runtime/sections only for the native branch.
  </behavior>
  <action>Write the five-host and unknown-version matrix assertions before production changes. Replace the binary style support decision with the immutable delivery union in `<interfaces>` while keeping `host-version-support.cts` as the only receipt authority. Make the `code-style-nudge` provider always render the manual Skill and four references and add handler/dispatcher/launcher/section contributions only for `native_pre_write`. Retain `code-style-nudge` as the capability ID and all internal handler names per D-07; do not infer evidence from host shape or add a non-Claude receipt per D-08.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities</automated>
  </verify>
  <acceptance_criteria>
    - D-07: Registry/provider tests still expose exactly two internal IDs and unchanged handler identities.
    - D-08: Manual delivery succeeds for all five hosts and only exact Claude yields native delivery plus receipt digest.
    - D-11: Manual and native inventories are deterministic and reference-complete.
  </acceptance_criteria>
  <done>The provider exposes one all-host manual base and one exact-Claude native overlay through a single evidence policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host/version observation to delivery decision | Missing or untrusted version strings must never grant native pre-write support. |
| Public Skill instruction to host editing | Guidance may advise, but does not acquire edit authority or define a mutation command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-02-01 | Spoofing | native delivery result | high | mitigate | D-08 matrix tests require exact Claude version and frozen digest before native selection. |
| T-06-02-02 | Elevation of Privilege | style Skill invocation | high | mitigate | D-06 tests separate guidance/review from normal host editing authority and define no public mutation subcommand. |
| T-06-02-03 | Tampering | renamed tests and public path | medium | mitigate | One task owns both source removals and both destinations; later plans consume the renamed tests without rewriting them. |
| T-06-02-04 | Information Disclosure | review output | medium | mitigate | D-06/D-11 require bounded evidence and prohibit configuration or credential exposure. |
</threat_model>

<verification>
1. Build and run capability plus full repository tests using existing scripts.
2. Confirm the renamed style tests pass from their sole destinations.
3. Assert the five-host support matrix and exact Claude receipt branch.
4. Assert the provider remains `code-style-nudge` and preserves the four reference paths and internal handlers.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 02

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | Fourth public Skill and split delivery | COVERED | Tasks 1-2 |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback |
| RESEARCH | — | No RESEARCH.md; use current style provider and receipt patterns | COVERED | read_first and 06-PATTERNS.md |
| CONTEXT | D-01–D-02 | Exact style name and no alias | COVERED | Task 1 |
| CONTEXT | D-06 | Preparation/review with no public apply operation | COVERED | Task 1 |
| CONTEXT | D-07–D-08 | Two capabilities and split delivery | COVERED | Task 2 |
| CONTEXT | D-10–D-11 | Metadata and selective references | COVERED | Task 1 |
| CONTEXT | D-03–D-05/D-09/D-12/D-13 | Other Skills, diagnostics, lifecycle, distribution | COVERED ELSEWHERE | Plans 06-01 and 06-03 through 06-08 |

Deferred ideas remain excluded by the locked decisions.
</source_audit>

<success_criteria>
- `$kcoderag-code-style` is the sole public style identity and its old tests are renamed once without destination conflicts.
- Preparation and review behavior is bounded and no public edit command is introduced.
- Manual style is eligible on every host while native pre-write remains exact-Claude-only.
- Provider and registry behavior retain two internal capabilities and four existing references.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-02-SUMMARY.md` when done.
</output>

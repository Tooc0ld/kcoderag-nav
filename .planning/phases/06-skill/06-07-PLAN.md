---
phase: 06-skill
plan: "07"
type: execute
wave: 7
depends_on:
  - 06-06
files_modified:
  - package.json
  - src/maintainer/pack-audit.cts
  - tests/maintainer/pack-audit.test.cts
  - src/maintainer/native-host-driver.cts
  - src/smoke/host-smoke.cts
  - tests/smoke/host-smoke.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-07/D-13: The exact npm archive contains all four current public Skill trees under exactly two internal capabilities and rejects missing, extra, or retired assets."
    - "D-08/D-09/D-13: One acquired package installs manual style on all five hosts and reports automatic availability only for exact Claude Code 2.1.241."
    - "D-12/D-13: Packaged smoke covers style-only/combined install, update, repeat update, selective uninstall, --all, drift, source conflict, rollback, and unrelated-content preservation."
    - "D-13: Packaged evidence stays PACKAGED and no test, driver, or receipt promotes direct launcher execution to LIVE host evidence."
  artifacts:
    - path: "src/maintainer/pack-audit.cts"
      provides: "Exact archive member inventory for current Skill assets"
    - path: "src/smoke/host-smoke.cts"
      provides: "Five-host packaged delivery and lifecycle evidence"
    - path: "src/maintainer/native-host-driver.cts"
      provides: "Host-driver assertions preserving PACKAGED versus LIVE evidence semantics"
  key_links:
    - from: "src/maintainer/pack-audit.cts"
      to: "package.json"
      via: "exact required assets agree with the npm files allow-list"
      pattern: "REQUIRED_PACKAGE_ASSETS|files"
    - from: "src/smoke/host-smoke.cts"
      to: "dist/bin/kcoderag-nav.cjs"
      via: "one acquired tgz drives install/status/doctor/update/uninstall fixtures"
      pattern: "manualSkill|automaticNudge"
---

<objective>
Bind the exact npm archive and required five-host packaged smoke to the four-public/two-internal delivery contract.

Purpose: Prove that the actual distributable artifact installs and manages manual style everywhere while keeping native evidence exact-Claude-only.
Output: Closed package inventory, five-host style lifecycle smoke, and unchanged PACKAGED/LIVE evidence boundaries.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-01/D-02, D-07 through D-09, D-12, and D-13 govern this plan.
</objective>

## Artifacts this phase produces

- Exact package allow-list for canonical, generated, metadata, reference, and runtime assets.
- Pack tests rejecting missing, extra, unresolved, retired, or credential-like members.
- Required five-host packaged receipts for style-only and combined lifecycle.
- Native-host driver assertions that preserve evidence classification.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-06-SUMMARY.md
@.planning/phases/05-hook-precision/05-04-SUMMARY.md
@src/maintainer/pack-audit.cts
@src/smoke/host-smoke.cts
@src/maintainer/native-host-driver.cts
@package.json
</context>

<interfaces>
- The archive path set is closed: every expected canonical/generated Skill, metadata, reference, and runtime member is present and every unexpected member is rejected (D-13).
- Required smoke consumes one exact package artifact and records PACKAGED behavior only; native LIVE evidence remains the completed Phase 05 responsibility (D-13).
- Smoke expects `available/available` only for exact Claude and `available/unsupported` for manual-only hosts, with other enum states exercised by negative fixtures (D-08/D-09).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the npm package inventory around four public Skills</name>
  <reversibility rating="costly">Archive member paths are public distribution contracts consumed by every adapter and smoke fixture.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-06-SUMMARY.md</file>
    <file>package.json</file>
    <file>src/maintainer/pack-audit.cts</file>
    <file>tests/maintainer/pack-audit.test.cts</file>
  </read_first>
  <files>package.json, src/maintainer/pack-audit.cts, tests/maintainer/pack-audit.test.cts</files>
  <behavior>
    - D-01/D-02: The tgz contains only the four current public Skill roots and no retired path or compatibility asset.
    - D-07/D-10/D-11: Inventory includes all required metadata and four style references while retaining exactly two capability sources.
    - D-13: Missing, extra, unresolved-template, and credential-like members fail the existing audit.
  </behavior>
  <action>Write failing exact-archive assertions before changing the allow-lists. Update `package.json` files entries and `REQUIRED_PACKAGE_ASSETS` with every canonical/generated Skill, Codex metadata, style reference, and runtime consumed by the five adapters. Keep the archive set exact and deterministic, and preserve unresolved-token and secret-pattern rejection. Assert both retired public asset paths are absent through the current-surface audit installed in Plan 06-06 rather than duplicating another scanner. Add no production dependency and perform no publish, tag, dist-tag, or registry mutation per D-13.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:pack &amp;&amp; npm run pack:audit</automated>
  </verify>
  <acceptance_criteria>
    - The actual dry-run archive has the exact current member set.
    - Metadata/reference/runtime members are complete for all host projections.
    - Missing, extra, template, retired, and secret fixtures fail closed.
  </acceptance_criteria>
  <done>The npm artifact has one exact, secret-safe four-Skill/two-capability inventory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the delivery matrix through one acquired package on five hosts</name>
  <read_first>
    <file>.planning/phases/05-hook-precision/05-04-SUMMARY.md</file>
    <file>.planning/phases/06-skill/06-05-SUMMARY.md</file>
    <file>src/smoke/host-smoke.cts</file>
    <file>tests/smoke/host-smoke.test.cts</file>
    <file>src/maintainer/native-host-driver.cts</file>
  </read_first>
  <files>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts, src/maintainer/native-host-driver.cts</files>
  <behavior>
    - D-08/D-09: Codex, Claude, Cursor, OpenCode, and ZCode all install manual style; exact Claude alone owns and reports the native overlay.
    - D-12: Each host passes style-only and combined install/status/doctor/update/repeat-update/selective-uninstall/`--all` with sibling and unrelated bytes preserved.
    - D-12/D-13: Drift, source conflict, unsafe input, and injected transaction failures remain write-before-stop or fully rolled back from the package.
    - D-13: Receipts contain metadata only and distinguish PACKAGED from LIVE evidence without manufacturing host events.
  </behavior>
  <action>Write the five-host delivery table and lifecycle assertions before updating the harness. Replace the old non-receipt install-refusal branch with a shared manual-success flow and layer native Hook execution/receipt assertions only onto exact Claude Code 2.1.241. For every host, exercise style-only and combined install, both diagnostic formats, targeted and repeated update, selective uninstall with sibling byte equality, and full uninstall with unrelated configuration restored. Retain existing acquisition, runtime-drift, source-conflict, transaction, secret-safe receipt, and fail-open probes. Update the native-host driver only for renamed paths/fields; do not promote direct execution or generated bytes to LIVE evidence and do not perform a network publish.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:smoke &amp;&amp; npm run smoke:required</automated>
  </verify>
  <acceptance_criteria>
    - Required packaged smoke passes manual style lifecycle on all five hosts.
    - Exact Claude alone owns and exercises the native automatic overlay.
    - Diagnostics, partial/full lifecycle, rollback, source, drift, and secret-safe receipt probes pass.
    - Evidence remains PACKAGED and no external publication occurs.
  </acceptance_criteria>
  <done>One acquired package proves five-host manual style and exact-Claude automatic behavior through the complete managed lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository tree to npm archive | The exact allow-list controls executable and instructional bytes distributed to users. |
| Acquired tgz to smoke projects | Packaged code mutates synthetic host projects and produces evidence receipts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-07-01 | Information Disclosure | npm archive/receipts | high | mitigate | Exact inventory, credential scans, and metadata-only receipt tests reject secret-bearing content. |
| T-06-07-02 | Spoofing | native delivery smoke | high | mitigate | D-08/D-09 require exact receipt-backed installed ownership and prohibit PACKAGED-to-LIVE promotion. |
| T-06-07-03 | Tampering | lifecycle from tgz | high | mitigate | D-12 smoke covers expected digests, source gates, rollback, and unrelated byte preservation. |
| T-06-07-04 | Repudiation | archive member set | medium | mitigate | Closed allow-list and dry-run tests record every accepted member and reject additions. |
| T-06-07-05 | Denial of Service | required smoke | low | accept | The existing serialized, bounded required-contract mode is retained with no foreground external dependency. |
</threat_model>

<verification>
1. Run existing pack test/audit scripts against the actual archive.
2. Run existing smoke test and required-contract scripts against one exact acquired artifact.
3. Confirm all five host receipts prove manual style and only exact Claude proves native automatic delivery.
4. Confirm lifecycle safety, drift, conflict, rollback, and secret-safe evidence probes pass.
5. Confirm no publish, tag, dist-tag, workflow dispatch, or LIVE evidence mutation occurs.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 07

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | Packaged five-host delivery proof | COVERED | Tasks 1-2 |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback |
| RESEARCH | — | No RESEARCH.md; use current pack/smoke patterns | COVERED | read_first and 06-PATTERNS.md |
| CONTEXT | D-01–D-02 | Exact current archive names | COVERED | Task 1 |
| CONTEXT | D-07–D-11 | Capability, delivery, diagnostics, metadata, references | COVERED | Tasks 1-2 |
| CONTEXT | D-12 | Managed lifecycle safety | COVERED | Task 2 |
| CONTEXT | D-13 | Pack and five-host smoke evidence | COVERED | Tasks 1-2 |
| CONTEXT | D-03–D-06 | Skill behavior | COVERED UPSTREAM | Plans 06-01/02/06 |

Deferred publishing and new native support lanes remain excluded.
</source_audit>

<success_criteria>
- Exact pack inventory contains every current Skill asset and no retired or unexpected member.
- Five-host required smoke proves manual style and independent diagnostics from one acquired package.
- Exact Claude alone proves the automatic native overlay.
- Lifecycle, rollback, source, drift, and secret boundaries remain passing and evidence stays PACKAGED.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-07-SUMMARY.md` when done.
</output>

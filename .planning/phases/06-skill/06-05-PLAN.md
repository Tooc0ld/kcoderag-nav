---
phase: 06-skill
plan: "05"
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - src/capabilities/compose.cts
  - tests/capabilities/compose.test.cts
  - tests/core/transaction.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/hosts/native-lifecycle.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-12: Update removes or restores only retired paths explicitly owned by a valid current state and never scans for similarly named user files."
    - "D-12: Drift, symlinks, special files, source ambiguity, or injected transaction failure causes zero partial mutation or complete rollback."
    - "D-07/D-12: Capability-selective uninstall preserves its sibling byte-for-byte and --all restores unrelated host configuration across all five hosts."
    - "D-08/D-09: Style-only and combined lifecycle transitions retain manual availability everywhere and native availability only for exact Claude evidence."
  artifacts:
    - path: "src/capabilities/compose.cts"
      provides: "Digest-guarded previous-owned-path reconciliation"
    - path: "tests/hosts/cross-host.test.cts"
      provides: "Five-host rename, isolation, and selective lifecycle proof"
    - path: "tests/core/transaction.test.cts"
      provides: "Unsafe-path and rollback regression proof"
  key_links:
    - from: "src/capabilities/compose.cts"
      to: "src/core/transaction.cts"
      via: "complete desired-state entries carry expected digests and state remains last"
      pattern: "previousState|expectedDigest|stateEntry"
    - from: "tests/hosts/cross-host.test.cts"
      to: "five HostAdapter implementations"
      via: "style-only and combined install/update/uninstall fixtures"
      pattern: "codex|claude|cursor|opencode|zcode"
---

<objective>
Prove that public Skill path renames and five-host lifecycle operations stay within current ownership and atomic transaction boundaries.

Purpose: Upgrade installed projects without aliases, broad deletion, partial writes, or damage to sibling capabilities, hosts, or user configuration.
Output: Owned-path compose behavior plus cross-host update/uninstall, unsafe-path, and rollback tests.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-02, D-07 through D-09, and D-12 govern this plan.
</objective>

## Artifacts this phase produces

- Exact previous-owned-path reconciliation in the complete desired-state composer.
- Current schema-version-1 rename fixtures for both retired public paths.
- Five-host style-only/combined update and uninstall proofs.
- Drift, symlink, special-file, ambiguity, rollback, sibling, and unrelated-content regressions.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-04-SUMMARY.md
@src/capabilities/compose.cts
@src/core/state.cts
@src/core/transaction.cts
</context>

<interfaces>
- Previous files become restore/delete entries only when valid current state names the exact path and expected digest; unmanaged lookalikes grant no authority (D-02/D-12).
- `applyTransaction` remains the only writer, preflights every entry before staging, commits state last, and rolls back the selected host as one unit (D-12).
- Capability arrays remain a subset of the two existing IDs; style-only never implies navigation and selective uninstall never removes the sibling contributor (D-07/D-12).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile only state-owned retired Skill paths</name>
  <reversibility rating="costly">The reconciliation rule governs updates for already installed projects and must preserve their originals and digests.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>src/capabilities/compose.cts</file>
    <file>src/core/state.cts</file>
    <file>src/core/transaction.cts</file>
    <file>tests/capabilities/compose.test.cts</file>
  </read_first>
  <files>src/capabilities/compose.cts, tests/capabilities/compose.test.cts</files>
  <behavior>
    - D-02/D-12: A valid state owning either retired public path produces an expected-digest restoration/deletion entry and the corresponding new contributor path.
    - D-02: An unmanaged same-name or lookalike path is preserved or causes the existing conflict refusal; it is never adopted or broadly deleted.
    - D-12: Complete desired state remains deterministic and state is the final transaction entry.
  </behavior>
  <action>Create current-state fixtures that own the two retired public Skill paths and unrelated canaries. Write failing compose assertions for exact old-to-new restoration/deletion entries, expected digests, contributor IDs, deterministic order, and state-last placement. Adjust composition only where needed to consume the existing previous-owned-file mechanism for the new provider paths. Preserve schema, source gates, ambiguous ownership refusal, and originals; do not add migration, adoption, recursive removal, or path-name scans per D-02/D-12.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities</automated>
  </verify>
  <acceptance_criteria>
    - Exact state-owned retired paths reconcile to the four public Skill model.
    - Unmanaged paths never become deletion authority.
    - Entries carry expected digests and state remains last.
  </acceptance_criteria>
  <done>The composer safely replaces only retired paths proven owned by current state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove atomic five-host update and uninstall behavior</name>
  <reversibility rating="costly">These tests lock the installed-project safety contract for all five host formats.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-04-SUMMARY.md</file>
    <file>src/core/transaction.cts</file>
    <file>tests/core/transaction.test.cts</file>
    <file>tests/hosts/cross-host.test.cts</file>
    <file>tests/hosts/native-lifecycle.test.cts</file>
  </read_first>
  <files>tests/core/transaction.test.cts, tests/hosts/cross-host.test.cts, tests/hosts/native-lifecycle.test.cts</files>
  <behavior>
    - D-12: Install, update, repeat update, capability-selective uninstall, and `--all` preserve unrelated host files/sections and sibling hosts.
    - D-07/D-12: Style-only and combined states contain only the selected existing capability IDs; selective uninstall preserves the sibling bytes exactly.
    - D-02/D-12: Drift, symlink, special file, manual/ambiguous source, and injected failure before or after staging stop before writes or fully roll back state and files.
    - D-08/D-09: Delivery diagnostics remain correct after every transition on all five hosts.
  </behavior>
  <action>Extend existing transaction, native-lifecycle, and cross-host fixtures with state-owned retired paths, style-only and combined states, and unrelated canary bytes. Exercise every lifecycle transition across Codex, Claude, Cursor, OpenCode, and ZCode. Inject drift, symlink, special-file, source ambiguity, and transaction failures at existing seams; compare complete before/after trees to prove write-before-stop or rollback. Assert the four manual Skill paths, two capability IDs, exact-Claude native overlay, manual-only host absence, delivery enums, sibling preservation, and other-host isolation per D-07/D-08/D-09/D-12. Do not alter the transaction implementation unless a failing existing invariant exposes a scoped defect; this plan's declared production ownership remains the composer.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:transaction &amp;&amp; npm run test:cross-host &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - All five hosts pass style-only and combined update/uninstall transitions.
    - Refusal probes make no writes and failure probes fully restore the selected host.
    - Selective uninstall preserves the sibling capability and other hosts byte-for-byte; `--all` restores user originals.
    - Manual/native diagnostic values remain correct after each transition.
  </acceptance_criteria>
  <done>Five-host lifecycle safely performs the public rename and capability changes without broad deletion, partial writes, or cross-host damage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing state to deletion/restoration authority | Only fully validated paths, originals, contributors, and digests may authorize mutation. |
| Desired state to filesystem transaction | Multi-file and multi-section changes cross the sole atomic write boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-05-01 | Tampering | retired-path reconciliation | high | mitigate | D-02/D-12 require exact current-state ownership, expected digests, preflight, and no directory scan. |
| T-06-05-02 | Elevation of Privilege | unmanaged lookalike paths | high | mitigate | Tests preserve or refuse paths absent from the validated ownership graph. |
| T-06-05-03 | Denial of Service | transaction failure | medium | mitigate | Inject failures before/after staging and compare full trees to prove complete rollback. |
| T-06-05-04 | Repudiation | selective lifecycle | medium | mitigate | State contributor records and byte comparisons identify exactly which capability and host changed. |
| T-06-05-05 | Information Disclosure | configuration preservation tests | low | accept | Fixtures use synthetic canaries and diagnostics remain path-only; no real configuration values are captured. |
</threat_model>

<verification>
1. Run existing capability, transaction, cross-host, and full test scripts.
2. Confirm exact state ownership is required for retired-path reconciliation.
3. Exercise all five lifecycle transitions for style-only and combined states.
4. Confirm unsafe and injected failure cases leave no partial writes.
5. Confirm delivery diagnostics and sibling/other-host byte preservation after every transition.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 05

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | Safe upgrade/uninstall of public Skill paths | COVERED | Tasks 1-2 |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback |
| RESEARCH | — | No RESEARCH.md; use current compose/transaction patterns | COVERED | read_first and 06-PATTERNS.md |
| CONTEXT | D-02 | No alias and ownership-safe rename | COVERED | Task 1 |
| CONTEXT | D-07–D-09 | Two capabilities and delivery state through transitions | COVERED | Task 2 |
| CONTEXT | D-12 | Source, drift, unsafe-path, atomic, and rollback boundaries | COVERED | Tasks 1-2 |
| CONTEXT | D-01/D-03–D-06/D-10–D-11/D-13 | Public contracts and distribution | COVERED ELSEWHERE | Plans 06-01 through 06-04 and 06-06 through 06-08 |

Deferred migration/adoption/cleanup and broad deletion remain excluded.
</source_audit>

<success_criteria>
- Only validated state-owned retired paths are reconciled.
- All five hosts pass fresh, update, repeat update, selective uninstall, and full uninstall flows.
- Unsafe files, drift, ambiguity, and failures make zero partial changes or fully roll back.
- Sibling capabilities, other hosts, and unrelated user configuration are preserved.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-05-SUMMARY.md` when done.
</output>

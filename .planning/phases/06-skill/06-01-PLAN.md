---
phase: 06-skill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - AGENTS.md
  - README.md
  - dist-tests/hooks/jx3-style-nudge.test.cjs
  - dist-tests/skills/code-style-correction.behavior.test.cjs
  - dist-tests/skills/code-style-correction.test.cjs
  - docs/MCP_QA_EXPERIENCE_GUIDE.md
  - kcoderag-cursor/README.md
  - kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
  - kcoderag-cursor/skills/code-style-correction/SKILL.md
  - kcoderag-cursor/skills/code-style-correction/references/change-hygiene-self-review.md
  - kcoderag-cursor/skills/code-style-correction/references/cpp-lifetime-control-flow.md
  - kcoderag-cursor/skills/code-style-correction/references/lua-contracts.md
  - kcoderag-cursor/skills/code-style-correction/references/protocol-serialization-data.md
  - kcoderag-cursor/skills/kcoderag-code-style/SKILL.md
  - kcoderag-cursor/skills/kcoderag-code-style/references/change-hygiene-self-review.md
  - kcoderag-cursor/skills/kcoderag-code-style/references/cpp-lifetime-control-flow.md
  - kcoderag-cursor/skills/kcoderag-code-style/references/lua-contracts.md
  - kcoderag-cursor/skills/kcoderag-code-style/references/protocol-serialization-data.md
  - kcoderag-cursor/skills/kcoderag-feedback/SKILL.md
  - kcoderag-cursor/skills/kcoderag-manage/SKILL.md
  - kcoderag-cursor/skills/kcoderag/SKILL.md
  - kcoderag-qa/README.md
  - kcoderag-qa/hooks/code-style-nudge.cjs
  - kcoderag-qa/hooks/pre-tool-dispatcher.cjs
  - kcoderag-qa/skills/code-lookup-discipline/SKILL.md
  - kcoderag-qa/skills/code-style-correction/SKILL.md
  - kcoderag-qa/skills/code-style-correction/references/change-hygiene-self-review.md
  - kcoderag-qa/skills/code-style-correction/references/cpp-lifetime-control-flow.md
  - kcoderag-qa/skills/code-style-correction/references/lua-contracts.md
  - kcoderag-qa/skills/code-style-correction/references/protocol-serialization-data.md
  - kcoderag-qa/skills/kcoderag-code-style/SKILL.md
  - kcoderag-qa/skills/kcoderag-code-style/agents/openai.yaml
  - kcoderag-qa/skills/kcoderag-code-style/references/change-hygiene-self-review.md
  - kcoderag-qa/skills/kcoderag-code-style/references/cpp-lifetime-control-flow.md
  - kcoderag-qa/skills/kcoderag-code-style/references/lua-contracts.md
  - kcoderag-qa/skills/kcoderag-code-style/references/protocol-serialization-data.md
  - kcoderag-qa/skills/kcoderag-feedback/SKILL.md
  - kcoderag-qa/skills/kcoderag-feedback/agents/openai.yaml
  - kcoderag-qa/skills/kcoderag-manage/SKILL.md
  - kcoderag-qa/skills/kcoderag-manage/agents/openai.yaml
  - kcoderag-qa/skills/kcoderag/SKILL.md
  - kcoderag-qa/skills/kcoderag/agents/openai.yaml
  - package.json
  - plugin-src/README.md.tmpl
  - plugin-src/capabilities/code-style-nudge/skill/SKILL.md
  - plugin-src/capabilities/code-style-nudge/skill/agents/openai.yaml
  - plugin-src/cursor/README.md.tmpl
  - plugin-src/skills/code-lookup-discipline/SKILL.md
  - plugin-src/skills/kcoderag-feedback/SKILL.md
  - plugin-src/skills/kcoderag-feedback/agents/openai.yaml
  - plugin-src/skills/kcoderag-manage/SKILL.md
  - plugin-src/skills/kcoderag-manage/agents/openai.yaml
  - plugin-src/skills/kcoderag/SKILL.md
  - plugin-src/skills/kcoderag/agents/openai.yaml
  - src/capabilities/code-style-nudge.cts
  - src/capabilities/compose.cts
  - src/capabilities/contracts.cts
  - src/capabilities/navigation.cts
  - src/cli/commands.cts
  - src/core/contracts.cts
  - src/core/state.cts
  - src/generator/index.cts
  - src/hooks/code-style-nudge.cts
  - src/hooks/pre-tool-dispatcher.cts
  - src/hosts/claude.cts
  - src/hosts/codex.cts
  - src/hosts/cursor.cts
  - src/hosts/host-version-support.cts
  - src/hosts/opencode.cts
  - src/hosts/zcode.cts
  - src/maintainer/docs-check.cts
  - src/maintainer/native-host-driver.cts
  - src/maintainer/pack-audit.cts
  - src/maintainer/pre-commit.cts
  - src/maintainer/retirement-audit.cts
  - src/smoke/host-smoke.cts
  - tests/capabilities/compose.test.cts
  - tests/capabilities/providers.test.cts
  - tests/capabilities/registry.test.cts
  - tests/cli/commands.test.cts
  - tests/core/transaction.test.cts
  - tests/generator/cursor-product.test.cts
  - tests/generator/generation.test.cts
  - tests/generator/qa-product.test.cts
  - tests/generator/repository-generation.test.cts
  - tests/hooks/code-style-nudge.test.cts
  - tests/hooks/pre-tool-dispatcher.test.cts
  - tests/hooks/session-start.test.cts
  - tests/hosts/claude.test.cts
  - tests/hosts/codex.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/hosts/cursor.test.cts
  - tests/hosts/honest-events.test.cts
  - tests/hosts/native-lifecycle.test.cts
  - tests/hosts/opencode.test.cts
  - tests/hosts/zcode.test.cts
  - tests/maintainer/docs-check.test.cts
  - tests/maintainer/native-host-driver.test.cts
  - tests/maintainer/pack-audit.test.cts
  - tests/maintainer/pre-commit.test.cts
  - tests/maintainer/release.test.cts
  - tests/maintainer/retirement-audit.test.cts
  - tests/skills/code-style-correction.behavior.test.cts
  - tests/skills/code-style-correction.test.cts
  - tests/skills/kcoderag-code-style.behavior.test.cts
  - tests/skills/kcoderag-code-style.test.cts
  - tests/skills/public-skills.test.cts
  - tests/smoke/host-smoke.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-01 through D-06: Five hosts expose exactly $kcoderag, $kcoderag-manage, $kcoderag-feedback, and $kcoderag-code-style with their locked read-only, explicit-authority, evidence-safe, prepare/review boundaries."
    - "D-07/D-08: The registry remains exactly kcoderag-navigation plus code-style-nudge; manual style is available on five hosts and native automatic style remains exact-Claude-2.1.241-only."
    - "D-09/D-12: status/doctor derive independent manualSkill and automaticNudge claims from valid schema-v1 ownership/integrity, and lifecycle reconciliation changes only exact state-owned paths through the transaction boundary."
    - "D-10/D-11: All four Codex Skills have valid agents/openai.yaml metadata, concise discriminating instructions, and only the style Skill retains its four selective references."
    - "D-13: Canonical sources, generated QA/Cursor trees, hooks, five adapters, docs, retirement audit, exact pack inventory, and required smoke converge before the first implementation commit."
    - "The pre-existing RED commit c821975 and its current staged GREEN work are adopted without resetting history, while .planning/config.json and the pre-existing experience-guide work remain preserved."
  artifacts:
    - path: "plugin-src/skills/kcoderag/SKILL.md"
      provides: "Canonical read-only navigation Skill"
    - path: "plugin-src/skills/kcoderag-manage/SKILL.md"
      provides: "Read-only-by-default lifecycle Skill"
    - path: "plugin-src/skills/kcoderag-feedback/SKILL.md"
      provides: "Evidence-backed secret-safe feedback Skill"
    - path: "plugin-src/capabilities/code-style-nudge/skill/SKILL.md"
      provides: "Manual prepare/review style Skill"
    - path: "src/hosts/host-version-support.cts"
      provides: "Five-host manual versus receipt-backed native delivery decision"
    - path: "src/core/state.cts"
      provides: "Integrity-derived manualSkill and automaticNudge diagnostics"
    - path: "src/generator/index.cts"
      provides: "Deterministic current-name QA/Cursor projection"
    - path: "src/maintainer/pre-commit.cts"
      provides: "Global current-name capability/generation enforcement"
    - path: "src/smoke/host-smoke.cts"
      provides: "Five-host packaged lifecycle proof"
  key_links:
    - from: "src/capabilities/navigation.cts"
      to: "src/generator/index.cts"
      via: "three public navigation-family Skill trees under one capability"
      pattern: "kcoderag|kcoderag-manage|kcoderag-feedback"
    - from: "src/capabilities/code-style-nudge.cts"
      to: "src/hosts/host-version-support.cts"
      via: "manual base plus receipt-backed native overlay"
      pattern: "manualSkill|automaticNudge|receiptDigest"
    - from: "src/core/state.cts"
      to: "src/cli/commands.cts"
      via: "one integrity-derived delivery object feeds JSON and human status/doctor"
      pattern: "manualSkill|automaticNudge"
    - from: "src/generator/index.cts"
      to: "kcoderag-qa/skills/kcoderag-code-style/SKILL.md"
      via: "explicit normalized current-name routes"
      pattern: "code-style-nudge|kcoderag-code-style"
    - from: "src/capabilities/compose.cts"
      to: "src/core/transaction.cts"
      via: "previous-state exact ownership and expected digests"
      pattern: "previousState|expectedDigest"
    - from: "package.json"
      to: "src/maintainer/pack-audit.cts"
      via: "one closed current-name archive inventory"
      pattern: "kcoderag-code-style|agents/openai.yaml"
---

<objective>
Converge the entire Phase 06 four-Skill product graph in one hook-verifiable implementation commit, adopting the existing RED commit and staged work.

Purpose: The repository pre-commit treats any staged canonical or generated path as a global product change: it rejects unstaged bytes anywhere under canonical/generated roots and runs capability, Hook, generator, repository-product, retirement, and drift checks. Any earlier implementation commit would therefore be an intentionally broken intermediate state unless the hook were bypassed, which is forbidden. This plan makes the global atomicity explicit instead of presenting thirteen commits that cannot pass the real hook.
Output: Four public Skills, split manual/native style delivery, honest diagnostics, state-owned reconciliation, deterministic five-host products, synchronized docs/audits/package/smoke, one cohesive GREEN implementation commit, and a Phase 06 Summary.

Requirement fallback: Phase 06 has no mapped requirement IDs. The phase goal and D-01 through D-13 are the complete acceptance source. The user explicitly authorized Phase 06 execution before the still-incomplete Phase 05 LIVE evidence; this plan neither consumes nor claims that evidence.
</objective>

## Execution-shape exception

This plan intentionally contains one broad tracer task. `src/maintainer/pre-commit.cts` examines the whole canonical set (`package.json`, `plugin-src`, `src`) plus both generated products and then runs global checks. Splitting the implementation into per-subsystem tasks or plans recreates the proven failure mode: the first commit sees later canonical/generated/test surfaces as stale. The pre-existing RED commit `c821975` remains the atomic test commit; every Phase 06 implementation surface converges before the executor's normal task commit. No hook bypass is permitted.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-SUPERSEDED.md
@package.json
@src/maintainer/pre-commit.cts
@tests/maintainer/pre-commit.test.cts
</context>

<interfaces>
- Public Skill identities are exactly kcoderag, kcoderag-manage, kcoderag-feedback, and kcoderag-code-style; internal capability and handler identities remain unchanged.
- kcoderag-navigation owns the first three trees. code-style-nudge owns the manual style Skill/four references for every host and the native overlay only when the frozen exact-Claude receipt passes.
- manualSkill and automaticNudge are derived from current schema-v1 contributor/section ownership plus individual/composite integrity; host shape or version text alone never creates a positive claim.
- apply_patch remains a host editing-tool event name. D-06 removes only a public style operation and does not rename the native tool event.
- applyTransaction remains the sole filesystem writer. Rename reconciliation receives authority only from exact prior state ownership/digests and never scans, adopts, or broadly deletes.
</interfaces>

<checkpoint_recovery_contract>
1. Before editing, verify `git merge-base --is-ancestor c821975 HEAD`, confirm `tests/skills/public-skills.test.cts` comes from that commit, and record `git status --short`, `git diff --cached --name-status`, and the staged blob IDs. The expected inherited staged product set is the exact deletion of `plugin-src/skills/code-lookup-discipline/SKILL.md`, additions of `plugin-src/skills/kcoderag/SKILL.md` and `plugin-src/skills/kcoderag/agents/openai.yaml`, and modification of `src/capabilities/navigation.cts`. If an expected entry is missing or an unrelated managed product is staged, halt before edits and report the mismatch; do not repair history or index state automatically.
2. Record the pre-run working diffs, full-file hashes, and a binary patch for `.planning/config.json` and `docs/MCP_QA_EXPERIENCE_GUIDE.md` in the validated OS temporary verification directory, never in the repository. Never reset, revert, checkout, clean, or stash either path. `.planning/config.json` is outside this plan and must remain unstaged and byte-identical. Classify the currently captured guide hunks before editing: the capability-support sentence is an explicit D-08 Phase 06 input and may be adopted as an intended Phase 06 hunk; the two added blank-line pairs and trailing blank line are user-owned formatting hunks and must remain outside the GREEN commit. Any newly observed or overlapping hunk is user-owned by default. Stage guide changes hunk-by-hunk, never with a whole-file add, and halt with the saved pre/post patch evidence if the phase hunk cannot be separated without taking a user-owned hunk.
3. The executor must not invoke an implementation commit until every canonical, generated, host, documentation, audit, pack, and smoke surface below is complete and the direct pre-commit/final gate chain passes. Focused checks are development checkpoints only and do not authorize intermediate commits. Do not use `--no-verify`, alter the hook, disable checks, or create a temporary bypass commit.
4. If a focused or final gate fails, keep the current worktree and index intact, report the exact command and safe failure code, apply only an in-scope fix, restage the closed Phase 06 set, and rerun the failed focused gate plus the full final chain. Allow at most two repair cycles for the direct pre-commit/final chain. If it still fails or requires a path outside the declared inventory, halt with `git status --short`, cached/worktree path lists, and the last failure; do not roll back or broaden scope silently.
5. Immediately before the direct pre-commit, derive the declared path set from this plan's `files_modified` through `gsd-tools query phase-plan-index 06`; do not maintain a second hand-written allow-list. Exclude only the three intentionally untracked stale compiled outputs and apply the guide exception from item 2. `git diff --name-only -- <every remaining declared production/test/docs path>` must be empty, proving the main worktree and index agree everywhere else. The cached path set must be a subset of that same declared set, `.planning/config.json` must have no cached diff and its original hash, the cached guide patch must contain only intended Phase 06 hunks, the remaining worktree guide patch must contain every snapshotted user-owned hunk, and `git diff --cached --check` must pass. Any undeclared staged path or declared unstaged difference is a hard stop.
6. Hash the main Git index, capture its staged path/blob inventory, and set `STAGED_TREE` to the output of `git write-tree` only after item 5 proves the main index complete. Materialize that tree in an isolated detached worktree under a freshly created, fully resolved OS temporary directory. After `git worktree add --detach <temp> HEAD`, resolve both `git rev-parse --git-path index` from the main repository and `git -C <temp> rev-parse --git-path index`; require both absolute paths to exist and differ. Run `git -C <temp> read-tree <STAGED_TREE>` so only the linked-worktree index changes, then `git -C <temp> checkout-index --all --force`. Remove only the main-index deletion list captured by `git diff --cached --name-only --diff-filter=D`, resolving and containment-checking each destination under the temp root; no scan-derived or recursive removal is allowed. Require `git -C <temp> write-tree` to equal `STAGED_TREE` before any gate. Do not copy the main working tree or `node_modules`. This worktree may be removed only after resolving it under the OS temp root and confirming it is neither the repository root nor a workspace ancestor; use the exact registered worktree path. Confirm the main index hash, `git write-tree` value, and cached path/blob inventory are unchanged after removal.
7. The index-exact isolated sequence is `npm.cmd ci --ignore-scripts`, `npm.cmd run build`, `npm.cmd run deps:audit`, `npm.cmd run generate:check`, `npm.cmd run docs:check`, `npm.cmd run audit:retirement`, `node dist/maintainer/pre-commit.cjs`, `npm.cmd run ci:local`, `npm.cmd run pack:audit`, and `npm.cmd run smoke:required`, all with the isolated root as cwd. The direct pre-commit is compatible here because the linked-worktree index holds `STAGED_TREE`, the linked worktree matches that index across all declared paths, and its index path is proven distinct from the main index. The normal main-tree task commit is permitted only after this sequence and the compiled direct pre-commit on the main index both pass. The commit must run the real hook again. If either gate or the commit is rejected, follow item 4 and retry normally; never bypass the hook.
8. If the index-exact direct pre-commit fails only because the linked-worktree index stat cache changed after materialization, allow exactly one additional verification-only attempt after refreshing or stabilizing that linked index, such as a read-only status refresh before capturing the pre-commit snapshot. Reassert the staged tree, staged path/blob inventory, materialized byte/deletion checks, and main-index before/after equality before accepting the retry. This allowance does not authorize any main-index mutation, product-byte change, reset, revert, stash, hook bypass, tag/version change, publish, or extra repair cycle.
</checkpoint_recovery_contract>

<index_exact_verification>
Run this procedure in one `pwsh.exe` session after item 5 has made the main index complete. Resolve the repository root and its actual index path first; capture the index SHA-256, `git ls-files --stage` output, the enumerated staged-deletion list, and `$stagedTree = (git write-tree).Trim()`. Require a successful forty-hex-character tree ID. Then create `$phase06VerifyRoot` as `Join-Path ([IO.Path]::GetFullPath([IO.Path]::GetTempPath())) ("kcoderag-nav-phase06-index-" + [guid]::NewGuid())`. Resolve it and reject it unless it is a strict descendant of the OS temp root and neither equal to nor an ancestor/descendant of the repository root.

1. Run `git worktree add --detach -- $phase06VerifyRoot HEAD` from the main repository. Resolve `$mainIndex = git rev-parse --git-path index` in the main repository and `$linkedIndex = git -C $phase06VerifyRoot rev-parse --git-path index`; normalize both to absolute paths, require both to exist, and require `$linkedIndex -ne $mainIndex`. Run `git -C $phase06VerifyRoot read-tree $stagedTree`, followed by `git -C $phase06VerifyRoot checkout-index --all --force`. For each deletion captured before worktree creation, resolve the destination under `$phase06VerifyRoot`, reject containment failure, and remove only that exact file with `Remove-Item -LiteralPath` and no recursive flag.
2. Require `(git -C $phase06VerifyRoot write-tree).Trim()` to equal `$stagedTree`. Parse the captured stage-0 blob inventory; for every regular indexed path require the materialized file to exist and require `git hash-object --no-filters -- $materializedPath` to equal its staged blob ID, and for every enumerated staged deletion require absence. Reject symlink/special-file ambiguity instead of following it. This proves both the linked-worktree index and its checked-out bytes represent the completed main staged tree before npm writes ignored outputs.
3. In `$phase06VerifyRoot`, run `npm.cmd ci --ignore-scripts` against the staged lockfile, followed in order by `npm.cmd run build`, `npm.cmd run deps:audit`, `npm.cmd run generate:check`, `npm.cmd run docs:check`, `npm.cmd run audit:retirement`, `node dist/maintainer/pre-commit.cjs`, `npm.cmd run ci:local`, `npm.cmd run pack:audit`, and `npm.cmd run smoke:required`. Stop on the first nonzero exit and retain its command/status in the recovery report.
4. In a `finally` block, return to the repository root. Resolve `$phase06VerifyRoot` again and reapply the strict temp/repository containment assertions before `git worktree remove --force -- $phase06VerifyRoot`; never pass a computed or unresolved path. Recompute the main index SHA-256, `git write-tree`, and staged path/blob inventory and require exact equality with the pre-materialization snapshot. A cleanup failure is reported with the exact validated temp path and must not trigger a broader delete.
</index_exact_verification>

<tasks>

<task type="tracer">
  <name>Task 1: Converge four public Skills through five-host packaged delivery and commit once</name>
  <precondition>HEAD contains c821975, the inherited four-path staged GREEN set matches the checkpoint contract, and the user has explicitly authorized Phase 06 to execute without waiting for Phase 05 LIVE completion.</precondition>
  <reversibility rating="costly">The public Skill paths, diagnostic enums, and five-host owned inventories change together and require coordinated reconciliation to undo.</reversibility>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-skill/06-PATTERNS.md</file>
    <file>package.json</file>
    <file>src/maintainer/pre-commit.cts</file>
    <file>tests/maintainer/pre-commit.test.cts</file>
    <file>tests/maintainer/release.test.cts</file>
    <file>tests/skills/public-skills.test.cts</file>
    <file>plugin-src/skills/kcoderag/SKILL.md</file>
    <file>plugin-src/skills/kcoderag/agents/openai.yaml</file>
    <file>src/capabilities/navigation.cts</file>
    <file>src/capabilities/code-style-nudge.cts</file>
    <file>src/hosts/host-version-support.cts</file>
    <file>src/generator/index.cts</file>
    <file>src/core/state.cts</file>
    <file>src/core/transaction.cts</file>
  </read_first>
  <files>The exact closed write/removal set is the 111-path files_modified inventory in this plan; `.planning/config.json`, `.planning/STATE.md`, unrelated untracked files, external repositories, tags, registry state, and workflows are excluded.</files>
  <behavior>
    - D-01/D-02: Canonical, generated, host, test, documentation, and archive surfaces expose only the four current public identities; exact old generated/current-state-owned paths are retired without aliases, scans, adoption, or broad deletion.
    - D-03/D-04/D-05/D-06: Navigation is read-only; management defaults to status/doctor and requires explicit mutation intent; feedback is result-backed and secret-safe; style supports natural-language preparation plus review of a file/current changes without a public apply operation.
    - D-07/D-08: Exactly two internal capabilities remain. Five hosts receive manual style, while native pre-write assets/sections/reminders require the frozen exact Claude Code 2.1.241 receipt; manual-only hosts remain healthy and silent on the native path.
    - D-09: JSON and human status/doctor independently report manualSkill and automaticNudge from contributor-scoped schema-v1 integrity, including not-installed, unknown, drifted, unsupported, and available outcomes without configuration values.
    - D-10/D-11: Four Codex trees contain valid quoted openai.yaml metadata with 25-64-character descriptions, exact default prompts, and implicit invocation enabled; only style has the four risk-routed references.
    - D-12/D-13: Fresh/update/repeat/selective-uninstall/--all flows preserve siblings, other hosts, and unrelated bytes; refusals and rollback remain atomic; generation, docs, audit, pack, and five-host PACKAGED smoke close on the same staged tree.
  </behavior>
  <action>Adopt `c821975` as the already-completed RED step and retain the inherited staged implementation bytes. Do not recreate or amend that commit. First execute the checkpoint contract and rerun the focused public-Skill test to establish the current GREEN tracer.

Then converge the entire product graph without committing between checkpoints. Complete the other two concise navigation-family Skills and their Codex metadata under `kcoderag-navigation`; rename the style source/tests to `kcoderag-code-style`, add its metadata, and preserve exactly four selective references. Resolve and confirm the three stale compiled outputs `dist-tests/skills/code-style-correction.test.cjs`, `dist-tests/skills/code-style-correction.behavior.test.cjs`, and `dist-tests/hooks/jx3-style-nudge.test.cjs` under the repository `dist-tests` root, remove only those exact files without recursion, rebuild, and assert all three remain absent before any glob-based `npm test` or `ci:local` execution. Extend focused tests before each behavior change.

Replace binary style support with one centralized manual-base/native-overlay decision. Keep both internal capability IDs and handler filenames fixed. Derive manualSkill and automaticNudge from contributor/section ownership and complete digests, render one object through JSON and human status/doctor, and cover manual-only, exact-Claude native, absent, invalid, partial-drift, and adjacent-version fixtures. Run focused capability and CLI tests at this checkpoint.

Update explicit generator routes, exact repository inventories, and QA/Cursor product tests for the four current Skill trees and four Codex metadata files. Remove only the enumerated obsolete QA/Cursor product files from the declared inventory, generate the complete QA/Cursor trees, and prove a second run plus check mode has no drift. Update automatic Hook/dispatcher wording and integrity so only receipt-backed native ownership can claim a reminder; repair the currently observed direct-ZCode capability-hook fixture against the complete current contributor inventory while preserving strict fail-open behavior. Regenerate the scoped Hook payloads and run generator/product/capability-Hook tests.

After generated inputs exist, update Codex, Claude, Cursor, OpenCode, and ZCode adapters and tests for navigation-only, style-only, and combined exact inventories. Codex installs all four metadata files; exact Claude alone receives the native overlay; the other hosts own no synthetic pre-write style surface. Preserve Cursor Rule/afterMCPExecution, OpenCode project-only JSON/JSONC refusal/plugin event, ZCode trust neutrality/native navigation events, source gates, and unrelated configuration. Extend composition only through exact previous-state ownership/digests, then run all focused host, capability, transaction, cross-host, and native-lifecycle checks.

Synchronize root/template/generated READMEs, the repository-owned experience guide, docs enforcement, PROJECT/STACK/ARCHITECTURE managed sections, and AGENTS assembly with scoped patches. Follow checkpoint item 2 for the dirty experience guide and do not touch `.planning/config.json`. Update the current-name retirement audit, pre-commit command inventory, registry assertion, package allow-list, pack audit, smoke harness, and native-host driver only after every scanned surface is current. The audit must exclude immutable historical planning/receipt records, construct its prohibited test tokens without self-matching, emit path-only failures, and preserve PACKAGED versus LIVE honesty. Update smoke for style-only and combined five-host lifecycle, exact-Claude native delivery, honest diagnostics, refusals, rollback, and unrelated-byte preservation; do not dispatch workflows or create LIVE evidence.

If the full test gate fails only because the real-repository snapshot in `tests/maintainer/release.test.cts` clones local tags and its patch-bump scenario collides with an already existing `v0.3.2`, narrowly add `--no-tags` to only that real-repository snapshot clone. Retain its GSD local-state/status-preservation assertions and the separate tag-collision fixture. Do not change production release tag checks, delete or create tags, alter a package version, publish, or weaken any release assertion.

Use the focused commands named in the verification section after each corresponding group; fix every failure before proceeding. Run the full generator once, review the exact QA/Cursor changes, run it again in check mode, and stage the complete closed Phase 06 production set explicitly. Stage only Phase 06 guide hunks identified by the checkpoint ledger and verify that every user-owned hunk remains unstaged. Apply the every-declared-path guard, run the compiled real pre-commit directly against the unchanged main index, then execute the complete final gate chain against the isolated index-exact worktree from checkpoint items 6-7. Only after both pass may the executor finish this task and create the normal single GREEN implementation commit; that commit must run the repository hook normally. Include the declared PROJECT/STACK/ARCHITECTURE/AGENTS contract updates, but exclude execution metadata (`06-*-PLAN.md`, `06-SUPERSEDED.md`, ROADMAP, STATE, config), debug/research files, and unrelated untracked work.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'),path=require('node:path');const root=path.resolve('dist-tests')+path.sep;for(const rel of ['skills/code-style-correction.test.cjs','skills/code-style-correction.behavior.test.cjs','hooks/jx3-style-nudge.test.cjs']){const target=path.resolve('dist-tests',rel);if(!target.startsWith(root))process.exit(1);fs.rmSync(target,{force:true})}" &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('node:fs');const paths=['dist-tests/skills/code-style-correction.test.cjs','dist-tests/skills/code-style-correction.behavior.test.cjs','dist-tests/hooks/jx3-style-nudge.test.cjs'];if(paths.some(fs.existsSync))process.exit(1)" &amp;&amp; node --test dist-tests/skills/public-skills.test.cjs dist-tests/skills/kcoderag-code-style.test.cjs dist-tests/skills/kcoderag-code-style.behavior.test.cjs &amp;&amp; npm run test:capabilities &amp;&amp; npm run test:capability-hooks &amp;&amp; npm run test:cli &amp;&amp; npm run test:generator &amp;&amp; npm run test:generator:repository &amp;&amp; npm run test:host:codex &amp;&amp; npm run test:host:claude &amp;&amp; npm run test:host:cursor &amp;&amp; npm run test:host:opencode &amp;&amp; npm run test:host:zcode &amp;&amp; npm run test:cross-host &amp;&amp; npm run test:transaction &amp;&amp; node --test dist-tests/hosts/honest-events.test.cjs dist-tests/hosts/native-lifecycle.test.cjs &amp;&amp; npm run generate &amp;&amp; npm run generate:check &amp;&amp; npm run test:docs &amp;&amp; npm run docs:check &amp;&amp; npm run test:retirement-audit &amp;&amp; npm run audit:retirement &amp;&amp; npm run test:precommit &amp;&amp; npm run test:pack &amp;&amp; npm run pack:audit &amp;&amp; npm run test:smoke &amp;&amp; node --test dist-tests/maintainer/native-host-driver.test.cjs &amp;&amp; node dist/maintainer/pre-commit.cjs</automated>
    <automated>After the main focused chain passes, execute all four numbered steps in index_exact_verification in one pwsh.exe session; require every staged-blob/deletion assertion, npm ci audited-graph step, final generation/docs/CI/pack/smoke command, temporary-worktree safety assertion, and main-index before/after equality check to pass.</automated>
  </verify>
  <done>The existing RED test remains in c821975; one normally hook-verified GREEN commit contains the complete Phase 06 implementation, all D-01 through D-13 gates pass on that same staged tree, no external/LIVE/publish action occurs, and all captured user work remains preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing index/worktree to Phase 06 staging | Inherited staged product work and unrelated dirty user work coexist before execution. |
| Public Skill discovery to mutation | Implicit invocation must never become lifecycle or edit authority. |
| Host/version/state to delivery claim | Only complete state integrity and frozen receipt evidence may create positive manual/native status. |
| Prior owned paths to transaction | On-disk state controls exact reconciliation authority. |
| Canonical source to generated trees/archive/smoke | The same instructions and runtime become distributed package bytes and disposable-host behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01-01 | Tampering | Git index and dirty worktree | high | mitigate | Entry/cached-diff snapshots, closed staging, no reset/stash, direct pre-commit, and preservation checks bind the cohesive change. |
| T-06-01-02 | Elevation of Privilege | kcoderag-manage and style guidance | high | mitigate | D-04/D-06/D-10 focused tests require explicit mutation/edit intent. |
| T-06-01-03 | Information Disclosure | feedback and diagnostics | high | mitigate | D-05/D-09 tests forbid source/configuration/credential material and retain path-only errors. |
| T-06-01-04 | Spoofing | native style availability | high | mitigate | D-08/D-09 exact receipt, version, contributor, section, and digest tests gate native claims. |
| T-06-01-05 | Tampering | rename reconciliation | high | mitigate | D-02/D-12 use exact schema-v1 ownership and expected digests through the existing transaction. |
| T-06-01-06 | Denial of Service | Hook/runtime inputs | high | mitigate | Bounded parsing and tested empty exit-zero failures preserve fail-open behavior. |
| T-06-01-07 | Tampering | generated package | high | mitigate | Deterministic routes, second-run drift check, closed pack inventory, and acquired-package smoke bind bytes. |
| T-06-01-08 | Repudiation | verification/commit | medium | mitigate | Direct pre-commit plus the normal commit hook and Summary record the exact same-tree gate results. |
</threat_model>

<verification>
Run focused checks as each subsystem converges, remove and prove absence of all three stale compiled outputs, then stage the entire closed implementation set. The direct `dist/maintainer/pre-commit.cjs` invocation must return verified on the main index, and the detached temporary worktree must prove the final dependency/generation/docs/CI/pack/smoke chain against those exact staged bytes. The normal task commit hook must run again without bypass. Confirm the commit excludes execution/config/state/debug/research/unrelated files and that the post-commit worktree still contains the captured user-owned guide hunks.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Phase 06 convergence

| Source | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Four stable public Skills on five hosts with split style delivery and separate diagnostics | 06-01 | COVERED |
| REQ | — | No mapped Phase 06 requirement IDs | — | N/A |
| RESEARCH | — | No RESEARCH.md; live code, AGENTS, and 06-PATTERNS constrain execution | 06-01 | COVERED |
| CONTEXT | D-01 | Four exact public names | 06-01 | COVERED |
| CONTEXT | D-02 | No aliases; exact ownership-safe retirement | 06-01 | COVERED |
| CONTEXT | D-03 | Read-only navigation routes | 06-01 | COVERED |
| CONTEXT | D-04 | Read-only management default and explicit mutation | 06-01 | COVERED |
| CONTEXT | D-05 | Evidence-backed secret-safe feedback | 06-01 | COVERED |
| CONTEXT | D-06 | Prepare/review style without a public apply operation | 06-01 | COVERED |
| CONTEXT | D-07 | Exactly two capabilities and fixed handlers | 06-01 | COVERED |
| CONTEXT | D-08 | Manual style on five hosts; exact-Claude native only | 06-01 | COVERED |
| CONTEXT | D-09 | Independent integrity-derived diagnostics | 06-01 | COVERED |
| CONTEXT | D-10 | Four Codex metadata files and implicit-discovery boundary | 06-01 | COVERED |
| CONTEXT | D-11 | Concise Skills and selective style references | 06-01 | COVERED |
| CONTEXT | D-12 | Ownership/source/atomic lifecycle safety | 06-01 | COVERED |
| CONTEXT | D-13 | Generation, docs, full CI, exact pack, and five-host smoke | 06-01 | COVERED |

Deferred items remain excluded: no additional native receipt/support, third capability, compatibility alias, npm publication/dist-tag action, production identity/HTTPS/token work, workflow dispatch, or LIVE promotion.
</source_audit>

<success_criteria>
- The prior impossible 13-plan routing surface is absent; GSD sees exactly this one incomplete plan.
- `c821975` remains the RED commit and the inherited staged files are incorporated without history/index bypass.
- Four current Skills, two internal capabilities, five-host manual style, exact-Claude native style, and independent diagnostics agree across source, generated, host, docs, archive, and smoke surfaces.
- The direct pre-commit and final command chain pass before the sole GREEN implementation commit, whose real hook passes again.
- All three stale compiled outputs are absent before the full test glob, and final gates execute from an isolated byte-for-byte materialization of the staged index.
- `.planning/config.json`, the captured experience-guide work, and unrelated dirty/untracked files survive unchanged except for separately attributable scoped Phase 06 guide hunks.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-01-SUMMARY.md when done. Record the inherited RED commit, sole GREEN commit, focused/final gate results, direct/hook pre-commit results, exact staged path audit, user-dirty preservation evidence, and any bounded recovery cycles.
</output>

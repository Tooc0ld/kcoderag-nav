---
phase: 06-skill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin-src/skills/code-lookup-discipline/SKILL.md
  - plugin-src/skills/kcoderag/SKILL.md
  - plugin-src/skills/kcoderag/agents/openai.yaml
  - plugin-src/skills/kcoderag-manage/SKILL.md
  - plugin-src/skills/kcoderag-manage/agents/openai.yaml
  - plugin-src/skills/kcoderag-feedback/SKILL.md
  - plugin-src/skills/kcoderag-feedback/agents/openai.yaml
  - src/capabilities/navigation.cts
  - tests/skills/public-skills.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: The navigation capability exposes only $kcoderag, $kcoderag-manage, and $kcoderag-feedback, with no retained navigation alias."
    - "D-03/D-04/D-05: Navigation, lifecycle management, and feedback have distinct bounded responsibilities and authorization rules."
    - "D-07: The three public navigation-family Skills remain contributors of kcoderag-navigation rather than new capabilities."
    - "D-10/D-11: Each navigation-family Skill is concise and has valid Codex discovery metadata; implicit discovery never authorizes mutation."
  artifacts:
    - path: "plugin-src/skills/kcoderag/SKILL.md"
      provides: "Read-only KCodeRag lookup contract"
    - path: "plugin-src/skills/kcoderag-manage/SKILL.md"
      provides: "Read-only-by-default lifecycle contract"
    - path: "plugin-src/skills/kcoderag-feedback/SKILL.md"
      provides: "Evidence-backed secret-safe feedback contract"
    - path: "src/capabilities/navigation.cts"
      provides: "Single-capability ownership of all three navigation-family Skill trees"
  key_links:
    - from: "src/capabilities/navigation.cts"
      to: "plugin-src/skills/kcoderag/SKILL.md"
      via: "deterministic projected files owned by kcoderag-navigation"
      pattern: "kcoderag-navigation|kcoderag-manage|kcoderag-feedback"
    - from: "plugin-src/skills/kcoderag-manage/SKILL.md"
      to: "existing CLI lifecycle"
      via: "instructions default to status/doctor and require explicit mutation intent"
      pattern: "status|doctor|update|uninstall"
---

<objective>
Define the three public navigation-family Skill contracts and project them from the existing navigation capability.

Purpose: Give users directly callable, discoverable navigation, management, and feedback interfaces without expanding capability count or mutation authority.
Output: Three canonical Skill trees, three Codex metadata files, navigation provider ownership, and focused contract tests.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-01 through D-05, D-07, D-10, and D-11 are the executable acceptance source for this plan.
</objective>

## Artifacts this phase produces

- Canonical `$kcoderag`, `$kcoderag-manage`, and `$kcoderag-feedback` instruction trees.
- Matching Codex `agents/openai.yaml` discovery metadata.
- One `kcoderag-navigation` contribution owning all three trees.
- Contract tests for exact names, responsibilities, authorization, and metadata.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@src/capabilities/navigation.cts
@plugin-src/skills/code-lookup-discipline/SKILL.md
</context>

<interfaces>
- `kcoderag-navigation` remains the only capability owner for these three Skill trees (D-07).
- Every metadata file contains `interface.display_name`, a 25-64 character `interface.short_description`, an `interface.default_prompt` naming its exact `$skill-name`, and `policy.allow_implicit_invocation: true` (D-10).
- Implicit discovery may select instructions. `$kcoderag-manage` still requires explicit user intent before update or uninstall and never infers cleanup (D-04/D-10).
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace read-only lookup from $kcoderag metadata through the navigation provider</name>
  <precondition>Phase 05 is complete and `.planning/phases/05-hook-precision/05-06-SUMMARY.md` exists; Phase 06 must not replace the prior phase's authenticated LIVE evidence.</precondition>
  <reversibility rating="costly">The public Skill path is installed by five hosts, so changing it later requires coordinated owned-path reconciliation.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-skill/06-PATTERNS.md</file>
    <file>plugin-src/skills/code-lookup-discipline/SKILL.md</file>
    <file>src/capabilities/navigation.cts</file>
  </read_first>
  <files>plugin-src/skills/code-lookup-discipline/SKILL.md, plugin-src/skills/kcoderag/SKILL.md, plugin-src/skills/kcoderag/agents/openai.yaml, src/capabilities/navigation.cts, tests/skills/public-skills.test.cts</files>
  <behavior>
    - D-01/D-02: The canonical entry is named `kcoderag`; the retired navigation directory and frontmatter identity are removed rather than retained as an alias.
    - D-03: Natural-language lookup and search/context/calls/indexes/graph routing are read-only, use graph evidence first when appropriate, and state the local fallback for unavailable or stale graph evidence.
    - D-07: The installed file is owned by `kcoderag-navigation` and does not create another registry entry.
    - D-10/D-11: Metadata names `$kcoderag`, allows implicit discovery, and the Skill stays concise without extra references or README files.
  </behavior>
  <action>Write failing assertions for the exact frontmatter name, read-only routing table, local fallback, metadata length/prompt/policy, provider path, and absence of an alias contribution. Replace the state-owned canonical navigation source with `plugin-src/skills/kcoderag/`; remove the retired canonical file as part of the same rename, not by scanning similarly named files, per D-02. Update the existing navigation provider to project the new Skill and metadata under `kcoderag-navigation`. Keep MCP lookup operations read-only and exclude lifecycle mutation, edits, and feedback submission per D-03.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-02: The provider emits `$kcoderag` and no navigation compatibility asset.
    - D-03: Tests cover all five lookup routes and explicit local fallback without mutation behavior.
    - D-07/D-10/D-11: The file and metadata are contributor-owned, concise, and discoverable without adding a capability.
  </acceptance_criteria>
  <done>A provider-rendered `$kcoderag` Skill is directly discoverable and performs only bounded read-only navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define management and feedback instruction contracts</name>
  <reversibility rating="costly">The names and authorization wording become shared public contracts across every host projection.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>plugin-src/skills/kcoderag/SKILL.md</file>
    <file>tests/skills/public-skills.test.cts</file>
  </read_first>
  <files>plugin-src/skills/kcoderag-manage/SKILL.md, plugin-src/skills/kcoderag-manage/agents/openai.yaml, plugin-src/skills/kcoderag-feedback/SKILL.md, plugin-src/skills/kcoderag-feedback/agents/openai.yaml, tests/skills/public-skills.test.cts</files>
  <behavior>
    - D-04: `$kcoderag-manage` defaults to status/doctor; update and uninstall require explicit user requests, and cleanup is never an inferred fallback.
    - D-05: `$kcoderag-feedback` follows a real KCodeRag result, submits only supported evaluation data through `submit_feedback`, and excludes source/configuration/credential material.
    - D-10/D-11: Both metadata files satisfy the exact discovery schema while the corresponding SKILL.md files remain short and distinguishable.
  </behavior>
  <action>Extend the failing public-Skill contract test before implementation. Add the management instructions with an explicit intent check before `update` or `uninstall`; allow implicit invocation to explain or run only read-only status/doctor paths. Add feedback instructions that require a real preceding KCodeRag result, prevent invented opinions, and prohibit source text, endpoint values, headers, bearer material, tokens, and configuration bodies. Add exact Codex metadata for both public names per D-04/D-05/D-10/D-11. Do not add references, README files, empty auxiliary directories, cleanup automation, or another capability.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-04: Mutation tests require explicit intent and preserve read-only defaults.
    - D-05: Feedback tests require real result evidence and reject sensitive payload categories.
    - D-10/D-11: Both concise Skills have matching metadata and no extra payload tree.
  </acceptance_criteria>
  <done>The management and feedback Skills have non-overlapping behavior, exact discovery metadata, and explicit safety boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add both new trees to the navigation contributor</name>
  <read_first>
    <file>src/capabilities/navigation.cts</file>
    <file>tests/skills/public-skills.test.cts</file>
    <file>plugin-src/skills/kcoderag-manage/SKILL.md</file>
    <file>plugin-src/skills/kcoderag-feedback/SKILL.md</file>
  </read_first>
  <files>src/capabilities/navigation.cts, tests/skills/public-skills.test.cts</files>
  <behavior>
    - D-07: `kcoderag-navigation` owns all three navigation-family Skill and metadata trees while registry capability count remains unchanged.
    - D-01/D-10: Exact provider inventory contains the three public roots and matching Codex metadata files.
    - D-03/D-04/D-05: Provider rendering preserves each contract unchanged rather than merging their responsibilities.
  </behavior>
  <action>Finish the provider-inventory test by asserting exact sorted paths and contributor ownership for all three Skill/metadata trees. Extend `src/capabilities/navigation.cts` to render the management and feedback assets alongside `$kcoderag`, with immutable normalized output and no new section or capability. Keep the three instruction bodies independent and preserve their read-only, explicit-authorization, and evidence-safe boundaries per D-03/D-04/D-05/D-07.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-07: One navigation contributor owns all three exact trees and the registry still has two capabilities.
    - D-01/D-10: Exact provider paths include all Skill and metadata files.
    - D-03/D-04/D-05: Rendering does not blur responsibilities or authority.
  </acceptance_criteria>
  <done>The navigation contributor exposes all three public Skill trees with deterministic ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Implicit Skill discovery to lifecycle action | Discovery selects instructions but must not become authority for project mutation. |
| Query result to feedback submission | A real result may support an evaluation, while source and configuration data remain private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01-01 | Elevation of Privilege | `$kcoderag-manage` | high | mitigate | D-04/D-10 tests require explicit update/uninstall intent and disallow inferred cleanup. |
| T-06-01-02 | Information Disclosure | `$kcoderag-feedback` | high | mitigate | D-05 instructions and tests exclude source/configuration values and credential-bearing material. |
| T-06-01-03 | Spoofing | public Skill identity | medium | mitigate | Exact frontmatter, metadata, and provider-inventory assertions bind names to their intended responsibilities. |
| T-06-01-04 | Repudiation | navigation-versus-feedback routing | low | accept | Distinct public names and deterministic contract tests make the selected responsibility observable without adding persistent telemetry. |
</threat_model>

<verification>
1. Build and run existing capability plus repository tests.
2. Assert exactly three navigation-family public roots and valid metadata.
3. Assert `$kcoderag` is read-only, management mutations require explicit intent, and feedback requires real evidence with secret-safe payload guidance.
4. Confirm the registry remains untouched and `kcoderag-navigation` owns all three trees.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 01

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | Stable directly callable public Skills | COVERED | Tasks 1-2 establish three of four Skills |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback; no ID invented |
| RESEARCH | — | No RESEARCH.md; follow live provider and Skill patterns | COVERED | 06-PATTERNS.md and read_first |
| CONTEXT | D-01–D-05 | Exact navigation-family names and behaviors | COVERED | Tasks 1-2 |
| CONTEXT | D-07 | Existing capability ownership | COVERED | Both tasks |
| CONTEXT | D-10–D-11 | Metadata and concise Skill structure | COVERED | Both tasks |
| CONTEXT | D-06/D-08/D-09/D-12/D-13 | Style, diagnostics, lifecycle, and distribution | COVERED ELSEWHERE | Plans 06-02 through 06-08 |

Deferred ideas remain excluded: no alias, third capability, non-Claude native support, publish action, or production-identity work.
</source_audit>

<success_criteria>
- `$kcoderag`, `$kcoderag-manage`, and `$kcoderag-feedback` have exact names, bounded responsibilities, and matching Codex metadata.
- The retired navigation public identity is not preserved as an alias.
- All three assets are owned by `kcoderag-navigation` and no capability is added.
- Mutation and feedback safety boundaries are covered by automated tests.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-01-SUMMARY.md` when done.
</output>

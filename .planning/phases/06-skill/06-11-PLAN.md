---
phase: 06-skill
plan: "11"
type: execute
wave: 11
depends_on:
  - 06-10
files_modified:
  - src/hosts/zcode.cts
  - tests/hosts/zcode.test.cts
  - src/capabilities/compose.cts
  - tests/capabilities/compose.test.cts
  - tests/core/transaction.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/hosts/native-lifecycle.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-08/D-09: ZCode installs all four manual Skills, needs no navigation prerequisite for style-only, and reports available/unsupported."
    - "D-02/D-12: Rename reconciliation acts only on exact paths/digests owned by valid schema-v1 state."
    - "D-07/D-12: Five-host fresh/update/selective uninstall/--all preserve sibling capabilities, other hosts, and unrelated configuration."
    - "D-13: Focused lifecycle tests pass after generated products and all adapters are current; repository-wide gates remain in Plan 06-13."
  artifacts:
    - path: "src/hosts/zcode.cts"
      provides: "ZCode four-Skill manual projection"
    - path: "src/capabilities/compose.cts"
      provides: "State-owned retired-path reconciliation"
    - path: "tests/hosts/cross-host.test.cts"
      provides: "Five-host lifecycle proof"
  key_links:
    - from: "src/capabilities/compose.cts"
      to: "src/core/transaction.cts"
      via: "expected-digest complete desired state with state last"
      pattern: "previousState|expectedDigest"
---

<objective>
Complete ZCode projection, then reconcile owned old paths and prove the atomic five-host lifecycle.

Purpose: Ensure every adapter knows current paths before any update/uninstall logic or full test glob executes.
Output: ZCode adapter, compose reconciliation, and focused lifecycle/rollback coverage.
</objective>

## Artifacts this phase produces

- ZCode style-only and combined manual projection.
- Exact previous-owned-file reconciliation.
- Focused five-host install/update/uninstall, drift, unsafe-path, and rollback tests.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-10-SUMMARY.md
@src/capabilities/compose.cts
@src/core/transaction.cts
</context>

<interfaces>
- ZCode owns no automatic style event and never changes workspace trust per D-08/D-12.
- Previous files become restore/delete entries only from exact valid state ownership and digest per D-02.
- applyTransaction remains the sole writer, preflights all entries, commits state last, and rolls back one selected host atomically.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Project manual-only style to ZCode</name>
  <reversibility rating="costly">Managed ZCode paths and config sections become lifecycle state.</reversibility>
  <read_first>
    <file>src/hosts/zcode.cts</file>
    <file>tests/hosts/zcode.test.cts</file>
    <file>.planning/phases/06-skill/06-05-SUMMARY.md</file>
  </read_first>
  <files>src/hosts/zcode.cts, tests/hosts/zcode.test.cts</files>
  <behavior>
    - D-01/D-07/D-08: Navigation/style/combined arrays contain exactly three/five/eight current files.
    - D-08/D-09: Style-only succeeds independently and reports available/unsupported with no native style section/runtime.
    - D-12: Workspace trust, project-only config, navigation events, source gates, and unrelated sections remain unchanged.
  </behavior>
  <action>Write exact inventory and style-only tests, then refactor the navigation-only early return into independent contributors. Map the current QA Skill inputs, omit native style assets, and preserve project trust neutrality and existing real Hook/marker behavior.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:zcode</automated>
  </verify>
  <done>ZCode installs all four manual Skills without navigation coupling or native style overclaim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile only exact state-owned retired Skill paths</name>
  <reversibility rating="costly">This rule controls updates for already installed projects and must preserve originals.</reversibility>
  <read_first>
    <file>src/capabilities/compose.cts</file>
    <file>tests/capabilities/compose.test.cts</file>
    <file>src/core/state.cts</file>
    <file>src/core/transaction.cts</file>
  </read_first>
  <files>src/capabilities/compose.cts, tests/capabilities/compose.test.cts</files>
  <behavior>
    - D-02/D-12: Valid state ownership of an exact retired installed path yields an expected-digest restore/delete entry and current replacement paths.
    - D-02: Unmanaged lookalikes are preserved or refused, never adopted or scanned.
    - D-12: Desired state stays deterministic and state remains the last transaction entry.
  </behavior>
  <action>Create current schema-v1 fixtures with exact old paths, originals, digests, and canaries. Extend composition only through the existing previous-owned-file mechanism. Do not add migration, adoption, recursive deletion, path-name scans, or old-state support.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capabilities</automated>
  </verify>
  <done>The composer replaces only exact retired files proven owned by valid current state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove atomic five-host lifecycle with focused tests</name>
  <reversibility rating="costly">The tests lock cross-host mutation and rollback behavior.</reversibility>
  <read_first>
    <file>tests/core/transaction.test.cts</file>
    <file>tests/hosts/cross-host.test.cts</file>
    <file>tests/hosts/native-lifecycle.test.cts</file>
    <file>.planning/phases/06-skill/06-10-SUMMARY.md</file>
  </read_first>
  <files>tests/core/transaction.test.cts, tests/hosts/cross-host.test.cts, tests/hosts/native-lifecycle.test.cts</files>
  <behavior>
    - D-07/D-12: Fresh install, update, repeat update, selective uninstall, and --all preserve sibling capability/host/user bytes.
    - D-02/D-12: Drift, symlink, special file, ambiguous source, and injected failure stop before writes or fully roll back.
    - D-08/D-09: Manual/native diagnostics remain correct through style-only and combined transitions on all hosts.
  </behavior>
  <action>Extend the three fixtures with exact current/retired paths and unrelated canaries. Exercise all five hosts and every transition, including failure seams, using only these focused lifecycle suites. Leave repository-wide npm test and ci:local to Plan 06-13 after documentation, retirement audit, and package/smoke surfaces are also current. Do not change transaction production code unless a focused invariant exposes a scoped defect.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:transaction &amp;&amp; npm run test:cross-host &amp;&amp; node --test dist-tests/hosts/native-lifecycle.test.cjs</automated>
  </verify>
  <done>Focused five-host lifecycle suites prove atomicity without triggering repository-wide gates early.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Valid state to deletion authority | Only exact owned files/digests may authorize mutation. |
| Desired state to filesystem | Multi-file/section changes cross the sole transaction boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-11-01 | Tampering | retired-path reconciliation | high | mitigate | D-02/D-12 exact ownership and expected-digest tests. |
| T-06-11-02 | Elevation of Privilege | unmanaged paths | high | mitigate | Unowned paths are preserved/refused and never adopted. |
| T-06-11-03 | Denial of Service | transaction failure | medium | mitigate | Injected failures prove complete rollback. |
| T-06-11-04 | Information Disclosure | lifecycle diagnostics | medium | mitigate | Synthetic canaries and path-only outputs preserve secret safety. |
</threat_model>

<verification>
Run ZCode, capability, transaction, cross-host, and native-lifecycle tests. Confirm stale compiled tests remain absent and all adapter/generator expectations are current; defer the full repository suite to 06-13.
</verification>

<source_audit>
D-01/D-02/D-07/D-08/D-09/D-12/D-13 are covered for ZCode and the complete five-host lifecycle. Documentation and distribution closure follow.
</source_audit>

<success_criteria>
- All five adapters know the new paths before reconciliation.
- Only current-state-owned retired paths are changed.
- Every focused lifecycle/refusal/rollback invariant passes; full npm test remains a final 06-13 gate.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-11-SUMMARY.md when done.
</output>

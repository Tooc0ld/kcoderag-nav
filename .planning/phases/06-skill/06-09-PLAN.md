---
phase: 06-skill
plan: "09"
type: execute
wave: 9
depends_on:
  - 06-08
files_modified:
  - src/hosts/codex.cts
  - tests/hosts/codex.test.cts
  - src/hosts/claude.cts
  - tests/hosts/claude.test.cts
  - tests/hosts/honest-events.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-07/D-10: Codex and Claude project the four exact public roots; Codex installs all four metadata files."
    - "D-08/D-09: Codex and adjacent Claude are manual-only while exact Claude 2.1.241 owns the native overlay."
    - "D-02/D-12: Old installed paths are only candidates for later state-owned reconciliation; adapters never scan or broadly delete."
  artifacts:
    - path: "src/hosts/codex.cts"
      provides: "Codex four-Skill manual projection"
    - path: "src/hosts/claude.cts"
      provides: "Claude manual base plus exact native overlay"
  key_links:
    - from: "src/hosts/claude.cts"
      to: "src/hosts/host-version-support.cts"
      via: "centralized delivery decision"
      pattern: "manual_skill|native_pre_write"
---

<objective>
Project already-generated four-Skill inputs into Codex and Claude with honest manual/native behavior.

Purpose: Prove one manual-only host and the sole receipt-backed native host without dependency inversion.
Output: Codex/Claude desired states and exact inventory/event tests.
</objective>

## Artifacts this phase produces

- Codex four-Skill/manual-only desired state with four metadata files.
- Claude four-Skill manual state plus exact-version native overlay.
- Exact navigation-only, style-only, combined, and event-honesty tests.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-08-SUMMARY.md
@src/hosts/host-version-support.cts
</context>

<interfaces>
- Navigation-only native inventory is three public Skill files; style-only is one Skill plus four references; combined is their exact union.
- Codex additionally projects all four agents/openai.yaml files per D-10.
- Only exact Claude adds native runtime/pre-write section and receipt; all other states remain healthy manual-only.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace generated Skills into a Codex manual-only desired state</name>
  <reversibility rating="costly">Installed paths become schema-v1 ownership records.</reversibility>
  <read_first>
    <file>src/hosts/codex.cts</file>
    <file>tests/hosts/codex.test.cts</file>
    <file>.planning/phases/06-skill/06-08-SUMMARY.md</file>
  </read_first>
  <files>src/hosts/codex.cts, tests/hosts/codex.test.cts</files>
  <behavior>
    - D-01/D-07/D-10: Exact style-only and combined inventories include current roots, four references, and four metadata files under two contributors.
    - D-08/D-09: Codex style-only succeeds without version evidence and reports available/unsupported with no style-native assets.
    - D-12: Adapter remains read/render-only and preserves existing navigation sources/events.
  </behavior>
  <action>Write exact desired-state arrays first, then map the QA product inputs to matching Codex roots. Remove the all-or-nothing style refusal, project only the manual style base, and preserve MCP, SessionStart, marker, source gate, normalization, and transaction boundaries. Do not perform legacy deletion in the adapter.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:codex</automated>
  </verify>
  <done>Codex can render all four Skills and metadata with healthy manual-only style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project Claude manual base and exact native overlay</name>
  <reversibility rating="costly">The contributor split changes managed Claude files and settings sections.</reversibility>
  <read_first>
    <file>src/hosts/claude.cts</file>
    <file>tests/hosts/claude.test.cts</file>
    <file>tests/hosts/honest-events.test.cts</file>
    <file>src/hosts/host-version-support.cts</file>
  </read_first>
  <files>src/hosts/claude.cts, tests/hosts/claude.test.cts, tests/hosts/honest-events.test.cts</files>
  <behavior>
    - D-01/D-07: Navigation-only, style-only, and combined inventories equal the exact three/five/eight-file unions.
    - D-08/D-09: Every Claude version receives manual style; exact 2.1.241 alone receives and reports native availability.
    - D-12: Unrelated .mcp.json/settings content and existing fail-open events are preserved.
  </behavior>
  <action>Write exact-version, adjacent-version, and inventory tests first. Replace the binary refusal with the centralized manual base plus optional native overlay. Map generated current paths only, preserve unrelated sections and existing source/drift gates, and assert adjacent Claude owns no style pre-write section or runtime.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:claude &amp;&amp; node --test dist-tests/hosts/honest-events.test.cjs</automated>
  </verify>
  <done>Claude always delivers manual style and only exact 2.1.241 delivers native pre-write behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated package paths to host ownership | Adapter mappings define installed project authority. |
| Claude version observation to native section | Only frozen receipt evidence may grant automatic delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-09-01 | Spoofing | Claude native support | high | mitigate | Exact version/receipt and native ownership tests enforce D-08. |
| T-06-09-02 | Tampering | host configuration | high | mitigate | Existing merge and preservation fixtures cover unrelated sections. |
| T-06-09-03 | Elevation of Privilege | Codex manual-only path | high | mitigate | Tests assert no style-native runtime or section. |
</threat_model>

<verification>
Run Codex, Claude, and honest-event tests. Assert exact inventories, four Codex metadata files, manual-only Codex/adjacent Claude, and exact-Claude native delivery.
</verification>

<source_audit>
D-01/D-02/D-07 through D-10/D-12 are covered for Codex and Claude. Other hosts, lifecycle, docs, audit, and distribution follow.
</source_audit>

<success_criteria>
- Codex and Claude consume current generated inputs only.
- Manual style works on both; automatic style is exact-Claude-only.
- Host configuration and event honesty remain intact.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-09-SUMMARY.md when done.
</output>

---
phase: 06-skill
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/hosts/codex.cts
  - src/core/contracts.cts
  - src/core/state.cts
  - src/cli/commands.cts
  - tests/hosts/codex.test.cts
  - tests/cli/commands.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-07/D-08: Codex projects receive all four public Skills through two capability contributors, while unreceipted Codex owns no code-style native pre-write overlay."
    - "D-09: status and doctor independently report manualSkill and automaticNudge from validated schema-version-1 ownership and digests."
    - "D-09: Absent, invalid, and drifted evidence never becomes an available claim; human and JSON output agree and remain secret-safe."
    - "D-10: The installed Codex projection includes all four matching agents/openai.yaml files."
  artifacts:
    - path: "src/hosts/codex.cts"
      provides: "Codex four-Skill manual projection with no unsupported native overlay"
    - path: "src/core/contracts.cts"
      provides: "Stable CodeStyleDeliveryStatus contract"
    - path: "src/core/state.cts"
      provides: "Validated ownership-to-delivery derivation"
    - path: "src/cli/commands.cts"
      provides: "Matching JSON and human delivery diagnostics"
  key_links:
    - from: "src/hosts/codex.cts"
      to: "src/capabilities/code-style-nudge.cts"
      via: "adapter projects the manual contributor selected by the delivery decision"
      pattern: "kcoderag-code-style|manual_skill"
    - from: "src/core/state.cts"
      to: "src/cli/commands.cts"
      via: "validated delivery evidence is formatted without re-reading configuration contents"
      pattern: "manualSkill|automaticNudge"
---

<objective>
Prove the four-Skill interface through a Codex installation tracer and expose honest manual/native delivery diagnostics.

Purpose: Connect canonical contributors to a real host projection and make delivery claims independently verifiable without persisting a second status cache.
Output: Codex desired-state projection, delivery status types and derivation, CLI formatting, and lifecycle/diagnostic tests.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-01, D-07 through D-10, and D-12 govern this plan.
</objective>

## Artifacts this phase produces

- Codex desired state containing all four public Skill trees and metadata.
- Stable `manualSkill` and `automaticNudge` enum contracts.
- State-integrity derivation for healthy, absent, invalid, and drifted delivery evidence.
- JSON/human status and doctor tests for style-only and combined Codex installations.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-01-SUMMARY.md
@.planning/phases/06-skill/06-02-SUMMARY.md
@src/hosts/codex.cts
@src/core/contracts.cts
@src/core/state.cts
@src/cli/commands.cts
</context>

<interfaces>
- Add `CodeStyleDeliveryStatus` with `manualSkill: "available" | "not_installed" | "drifted" | "unknown"` and `automaticNudge: "available" | "unsupported" | "not_installed" | "drifted" | "unknown"` (D-09).
- Keep install-state `schemaVersion: 1` unchanged. Positive delivery values are derived from validated contributor file/section ownership, not persisted as another cache (D-09).
- Manual availability requires the style Skill plus all four required references with matching contributor digests. Automatic availability additionally requires receipt-backed native runtime and pre-write section ownership (D-08/D-09).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace all four public Skills through a manual-only Codex desired state</name>
  <reversibility rating="costly">Codex installed paths become managed ownership records reconciled by update and uninstall.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-01-SUMMARY.md</file>
    <file>.planning/phases/06-skill/06-02-SUMMARY.md</file>
    <file>src/hosts/codex.cts</file>
    <file>tests/hosts/codex.test.cts</file>
  </read_first>
  <files>src/hosts/codex.cts, tests/hosts/codex.test.cts</files>
  <behavior>
    - D-01/D-07: Navigation contributes `$kcoderag`, `$kcoderag-manage`, and `$kcoderag-feedback`; style contributes `$kcoderag-code-style` and four references.
    - D-08: Codex style-only and combined install rendering succeeds without a detectable version and owns no code-style native runtime or pre-write section.
    - D-10: All four installed Codex roots include their matching metadata files.
    - D-12: Desired state remains immutable/read-only until the existing transaction layer commits it.
  </behavior>
  <action>Write failing Codex style-only and combined desired-state assertions before adapter changes. Consume the provider contributions from Plans 06-01/06-02 and render all four public Skill roots at Codex-native paths. Include each `agents/openai.yaml`; omit the code-style handler, dispatcher, launcher, receipt, and pre-write section because Codex lacks receipt evidence per D-08. Preserve existing navigation MCP, SessionStart, success marker, source gate, normalization, ordering, and transaction routing. Do not make navigation an implicit prerequisite for style-only rendering.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:codex</automated>
  </verify>
  <acceptance_criteria>
    - D-01/D-07/D-10: Codex desired state contains the exact four public roots and metadata under two contributors.
    - D-08: Style-only Codex rendering is healthy and has no native code-style ownership.
    - D-12: Existing source, normalization, and commit boundaries remain unchanged.
  </acceptance_criteria>
  <done>A fresh Codex target can install all four public Skills, including manual-only style, without fabricated automatic ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and render independent code-style delivery diagnostics</name>
  <reversibility rating="costly">The enum strings are stable machine-readable CLI output consumed by automation and documentation.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-CONTEXT.md</file>
    <file>src/core/contracts.cts</file>
    <file>src/core/state.cts</file>
    <file>src/cli/commands.cts</file>
    <file>tests/cli/commands.test.cts</file>
    <file>tests/hosts/codex.test.cts</file>
  </read_first>
  <files>src/core/contracts.cts, src/core/state.cts, src/cli/commands.cts, tests/cli/commands.test.cts, tests/hosts/codex.test.cts</files>
  <behavior>
    - D-09: Healthy Codex style reports `manualSkill=available` and `automaticNudge=unsupported` in both JSON and human status/doctor output.
    - D-09: Capability absence reports `not_installed`; invalid state reports `unknown`; a mismatched required delivery subset reports `drifted` without turning an unaffected subset into drift.
    - D-08/D-09: A host/version label alone never produces `automaticNudge=available`.
    - Diagnostics remain read-only and return only stable codes, enum values, versions/digests when already allowed, and safe paths.
  </behavior>
  <action>Write failing status/doctor cases for healthy manual-only, capability absence, invalid state, manual-file drift, native-section drift, and unproved native support. Add the immutable `CodeStyleDeliveryStatus` contract from `<interfaces>` and derive it from the already parsed current state, contributor-specific files/sections, and digest results. Keep manual and automatic subsets independent: native absence on an unreceipted host is `unsupported`, not drift. Extend JSON and human renderers from the same status object so values cannot diverge. Preserve `status` and `doctor` read-only behavior and never inspect or emit MCP configuration values.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:codex &amp;&amp; npm run test:cli</automated>
  </verify>
  <acceptance_criteria>
    - D-09: Every evidence state maps to the exact enum contract and human/JSON output agrees.
    - D-08/D-09: Positive automatic status requires native receipt plus contributor/section integrity.
    - Diagnostics remain read-only and secret-safe.
  </acceptance_criteria>
  <done>Codex status and doctor independently and honestly report manual Skill and automatic nudge delivery from validated ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Managed state to user-visible diagnostics | Local state and digests become claims about installed behavior. |
| Provider contribution to Codex files | Canonical assets become project-owned host paths and sections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03-01 | Spoofing | `automaticNudge=available` | high | mitigate | D-08/D-09 require receipt-backed native files and section ownership before a positive claim. |
| T-06-03-02 | Tampering | installed Skill files | high | mitigate | Contributor-specific digests and composite-state validation determine status and later mutation preflight. |
| T-06-03-03 | Information Disclosure | status/doctor output | high | mitigate | Render only stable enums and safe metadata; never read or emit MCP values or file bodies. |
| T-06-03-04 | Repudiation | human/JSON mismatch | medium | mitigate | Both formats consume the same immutable status object and share deterministic tests. |
</threat_model>

<verification>
1. Run existing Codex and CLI scripts after the build.
2. Verify style-only and combined desired states own manual assets but no native style overlay.
3. Verify exact enum mappings for healthy, absent, invalid, manual drift, and native drift cases.
4. Verify human and JSON status/doctor agree and remain read-only and secret-safe.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 03

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | Codex installation and separate delivery diagnostics | COVERED | Tasks 1-2 |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback |
| RESEARCH | — | No RESEARCH.md; follow current adapter/status patterns | COVERED | read_first and 06-PATTERNS.md |
| CONTEXT | D-01/D-07/D-08 | Four Skills, two contributors, manual-only Codex | COVERED | Task 1 |
| CONTEXT | D-09 | Honest independent delivery diagnostics | COVERED | Task 2 |
| CONTEXT | D-10 | Codex metadata projection | COVERED | Task 1 |
| CONTEXT | D-12 | Existing mutation boundary | COVERED | Both tasks |
| CONTEXT | D-02–D-06/D-11/D-13 | Other contracts and closure | COVERED ELSEWHERE | Plans 06-01/02 and 06-04 through 06-08 |

Deferred ideas remain excluded: no Codex automatic nudge is enabled without a separate frozen receipt.
</source_audit>

<success_criteria>
- Codex installs all four public Skills and metadata through two capability contributors.
- Manual style is healthy without a Codex version and no native style overlay is owned.
- status and doctor report independent stable delivery enums from validated evidence.
- Invalid or drifted state cannot generate a false positive or expose sensitive configuration.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-03-SUMMARY.md` when done.
</output>

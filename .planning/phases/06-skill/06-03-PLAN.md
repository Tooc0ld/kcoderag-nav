---
phase: 06-skill
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/capabilities/contracts.cts
  - src/capabilities/code-style-nudge.cts
  - src/hosts/host-version-support.cts
  - tests/capabilities/providers.test.cts
  - src/core/contracts.cts
  - src/core/state.cts
  - src/cli/commands.cts
  - tests/cli/commands.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-07/D-08: code-style-nudge always offers the manual Skill on five hosts and adds native delivery only for exact Claude Code 2.1.241 receipt evidence."
    - "D-09: status and doctor report independent manualSkill and automaticNudge values derived from schema-v1 ownership and digests."
    - "D-09/D-12: Invalid, absent, or drifted evidence cannot become a positive claim and diagnostics remain read-only and secret-safe."
  artifacts:
    - path: "src/hosts/host-version-support.cts"
      provides: "Single manual/native evidence policy"
    - path: "src/core/contracts.cts"
      provides: "Stable delivery status enums"
    - path: "src/core/state.cts"
      provides: "Ownership-based status derivation"
    - path: "src/cli/commands.cts"
      provides: "Consistent JSON and human rendering"
  key_links:
    - from: "src/capabilities/code-style-nudge.cts"
      to: "src/hosts/host-version-support.cts"
      via: "immutable delivery decision"
      pattern: "manual_skill|native_pre_write|receiptDigest"
    - from: "src/core/state.cts"
      to: "src/cli/commands.cts"
      via: "one status object feeds both output formats"
      pattern: "manualSkill|automaticNudge"
---

<objective>
Separate code-style manual/native provider delivery and define honest independent diagnostics before host adapters consume them.

Purpose: Make all-host manual installation possible without weakening the exact receipt gate or persisting a second status cache.
Output: Provider support union, receipt matrix, delivery enums, state derivation, and focused tests.

Requirement fallback: D-07 through D-09 and D-12 are authoritative; no Phase 06 requirement ID is mapped.
</objective>

## Artifacts this phase produces

- One all-host manual style base plus exact-Claude native overlay.
- Stable manualSkill and automaticNudge enums.
- Read-only state-integrity derivation and matching output renderers.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-02-SUMMARY.md
@src/capabilities/contracts.cts
@src/hosts/host-version-support.cts
@src/core/state.cts
</context>

<interfaces>
- CapabilityId remains exactly kcoderag-navigation or code-style-nudge per D-07.
- Delivery decision is manual_skill or native_pre_write; only the native branch carries receiptDigest per D-08.
- manualSkill values are available, not_installed, drifted, unknown; automaticNudge values add unsupported per D-09.
- Positive values are derived from validated contributor files/sections and composite integrity, not stored separately.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Implement the all-host manual and exact-Claude native provider decision</name>
  <reversibility rating="costly">The support union is consumed by every adapter and diagnostic path.</reversibility>
  <read_first>
    <file>src/capabilities/contracts.cts</file>
    <file>src/capabilities/code-style-nudge.cts</file>
    <file>src/hosts/host-version-support.cts</file>
    <file>tests/capabilities/providers.test.cts</file>
  </read_first>
  <files>src/capabilities/contracts.cts, src/capabilities/code-style-nudge.cts, src/hosts/host-version-support.cts, tests/capabilities/providers.test.cts</files>
  <behavior>
    - D-07: Registry-facing contracts retain exactly two capability IDs and unchanged handler filenames.
    - D-08: Codex, Cursor, OpenCode, ZCode, unknown versions, and unreceipted Claude receive manual_skill without install refusal or native assets.
    - D-08: Exact Claude 2.1.241 alone receives native_pre_write and the frozen digest.
  </behavior>
  <action>Write the five-host and adjacent-version matrix first. Replace binary support with the immutable delivery union while retaining host-version-support.cts as the sole receipt authority. Render the Skill and four references for every host, then add native runtime/sections only on the native branch. Do not add a capability, host-local policy, receipt, dependency, or network lookup.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/capabilities/providers.test.cjs</automated>
  </verify>
  <done>The provider deterministically offers manual style everywhere and native style only for the frozen exact-Claude receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and render independent delivery diagnostics</name>
  <reversibility rating="costly">The enum strings are stable machine-readable CLI output.</reversibility>
  <read_first>
    <file>src/core/contracts.cts</file>
    <file>src/core/state.cts</file>
    <file>src/cli/commands.cts</file>
    <file>tests/cli/commands.test.cts</file>
  </read_first>
  <files>src/core/contracts.cts, src/core/state.cts, src/cli/commands.cts, tests/cli/commands.test.cts</files>
  <behavior>
    - D-09: Healthy manual-only state reports available/unsupported in both JSON and human status/doctor.
    - D-09: Absence is not_installed, invalid state is unknown, and contributor subset mismatch is drifted without contaminating an unaffected subset.
    - D-08/D-09: A host/version label alone never yields automatic availability.
  </behavior>
  <action>Write focused CLI/state fixtures for healthy manual-only, native-positive, absence, invalid state, manual drift, and native drift. Derive both dimensions from the parsed current state and contributor-specific integrity, then feed both output formats from the same object. Preserve schemaVersion 1, read-only status/doctor, path-only diagnostics, and opacity of MCP configuration values.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/cli/commands.test.cjs</automated>
  </verify>
  <done>Human and JSON diagnostics independently report provable manual and native delivery without secrets or inference from host shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host/version observation to native delivery | Untrusted labels must not grant native behavior. |
| Managed state to diagnostics | Local ownership becomes a user-visible capability claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03-01 | Spoofing | native delivery | high | mitigate | D-08 exact-version and receipt matrix tests gate native output. |
| T-06-03-02 | Tampering | contributor state | high | mitigate | D-09 validates composite and contributor digests before positive status. |
| T-06-03-03 | Information Disclosure | status/doctor | high | mitigate | Only stable enums and safe paths are rendered. |
| T-06-03-04 | Repudiation | human/JSON output | medium | mitigate | Both formats consume one immutable status object. |
</threat_model>

<verification>
Build and run only provider and CLI tests by exact path. Confirm five-host manual support, exact-Claude native support, stable enums, schema-v1 derivation, and secret-safe read-only output.
</verification>

<source_audit>
D-07/D-08/D-09/D-12 and the split-delivery portion of the goal are covered. Public assets are upstream; generated products, adapters, lifecycle, docs, and distribution are Plans 06-04 through 06-13.
</source_audit>

<success_criteria>
- Manual style is eligible on all five hosts without host_version_unsupported.
- Only exact Claude 2.1.241 has receipt-backed native delivery.
- manualSkill and automaticNudge remain independent, honest, and secret-safe.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-03-SUMMARY.md when done.
</output>

---
phase: 06-skill
plan: "08"
type: execute
wave: 8
depends_on:
  - 06-07
files_modified:
  - src/hooks/code-style-nudge.cts
  - src/hooks/pre-tool-dispatcher.cts
  - tests/hooks/code-style-nudge.test.cts
  - tests/hooks/pre-tool-dispatcher.test.cts
  - tests/hooks/session-start.test.cts
  - kcoderag-qa/hooks/code-style-nudge.cjs
  - kcoderag-qa/hooks/pre-tool-dispatcher.cjs
autonomous: true
requirements: []
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-06/D-08: Eligible automatic advice names $kcoderag-code-style and never claims a completed review."
    - "D-08/D-09: Manual-only state cannot satisfy automatic integrity or consume a reminder."
    - "D-11/D-12: Hook behavior remains bounded, contributor-specific, secret-safe, and fail-open."
  artifacts:
    - path: "src/hooks/code-style-nudge.cts"
      provides: "Current public prompt and native integrity gate"
    - path: "kcoderag-qa/hooks/code-style-nudge.cjs"
      provides: "Generated packaged Hook payload"
  key_links:
    - from: "src/hooks/code-style-nudge.cts"
      to: "src/core/state.cts"
      via: "receipt-backed native contributor verification"
      pattern: "kcoderag-code-style|receiptDigest"
---

<objective>
Align automatic style guidance and generated Hook payloads with the new public Skill after all generated Skill trees exist.

Purpose: Preserve Phase 05 fail-open integrity while preventing manual-only delivery from masquerading as native automatic support.
Output: Current Hook prompt/gate, focused tests, and two regenerated packaged CJS files.
</objective>

## Artifacts this phase produces

- Source Hook/dispatcher logic naming $kcoderag-code-style.
- Manual-only silence and receipt-backed eligibility tests.
- Byte-aligned generated Hook payloads.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-07-SUMMARY.md
@.planning/phases/05-hook-precision/05-01-SUMMARY.md
@src/hooks/code-style-nudge.cts
</context>

<interfaces>
- Manual files are insufficient for automatic advice; native files, section ownership, receipt, and composite integrity are all required per D-08/D-09.
- apply_patch remains a host editing-tool event name; D-06 removes only a public Skill operation.
- Every malformed, missing, drifted, or marker-error path emits empty output and exits zero.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rename and harden the automatic Hook path</name>
  <read_first>
    <file>src/hooks/code-style-nudge.cts</file>
    <file>src/hooks/pre-tool-dispatcher.cts</file>
    <file>tests/hooks/code-style-nudge.test.cts</file>
    <file>tests/hooks/pre-tool-dispatcher.test.cts</file>
  </read_first>
  <files>src/hooks/code-style-nudge.cts, src/hooks/pre-tool-dispatcher.cts, tests/hooks/code-style-nudge.test.cts, tests/hooks/pre-tool-dispatcher.test.cts</files>
  <behavior>
    - D-06/D-08: Eligible advice names the current Skill and remains advisory.
    - D-08/D-09: Manual-only, missing receipt, missing native section/file, and drift are silent.
    - D-12: Eligibility precedes marker claim and every failure remains fail-open.
  </behavior>
  <action>Write failing Hook/dispatcher cases first. Rename the prompt and require receipt-backed native contributor files plus pre-tool section ownership before eligibility. Preserve bounded parsing, marker order, contributor isolation, editing-tool classification, secret-free output, and exit-zero behavior.</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/hooks/code-style-nudge.test.cjs dist-tests/hooks/pre-tool-dispatcher.test.cjs</automated>
  </verify>
  <done>Only intact receipt-backed native state can emit current-name automatic advice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate Hook payloads and verify SessionStart honesty</name>
  <read_first>
    <file>tests/hooks/session-start.test.cts</file>
    <file>src/hooks/code-style-nudge.cts</file>
    <file>src/hooks/pre-tool-dispatcher.cts</file>
  </read_first>
  <files>tests/hooks/session-start.test.cts, kcoderag-qa/hooks/code-style-nudge.cjs, kcoderag-qa/hooks/pre-tool-dispatcher.cjs</files>
  <behavior>
    - D-08/D-09: SessionStart distinguishes manual availability from automatic eligibility.
    - D-13: Packaged Hook CJS is byte-aligned with compiled canonical sources.
  </behavior>
  <action>Update SessionStart focused assertions, build canonical CJS, and run QA runtime generation scoped to code-style-nudge. Verify only the two changed payloads, then run the three explicit Hook tests. Do not promote generated execution to LIVE evidence.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-cjs --capability code-style-nudge &amp;&amp; node --test dist-tests/hooks/code-style-nudge.test.cjs dist-tests/hooks/pre-tool-dispatcher.test.cjs dist-tests/hooks/session-start.test.cjs</automated>
  </verify>
  <done>Generated Hook payloads and SessionStart reporting match the split delivery contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Managed state to automatic Hook | Only complete receipt-backed ownership may trigger advice. |
| Hook input to host protocol output | Untrusted events must remain bounded and fail-open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-08-01 | Spoofing | automatic eligibility | high | mitigate | D-08/D-09 require receipt, native files, section, and digest integrity. |
| T-06-08-02 | Denial of Service | Hook dispatcher | high | mitigate | Bounded parsing and empty exit-zero failures are tested. |
| T-06-08-03 | Information Disclosure | Hook output | medium | mitigate | Only bounded advisory protocol fields are emitted. |
</threat_model>

<verification>
Run the three explicit Hook tests and scoped runtime generation; prove current prompt, manual-only silence, fail-open errors, and compiled/generated byte alignment.
</verification>

<source_audit>
D-06/D-08/D-09/D-11/D-12/D-13 are covered for Hook delivery. Five-host adapters now consume already-existing Skill/Hook inputs in Plans 06-09 through 06-11.
</source_audit>

<success_criteria>
- Automatic advice uses only $kcoderag-code-style.
- Manual-only hosts never trigger or consume an automatic reminder.
- Generated Hook payloads remain fail-open and PACKAGED-only evidence.
</success_criteria>

<output>
Create .planning/phases/06-skill/06-08-SUMMARY.md when done.
</output>

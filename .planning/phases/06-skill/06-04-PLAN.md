---
phase: 06-skill
plan: "04"
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/hooks/code-style-nudge.cts
  - src/hooks/pre-tool-dispatcher.cts
  - tests/hooks/code-style-nudge.test.cts
  - tests/hooks/pre-tool-dispatcher.test.cts
  - tests/hooks/session-start.test.cts
  - src/hosts/claude.cts
  - src/hosts/cursor.cts
  - src/hosts/opencode.cts
  - src/hosts/zcode.cts
  - tests/hosts/claude.test.cts
  - tests/hosts/cursor.test.cts
  - tests/hosts/opencode.test.cts
  - tests/hosts/zcode.test.cts
  - tests/hosts/honest-events.test.cts
autonomous: true
requirements: []
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-06/D-08: Eligible automatic guidance names $kcoderag-code-style, remains advisory, and is emitted only from exact receipt-backed native ownership."
    - "D-07/D-08: Claude, Cursor, OpenCode, and ZCode all project the manual style Skill; only exact Claude Code 2.1.241 adds native pre-write files and sections."
    - "D-09: Every adapter reports manual/native delivery from contributor evidence rather than host-shape inference."
    - "D-12: Host-specific Rule/plugin/trust/config boundaries, source gates, unrelated configuration, and fail-open events remain intact."
  artifacts:
    - path: "src/hooks/code-style-nudge.cts"
      provides: "New public prompt with native-ownership integrity gate"
    - path: "src/hosts/claude.cts"
      provides: "Manual base plus exact-receipt native overlay"
    - path: "src/hosts/cursor.cts"
      provides: "Manual-only style alongside honest Cursor surfaces"
    - path: "src/hosts/opencode.cts"
      provides: "Project-only manual style preserving config ambiguity refusal"
    - path: "src/hosts/zcode.cts"
      provides: "Style-only and combined manual projection without trust mutation"
  key_links:
    - from: "src/hooks/code-style-nudge.cts"
      to: "src/core/state.cts"
      via: "native reminder verifies receipt-backed contributor files and pre-tool section"
      pattern: "kcoderag-code-style|receiptDigest|pre-tool"
    - from: "src/hosts/claude.cts"
      to: "src/hosts/host-version-support.cts"
      via: "exact delivery mode chooses the optional native overlay"
      pattern: "native_pre_write"
---

<objective>
Align automatic guidance with the new style Skill and project the split delivery model across Claude, Cursor, OpenCode, and ZCode.

Purpose: Complete host delivery while preserving each host's actual event surface and the Phase 05 fail-open integrity contract.
Output: Renamed Hook prompt/integrity checks, four non-Codex adapter projections, and host-honesty tests.

Requirement fallback: Phase 06 has no mapped REQUIREMENTS.md IDs. D-01, D-06 through D-09, D-11, and D-12 govern this plan.
</objective>

## Artifacts this phase produces

- Automatic Hook guidance naming `$kcoderag-code-style` only after native integrity succeeds.
- Exact-Claude manual-plus-native contribution.
- Cursor, OpenCode, and ZCode manual-only style contributions.
- Host tests proving style-only/combined delivery and absence of synthetic native events.

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/06-skill/06-CONTEXT.md
@.planning/phases/06-skill/06-PATTERNS.md
@.planning/phases/06-skill/06-02-SUMMARY.md
@.planning/phases/06-skill/06-03-SUMMARY.md
@.planning/phases/05-hook-precision/05-01-SUMMARY.md
@src/hosts/host-version-support.cts
</context>

<interfaces>
- Consume the `manual_skill | native_pre_write` decision from Plan 06-02 and `CodeStyleDeliveryStatus` from Plan 06-03; do not create host-local variants (D-08/D-09).
- A manual style contribution owns the Skill plus four references. Native handlers, dispatchers, launchers, receipts, and pre-write sections are an exact-Claude overlay (D-07/D-08).
- Hook output remains bounded host-protocol JSON or empty output with exit zero; manual-only state is healthy for diagnostics but ineligible for automatic dispatch (D-08/D-09).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename and harden automatic style guidance</name>
  <read_first>
    <file>.planning/phases/05-hook-precision/05-01-SUMMARY.md</file>
    <file>src/hooks/code-style-nudge.cts</file>
    <file>src/hooks/pre-tool-dispatcher.cts</file>
    <file>tests/hooks/code-style-nudge.test.cts</file>
    <file>tests/hooks/pre-tool-dispatcher.test.cts</file>
    <file>tests/hooks/session-start.test.cts</file>
  </read_first>
  <files>src/hooks/code-style-nudge.cts, src/hooks/pre-tool-dispatcher.cts, tests/hooks/code-style-nudge.test.cts, tests/hooks/pre-tool-dispatcher.test.cts, tests/hooks/session-start.test.cts</files>
  <behavior>
    - D-06/D-08: An eligible reminder says to load `$kcoderag-code-style` and never claims the reminder performed a full review.
    - D-08/D-09: Manual-only state cannot satisfy automatic integrity even when navigation owns shared runtime files.
    - D-11: The reminder points to the short Skill; substantial guidance remains in selectively loaded references.
    - Malformed input, missing or invalid state, digest drift, unsupported delivery, and marker failure all emit no advice and exit successfully.
  </behavior>
  <action>Write failing Hook, dispatcher, and SessionStart assertions before production changes. Rename every current automatic style prompt to `$kcoderag-code-style`. Require the style contributor's receipt-backed native handler/dispatcher/launcher and pre-tool section ownership before eligibility or marker claim; shared navigation ownership and manual-only style files are insufficient. Preserve eligibility-before-claim ordering, bounded input, contributor isolation, marker semantics, empty failure output, and exit-zero behavior. Keep `apply_patch` classified as a host editing-tool event; D-06 removes only the public Skill operation.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capability-hooks &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - D-06/D-08: Every eligible style reminder names the new public Skill and remains advisory.
    - D-08/D-09: Manual-only state stays silent on the automatic path.
    - D-11: Long-form rules remain reference-owned.
    - All failure cases remain bounded and fail-open.
  </acceptance_criteria>
  <done>Automatic guidance uses the new name only when exact native ownership is valid; all other states remain silent and non-blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project Claude and Cursor delivery without event overclaim</name>
  <reversibility rating="costly">Changing the contributor split alters managed file and section ownership for installed hosts.</reversibility>
  <read_first>
    <file>.planning/phases/06-skill/06-02-SUMMARY.md</file>
    <file>.planning/phases/06-skill/06-03-SUMMARY.md</file>
    <file>src/hosts/claude.cts</file>
    <file>src/hosts/cursor.cts</file>
    <file>tests/hosts/claude.test.cts</file>
    <file>tests/hosts/cursor.test.cts</file>
    <file>tests/hosts/honest-events.test.cts</file>
  </read_first>
  <files>src/hosts/claude.cts, src/hosts/cursor.cts, tests/hosts/claude.test.cts, tests/hosts/cursor.test.cts, tests/hosts/honest-events.test.cts</files>
  <behavior>
    - D-07/D-08: Every Claude version and Cursor installation receives the manual style tree; exact Claude 2.1.241 alone receives the native style overlay.
    - D-08/D-09: Unreceipted Claude and Cursor report `available/unsupported`, while exact Claude with complete ownership reports `available/available`.
    - D-12: Cursor retains Rule/MCP/afterMCPExecution without a synthetic pre-write section; Claude preserves unrelated `.mcp.json` and settings content.
  </behavior>
  <action>Write exact-Claude, adjacent-Claude, and Cursor style-only/combined assertions first. Replace Claude's all-or-nothing refusal with a manual base plus the centralized native overlay. Remove Cursor's manual-style refusal and project only the Skill/reference base; preserve its Rule, MCP, and afterMCPExecution marker without fabricating pre-write parity. Derive both adapters' diagnostic values from actual contributor ownership and keep navigation events independent. Preserve source conflicts, drift refusal, normalization, and unrelated host configuration per D-08/D-09/D-12.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:claude &amp;&amp; npm run test:host:cursor &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Exact Claude is `available/available`; adjacent Claude and Cursor are `available/unsupported`.
    - Cursor has no code-style pre-write runtime or section.
    - Host configuration and existing navigation event behavior remain unchanged outside declared contributors.
  </acceptance_criteria>
  <done>Claude and Cursor both deliver manual style, while only exact Claude exposes receipt-backed native behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Project manual-only style to OpenCode and ZCode</name>
  <read_first>
    <file>.planning/phases/06-skill/06-PATTERNS.md</file>
    <file>src/hosts/opencode.cts</file>
    <file>src/hosts/zcode.cts</file>
    <file>tests/hosts/opencode.test.cts</file>
    <file>tests/hosts/zcode.test.cts</file>
  </read_first>
  <files>src/hosts/opencode.cts, src/hosts/zcode.cts, tests/hosts/opencode.test.cts, tests/hosts/zcode.test.cts</files>
  <behavior>
    - D-01/D-07/D-08: OpenCode and ZCode support style-only and combined states with the exact manual Skill and four references owned by `code-style-nudge`.
    - D-08/D-09: Both hosts own no native style pre-write contribution and report `available/unsupported` from valid state.
    - D-12: OpenCode remains project-only and rejects JSON/JSONC ambiguity; ZCode does not require navigation or modify workspace trust for style-only delivery.
  </behavior>
  <action>Write failing style-only, combined, diagnostic, native-absence, and preservation cases before adapter changes. Remove the manual-style support refusals and project only the Skill/reference base. Refactor ZCode's navigation-only early return into independent contributors so style-only state is complete. Preserve OpenCode's project configuration selection and dual-config hard stop, ZCode workspace-trust neutrality, their real success-marker events, source gates, and unrelated configuration. Do not project code-style handlers, launchers, markers, or pre-write sections on either host per D-08/D-12.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:opencode &amp;&amp; npm run test:host:zcode &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Style-only and combined delivery succeeds on both hosts.
    - Both report `available/unsupported` with no native code-style ownership.
    - OpenCode ambiguity and ZCode trust/project boundaries remain unchanged.
  </acceptance_criteria>
  <done>OpenCode and ZCode expose manual code-style guidance without synthetic native behavior or weakened host boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Managed state to automatic Hook | Only receipt-backed native ownership may trigger advice and consume a marker. |
| Host/version observation to adapter projection | Untrusted version input must not grant native assets or status. |
| Existing host configuration to contributor merge | User MCP, settings, Rules, plugins, and trust choices must survive capability changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-04-01 | Spoofing | automatic style eligibility | high | mitigate | D-08/D-09 require exact receipt plus native contributor and section integrity. |
| T-06-04-02 | Elevation of Privilege | manual-only host events | high | mitigate | Adapter tests assert no code-style native files or pre-write sections on unreceipted hosts. |
| T-06-04-03 | Tampering | host configuration merge | high | mitigate | Existing merge/source/drift tests preserve unrelated bytes and stop before unsafe writes. |
| T-06-04-04 | Denial of Service | advisory Hook | medium | mitigate | Bounded parsing, isolated contributor catches, empty failure output, and exit-zero tests remain required. |
| T-06-04-05 | Information Disclosure | Hook and diagnostics | medium | mitigate | Outputs contain only advisory protocol fields and safe delivery metadata, never configuration values. |
</threat_model>

<verification>
1. Build and run all existing Hook and host scripts plus the full repository test script.
2. Assert exact Claude is the only native-positive host/version.
3. Assert all four adapters support style-only and combined manual delivery.
4. Assert Cursor/OpenCode/ZCode and unreceipted Claude own no synthetic code-style native event surface.
5. Exercise malformed input and integrity failures to confirm silent fail-open behavior.
</verification>

<source_audit>
## Multi-Source Coverage Audit — Plan 04

| Source | ID | Feature / constraint | Status | Plan evidence |
|---|---|---|---|---|
| GOAL | — | All-host manual style and exact-evidence automatic delivery | COVERED | Tasks 1-3 |
| REQ | — | No Phase 06 requirement IDs are mapped | N/A | Visible fallback |
| RESEARCH | — | No RESEARCH.md; use Phase 05 Hook and existing adapter patterns | COVERED | context/read_first |
| CONTEXT | D-01/D-06 | Exact public style prompt and grammar | COVERED | Task 1 |
| CONTEXT | D-07–D-09 | Two capabilities, split delivery, honest status | COVERED | Tasks 1-3 |
| CONTEXT | D-11 | Selective references | COVERED | Task 1 |
| CONTEXT | D-12 | Host and lifecycle safety boundaries | COVERED | Tasks 2-3 |
| CONTEXT | D-02–D-05/D-10/D-13 | Other Skill contracts and distribution | COVERED ELSEWHERE | Plans 06-01/03 and 06-05 through 06-08 |

Deferred automatic support for unreceipted hosts remains excluded.
</source_audit>

<success_criteria>
- Every host can project manual `$kcoderag-code-style`; exact Claude alone owns native pre-write support.
- Hook and SessionStart guidance use only the new public name and preserve fail-open integrity.
- Manual/native diagnostics come from contributor evidence for every adapter.
- Host-specific event, project, ambiguity, trust, and configuration boundaries remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-skill/06-04-SUMMARY.md` when done.
</output>

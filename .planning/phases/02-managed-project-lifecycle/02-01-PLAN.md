---
phase: 02-managed-project-lifecycle
plan: 01
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - .planning/phases/02-managed-project-lifecycle/02-01-SUMMARY.md
  - .planning/phases/02-managed-project-lifecycle/02-VERIFICATION.md
  - .planning/phases/02-managed-project-lifecycle/02-VALIDATION.md
autonomous: true
requirements:
  - PKG-02
  - PKG-06
  - TEST-03
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "Dev is independently installable and does not depend on QA package files."
    - "QA and Dev are mutually exclusive; switching requires explicit uninstall and conflicts cause no writes."
    - "Install/status/update/uninstall preserve user configuration, plugin caches, and unrelated project bytes."
    - "Codex and Claude Code consume independently valid host-specific package metadata."
  artifacts:
    - path: "scripts/manage_project_install.py"
      provides: "Owned project lifecycle and mutual exclusion"
    - path: "tests/test_project_install.py"
      provides: "Lifecycle, idempotency, boundary, drift, and conflict evidence"
    - path: "kcoderag-dev/"
      provides: "Standalone Dev package"
    - path: ".claude-plugin/marketplace.json"
      provides: "Claude Code package distribution"
    - path: ".agents/plugins/marketplace.json"
      provides: "Codex package distribution"
  key_links:
    - from: "environment selection"
      to: "owned project state"
      via: "single-environment install transaction"
      pattern: "qa|dev"
    - from: "owned-file digests"
      to: "update/uninstall"
      via: "drift checks before replacement or deletion"
      pattern: "drifted|invalid"
---

<objective>
Retrospectively verify the managed project lifecycle already delivered by quick tasks. Confirm
Dev independence, QA/Dev mutual exclusion, explicit switching, host compatibility, and strict
project/user boundaries without changing production implementation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit lifecycle and environment isolation</name>
  <files>scripts/manage_project_install.py, tests/test_project_install.py, kcoderag-qa/, kcoderag-dev/</files>
  <action>Map the five Phase 2 requirements to current install/status/update/uninstall behavior and tests for QA-only, Dev-only, both rejection, idempotency, drift, explicit switching, user config preservation, and symlink escape.</action>
  <verify>python -m unittest tests.test_project_install -q</verify>
  <done>All lifecycle paths are covered and no original Phase 2 requirement depends on Phase 4 hardening.</done>
</task>

<task type="auto">
  <name>Task 2: Record host and boundary verification</name>
  <files>.planning/phases/02-managed-project-lifecycle/02-01-SUMMARY.md, .planning/phases/02-managed-project-lifecycle/02-VERIFICATION.md, .planning/phases/02-managed-project-lifecycle/02-VALIDATION.md</files>
  <action>Record Codex/Claude package contracts and ownership-boundary evidence. Defer user-level doctor and authenticated real-host smoke to Phase 4 and Phase 6.</action>
  <verify>python -m unittest tests.test_project_install tests.test_generation tests.test_host_smoke -q</verify>
  <done>Phase 2 is verified and all five requirements are ready to mark complete.</done>
</task>

</tasks>

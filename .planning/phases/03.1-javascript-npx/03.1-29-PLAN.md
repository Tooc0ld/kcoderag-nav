---
phase: 03.1-javascript-npx
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [JS-01, JS-02, JS-07]
estimate: {tokens: 7000, raw_tokens: 7000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-02/D-15: npm install 与 TypeScript build 前，仓库明确忽略根级 node_modules、dist、dist-tests，生成输出不污染 release cleanliness。"
    - "忽略策略只覆盖三个确定的 Node 构建根，不掩盖产品生成树、source、tests、docs 或 planning 的真实变更。"
  artifacts:
    - {path: ".gitignore", provides: "Node dependency/build output boundary"}
  key_links:
    - {from: ".gitignore", to: "Plan01 npm ci/build", via: "root-anchored ignore rules", pattern: "/(node_modules|dist|dist-tests)/"}
---

<objective>在首次 npm 安装或编译前固定根级依赖/构建输出忽略边界，使后续 pack、CI 与 release cleanliness 有稳定基线。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.gitignore
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md</context>
<tasks>
<task type="auto"><name>Task 1: 加入精确 Node 构建输出 ignore policy</name><files>.gitignore</files><action>保留现有 Python cache 与项目 credential 规则，加入且仅加入 root-anchored `/node_modules/`、`/dist/`、`/dist-tests/`。不添加 `kcoderag-*`、`src`、`tests`、`.planning`、`.gsd` 或通配 `dist*`，避免隐藏发布资产和用户工作。</action><verify><automated>git check-ignore node_modules/.probe dist/.probe dist-tests/.probe &gt; $null</automated></verify><acceptance_criteria>- 三个根路径均被 ignore。- product/source/test/planning probe 均未因新规则被 ignore。</acceptance_criteria><done>首次 install/build 不会把依赖或编译树引入工作树状态。</done></task>
<task type="auto"><name>Task 2: 固化 ignore 边界与 clean-status 基线</name><files>.gitignore</files><action>用临时 probe 路径验证规则根锚定：嵌套同名目录、生成产品树和 planning 状态不被本计划新增规则吞掉；删除 probe 后记录 `git status --short` 的既有无关基线，后续计划只比较不修改该基线。</action><verify><automated>git diff --check -- .gitignore &amp;&amp; node -e "const{spawnSync}=require('node:child_process');for(const p of ['node_modules/.probe','dist/.probe','dist-tests/.probe'])if(spawnSync('git',['check-ignore','-q',p]).status!==0)process.exit(1);for(const p of ['nested/node_modules/.probe','kcoderag-qa/.probe','src/.probe','tests/.probe','.planning/.probe'])if(spawnSync('git',['check-ignore','-q',p]).status===0)process.exit(1)"</automated></verify><acceptance_criteria>- `.gitignore` 无宽泛规则。- probe cleanup 后除 `.gitignore` 外的 tracked/untracked baseline 不变。</acceptance_criteria><done>pack/release cleanliness 可区分构建输出与真实源码/产品变更。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-29-01 | Tampering | ignore rules | high | mitigate | 三个 root-anchored exact rules 与 negative probes，禁止 broad product/source ignores。 |
| T-03.1-29-02 | Repudiation | cleanliness baseline | medium | mitigate | probe cleanup 与前后 status manifest 比对。 |
</threat_model>
<verification>`git diff --check -- .gitignore` 且三个根 probe 被 ignore、非目标 probe 不被新规则 ignore。</verification>
<success_criteria>- Node build/dependency outputs 在首次创建前拥有明确 ignore policy。- 真实发布输入仍可见。</success_criteria>
## Artifacts this phase produces
- Updated `.gitignore` with exact `/node_modules/`, `/dist/`, `/dist-tests/` rules.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-29-SUMMARY.md` when done.</output>

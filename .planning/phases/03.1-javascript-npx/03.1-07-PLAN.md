---
phase: 03.1-javascript-npx
plan: 07
type: execute
wave: 8
depends_on: ["03.1-26"]
files_modified:
  - src/hosts/codex.cts
  - src/cli/commands.cts
  - src/core/state.cts
  - tests/hosts/codex.test.cts
  - tests/migration/legacy-state.test.cts
autonomous: true
requirements: [JS-03, JS-04]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-10/D-12: Codex 仅管理目标项目 `.codex/` 与 `.agents/skills/` 中明确 section/files，并保留无关配置。"
    - "D-05: 无漂移旧 Python state 原地迁移并保留环境；漂移时写前硬停止且只删除 confirmed-owned Python。"
  artifacts:
    - {path: "src/hosts/codex.cts", provides: "Codex 完整生命周期 adapter", exports: ["codexAdapter"]}
    - {path: "tests/migration/legacy-state.test.cts", provides: "旧 state 安全迁移矩阵"}
  key_links:
    - {from: "src/hosts/codex.cts", to: "src/core/transaction.cts", via: "DesiredState", pattern: "DesiredState"}
    - {from: "src/core/state.cts", to: "legacy install-state.json", via: "schema/digest migration", pattern: "legacy"}
---
<objective>在共享核心上完成 Codex install/status/doctor/update/uninstall 与旧 Python 安装原地迁移。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-26-SUMMARY.md
@scripts/manage_project_install.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 Codex adapter 全生命周期</name>
  <files>src/hosts/codex.cts, src/cli/commands.cts, tests/hosts/codex.test.cts</files>
  <behavior>- QA 默认、Dev 显式且同 host 互斥。- 合并 Codex MCP/hook/skill 只拥有 KCodeRag section/文件；幂等与漂移语义稳定。- status/doctor 只读，update/uninstall 漂移时零写入。</behavior>
  <action>用合成配置覆盖空项目、无关 section、QA/Dev、幂等、类型冲突、symlink、drift、rollback 和 uninstall restore。`codexAdapter` 只 render/status，使用共享事务提交；不调用 marketplace 或用户级配置，不输出不透明 MCP 值。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:host:codex</automated></verify>
  <acceptance_criteria>- 五命令全通过，任何失败不改变目标。- 无关配置逐字节保留。</acceptance_criteria>
  <done>D-10、D-12、D-18 的 Codex adapter 完整可用。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 迁移无漂移旧 Python install-state</name>
  <files>src/hosts/codex.cts, src/core/state.cts, tests/migration/legacy-state.test.cts</files>
  <behavior>- 识别旧 schema，校验每个受管摘要后保留 QA/Dev profile 并事务替换 CJS/state。- 漂移、未知 owner/schema 或部分旧 payload 时零写入。- 只移除 state 明确拥有且摘要匹配的 Python 文件。</behavior>
  <action>从旧 installer fixtures 固化 legacy schema/路径。迁移先生成只读 observation，再把 CJS 写入、旧 owned Python 删除和新 state 合成一个事务；跨宿主文件不进入 desired-state。status/doctor 预览 migration，不暗中写盘。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:migration</automated></verify>
  <acceptance_criteria>- QA/Dev 无漂移样本迁移后 healthy。- 每种漂移/未知状态保持完整树不变。</acceptance_criteria>
  <done>D-05 原地迁移有成功、拒绝和回滚证据。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-07-01 | Tampering | managed Codex config | high | mitigate | section ownership、digest 与结构化 merge。 |
| T-03.1-07-02 | Elevation of Privilege | legacy deletion | high | mitigate | 仅 state-owned+digest-match 路径可删除。 |
| T-03.1-07-03 | Information Disclosure | doctor output | high | mitigate | 只报告 code/key/path，不报告值。 |
</threat_model>
<verification>`npm run test:host:codex &amp;&amp; npm run test:migration`</verification>
<success_criteria>- Codex 生命周期完整。- legacy 无漂移迁移，漂移零写入。</success_criteria>
## Artifacts this phase produces
- New files/symbols: `src/hosts/codex.cts`, `codexAdapter`, Codex lifecycle and legacy migration tests.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-07-SUMMARY.md` when done.</output>

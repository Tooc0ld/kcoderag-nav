---
phase: 03.1-javascript-npx
plan: 05
type: execute
wave: 13
depends_on: ["03.1-04"]
files_modified:
  - src/maintainer/pre-commit.cts
  - tests/maintainer/pre-commit.test.cts
  - .githooks/pre-commit
  - src/maintainer/pack-audit.cts
  - tests/maintainer/pack-audit.test.cts
  - .github/workflows/ci.yml
  - tests/maintainer/ci-contract.test.cts
autonomous: true
requirements: [JS-01, JS-06, JS-07]
estimate: {tokens: 33000, raw_tokens: 33000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-01/D-07: pre-commit 以 package.json 版本和 Node generator 为唯一事实源，并保持暂存区安全。"
    - "JS-06: pack audit 对真实 tarball 拒绝 Python、runtime compiler、未解析占位符、版本漂移或缺失自包含资产。"
    - "D-15/JS-07: required CI 在 Windows/Linux × Node22/24 运行 dependency audit、build、tests、generate-check 与 pack audit，普通 push 不发布。"
  artifacts:
    - {path: "src/maintainer/pre-commit.cts", provides: "Node 生成/暂存区提交门禁"}
    - {path: "src/maintainer/pack-audit.cts", provides: "npm tarball allow-list 审计", exports: ["auditPack"]}
    - {path: ".github/workflows/ci.yml", provides: "4-lane required test matrix"}
  key_links:
    - {from: ".githooks/pre-commit", to: "dist/maintainer/pre-commit.cjs", via: "Node launcher", pattern: "pre-commit.cjs"}
    - {from: ".github/workflows/ci.yml", to: "src/maintainer/dependency-audit.cts", via: "npm run deps:audit", pattern: "deps:audit"}
---

<objective>迁移本地提交门禁、真实 npm pack 审计和 required CI；发布 helper/workflow 与首次公开发布另行分层。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-04-SUMMARY.md
@scripts/pre_commit_generate.py
@.github/workflows/ci.yml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 迁移 pre-commit 生成与暂存区保护</name>
  <files>src/maintainer/pre-commit.cts, tests/maintainer/pre-commit.test.cts, .githooks/pre-commit</files>
  <behavior>- 受管源或生成物被暂存时运行 build+generator check；不相关提交快速通过。- hook 永不执行 git add/update-index/reset/checkout，也不改 index；若确定性生成会改变工作树，非零并要求用户显式生成、审阅和暂存。- Node/runtime/generator 失败非零并给安全路径。</behavior>
  <action>以临时 git repo + 独立 `GIT_INDEX_FILE` fixtures 迁移 Python pre-commit 行为。shell hook 只启动已构建 CJS；维护程序先保存 index 文件原始 bytes/hash和每个 staged blob OID，再使用 `git diff --cached --name-only` 与 generator check-only 模式判定。它不得运行任何 staging/revert 命令；即使选择可选 deterministic write 来展示 drift，也必须失败并保持 index byte-for-byte 不变，由用户随后显式 add。加入同一文件 staged blob=A、unstaged working bytes=B 的 partial-stage fixture，执行 hook 后断言 index bytes、blob A、working B 全部 exact unchanged；再覆盖脏工作树、路径空格和生成 drift。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:precommit</automated></verify>
  <acceptance_criteria>- partial-stage fixture 的整个 index bytes 与 staged blob OID exact unchanged，unstaged working bytes不被吸收入 index。- hook 无任何 auto-stage path；所有生成漂移阻止提交并提示 explicit user staging。- 无关 staged/unstaged bytes 不变且不泄露配置。</acceptance_criteria>
  <done>D-01/D-07 的本地自动生成门禁改为 Node。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 审计真实 npm pack 制品</name>
  <files>src/maintainer/pack-audit.cts, tests/maintainer/pack-audit.test.cts</files>
  <behavior>- 解析 `npm pack --dry-run --json`/实际临时 tgz 清单并校验 bin、`engines.node` exact `&gt;=22`、版本、自包含三宿主资产。- archive path set 必须恰好等于 Plan01 `files` 展开的逐文件清单加 npm 自动包含的根 package.json/README.md；Python runtime、`.ts/.cts`、dist-tests、node_modules、source/tests/planning、credential fixture、未解析模板或额外文件均失败。- pack 前后 tracked/untracked status 与 tree digest 不变。</behavior>
  <action>实现 `auditPack`，在隔离临时目录创建 tgz 并读取 entry names/manifest；从 package.json exact `files` 展开预期普通文件，不接受产品目录宽通配或“扩展名看似安全”代替 path equality。断言 archive paths 与 expected set 双向 exact equality、bin 指向存在的 `dist/bin/kcoderag-nav.cjs`、所有产品 runtime/config/guidance 自包含、`engines.node` 精确为 `&gt;=22`。加入额外 source/test/planning/Python/dist-tests/node_modules 文件、files-array 扩宽/漏项与 engines 漂移 fixtures。真实仓库 pack 前后比较 `git status --short` 和非 ignored tree digests；`node_modules/`、`dist/`、`dist-tests/` 必须由 Plan29 root rules ignore，临时 tgz 在隔离目录并清理。复用 `npm run deps:audit` 与 deterministic generator check，不发布。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:pack &amp;&amp; npm run pack:audit</automated></verify>
  <acceptance_criteria>- 实际 tarball path set 与 exact `files` expansion + two npm auto entries 完全相等；每类污染/漏项/engines 漂移 fixture 都非零。- pack 检查不执行 lifecycle scripts/网络 publish，且前后 status/tree exact unchanged。</acceptance_criteria>
  <done>JS-06 的自包含、无 Python、无 runtime compiler 有 tarball 证据。</done>
</task>

<task type="auto">
  <name>Task 3: 建立 Node22/24 Windows/Linux required CI</name>
  <files>.github/workflows/ci.yml, tests/maintainer/ci-contract.test.cts</files>
  <action>将 CI 改为 `windows-latest`/`ubuntu-latest` × Node 22/24；每 lane 使用 `npm ci --ignore-scripts`，随后 `npm run deps:audit`、build、完整 Node tests、generate check 和 pack audit。普通 push/PR 只运行测试且 workflow 不含 npm publish/token。为 workflow 添加 concurrency/cancel 和最小 permissions；任何 required test 的 NOT_RUN/skip 不算成功。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:ci-contract &amp;&amp; npm run ci:local</automated></verify>
  <acceptance_criteria>- 静态契约测试证明四 lane、Node22/24、`--ignore-scripts`、deps audit 与无 publish。- 本地完整 CI 命令通过。</acceptance_criteria>
  <done>D-15/JS-07 的 required CI 就绪，发布仍与普通 push 隔离。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-05-SC | Tampering | CI dependencies | high | mitigate | 审计 lock、`npm ci --ignore-scripts`、复用 exact graph audit。 |
| T-03.1-05-01 | Tampering | git index | high | mitigate | 独立 index byte snapshot、partial-stage blob fixture、禁止所有 staging/reset/checkout 操作。 |
| T-03.1-05-02 | Information Disclosure | npm tarball/logs | high | mitigate | allow-list pack 与敏感哨兵扫描。 |
</threat_model>
<verification>`npm run deps:audit &amp;&amp; npm run ci:local &amp;&amp; npm run pack:audit`</verification>
<success_criteria>- pre-commit、pack 与 required CI 全部使用 Node。- CI 复用 exact graph 审计且不含发布行为。</success_criteria>

## Artifacts this phase produces

- New files/symbols: `pre-commit.cts`, `pack-audit.cts`/`auditPack`, Node tests and required CI contract test.
- New pre-commit evidence: isolated-index and partial-stage fixtures proving index bytes/staged blob/unstaged bytes are immutable and no auto-stage command exists.
- Plan01-registered scripts made executable: `test:precommit`, `test:pack`, `pack:audit`, `test:ci-contract`, `ci:local`.

<output>Create `.planning/phases/03.1-javascript-npx/03.1-05-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 16
type: execute
wave: 11
depends_on: ["03.1-15"]
files_modified:
  - plugin-src/version.txt
  - plugin-src/hooks/grep_nudge.py
  - plugin-src/hooks/update_check.py
  - plugin-src/hooks/test_grep_nudge.py
  - kcoderag-qa/hooks/grep_nudge.py
  - kcoderag-qa/hooks/update_check.py
  - kcoderag-qa/hooks/test_grep_nudge.py
  - kcoderag-dev/hooks/grep_nudge.py
  - kcoderag-dev/hooks/update_check.py
  - kcoderag-dev/hooks/test_grep_nudge.py
autonomous: true
requirements: [JS-01, JS-06]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-01/D-07: parity gate 后删除规范源与 QA/Dev 生成树的 Python runtime/tests，并移除旧 version.txt。"
    - "删除只使用明确 tracked paths；CJS 生成/launcher tests 随每组删除继续通过。"
  artifacts:
    - {path: "plugin-src/hooks/", provides: "仅模板/非 Python runtime 规范源"}
    - {path: "kcoderag-qa/hooks/", provides: "仅 CJS/launcher/config 的 QA hook tree"}
    - {path: "kcoderag-dev/hooks/", provides: "仅 CJS/launcher/config 的 Dev hook tree"}
  key_links:
    - {from: "package.json.version", to: "generated manifests", via: "generator", pattern: "version"}
---
<objective>在 Plan15 全绿后，按三组明确路径退役插件规范源和两份生成包的 Python runtime/test。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 删除 plugin-src Python runtime 与旧版本源</name><files>plugin-src/version.txt, plugin-src/hooks/grep_nudge.py, plugin-src/hooks/update_check.py, plugin-src/hooks/test_grep_nudge.py</files><action>先再次运行预退役 gate，再仅删除列出的 tracked files。确认 generator 已以 package.json+CJS sources 替代它们；禁止递归删除、glob 或触碰 environment/templates。</action><verify><automated>npm run verify:parity-before-retire &amp;&amp; npm run generate -- --check --package all</automated></verify><acceptance_criteria>- 四个路径不存在。- package version/generator/hook tests 仍通过。</acceptance_criteria><done>规范源不再含 Python runtime 或第二版本源。</done></task>
<task type="auto"><name>Task 2: 删除 QA 生成树 Python runtime/test</name><files>kcoderag-qa/hooks/grep_nudge.py, kcoderag-qa/hooks/update_check.py, kcoderag-qa/hooks/test_grep_nudge.py</files><action>仅删除三个 tracked QA Python 文件，运行 QA generation check、launcher/hook/update tests，确认 CJS 是唯一启动目标。不要删除可能存在的用户/未跟踪文件；若发现意外 pycache 由 retirement audit 报告而非宽泛删除。</action><verify><automated>npm run generate -- --check --package qa &amp;&amp; npm run test:hook &amp;&amp; npm run test:update-check</automated></verify><acceptance_criteria>- QA 生成树无 tracked Python。- QA CJS/launcher 自包含继续通过。</acceptance_criteria><done>QA shipped runtime 完全 Node 化。</done></task>
<task type="auto"><name>Task 3: 删除 Dev 生成树 Python runtime/test</name><files>kcoderag-dev/hooks/grep_nudge.py, kcoderag-dev/hooks/update_check.py, kcoderag-dev/hooks/test_grep_nudge.py</files><action>仅删除三个 tracked Dev Python 文件，运行 Dev generation check 与隔离 launcher/hook/update tests；保持 Dev 独立于 QA。</action><verify><automated>npm run generate -- --check --package dev &amp;&amp; npm run test:generator -- --package dev</automated></verify><acceptance_criteria>- Dev 生成树无 tracked Python。- 删除 QA tree 的 fixture 中 Dev 仍通过。</acceptance_criteria><done>Dev shipped runtime 完全 Node 化。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-16-01 | Tampering | deletion scope | high | mitigate | explicit tracked paths、无递归/glob、每组后 generation check。 |
| T-03.1-16-02 | Denial of Service | missing runtime | high | mitigate | parity prerequisite 与逐环境 CJS launcher tests。 |
</threat_model>
<verification>`npm run generate:check &amp;&amp; npm run test:hook &amp;&amp; npm run test:update-check`</verification>
<success_criteria>- plugin-src/QA/Dev 不再携带 Python runtime/test/version source。- CJS 路径保持全绿。</success_criteria>
## Artifacts this phase produces
- Removed files: the ten explicitly listed Python/version artifacts; no wildcard removal.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-16-SUMMARY.md` when done.</output>

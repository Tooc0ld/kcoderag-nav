---
phase: 03.1-javascript-npx
plan: 16
type: execute
wave: 18
depends_on: ["03.1-30"]
files_modified:
  - plugin-src/version.txt
  - plugin-src/hooks/grep_nudge.py
  - plugin-src/hooks/update_check.py
  - plugin-src/hooks/test_grep_nudge.py
  - kcoderag-qa/hooks/grep_nudge.py
  - kcoderag-qa/hooks/update_check.py
  - kcoderag-qa/hooks/test_grep_nudge.py
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Pre-retirement receipt is verified before the first deletion, never rerun against deleted sources."
    - "Plan30 已在最后一次 Python parity 后清除五个 exact owned cache roots；本计划不得重新运行 Python 或重建 pyc。"
    - "Only four plugin-src and three QA tracked legacy paths are deleted; no stage-wide generate check runs during partial retirement."
  artifacts:
    - {path: "plugin-src/hooks/", provides: "CJS-oriented canonical hook templates only"}
    - {path: "kcoderag-qa/hooks/", provides: "QA CJS runtime plus launchers/config"}
  key_links:
    - {from: "Plan15 receipt", to: "first delete", via: "verify-pre-receipt", pattern: "PRE-RETIREMENT"}
---
<objective>先复验持久化 parity receipt，再精确删除 plugin-src 和 QA Python；Dev/update-index 留给 Plan24 完成同一 source stage。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 在任何 tracked 删除前验证 receipt 与 cache-clean 状态</name><files>None (read-only receipt/inventory check)</files><action>执行 `--verify-pre-receipt`，确认 receipt、inventory hash、required suite bits和当前代码 lineage 有效；断言 Plan30 的五个 exact `__pycache__` roots 均不存在，保存删除前 tracked status。失败立即停止，不重新运行依赖旧 Python 的 pre parity，也不在本计划执行任何 cache/parent recursive delete。</action><verify><automated>node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json &amp;&amp; node -e "const fs=require('node:fs');for(const r of ['plugin-src/hooks/__pycache__','kcoderag-qa/hooks/__pycache__','kcoderag-dev/hooks/__pycache__','scripts/__pycache__','tests/__pycache__'])if(fs.existsSync(r))process.exit(1)"</automated></verify><acceptance_criteria>- Valid receipt and five-root absence required before deletion.- no files changed and no Python invocation.</acceptance_criteria><done>tracked 删除获得可复验授权且 cache 不会在退役中复生。</done></task>
<task type="auto"><name>Task 2: 删除 plugin-src Python 与 version.txt</name><files>plugin-src/version.txt, plugin-src/hooks/grep_nudge.py, plugin-src/hooks/update_check.py, plugin-src/hooks/test_grep_nudge.py</files><action>仅删除四个 tracked paths；禁止 glob/recursive delete。验证 package.json 仍是唯一版本源且 Node hook/generator fixture tests 通过；不运行 pre/post-source/all-product generate check，因为 Dev/QA stage 尚未完整。</action><verify><automated>npm run build &amp;&amp; npm run test:hook &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- four files absent.- no other plugin-src/user files changed.</acceptance_criteria><done>canonical Python source/version retired。</done></task>
<task type="auto"><name>Task 3: 删除 QA generated Python runtime/tests</name><files>kcoderag-qa/hooks/grep_nudge.py, kcoderag-qa/hooks/update_check.py, kcoderag-qa/hooks/test_grep_nudge.py</files><action>仅删除三项 QA tracked legacy files。运行 Node hook/launcher/update tests和 QA CJS smoke；仍不运行 stage-wide generate/post-source，Dev/update index 由 Plan24 完成。</action><verify><automated>npm run test:hook &amp;&amp; npm run test:launcher &amp;&amp; npm run test:update-check</automated></verify><acceptance_criteria>- QA tracked Python absent and CJS path passes.- Dev tree untouched.</acceptance_criteria><done>QA shipped runtime Node-only，partial source stage clearly remains。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-16-01 | Tampering | deletion scope | high | mitigate | exact files and no glob/recursive operation. |
| T-03.1-16-02 | Repudiation | pre authorization | high | mitigate | receipt verified before first delete. |
</threat_model>
<verification>Receipt verification precedes targeted Node tests; no unsupported intermediate retirement mode is invoked.</verification>
<success_criteria>- plugin-src and QA legacy files are removed without premature all-stage checks.</success_criteria>
## Artifacts this phase produces
- Removed seven explicit plugin-src/QA legacy files.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-16-SUMMARY.md` when done.</output>

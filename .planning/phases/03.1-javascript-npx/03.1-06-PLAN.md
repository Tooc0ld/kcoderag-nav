---
phase: 03.1-javascript-npx
plan: 06
type: execute
wave: 10
depends_on: ["03.1-04", "03.1-05"]
files_modified:
  - src/smoke/host-smoke.cts
  - src/smoke/stub-mcp-server.cts
  - tests/smoke/host-smoke.test.cts
  - .github/workflows/ci.yml
autonomous: true
requirements: [JS-01, JS-08]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "JS-08: required contract smoke 对三宿主逐项证明 install、MCP tool 名、hook/Rule evidence、update、uninstall 与 stub receipt。"
    - "required 模式的 FAIL 和 NOT_RUN 都非零；optional authenticated/live 模式可以 NOT_RUN，但没有完整证据绝不 PASS。"
  artifacts:
    - {path: "src/smoke/host-smoke.cts", provides: "诚实三态 host smoke runner", exports: ["runHostSmoke"]}
    - {path: "src/smoke/stub-mcp-server.cts", provides: "loopback MCP 请求收据服务"}
  key_links:
    - {from: "src/smoke/host-smoke.cts", to: "src/smoke/stub-mcp-server.cts", via: "真实 MCP initialize/tools/list/call receipt", pattern: "receipt"}
    - {from: ".github/workflows/ci.yml", to: "src/smoke/host-smoke.cts", via: "required contract mode", pattern: "smoke:required"}
---

<objective>把 host smoke 与 stub MCP 迁移为 Node，并修正 required/optional 三态语义后接入 required CI。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/03.1-javascript-npx/03.1-04-SUMMARY.md
@scripts/run_host_smoke.py
@tests/stub_mcp_server.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现带证据位和 receipt 的诚实 smoke runner</name>
  <files>src/smoke/host-smoke.cts, src/smoke/stub-mcp-server.cts, tests/smoke/host-smoke.test.cts</files>
  <behavior>
    - required contract mode 只有 install/status、预期工具注册名、hook 或 Cursor Rule、MCP initialize/list/call receipt、update、uninstall 全部为真才 PASS/exit0。
    - required 模式 FAIL 与 NOT_RUN 都 exit 非零；缺任一证据位或缺 stub receipt 不能通过。
    - optional authenticated/live 模式允许环境不可用时 NOT_RUN/exit0，但只有真实完整证据才 PASS，失败仍非零。
    - `--package-spec <local-tgz|name@version>` 在全新临时项目执行实际 npx/npm-exec 获取；CI 默认本地 tgz，Plan14 使用公开 exact version。
  </behavior>
  <action>先写状态矩阵测试，显式定义 `mode=required-contract|optional-live`、`status=PASS|FAIL|NOT_RUN` 与 evidence schema。Node stub 必须记录去敏的 initialize、tools/list、tools/call 请求收据；runner 为 codex/claude/cursor 建临时项目，通过 `--package-spec` 指定的本地 tgz或 exact public version 获取 CLI，再以实际配置/launcher/Rule 驱动 loopback server。首次包获取失败必须发生在任何目标写入前。禁止把“命令存在”或 skip 当工具调用证据。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:smoke &amp;&amp; npm run smoke:required</automated></verify>
  <acceptance_criteria>
    - 每个 evidence bit 与 receipt 缺失 fixture 都使 required 模式非零。
    - optional NOT_RUN 明确标记且不伪装 PASS；optional FAIL 非零。
    - 输出不含 MCP header/Bearer/完整配置。
  </acceptance_criteria>
  <done>required smoke 不再以 NOT_RUN 成功，三宿主合同具有真实 loopback 收据。</done>
</task>

<task type="auto">
  <name>Task 2: 将 required contract smoke 接入四 lane CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>使用 Plan01 已注册的 `smoke:required` 与 `smoke:live`。四个 required lane 都运行 loopback contract smoke，任何 FAIL/NOT_RUN 使 job 失败；authenticated/live job 仅在显式凭据/runner 条件满足时运行并保留三态报告，不能成为 required contract 的替代。CI 日志只上传去敏 evidence JSON，不上传项目配置或环境值。</action>
  <verify><automated>npm run smoke:required &amp;&amp; npm run test:ci-contract</automated></verify>
  <acceptance_criteria>- required CI 没有 allow-failure/continue-on-error/skip-as-pass。- live 未配置时只能显示 NOT_RUN，不能生成 PASS。</acceptance_criteria>
  <done>Windows/Linux × Node22/24 都强制执行诚实 contract smoke。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-06-01 | Spoofing | smoke evidence | high | mitigate | 显式 evidence bits、stub receipt 与缺证据非零。 |
| T-03.1-06-02 | Information Disclosure | CI artifact | high | mitigate | 去敏 schema 与禁止上传配置/环境。 |
| T-03.1-06-03 | Denial of Service | loopback server | medium | mitigate | localhost、随机端口、有界超时与 finally 清理。 |
</threat_model>
<verification>`npm run test:smoke &amp;&amp; npm run smoke:required &amp;&amp; npm run ci:local`</verification>
<success_criteria>- required NOT_RUN/FAIL 非零。- 三宿主全证据与 stub receipt 才能 PASS。</success_criteria>

## Artifacts this phase produces

- New files/symbols: Node smoke runner `runHostSmoke`, loopback stub and evidence-state tests.
- New modes/scripts/flags: `required-contract`, `optional-live`, `smoke:required`, `smoke:live`, `--package-spec`.

<output>Create `.planning/phases/03.1-javascript-npx/03.1-06-SUMMARY.md` when done.</output>

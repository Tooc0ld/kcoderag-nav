---
phase: 03.1-javascript-npx
plan: 12
type: execute
wave: 9
depends_on: ["03.1-09"]
files_modified:
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - kcoderag-cursor/mcp.json
  - kcoderag-cursor/rules/kcoderag-navigation.mdc
  - kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
autonomous: true
requirements: [JS-03, JS-06]
estimate: {tokens: 17000, raw_tokens: 17000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-07/D-10: Cursor metadata、MCP、Rule 与 skill 由 package.json/profile 生成，项目集成不伪装 hook。"
    - "Cursor 生成树确定、自包含且可由 npx adapter 复制到 `.cursor/`。"
  artifacts:
    - {path: "kcoderag-cursor/rules/kcoderag-navigation.mdc", provides: "Cursor always-on 导航 Rule"}
    - {path: "kcoderag-cursor/mcp.json", provides: "Cursor MCP profile"}
  key_links:
    - {from: "src/generator/index.cts", to: "kcoderag-cursor/", via: "--package cursor", pattern: "cursor"}
---
<objective>确定性生成 Cursor 免费项目集成所需的 Rule、skill、MCP 和版本元数据。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md
@plugin-src/cursor/rules/kcoderag-navigation.mdc</context>
<tasks>
<task type="auto"><name>Task 1: 生成 Cursor Rule 与 skill guidance</name><files>kcoderag-cursor/rules/kcoderag-navigation.mdc, kcoderag-cursor/skills/code-lookup-discipline/SKILL.md</files><action>运行 `generate --package cursor --group metadata-guidance`，只从规范模板生成 Rule/skill并断言 observed write set 恰好两路径；Rule 明确是指导规则而非 PreToolUse hook。</action><verify><automated>npm run generate -- --package cursor --group metadata-guidance &amp;&amp; npm run generate -- --check --package cursor --group metadata-guidance</automated></verify><acceptance_criteria>- 无 hook 文件/声明。- 未选 config/docs bytes不变。</acceptance_criteria><done>Cursor guidance 按真实宿主语义生成。</done></task>
<task type="auto"><name>Task 2: 生成 Cursor manifest/MCP config 并验证 metadata union</name><files>kcoderag-cursor/.cursor-plugin/plugin.json, kcoderag-cursor/mcp.json</files><action>运行 `generate --package cursor --group metadata-config`，从 package.json version 与选定环境 profile 只生成 manifest/MCP，断言 observed write set 恰好两路径且配置内容不进入日志/快照；兼容 manifest 不构成 D-03 marketplace 用户入口。随后 check Cursor metadata union，校验 version、QA 默认 profile、Rule/skill namespace与无未解析 token。</action><verify><automated>npm run generate -- --package cursor --group metadata-config &amp;&amp; npm run generate -- --check --package cursor --group metadata &amp;&amp; npm run test:generator -- --package cursor</automated></verify><acceptance_criteria>- metadata union 两次生成字节一致，docs bytes不变。- metadata/version/profile 与 package source 一致。</acceptance_criteria><done>Cursor metadata tree 可被 adapter 自包含消费。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-12-01 | Spoofing | Cursor capability claim | medium | mitigate | Rule 明示非 hook，测试禁止 hook 声明。 |
| T-03.1-12-02 | Information Disclosure | MCP generation | high | mitigate | 不透明复制与无值日志。 |
</threat_model>
<verification>`npm run generate -- --check --package cursor --group metadata &amp;&amp; npm run test:generator -- --package cursor`</verification>
<success_criteria>- Cursor Rule/skill/MCP/metadata deterministic。- 不声称 hook 等价或 marketplace 依赖。</success_criteria>
## Artifacts this phase produces
- Generated Cursor metadata, MCP file, Rule and skill.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-12-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 11
type: execute
wave: 9
depends_on: ["03.1-09"]
files_modified:
  - kcoderag-dev/hooks/grep-nudge.cjs
  - kcoderag-dev/hooks/update-check.cjs
  - kcoderag-dev/hooks/update-worker.cjs
  - kcoderag-dev/hooks/run_hook.cmd
  - kcoderag-dev/hooks/run_hook.sh
  - kcoderag-dev/hooks/hooks.json
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["D-01/D-02: Dev tree has an independent CJS hook/update runtime.", "Dev runtime has no QA file dependency and preserves advisory fail-open behavior."]
  artifacts:
    - {path: "kcoderag-dev/hooks/grep-nudge.cjs", provides: "Dev hook runtime"}
    - {path: "kcoderag-dev/hooks/hooks.json", provides: "Dev host hook registration"}
  key_links:
    - {from: "kcoderag-dev/hooks/hooks.json", to: "kcoderag-dev/hooks/run_hook.sh", via: "relative launcher", pattern: "run_hook"}
---
<objective>生成并校验 Dev 的六项 Node hook/runtime 资产；其它 Dev 产品资产由 Plan28 独立拥有。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 生成 Dev CJS runtime 与 launchers</name><files>kcoderag-dev/hooks/grep-nudge.cjs, kcoderag-dev/hooks/update-check.cjs, kcoderag-dev/hooks/update-worker.cjs, kcoderag-dev/hooks/run_hook.cmd, kcoderag-dev/hooks/run_hook.sh</files><action>运行 `generate --package dev --group runtime-code`，只复制三个 CJS 和 launchers并断言 observed write allow-list 恰好本任务五路径；暂留旧 Python 到 parity gate。用缺失 QA tree fixture 验证 Dev 启动不依赖 QA。</action><verify><automated>npm run generate -- --package dev --group runtime-code &amp;&amp; npm run test:launcher</automated></verify><acceptance_criteria>- Dev CJS/launcher 自包含且 metadata/docs/registration bytes不变。- Node 异常仍 exit0。</acceptance_criteria><done>Dev runtime code 独立 Node 化。</done></task>
<task type="auto"><name>Task 2: 生成 Dev hook 注册并做单组 check</name><files>kcoderag-dev/hooks/hooks.json</files><action>运行 `generate --package dev --group runtime-registration`，只生成 Dev hook 注册并断言 observed write set 恰好本任务一路径；验证只引用同树 launchers/Dev namespace，随后 check runtime union。</action><verify><automated>npm run generate -- --package dev --group runtime-registration &amp;&amp; npm run generate -- --check --package dev --group runtime &amp;&amp; npm run test:hook</automated></verify><acceptance_criteria>- Dev runtime union check 通过且 metadata/docs bytes不变。- 无 QA runtime dependency。</acceptance_criteria><done>Dev hook 注册和 runtime code 一致。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-11-01 | Denial of Service | Dev launcher | high | mitigate | self-relative CJS and fail-open. |
| T-03.1-11-02 | Tampering | Dev/QA isolation | high | mitigate | missing-QA fixture and namespace assertions. |
</threat_model>
<verification>`npm run generate -- --check --package dev --group runtime &amp;&amp; npm run test:hook &amp;&amp; npm run test:launcher`</verification>
<success_criteria>- Dev six-file hook runtime is generated and independent.</success_criteria>
## Artifacts this phase produces
- Generated Dev CJS hook/update runtimes, launchers and hook registration.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-11-SUMMARY.md` when done.</output>

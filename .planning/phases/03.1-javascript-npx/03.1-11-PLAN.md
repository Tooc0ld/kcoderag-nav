---
phase: 03.1-javascript-npx
plan: 11
type: execute
wave: 10
depends_on: ["03.1-09"]
files_modified:
  - kcoderag-dev/hooks/grep-nudge.cjs
  - kcoderag-dev/hooks/update-check.cjs
  - kcoderag-dev/hooks/update-worker.cjs
  - kcoderag-dev/hooks/run_hook.cmd
  - kcoderag-dev/hooks/run_hook.sh
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["D-01/D-02: Dev tree has an independent CJS hook/update runtime.", "Dev runtime has no QA file dependency and preserves advisory fail-open behavior."]
  artifacts:
    - {path: "kcoderag-dev/hooks/grep-nudge.cjs", provides: "Dev hook runtime"}
    - {path: "kcoderag-dev/hooks/run_hook.sh", provides: "Dev Unix fail-open launcher"}
  key_links:
    - {from: "kcoderag-dev/hooks/hooks.json", to: "kcoderag-dev/hooks/run_hook.sh", via: "relative launcher", pattern: "run_hook"}
---
<objective>只生成 live bytes 确认会变化的 Dev 三项 CJS 与两项 launcher；既有注册/metadata 由 Plan23 只读校验。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 生成 Dev 三项 CJS runtime</name><files>kcoderag-dev/hooks/grep-nudge.cjs, kcoderag-dev/hooks/update-check.cjs, kcoderag-dev/hooks/update-worker.cjs</files><action>运行 `generate --package dev --group runtime-cjs`，只写 live tree 中尚不存在的三个 CJS 并断言 observed write set 恰好本任务三路径；暂留旧 Python 到 parity gate。用缺失 QA tree fixture 验证 Dev 运行不依赖 QA。</action><verify><automated>npm run generate -- --package dev --group runtime-cjs &amp;&amp; npm run generate -- --check --package dev --group runtime-cjs &amp;&amp; npm run test:hook</automated></verify><acceptance_criteria>- three CJS paths are new and match dist.- QA及其它 group bytes不变。</acceptance_criteria><done>Dev CJS runtime 独立 Node 化。</done></task>
<task type="auto"><name>Task 2: 生成 Dev 两项 Node launcher</name><files>kcoderag-dev/hooks/run_hook.cmd, kcoderag-dev/hooks/run_hook.sh</files><action>运行 `generate --package dev --group runtime-launcher`；live launcher 当前调用 Python，canonical Node bytes 保证变化，observed write set 必须恰好本任务两路径。验证 self-relative CJS 与异常 exit0；registration 只读留给 Plan23。</action><verify><automated>npm run generate -- --package dev --group runtime-launcher &amp;&amp; npm run generate -- --check --package dev --group runtime-code &amp;&amp; npm run test:launcher</automated></verify><acceptance_criteria>- two launchers changed exactly once, runtime-code union clean.- 无 QA runtime dependency。</acceptance_criteria><done>Dev runtime-code 五个保证变化路径完成。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-11-01 | Denial of Service | Dev launcher | high | mitigate | self-relative CJS and fail-open. |
| T-03.1-11-02 | Tampering | Dev/QA isolation | high | mitigate | missing-QA fixture and namespace assertions. |
</threat_model>
<verification>`npm run generate -- --check --package dev --group runtime-code &amp;&amp; npm run test:hook &amp;&amp; npm run test:launcher`</verification>
<success_criteria>- Dev five guaranteed-changing runtime paths are generated and independent; registration remains byte-equal and unwritten.</success_criteria>
## Artifacts this phase produces
- Generated Dev CJS hook/update runtimes and launchers; registration is read-only checked in Plan23.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-11-SUMMARY.md` when done.</output>

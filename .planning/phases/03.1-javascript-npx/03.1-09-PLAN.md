---
phase: 03.1-javascript-npx
plan: 09
type: execute
wave: 8
depends_on: ["03.1-07"]
files_modified:
  - src/generator/index.cts
  - tests/generator/generation.test.cts
autonomous: true
requirements: [JS-01, JS-06]
estimate: {tokens: 25000, raw_tokens: 25000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-07: Node generator 从 package.json 唯一版本源和 plugin-src 规范源生成 QA/Dev/Cursor，支持 deterministic write/check。"
    - "生成器复制编译 CJS、launcher、skills 与不透明环境配置，不执行或打印凭据。"
  artifacts:
    - {path: "src/generator/index.cts", provides: "确定性多产品生成器", exports: ["generatePackage", "checkGenerated"]}
  key_links:
    - {from: "package.json", to: "src/generator/index.cts", via: "唯一版本字段", pattern: "version"}
    - {from: "src/generator/index.cts", to: "kcoderag-qa|dev|cursor", via: "sorted atomic render", pattern: "generatePackage"}
---
<objective>实现可按单产品运行的 Node 确定性生成器，为后三个生成包窄计划提供稳定接口。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-03-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-08-SUMMARY.md
@scripts/generate_plugins.py
@plugin-src/environments.json</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现纯渲染与确定性 check</name>
  <files>src/generator/index.cts, tests/generator/generation.test.cts</files>
  <behavior>- 相同 source/version 输入连续生成字节级一致。- `--check` 只比较并非零报告安全相对路径，不写文件。- 单产品 qa/dev/cursor 与 all 使用相同 render core。- desired manifest 为每个产品标注 leaf asset group，组合 group 只取明确 leaf 并集，writer 只能触及所选 exact allow-list。</behavior>
  <action>先固定现有模板/env 行为，定义排序 path→Buffer manifest、token allow-list、newline/JSON determinism 和 atomic writes。固定 leaf/union map：QA/Dev `runtime-code` 各为三个 CJS+两个 launcher，`runtime-registration` 各为 hooks.json，`runtime` 为两者并集；`metadata-config` 各为两 MCP+两 host manifest，`metadata-guidance` 各为 skill+agent，`metadata` 为两者并集；`docs` 各为生成 README，`version` 各为两 host manifest。Cursor `metadata-config` 为 manifest+MCP、`metadata-guidance` 为 Rule+skill、`metadata` 为并集、`docs` 为 README、`version` 为 manifest，不提供任何 runtime group。`all` 是产品合法 leaf 的并集。writer 必须先渲染/验证选中集合，只替换 bytes 变化的选中路径，不能扫描后顺手重写其它 group；测试记录实际 open/rename write set。环境配置只作为不透明 bytes/template values，测试使用合成哨兵并确保日志无值。生成器要求 build outputs 已存在，不在用户运行时编译；新 desired output 明确不生成 `kcoderag-update.json` 或 GitHub-raw consumer。迁移窗口中 check 忽略但不拥有/重建 explicit legacy Python/update-index inventory，最终清理由 retirement state machine 负责。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:generator</automated></verify>
  <acceptance_criteria>- write/check/double-generate/unknown-token/path escape 都有测试。- 每个 product/group fixture 的 observed write set 恰好等于 selected changed allow-list，未选 group bytes/mtime 不变。- check 模式完整树摘要不变。</acceptance_criteria>
  <done>D-07 的单版本源与 JS-06 determinism core 成立。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 暴露按产品生成/检查脚本</name>
  <files>src/generator/index.cts, tests/generator/generation.test.cts</files>
  <action>实现 Plan01 已注册的 `generate --package qa|dev|cursor|all --group runtime-code|runtime-registration|runtime|metadata-config|metadata-guidance|metadata|docs|version|all` 与 `--check`，并提供测试专用依赖注入/`--source-root`、`--output-root`，使本计划只在临时 fixture 验证 write/check/double-generate。写前验证 fixture package version、编译 CJS 集合、模板 token、product/group compatibility 和 exact target allow-list；未知/空 group、任一 `cursor+runtime*`、跨 group write 和任一产品失败都非零且不提交其它结果。不得在本计划对仓库 `kcoderag-*` 树执行 real `generate:check`，该整合门由 Plan23 在 Plans10-12/27-28 完成后运行。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:generator</automated></verify>
  <acceptance_criteria>- temp fixtures 证明三种单产品/all × 全部合法 groups 的 exact write set、write/check/determinism 与 failure rollback。- incompatible/unknown/empty selection fail。- repo generated trees 未被检查或修改。</acceptance_criteria>
  <done>QA/Dev/Cursor 可在独立后续计划中无冲突生成。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-09-01 | Tampering | template path/token/group | high | mitigate | product×leaf-group exact path map、observed-write equality、root containment、unknown token/group hard fail。 |
| T-03.1-09-02 | Information Disclosure | generator logs | high | mitigate | 不透明配置值，仅输出产品/version/path。 |
| T-03.1-09-03 | Denial of Service | partial generation | medium | mitigate | 全部渲染校验后 atomic swap。 |
</threat_model>
<verification>`npm run test:generator`；该测试只使用临时 source/output roots，并覆盖 write/check/double-generate。仓库树的首次 `generate:check` 属于 Plan23。</verification>
<success_criteria>- 生成器可按产品及资产组确定性运行，实际写集严格等于声明 allow-list。- check 只读且不泄露环境配置。</success_criteria>
## Artifacts this phase produces
- New symbols: `generatePackage`, `checkGenerated`, `ASSET_GROUP_PATHS`; CLI flags `--package`, `--group runtime-code|runtime-registration|runtime|metadata-config|metadata-guidance|metadata|docs|version|all`, `--check`, test-only `--source-root`, `--output-root`.
- Plan01-registered package scripts `generate`, `generate:check`, `test:generator` become executable here; only `test:generator` is invoked in this plan, while Plan23 first invokes repository-default `generate:check`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md` when done.</output>

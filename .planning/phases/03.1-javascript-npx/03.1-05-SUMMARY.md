---
phase: 03.1-javascript-npx
plan: 05
subsystem: maintainer-ci-packaging
tags: [nodejs, npm-pack, pre-commit, github-actions, supply-chain, windows, linux]

requires:
  - phase: 03.1-01
    provides: Audited Node 22+ CJS toolchain, exact dependency graph, and npm package contract
  - phase: 03.1-04
    provides: Complete Codex, Claude Code, and Cursor project-native package assets
provides:
  - Index-immutable Node pre-commit generation gate with partial-stage proof
  - Exact real-tarball npm pack audit for three self-contained host integrations
  - Test-only Windows/Linux by Node 22/24 required GitHub Actions matrix
affects: [03.1-06, 03.1-13, release, host-smoke, npm-publish]

actuals:
  tokens: 12977
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns:
    - Snapshot the complete alternate Git index and staged blob map around read-only commit gates
    - Compare actual npm tgz entries to an expanded explicit publish allow-list
    - Pin GitHub Actions by immutable commit and keep required CI separate from publication

key-files:
  created:
    - src/maintainer/pre-commit.cts
    - tests/maintainer/pre-commit.test.cts
    - src/maintainer/pack-audit.cts
    - tests/maintainer/pack-audit.test.cts
    - tests/maintainer/ci-contract.test.cts
  modified:
    - .githooks/pre-commit
    - .github/workflows/ci.yml
    - package.json
    - src/maintainer/dependency-audit.cts

key-decisions:
  - "The Node pre-commit gate is check-only: it snapshots index bytes and staged blob OIDs, refuses partial canonical staging, and never stages or resets files."
  - "The pack gate audits one real temporary tgz against exact expanded files plus npm's package.json and README.md entries; generated user assets are placeholder-free while compiled generator logic may contain token-handling literals."
  - "Required CI is a test-only Windows/Linux by Node 22/24 matrix with immutable action pins; compiled build must precede the compiled dependency audit."
  - "The aggregate Node suite runs with concurrency one so repository immutability checks cannot race tests that intentionally create short-lived root fixtures."

patterns-established:
  - "Commit gate: relevant staging triggers build and generator check, while unrelated staging exits immediately and every path is diagnostic-only."
  - "Pack gate: local npm pack uses --ignore-scripts, a temporary destination, tar-level path equality, and before/after status plus source-tree evidence."
  - "CI gate: npm ci --ignore-scripts precedes build, dependency audit, native launcher smoke, all Node tests, generation check, and pack audit in every lane."

requirements-completed: [JS-01, JS-06, JS-07]

coverage:
  - id: D1
    description: "Node pre-commit protects partial staging and generated-product consistency without changing index or working bytes."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "tests/maintainer/pre-commit.test.cts — isolated alternate-index fixtures, 5/5 passing"
        status: pass
      - kind: other
        ref: "npm run build && npm run test:precommit"
        status: pass
    human_judgment: false
  - id: D2
    description: "A real npm tgz has exactly the approved self-contained host assets and rejects Python, runtime compilers, source/tests/planning, drift, and unresolved rendered tokens."
    requirement: JS-06
    verification:
      - kind: integration
        ref: "tests/maintainer/pack-audit.test.cts — real tgz plus contamination matrix, 6/6 passing"
        status: pass
      - kind: e2e
        ref: "npm run pack:audit — 54 exact archive entries"
        status: pass
    human_judgment: false
  - id: D3
    description: "Required CI runs the release-free Node gates on Windows/Linux with Node 22/24 and immutable action pins."
    requirement: JS-07
    verification:
      - kind: unit
        ref: "tests/maintainer/ci-contract.test.cts — matrix/order/authority contract, 4/4 passing"
        status: pass
      - kind: integration
        ref: "npm run ci:local — 106/106 Node tests plus dependency, generation, and pack gates"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 05: Node Commit, Pack, and Required CI Gates Summary

**An index-immutable Node commit gate and exact real-tarball audit now feed a release-free four-lane Windows/Linux Node 22/24 CI contract.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-08-23T16:34:48Z
- **Completed:** 2026-08-23T16:49:57Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Replaced the Python pre-commit launcher with compiled CJS and proved alternate-index bytes, staged blob OIDs, and unstaged working bytes remain exact across fast, partial-stage, drift, and failure paths.
- Added a real temporary npm tgz audit whose tar entries exactly equal the expanded publish allow-list and whose three host products contain the required runtime, configuration, and guidance assets without Python or runtime compilation.
- Replaced Python CI with a pinned, minimal-permission Windows/Linux by Node 22/24 test matrix that runs the audited dependency graph, native launchers, complete Node suite, deterministic generator, and pack gate without publication authority.

## Task Commits

TDD behavior was committed in RED/GREEN order:

1. **Task 1 RED: Specify index-safe Node pre-commit** - `58e4a9e` (test)
2. **Task 1 GREEN: Migrate pre-commit gate to Node** - `14809f4` (feat)
3. **Task 2 RED: Specify exact npm pack audit** - `c2c5a28` (test)
4. **Task 2 GREEN: Audit exact npm tarball contents** - `5504791` (feat)
5. **Task 3 RED: Specify required Node CI contract** - `821ace0` (test)
6. **Task 3 GREEN: Establish required Node CI matrix** - `1eecb5f` (feat)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `src/maintainer/pre-commit.cts` - Read-only staged-path classifier, alternate-index snapshot, staged blob proof, build/generator gate, and safe diagnostics.
- `tests/maintainer/pre-commit.test.cts` - Isolated repositories covering irrelevant staging, partial staging, path spaces, drift, and forbidden index mutation commands.
- `.githooks/pre-commit` - Node 22 launcher for the compiled pre-commit helper.
- `src/maintainer/pack-audit.cts` - Exact package allow-list expansion, tar reader, archive validator, self-contained asset checks, and repository preservation evidence.
- `tests/maintainer/pack-audit.test.cts` - Real tgz validation plus missing, extra, Python, compiler, source, planning, engine, bin, version, and placeholder fixtures.
- `.github/workflows/ci.yml` - Test-only four-lane required matrix with minimal permissions, concurrency cancellation, immutable actions, and no publish step.
- `tests/maintainer/ci-contract.test.cts` - Static workflow matrix, command-order, pin, authority, and skip-policy checks.
- `package.json` - Serializable aggregate test execution and Plan 05 local CI command.
- `src/maintainer/dependency-audit.cts` - Exact local CI orchestration policy aligned with the currently available Plan 05 gates.

## Decisions Made

- Used Git plumbing only for read-only snapshots and comparisons; the helper contains no add, update-index, reset, or checkout path.
- Allowed only `dist/` as a directory entry in `files`; every QA, Dev, and Cursor product asset remains an explicit path so a product-directory expansion cannot silently publish extras.
- Scanned rendered/user archive assets for unresolved tokens while excluding compiled `dist/` from that token check, because the generator and audit implementations intentionally contain token-matching literals.
- Kept required CI independent from release and authenticated host workflows. Plan 06 will add required loopback host smoke after its Node runner exists; Plan 13/14 remain responsible for release behavior.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Built CJS before executing the compiled dependency audit**
- **Found during:** Task 3 CI construction
- **Issue:** The action wording listed dependency audit before build, but `deps:audit` executes ignored `dist/maintainer/dependency-audit.cjs` and cannot run from a clean checkout first.
- **Fix:** Every CI lane installs with scripts disabled, builds, then audits the exact installed graph before any tests.
- **Files modified:** `.github/workflows/ci.yml`
- **Verification:** `npm run ci:local` completed the dependency audit and all later gates.
- **Committed in:** `1eecb5f`

**2. [Rule 3 - Blocking] Removed the not-yet-implemented Plan 06 smoke gate from Plan 05 local CI**
- **Found during:** Task 3 local verification
- **Issue:** Plan 01 registered `smoke:required` in `ci:local`, but its compiled runner is deliberately assigned to the next plan and does not exist yet, making Plan 05 unverifiable.
- **Fix:** Plan 05 local CI contains only its available required gates, and the exact dependency-audit script policy was synchronized. Plan 06 must add `smoke:required` back when its runner is implemented.
- **Files modified:** `package.json`, `src/maintainer/dependency-audit.cts`
- **Verification:** `npm run deps:audit && npm run ci:local` passed.
- **Committed in:** `1eecb5f`

**3. [Rule 1 - Bug] Serialized the aggregate suite around repository immutability evidence**
- **Found during:** Task 3 full-suite verification
- **Issue:** Node ran test files concurrently while the docs-check suite intentionally created a short-lived root fixture, racing the pack test's exact before/after `git status` assertion.
- **Fix:** The aggregate suite uses `--test-concurrency=1`; pack tree hashing also excludes only GSD's volatile `.gsd/` records and milestone lock bytes while exact status still covers their path presence.
- **Files modified:** `package.json`, `src/maintainer/pack-audit.cts`, `tests/maintainer/ci-contract.test.cts`
- **Verification:** The complete serial suite passed 106/106 and the real tgz test retained both status and source-tree evidence.
- **Committed in:** `1eecb5f`

**4. [Rule 1 - Bug] Distinguished template logic from unresolved rendered assets**
- **Found during:** Task 2 real archive verification
- **Issue:** The compiled generator and pack auditor legitimately contain placeholder/sentinel matching constants, so scanning all CJS as rendered templates produced false failures.
- **Fix:** Forbidden runtime paths still apply archive-wide, while unresolved-token and credential-fixture content scans apply to rendered/user assets outside `dist/`.
- **Files modified:** `src/maintainer/pack-audit.cts`
- **Verification:** The real 54-entry tarball passes and rendered README placeholder fixtures still fail.
- **Committed in:** `1eecb5f`

---

**Total deviations:** 4 auto-fixed (2 Rule 1 bugs, 2 Rule 3 blockers)
**Impact on plan:** The fixes make clean-checkout CI executable, preserve exact repository-state evidence, and retain the planned Plan 06 smoke boundary; no release or external publication behavior was added.

## Issues Encountered

- The repository does not contain the optional `scripts/ci_monitor.cjs` helper named by the GitHub workflow skill. Workflow syntax was checked against current official GitHub Actions documentation, action pins were resolved from the official action repositories, and the local CI contract test enforces the resulting structure.
- No remote Actions run was triggered because this plan does not push or publish; the workflow is ready to run on the next push or pull request.

## User Setup Required

None - no external service configuration or publication is required for this plan.

## Next Phase Readiness

- Plan 03.1-06 can implement the honest three-host loopback smoke runner, add `smoke:required` to every matrix lane, and restore it to `ci:local` plus the dependency-audit exact script policy.
- Plan 03.1-13 can build a separately triggered release workflow on top of the same dependency, generation, test, and pack gates without changing ordinary push/PR behavior.
- No npm package was published and no remote release state changed.

## Known Stubs

None. Plan 06 smoke is a scheduled next-plan deliverable and is not represented as a passing or skipped Plan 05 gate.

## Self-Check: PASSED

- All nine production/test/config artifacts and this SUMMARY exist.
- Task commits `58e4a9e`, `14809f4`, `c2c5a28`, `5504791`, `821ace0`, and `1eecb5f` exist in Git history with no unexpected deletions.
- Fresh overall verification passed: exact dependency audit, 106/106 Node tests, deterministic generation check, and two consecutive 54-entry real tarball audits.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

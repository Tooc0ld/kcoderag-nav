---
phase: 03.1-javascript-npx
plan: 13
type: execute
wave: 15
depends_on: ["03.1-06"]
files_modified:
  - src/maintainer/release.cts
  - tests/maintainer/release.test.cts
  - src/maintainer/publish-receipt.cts
  - tests/maintainer/publish-receipt.test.cts
  - .github/workflows/release.yml
  - tests/maintainer/release-workflow.test.cts
autonomous: true
requirements: [JS-02, JS-07]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-07/D-08: release helper performs patch/minor/major, explicit owned-path commit and exact vX.Y.Z tag after all gates."
    - "Strict dirty rejection is tested in temp git; real release ignores only explicit .gsd/.planning local-state paths and never stages/modifies them."
    - "Matching-tag workflow publishes only after all required gates; sanitized receipt validator proves run/tag/SHA/version/evidence consistency."
  artifacts:
    - {path: "src/maintainer/release.cts", provides: "audited release helper", exports: ["prepareRelease"]}
    - {path: "src/maintainer/publish-receipt.cts", provides: "offline sanitized publish receipt validator", exports: ["verifyPublishReceipt"]}
    - {path: ".github/workflows/release.yml", provides: "tag-only npm publish workflow"}
  key_links:
    - {from: "release helper", to: "git commit/tag", via: "explicit release-owned path list", pattern: "prepareRelease"}
    - {from: "publish receipt", to: "tag/run/SHA/version", via: "offline schema/cross-field assertions", pattern: "verifyPublishReceipt"}
---
<objective>实现可在当前含 GSD 本地状态的仓库安全运行的 release helper、tag workflow 和离线发布证据验证器。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-06-SUMMARY.md
@.planning/phases/03.1-javascript-npx/COVERAGE.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 实现 exact-write-set dirty-safe release helper</name><files>src/maintainer/release.cts, tests/maintainer/release.test.cts</files><action>在临时 git fixtures 测试严格拒绝 tracked/untracked release-owned dirty paths、已有 tag、版本/生成漂移和 gate failure。定义唯一 release-owned mutable allow-list：`package.json`、`package-lock.json`、`kcoderag-qa/.codex-plugin/plugin.json`、`kcoderag-qa/.claude-plugin/plugin.json`、`kcoderag-dev/.codex-plugin/plugin.json`、`kcoderag-dev/.claude-plugin/plugin.json`、`kcoderag-cursor/.cursor-plugin/plugin.json`，共七路径；helper bump package/lock 后只能调用 `generate --package all --group version` 更新后五 manifest，要求 generator observed write set 与五路径 exact equality，再要求 git diff/staged/commit file set 与七路径 exact equality。README 使用 `@latest`，不属于 release bump。真实模式只在 cleanliness 判断忽略 `.gsd/**`、`.planning/**`，并依赖 Plan29 的 root-only node_modules/dist/dist-tests ignore；绝不 stage、commit、stash、reset、删除或改写 ignore/local-state paths。temp fixture 先创建 node_modules/dist/dist-tests probes，证明它们不进入 status，再创建任一 source/test/product untracked probe，证明 release 拒绝；真实 dry-run 前后 status baseline exact unchanged。实现 patch/minor/major、`--dry-run`、`--json`、`--yes`；测试 allow-list 少/多一路径、跨 group write、ignored bytes/status 漂移均失败。</action><verify><automated>npm run build &amp;&amp; npm run test:release &amp;&amp; npm run release:patch -- --dry-run --json</automated></verify><acceptance_criteria>- strict dirty failure 在 temp repo 证明，只有 exact build roots 被 ignore。- actual mutation、explicit staged set、release commit file set 都恰好七个 release-owned paths。- 当前仓库 GSD/planning state 不阻塞且不被修改/提交；dry-run 前后 status exact unchanged。- helper 创建 release commit/tag但不 push/publish。</acceptance_criteria><done>D-07/D-08 helper 可在受保护 dirty worktree 中以 exact write set 安全运行。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 sanitized publish receipt validator</name><files>src/maintainer/publish-receipt.cts, tests/maintainer/publish-receipt.test.cts</files><action>定义 receipt schema：package/version/tag、release_commit_sha、workflow_run_id/head_sha/event/conclusion、registry latest/bin/engines/repository、public npx evidence bits和 timestamp；禁止 command output、env、headers、credential-bearing URLs。实现并测试 `--record <file>`（只接受 allow-listed nonsecret fields）和 `--verify <file>`（离线校验 exact tag/version、两个 SHA、success、all evidence true、secret sentinel absent）；每个缺失/错配 fixture 非零。Plan01 注册的 `test:publish-receipt` 指向本任务 compiled test。</action><verify><automated>npm run build &amp;&amp; npm run test:publish-receipt</automated></verify><acceptance_criteria>- receipt validator 无网络。- wrong run/SHA/tag/version/NOT_RUN/secret fixture 均失败。</acceptance_criteria><done>Plan14 可把公开操作和 automated verify 通过去敏 receipt 隔离。</done></task>
<task type="auto" tdd="true"><name>Task 3: 建立 matching-tag publish workflow contract</name><files>.github/workflows/release.yml, tests/maintainer/release-workflow.test.cts</files><action>workflow 仅 `v*.*.*` tag 触发，校验 tag==package version，依次 deps audit/build/full tests/generate check/smoke required/pack audit 后使用 secret publish。compiled contract test 拒绝 branch trigger、弱 permissions、continue-on-error、遗漏 gate或token echo；本任务只 `npm publish --dry-run`，不创建远端状态。</action><verify><automated>npm run build &amp;&amp; npm run test:release-workflow &amp;&amp; npm publish --dry-run --ignore-scripts</automated></verify><acceptance_criteria>- workflow static contract and dry-run pass.- no actual tag push/publish.</acceptance_criteria><done>tag-only publish path ready for controlled Plan14。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-13-01 | Tampering | release commit/tag | high | mitigate | temp-fixture strict tests、seven-path write/stage/commit equality、version group、exact tag/version。 |
| T-03.1-13-02 | Information Disclosure | token/receipt | critical | mitigate | secret injection and offline sanitized receipt schema. |
| T-03.1-13-SC | Tampering | dependency graph | high | mitigate | `npm ci --ignore-scripts` and deps audit before publish. |
</threat_model>
<verification>`npm run test:release &amp;&amp; npm run test:publish-receipt &amp;&amp; npm run test:release-workflow &amp;&amp; npm publish --dry-run --ignore-scripts`</verification>
<success_criteria>- Release helper tolerates but never modifies unrelated GSD/planning state.- Receipt validator and workflow contract are executable before first release.</success_criteria>
## Artifacts this phase produces
- New symbols: `prepareRelease`, `verifyPublishReceipt`; tested flags `--dry-run`, `--json`, `--yes`, `--record`, `--verify`.
- New tag-only release workflow and compiled contract tests.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md` when done.</output>

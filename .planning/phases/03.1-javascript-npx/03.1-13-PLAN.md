---
phase: 03.1-javascript-npx
plan: 13
type: execute
wave: 9
depends_on: ["03.1-06"]
files_modified:
  - src/maintainer/release.cts
  - tests/maintainer/release.test.cts
  - tests/maintainer/release-workflow.test.cts
  - package.json
  - package-lock.json
  - .github/workflows/release.yml
autonomous: true
requirements: [JS-02, JS-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-07/D-08: release:patch|minor|major 只从 package.json 计算版本、同步 lock/generated manifests、创建匹配 vX.Y.Z tag。"
    - "只有匹配 package version 的 tag workflow 在全部 required gates 后可执行 public npm publish；普通 push 永不发布。"
  artifacts:
    - {path: "src/maintainer/release.cts", provides: "本地版本/生成/tag helper", exports: ["prepareRelease"]}
    - {path: ".github/workflows/release.yml", provides: "tag-only npm publish workflow"}
  key_links:
    - {from: "src/maintainer/release.cts", to: "src/generator/index.cts", via: "version bump 后 regenerate/check", pattern: "generate"}
    - {from: ".github/workflows/release.yml", to: "npm Registry publish", via: "matching tag and NPM_TOKEN", pattern: "npm publish"}
---
<objective>建立可重复的本地版本/tag helper 与严格 tag-only 发布 workflow，但本计划不创建远端 tag 或实际发布。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-06-SUMMARY.md
@.planning/phases/03.1-javascript-npx/COVERAGE.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现安全 release helper</name>
  <files>src/maintainer/release.cts, tests/maintainer/release.test.cts, package.json, package-lock.json</files>
  <behavior>- patch/minor/major 正确 bump package+lock，运行 generator/check/pack gates，再创建完全匹配 tag。- dirty tree、已有 tag、版本不一致或 gate 失败不 commit/tag/push。- helper 不执行 npm publish，不读取 token。</behavior>
  <action>在临时 git repo 测试 `prepareRelease`，按顺序检查 clean tree、SemVer、目标 tag 未存在，更新 package/lock、生成并验证、建立单个 release commit/tag；`--dry-run --json` 展示安全计划。提供 npm scripts `release:patch|minor|major`。实际 push 由明确批准后的 Plan14 完成。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:release &amp;&amp; npm run release:patch -- --dry-run --json</automated></verify>
  <acceptance_criteria>- dry-run 不改工作树/tag。- failure fixtures 不留下部分版本/commit/tag。</acceptance_criteria><done>D-08 的本地版本生成与 tag 契约可测试。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立匹配 tag 的 npm publish workflow</name>
  <files>.github/workflows/release.yml, tests/maintainer/release-workflow.test.cts</files>
  <behavior>- 仅 `v*.*.*` tag 触发，校验 tag==package version。- 运行 deps audit、build、full tests、generate check、smoke required、pack audit 后才 publish。- master/PR 不命中该 workflow；最小 permissions，不打印 token。</behavior>
  <action>创建独立 release workflow，使用 Node 22、`npm ci --ignore-scripts` 和 npm trusted auth secret injection。publish step 只在所有 gates 成功且 exact tag match 时执行 `npm publish --access public`；静态测试解析 YAML 并拒绝 branch trigger、跳过 gate、宽 permissions、continue-on-error 或 token echo。验证只用 `npm publish --dry-run`/workflow contract，不实际发布。</action>
  <verify><automated>node --test tests/maintainer/release-workflow.test.cts &amp;&amp; npm publish --dry-run --ignore-scripts</automated></verify>
  <acceptance_criteria>- workflow contract test 证明 tag-only/version gate/test-before-publish。- 本任务验证不会创建 tag、push 或 publish。</acceptance_criteria><done>首次和后续公开发布的自动化路径已就绪，尚待控制 checkpoint 执行。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-13-01 | Tampering | release tag/version | high | mitigate | clean tree、exact tag/version gate、生成/pack/测试先行。 |
| T-03.1-13-02 | Information Disclosure | NPM_TOKEN | critical | mitigate | GitHub secret injection、最小权限、无 echo/artifact。 |
| T-03.1-13-SC | Tampering | release dependencies | high | mitigate | `npm ci --ignore-scripts` 与 `deps:audit`。 |
</threat_model>
<verification>`npm run test:release &amp;&amp; node --test tests/maintainer/release-workflow.test.cts &amp;&amp; npm publish --dry-run --ignore-scripts`</verification>
<success_criteria>- release helper 可回滚且不自行 publish。- workflow 只有 matching tag 全门禁成功才 publish。</success_criteria>
## Artifacts this phase produces
- New files/symbols: `release.cts`, `prepareRelease`, release tests and `.github/workflows/release.yml`.
- New scripts: `release:patch`, `release:minor`, `release:major`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md` when done.</output>

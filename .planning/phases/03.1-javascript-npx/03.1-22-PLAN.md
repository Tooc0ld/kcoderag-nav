---
phase: 03.1-javascript-npx
plan: 22
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/maintainer/docs-check.cts
  - tests/maintainer/docs-check.test.cts
  - src/maintainer/sibling-guide-audit.cts
  - tests/maintainer/sibling-guide-audit.test.cts
autonomous: true
requirements: [JS-08]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Plan01-registered docs:check has a compiled implementation before any documentation plan uses it."
    - "Scoped checks validate links/commands/forbidden user instructions without reading or printing credential values."
  artifacts:
    - {path: "src/maintainer/docs-check.cts", provides: "scoped documentation contract checker", exports: ["checkDocs"]}
    - {path: "tests/maintainer/docs-check.test.cts", provides: "compiled docs-check fixtures"}
    - {path: "src/maintainer/sibling-guide-audit.cts", provides: "sanitized sibling status/commit/hash evidence", exports: ["captureBaseline", "verifySiblingReceipt"]}
  key_links:
    - {from: "package.json scripts.docs:check", to: "dist/maintainer/docs-check.cjs", via: "Plan01 registry", pattern: "docs-check.cjs"}
---
<objective>在文档修改前实现、测试并接通 `docs:check`，避免 verify 调用不存在的命令。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md
@README.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 实现路径受限的 docs checker</name><files>src/maintainer/docs-check.cts, tests/maintainer/docs-check.test.cts</files><action>实现 `checkDocs(paths, policy)`：只读显式相对文件/目录，拒绝 root escape/symlink，校验 Markdown links、npx command/package/host flags、Node22、Cursor Rule wording、single-guide ownership和 forbidden active user instructions。负向词只扫描 fenced command/active installation sections，避免注释/历史/规划自误报；内容只返回 code/path/line，不回显整行或秘密。</action><verify><automated>npm run build &amp;&amp; npm run test:docs</automated></verify><acceptance_criteria>- broken link/old active Python或marketplace command/secret sentinel fixtures fail.- explanatory history/comment does not false-positive.</acceptance_criteria><done>compiled docs checker implementation/tests exist。</done></task>
<task type="auto"><name>Task 2: 验证 Plan01 script wiring 和 scoped CLI</name><files>src/maintainer/docs-check.cts, tests/maintainer/docs-check.test.cts</files><action>验证 `npm run docs:check -- <paths...>` 确实调用 compiled checker、要求至少一个 scoped path，并支持 `--policy user-docs|project-instructions|planning|sibling-guide`。前三类只接受仓库内显式路径；`sibling-guide` 只接受解析后恰好为 `D:/AIProgram/KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md` 的单一路径。测试 unknown flag/policy、empty scope、混合/其它绝对路径失败；不在本任务要求尚未迁移的 README 通过新 policy。</action><verify><automated>npm run build &amp;&amp; npm run test:docs &amp;&amp; npm run docs:check -- --policy planning .planning/phases/03.1-javascript-npx/03.1-CONTEXT.md</automated></verify><acceptance_criteria>- script entry and all modes/flags execute compiled CJS.- no whole-repo implicit scan or arbitrary sibling path.</acceptance_criteria><done>Plans19/20/21 可安全使用 scoped docs:check。</done></task>
<task type="auto" tdd="true"><name>Task 3: 实现 sibling guide 基线与提交证据审计器</name><files>src/maintainer/sibling-guide-audit.cts, tests/maintainer/sibling-guide-audit.test.cts</files><action>实现并测试 `--capture-baseline <file>`、`--record-receipt <file> --baseline <file>`、`--verify-receipt <file>` 和 `--verify-summary <summary> --receipt <file>`：baseline 只保存 sibling repo path、HEAD、sorted status code+path和目标 guide-clean assertion；receipt 保存 before/after unrelated status digest、sibling/nav 40-hex HEAD、commit file list、secret_scan=true。receipt 验证器要求 commit file set 恰好 `MCP_QA_EXPERIENCE_GUIDE.md`、unrelated status exact unchanged、两个 hash 合法、receipt/baseline一致且无 secret-like values；summary 验证器要求 `kcoderag_nav_head` 与 `kcoderag_head` 恰好等于已验证 receipt。两者都不读取/输出无关 diff content。</action><verify><automated>npm run build &amp;&amp; npm run test:sibling-guide-audit</automated></verify><acceptance_criteria>- added/removed/changed unrelated status, extra commit file, invalid hash, secret receipt fixtures and summary hash drift all fail.- all four modes have compiled tests.</acceptance_criteria><done>Plan21 mutation与最终 SUMMARY evidence assertions 均在跨仓库操作前存在。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-22-01 | Information Disclosure | docs diagnostics | high | mitigate | code/path/line only; no line/body echo. |
| T-03.1-22-02 | Elevation of Privilege | scan paths | high | mitigate | explicit roots, symlink and traversal rejection. |
| T-03.1-22-03 | Tampering | sibling evidence | high | mitigate | sorted status manifests, exact commit file set and dual hash checks. |
</threat_model>
<verification>`npm run test:docs &amp;&amp; npm run docs:check -- --policy planning .planning/phases/03.1-javascript-npx/03.1-CONTEXT.md`</verification>
<success_criteria>- docs:check is implemented, compiled, registered and flag-tested before consumers.</success_criteria>
## Artifacts this phase produces
- New symbols/files: `checkDocs`, `captureBaseline`, `verifySiblingReceipt` and compiled tests.
- New tested flags: positional paths, `--policy ...`, `--capture-baseline`, `--record-receipt`, `--verify-receipt`, `--verify-summary ... --receipt ...`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-22-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 19
type: execute
wave: 19
depends_on: ["03.1-20"]
files_modified:
  - README.md
  - plugin-src/README.md.tmpl
  - plugin-src/cursor/README.md.tmpl
  - kcoderag-qa/README.md
  - kcoderag-dev/README.md
  - kcoderag-cursor/README.md
autonomous: true
requirements: [JS-01, JS-02, JS-03, JS-08]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-03/D-06: README 主路径仅使用 `npx kcoderag-nav@latest` 的 install/status/doctor/update/uninstall，默认 QA、Dev 显式。"
    - "D-01/D-02/D-15: 用户说明只要求 Node22+，不再要求 clone+Python、运行时编译或 marketplace。"
    - "retirement post gate 证明 shipped/generated/docs 无 Python runtime 或用户 Python 命令。"
  artifacts:
    - {path: "README.md", provides: "三宿主 npx 项目集成总览"}
    - {path: "plugin-src/README.md.tmpl", provides: "QA/Dev 生成 README 模板"}
    - {path: "plugin-src/cursor/README.md.tmpl", provides: "Cursor 项目 Rule/MCP README 模板"}
  key_links:
    - {from: "plugin-src/README.md.tmpl", to: "kcoderag-qa/README.md", via: "Node generator", pattern: "generate"}
---
<objective>更新本仓库与三份生成 README 到 npx 项目集成，并运行完整 Python 退役后扫描。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-20-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 重写根 README 的 npx 用户和维护者路径</name><files>README.md</files><action>说明 Node22+、默认 cwd+写前绝对路径确认、三 host 交互/`--host`、QA 默认/Dev 显式且同 host 互斥、跨 host 共存、五命令、离线 hook/后台 update、tag 发布与首次网络获取错误。删除 clone+Python、marketplace/local-plugin 作为主安装步骤和旧版本源说明；不得复制权威体验指南正文或凭据值。</action><verify><automated>npm run docs:check -- --policy user-docs README.md</automated></verify><acceptance_criteria>- 所有示例为 `npx kcoderag-nav@latest ...`。- Codex/Claude/Cursor 语义及 Cursor Rule 非 hook 写实。</acceptance_criteria><done>根 README 与 D-03/D-06 新入口一致。</done></task>
<task type="auto"><name>Task 2: 更新 QA/Dev 与 Cursor README 模板</name><files>plugin-src/README.md.tmpl, plugin-src/cursor/README.md.tmpl</files><action>QA/Dev 模板说明普通用户默认 QA、Dev 仅测试开发、切换前显式 uninstall；三宿主都经 npx 写项目原生目录。Cursor 模板明确 Rule/skill/MCP 与免费项目路径，不声明 marketplace 或 PreToolUse。版本 token 只来自 package.json generator。</action><verify><automated>npm run docs:check -- --policy user-docs plugin-src/README.md.tmpl plugin-src/cursor/README.md.tmpl</automated></verify><acceptance_criteria>- 无 Python、git clone 或用户 marketplace 安装命令。- 模板 token 均在 allow-list。</acceptance_criteria><done>生成说明模板与项目集成合同一致。</done></task>
<task type="auto"><name>Task 3: 再生 README 并通过最终退役审计</name><files>kcoderag-qa/README.md, kcoderag-dev/README.md, kcoderag-cursor/README.md</files><action>用 Node generator 再生三份 README，运行唯一的 full post retirement audit、pack audit 与敏感哨兵扫描。负向扫描只针对 shipped/runtime/docs 区域并排除 planning/history；必须拒绝 Python runtime/active Python commands、`kcoderag-update.json`、raw.githubusercontent/GitHub-raw/update-index logic。确认本仓库不存在指南副本。</action><verify><automated>npm run generate &amp;&amp; npm run generate:check &amp;&amp; npm run docs:check -- --policy user-docs README.md plugin-src/README.md.tmpl plugin-src/cursor/README.md.tmpl kcoderag-qa/README.md kcoderag-dev/README.md kcoderag-cursor/README.md &amp;&amp; npm run audit:retirement -- --mode post &amp;&amp; npm run pack:audit</automated></verify><acceptance_criteria>- shipped tarball/generated tree/README 无 Python runtime/source/用户命令、runtime compiler 或 GitHub-raw update index。- 三份 README version/commands 与 package.json 一致。</acceptance_criteria><done>D-01、JS-01、JS-08 的本仓库退役与文档证据完成。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-19-01 | Spoofing | user install docs | high | mitigate | exact public package/host/Rule semantics 与 docs contract tests。 |
| T-03.1-19-02 | Information Disclosure | generated docs | high | mitigate | 不展示 MCP values/Bearer，敏感哨兵扫描。 |
</threat_model>
<verification>`npm run docs:check -- --policy user-docs README.md plugin-src/README.md.tmpl plugin-src/cursor/README.md.tmpl kcoderag-qa/README.md kcoderag-dev/README.md kcoderag-cursor/README.md &amp;&amp; npm run audit:retirement -- --mode post &amp;&amp; npm run pack:audit`</verification>
<success_criteria>- 本仓库用户说明全部 npx/Node22+。- 最终 shipped/runtime/doc 扫描无 Python 依赖。</success_criteria>
## Artifacts this phase produces
- Updated root/template/generated READMEs and `docs:check`/post-retirement evidence.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-19-SUMMARY.md` when done.</output>

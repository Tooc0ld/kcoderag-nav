---
phase: 03.1-javascript-npx
plan: 02
type: execute
wave: 3
depends_on: ["03.1-01"]
files_modified:
  - src/core/contracts.cts
  - src/core/project-target.cts
  - src/core/state.cts
  - src/core/transaction.cts
  - tests/core/transaction.test.cts
autonomous: true
requirements: [JS-02, JS-04]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-11/D-15/D-16: target/runtime/status contracts are host-neutral and pre-write."
    - "D-12/D-18: only transaction mutates disk; staging/state-last/rollback preserve one-host boundaries."
  artifacts:
    - {path: "src/core/contracts.cts", provides: "stable result/error/state/desired-state types", exports: ["InstallError", "DesiredState"]}
    - {path: "src/core/transaction.cts", provides: "single-host atomic transaction", exports: ["applyTransaction"]}
  key_links:
    - {from: "src/core/project-target.cts", to: "src/core/transaction.cts", via: "validated relative paths", pattern: "DesiredState"}
---
<objective>提炼路径、状态和原子事务核心；CLI/HostAdapter 在 Plan26 单独接线，保持每计划小于十文件。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md
@scripts/manage_project_install.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 定义共享 contracts、target 和 state</name>
  <files>src/core/contracts.cts, src/core/project-target.cts, src/core/state.cts</files>
  <action>定义 schema-versioned `InstallError`、`CliResult`、`DesiredState`、install state/status vocabulary。D-11 target 规范化但不向上查 Git/SVN；拒绝 absolute relative-entry、`..`、symlink ancestor、special file 和 root escape。D-15/D-16 区分 mutating runtime hard-stop 与只读 runtime issue。测试接口由下一任务同一 compiled suite 覆盖。</action>
  <verify><automated>npm run build</automated></verify>
  <acceptance_criteria>- core 没有 `.codex/.claude/.cursor` 或配置值硬编码。- errors 只含 code 和安全相对路径。</acceptance_criteria><done>D-11、D-15、D-16 的共享数据边界建立。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现事务引擎和 compiled transaction tests</name>
  <files>src/core/transaction.cts, tests/core/transaction.test.cts</files>
  <action>先写 compiled Node test，覆盖完整预读/摘要校验、staging、atomic replace、state-last、每个注入失败点、恢复和 `rollback_failed` 恢复目录。事务是唯一 filesystem mutator，只接受 Plan02 contracts 的 validated desired-state；测试输出不含 payload bytes。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:transaction</automated></verify>
  <acceptance_criteria>- script 由 Plan01 注册，compiled test entry 本任务创建。- 每个失败点全树不变或进入明确恢复态。</acceptance_criteria><done>D-12/D-18 的共享原子事务有机器证据。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-02-01 | Elevation of Privilege | target resolver | high | mitigate | managed-root containment and symlink checks. |
| T-03.1-02-02 | Tampering | transaction | high | mitigate | pre-read digest, full staging, state-last, rollback injection. |
| T-03.1-02-03 | Information Disclosure | errors | high | mitigate | code/path-only diagnostics. |
</threat_model>
<verification>`npm run build &amp;&amp; npm run test:transaction`</verification>
<success_criteria>- 共享核心不含宿主分支。- 事务失败不留下不一致安装。</success_criteria>
## Artifacts this phase produces
- New core contracts/target/state/transaction sources and compiled transaction tests.
- New symbols: `InstallError`, `DesiredState`, `applyTransaction`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-02-SUMMARY.md` when done.</output>

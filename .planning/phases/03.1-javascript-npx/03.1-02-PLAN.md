---
phase: 03.1-javascript-npx
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/core/contracts.cts
  - src/core/project-target.cts
  - src/core/state.cts
  - src/core/transaction.cts
  - tests/core/transaction.test.cts
  - src/hosts/host-adapter.cts
  - src/cli/commands.cts
  - src/bin/kcoderag-nav.cts
  - src/tracer/codex-install.cts
  - tests/cli/commands.test.cts
autonomous: true
requirements: [JS-02, JS-03, JS-04]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-04/D-11/D-15/D-16: CLI 选择单一 host，默认 cwd 不向上找根，mutating 在确认与 Node 校验前零写入，status/doctor 只读。"
    - "D-12/D-14/D-18: HostAdapter 只生成 desired-state，共享事务层独占写入并在失败时恢复本宿主。"
  artifacts:
    - {path: "src/core/transaction.cts", provides: "state-last 单宿主原子事务", exports: ["applyTransaction"]}
    - {path: "src/hosts/host-adapter.cts", provides: "可扩展纯 host adapter 契约", exports: ["HostAdapter"]}
    - {path: "src/cli/commands.cts", provides: "五命令共享控制器", exports: ["executeCommand"]}
  key_links:
    - {from: "src/cli/commands.cts", to: "src/hosts/host-adapter.cts", via: "host registry", pattern: "HostAdapter"}
    - {from: "src/hosts/host-adapter.cts", to: "src/core/transaction.cts", via: "DesiredState", pattern: "DesiredState"}
---

<objective>
把 tracer 提炼为宿主无关的 CLI、目标、状态、adapter 与事务核心，不在本计划混入 Codex 具体生命周期或 legacy 迁移。

Purpose: 给后三个宿主建立唯一、安全、可测试的写入架构。
Output: 核心 contracts/target/state/transaction、HostAdapter 和稳定 CLI 控制器。
</objective>

<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md
@scripts/manage_project_install.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现路径、状态与单宿主事务核心</name>
  <files>src/core/contracts.cts, src/core/project-target.cts, src/core/state.cts, src/core/transaction.cts, tests/core/transaction.test.cts</files>
  <behavior>
    - D-11：cwd/--target 规范化但不向上发现 Git/SVN 根，所有 relative desired paths 必须留在 adapter managed roots。
    - D-12/D-18：预读、漂移校验、完整 staging、原子替换、state-last；任一点失败恢复原字节，rollback failure 留安全恢复目录。
    - D-15/D-16：mutating runtime gate 发生在写前；只读状态可以报告 runtime_invalid。
  </behavior>
  <action>从 Python installer 行为和 tracer 抽取稳定 `InstallError`、`CliResult`、`DesiredState`、state schema 与纯路径校验。拒绝 absolute/`..`/symlink ancestor/special file/root escape。事务层是唯一 filesystem mutator，并用注入式故障覆盖每个 staging/commit/rollback 点；错误只含 code 和安全相对路径。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:transaction</automated></verify>
  <acceptance_criteria>
    - 每个失败点的完整树快照保持不变或明确进入 `rollback_failed` 恢复态。
    - 核心文件不硬编码 `.codex`、`.claude`、`.cursor` 或凭据内容。
  </acceptance_criteria>
  <done>D-11、D-12、D-15、D-16、D-18 的共享安全边界可由任意 adapter 复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提炼 HostAdapter 与五命令控制器</name>
  <files>src/hosts/host-adapter.cts, src/cli/commands.cts, src/bin/kcoderag-nav.cts, src/tracer/codex-install.cts, tests/cli/commands.test.cts</files>
  <behavior>
    - D-04：无 host 交互只列 codex/claude/cursor；JSON 自动化必须显式 host 且 mutating 要 `--yes`。
    - install/status/doctor/update/uninstall 使用同一 registry；一次命令恰好选择一个 adapter。
    - status vocabulary 固定为 healthy/not_installed/drifted/update_available/invalid，JSON stdout 单值可解析。
  </behavior>
  <action>定义纯 `HostAdapter.detect/renderInstall/renderUninstall/status`，将 tracer 路径重构到 registry+transaction，保持 Plan01 外部契约。CLI 只负责编排、确认、runtime gate 与 human/JSON rendering；adapter 不写盘，核心不按 host 分支。保留 D-14 OpenCode 扩展 seam，但不注册或实现 OpenCode。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:cli &amp;&amp; npm run test:tracer</automated></verify>
  <acceptance_criteria>
    - 五命令均通过 adapter registry；缺 host 的非交互调用在写前失败。
    - stdout/stderr 不混流且不包含配置值。
  </acceptance_criteria>
  <done>D-04、D-14 的共享接口完成，Codex 具体生命周期留给 Plan07。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-02-01 | Elevation of Privilege | target resolver | high | mitigate | managed-root allow-list、realpath/symlink 检查与无向上根发现。 |
| T-03.1-02-02 | Tampering | transaction | high | mitigate | 写前摘要、完整 staging、state-last、故障注入回滚。 |
| T-03.1-02-03 | Information Disclosure | CLI output | high | mitigate | schema 化 code/path 输出，不回显配置正文。 |
</threat_model>

<verification>`npm run build &amp;&amp; npm run test:transaction &amp;&amp; npm run test:cli &amp;&amp; npm run test:tracer`</verification>
<success_criteria>
- 共享核心不含宿主路径分支且只有事务层写盘。
- 单 host 命令、确认、runtime 与 JSON 契约均有回归。
</success_criteria>

## Artifacts this phase produces

- New symbols: `HostAdapter`, `DesiredState`, `InstallError`, `applyTransaction`, `executeCommand`.
- Expanded CLI: `status`, `doctor`, `update`, `uninstall` and interactive host selection.

<output>Create `.planning/phases/03.1-javascript-npx/03.1-02-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 14
type: execute
wave: 26
depends_on: ["03.1-13", "03.1-21"]
files_modified:
  - package.json
  - package-lock.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
autonomous: false
requirements: [JS-02, JS-07]
user_setup:
  - service: npm/GitHub Actions
    why: "first public kcoderag-nav release"
    env_vars:
      - {name: NPM_TOKEN, source: "GitHub Actions repository secret; value is never exposed"}
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-06/D-08: human authorization precedes audited release helper, branch/tag pushes and public publish."
    - "Exact workflow run ID is discovered from tag+release SHA, watched with exit status and cross-checked before registry/npx evidence."
    - "Public exact-version npx runs only in controlled action; automated verify is offline receipt validation."
  artifacts:
    - {path: "package.json + package-lock.json + five product manifests", provides: "exact seven-path release bump write set"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json", provides: "sanitized release/run/registry/npx evidence"}
    - {path: "npm:kcoderag-nav@latest", provides: "public package and bin"}
  key_links:
    - {from: "release helper output", to: "GitHub run ID", via: "tag and release SHA filter", pattern: "workflow_run_id"}
    - {from: "controlled npx smoke", to: "publish receipt", via: "boolean evidence only", pattern: "public_npx"}
---
<objective>在人工授权后用 audited helper 完成首次版本/tag/publish，并把网络与 npx 证据固化为可离线验证的去敏 receipt。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-21-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 只读预检发布授权事实</name><files>None (read-only git/GitHub/npm inspection)</files><action>运行 release/helper/workflow/receipt tests；只通过 `gh secret list` 检查 `NPM_TOKEN` name present，不读取值。检查 GitHub auth、remote/default branch、package owner/name availability/current public metadata和本地 ignored-state baseline。禁止在本任务创建 commit/tag/push/publish。</action><verify><automated>npm run test:release &amp;&amp; npm run test:publish-receipt &amp;&amp; npm run test:release-workflow</automated></verify><acceptance_criteria>- secret only recorded as present/absent.- name/version/remote state unambiguous; any ownership conflict stops.</acceptance_criteria><done>不可逆操作前事实已安全收集。</done></task>
<task type="checkpoint:decision" gate="blocking-human"><name>Task 2: 授权 audited release helper 与首次 public publish</name><decision>确认 NPM_TOKEN 已配置且有 publish 权限、包名/目标 bump 正确，并授权 release helper 创建 release commit/tag、push branch/tag 和触发 public publish。</decision><context>这会产生不可逆公共包版本。执行器不得手工创建绕过 helper 的 annotated tag，也不得直接 `npm publish`。</context><options><option id="authorize"><name>授权首次发布（推荐）</name><pros>关闭 npm 404 并验证真实用户入口。</pros><cons>package/version 永久公开。</cons></option><option id="stop"><name>停止</name><pros>不产生外部状态。</pros><cons>phase 不能完成。</cons></option></options><resume-signal>仅 authorize 继续；不记录 token value。</resume-signal></task>
<task type="auto"><name>Task 3: 以 exact write set 调用 helper、锁定 run ID 并记录公开 npx 证据</name><files>package.json, package-lock.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-cursor/.cursor-plugin/plugin.json, .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json</files><action>调用 `npm run release:patch -- --yes --json`，从其安全输出取得 version/tag/release commit SHA；断言 helper actual write/staged/commit set 恰好 package/lock+QA/Dev 四 host manifests+Cursor manifest 七路径，manifest version 全等新 package version，任何 extra/missing path 停止；确认 helper 未改变忽略预期 receipt 后的其它 `.gsd/.planning` baseline。push release commit 到当前默认分支，再 push helper 创建的 exact tag。轮询 `gh run list --workflow release.yml --event push --commit <release-sha> --json databaseId,headSha,headBranch,event,status,conclusion`，只接受 headSha=release SHA、headBranch=tag 的唯一 run ID；执行 `gh run watch <id> --exit-status`，再 `gh run view <id> --json databaseId,headSha,headBranch,event,conclusion` 并逐字段断言。随后受控读取 exact/latest npm metadata，在新临时项目运行 public exact-version npx 的 install/status/update/uninstall 与 receipt-bearing loopback smoke；这是 action，不放进 automated verify。最后用 `publish-receipt --record` 只写 allow-listed fields/booleans，并立即离线验证；不保存命令原文、日志、env、header、Bearer或配置正文。</action><verify><automated>node dist/maintainer/publish-receipt.cjs --verify .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json</automated></verify><acceptance_criteria>- helper writes/commit exactly seven declared release paths；receipt 是第八个且不进 release commit。- receipt 证明 exact tag/version/release SHA/workflow run ID/head SHA/success/public metadata/all npx evidence。- automated verify 无网络且不 publish。- ignored unrelated baseline unchanged。</acceptance_criteria><done>公共包、exact release write set、workflow 和真实 npx 由去敏 receipt 连接。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-14-01 | Spoofing | workflow/package evidence | critical | mitigate | exact run ID, tag/SHA/version cross-check and receipt. |
| T-03.1-14-02 | Information Disclosure | NPM_TOKEN/logs | critical | mitigate | presence-only secret check and allow-listed receipt. |
| T-03.1-14-03 | Tampering | release write/tag path | high | mitigate | seven-path actual-write/stage/commit equality；only audited helper creates tag，manual tag forbidden。 |
</threat_model>
<verification>Offline `publish-receipt --verify` only; real publish and public npx occur in the approved action.</verification>
<success_criteria>- Public package is installable and no longer 404.- Run/tag/SHA/version/npx evidence is consistent and sanitized.</success_criteria>
## Artifacts this phase produces
- Audited release commit/tag/workflow/public npm version.
- Exact release-owned mutable paths: package/lock, QA/Dev four host manifests, Cursor manifest.
- Sanitized `03.1-14-PUBLISH-RECEIPT.json`; no credentials or raw logs.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md` when done.</output>

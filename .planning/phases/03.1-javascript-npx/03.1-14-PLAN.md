---
phase: 03.1-javascript-npx
plan: 14
type: execute
wave: 14
depends_on: ["03.1-13", "03.1-19", "03.1-20", "03.1-21"]
files_modified: []
autonomous: false
requirements: [JS-02, JS-07]
user_setup:
  - service: npm/GitHub Actions
    why: "首次公开发布 kcoderag-nav"
    env_vars:
      - name: NPM_TOKEN
        source: "npm automation token stored only as GitHub Actions repository secret"
    dashboard_config:
      - task: "Confirm publish access for the kcoderag-nav package/name"
        location: "npm account/organization access settings"
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-06: phase 不能在公共包仍 404 的状态下完成；首次 matching tag workflow 必须成功。"
    - "D-08: 人工确认 secret/name/version 后才 push tag，真实 publish 只由审计 workflow 执行。"
    - "发布后 read-only 验证 npm latest/bin/engines，并从临时项目运行 exact-version npx 生命周期。"
  artifacts:
    - {path: "npm:kcoderag-nav@latest", provides: "公开可解析的 npm 包与 kcoderag-nav bin"}
    - {path: "github-actions:release.yml run", provides: "首次 tag 发布成功证据"}
  key_links:
    - {from: "git tag vX.Y.Z", to: ".github/workflows/release.yml", via: "GitHub tag push", pattern: "vX.Y.Z"}
    - {from: "npm:kcoderag-nav@X.Y.Z", to: "temporary project", via: "npx exact version", pattern: "kcoderag-nav"}
---
<objective>在人工授权下完成首次公开 npm 发布，并以 registry 和临时项目证据关闭 404 缺口。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/COVERAGE.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 只读预检首次发布状态</name>
  <files>None (read-only local git, GitHub and npm Registry inspection)</files>
  <action>读取 package version/name/bin/engines，确认工作树无 phase 外待提交实现、release workflow 在默认分支、目标 `vX.Y.Z` 本地/远端状态明确；用 `gh secret list` 仅确认 `NPM_TOKEN` 名称存在，绝不读取值。用 `npm view kcoderag-nav ... --json` 区分 404、已发布 exact version、名称被他人占用；若已发布则必须核对 repository/bin/engines/version 后走幂等验证，任何 ownership/name/version 异常停止。</action>
  <verify><automated>node -e "const p=require('./package.json'); if(p.name!=='kcoderag-nav'||!p.bin?.['kcoderag-nav']||!/^>=22/.test(p.engines?.node||'')) process.exit(1)" &amp;&amp; gh workflow view release.yml</automated></verify>
  <acceptance_criteria>- 预检只显示 secret 名称和公开 metadata。- package/version/tag 状态可唯一决定首次发布或幂等验证路径。</acceptance_criteria><done>真实发布前置事实已安全收集。</done>
</task>
<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: 授权首次 tag push 与 public publish workflow</name>
  <decision>确认 GitHub repository secret `NPM_TOKEN` 已配置且拥有 publish 权限（不提供其值），包名/版本/目标 tag 正确，并授权 push 首个 matching tag 触发 public npm publish。</decision>
  <context>这是不可逆外部状态变更。执行器将只 push 已审计当前提交的 `vX.Y.Z` tag；不会直接在验证命令中运行真实 `npm publish`，发布由 Plan13 workflow 执行。</context>
  <options><option id="authorize-first-release"><name>授权 tag 与首次发布（推荐）</name><pros>关闭公共包 404，真实用户可 npx 安装。</pros><cons>版本与包名成为永久公开契约。</cons></option><option id="stop-before-release"><name>停止不发布</name><pros>不产生外部不可逆状态。</pros><cons>JS-02/JS-07 未完成，本 phase 不能标记完成。</cons></option></options>
  <resume-signal>仅 `authorize-first-release` 继续；记录确认的 package/version/tag 和 secret-present=true，禁止记录 secret 值。</resume-signal>
</task>
<task type="auto">
  <name>Task 3: 推送 matching tag、等待 workflow 并验证公开 npx</name>
  <files>None (git tag/GitHub Actions/npm Registry external state only)</files>
  <action>若 exact version 未发布且 tag 不存在，为当前已验证 commit 创建 annotated `vX.Y.Z` 并只 push 该 tag；等待对应 release workflow 完成并要求 success。若 tag/版本已存在，只在 commit 与 metadata 完全匹配时幂等继续。随后轮询公开 registry 有界等待，读取 latest/version/bin/engines/repository；在新临时项目用 `npx --yes kcoderag-nav@X.Y.Z status --host codex --target ... --json`，再执行 QA install/status/uninstall 并证明目标外无写入。真实 publish 只能来自 tag workflow，不能作为 `<verify>` 命令。</action>
  <verify><automated>gh run view --workflow release.yml --json conclusion,headSha,event &amp;&amp; npm view kcoderag-nav@latest version bin engines repository --json &amp;&amp; powershell -NoProfile -Command "$v=(node -p 'require(\"./package.json\").version'); node dist/smoke/host-smoke.cjs --mode required-contract --package-spec ('kcoderag-nav@'+$v) --host codex --public-registry"</automated></verify>
  <acceptance_criteria>- matching tag workflow conclusion=success 且 headSha 为批准 commit。- npm latest/exact metadata 匹配 package。- 临时项目 exact-version npx install/status/uninstall 全通过且无敏感输出。</acceptance_criteria><done>公共 `kcoderag-nav` 不再 404，首次发布与用户安装均有可审计证据。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-14-01 | Spoofing | npm package owner/name | critical | mitigate | 发布前 owner/name/repository 与 404/metadata 人工确认。 |
| T-03.1-14-02 | Information Disclosure | NPM_TOKEN | critical | mitigate | 只检查 secret name/presence，发布由 Actions 注入，永不读取值。 |
| T-03.1-14-03 | Tampering | tag target | high | mitigate | annotated exact version tag、HEAD/workflow sha matching。 |
</threat_model>
<verification>只读核对 workflow success、npm metadata 和 exact-version 临时 npx；不以任何 verify 命令实际 publish。</verification>
<success_criteria>- 首次 public package 可解析并执行。- tag、workflow、registry、temp-project 四层证据一致。</success_criteria>
## Artifacts this phase produces
- External artifacts: first `vX.Y.Z` tag, successful release workflow run, public `kcoderag-nav@X.Y.Z`/`latest`.
- No repository file changes in this plan.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md` when done.</output>

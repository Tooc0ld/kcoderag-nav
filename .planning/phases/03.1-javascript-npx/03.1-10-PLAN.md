---
phase: 03.1-javascript-npx
plan: 10
type: execute
wave: 5
depends_on: ["03.1-09"]
files_modified:
  - kcoderag-qa/hooks/grep-nudge.cjs
  - kcoderag-qa/hooks/update-check.cjs
  - kcoderag-qa/hooks/update-worker.cjs
  - kcoderag-qa/hooks/run_hook.cmd
  - kcoderag-qa/hooks/run_hook.sh
  - kcoderag-qa/hooks/hooks.json
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["D-01/D-02: QA tree has self-contained CJS hook/update runtime and fail-open launchers.", "D-09/D-17: QA deployed hook remains offline; update worker is detached/bounded."]
  artifacts:
    - {path: "kcoderag-qa/hooks/grep-nudge.cjs", provides: "QA hook runtime"}
    - {path: "kcoderag-qa/hooks/hooks.json", provides: "QA host hook registration"}
  key_links:
    - {from: "kcoderag-qa/hooks/hooks.json", to: "kcoderag-qa/hooks/run_hook.cmd", via: "relative launcher", pattern: "run_hook"}
---
<objective>生成并校验 QA 的六项 Node hook/runtime 资产；配置、skill、agent 由 Plan27 独立拥有。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 生成 QA CJS runtime 与 launchers</name><files>kcoderag-qa/hooks/grep-nudge.cjs, kcoderag-qa/hooks/update-check.cjs, kcoderag-qa/hooks/update-worker.cjs, kcoderag-qa/hooks/run_hook.cmd, kcoderag-qa/hooks/run_hook.sh</files><action>运行 `generate --package qa`，把已构建三个 CJS 和双平台 launcher 复制到 QA tree。保持旧 Python 暂存到 parity gate 后；launcher 只按自身目录定位 CJS，Node 异常 exit0。</action><verify><automated>npm run generate -- --package qa &amp;&amp; npm run test:launcher</automated></verify><acceptance_criteria>- runtime 摘要匹配 dist。- 无 cwd、Python 或 runtime compiler 依赖。</acceptance_criteria><done>QA Node runtime 自包含。</done></task>
<task type="auto"><name>Task 2: 生成 QA hook 注册并做单产品 check</name><files>kcoderag-qa/hooks/hooks.json</files><action>生成 Codex Bash 与 Claude Grep/Glob/Bash 的 QA hook 注册；验证 command 仅指同树 launchers。执行 QA 单产品 check，不要求尚未更新的 Dev/Cursor 树一致。</action><verify><automated>npm run generate -- --check --package qa &amp;&amp; npm run test:hook</automated></verify><acceptance_criteria>- QA hook tree check 通过。- fail-open corpus 通过。</acceptance_criteria><done>QA hook 注册和 runtime 一致。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-10-01 | Denial of Service | QA launcher | high | mitigate | self-relative CJS and unconditional fail-open. |
| T-03.1-10-02 | Tampering | generated runtime | high | mitigate | dist digest and single-package check. |
</threat_model>
<verification>`npm run generate -- --check --package qa &amp;&amp; npm run test:hook &amp;&amp; npm run test:launcher`</verification>
<success_criteria>- QA six-file hook runtime is generated, offline and fail-open.</success_criteria>
## Artifacts this phase produces
- Generated QA CJS hook/update runtimes, launchers and hook registration.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-10-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 10
type: execute
wave: 10
depends_on: ["03.1-09"]
files_modified:
  - kcoderag-qa/hooks/grep-nudge.cjs
  - kcoderag-qa/hooks/update-check.cjs
  - kcoderag-qa/hooks/update-worker.cjs
  - kcoderag-qa/hooks/run_hook.cmd
  - kcoderag-qa/hooks/run_hook.sh
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["D-01/D-02: QA tree has self-contained CJS hook/update runtime and fail-open launchers.", "D-09/D-17: QA deployed hook remains offline; update worker is detached/bounded."]
  artifacts:
    - {path: "kcoderag-qa/hooks/grep-nudge.cjs", provides: "QA hook runtime"}
    - {path: "kcoderag-qa/hooks/run_hook.cmd", provides: "QA Windows fail-open launcher"}
  key_links:
    - {from: "kcoderag-qa/hooks/hooks.json", to: "kcoderag-qa/hooks/run_hook.cmd", via: "relative launcher", pattern: "run_hook"}
---
<objective>只生成 live bytes 确认会变化的 QA 三项 CJS 与两项 launcher；既有注册/metadata 由 Plan23 只读校验。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-09-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 生成 QA 三项 CJS runtime</name><files>kcoderag-qa/hooks/grep-nudge.cjs, kcoderag-qa/hooks/update-check.cjs, kcoderag-qa/hooks/update-worker.cjs</files><action>运行 `generate --package qa --group runtime-cjs`，只把三个已构建且 live tree 中尚不存在的 CJS 复制到 QA tree；断言 observed write set 恰好为本任务三路径。保持旧 Python 到 parity gate 后，不触碰 launcher、registration、metadata 或 docs。</action><verify><automated>npm run generate -- --package qa --group runtime-cjs &amp;&amp; npm run generate -- --check --package qa --group runtime-cjs &amp;&amp; npm run test:hook</automated></verify><acceptance_criteria>- 三个 CJS 新路径均写入且摘要匹配 dist。- 其它 QA/Dev/Cursor bytes不变。</acceptance_criteria><done>QA CJS runtime 自包含。</done></task>
<task type="auto"><name>Task 2: 生成 QA 两项 Node launcher</name><files>kcoderag-qa/hooks/run_hook.cmd, kcoderag-qa/hooks/run_hook.sh</files><action>运行 `generate --package qa --group runtime-launcher`，live launcher 当前仍调用 Python，因此 canonical Node launcher bytes 保证变化；断言 observed write set 恰好为本任务两路径。launcher 仅按自身目录定位 CJS，Node 缺失/异常仍 exit0；registration 仍只读留给 Plan23。</action><verify><automated>npm run generate -- --package qa --group runtime-launcher &amp;&amp; npm run generate -- --check --package qa --group runtime-code &amp;&amp; npm run test:launcher</automated></verify><acceptance_criteria>- two launcher paths changed exactly once and runtime-code union checks clean.- 无 cwd、Python 或 runtime compiler 依赖。</acceptance_criteria><done>QA runtime-code 五个保证变化路径完成。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-10-01 | Denial of Service | QA launcher | high | mitigate | self-relative CJS and unconditional fail-open. |
| T-03.1-10-02 | Tampering | generated runtime | high | mitigate | dist digest and single-package check. |
</threat_model>
<verification>`npm run generate -- --check --package qa --group runtime-code &amp;&amp; npm run test:hook &amp;&amp; npm run test:launcher`</verification>
<success_criteria>- QA five guaranteed-changing runtime paths are generated, offline and fail-open; registration remains byte-equal and unwritten.</success_criteria>
## Artifacts this phase produces
- Generated QA CJS hook/update runtimes and launchers; registration is a Plan23 read-only checked asset.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-10-SUMMARY.md` when done.</output>

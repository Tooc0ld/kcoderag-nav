---
phase: 03.1-javascript-npx
fixed_at: 2026-08-24T05:42:58.2098287Z
review_path: .planning/phases/03.1-javascript-npx/03.1-REVIEW.md
iteration: 3
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 03.1: Code Review Fix Report

**Fixed at:** 2026-08-24T05:42:58.2098287Z
**Source review:** `.planning/phases/03.1-javascript-npx/03.1-REVIEW.md`
**Iteration:** 3

**Summary:**

- Findings in scope: 5
- Fixed: 5
- Skipped: 0
- Published package: npm `0.1.6` was not changed, republished, pushed, tagged, or otherwise mutated.

## Fixed Issues

### CR-01: Recovery cleanup recursively deletes replacement data after detecting an identity swap

**Files modified:** `src/core/transaction.cts`, `tests/core/transaction.test.cts`
**Commit:** 49a51cf
**Applied fix:** Recovery creation and cleanup now track the identities of private files, unlink only individually revalidated objects, and remove directories non-recursively only after they are proven empty. Any pathname replacement or identity mismatch preserves the uncertain replacement instead of recursively deleting it. Deterministic `recovery-write` and final cleanup-window swaps prove that replacement sentinels survive. This is filesystem security and rollback logic and requires human verification.

### CR-02: Cursor migration deletes the only backup when the second legacy deletion fails

**Files modified:** `src/hosts/cursor.cts`, `tests/hosts/cursor.test.cts`
**Commit:** 9c60c54
**Applied fix:** Cursor migration keeps a digest-verified project backup until legacy restoration and project rollback are both verified. Missing legacy plugin or state quarantine data can be rebuilt from verified backup payloads, and any incomplete restore retains the complete recovery tree. Deterministic failures after plugin removal, before state unlink, and after state unlink prove exact restoration or retained recoverability of every byte. This is migration rollback logic and requires human verification.

### CR-03: Successful uninstall removes pre-existing shared JSON parent containers

**Files modified:** `src/core/contracts.cts`, `src/core/state.cts`, `src/hosts/claude.cts`, `src/hosts/codex.cts`, `src/hosts/cursor.cts`, `tests/hosts/claude.test.cts`, `tests/hosts/codex.test.cts`, `tests/hosts/cursor.test.cts`
**Commit:** eb31367
**Applied fix:** Managed section state now records each shared JSON parent container created by installation, validates the optional provenance field backward-compatibly, and carries it through updates. Uninstall removes only installer-owned parents that remain empty; pre-existing empty `mcpServers`, `hooks`, and `PreToolUse` containers remain byte-for-byte unchanged. Lifecycle regressions cover install, update, and uninstall for Claude Code, Codex, and Cursor. This is structured ownership logic and requires human verification.

### CR-04: npm publication can complete while required Node 22 or Windows CI is failing

**Files modified:** `.github/workflows/release.yml`, `tests/maintainer/release-workflow.test.cts`
**Commit:** bf2f286
**Applied fix:** The release workflow now runs an explicit `ubuntu-latest`/`windows-latest` by Node 22/24 required-contract matrix with fail-fast disabled, and the credential-bearing publish job depends on that complete matrix. Registry credentials remain scoped to the final publish step. Workflow contract tests prove the matrix lanes, dependency, and credential separation. This is release-gating logic and requires human verification.

### WR-01: A transient marker replacement failure permanently disables session-less renewal

**Files modified:** `src/hooks/update-check.cts`, `tests/hooks/update-check.test.cts`, `kcoderag-qa/hooks/update-check.cjs`, `kcoderag-dev/hooks/update-check.cjs`
**Commit:** 957d782
**Applied fix:** A failed marker replacement now rereads the marker: an unchanged stale generation releases the owned token for retry, committed new contents count as completion, and only a genuinely ambiguous third state retains the token. Renewal tokens also have a bounded five-minute lease and are pruned before a new claim. Deterministic regressions cover transient retry, completion-before-error, and recovery from an undeletable stale token; generated QA/Dev hooks were regenerated and syntax-checked. This is concurrency logic and requires human verification.

## Verification

- Isolated worktree: the final build and focused transaction, update-check, Claude Code, Codex, Cursor, and release-workflow suites passed 63/63. Generated hook CJS syntax checks and deterministic generation checks passed.
- Main checkout after fast-forward and worktree cleanup: `npm run ci:local` exited 0. Dependency audit passed; all 170 tests passed; `generate:check` reported no drift; pack audit accepted the exact 60-entry archive; required-contract smoke passed for Codex, Claude Code, and Cursor.
- Cleanup: `master` fast-forwarded to `957d782`; the isolated worktree, temporary `gsd-reviewfix/03.1-*` branch, and recovery sentinel were removed in transactional order.
- Working tree: only preserved pre-existing/untracked planning and GSD artifacts plus this intentionally uncommitted report remain; no source fix is uncommitted.

---

_Fixed: 2026-08-24T05:42:58.2098287Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 3_

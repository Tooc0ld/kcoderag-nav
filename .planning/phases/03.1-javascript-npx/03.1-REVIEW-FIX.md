---
phase: 03.1-javascript-npx
fixed_at: 2026-08-24T11:56:00Z
review_path: .planning/phases/03.1-javascript-npx/03.1-REVIEW.md
iteration: 3
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 03.1: Code Review Fix Report

**Fixed at:** 2026-08-24T11:56:00Z
**Source review:** `.planning/phases/03.1-javascript-npx/03.1-REVIEW.md`
**Iteration:** 3

**Summary:**

- Findings in scope: 2
- Fixed: 2
- Skipped: 0
- Release boundary: both fixes are descendants of immutable public `0.1.7`; they are not contained in `0.1.7`. A later release still requires a new version and explicit authorization.

## Fixed Issues

### CR-01: Schema v3 rejects every genuine public-smoke lifecycle artifact

**Status:** fixed: requires human verification
**Files modified:** `src/maintainer/publish-receipt.cts`, `tests/maintainer/publish-receipt.test.cts`
**Commit:** `f24aed2`
**Applied fix:** Schema v3 now treats the original public Registry tarball identity and the loopback-instrumented lifecycle tarball digest as independent evidence. It still validates each lifecycle digest, validates every public Registry identity field, and requires exact/latest Registry origin, tarball URL, SRI, SHA-256, and SHA-512 to match. The regression fixture now uses distinct exact/latest lifecycle digests and records genuine `0.1.7` exact/latest smoke evidence with the verified public Registry artifact identity; deliberately distinct public artifact identities remain rejected with `registry_artifact_mismatch`.

### WR-01: Deeply nested malformed JSON degrades to the generic CLI error

**Status:** fixed: requires human verification
**Files modified:** `src/maintainer/publish-receipt.cts`, `tests/maintainer/publish-receipt.test.cts`
**Commit:** `a5de1dd`
**Applied fix:** Secret-like scanning now uses an explicit stack with a 64-level depth limit and 10,000-node budget instead of recursion. Oversized or 20,000-level inputs fail with stable `invalid_receipt_schema` through direct and CLI verification without creating output; ordinary-depth secret-like keys and values still fail with `secret_like_value` and their contents are never emitted.

## Verification

- Focused verification ran in the isolated review-fix worktree: `npm run build`, `npm run test:publish-receipt` (21/21), and `git diff --check` passed.
- The complete reproducible gate ran in the main checkout after transactional fast-forward and worktree cleanup: `npm run ci:local` passed.
- Full test suite: 197/197 passed, with no skipped, cancelled, or todo tests.
- Generation: canonical generated assets reported no changed or written paths.
- Package audit: the real npm tarball passed with exactly 59 entries.
- Required smoke: Codex, Claude Code, and Cursor all returned `PASS`; every required evidence bit was true.
- A worktree-local complete-gate attempt stopped at dependency audit because the isolated worktree intentionally has no private `node_modules`; the same gate passed from the main checkout as required by the worktree policy.

---

_Fixed: 2026-08-24T11:56:00Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 3_

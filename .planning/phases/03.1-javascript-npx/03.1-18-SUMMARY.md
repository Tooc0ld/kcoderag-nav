---
phase: 03.1-javascript-npx
plan: 18
subsystem: testing-retirement
tags: [nodejs, python-retirement, parity, hooks, generator, host-adapters]

requires:
  - phase: 03.1-15
    provides: Immutable production baseline and final all-PASS Python/Node parity receipt
  - phase: 03.1-17
    provides: Node-only maintainer entrypoints with eleven Python test artifacts preserved
provides:
  - Install, generator, hook, routing, launcher, and update verification is Node-only
  - Exact six-file first-stage Python test retirement with compiled replacement evidence
  - Explicit five-file Plan 25 inventory and an intentionally unadvanced retirement stage
affects: [03.1-25, python-retirement, node-test-coverage]

actuals:
  tokens: 31558
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - Consolidated Node contract tests replace legacy Python cases by behavioral domain rather than test-name count
    - Partial retirement deletes only plan-owned paths and leaves the monotonic post-tests transition to the final test-retirement plan

key-files:
  created: []
  modified:
    - tests/test_project_install.py
    - tests/test_cursor_local_install.py
    - tests/test_generation.py
    - tests/test_hook_runtime.py
    - tests/test_update_check.py
    - tests/test_routing_and_hooks.py

key-decisions:
  - "The Plan 15 all-PASS receipt plus fresh compiled CLI, host, generator, hook, launcher, and update suites constitute case-by-case replacement evidence for the six retired Python files."
  - "Plan 18 does not invoke post-tests: the five remaining Python test/stub/package paths belong exclusively to Plan 25."
  - "The intermediate five-file state intentionally matches no retirement audit stage; post-scripts requires all eleven tests and post-tests requires none."

patterns-established:
  - "Retire tests by group: prove mapped compiled suites -> validate exact absolute deletion targets -> delete only named files -> rerun suites -> verify remaining inventory."
  - "Recovery remains ordinary Git revert, with one independent commit per three-file responsibility group."

requirements-completed: [JS-01, JS-04, JS-05, JS-06]

coverage:
  - id: D1
    description: "Project install, Cursor, cross-host, and deterministic generation coverage remains executable after the first three Python tests are removed."
    requirement: JS-04
    verification:
      - kind: integration
        ref: "npm run test:cli && npm run test:host:codex && npm run test:host:claude && npm run test:host:cursor && npm run test:cross-host && npm run test:generator — 31/31 passing after deletion"
        status: pass
    human_judgment: false
  - id: D2
    description: "Advisory hook, launcher fail-open, routing corpus, and offline update contracts remain executable after the second three Python tests are removed."
    requirement: JS-06
    verification:
      - kind: integration
        ref: "npm run test:hook && npm run test:launcher && npm run test:update-check — 20/20 passing after deletion"
        status: pass
      - kind: integration
        ref: "npm test — 140/140 compiled Node tests passing after all six deletions"
        status: pass
    human_judgment: false
  - id: D3
    description: "The remaining Python inventory is exactly the five Plan 25 paths and active runtime/test entrypoints do not reference the six retired tests."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "exact tests/*.py inventory comparison — Plan 25 paths preserved 5/5"
        status: pass
      - kind: integration
        ref: "scoped package.json/.githooks/.github/src/tests reference scan — unexpected active references 0"
        status: pass
    human_judgment: false

duration: 6min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 18: First-Stage Python Test Retirement Summary

**Six legacy install, generator, routing, hook, and update Python tests are gone while all mapped compiled Node contracts remain green and Plan 25 retains exclusive ownership of the final five Python paths.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-23T18:38:00Z
- **Completed:** 2026-08-23T18:44:10Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Removed the project-install, Cursor-install, and generation Python suites only after 31 compiled CLI/host/cross-host/generator cases passed before and after deletion.
- Removed the hook-runtime, update-check, and routing/hook Python suites only after 20 compiled hook/launcher/update cases passed before and after deletion.
- Rebuilt and ran the complete compiled Node suite at 140/140 after all six deletions.
- Preserved exactly `tests/__init__.py`, `tests/stub_mcp_server.py`, `tests/test_host_smoke.py`, `tests/test_plugin_update.py`, and `tests/test_pre_commit_generate.py` for Plan 25.

## Task Commits

Each task was committed atomically:

1. **Task 1: 删除安装与生成 Python tests** - `116ffd0` (test)
2. **Task 2: 删除 hook/update/routing Python tests** - `4293453` (test)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `tests/test_project_install.py` - Removed after CLI, Codex/Claude/Cursor, and cross-host replacement evidence.
- `tests/test_cursor_local_install.py` - Removed after project-native Cursor lifecycle and legacy migration replacement evidence.
- `tests/test_generation.py` - Removed after deterministic compiled generator replacement evidence.
- `tests/test_hook_runtime.py` - Removed after Windows/POSIX Node launcher and fail-open evidence.
- `tests/test_update_check.py` - Removed after foreground-offline, bounded-cache, detached-worker, and registry-validation evidence.
- `tests/test_routing_and_hooks.py` - Removed after the compiled Python-corpus parity and hook protocol evidence.

All six changes are intentional tracked deletions. Restore Task 2 by reverting `4293453`, then restore Task 1 by reverting `116ffd0`; no generated or external state is needed for recovery.

## Decisions Made

- Used behavioral-domain mapping rather than requiring a one-to-one Node test name for every Python method: the Node suites deliberately consolidate related input matrices and failure cases while executing the same compiled runtime boundaries.
- Kept Plan 25's five files outside both commits and verified their exact inventory after the second deletion.
- Did not advance the retirement audit to `post-tests`; only Plan 25 may make every test-retirement path absent and run that mode.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- A read-only extra check of the prior `post-scripts` mode returned the expected `retirement_mode_mismatch`: that mode requires all eleven Python test paths to remain, while this intentionally partial plan leaves five. The command made no mutation, `post-tests` was not invoked, and the planned mapped-suite verification remained fully green.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 25 can now prove smoke/pre-commit/release replacements, remove its exact five remaining Python files, and run `post-tests` for the first time.
- There are no Python install/generator/hook/update/routing tests left, no unexpected active references to their filenames, and no blocker for the final test-retirement stage.

## Known Stubs

None.

## Self-Check: PASSED

- Summary file exists and coverage metadata classifies all three deliverables as automated PASS.
- Task commits `116ffd0` and `4293453` exist and contain exactly the planned three deletions each.
- All six retired paths are absent; all five Plan 25 paths are present.
- Fresh post-deletion build and full compiled Node regression passed 140/140; scoped active-reference scan found zero unexpected references.
- Pre-existing `.planning/config.json`, `.gsd/`, milestone lock, `.gitkeep`, and unrelated phase directories remain outside this plan's task commits and were not reverted.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

---
phase: 03.1-javascript-npx
plan: 31
type: execute
wave: 27
depends_on: ["03.1-14"]
files_modified:
  - package.json
  - src/maintainer/retirement-audit.cts
  - src/maintainer/pack-audit.cts
  - tests/maintainer/retirement-audit.test.cts
  - tests/maintainer/pack-audit.test.cts
autonomous: true
gap_closure: true
requirements: [JS-01, JS-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "JS-01/D-01/D-02: every CJS member in the real npm tarball is directly runnable on Node.js 22+ without a reachable Python interpreter or Python script subprocess path."
    - "JS-06: the real-tarball audit rejects Python execution embedded in an otherwise allowed `.cjs` member, not merely forbidden filename extensions."
    - "Historical retirement receipt labels, legacy migration identifiers, comments, and fixtures may mention Python without being misclassified as a runtime dependency."
    - "The obsolete parity command and its package script are absent, while Node-only post-retirement and historical receipt verification remain executable."
  artifacts:
    - path: "src/maintainer/retirement-audit.cts"
      provides: "Node-only retirement modes and historical receipt verification with the retired parity runner removed"
    - path: "src/maintainer/pack-audit.cts"
      provides: "Content-aware Python subprocess rejection for every packed CJS member"
      exports: ["validatePack", "auditPack"]
    - path: "tests/maintainer/pack-audit.test.cts"
      provides: "Allowed-CJS negative fixtures and false-positive controls"
    - path: "package.json"
      provides: "Published manifest without the obsolete parity script"
  key_links:
    - from: "src/maintainer/retirement-audit.cts"
      to: "dist/maintainer/retirement-audit.cjs"
      via: "TypeScript CJS build before the real pack audit"
      pattern: "retirement-audit.cjs"
    - from: "package.json files"
      to: "src/maintainer/pack-audit.cts"
      via: "exact tgz entry expansion plus CJS byte inspection"
      pattern: "validatePack"
---

<objective>
Close the JS-01/JS-06 verifier gap by removing the retired Python parity execution path and making the exact npm tarball gate prove that no allowed CJS member can invoke a Python user runtime.

Purpose: Restore the phase's Node-only distribution invariant without rejecting historical evidence or the legitimate Python migration vocabulary retained by the Node installer.
Output: A Node-only retirement auditor, a content-aware pack audit, focused regressions, and one full locally green implementation commit.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-05-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@src/maintainer/retirement-audit.cts
@src/maintainer/pack-audit.cts
@tests/maintainer/retirement-audit.test.cts
@tests/maintainer/pack-audit.test.cts
@package.json

<interfaces>
- `validatePack({ packageJson, expectedPaths, archiveEntries })` is the pure tar-member policy boundary; it returns only version and entry count or a stable `PackAuditError.code`.
- `auditPack({ root })` builds and inspects one real temporary tgz, then proves repository status and tree preservation.
- `retirement-audit main(argv)` must retain `--verify-pre-receipt` and `--mode post` behavior after the obsolete execution branch is removed.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Node-only tarball rejection through one allowed CJS member</name>
  <read_first>
    package.json
    src/maintainer/retirement-audit.cts
    src/maintainer/pack-audit.cts
    tests/maintainer/retirement-audit.test.cts
    tests/maintainer/pack-audit.test.cts
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <reversibility rating="costly">D-01 already makes the Python-to-CJS migration a coordinated distribution contract; this task completes that locked choice without reopening it.</reversibility>
  <files>package.json, src/maintainer/retirement-audit.cts, src/maintainer/pack-audit.cts, tests/maintainer/retirement-audit.test.cts, tests/maintainer/pack-audit.test.cts</files>
  <behavior>
    - RED: replace one allowed `dist/**/*.cjs` archive member in a fixture with executable code that binds Node child-process APIs and invokes a Python interpreter; `validatePack` must fail with a stable content-policy code.
    - RED: cover direct command literals, a local constant passed to a child-process sink, and shell-command execution of a `.py` entry; these are executable data-flow cases rather than forbidden archive paths.
    - GREEN: an allowed CJS member containing historical prose, suite labels such as `python-legacy`, `.py` migration path constants, or `legacyPythonFiles` identifiers but no child-process execution remains accepted.
    - GREEN: the compiled retirement CLI rejects the retired parity flag, while post-retirement audit and historical pre-receipt verification still pass.
  </behavior>
  <action>
    Start with the failing allowed-member fixtures, then implement a bounded CJS execution-policy check inside `validatePack`. Inspect every packed `.cjs` member and identify actual command sinks originating from `node:child_process` or `child_process` (`spawn`, `spawnSync`, `exec`, `execSync`, `execFile`, and `execFileSync`, including namespace and destructured bindings). Reject a resolved interpreter command whose basename is `py`, `python`, `python3`, or a versioned/Windows executable form, and reject a resolved shell command that launches such an interpreter against a Python script. Keep the check sink-aware: do not reject a member merely because its comments, strings, receipt suite names, migration identifiers, or owned legacy paths contain historical Python vocabulary. Keep diagnostics at stable code/path metadata only and never emit member bytes.

    In `retirement-audit.cts`, remove `runCommand`, `runParity`, the parity CLI branch, and all Python suite subprocess construction. Preserve the child-process calls used for Git provenance, every post-retirement mode, immutable baseline verification, and `--verify-pre-receipt`; historical receipts may retain their old suite labels as data. Remove only the obsolete `verify:parity-before-retire` script from `package.json`; do not alter dependency ranges, the public bin, the exact package file allow-list, or the historical Plan 15 receipt. Build regenerates the ignored `dist` CJS, and the real pack audit must scan that compiled output per D-01/D-02.
  </action>
  <verify>
    <automated>npm run ci:local</automated>
  </verify>
  <acceptance_criteria>
    - The full clean local gate passes after build: exact dependency audit, complete Node tests, deterministic generation, real pack audit, and all three required loopback host smokes.
    - The allowed-CJS negative fixtures fail for executable interpreter data flow, while historical words, receipt labels, migration paths, and legacy identifiers pass.
    - The real packed `dist/maintainer/retirement-audit.cjs` contains no reachable parity flag or Python subprocess path; the obsolete package script is absent.
    - Post-retirement and historical receipt verification remain Node-only and green; no current public-version or publication claim is changed.
  </acceptance_criteria>
  <done>The actual package is Node-only and the tarball gate now proves the verifier's missing content invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild from the atomic fix commit and independently re-prove the packed bytes</name>
  <read_first>
    package.json
    dist/maintainer/retirement-audit.cjs
    dist/maintainer/pack-audit.cjs
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <files>None (read-only verification after Task 1 commit)</files>
  <action>
    Confirm the Task 1 implementation commit contains exactly the five declared source/test/manifest paths and leaves the pre-existing `.planning`/`.gsd` worktree baseline untouched. Rebuild from that committed source, rerun the complete local gate, run the post-retirement audit, and run the real tgz audit again. Treat the compiled CJS and the temporary tgz as the evidence surface; do not infer success from source-only searches. Record only stable pass/fail metadata and the commit SHA in the SUMMARY, never archive member contents, MCP values, npm credentials, or subprocess output bodies.
  </action>
  <verify>
    <automated>npm run ci:local &amp;&amp; npm run audit:retirement -- --mode post</automated>
  </verify>
  <acceptance_criteria>
    - The exact committed implementation rebuilds and passes the complete local gate a second time with the real tgz content check active.
    - `audit:retirement --mode post` remains green without invoking Python.
    - The implementation commit is atomic for the declared five paths and does not include planning state, build outputs, credentials, or unrelated user work.
  </acceptance_criteria>
  <done>A committed, reproducible Node-only tarball fix is ready for the pre-release source push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiled CJS → npm tarball | Locally generated executable bytes cross into the public installation artifact. |
| Tar member bytes → pack policy | Untrusted/changed CJS content is classified without exposing its contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-31-01 | Tampering | `validatePack` CJS content policy | high | mitigate | Scan every allowed CJS member for interpreter-to-child-process execution data flow and exercise direct, aliased, and shell fixtures. |
| T-03.1-31-02 | Denial of Service | historical migration/receipt content | medium | mitigate | Sink-aware fixtures prove historical words and legacy identifiers do not cause archive rejection. |
| T-03.1-31-03 | Information Disclosure | pack diagnostics | high | mitigate | Emit stable codes and relative paths only; never emit archive bytes, command bodies, or credential-bearing configuration. |
</threat_model>

<verification>
`npm run ci:local` and `npm run audit:retirement -- --mode post` pass from the exact Task 1 commit, and the real tgz content check rejects executable Python subprocess fixtures in allowed CJS members.
</verification>

<success_criteria>
- JS-01 is true for the bytes users install, not only for tracked filename extensions.
- JS-06 has a negative real-content test that would have caught the shipped parity branch.
- Historical receipts and migration identifiers remain valid data, and public `0.1.6` remains unchanged historical evidence.
</success_criteria>

## Artifacts this phase produces

- A five-path atomic implementation commit removing the retired parity route and package script.
- A content-aware `validatePack` policy with executable-CJS negative fixtures and false-positive controls.
- Fresh complete local-gate and post-retirement evidence tied to the exact implementation commit.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md` when done.
</output>

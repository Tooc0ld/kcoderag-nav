---
phase: 03.1-javascript-npx
plan: 31
type: execute
wave: 27
depends_on: ["03.1-14"]
files_modified:
  - package.json
  - src/maintainer/retirement-audit.cts
  - src/maintainer/pack-audit.cts
  - tests/maintainer/retirement-audit.test.cts
  - tests/maintainer/pack-audit.test.cts
autonomous: true
gap_closure: true
requirements: [JS-01, JS-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "JS-01/D-01/D-02: the maintainer-only retirement auditor is outside the closed npm allow-list, so its historical parity implementation is impossible to ship in the public tarball."
    - "JS-06: the real-tarball inventory rejects the explicitly excluded retirement auditor and any undeclared compiled output; the proof is exact membership, not partial JavaScript semantic detection."
    - "The obsolete parity package script and reachable parity CLI branch are removed, while Node-only post-retirement and historical receipt verification remain executable from repository builds."
    - "Historical retirement receipt labels, legacy migration identifiers, comments, and fixtures remain valid data and do not weaken the closed inventory proof."
  artifacts:
    - path: "src/maintainer/retirement-audit.cts"
      provides: "Node-only retirement modes and historical receipt verification with the retired parity runner removed"
    - path: "src/maintainer/pack-audit.cts"
      provides: "Closed compiled-output publication policy with an explicit non-publishable retirement-auditor set"
      exports: ["validatePack", "auditPack"]
    - path: "tests/maintainer/pack-audit.test.cts"
      provides: "Allowed-CJS negative fixtures and false-positive controls"
    - path: "package.json"
      provides: "Published manifest without the obsolete parity script"
  key_links:
    - from: "src/maintainer/retirement-audit.cts"
      to: "dist/maintainer/retirement-audit.cjs"
      via: "TypeScript CJS build before the real pack audit"
      pattern: "retirement-audit.cjs"
    - from: "package.json files"
      to: "src/maintainer/pack-audit.cts"
      via: "exact package allow-list expansion, explicit compiled-output exclusion, and byte-for-byte tar inventory equality"
      pattern: "NON_PUBLISHED_COMPILED_OUTPUTS"
---

<objective>
Close the JS-01/JS-06 verifier gap by removing the retired parity command and excluding its maintainer-only compiled auditor from the exact npm publication inventory.

Purpose: Restore the phase's Node-only distribution invariant with a closed membership proof rather than an unsound selected-pattern JavaScript detector.
Output: A retired parity entry point, a closed pack inventory with an explicit non-publishable compiled artifact, focused regressions, and one full locally green implementation commit.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-05-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@src/maintainer/retirement-audit.cts
@src/maintainer/pack-audit.cts
@tests/maintainer/retirement-audit.test.cts
@tests/maintainer/pack-audit.test.cts
@package.json

<interfaces>
- `validatePack({ packageJson, expectedPaths, archiveEntries })` is the pure closed tar-member policy boundary; exact path equality and the explicit non-publishable compiled-output set determine acceptance without parsing JavaScript semantics.
- `auditPack({ root })` builds and inspects one real temporary tgz, then proves repository status and tree preservation.
- `retirement-audit main(argv)` must retain `--verify-pre-receipt` and `--mode post` behavior after the obsolete execution branch is removed.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end closed tarball exclusion of the retired parity auditor</name>
  <read_first>
    package.json
    src/maintainer/retirement-audit.cts
    src/maintainer/pack-audit.cts
    tests/maintainer/retirement-audit.test.cts
    tests/maintainer/pack-audit.test.cts
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <reversibility rating="costly">D-01 already makes the Python-to-CJS migration a coordinated distribution contract; this task completes that locked choice without reopening it.</reversibility>
  <files>package.json, src/maintainer/retirement-audit.cts, src/maintainer/pack-audit.cts, tests/maintainer/retirement-audit.test.cts, tests/maintainer/pack-audit.test.cts</files>
  <behavior>
    - RED: re-adding `dist/maintainer/retirement-audit.cjs` to `package.json.files`, the expected pack paths, or a synthetic archive fails with a stable non-publishable/artifact-inventory code.
    - RED: any extra compiled CJS member not present in the declared closed allow-list fails exact archive equality; the test does not depend on recognizing command spelling, wrappers, computed values, or concatenated strings.
    - GREEN: historical receipt labels, migration identifiers, `.py` path data, comments, and fixtures outside the published inventory remain accepted evidence rather than triggering content heuristics.
    - GREEN: repository-built retirement audit rejects the retired parity flag, while post-retirement audit and historical pre-receipt verification still pass.
  </behavior>
  <action>
    Start with failing inventory fixtures. Confirm by import/call-site tests that no public CLI, host adapter, hook, generator asset, or smoke runtime loads `dist/maintainer/retirement-audit.cjs`; it is a maintainer-only repository gate. Remove that path from `package.json.files` and define it in one frozen `NON_PUBLISHED_COMPILED_OUTPUTS` policy in `pack-audit.cts`. Change `assertCompiledOutputPolicy` so the only compiled source output permitted to exist outside the publication allow-list is that exact path, while every other compiled CJS output must remain declared. Make `validatePack` fail closed if an explicitly non-publishable path appears in declared paths, expected paths, npm's pack manifest, or parsed archive entries, and retain exact equality among the declared allow-list expansion, npm pack manifest, and actual tar members. Do not add a JavaScript parser, dependency, selected command regex, or semantic claim about arbitrary program reachability; the proof is that the risky artifact cannot be a tar member at all. Diagnostics remain stable codes and safe relative paths only.

    In `retirement-audit.cts`, remove `runCommand`, `runParity`, the parity CLI branch, and all Python suite subprocess construction. Preserve the child-process calls used for Git provenance, every post-retirement mode, immutable baseline verification, and `--verify-pre-receipt`; historical receipts may retain their old suite labels as data. Remove the obsolete `verify:parity-before-retire` script from `package.json`; do not alter dependency ranges, the public bin, other exact package entries, or the historical Plan 15 receipt. Build still produces the repository-only auditor for historical/post-retirement verification, while the real `npm pack` audit proves its absence from published bytes per D-01/D-02.
  </action>
  <verify>
    <automated>npm run ci:local</automated>
  </verify>
  <acceptance_criteria>
    - The full clean local gate passes after build: exact dependency audit, complete Node tests, deterministic generation, real pack audit, and all three required loopback host smokes.
    - Reintroducing the excluded retirement auditor through `package.json.files`, expected paths, npm's manifest, or actual tar entries fails; unknown/computed/wrapper/concatenated command shapes cannot evade a path-absence proof.
    - The real tarball has no `dist/maintainer/retirement-audit.cjs`, no undeclared compiled member, and no obsolete parity package script.
    - Post-retirement and historical receipt verification remain Node-only and green; no current public-version or publication claim is changed.
  </acceptance_criteria>
  <done>The maintainer-only parity artifact is impossible to ship under the closed npm inventory, and the obsolete executable route is retired in repository source.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild from the atomic fix commit and independently re-prove the packed bytes</name>
  <read_first>
    package.json
    dist/maintainer/retirement-audit.cjs
    dist/maintainer/pack-audit.cjs
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <files>None (read-only verification after Task 1 commit)</files>
  <action>
    Confirm the Task 1 implementation commit contains exactly the five declared source/test/manifest paths and leaves the pre-existing `.planning`/`.gsd` worktree baseline untouched. Rebuild from that committed source, rerun the complete local gate, run the post-retirement audit, and run the real tgz audit again. Treat exact tar member names as the evidence surface: assert the excluded retirement auditor is absent and every present compiled member belongs to the declared allow-list. Do not infer publication safety from source-only searches or selected command patterns. Record only stable pass/fail metadata and the commit SHA in the SUMMARY, never archive member contents, MCP values, npm credentials, or subprocess output bodies.
  </action>
  <verify>
    <automated>npm run ci:local &amp;&amp; npm run audit:retirement -- --mode post</automated>
  </verify>
  <acceptance_criteria>
    - The exact committed implementation rebuilds and passes the complete local gate a second time with the real tgz closed-inventory check active.
    - `audit:retirement --mode post` remains green without invoking Python.
    - The implementation commit is atomic for the declared five paths and does not include planning state, build outputs, credentials, or unrelated user work.
  </acceptance_criteria>
  <done>A committed, reproducible Node-only tarball fix is ready for the pre-release source push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiled CJS → npm tarball | Locally generated executable bytes cross into the public installation artifact. |
| Tar member names → pack policy | Untrusted/changed archive membership is compared with the exact allow-list and explicit exclusion without interpreting JavaScript semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-31-01 | Tampering | closed npm archive inventory | high | mitigate | Explicitly deny the maintainer-only auditor and require exact equality among source allow-list, npm manifest, and parsed tar members. |
| T-03.1-31-02 | Denial of Service | historical migration/receipt content | medium | mitigate | Avoid semantic JavaScript scanning; historical vocabulary outside the public artifact set cannot cause false rejection. |
| T-03.1-31-03 | Information Disclosure | pack diagnostics | high | mitigate | Emit stable codes and relative paths only; never emit archive bytes, command bodies, or credential-bearing configuration. |
</threat_model>

<verification>
`npm run ci:local` and `npm run audit:retirement -- --mode post` pass from the exact Task 1 commit, and the real tgz inventory omits the explicitly non-publishable retirement auditor while rejecting every attempt to reintroduce it.
</verification>

<success_criteria>
- JS-01 is true for the bytes users install because the only artifact that carried the parity branch is excluded by closed membership.
- JS-06 has a real-tarball negative inventory test that would have caught the shipped retirement auditor without an incomplete semantic parser.
- Historical receipts and migration identifiers remain valid data, and public `0.1.6` remains unchanged historical evidence.
</success_criteria>

## Artifacts this phase produces

- A five-path atomic implementation commit removing the retired parity route and package script.
- A closed `validatePack` publication policy with explicit retirement-auditor exclusion and exact-inventory negative fixtures.
- Fresh complete local-gate and post-retirement evidence tied to the exact implementation commit.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md` when done.
</output>

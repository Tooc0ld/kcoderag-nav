---
phase: 03.1-javascript-npx
plan: 01
subsystem: build-tooling
tags: [nodejs, typescript, npm, supply-chain, codex, transaction]

requires:
  - phase: 03.1-29
    provides: Root-anchored ignore boundary for Node dependencies and compiled outputs
provides:
  - Audited Node 22+ TypeScript-to-CJS build and public kcoderag-nav bin contract
  - Machine enforcement of the approved exact npm development graph
  - Production-quality Codex QA npx install tracer with safe output and complete rollback
affects: [03.1-02, host-adapters, cli, dependency-audit, npm-pack, release]

actuals:
  tokens: 14810
  tasks: 3
  commits: 3

tech-stack:
  added: [typescript@6.0.3, "@types/node@22.20.1", undici-types@6.21.0]
  patterns:
    - TypeScript maintenance source compiled to directly executable CommonJS
    - Exact offline dependency graph audit across package metadata, lock v3, and npm ls
    - Validate-stage-commit-state-last transaction with full rollback

key-files:
  created:
    - package.json
    - package-lock.json
    - tsconfig.json
    - tsconfig.tests.json
    - src/maintainer/dependency-audit.cts
    - src/bin/kcoderag-nav.cts
    - src/tracer/codex-install.cts
    - tests/maintainer/dependency-audit.test.cts
    - tests/tracer/codex-install.test.cts
  modified: []

key-decisions:
  - "Accepted the exact audited graph: typescript@6.0.3 in >=5.7.0 <7.0.0, @types/node@22.20.1 in >=22.0.0 <23.0.0, and sole transitive undici-types@6.21.0."
  - "Accepted the audited registry resolution, integrity, official ownership, and absence of lifecycle scripts; any version, edge, integrity, ownership, or lifecycle drift requires a blocking re-audit."
  - "Confirmed the public unscoped package name kcoderag-nav and user command npx kcoderag-nav@latest install."
  - "Build precedes compiled audits/tests in clean CI, and the aggregate Node test command uses an explicit compiled test glob."

patterns-established:
  - "Supply-chain gate: dependencies remain empty and the only allowed development graph is frozen in dependency-audit.cjs."
  - "CLI boundary: JSON mode emits exactly one safe JSON value and never includes MCP values."
  - "Install transaction: complete preflight and staging precede replacement, with install-state.json committed last and every failure restoring the original tree."

requirements-completed: [JS-01, JS-02, JS-03, JS-04]

coverage:
  - id: D1
    description: "The public package uses Node 22+ compiled CJS with no production dependency or runtime compiler."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "npm run build && npm run deps:audit"
        status: pass
      - kind: other
        ref: "npm pack --dry-run --json forbidden-content audit"
        status: pass
    human_judgment: false
  - id: D2
    description: "The exact approved npm graph, parent edge, resolution, integrity, ownership proxy, lifecycle, bin, engine, and script policies are machine-enforced."
    requirement: JS-02
    verification:
      - kind: unit
        ref: "tests/maintainer/dependency-audit.test.cts — 7/7 passing"
        status: pass
    human_judgment: false
  - id: D3
    description: "The Codex QA npx path uses cwd/explicit target, normalized confirmation, Node gating, stable JSON, and zero-write refusal."
    requirement: JS-03
    verification:
      - kind: e2e
        ref: "tests/tracer/codex-install.test.cts — local npm exec bin slice"
        status: pass
    human_judgment: false
  - id: D4
    description: "Codex QA installation stages all files, commits state last, rejects drift and path escape, and restores the full tree at every injected failure point."
    requirement: JS-04
    verification:
      - kind: integration
        ref: "tests/tracer/codex-install.test.cts — staging/commit rollback matrix, 5/5 passing"
        status: pass
    human_judgment: false

duration: 19min
completed: 2026-08-23
status: complete
---

# Phase 03.1 Plan 01: Audited Node Runtime and Codex QA npx Tracer Summary

**An exact-audited Node 22 CJS toolchain now drives a secret-safe Codex QA npx install slice with normalized confirmation, drift protection, state-last commit, and complete rollback.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-08-23T13:26:18Z
- **Completed:** 2026-08-23T13:45:19Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Established `kcoderag-nav` as a Node 22+ public CJS bin package with no production dependencies, no runtime compiler, and an explicit publish allow-list.
- Added a machine dependency audit that cross-checks `package.json`, lockfile v3, and the installed `npm ls` tree against the human-approved graph.
- Proved the compiled local npx/npm-exec Codex QA path installs MCP, hook, skill, and versioned state while preserving unrelated project bytes and restoring the complete tree on staged or committed failures.

## Accepted Blocking-Human Decision

The user approved `accept-audit-and-name` before any package-manager installation or public package write:

- `typescript >=5.7.0 <7.0.0`, locked to `6.0.3`, Microsoft ownership, registry resolution `https://registry.npmjs.org/typescript/-/typescript-6.0.3.tgz`, integrity `sha512-y2TvuxSZPDyQakkFRPZHKFm+KKVqIisdg9/CZwm9ftvKXLP8NRWj38/ODjNbr43SsoXqNuAisEf1GdCxqWcdBw==`.
- `@types/node >=22.0.0 <23.0.0`, locked to `22.20.1`, DefinitelyTyped/Microsoft types ownership, registry resolution `https://registry.npmjs.org/@types/node/-/node-22.20.1.tgz`, integrity `sha512-EANqOCF9QFyra+4pfxUcX9STKJpCLjMbObVzljIJomAWSnuSIEAvyzEU53GaajbXJEgdh0iEcPL+DGvpUd4k1Q==`.
- Sole transitive `undici-types ~6.21.0`, locked to `6.21.0`, Node.js/Undici ownership, registry resolution `https://registry.npmjs.org/undici-types/-/undici-types-6.21.0.tgz`, integrity `sha512-iwDZqg0QAGrg9Rav5H4n0M64c3mkR59cJ6wQp+7C4nI0gsmExaedaYLNO44eT4AtBBwjbTiGPMlt2Md0T9H9JQ==`.
- No package in the graph has an install lifecycle script. Any dependency version, edge, resolution, integrity, ownership, or lifecycle-script drift must hard-stop for re-audit.
- The one-way public contract is the unscoped package `kcoderag-nav` and command `npx kcoderag-nav@latest install`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Accept exact npm graph and public contract** - explicit human decision (no file commit)
2. **Task 2: Lock approved toolchain and npm command registry** - `4069b91` (chore)
3. **Task 3 RED: Specify dependency and Codex QA tracer behavior** - `da7a367` (test)
4. **Task 3 GREEN: Implement audit, compiled CLI, and atomic install tracer** - `127eb73` (feat)

**Plan metadata:** committed separately after this SUMMARY is verified.

## Files Created/Modified

- `package.json` - Public package/bin, Node engine, publish allow-list, compiled command registry, and dependency boundary.
- `package-lock.json` - Lock v3 graph for the two approved direct development dependencies and sole transitive edge.
- `tsconfig.json` - Strict `.cts` source compilation to `dist/**/*.cjs`.
- `tsconfig.tests.json` - Strict test compilation to `dist-tests/**/*.cjs`.
- `src/maintainer/dependency-audit.cts` - Exact package, lock, installed-tree, lifecycle, integrity, and script-policy audit.
- `src/bin/kcoderag-nav.cts` - Shebang-backed public CJS bin entry.
- `src/tracer/codex-install.cts` - Codex QA argument, target, merge, ownership, staging, transaction, state, and safe-output slice.
- `tests/maintainer/dependency-audit.test.cts` - Approved-graph success and graph/policy tamper rejection.
- `tests/tracer/codex-install.test.cts` - Local npm-exec E2E, confirmation/runtime/refusal, drift/symlink, idempotency, and rollback matrix.

## Decisions Made

- Kept production `dependencies` empty; TypeScript, Node declarations, and their declaration-only transitive remain development-only.
- Froze registry resolutions and integrity values in the audit so an offline clean install cannot silently accept a changed graph.
- Treated MCP source material as sensitive input: installation may render it into the selected target, but result/error/state output contains only safe codes, paths, versions, and digests.
- Kept the Plan 01 tracer deliberately scoped to Codex QA; the shared host adapter and full command controller remain assigned to later plans.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Made the audited Node declarations visible to both TypeScript builds**
- **Found during:** Task 3 GREEN compilation
- **Issue:** The initial tsconfig files omitted `types: ["node"]` and explicitly set a TypeScript 6 deprecated `esModuleInterop: false`, so the approved compiler could not resolve Node globals or complete a build.
- **Fix:** Added the Node type scope and removed the deprecated no-op option in both configs.
- **Files modified:** `tsconfig.json`, `tsconfig.tests.json`
- **Verification:** Fresh `npm run build` exits 0 before all compiled tests.
- **Committed in:** `127eb73`

**2. [Rule 1 - Bug] Corrected the clean-build ordering contradiction**
- **Found during:** Task 3 overall verification
- **Issue:** The planned `ci:local` placed `deps:audit` before `build`, although `deps:audit` intentionally executes `dist/maintainer/dependency-audit.cjs` and `dist/` is ignored rather than committed.
- **Fix:** `ci:local` now builds first, then audits and tests compiled CJS. The audit permits `ci:local` as the sole orchestration script that invokes `build`; all other non-build scripts remain forbidden from compiling.
- **Files modified:** `package.json`, `src/maintainer/dependency-audit.cts`, `tests/maintainer/dependency-audit.test.cts`
- **Verification:** Outputs were cleaned, followed by `npm ci --ignore-scripts`, build, audit, and the complete compiled suite.
- **Committed in:** `127eb73`

**3. [Rule 1 - Bug] Replaced a non-runnable Node test directory argument**
- **Found during:** Task 3 full-suite verification on Node 24
- **Issue:** `node --test dist-tests` treated the directory as a module path and failed with `MODULE_NOT_FOUND`.
- **Fix:** The aggregate test command now uses the cross-platform compiled glob `node --test "dist-tests/**/*.test.cjs"`.
- **Files modified:** `package.json`
- **Verification:** `npm test` discovers and passes all 12 compiled tests on Windows.
- **Committed in:** `127eb73`

---

**Total deviations:** 3 auto-fixed (2 Rule 1 bugs, 1 Rule 3 blocker)
**Impact on plan:** All changes were required to make the locked compiled-CJS contract executable from a clean checkout; no runtime dependency or host scope was added.

## Issues Encountered

- Windows Node emitted a deprecation warning for `execFile(..., { shell: true })`; fixed by invoking the known npm CLI through `cmd.exe` without Node's shell option or argument interpolation.
- The tracer feedback gate was approved after the compiled E2E and rollback evidence was presented.

## User Setup Required

None - no external service configuration or publication is required for this plan.

## Next Phase Readiness

- Plan 03.1-02 can build shared transaction/state abstractions on the proven tracer behavior and compiled command registry.
- The current tracer intentionally requires generated CJS hook assets; later generator/hook plans must create the final QA/Dev assets before public packing or release.
- No package was published and no remote state was changed.

## Known Stubs

None. The conditional symlink fallback was not activated in the latest Windows run; the symlink escape test executed and passed.

## Self-Check: PASSED

- All 9 production/test/config files and this SUMMARY exist.
- Task commits `4069b91`, `da7a367`, and `127eb73` exist in Git history.
- Fresh verification passed: build, dependency audit, 7/7 dependency tests, 5/5 tracer tests, 12/12 aggregate tests, package allow-list scan, and runtime-load scan.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-23*

---
phase: 03.1-javascript-npx
plan: 22
subsystem: maintainer-safety
tags: [nodejs, typescript, documentation, git, provenance, secret-safety]

requires:
  - phase: 03.1-03
    provides: Compiled Node/CJS runtime and launcher migration baseline
provides:
  - Scoped documentation checker with path, link, command, host, runtime, and ownership policies
  - Sanitized sibling guide baseline, receipt, and summary provenance verification
  - Compiled CLI gates for later project and authoritative-guide documentation plans
affects: [03.1-19, 03.1-20, 03.1-21, documentation, release-evidence]

actuals:
  tokens: 12191
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - Explicit-path documentation scans with no implicit repository traversal
    - Stable code/path/line diagnostics that never echo source lines
    - Git-metadata-only sibling baselines tied to one guide-only child commit

key-files:
  created:
    - src/maintainer/docs-check.cts
    - tests/maintainer/docs-check.test.cts
    - src/maintainer/sibling-guide-audit.cts
    - tests/maintainer/sibling-guide-audit.test.cts
  modified: []

key-decisions:
  - "Planning policy treats only fenced commands as active instructions so migration history and decision prose do not create false positives."
  - "Sibling-guide policy accepts exactly the authoritative KCodeRag guide path; all repository policies require explicit relative in-repository paths."
  - "A receipt is valid only when its guide-only commit parent equals the captured baseline HEAD and its baseline evidence digest is independently reproducible."

patterns-established:
  - "Documentation diagnostics contain only code, repository-relative path, and line number."
  - "Sibling evidence stores sorted status code/path metadata and digests, never unrelated diff or file content."
  - "Evidence CLI writes only named files inside the navigation repository and rejects traversal or symlink paths."

requirements-completed: []

coverage:
  - id: D1
    description: "Explicit scoped documentation checks reject path escape, symlinks, broken links, obsolete active commands, invalid hosts, local guide copies, and secret-shaped values."
    requirement: JS-08
    verification:
      - kind: unit
        ref: "tests/maintainer/docs-check.test.cts — 7/7 passing"
        status: pass
      - kind: integration
        ref: "npm run docs:check -- --policy planning .planning/phases/03.1-javascript-npx/03.1-CONTEXT.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Sibling guide evidence proves a guide-clean baseline, exact guide-only child commit, unchanged unrelated status, secret scan, and dual repository hashes."
    requirement: JS-08
    verification:
      - kind: integration
        ref: "tests/maintainer/sibling-guide-audit.test.cts — 6/6 passing"
        status: pass
    human_judgment: false
  - id: D3
    description: "All four sibling audit CLI modes and all four documentation policies execute as compiled CJS."
    requirement: JS-08
    verification:
      - kind: integration
        ref: "npm run build && npm run test:docs && npm run test:sibling-guide-audit"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-23
status: complete
---

# Phase 03.1 Plan 22: Scoped Documentation and Sibling Guide Evidence Summary

**Later documentation plans now have compiled, secret-safe gates for explicit local files and a tamper-evident guide-only sibling commit chain.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-23T14:24:10Z
- **Completed:** 2026-08-23T14:32:19Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added a scoped Markdown checker that validates local links, current npx/host/Node/Cursor wording, single-guide ownership, and active obsolete instructions without scanning implicit repository scope.
- Added safe compiled CLI behavior for `user-docs`, `project-instructions`, `planning`, and the exact authoritative `sibling-guide` path.
- Added sibling baseline, receipt, and summary verification that proves unchanged unrelated status, an exact guide-only child commit, safe guide content, and matching 40-hex repository heads.

## Task Commits

TDD behavior was committed in RED/GREEN order:

1. **Task 1 RED: Specify scoped documentation checks** - `13d2a2b` (test)
2. **Task 1 GREEN: Implement scoped documentation checker** - `6d292da` (feat)
3. **Task 2: Verify compiled docs checker CLI** - `2706fa0` (test)
4. **Task 3 RED: Specify sibling guide evidence audit** - `e1a468b` (test)
5. **Task 3 GREEN: Implement sibling guide evidence audit** - `77b4d3c` (feat)

## Files Created/Modified

- `src/maintainer/docs-check.cts` - Path-constrained checker, safe diagnostics, policy rules, and compiled CLI.
- `tests/maintainer/docs-check.test.cts` - Content, path, symlink, policy, and black-box CLI coverage.
- `src/maintainer/sibling-guide-audit.cts` - Git metadata capture, evidence digest, commit-scope, secret, and summary-hash verification.
- `tests/maintainer/sibling-guide-audit.test.cts` - Temporary-repository coverage for all four evidence modes and tamper cases.

## Decisions Made

- Kept migration prose valid under the planning policy while still scanning fenced commands; user-facing and project instruction policies additionally scan active install/update sections.
- Fixed sibling mutation authority to the one resolved KCodeRag guide and constrained baseline/receipt/summary evidence files to the navigation repository.
- Bound receipt provenance to one child commit whose parent is the captured baseline HEAD, rather than accepting two unrelated valid hashes.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Windows file symlink creation requires elevated privileges, so the symlink-path test falls back to a directory junction and still executes the refusal path with zero skips.
- An already-dirty file can change bytes without changing porcelain status code/path; the required “changed status” fixture therefore changes the tracked entry from worktree-modified to index-modified, matching the plan's intentionally content-free status contract.

## User Setup Required

None - this plan performs no sibling guide mutation and requires no credentials.

## Next Phase Readiness

- Plans 19 and 20 can invoke scoped documentation policies without whole-repository false positives.
- Plan 21 can capture the sibling baseline before editing, record a guide-only receipt after committing, and verify its SUMMARY hashes.
- JS-08 remains open until the three-host lifecycle/smoke and actual README/authoritative-guide synchronization plans complete.

## Known Stubs

None. All 13 plan-specific tests and all 45 aggregate Node tests pass with zero skipped or todo cases.

## Self-Check: PASSED

- All four planned source/test artifacts and this SUMMARY exist.
- Task commits `13d2a2b`, `6d292da`, `2706fa0`, `e1a468b`, and `77b4d3c` exist in Git history.
- Fresh verification passed: build, exact dependency audit, docs 7/7, sibling audit 6/6, required planning document check, and aggregate Node 45/45.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-23*

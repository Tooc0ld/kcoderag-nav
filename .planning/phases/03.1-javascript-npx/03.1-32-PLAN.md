---
phase: 03.1-javascript-npx
plan: 32
type: execute
wave: 28
depends_on: ["03.1-31"]
files_modified:
  - src/smoke/host-smoke.cts
  - tests/smoke/host-smoke.test.cts
  - src/maintainer/publish-receipt.cts
  - tests/maintainer/publish-receipt.test.cts
autonomous: true
gap_closure: true
requirements: [JS-04, JS-07, JS-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The required three-host harness can independently acquire an exact public version and `kcoderag-nav@latest`, while rejecting arbitrary tags, ranges, URLs, and package names."
    - "A versioned sanitized receipt can distinguish exact-version and latest lifecycle PASS for Codex, Claude Code, and Cursor, bind registry `gitHead` to the release SHA, and prove all four required release lanes."
    - "JS-04/JS-07: the final reviewed source is committed, pushed to the default branch, and receives one successful Windows/Linux × Node 22/24 CI run whose head SHA is that exact pre-release commit."
    - "The pre-release push creates no package version, npm publication, or tag and makes no claim that public `0.1.6` contains the later safety fixes."
  artifacts:
    - path: "src/smoke/host-smoke.cts"
      provides: "Strict exact-version and `@latest` required-contract acquisition"
      exports: ["runHostSmoke", "main"]
    - path: "src/maintainer/publish-receipt.cts"
      provides: "Backward-compatible historical v1 verification plus v2 dual-specifier public release evidence"
      exports: ["verifyPublishReceipt", "recordPublishReceipt"]
    - path: ".github/workflows/ci.yml"
      provides: "Existing four-lane pre-release source validation exercised on the exact pushed SHA"
  key_links:
    - from: "public package specifier"
      to: "src/smoke/host-smoke.cts"
      via: "strict package-spec normalization and fresh npm acquisition"
      pattern: "packageSpec"
    - from: "GitHub CI run"
      to: "pre-release commit"
      via: "unique push run with exact `headSha` and four successful required-contract jobs"
      pattern: "headSha"
    - from: "v2 publish receipt"
      to: "exact/latest host evidence and release workflow lanes"
      via: "closed offline schema and all-true nested evidence"
      pattern: "schema_version"
---

<objective>
Prepare honest exact-version/latest public evidence and prove the final reversible source on the required remote CI matrix before any new version is authorized.

Purpose: Close the local-versus-public and unexercised-matrix seams without crossing the one-way npm publication boundary.
Output: A dual-specifier smoke contract, a backward-compatible v2 receipt contract, a pushed final source SHA, and exact four-lane CI evidence.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
@.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
@src/smoke/host-smoke.cts
@tests/smoke/host-smoke.test.cts
@src/maintainer/publish-receipt.cts
@tests/maintainer/publish-receipt.test.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- `runHostSmoke({ mode, packageSpec, temporaryRoot, hosts })` returns per-host PASS/FAIL/NOT_RUN with eleven required evidence bits; required FAIL or NOT_RUN is nonzero at the CLI.
- `verifyPublishReceipt(value)` is offline and closed-schema; historical schema v1 must continue to validate the immutable `03.1-14` receipt.
- `recordPublishReceipt(file, value)` performs private atomic output after verification and must never accept raw logs, command output, headers, environment values, or credential-bearing URLs.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end exact-version and latest acquisition through all three host lifecycles</name>
  <read_first>
    src/smoke/host-smoke.cts
    tests/smoke/host-smoke.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <files>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts</files>
  <behavior>
    - RED: required-contract mode accepts `kcoderag-nav@1.2.3` and `kcoderag-nav@latest` as separate public acquisition contracts and carries each normalized specifier through install, status, MCP/navigation, update, and uninstall for Codex, Claude Code, and Cursor.
    - RED: package aliases, prereleases, SemVer ranges, alternate dist-tags, URLs, directories other than the existing local tgz seam, and other package names fail before acquisition.
    - GREEN: each run uses an isolated temporary project/runtime and can be given a separate fresh npm cache/user-config boundary; missing evidence or NOT_RUN remains a required failure.
  </behavior>
  <action>
    Replace the exact-only public package matcher with a strict matcher that permits only `kcoderag-nav@X.Y.Z` or the literal `kcoderag-nav@latest`; retain the existing contained local `.tgz` contract used by local CI. Do not accept arbitrary npm tags, ranges, URLs, aliases, or packages. Preserve the eleven-bit required-contract behavior for each host and Cursor's Rule/skill/MCP capability boundary. Add parser/acquisition fixtures for both allowed public forms and every rejected form, plus a three-host injected acquisition tracer proving the chosen specifier reaches every lifecycle command unchanged. Keep package acquisition before project creation, output metadata-only, and npm/MCP credentials outside results.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - Exact-version and literal latest package specs traverse the complete three-host required contract independently.
    - All non-approved public specifier forms fail before acquisition or project writes.
    - PASS still requires every existing lifecycle, navigation, MCP, update, uninstall, and stub-receipt bit; no unavailable evidence becomes PASS.
  </acceptance_criteria>
  <done>The release action can produce fresh, comparable exact-version and latest three-host lifecycle evidence without a separate ad-hoc harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Version the sanitized receipt for lane-bound dual public evidence</name>
  <read_first>
    src/maintainer/publish-receipt.cts
    tests/maintainer/publish-receipt.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
  </read_first>
  <files>src/maintainer/publish-receipt.cts, tests/maintainer/publish-receipt.test.cts</files>
  <behavior>
    - Historical schema v1 verification remains byte-compatible with the immutable `03.1-14` receipt and never upgrades or rewrites it.
    - Schema v2 binds version/tag/release SHA to workflow head SHA, exact registry version and `gitHead`, latest dist-tag, the four exact OS/Node lane conclusions, publish-job success, and nested exact/latest host booleans.
    - Missing, extra, false, NOT_RUN, mismatched, or secret-like values fail offline with stable codes and without echoing input.
  </behavior>
  <action>
    Extend `PublishReceipt` as a discriminated v1/v2 union. Preserve the existing v1 verifier solely for historical evidence. Define v2 with the existing package/version/tag/release SHA/timestamp fields plus: registry exact version and `gitHead` bound to the release SHA; exact latest/version/bin/engine/repository metadata; workflow run ID/head SHA/push/success plus an exact-key four-lane map for Ubuntu/Windows and Node 22/24 and a successful publish-job field; and two exact-key host maps (`exact_version`, `latest`) where Codex, Claude, and Cursor must all be true. Keep the schema closed at every nesting level and keep record/verify offline. Tests must load the real historical v1 receipt, accept a complete v2 example, and independently corrupt every new cross-field/lane/specifier/host field. Continue recursively rejecting raw output, environment/header fields, credential URLs, NPM token markers, and MCP/Bearer material.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:publish-receipt &amp;&amp; npm run ci:local</automated>
  </verify>
  <acceptance_criteria>
    - The historical `0.1.6` v1 receipt still verifies exactly as historical evidence and is not rewritten or treated as evidence for later fixes.
    - A v2 receipt cannot pass unless release SHA, registry `gitHead`, exact/latest version, four required lanes, publish job, and both three-host maps all agree.
    - The complete local gate passes after the final Plan 31/32 source changes, including the Node-only real tarball audit.
  </acceptance_criteria>
  <done>The next public release can be represented by one closed, offline-verifiable receipt without collapsing exact-version and latest evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Push the exact final source SHA and require its four-lane ordinary CI run</name>
  <read_first>
    .github/workflows/ci.yml
    tests/maintainer/ci-contract.test.cts
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
    .planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
  </read_first>
  <precondition>`origin/master` is an ancestor of local `master`, GitHub CLI authentication succeeds, and no tracked/index change exists outside committed work; existing `.planning`/`.gsd` local state remains preserved.</precondition>
  <reversibility rating="costly">A default-branch push and CI record are persistent external history; recovery is a new audited revert commit, never a force rewrite.</reversibility>
  <files>None (remote default-branch push and read-only GitHub Actions evidence)</files>
  <action>
    After Tasks 1-2 are committed, rerun the complete local gate and capture the exact 40-character pre-release SHA. Assert the current branch is `master`, the remote branch is an ancestor, the index has no staged entries, and the only uncommitted state is the preserved pre-existing planning/GSD baseline. Push local `master` to `origin/master`; do not create or push a tag and do not invoke the release helper or npm publication.

    Discover the ordinary `CI` workflow run only by workflow name, push event, and the exact pre-release commit SHA. Require one unambiguous run, watch it with exit status, then re-query the run and jobs. Accept success only when `headSha` equals the captured SHA and all four required-contract jobs—Ubuntu Node 22, Ubuntu Node 24, Windows Node 22, and Windows Node 24—exist and conclude success. The optional authenticated-live job is not a substitute. Finally confirm local `HEAD`, `origin/master`, and the accepted run SHA are identical, while root package version/tag state and public npm latest remain historical `0.1.6`; state explicitly that this push has not published the later fixes.
  </action>
  <verify>
    <automated>$sha = (git rev-parse HEAD).Trim(); if ((git rev-parse origin/master).Trim() -ne $sha) { throw 'origin SHA mismatch' }; $runs = @(gh run list --workflow ci.yml --event push --commit $sha --json databaseId,headSha,status,conclusion | ConvertFrom-Json | Where-Object { $_.headSha -eq $sha -and $_.status -eq 'completed' -and $_.conclusion -eq 'success' }); if ($runs.Count -ne 1) { throw 'expected one successful CI run' }; $run = gh run view $runs[0].databaseId --json headSha,event,conclusion,jobs | ConvertFrom-Json; if ($run.headSha -ne $sha -or $run.event -ne 'push' -or $run.conclusion -ne 'success') { throw 'CI provenance mismatch' }; $required = @('Required contracts (ubuntu-latest, Node 22)','Required contracts (ubuntu-latest, Node 24)','Required contracts (windows-latest, Node 22)','Required contracts (windows-latest, Node 24)'); foreach ($name in $required) { $jobs = @($run.jobs | Where-Object { $_.name -eq $name -and $_.conclusion -eq 'success' }); if ($jobs.Count -ne 1) { throw "missing successful lane: $name" } }</automated>
  </verify>
  <acceptance_criteria>
    - The complete local gate passes immediately before push and the pushed SHA is the exact final Plan 31/32 source commit.
    - One ordinary push CI run is bound to that SHA and all four required OS/Node lanes succeed; missing/skipped/failed lanes stop execution.
    - No version file, tag, npm dist-tag, or public package changes in this plan; `0.1.6` remains acknowledged as the earlier release without these later fixes.
    - No NPM token value, workflow log body, MCP value, or Bearer value is read, printed, or persisted.
  </acceptance_criteria>
  <done>The reversible final source is on the default branch and has exact pre-release Windows/Linux × Node 22/24 evidence, ready for an explicit publication decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public npm specifier → smoke acquisition | A network package reference can create executable temporary package state. |
| Local final source → remote default branch | Reviewed commits become persistent shared history and trigger CI. |
| GitHub run/job metadata → release evidence | External run metadata is trusted only after exact SHA/event/job cross-checks. |
| External evidence → offline receipt | Only closed, nonsecret metadata and boolean outcomes may persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-32-01 | Tampering | package-spec normalization | high | mitigate | Exact allow-list for semantic version, literal latest, and contained local tgz; all aliases/ranges/URLs reject before acquisition. |
| T-03.1-32-02 | Spoofing | GitHub CI evidence | high | mitigate | Bind the unique push run and all four job conclusions to the exact pre-release SHA. |
| T-03.1-32-03 | Tampering | default-branch push | high | mitigate | Require ancestor relation, clean index, full local gates, exact HEAD/origin equality, and no force push. |
| T-03.1-32-04 | Information Disclosure | receipt and smoke output | critical | mitigate | Closed schemas, recursive secret rejection, metadata-only host results, and no raw workflow/npm/MCP output persistence. |
</threat_model>

<verification>
The exact pushed final source SHA passes the complete local gate and one ordinary GitHub CI run with successful Ubuntu/Windows Node 22/24 required-contract jobs; no tag or npm version is created.
</verification>

<success_criteria>
- The evidence harness and receipt can represent both exact-version and latest three-host lifecycle truthfully.
- The final source, including the Node-only pack fix and later lifecycle safety fixes, is pushed and four-lane green at one exact pre-release SHA.
- Publication remains completely separate and blocked on Plan 33's human decision.
</success_criteria>

## Artifacts this phase produces

- A four-path evidence-contract implementation with dual public package-specifier smoke and backward-compatible receipt v2.
- The exact final pre-release source commit pushed to `origin/master`.
- One accepted ordinary CI run and four successful required-contract job records bound to that commit SHA.
- No release commit, npm version, tag, or public-package mutation.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md` when done.
</output>

---
phase: 03.1-javascript-npx
plan: 32
type: execute
wave: 28
depends_on: ["03.1-31"]
files_modified:
  - src/smoke/host-smoke.cts
  - tests/smoke/host-smoke.test.cts
  - src/maintainer/publish-receipt.cts
  - tests/maintainer/publish-receipt.test.cts
autonomous: true
gap_closure: true
requirements: [JS-04, JS-07, JS-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The required three-host harness binds each requested exact-version or literal `@latest` specifier to the actual acquired package manifest version before any synthetic tgz lifecycle runs."
    - "Exact requests resolve to the requested X.Y.Z, while `@latest` resolves to the caller-supplied expected release version; latest races and arbitrary tags/ranges/URLs fail before project writes."
    - "A versioned sanitized receipt binds requested/resolved lifecycle provenance, registry exact/latest and `gitHead`, Release workflow identity/ref/head SHA, four lanes, and the dependent publish result."
    - "Plan 32 commits only the evidence-contract implementation and its SUMMARY; final default-branch push and external CI evidence wait for Plan 33 so the tested SHA includes this plan's SUMMARY commit."
  artifacts:
    - path: "src/smoke/host-smoke.cts"
      provides: "Strict exact-version and `@latest` required-contract acquisition"
      exports: ["runHostSmoke", "main"]
    - path: "src/maintainer/publish-receipt.cts"
      provides: "Backward-compatible historical v1 verification plus v2 dual-specifier public release evidence"
      exports: ["verifyPublishReceipt", "recordPublishReceipt"]
  key_links:
    - from: "public package specifier"
      to: "src/smoke/host-smoke.cts"
      via: "strict package-spec normalization and fresh npm acquisition"
      pattern: "packageSpec"
    - from: "requested public package specifier"
      to: "acquired installed manifest and synthetic lifecycle result"
      via: "expected-version cross-check on the installed acquired manifest plus immutable requested/resolved provenance carried with the synthetic tgz digest"
      pattern: "resolvedVersion"
    - from: "v2 publish receipt"
      to: "exact/latest host evidence and release workflow lanes"
      via: "closed offline schema and all-true nested evidence"
      pattern: "schema_version"
---

<objective>
Prepare honest exact-version/latest acquisition provenance and the closed receipt contract needed for the final release evidence.

Purpose: Make public lifecycle and workflow evidence resistant to specifier substitution and latest races before any external push or publication occurs.
Output: A provenance-bound dual-specifier smoke contract and backward-compatible v2 receipt contract, committed locally with a SUMMARY for Plan 33's tested pre-release SHA.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
@.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
@src/smoke/host-smoke.cts
@tests/smoke/host-smoke.test.cts
@src/maintainer/publish-receipt.cts
@tests/maintainer/publish-receipt.test.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- `runHostSmoke({ mode, packageSpec, expectedVersion, temporaryRoot, hosts })` returns immutable requested/resolved package provenance plus per-host PASS/FAIL/NOT_RUN with eleven required evidence bits; required FAIL, NOT_RUN, or version mismatch is nonzero at the CLI.
- `verifyPublishReceipt(value)` is offline and closed-schema; historical schema v1 must continue to validate the immutable `03.1-14` receipt.
- `recordPublishReceipt(file, value)` performs private atomic output after verification and must never accept raw logs, command output, headers, environment values, or credential-bearing URLs.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end exact-version and latest acquisition through all three host lifecycles</name>
  <read_first>
    src/smoke/host-smoke.cts
    tests/smoke/host-smoke.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <files>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts</files>
  <behavior>
    - RED: an exact request `kcoderag-nav@1.2.3` fails if the acquired installed manifest is not package `kcoderag-nav` version `1.2.3`; a latest request fails if that manifest version differs from required `expectedVersion`.
    - RED: the requested specifier, expected version, acquired installed-manifest name/version, and synthetic lifecycle tgz digest remain immutable provenance while Codex, Claude Code, and Cursor commands execute that local tgz.
    - RED: package aliases, prereleases, SemVer ranges, alternate dist-tags, URLs, directories other than the existing local tgz seam, and other package names fail before acquisition.
    - GREEN: exact and latest each use an isolated temporary project/runtime and fresh npm cache/user-config boundary; missing provenance/evidence, version drift, or NOT_RUN remains a required failure.
  </behavior>
  <action>
    Replace the exact-only public package matcher with a strict matcher that permits only `kcoderag-nav@X.Y.Z` or literal `kcoderag-nav@latest`; retain the existing contained local `.tgz` contract used by local CI. Add `expectedVersion` to the programmatic input and `--expected-version X.Y.Z` to the CLI; require it for public latest and derive/validate it from an exact request's X.Y.Z. Acquire before creating any host project. In the existing isolated acquisition root, read `node_modules/kcoderag-nav/package.json` before `writeSyntheticMcpSources`; require name `kcoderag-nav` and require that actual installed manifest version to equal the exact request or latest's `expectedVersion`. After injecting only the loopback MCP source and repacking, hash the synthetic tgz and freeze `{ requestedPackageSpec, expectedVersion, resolvedPackageName, resolvedVersion, lifecycleTarballSha256 }` before lifecycle execution. Change the injected acquisition seam to return this object rather than an unlabelled string path.

    Preserve that provenance unchanged in the top-level result and each host result even though install/update commands operate on `lifecyclePackageSpec`, the local synthetic tgz path; never substitute that path as the reported requested specifier. Do not accept arbitrary tags, ranges, URLs, aliases, or packages. Preserve the eleven-bit required-contract behavior and Cursor's Rule/skill/MCP boundary. Tests must cover exact installed-manifest mismatch, a latest acquisition resolving to the previous/next version, wrong package name, mutated provenance during synthetic execution, synthetic tgz digest mismatch, and all rejected forms, plus happy exact/latest three-host traces. Keep paths, npm/MCP credentials, and synthetic source contents outside results.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - Exact-version and literal latest package specs traverse the complete three-host required contract independently with requested/expected/acquired-manifest version and synthetic tgz digest provenance intact.
    - Exact and latest manifest mismatches, including a latest race, fail before host project writes and cannot be represented as PASS.
    - All non-approved public specifier forms fail before acquisition or project writes.
    - PASS still requires every existing lifecycle, navigation, MCP, update, uninstall, and stub-receipt bit; no unavailable evidence becomes PASS.
  </acceptance_criteria>
  <done>The release action can produce fresh exact/latest lifecycle results cryptographically and semantically bound to what npm actually supplied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Version the sanitized receipt for lane-bound dual public evidence</name>
  <read_first>
    src/maintainer/publish-receipt.cts
    tests/maintainer/publish-receipt.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
  </read_first>
  <files>src/maintainer/publish-receipt.cts, tests/maintainer/publish-receipt.test.cts</files>
  <behavior>
    - Historical schema v1 verification remains byte-compatible with the immutable `03.1-14` receipt and never upgrades or rewrites it.
    - Schema v2 binds version/tag/release SHA to registry exact/latest plus `gitHead`, Release workflow name/path/run ID/tag ref/head SHA/event, the four exact OS/Node lanes, dependent publish success, and exact/latest requested/expected/resolved host evidence.
    - Missing, extra, false, NOT_RUN, mismatched, latest-raced, wrong-workflow/ref, or secret-like values fail offline with stable codes and without echoing input.
  </behavior>
  <action>
    Extend `PublishReceipt` as a discriminated v1/v2 union. Preserve the existing v1 verifier solely for historical evidence. Define closed v2 sections whose fields cross-check rather than merely coexist: top-level package/version/tag/release SHA; registry exact requested version, exact resolved version, latest resolved version, `gitHead`, bin/engine/repository; workflow identity fixed to `Release` and `.github/workflows/release.yml`, run ID, `refs/tags/v&lt;version&gt;`, matching tag, head SHA equal to release SHA, push event, success, an exact-key four-lane Ubuntu/Windows × Node 22/24 map, and successful publish job; lifecycle `exact_version` and `latest` objects containing requested specifier, expected version, resolved installed-manifest version, synthetic lifecycle tgz digest, and exact Codex/Claude/Cursor all-true maps.

    Enforce cross-fields: exact requested/expected/resolved, latest expected/resolved, registry exact/latest, tag/ref/version, registry `gitHead`, workflow head SHA, and release SHA all converge on the same release; each lifecycle requested/resolved tuple must match its registry counterpart. Keep record/verify offline and closed at every nesting level. Tests must load the real historical v1 receipt, accept a complete v2 example, then independently corrupt requested/resolved versions, latest expected/resolved values, registry latest, gitHead, workflow name/path/tag/ref/head/event, each lane/publish result, lifecycle tgz digest, and each host field. Include a latest-race fixture where registry latest or the acquired installed manifest advances between observations and require rejection. Continue recursively rejecting raw output, environment/header fields, credential URLs, NPM token markers, and MCP/Bearer material.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:publish-receipt &amp;&amp; npm run ci:local</automated>
  </verify>
  <acceptance_criteria>
    - The historical `0.1.6` v1 receipt still verifies exactly as historical evidence and is not rewritten or treated as evidence for later fixes.
    - A v2 receipt cannot pass unless requested/expected/resolved exact/latest provenance, registry values, release SHA/`gitHead`, Release workflow identity/tag ref/head SHA, four lanes, publish job, tgz digests, and both three-host maps all agree.
    - The complete local gate passes after the final Plan 31/32 source changes, including the Node-only real tarball audit.
  </acceptance_criteria>
  <done>The next public release can be represented by one closed, offline-verifiable receipt that detects specifier substitution, workflow substitution, and latest races.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public npm specifier → smoke acquisition | A network package reference can create executable temporary package state. |
| Acquired tgz → synthetic lifecycle | A local tarball path must not erase or spoof the requested/resolved public provenance that produced it. |
| External evidence → offline receipt | Only closed, nonsecret metadata and boolean outcomes may persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-32-01 | Tampering | package-spec normalization | high | mitigate | Exact allow-list for semantic version, literal latest, and contained local tgz; bind acquired installed-manifest version before any project write. |
| T-03.1-32-02 | Spoofing | synthetic tgz lifecycle provenance | high | mitigate | Freeze requested/expected/resolved identity and synthetic tgz digest before lifecycle execution and carry it unchanged through every host result. |
| T-03.1-32-03 | Tampering | v2 release cross-fields | high | mitigate | Bind registry, workflow identity/ref/head, lanes/publish, and exact/latest lifecycle tuples to one version/tag/release SHA; reject latest races. |
| T-03.1-32-04 | Information Disclosure | receipt and smoke output | critical | mitigate | Closed schemas, recursive secret rejection, metadata-only host results, and no raw workflow/npm/MCP output persistence. |
</threat_model>

<verification>
The four evidence-contract source/test files pass focused tests and `npm run ci:local`; exact/latest requested and acquired versions remain bound through synthetic tgz execution, and v2 cross-field/latest-race fixtures fail closed. No push, tag, or npm version is created.
</verification>

<success_criteria>
- The evidence harness and receipt can represent both exact-version and latest three-host lifecycle truthfully.
- Requested specifier, expected version, acquired installed-manifest version, and synthetic tgz identity cannot diverge silently.
- Plan 32 finishes with its implementation commits and SUMMARY committed locally; Plan 33 alone owns the final source push, four-lane CI, and publication checkpoint.
</success_criteria>

## Artifacts this phase produces

- A four-path evidence-contract implementation with dual public package-specifier smoke and backward-compatible receipt v2.
- Cross-field and latest-race regressions covering acquired-manifest and Release workflow provenance.
- A committed `03.1-32-SUMMARY.md` ready to be included in Plan 33's exact pre-release SHA.
- No default-branch push, CI evidence claim, release commit, npm version, tag, or public-package mutation.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md` when done.
</output>

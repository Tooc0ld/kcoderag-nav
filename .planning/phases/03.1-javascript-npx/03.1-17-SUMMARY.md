---
phase: 03.1-javascript-npx
plan: 17
subsystem: runtime-retirement
tags: [nodejs, cjs, python-retirement, maintainer-cli, smoke]

requires:
  - phase: 03.1-15
    provides: Immutable production baseline and final Python parity receipt
  - phase: 03.1-16
    provides: Canonical and QA Python runtime retirement
  - phase: 03.1-24
    provides: Dev Python runtime and GitHub-raw update index retirement
provides:
  - Project, Cursor, update, generation, pre-commit, and smoke maintenance paths are Node-only
  - The tracked scripts package contains no Python files or package marker
  - Exact seven-path retirement evidence with all later Python tests preserved
affects: [03.1-18, 03.1-25, 03.1-19, python-retirement, maintainer-tooling]

actuals:
  tokens: 25711
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - Retire maintainer entrypoints only after their compiled Node replacement suites pass
    - Keep destructive retirement commits grouped by responsibility and recoverable through ordinary Git revert

key-files:
  created: []
  modified:
    - scripts/manage_project_install.py
    - scripts/manage_cursor_local_install.py
    - scripts/generate_plugins.py
    - scripts/update_plugin.py
    - scripts/pre_commit_generate.py
    - scripts/run_host_smoke.py
    - scripts/__init__.py

key-decisions:
  - "Plan 17 removes exactly the seven authorized scripts paths after Node replacement evidence; Python tests remain for their ordered Plan 18/25 retirement."
  - "Executable package, hook, pre-commit, and CI entrypoints must be Node-only before script deletion; historical test and documentation references remain owned by later plans."
  - "Each responsibility group has its own revertable commit, while the post-scripts audit is the monotonic completion gate."

patterns-established:
  - "Maintainer retirement: prove replacement -> scan active invocations -> delete exact tracked paths -> rerun replacement -> commit responsibility group."

requirements-completed: [JS-01, JS-02, JS-03, JS-06, JS-08]

coverage:
  - id: D1
    description: "Project install, Cursor lifecycle, and deterministic generation no longer have Python maintainer entrypoints."
    requirement: JS-03
    verification:
      - kind: integration
        ref: "npm run test:cli && npm run test:cross-host && npm run generate:check"
        status: pass
    human_judgment: false
  - id: D2
    description: "Update checking, pre-commit generation, and required three-host smoke run only through compiled Node entrypoints."
    requirement: JS-06
    verification:
      - kind: integration
        ref: "npm run test:update-check && npm run test:precommit && npm run smoke:required"
        status: pass
      - kind: integration
        ref: "npm test — 140/140 passing"
        status: pass
    human_judgment: false
  - id: D3
    description: "The scripts package contains no tracked Python file while all later test-retirement files remain untouched."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "npm run audit:retirement -- --mode post-scripts — scripts_remaining 0, tests_remaining 11"
        status: pass
      - kind: integration
        ref: "git diff --name-status bdb368d..HEAD -- scripts — exact seven deletions"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 17: Python Maintainer Script Retirement Summary

**Seven legacy Python maintainer entrypoints are gone, with installation, generation, update, pre-commit, and three-host smoke responsibilities proven on compiled Node.js.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-23T18:32:55Z
- **Completed:** 2026-08-23T18:37:27Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Removed the project installer, Cursor installer, and generator Python entrypoints after CLI 9/9, cross-host 3/3, and zero-drift generation evidence.
- Removed the Python update, pre-commit, and host-smoke entrypoints after update 9/9, pre-commit 5/5, and required Codex/Claude/Cursor smoke PASS evidence.
- Removed the empty `scripts/__init__.py` marker and reached the monotonic `post-scripts` audit state with zero script/source files and eleven later-retirement tests preserved.
- Rebuilt and ran the complete Node suite at 140/140 before close-out.

## Task Commits

Each task was committed atomically:

1. **Task 1: Retire install, Cursor, and generation Python entrypoints** - `6397719` (chore)
2. **Task 2: Retire update, pre-commit, and smoke Python entrypoints** - `ea59473` (chore)
3. **Task 3: Remove the empty Python scripts package marker** - `b48b44e` (chore)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `scripts/manage_project_install.py` - Removed the legacy project installer.
- `scripts/manage_cursor_local_install.py` - Removed the legacy Cursor local installer.
- `scripts/generate_plugins.py` - Removed the legacy deterministic generator.
- `scripts/update_plugin.py` - Removed the legacy update dispatcher.
- `scripts/pre_commit_generate.py` - Removed the legacy pre-commit helper.
- `scripts/run_host_smoke.py` - Removed the legacy Python smoke harness.
- `scripts/__init__.py` - Removed the now-empty Python package marker.

All seven changes are intentional tracked deletions. They can be recovered by reverting `6397719`, `ea59473`, and `b48b44e` in reverse order; the committed Plan 15/30 receipt chain remains the authorization provenance.

## Decisions Made

- Kept the deletion boundary exact: no `tests/*.py`, documentation, generated host assets, or sibling-repository content changed in this plan.
- Treated current npm/package/hooks/pre-commit/CI references as the executable boundary. Historical Python test imports and user-document commands remain visible for their explicitly ordered Plan 18/25 and Plan 19 retirement work.
- Required the dedicated `post-scripts` audit in addition to replacement suites and the full Node test run before declaring the scripts layer retired.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first path-containment diagnostic compared mixed slash styles and reported false negatives; the read-only check was corrected by normalizing the repository root before any deletion.
- The first importer inventory indexed a scalar PowerShell string as a character; the read-only check was corrected to materialize an array. Neither issue changed production files or the authorized deletion set.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 18 and Plan 25 can now remove the eleven preserved Python test/stub paths in their authorized groups using Node-only evidence.
- Plan 19 still owns replacement of historical Python commands in README/templates/generated documentation; those references are not package, hook, pre-commit, or CI execution paths.
- No blocker remains for the ordered retirement sequence.

## Known Stubs

None.

## Threat Flags

None. This plan removed executable file surfaces and introduced no endpoint, authentication path, schema, or new file-access boundary.

## Self-Check: PASSED

- The SUMMARY exists and classifies all three deliverables with passing automated evidence.
- Task commits `6397719`, `ea59473`, and `b48b44e` exist and together delete exactly the seven authorized tracked paths.
- Fresh Node-only evidence passed: build, aggregate 140/140, CLI 9/9, deterministic generation, required three-host smoke, and `post-scripts` retirement audit.
- `git ls-files scripts/**` is empty; the audit still reports exactly eleven Python test paths for later plans.
- Pre-existing `.planning/config.json`, `.gsd/`, milestone lock, and unrelated untracked phase directories were not staged, reverted, or modified by this plan.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

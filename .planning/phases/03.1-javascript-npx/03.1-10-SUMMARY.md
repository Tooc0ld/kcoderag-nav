---
phase: 03.1-javascript-npx
plan: 10
subsystem: generated-qa-runtime
tags: [nodejs, commonjs, generator, hooks, fail-open, qa]

requires:
  - phase: 03.1-09
    provides: Deterministic product-scoped generator with exact asset-group write sets
  - phase: 03.1-03
    provides: Compiled advisory hook CJS and self-relative Node launchers
  - phase: 03.1-08
    provides: Offline foreground update check and bounded detached worker CJS
provides:
  - Self-contained QA advisory and update runtime as directly executable CommonJS
  - Self-relative Windows and POSIX QA launchers with unconditional fail-open behavior
  - Exact five-path QA runtime-code generation evidence with registration preserved read-only
affects: [03.1-11, 03.1-23, generated-packages, qa-distribution]

actuals:
  tokens: 8741
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - Generated deployment bytes are copied from built CJS and canonical launchers by exact asset group
    - Product migration commits are bounded by observed before/after digest sets
    - Deployed launchers resolve only sibling CJS and never depend on cwd, Python, or runtime compilation

key-files:
  created:
    - kcoderag-qa/hooks/grep-nudge.cjs
    - kcoderag-qa/hooks/update-check.cjs
    - kcoderag-qa/hooks/update-worker.cjs
  modified:
    - kcoderag-qa/hooks/run_hook.cmd
    - kcoderag-qa/hooks/run_hook.sh

key-decisions:
  - "QA runtime migration is limited to the five guaranteed-changing runtime-code paths; hook registration and metadata remain read-only for Plan 23."
  - "Generated QA CJS bytes must match dist exactly, while each launcher must match its canonical plugin-src source exactly."

patterns-established:
  - "Narrow generation: invoke one product and one leaf group, then compare the observed product-tree digest delta with the exact allow-list before committing."
  - "Deployment boundary: foreground hook/update code remains network-free; only the detached bounded worker owns the fixed npm Registry request."

requirements-completed: [JS-01, JS-05, JS-06]

coverage:
  - id: D1
    description: "The QA package contains self-contained compiled advisory, update-check, and update-worker CommonJS bytes matching the build outputs."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "npm run generate -- --check --package qa --group runtime-cjs"
        status: pass
      - kind: unit
        ref: "npm run test:hook — 6/6 passing"
        status: pass
      - kind: unit
        ref: "npm run test:update-check — 9/9 passing"
        status: pass
    human_judgment: false
  - id: D2
    description: "QA Windows and POSIX launchers resolve sibling CJS without cwd, Python, or compiler dependencies and always fail open."
    requirement: JS-05
    verification:
      - kind: integration
        ref: "npm run test:launcher — 5/5 passing on Windows cmd and POSIX Git sh"
        status: pass
      - kind: integration
        ref: "npm run generate -- --check --package qa --group runtime-code"
        status: pass
    human_judgment: false
  - id: D3
    description: "Generation changed exactly three QA CJS paths and then exactly two QA launcher paths without writing registration, metadata, Dev, or Cursor assets."
    requirement: JS-06
    verification:
      - kind: other
        ref: "before/after SHA-256 product-tree comparison for both generation tasks"
        status: pass
      - kind: other
        ref: "git diff --name-only 08f8441..980c024 — exact five QA runtime-code paths"
        status: pass
    human_judgment: false

duration: 3min
completed: 2026-08-23
status: complete
---

# Phase 03.1 Plan 10: Generated QA Node Runtime Summary

**The QA distribution now carries exact built CommonJS hook/update bytes and self-relative Node launchers, with a five-path-only generated migration proven offline and fail-open.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-08-23T15:32:29Z
- **Completed:** 2026-08-23T15:35:09Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Generated the three QA CJS runtime entries byte-for-byte from `dist/hooks/` and confirmed the observed product-tree write set contained only those paths.
- Replaced both QA Python launchers with canonical self-relative Node 22 launchers that preserve stdin/stdout and silently exit zero on every unavailable/error path.
- Proved the complete QA `runtime-code` group clean while keeping `hooks.json`, metadata, Dev, and Cursor assets unwritten.

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate QA CJS runtime** - `02f09bf` (feat)
2. **Task 2: Generate QA Node launchers** - `980c024` (feat)

**Plan metadata:** committed separately after this SUMMARY is self-checked.

## Files Created/Modified

- `kcoderag-qa/hooks/grep-nudge.cjs` - Compiled bounded advisory hook and protocol entry.
- `kcoderag-qa/hooks/update-check.cjs` - Foreground-only cache/session update decision runtime.
- `kcoderag-qa/hooks/update-worker.cjs` - Detached bounded npm Registry refresh worker.
- `kcoderag-qa/hooks/run_hook.cmd` - Windows Node 22 self-relative fail-open launcher.
- `kcoderag-qa/hooks/run_hook.sh` - POSIX Node 22 self-relative fail-open launcher.

## Decisions Made

- Kept the migration strictly on the generator's two leaf groups so registration and metadata stay owned by the later repository integration gate.
- Used both generator results and full product-tree SHA-256 comparisons as the write-set proof; a clean Git diff alone would not prove transient unrelated writes never happened.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- A supplemental PowerShell `rg` command using a shell glob exited before the planned commit because PowerShell did not expand the glob for the native process. No files were staged or committed by that attempt; the scan was rerun with an explicit directory filter.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 11 can repeat the same two-group migration for Dev without coupling to the QA package.
- Plan 23 can now run the repository-wide read-only generation gate after both environment packages are migrated.
- No credential values were read, logged, or copied into evidence; connection-bearing configuration remained untouched.

## Known Stubs

None. The `TODO|FIXME|XXX|HACK` text in `grep-nudge.cjs` is an intentional local-search silence classifier, not placeholder implementation.

## Self-Check: PASSED

- All five generated QA runtime-code files and this SUMMARY exist.
- Task commits `02f09bf` and `980c024` exist and contain exactly the five planned QA paths with no deletions.
- Fresh verification passed: runtime-code drift check, Hook 6/6, launcher 5/5, update-check 9/9, and exact dependency audit.
- Registration, metadata, Dev, and Cursor trees have no uncommitted changes from this plan.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-23*

---
phase: 03.1-javascript-npx
verified: "2026-08-24T06:23:21Z"
status: gaps_found
score: "4/8 must-haves verified"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "JS-01/JS-06: the published package contains no Python-running code and the pack gate proves that invariant"
    status: failed
    reason: "The exact current pack and the v0.1.6 release both include dist/maintainer/retirement-audit.cjs; its reachable --run-parity branch spawns four Python suites. The pack audit rejects Python/TypeScript filename extensions but does not reject Python execution embedded in CJS, so its passing result is not evidence for this contract."
    artifacts:
      - path: "package.json"
        issue: "The publish allow-list includes dist/maintainer/retirement-audit.cjs; v0.1.6 used the even broader dist/ entry."
      - path: "src/maintainer/retirement-audit.cts"
        issue: "runParity invokes the python executable at lines 614-617 and main exposes --run-parity."
      - path: "src/maintainer/pack-audit.cts"
        issue: "forbiddenArchivePath only checks .py/.pyc/.ts/.cts path suffixes; archive CJS contents are not checked for Python execution."
    missing:
      - "Remove the retired parity runner from the shipped runtime (or remove retirement-audit.cjs from the package), while keeping any historical receipt verifier that is still required Node-only."
      - "Add a negative tarball test that injects a Python subprocess/user runtime dependency inside a .cjs file and must fail."
  - truth: "JS-04: the public @latest package is the reviewed safe ownership/drift/rollback implementation"
    status: failed
    reason: "npm latest and tag v0.1.6 resolve to 5ff0a978, while the critical recovery cleanup, Cursor migration rollback, shared JSON provenance, filesystem identity, and related regression fixes are later local commits. Users installing @latest do not receive the safety implementation verified at current HEAD."
    artifacts:
      - path: "src/core/transaction.cts"
        issue: "Current identity-bound recovery cleanup fixes are after v0.1.6."
      - path: "src/hosts/cursor.cts"
        issue: "Current verified-backup rollback fix is after v0.1.6."
      - path: "src/hosts/codex.cts"
        issue: "Current shared-config provenance and mutation-fencing fixes are after v0.1.6."
      - path: "src/hosts/claude.cts"
        issue: "Current shared-config provenance and mutation-fencing fixes are after v0.1.6."
    missing:
      - "Create a new version from the reviewed fixed source, rerun all required gates, publish the matching new tag, and verify npm latest/exact-version lifecycle evidence against that commit."
  - truth: "JS-07: publication is gated by Windows/Linux x Node 22/24 before npm publish"
    status: failed
    reason: "The current local release workflow now has a four-lane required-contracts matrix and publish needs it, but the only public v0.1.6 release workflow at 5ff0a978 had one Ubuntu/Node 24 publish job with no matrix dependency. Current HEAD is 23 commits ahead of origin/master and has no GitHub Actions run, so the repaired gate has not protected a publication or run on the final code."
    artifacts:
      - path: ".github/workflows/release.yml"
        issue: "Correct in current local source, but not exercised by the existing public release and not run for current HEAD."
      - path: ".planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json"
        issue: "Valid evidence for historical v0.1.6 publication only; it cannot attest later local fixes or the repaired matrix gate."
    missing:
      - "Run the final source through the Windows/Linux x Node 22/24 required release matrix and publish only after all lanes succeed."
      - "Record a new matching-tag workflow/public registry receipt tied to the final reviewed commit."
---

# Phase 03.1: JavaScript 与 npx 安装运行时迁移 Verification Report

**Phase Goal:** 用户可在 Node.js 22+ 环境中通过统一 npx CLI，将无 Python 运行时依赖的 KCodeRag Nav 项目集成安全地安装、诊断、更新或卸载到 Codex、Claude Code 与 Cursor。

**Verified:** 2026-08-24T06:23:21Z  
**Status:** `gaps_found`  
**Re-verification:** No — initial verification; no prior `03.1-VERIFICATION.md` existed.

## Verdict

The current checkout contains substantial, well-tested Node/CJS implementations and passes its complete local gate, including 170 tests and all three required loopback host smokes. That is not enough to achieve the roadmap goal.

Three observable facts block the user outcome:

1. The package still ships a reachable CJS maintainer branch that invokes Python, while the pack audit only rejects forbidden filename extensions.
2. Public `kcoderag-nav@latest` is `0.1.6` at commit `5ff0a978`; critical lifecycle-safety fixes exist only in later local commits through current HEAD `2e49ee3`.
3. The public `v0.1.6` release was not gated by the required Windows/Linux × Node 22/24 matrix. The repaired workflow is local and has not run for current HEAD.

The published `0.1.6` receipt remains valid historical evidence. It is not evidence that the later fixes have already been published.

## Goal Achievement

### Observable Truths

The 71 PLAN frontmatter truth lines were deduplicated into the eight non-reducing requirement/goal contracts below. All 27 plans, 59 declared artifact entries, and 39 declared key-link entries were traced through these contracts.

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | JS-01: maintained TypeScript builds to directly runnable Node 22+ CJS, with no production dependencies, runtime compiler, or Python-running code in the package. | ✗ FAILED | Node/CJS, engine, and zero production dependency checks pass; no tracked `.py/.pyc/.pyo` or packed `.ts/.cts` paths exist. However `package.json` ships `dist/maintainer/retirement-audit.cjs`, whose `--run-parity` branch invokes `python` four times. |
| 2 | JS-02: a public `kcoderag-nav` package exposes one five-command npx CLI and the root manifest is the version source. | ✓ VERIFIED | Fresh npm metadata reports `latest=0.1.6`, `engines.node=>=22`, and bin `dist/bin/kcoderag-nav.cjs`; `COMMANDS` is exactly install/status/doctor/update/uninstall. This verifies availability and command shape, not freshness of the safety fixes. |
| 3 | JS-03: Codex, Claude Code, and Cursor use native project adapters; one invocation mutates only one selected host and cross-host installs coexist. | ✓ VERIFIED | Registry and adapter implementations are substantive; full tests passed `registry exposes exactly three`, `cross-host coexist`, and `same-host environment conflict` behaviors. Cursor uses Rule/skill/MCP rather than a false hook equivalent. |
| 4 | JS-04: legacy migration and every lifecycle operation enforce ownership, drift preflight, identity fencing, and complete one-host rollback in the public user path. | ✗ FAILED | Current source and regression tests verify the transitions. Public `@latest` is the earlier `5ff0a978` implementation and excludes the later critical recovery, Cursor backup, JSON provenance, and identity-fencing fixes. |
| 5 | JS-05: hook behavior is advisory/fail-open; foreground execution is offline and schedules only a bounded detached npm latest refresh with a 24-hour cache. | ✓ VERIFIED | `CACHE_TTL_MS` is 24h; foreground source has no network client; worker request/body/cache are bounded; malformed, clock, permission, spawn, renewal-token, and network failures are tested fail-open. |
| 6 | JS-06: generator/pre-commit/pack gates prove deterministic, version-consistent, self-contained, Python-free products. | ✗ FAILED | Generation and exact archive equality pass, but pack evidence cannot prove Python-free CJS: the real 60-entry pack contains the Python-spawning retirement audit and the negative matrix does not cover that content pattern. |
| 7 | JS-07: Windows/Linux × Node 22/24 are required before a matching tag can publish; ordinary pushes only test. | ✗ FAILED | Current YAML and tests encode the matrix and `publish.needs`. The public `v0.1.6` workflow did not, and current HEAD has no CI/release run. |
| 8 | JS-08: Node tests and honest loopback smoke cover all three host lifecycles, hook/Rule, MCP evidence, offline behavior, and synchronized user/authoritative docs. | ✓ VERIFIED | Fresh `npm run ci:local` passed 170/170 tests and required smoke PASS for Codex/Claude/Cursor with all evidence bits. User docs and the sole sibling guide check/receipt/summary all pass. Authenticated real-host evidence is explicitly Phase 6. |

**Score:** 4/8 truths verified (0 present-but-behavior-unverified).

### PLAN Must-Have Resolution

This table resolves every PLAN frontmatter group against current code/history. `PARTIAL (BLOCKER)` means at least one must-have in that plan fails even though other artifacts are substantive.

| Plan | Frontmatter contract resolution | Status |
|---|---|---|
| 03.1-01 | Exact dependency/engine/CJS and current leaf allow-list exist; the no-Python tarball claim is false because shipped CJS can spawn Python. The later deletion of the obsolete tracer is a reviewed supersession, not a missing runtime artifact. | PARTIAL (BLOCKER) |
| 03.1-02 | Host-neutral target/state/runtime contracts, transaction-only writes, state-last commit, and rollback are substantive and behavior-tested. | VERIFIED (current source) |
| 03.1-03 | CJS advisory hook and Windows/POSIX launchers fail open for malformed, oversized, old/missing runtime, and launcher errors. | VERIFIED |
| 03.1-04 | Claude/Cursor native project roots, independent Cursor migration authority, drift refusal, rollback, host-local mutex, and cross-host coexistence are current and tested. | VERIFIED (current source) |
| 03.1-05 | Pre-commit and four-lane CI contracts exist; the pack audit does not reject Python execution embedded in CJS. | PARTIAL (BLOCKER) |
| 03.1-06 | Required smoke demands every evidence bit and makes FAIL/NOT_RUN nonzero; optional live preserves NOT_RUN honesty. | VERIFIED |
| 03.1-07 | Codex narrow ownership, shared TOML preservation, legacy-state migration, and write-before-drift refusal are current and tested. | VERIFIED (current source) |
| 03.1-08 | Foreground zero-network, detached one-per-session refresh, 24h cache, bounded markers, and fail-open behavior are current and tested. | VERIFIED (current source) |
| 03.1-09 | Generator uses root package version/canonical templates, exact product/group selections, deterministic check mode, rollback, and opaque credential handling. | VERIFIED |
| 03.1-10 | QA CJS/launchers are generated and repository generation checks byte equality. | VERIFIED |
| 03.1-11 | Dev CJS/launchers are generated and repository generation checks byte equality. | VERIFIED |
| 03.1-13 | Release helper uses explicit version/tag ownership, dirty-tree refusal, gates, and sanitized receipt validation; current workflow source is correctly wired. | VERIFIED (current source) |
| 03.1-14 | Human-authorized `v0.1.6` publication, exact run discovery, registry/npx evidence, and offline receipt verification occurred at `5ff0a978`. This is historical evidence only and does not publish later fixes. | VERIFIED (historical only) |
| 03.1-15 | Monotonic retirement modes, immutable baseline, canonical receipt/path rules, cache authority, and zero-write negative cleanup fixtures are implemented/tested. | VERIFIED |
| 03.1-16 | Receipt-first deletion sequencing and exact plugin/QA Python retirement are visible in history; no Python source/cache remains tracked. | VERIFIED |
| 03.1-17 | Replaced Python installer/generator/update/pre-commit/smoke source entrypoints are retired after Node equivalents. | VERIFIED |
| 03.1-18 | Mapped Python test groups were retired only after their Node suites; final state contains their Node coverage. | VERIFIED |
| 03.1-19 | npx/Node22+ docs and scoped post audit pass, but the claimed shipped-runtime Python exclusion is false because the audit does not scan shipped `dist/maintainer` CJS behavior. | PARTIAL (BLOCKER) |
| 03.1-20 | Root marketplace entrypoints remain retired; project/agent/roadmap language owns npx integration and preserves later Phase 6/8 boundaries. | VERIFIED |
| 03.1-21 | The sibling guide-only receipt, unchanged unrelated status digest, secret scan, and dual HEAD hashes verify. | VERIFIED |
| 03.1-22 | Compiled scoped docs checker and sibling evidence checker exist, are registered, and pass current scoped checks. | VERIFIED |
| 03.1-23 | Repository generation check observes no drift across all three product trees. | VERIFIED |
| 03.1-24 | Dev Python/runtime and GitHub-raw update index are retired; post-source/final retirement scans pass their declared scope. | VERIFIED |
| 03.1-25 | Remaining Python tests/stub/package marker are absent and Node replacements pass. | VERIFIED |
| 03.1-26 | CLI selects one adapter, keeps OpenCode unregistered, and uses shared confirmation/runtime/JSON core behavior. | VERIFIED |
| 03.1-29 | Root-only `node_modules`, `dist`, and `dist-tests` ignore policy is present without hiding product/source/test/planning paths. | VERIFIED |
| 03.1-30 | Exact cleanup authorization/execution receipts and isolated negative/success tests prove the 26-file/5-root cleanup without recursive deletion. | VERIFIED |

No PLAN defines `must_haves.prohibitions`; no prohibition disposition is required.

## Required Artifacts

| Artifact group | Expected | Status | Details |
|---|---|---|---|
| `package.json`, lock, tsconfigs, `dist/**` | Node 22+ CJS, exact bin/version/dependency/pack policy | ⚠ PARTIAL | Current manifest has exact leaf files, `>=22`, zero production dependencies, and one bin. It still ships Python-spawning retirement CJS; released v0.1.6 used broad `dist/`. |
| `src/bin/kcoderag-nav.cts`, `src/cli/commands.cts` | Five-command composition root | ✓ VERIFIED | Imports host registry and transaction, selects one adapter, status/doctor remain read-only, mutation commands render one desired state. |
| `src/core/**` | Path/state/ownership/atomic transaction | ✓ VERIFIED (current) | Substantive identity/digest/symlink/special-file checks, complete preflight, state-last commit, rollback/recovery. Current regression tests pass. |
| `src/hosts/{codex,claude,cursor}.cts` | Native project adapters and legacy migrations | ✓ VERIFIED (current) | Native roots and lossless shared-config behavior are wired through registry/controller. Current regression tests pass. |
| `src/hooks/**`, generated launchers | Advisory hook and detached update awareness | ✓ VERIFIED | Hook/update paths are wired and behavior-tested; generated QA/Dev assets match. |
| `src/generator/index.cts`, `plugin-src/**`, generated products | Deterministic self-contained products | ✓ VERIFIED | Check mode reports no changed/written paths; tests cover exact group writes and rollback. |
| `src/maintainer/pack-audit.cts` | Prove exact tarball and no Python/runtime compiler | ✗ INSUFFICIENT | Real pack equality works, but the negative check is path-extension based and misses a Python subprocess in packed CJS. |
| `.github/workflows/ci.yml`, `.github/workflows/release.yml` | Four supported lanes; publish after required gates | ⚠ CURRENT-ONLY | Current static contract passes. It has not run for current HEAD and did not protect v0.1.6. |
| `src/smoke/**` | Honest three-host lifecycle/MCP evidence | ✓ VERIFIED | Fresh required smoke reports all three hosts PASS and every evidence field true. |
| README/templates/generated docs and sibling receipt | One npx narrative and sole authoritative sibling guide | ✓ VERIFIED | Six user docs and the sibling guide pass scoped checks; receipt and summary hashes verify. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| npm bin | CLI command registry | `dist/bin/kcoderag-nav.cjs` → `runCli`/`COMMANDS` | ✓ WIRED | Fresh npm metadata and source import/dispatch agree. |
| CLI | selected host adapter | `getHostAdapter(host)` | ✓ WIRED | One adapter is resolved before status/render. |
| Adapter desired state | project filesystem | single `applyTransaction` boundary | ✓ WIRED (current) | Current transition tests exercise commit, failure, rollback, recovery, and host isolation. |
| Codex/Claude launchers | hook/update CJS | sibling launcher resolution | ✓ WIRED | Launcher tests prove protocol output/silence and exit-0 failures. |
| Foreground update check | detached worker | `spawn(process.execPath, ..., {detached:true})` | ✓ WIRED | Foreground makes no network call; worker owns bounded Registry request/cache write. |
| package version/templates | generator | deterministic render/check | ✓ WIRED | Current repository generation check passes with no writes. |
| package `files` | pack audit | exact archive path comparison | ⚠ PARTIAL | Exact paths are compared, but packed CJS Python execution is not prohibited. |
| matching tag | required lanes | `publish.needs: required-contracts` | ⚠ CURRENT-ONLY | Wired in local source; absent from the workflow that published v0.1.6. |
| smoke package acquisition | CLI → native config → loopback MCP | temporary package/project and stub receipts | ✓ WIRED | Required smoke exercises install/status/update/uninstall and MCP initialize/list/call per host. |

## Data-Flow Trace (Level 4)

| Artifact | Data | Source | Sink | Status |
|---|---|---|---|---|
| CLI/controller | command, host, environment, target | argv/prompt | adapter render → transaction | ✓ FLOWING |
| Host adapters | managed config bytes and digests | existing project files + selected generated package assets | desired state entries | ✓ FLOWING |
| Transaction | originals, identities, expected digests | complete preflight | staged replacements/state-last or full rollback | ✓ FLOWING (current tests) |
| Hook | bounded host payload | stdin | advisory protocol JSON or empty stdout | ✓ FLOWING |
| Update awareness | installed version/cache/session claim | bounded state/cache files | advisory text + detached worker | ✓ FLOWING |
| Generator/pack | root version and canonical templates | `package.json`/`plugin-src`/compiled CJS | three generated products/tarball | ⚠ HOLLOW GUARD: real bytes flow, but the no-Python invariant is not enforced |
| Release | tag/SHA/version and gate results | git tag/workflow | npm public package/receipt | ✗ DISCONNECTED FROM FINAL SOURCE: public sink remains old commit |
| Smoke | acquired package and loopback receipts | temporary pack/stub MCP | per-host PASS/FAIL/NOT_RUN record | ✓ FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Complete final local gate | `npm run ci:local` | Windows, Node `v24.14.0`; build/dependency audit PASS; 170 tests PASS, 0 fail/skip/todo; generation check PASS; pack audit PASS (60 entries); Codex/Claude/Cursor required smoke all PASS | ✓ PASS for current checkout |
| Three-host lifecycle/MCP evidence | `npm run smoke:required` (inside local gate) | Each host has packageAcquired/install/status/toolRegistration/navigation/MCP initialize/list/call/update/uninstall/stubReceipt=true | ✓ PASS |
| Public package metadata | `npm view kcoderag-nav@latest ... --json` | latest/version `0.1.6`, Node `>=22`, expected bin, gitHead `5ff0a978...` | ✓ PASS metadata; ✗ final-source freshness |
| Public release run | `gh run view 32682791252 --json ...` | completed/success, `v0.1.6`, head `5ff0a978...` | ✓ PASS historical release; ✗ current matrix/fixes |
| Current HEAD CI availability | `gh run list --commit 2e49ee3...` | `[]`; branch is 23 commits ahead of `origin/master` | ✗ FAIL current cross-platform/release evidence |
| Current real pack content | `npm pack --dry-run --json --ignore-scripts` plus CJS inspection | 60 entries include `dist/maintainer/retirement-audit.cjs`; compiled lines invoke Python and expose `--run-parity` | ✗ FAIL Node-only package invariant |
| Retirement path scan | `npm run audit:retirement -- --mode post` | PASS with zero Python source/script/test paths | ✓ PASS declared scope; insufficient for packed CJS behavior |
| User/sibling documentation | scoped `docs:check`, sibling receipt and summary verification | user docs 6/6, guide 1/1, receipt and summary PASS | ✓ PASS |
| Disabled test scan | search `test.skip/test.todo/xit/xdescribe/xtest` | none | ✓ PASS |

The full workspace test command was run once. No additional full-suite filtering was used.

## Probe Execution

No conventional `scripts/**/tests/probe-*.sh` files or phase-declared probe script references exist (`0`/`0`). Step 7c is not applicable; the executable verification paths are the CLI tests, pack audit, release/static contract checks, and required smoke above.

## Requirements Coverage

Every requirement ID declared across the 27 PLAN frontmatters appears in `REQUIREMENTS.md`; Phase 03.1 has no orphaned additional requirement. All JS-01..JS-08 are accounted for.

| Requirement | Source plans | Status | Direct evidence |
|---|---|---|---|
| JS-01 | 01,03,05,06,09,10,11,15,16,17,18,19,23,24,25,29,30 | ✗ BLOCKED | Node/CJS migration is real, but packed reachable CJS can spawn Python. |
| JS-02 | 01,02,13,14,17,19,20,21,26,29 | ✓ SATISFIED | Public package/bin/version metadata and exact five-command source wiring exist. Public freshness is separately a JS-04/JS-07 safety gap. |
| JS-03 | 01,04,07,17,19,20,21,26 | ✓ SATISFIED | Exact three-adapter registry, native host paths, one-host dispatch, cross-host coexistence tests. |
| JS-04 | 01,02,04,07,15,18,26 | ✗ BLOCKED | Current tests pass the required transitions, but public `@latest` predates critical safety fixes. |
| JS-05 | 03,08,10,11,15,16,18,24,30 | ✓ SATISFIED (current) | Advisory/fail-open and update cache/session/worker tests pass, including renewal and concurrency paths. |
| JS-06 | 05,09,10,11,15,16,17,18,23,24,25 | ✗ BLOCKED | Determinism/version/self-contained checks pass, but the pack gate cannot prove the declared no-Python CJS invariant. |
| JS-07 | 05,13,14,20,29 | ✗ BLOCKED | Current workflow contract is correct but unexecuted; v0.1.6 published through an older single Ubuntu/Node24 job. |
| JS-08 | 06,15,17,19,20,21,22,25 | ✓ SATISFIED | Fresh required loopback smoke, honest tri-state tests, user-doc checks, and sole sibling-guide evidence pass. |

## Decision Coverage

The GSD decision-coverage query reports `18/18` decisions honored with no non-honored items. This is advisory and does not override the observable package/publication failures.

## Test Quality Audit

| Dimension | Assessment |
|---|---|
| Assertion depth | Lifecycle/transaction/update tests assert state transitions, preserved unrelated bytes, zero-write refusals, rollback restoration, bounded claims, and emitted protocol; generator/pack/release tests assert exact values and manifests. |
| Disabled coverage | No skip/todo/x-prefixed tests found; full run reports 0 skipped/todo. |
| Circularity | Fixtures supply external inputs/configs and expected policy; the production code does not generate its own expected baseline for the core lifecycle assertions. |
| Confirmation-bias check | The most misleading passing evidence is `pack:audit`: it proves exact paths and selected byte sentinels, but its Python fixture is a forbidden path, not Python execution embedded in allowed CJS. |
| Public-vs-local seam | The 170 passing tests exercise current local source. They cannot be projected backward onto public `5ff0a978`; git ancestry and npm `gitHead` disprove that projection. |
| Platform seam | Current final source was freshly run on Windows/Node24 only. No Node22/Linux/Windows matrix run exists for current HEAD; this is part of the JS-07 blocker. |

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `src/maintainer/retirement-audit.cts` | 614-617 | Shipped reachable `python` subprocess calls | 🛑 BLOCKER | Contradicts JS-01 and the roadmap's no-Python runtime dependency; also invalidates JS-06 proof. |
| `src/maintainer/pack-audit.cts` | 176-184 | Extension-only forbidden archive check | 🛑 BLOCKER | Allows Python execution inside an allowed `.cjs` artifact. |
| `tests/maintainer/pack-audit.test.cts` | 103,121 | Negative Python case covers path extension, while compiled byte scan covers credentials/placeholders only | ⚠ WARNING | Passing tests overstate the no-Python property. |
| `src/hooks/grep-nudge.cts` | 36 | Literal `TODO|FIXME|XXX|HACK` classifier regex | ℹ INFO | Intentional search classification, not an unresolved debt-marker comment. |

No unreferenced `TBD`, `FIXME`, or `XXX` debt-marker comment was found in phase production/test files. Empty-return matches were error/parse defaults with populated real paths, not user-visible stubs.

## Human Verification Required

None for the Phase 03.1 contract. All in-scope CLI/transaction/hook/generator/pack/loopback behaviors have automated paths; the blockers are directly observable, not uncertain.

Authenticated actual-host compatibility is intentionally deferred to Phase 6 and must not be converted into a Phase 03.1 human-check warning. Visual UX is not a material output of this CLI/tooling phase.

## Later-Phase Boundary Check

| Later phase | Explicit boundary preserved | Effect on this verdict |
|---|---|---|
| Phase 4 | deployed-project/nested-cwd and user-level duplicate-source diagnostics | Not a Phase 03.1 gap. |
| Phase 5 | hook precision, common Lua handlers, honest index-aware routing | Not a Phase 03.1 gap. |
| Phase 6 | authenticated Codex/Claude/Cursor evidence from an exact public package | Correctly deferred; loopback evidence is sufficient for JS-08 here. |
| Phase 7 | GSD runtime/global-hook hygiene | Out of scope. |
| Phase 8 | production identity/HTTPS/credential rotation and ongoing compatibility/release hardening | Out of scope, but it does not defer JS-07: Phase 03.1 itself explicitly requires the first safe matrix-gated public npx release. |
| OpenCode | adapter extension seam only | Explicitly deferred and not registered. |

None of the three identified gaps is moved to `deferred`: each directly contradicts Phase 03.1's own goal or JS-01/04/06/07 contract.

## Gaps Summary

The implementation is close but the delivered user outcome is not yet true. The immediate closure sequence is:

1. Make the tarball genuinely Node-only by removing the Python parity execution path from shipped artifacts and extend pack tests/audit to catch Python subprocess behavior inside CJS.
2. Cut a new version from the reviewed fixed source; do not overwrite or reinterpret `0.1.6`.
3. Run the new matching tag through the repaired Windows/Linux × Node 22/24 required matrix, publish only after all lanes pass, and capture registry/exact-version three-host lifecycle evidence tied to that final commit.

Until then, local task completion must not be reported as public phase-goal achievement.

---

_Verified: 2026-08-24T06:23:21Z_  
_Verifier: the agent (gsd-verifier)_

---
phase: 03.1-javascript-npx
verified: 2026-08-24T14:12:26Z
status: passed
score: "8/8 must-haves verified"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "4/8"
  gaps_closed:
    - "Public npm inventory is now a closed 59-member allow-list and excludes the repository-only retirement auditor and every Python/TypeScript/source/test/planning path."
    - "Public exact/latest 0.1.8 contain the final clean-reviewed ownership, provenance, integrity, rollback, and anti-TOCTOU fixes."
    - "The exact pre-release and release SHAs passed the required Windows/Linux x Node.js 22/24 matrices before publication."
  gaps_remaining: []
  regressions: []
decision_coverage:
  honored: 18
  total: 18
  not_honored: []
---

# Phase 03.1: JavaScript 与 npx 安装运行时迁移 Verification Report

**Phase Goal:** 用户可在 Node.js 22+ 环境中通过统一 npx CLI，将无 Python 运行时依赖的 KCodeRag Nav 项目集成安全地安装、诊断、更新或卸载到 Codex、Claude Code 与 Cursor。
**Verified:** 2026-08-24T14:12:26Z
**Status:** passed
**Re-verification:** Yes — the prior three implementation/release gaps and two subsequent provenance gaps were independently rechecked against the current code, public package, immutable receipts, git refs, and GitHub Actions.

## Verdict

The phase goal is achieved. The public `kcoderag-nav@0.1.8` package is the reviewed Node.js 22+ CJS implementation, exposes the five-command npx lifecycle, installs one selected project host transactionally, and contains neither Python/runtime TypeScript nor production dependencies. Exact and latest npm identities converge on the same 59-member artifact, and both required four-lane GitHub Actions matrices passed at the SHAs that were actually released. No human-only verification item remains because the host lifecycle, rollback, hook fail-open behavior, update worker, public acquisition, integrity, and anti-TOCTOU invariants are exercised by automated tests or independently observed smoke runs.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | **JS-01:** Maintainer TypeScript builds to directly runnable CJS, while the public package contains no Python runtime, runtime compiler, source, tests, or planning material. | ✓ VERIFIED | Fresh `ci:local` passed 197/197; post-retirement audit reported zero remaining Python source/script/test paths; direct exact/latest tar inspection found 59 allowed members, zero forbidden extensions/roots, no `src`, tests, `.planning`, or retirement auditor, and zero production dependencies. |
| 2 | **JS-02:** One public `kcoderag-nav` npx CLI provides install/status/doctor/update/uninstall and root `package.json` is the version source. | ✓ VERIFIED | CLI source exposes exactly the five commands; generator reads the root package version; all product manifests are 0.1.8; a public exact-version `npx` doctor invocation from an unrelated empty project returned valid read-only JSON. |
| 3 | **JS-03:** Codex, Claude Code, and Cursor use host-native project configuration behind extensible adapters, with one selected host per invocation. | ✓ VERIFIED | Adapter registry contains exactly the three supported hosts. Command orchestration selects one adapter and one transaction. Fresh required-contract smoke passed all 11 evidence fields for each host. Codex/Claude emit PreToolUse configuration; Cursor emits Rule, skill, and MCP configuration without claiming hook equivalence. |
| 4 | **JS-04:** Legacy migration and all mutations enforce managed ownership, pre-write validation, drift refusal, and single-host atomic rollback. | ✓ VERIFIED | Target/state/transaction code rejects escapes, symlinks, special files, ambiguity, and digest drift; commits state last and restores the complete selected-host preimage. Lifecycle, rollback, ownership, and migration tests passed in the 197-test run, including QA/Dev same-host conflict and cross-host coexistence. |
| 5 | **JS-05:** CJS hooks remain advisory/fail-open and installed operation is offline; update refresh is detached, npm-latest-only, cached for 24 hours, and bounded. | ✓ VERIFIED | Hook and launcher boundaries return success with empty output on malformed input/runtime failure. Foreground update code reads only bounded local cache and schedules a detached worker; the worker uses fixed Registry HTTPS, bounded response/time, atomic cache replacement, and swallows failures. Hook/update/offline tests passed. |
| 6 | **JS-06:** Generator, pre-commit, and pack gates prove determinism, version alignment, self-containment, and absence of Python/placeholders. | ✓ VERIFIED | Fresh generate check selected 31 paths with no changed/written files; pack audit passed with exactly 59 entries. Negative pack tests reject the maintainer-only retirement auditor, undeclared members, Python/runtime compiler/source/test/planning/dependency paths, placeholders, and credential-like bytes. |
| 7 | **JS-07:** CI covers Windows/Linux and Node.js 22/24; only a matching `vX.Y.Z` tag publishes, while ordinary master pushes test only. | ✓ VERIFIED | Ordinary CI run `32728244449` succeeded at exact pre-release SHA `26820a8558b984285d1a642db8e5381d243c3aa5` in all four required lanes. Release run `32731317466` succeeded on `v0.1.8` at release SHA `beb5bfa08ceb6e3677b6d623bc5eab4d09f2dc8a` in all four lanes, then its dependent matching-tag publish job succeeded. Workflow wiring makes publish depend on required contracts and validates tag/package version. |
| 8 | **JS-08:** Node tests and loopback smoke cover three-host lifecycle, hook/Rule, MCP evidence, offline behavior, and honest evidence; README and the single authoritative guide describe the npx flow. | ✓ VERIFIED | Fresh local suite passed 197/197 and local three-host smoke passed 11/11 evidence per host. Public exact and latest hardened smoke independently passed all hosts with complete 11-field evidence. Docs gates passed; this repository has no guide copy, and the tracked authoritative `D:/AIProgram/KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md` covers Node 22, all five commands, host/environment semantics, Cursor's boundary, offline update behavior, and no-Python/no-checkout installation. |

**Score:** 8/8 truths verified (0 present but behavior-unverified)

## Closed Gap Audit

| Prior/candidate gap | Current evidence | Result |
|---|---|---|
| Public package contained a Python-running or maintainer-only artifact; pack proof was too weak. | Official exact/latest tarballs each contain exactly the closed 59-member inventory. `dist/maintainer/retirement-audit.cjs` and all Python, `.cts`/`.ts`, source, test, planning, and runtime-compiler paths are absent. The manifest has no production dependency and pack negative tests exercise both allow-list drift directions. | CLOSED |
| The reviewed ownership/provenance/rollback fixes were not public. | npm exact and latest are 0.1.8 with `gitHead` at release SHA `beb5bfa08ceb6e3677b6d623bc5eab4d09f2dc8a`. The hardened fix commits are ancestors of that SHA; its pre-release tree contains the final `clean` review and `all_fixed` fix audit. There is no source/package/workflow drift from the release tree to the current checkout. | CLOSED |
| No four-lane release proof existed. | CI run `32728244449` is green for Windows/Linux on Node 22/24 at the exact pre-release SHA. Release run `32731317466` is green for the same four required lanes at `v0.1.8`/the exact release SHA, followed by a green dependent publish job. | CLOSED |
| Registry provenance could differ between exact, latest, receipt, or released source. | Independent official Registry queries and downloads show exact/latest version, canonical URL, SRI, SHA-256, SHA-512, repository, bin, engine, inventory, and `gitHead` identity are equal and match the immutable schema-v3 receipt. An immediate latest re-query remained stable. | CLOSED |
| A smoke could install one tarball and execute substituted bytes. | Hardened smoke validates canonical URL, SRI and both hashes, then performs exclusive content-addressed checks before and after every lifecycle command. Replacement-tarball tests make Codex, Claude Code, and Cursor all fail as required. Exact/latest public smokes each returned complete 11-field host evidence. | CLOSED |

## Public Artifact and Release Provenance

| Check | Verified result |
|---|---|
| npm exact and latest | Both resolve to `0.1.8`, the same release `gitHead`, and the same canonical official Registry tarball identity. |
| Published inventory | Exactly 59 members: the 57 declared package members plus npm's mandatory manifest and README; no unexpected member. |
| Runtime contract | CommonJS, Node.js `>=22`, bin `dist/bin/kcoderag-nav.cjs`, zero production dependencies, and no runtime compile step. |
| Release identity | Local and remote `v0.1.8` both resolve to `beb5bfa08ceb6e3677b6d623bc5eab4d09f2dc8a`; the release commit changes only the six version-bearing package/product files plus the lockfile. |
| Review ancestry | Final hardened fixes, `status: clean` review, and `status: all_fixed` fix report precede the release SHA. |
| Ordinary CI | Run `32728244449`, push event, exact pre-release SHA, four required lanes successful. |
| Release CI/publish | Run `32731317466`, tag event for `v0.1.8`, exact release SHA, four required lanes and dependent publish successful. |
| Receipt v3 | Plan 34 receipt independently verifies; Registry fields and all three hosts' 11-item lifecycle evidence match the public artifact. |
| Receipt v2 compatibility | Plan 33's 0.1.7 receipt independently verifies; its current/historical git blob identity and local/remote `v0.1.7` tag remain unchanged. |
| Latest race check | A fresh second Registry lookup retained 0.1.8 and the same `gitHead`; exact/latest identities converge. |

The verified public integrity values were recomputed from downloaded official Registry bytes and matched the receipt. They are intentionally not reproduced here because the equality and verifier result, rather than a copied value, is the evidence-bearing claim.

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `package.json` / `package-lock.json` | Single version source, Node 22 engine, bin, dependency and pack contract | ✓ VERIFIED | Version 0.1.8; exact public bin/engine/repository metadata matches; no runtime dependency. |
| `src/bin/`, `src/cli/`, `src/core/` | Five-command policy, exact project target, managed state and one transaction boundary | ✓ VERIFIED | Substantive and wired from public bin through one selected adapter to `applyTransaction`; read-only commands remain non-mutating. |
| `src/hosts/` | Codex/Claude/Cursor native desired state and ownership | ✓ VERIFIED | Three adapters are registered, rendered, installed and exercised independently. |
| `src/hooks/` and generated launchers | Advisory/fail-open CJS hook plus offline foreground/detached update worker | ✓ VERIFIED | Wired into Codex/Claude generated products; Cursor intentionally uses Rule/skill/MCP. |
| `src/generator/`, `plugin-src/`, generated product trees | Deterministic version-aligned self-contained products | ✓ VERIFIED | Fresh generator check has zero drift across 31 selected paths; generated manifests are 0.1.8. |
| `src/maintainer/` and `.githooks/pre-commit` | Dependency, generation, docs, retirement, pack, and release gates | ✓ VERIFIED | Gates are runnable and exercised by fresh local CI and the successful remote matrices. Retirement auditor remains repository-only. |
| `src/smoke/` and tests | Real package acquisition, lifecycle/MCP evidence, integrity and anti-TOCTOU proof | ✓ VERIFIED | Local and public exact/latest smoke pass all three hosts; adversarial substitution tests fail closed. |
| `.github/workflows/ci.yml` / `release.yml` | Four required platform/runtime lanes and tag-gated dependent publication | ✓ VERIFIED | Live run metadata and workflow source agree on event, SHA, matrix, dependency and publication policy. |
| Plan 33 v2 / Plan 34 v3 receipts | Immutable historical and current release evidence | ✓ VERIFIED | Both receipt verifiers exit 0; the v2 artifact/tag remains unchanged and the v3 artifact matches current exact/latest Registry data. |
| Authoritative KCodeRag guide | Only guide owner and current npx/host semantics | ✓ VERIFIED | Tracked and clean in the sibling service repository; docs check passes; no duplicate exists here. |

Plan frontmatter artifact queries passed 4/4 for Plan 31, 2/2 for Plan 32, and 6/6 for Plan 34. Plan 33 reported 4/5 only because one declared artifact is prose (`package-lock.json + five product manifests`) rather than a literal path; manual verification confirmed all six concrete version-bearing files exist and equal 0.1.8. This is verifier-input notation, not an implementation gap.

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Public npm bin | CLI command dispatcher | `package.json` bin to compiled CJS | ✓ WIRED | Public `npx ... doctor` executed from outside the repository and returned valid JSON. |
| CLI | selected host adapter | exact `--host` selection | ✓ WIRED | One invocation resolves one of three adapters; no multi-host write loop exists. |
| Adapter desired state | filesystem | sole `applyTransaction` commit boundary | ✓ WIRED | Expected digests and identity are validated before staging; state commits last; rollback restores the host-local preimage. |
| Codex/Claude host assets | hook launcher/runtime | native PreToolUse command | ✓ WIRED | Generated self-relative launchers call sibling CJS and always fail open. |
| Cursor host assets | project guidance | always-on Rule + skill + MCP | ✓ WIRED | No false PreToolUse equivalence is emitted. |
| Hook foreground | update worker | local cache plus detached spawn | ✓ WIRED | Foreground performs no network access; worker alone refreshes the fixed npm latest endpoint. |
| Generator | product version fields | root package version | ✓ WIRED | Generated-tree check is drift-free and published metadata is 0.1.8. |
| Release matrix | npm publish | dependent job after required contracts | ✓ WIRED | Live release run proves all four required lanes completed before successful publication. |

The generic key-link query cannot resolve prose labels or external services as filesystem paths, so these links were verified from code wiring, runtime behavior, official Registry data, git refs, and live workflow metadata rather than treated as false negatives.

## Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces real data | Status |
|---|---|---|---|---|
| CLI lifecycle | requested command/host/environment/target | argv plus explicit current/target directory | Yes — drives exactly one desired state and transaction or a read-only report | ✓ FLOWING |
| Host adapter | installed project files and sections | immutable desired state rendered from canonical generated assets | Yes — fresh lifecycle smoke observes native project configuration | ✓ FLOWING |
| Ownership transaction | digests/state/recovery | actual pre-read filesystem identities and bytes | Yes — rollback and drift tests exercise state transitions | ✓ FLOWING |
| Hook response | advisory context | bounded host event input through structural-search classifier | Yes — hook smoke observes valid advisory or intentional silence | ✓ FLOWING |
| Update notice | cached/latest version metadata | bounded local cache refreshed only by the detached Registry worker | Yes — offline, stale, timeout, and atomic replacement tests pass | ✓ FLOWING |
| Release receipt | artifact and host evidence | official Registry bytes plus content-addressed per-command smoke | Yes — independent verifier recomputes identity and validates every host evidence bit | ✓ FLOWING |

## Behavioral Spot-Checks

| Behavior | Command/evidence | Result | Status |
|---|---|---|---|
| Complete local gate | `npm run ci:local` | 197 tests passed; generation unchanged; 59-member pack audit; Codex/Claude/Cursor required smoke PASS | ✓ PASS |
| Repository retirement | post-mode retirement auditor | Python source/script/test counts all zero | ✓ PASS |
| Public exact package | required-contract smoke against `kcoderag-nav@0.1.8` | Three hosts PASS, 11/11 evidence each | ✓ PASS |
| Public latest package | required-contract smoke against `kcoderag-nav@latest` expecting 0.1.8 | Three hosts PASS, 11/11 evidence each; same artifact identity | ✓ PASS |
| Public npx entry point | exact 0.1.8 `doctor --host codex --json` from unrelated empty project | Exit 0 and one valid read-only JSON result | ✓ PASS |
| Receipt compatibility | independent Plan 33 v2 and Plan 34 v3 receipt verification | Both exit 0 | ✓ PASS |
| Decision coverage | decision coverage verifier across all phase plans | 18/18 honored; zero not-honored decisions | ✓ PASS |

## Probe Execution

Step 7c is **SKIPPED**: none of the 31 plans/summaries declares a phase probe and no conventional phase probe file exists. The runnable contracts are covered by the fresh local CI, public exact/latest smoke, receipt verifiers, and remote matrices above.

## Requirements Coverage

| Requirement | Source plans | Description | Status | Evidence |
|---|---|---|---|---|
| JS-01 | Phase 03.1 plans | Node/CJS migration and no published Python/runtime compiler | ✓ SATISFIED | Closed public allow-list, retirement audit, dependency and tar inspection. |
| JS-02 | Phase 03.1 plans | Public five-command npx CLI and one version source | ✓ SATISFIED | CLI/generator source, aligned manifests, public npx execution. |
| JS-03 | Phase 03.1 plans | Three native hosts, adapter extension, one host per command | ✓ SATISFIED | Adapter/CLI wiring and three-host lifecycle smoke. |
| JS-04 | Phase 03.1 plans | Safe legacy migration, ownership, preflight, rollback | ✓ SATISFIED | Transaction implementation plus passing migration/drift/rollback tests. |
| JS-05 | Phase 03.1 plans | Advisory fail-open hook and asynchronous cached update check | ✓ SATISFIED | Hook/worker source plus offline/failure behavioral tests. |
| JS-06 | Phase 03.1 plans | Deterministic generator/pre-commit/pack proof | ✓ SATISFIED | Drift-free generator, 59-member pack audit, negative pack tests. |
| JS-07 | Phase 03.1 plans | Four-lane CI and matching-tag-only release | ✓ SATISFIED | Live ordinary CI and release runs at exact SHAs; workflow dependency wiring. |
| JS-08 | Phase 03.1 plans | Node tests, honest three-host smoke, synchronized docs | ✓ SATISFIED | 197/197, local/public smokes, complete receipts, README/guide gates. |

All eight requirements mapped to Phase 03.1 are claimed by phase plans and have implementation evidence. There are no orphaned Phase 03.1 requirements.

## Decision Coverage

The decision coverage verifier reported **18/18 honored** with an empty `not_honored` list. In particular, QA remains the default and Dev explicit; QA/Dev conflict only within one host; cross-host installations coexist; target resolution does not walk upward; mutations require explicit automation confirmation; Cursor keeps its Rule/skill/MCP boundary; and OpenCode remains deferred.

## Test Quality

- The fresh full run recorded 197 passed, 0 failed, 0 cancelled, 0 skipped, and 0 todo tests.
- No static `test.skip`, `describe.skip`, or `test.todo` marker was found. The one platform-conditional transaction skip did not skip in the fresh run.
- Pack tests cover both false inclusion and allow-list synchronization, not only the happy-path archive.
- Anti-TOCTOU tests replace the acquired archive and require all three host paths to fail, proving the content-addressed checks are per command rather than install-only.
- Receipt v3 tests reject canonical URL, origin, SRI, hash, and exact/latest identity drift.
- Required loopback evidence is exactly 11 booleans per host; absent execution remains distinguishable from PASS.

## Anti-Patterns Found

| File scope | Pattern | Severity | Impact |
|---|---|---|---|
| Modified implementation/test scope | Debt/stub scan | None | `TODO`/`FIXME`/`XXX`/`HACK` occurrences are classifier fixtures or regex input, not unresolved debt. Typed `null` returns represent explicit absence/identity checks, not placeholders. |
| Public tarball | Forbidden runtime/source material | None | Closed 59-member allow-list contains no Python, TypeScript source, tests, planning files, maintainer-only auditor, dependency tree, or placeholder/credential material. |
| Generated state | Manual/generated drift | None | Generator check reports no changed or written path. |

## Security and Credential Boundary

The current security report is `verified` with **97/97 threats closed** and zero open threats. The final code review is `clean`, and the fix report is `all_fixed`. The public and local evidence paths use metadata-only diagnostics and do not expose MCP connection values, authorization headers, subprocess/network bodies, or secret values. The release workflow limits the npm credential to the final publish step; only the required secret's presence was checked, never its value.

The project explicitly accepts an embedded internal Bearer during the current QA/Dev stage. Verification treats that as the documented project constraint, not as approval to surface it: no credential value was inspected or reproduced in this report.

## Deferred Boundaries (Not Phase Gaps)

- Phase 4 owns deployed nested-directory behavior and duplicate user installation sources.
- Phase 5 owns further hook precision and graph/local-search routing refinements.
- Phase 6 owns authenticated real-host evidence; the required Phase 03.1 loopback contracts are complete, while optional authenticated jobs may remain skipped.
- Phase 7 owns GSD runtime/global-hook concerns.
- Phase 8 owns production identity, HTTPS, and credential-rotation hardening.
- OpenCode remains an adapter-extension boundary only, as required by this phase.

These items are explicitly assigned to later roadmap phases and do not weaken the present Node/npx migration goal.

## Human Verification Required

None. External artifact identity and workflow state were independently read from the official Registry, git refs, and GitHub Actions; all runtime state-transition claims used by this verdict are covered by passing behavioral tests or direct smoke execution.

## Gaps Summary

No blocking or warning gap remains. The three prior verifier gaps are closed, the public provenance and per-command anti-TOCTOU follow-up gaps are closed, all JS-01 through JS-08 requirements are satisfied, and no regression or human-needed item was found.

---

_Verified: 2026-08-24T14:12:26Z_
_Verifier: the agent (gsd-verifier)_

---
phase: 03.1-javascript-npx
plan: 21
subsystem: authoritative-documentation
tags: [nodejs, npx, codex, claude-code, cursor, sibling-provenance]

requires:
  - phase: 03.1-19
    provides: Final Node.js 22+ npx lifecycle and three-host documentation contract
  - phase: 03.1-22
    provides: Secret-safe documentation and sibling provenance auditors
provides:
  - Sole authoritative QA experience guide aligned to the public npx project integration
  - Guide-only KCodeRag commit with exact preservation of unrelated dirty status
  - Sanitized dual-repository provenance evidence bound to the sibling baseline
affects: [03.1-14, phase-6, authoritative-guide, npm-release]

actuals:
  tokens: 1818
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - Authoritative cross-repository documentation changes are bound to a clean-guide baseline and guide-only child commit
    - Public setup documentation uses one Node.js 22+ npx lifecycle across Codex, Claude Code, and Cursor

key-files:
  created:
    - .planning/phases/03.1-javascript-npx/03.1-21-SIBLING-BASELINE.json
    - .planning/phases/03.1-javascript-npx/03.1-21-SIBLING-RECEIPT.json
  modified:
    - ../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md

key-decisions:
  - "The KCodeRag service repository remains the sole owner of MCP_QA_EXPERIENCE_GUIDE.md; the navigation repository keeps only sanitized provenance evidence."
  - "Codex, Claude Code, and Cursor share the public npx lifecycle while Cursor keeps an honest Rule/skill/MCP capability boundary."
  - "A normal-hook temporary clone may create a guide-only child commit when a sibling dirty index prevents a safe path-only commit; the original repository accepts it only by exact fast-forward."

patterns-established:
  - "Sibling evidence contains status codes, paths, digests, commit file sets, and hashes only—never unrelated diffs or connection values."
  - "QA is the default, Dev is explicit and host-local exclusive, while separate host integrations may coexist."

requirements-completed: [JS-02, JS-03, JS-08]

kcoderag_nav_head: "aa2704ab7b20f392b9725e0dbf16ce4c6f762e9f"
kcoderag_head: "9d1772d8c5c95948b1719e8a95cd9b374da7a290"

coverage:
  - id: D1
    description: "The sole authoritative guide documents Node.js 22+, public @latest npx installation, five lifecycle commands, QA/Dev policy, three project-native hosts, Cursor Rule semantics, asynchronous update awareness, legacy Cursor authority, and pure MCP limits."
    requirement: JS-03
    verification:
      - kind: integration
        ref: "npm run docs:check -- --policy sibling-guide D:/AIProgram/KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md — PASS"
        status: pass
    human_judgment: false
  - id: D2
    description: "The guide-only KCodeRag child commit preserves every unrelated dirty status entry and is tied to valid KCodeRag Nav and KCodeRag hashes without secret-shaped evidence."
    requirement: JS-08
    verification:
      - kind: integration
        ref: "node dist/maintainer/sibling-guide-audit.cjs --verify-receipt .planning/phases/03.1-javascript-npx/03.1-21-SIBLING-RECEIPT.json — PASS"
        status: pass
    human_judgment: false

duration: 18min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 21: Authoritative npx Experience Guide Summary

**The KCodeRag authoritative guide now gives Codex, Claude Code, and Cursor one Node.js 22+ npx project lifecycle backed by a guide-only, secret-safe cross-repository receipt.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-08-24T03:14:00+08:00
- **Completed:** 2026-08-24T03:32:00+08:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Replaced active checkout, Python, marketplace, and Cursor user-local setup flows with `npx kcoderag-nav@latest` project installation and the complete five-command lifecycle.
- Documented QA-default/Dev-explicit host-local exclusivity, cross-host coexistence, exact project roots, advisory Codex/Claude hooks, Cursor Rule semantics, detached update checks, and pure MCP limits.
- Created a guide-only KCodeRag commit while preserving all ten unrelated baseline entries and recorded both repository hashes in a validated, secret-safe receipt.

## Task Commits

1. **Task 1: Capture sanitized baseline and edit only the authoritative guide** - `aa2704a` (nav baseline)
2. **Task 2: Commit and prove sibling guide provenance** - `9d1772d8` (KCodeRag guide) and `308cd22` (nav receipt)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `.planning/phases/03.1-javascript-npx/03.1-21-SIBLING-BASELINE.json` - Guide-clean sibling HEAD and sorted status code/path baseline.
- `.planning/phases/03.1-javascript-npx/03.1-21-SIBLING-RECEIPT.json` - Commit parent, guide-only file set, unchanged-status digests, secret scan, and dual hashes.
- `../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md` - Sole authoritative three-host QA experience guide.

## Decisions Made

- Kept the guide only in KCodeRag; no navigation-repository copy was created.
- Described pure MCP as MCP-only configuration without claiming hook, Rule, skill, update-awareness, or managed lifecycle behavior.
- Kept update awareness advisory and asynchronous: installed hooks do not perform foreground network I/O or automatic project mutation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Preserved a pre-existing staged sibling index during guide-only commit**
- **Found during:** Task 2
- **Issue:** Nine unrelated sibling files were already staged at baseline. `git commit --only` caused the repository pre-commit framework to construct a temporary index and reject it as `gate_staged_snapshot_diverged`; its automatic restore also reverted the uncommitted guide edit.
- **Fix:** Reapplied the guide, created an ordinary temporary clone from the exact baseline parent, copied the already-installed normal pre-commit hook, committed only the guide there, restored only the guide file authored by this task in the original repository, then fast-forwarded the verified child commit into the original branch. No worktree, stash, reset, hook bypass, or unrelated index mutation was used.
- **Files modified:** `../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md`
- **Verification:** Normal `canonical staged GatePlan` hook passed; commit parent equals the baseline HEAD; commit file set is exactly the guide; the original nine staged entries and one untracked lock match the baseline.
- **Committed in:** `9d1772d8`

**2. [Rule 3 - Blocking] Isolated the temporary clone's uv environment**
- **Found during:** Task 2 retry
- **Issue:** Reusing the original repository's `.venv` made uv attempt to replace an in-use executable and correctly stopped before commit.
- **Fix:** Retried the same normal hook with the temporary clone's own isolated environment; no dependency or project-file changes were added to either repository.
- **Files modified:** None in either repository.
- **Verification:** The hook passed and the exact guide-only commit was created from the required parent.
- **Committed in:** `9d1772d8`

---

**Total deviations:** 2 auto-fixed Rule 3 blocking issues.
**Impact on plan:** The required guide-only scope, normal hooks, exact parent, dirty-state preservation, and secret-safe receipt were all retained; no product scope changed.

## Issues Encountered

- The sibling repository's canonical staged gate deliberately rejects any effective worktree divergence. The final isolated commit path satisfied the gate instead of weakening or bypassing it.

## User Setup Required

None - the guide contains no user-specific connection values or external account steps.

## Known Stubs

None.

## Threat Flags

None. This plan changed documentation and provenance metadata only; it introduced no endpoint, authentication path, schema, or runtime file access.

## Next Phase Readiness

- Plan 14 can publish the first npm release against guide text that already matches the package's Node.js 22+ npx contract.
- Future real-host validation can cite the exact install/status/doctor/update/uninstall flows without reviving marketplace or Python setup paths.

## Self-Check: PASSED

- The authoritative guide exists only in KCodeRag and passes the scoped sibling-guide policy.
- `aa2704a`, `308cd22`, and sibling commit `9d1772d8` exist; the sibling commit changes exactly `MCP_QA_EXPERIENCE_GUIDE.md`.
- The receipt verifies the clean-guide parent, unchanged unrelated status, secret scan, and both 40-hex hashes.
- The two top-level SUMMARY hashes exactly match the validated receipt.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

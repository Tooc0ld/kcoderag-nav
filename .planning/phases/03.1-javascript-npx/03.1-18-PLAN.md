---
phase: 03.1-javascript-npx
plan: 18
type: execute
wave: 21
depends_on: ["03.1-17"]
files_modified:
  - tests/test_project_install.py
  - tests/test_cursor_local_install.py
  - tests/test_generation.py
  - tests/test_hook_runtime.py
  - tests/test_update_check.py
  - tests/test_routing_and_hooks.py
autonomous: true
requirements: [JS-01, JS-04, JS-05, JS-06]
estimate: {tokens: 13000, raw_tokens: 13000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Six install/generator/hook/update Python tests retire only after mapped compiled Node suites pass."
    - "This partial test deletion does not invoke post-tests; Plan25 completes that state."
  artifacts:
    - {path: "tests/**/*.test.cts", provides: "mapped Node replacements"}
  key_links:
    - {from: "Plan15 parity map", to: "six deletions", via: "case-by-case replacement evidence", pattern: "parity"}
---
<objective>分两组三文件退役安装/生成与 hook/update Python tests，不提前宣称 post-tests 完成。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-17-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 删除安装与生成 Python tests</name><files>tests/test_project_install.py, tests/test_cursor_local_install.py, tests/test_generation.py</files><action>确认 parity map 对每个 case 有 `test:cli`、三 host/cross-host 或 generator compiled replacement，再精确删除三文件；任何 unmapped case 先补 Node test，不降覆盖。</action><verify><automated>npm run test:cli &amp;&amp; npm run test:host:codex &amp;&amp; npm run test:host:claude &amp;&amp; npm run test:host:cursor &amp;&amp; npm run test:cross-host &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- three files absent and Node mappings actually run.</acceptance_criteria><done>install/generator tests Node-only。</done></task>
<task type="auto"><name>Task 2: 删除 hook/update/routing Python tests</name><files>tests/test_hook_runtime.py, tests/test_update_check.py, tests/test_routing_and_hooks.py</files><action>逐 case 确认 Node hook/launcher/update suites，再精确删除三文件。只做 scoped unexpected-reference scan；不运行 post-tests，因为 smoke/precommit/update/stub tests remain。</action><verify><automated>npm run test:hook &amp;&amp; npm run test:launcher &amp;&amp; npm run test:update-check</automated></verify><acceptance_criteria>- three files absent, current advisory/offline contracts still pass.- remaining Python test inventory exactly Plan25 list.</acceptance_criteria><done>hook/update/routing tests Node-only，partial test state explicit。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-18-01 | Repudiation | coverage mapping | high | mitigate | per-case parity mapping and compiled suite runs. |
| T-03.1-18-02 | Tampering | deletion scope | high | mitigate | exact six paths, no glob. |
</threat_model>
<verification>Mapped Node suites run; no retirement stage mode is advanced in this partial plan.</verification>
<success_criteria>- Six Python tests removed with no coverage loss; post-tests remains pending.</success_criteria>
## Artifacts this phase produces
- Removed six explicit Python tests.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-18-SUMMARY.md` when done.</output>

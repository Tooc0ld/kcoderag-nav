---
phase: 03.1-javascript-npx
plan: 08
type: execute
wave: 3
depends_on: ["03.1-03"]
files_modified:
  - src/hooks/grep-nudge.cts
  - src/hooks/update-check.cts
  - src/hooks/update-worker.cts
  - tests/hooks/update-check.test.cts
autonomous: true
requirements: [JS-05]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-09/D-17: 每会话第一次相关工具调用只派生后台 worker；前台不等待网络，缓存 24h，失败静默。"
    - "已安装 hook 完全离线可用；只有缓存已知新版时附加 `npx kcoderag-nav@latest update` 提示。"
  artifacts:
    - {path: "src/hooks/update-check.cts", provides: "前台零网络 cache/session 决策", exports: ["readUpdateHint", "scheduleRefresh"]}
    - {path: "src/hooks/update-worker.cts", provides: "有界 npm latest 后台刷新", exports: ["refreshLatest"]}
  key_links:
    - {from: "src/hooks/grep-nudge.cts", to: "src/hooks/update-check.cts", via: "首次 relevant tool 调用", pattern: "scheduleRefresh"}
    - {from: "src/hooks/update-worker.cts", to: "npm Registry dist-tags.latest", via: "bounded HTTPS GET", pattern: "dist-tags"}
---
<objective>把同步更新探测迁成前台零网络、后台有界、24h 缓存的 Node 路径。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-03-SUMMARY.md
@.planning/phases/03.1-javascript-npx/COVERAGE.md
@plugin-src/hooks/update_check.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现前台 session/cache 状态机</name>
  <files>src/hooks/update-check.cts, tests/hooks/update-check.test.cts</files>
  <behavior>- fresh cache 零网络零 spawn；stale/missing cache 每会话只尝试一次 detached spawn。- cache invalid/permission/race/clock skew 都静默 fail-open。- 只有已解析且 greater-than installed 的 latest 产生提示。</behavior>
  <action>使用原子 JSON cache 与有界 session marker，键不含 QA/Dev 凭据或用户查询。`readUpdateHint` 同步只读；`scheduleRefresh` 只调用注入的 detached spawn，不能 await worker。用 fake clock/fs/spawn 覆盖 24h 边界、并发和离线。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:update-check</automated></verify>
  <acceptance_criteria>- 前台测试断言没有 HTTP 调用。- 每 session 最多一次 spawn，所有异常返回无提示。</acceptance_criteria>
  <done>D-09 的 first-relevant-tool、24h、foreground-zero-network 成立。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现有界后台 Registry worker 并接入 nudge</name>
  <files>src/hooks/update-worker.cts, src/hooks/grep-nudge.cts, tests/hooks/update-check.test.cts</files>
  <behavior>- worker 只读取 `kcoderag-nav` metadata 的 `dist-tags.latest`，1.5s 内超时并原子写安全 cache。- HTTP/DNS/TLS/JSON/semver/permission 错误 exit0 且不污染旧 cache。- nudge 先完成自身输出决策，再 fire-and-forget refresh。</behavior>
  <action>按 COVERAGE.md 仅实现 registry metadata GET，不实现 auth/admin。限制响应体、重定向和协议，拒绝非 HTTPS/非预期 JSON；将 network/client 注入测试。hook 不能 await worker，也不能因 worker spawn 失败改变退出码/原始工具。</action>
  <verify><automated>npm run test:update-check</automated></verify>
  <acceptance_criteria>- 慢/不可达 registry 不增加 hook 关键路径等待。- 更新提示精确为 D-09 指定命令。</acceptance_criteria>
  <done>D-17 离线已安装路径和异步 fail-open 有自动化证明。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-08-01 | Denial of Service | foreground hook | high | mitigate | 零网络、detached worker、有界 session marker。 |
| T-03.1-08-02 | Spoofing | registry response | medium | mitigate | HTTPS、固定 package endpoint、strict latest/size/timeout。 |
| T-03.1-08-03 | Tampering | cache | medium | mitigate | schema validate、atomic replace、invalid cache ignored。 |
</threat_model>
<verification>`npm run test:update-check &amp;&amp; npm run test:hook`</verification>
<success_criteria>- foreground latency 与网络解耦。- 更新失败不阻断 hook 且旧 cache 不被破坏。</success_criteria>
## Artifacts this phase produces
- New symbols/files: `readUpdateHint`, `scheduleRefresh`, `refreshLatest`, update cache/session schema.
- New user hint: `npx kcoderag-nav@latest update` only from verified cached newer version.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-08-SUMMARY.md` when done.</output>

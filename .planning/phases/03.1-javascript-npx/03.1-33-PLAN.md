---
phase: 03.1-javascript-npx
plan: 33
type: execute
wave: 29
depends_on: ["03.1-32"]
files_modified:
  - package.json
  - package-lock.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
autonomous: false
gap_closure: true
requirements: [JS-02, JS-04, JS-07, JS-08]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "JS-04: public `kcoderag-nav@latest` resolves to the final reviewed ownership/drift/identity/rollback implementation from the exact release commit, not historical `0.1.6`."
    - "JS-07/D-08/D-15: the credential-bearing publish job starts only after successful Ubuntu/Windows × Node 22/24 required release lanes for the exact matching tag SHA."
    - "D-06/D-07: the audited helper creates a new immutable patch version (`0.1.7` from current `0.1.6`, or the next patch derived from a later valid source state), with package/lock/five manifests as the exact seven-path release commit."
    - "A sanitized v2 receipt binds tag, release SHA, workflow run, four lanes, publish job, registry exact/latest metadata and `gitHead`, plus fresh exact-version and latest lifecycle PASS for all three hosts."
    - "No executor action overwrites an npm version or tag, reinterprets `0.1.6`, exposes `NPM_TOKEN`, or persists MCP/Bearer values."
  artifacts:
    - path: "package.json"
      provides: "New root-source package version created only by the audited release helper"
    - path: "package-lock.json + five product manifests"
      provides: "Exact seven-path version-synchronized release write set"
    - path: ".planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json"
      provides: "Sanitized schema-v2 tag/SHA/run/lane/registry/exact/latest three-host evidence"
    - path: ".github/workflows/release.yml"
      provides: "Existing tag-only four-lane gate with publish depending on `required-contracts`"
  key_links:
    - from: "Plan 32 pre-release SHA"
      to: "release commit"
      via: "audited helper creates one direct child changing exactly seven version paths"
      pattern: "release: v"
    - from: "matching immutable tag"
      to: "credential-bearing publish job"
      via: "release workflow `publish.needs: required-contracts` after four successful matrix lanes"
      pattern: "needs: required-contracts"
    - from: "npm exact/latest acquisition"
      to: ".planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json"
      via: "two fresh three-host required-contract runs reduced to allow-listed all-true evidence"
      pattern: "exact_version"
---

<objective>
After explicit new-version authorization, publish the final safe source as a new immutable patch through the audited helper and four-lane release workflow, then capture exact and latest public evidence in a v2 offline-verifiable receipt.

Purpose: Close the JS-04/JS-07 public-delivery gaps while preserving the historical truth about `0.1.6` and every one-way release control.
Output: One exact seven-path release commit, one immutable matching tag, one matrix-gated npm version, fresh dual-specifier three-host evidence, and one sanitized receipt.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
@.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
@src/maintainer/release.cts
@src/maintainer/publish-receipt.cts
@src/smoke/host-smoke.cts
@.github/workflows/release.yml
@tests/maintainer/release.test.cts
@tests/maintainer/release-workflow.test.cts
@tests/maintainer/publish-receipt.test.cts

<interfaces>
- `prepareRelease({ level: "patch", dryRun, yes })` runs the complete local gate, derives the next patch from root `package.json`, commits exactly `RELEASE_OWNED_PATHS`, and creates the matching local tag; it never pushes or publishes.
- `runHostSmoke` accepts only exact `kcoderag-nav@X.Y.Z`, literal `kcoderag-nav@latest`, or the contained local tgz seam and requires eleven evidence bits per host.
- Publish-receipt schema v2 requires exact four-lane and dual-specifier host evidence; schema v1 remains historical-only.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the exact pre-release SHA to one collision-free patch candidate without mutation</name>
  <read_first>
    .planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
    package.json
    package-lock.json
    src/maintainer/release.cts
    tests/maintainer/release.test.cts
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
    src/maintainer/publish-receipt.cts
    tests/maintainer/publish-receipt.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
  </read_first>
  <files>None (read-only GitHub/npm/git/helper preflight)</files>
  <action>
    Revalidate the exact Plan 32 pre-release SHA: local `HEAD` and `origin/master` must match, its ordinary CI run must be completed/success with all four required jobs, and tracked/index state must be clean apart from preserved local planning/GSD state. Run the release, workflow, receipt, smoke, and pack contract tests, then run `npm run release:patch -- --dry-run --json` and parse only its safe version/tag/release-path output. The expected candidate is `0.1.7` while the root remains `0.1.6`; if a valid later root version is present, use only the helper-derived next patch. Never hand-edit a version to skip a collision.

    Read public npm metadata and local/remote tags without mutation. Require public latest to equal the current root version before the bump, require the candidate exact npm version to be unpublished, and require the candidate tag to be absent locally and remotely. Verify GitHub CLI authentication and check only that a repository secret named `NPM_TOKEN` is present; never read or print its value. The user's active goal and prior secret configuration are readiness context, not authorization for a new immutable version. This task must not create a commit/tag, push, call npm publish, or alter a dist-tag.
  </action>
  <verify>
    <automated>npm run test:release &amp;&amp; npm run test:release-workflow &amp;&amp; npm run test:publish-receipt &amp;&amp; npm run test:smoke &amp;&amp; npm run pack:audit &amp;&amp; npm run release:patch -- --dry-run --json</automated>
  </verify>
  <acceptance_criteria>
    - The exact pre-release SHA is still the four-lane-green `origin/master` commit from Plan 32.
    - The helper derives one collision-free next patch and reports exactly package/lock plus the five product manifests; current state yields `0.1.7` unless the source version has legitimately advanced.
    - Public latest still points to the historical current source version before release, and no candidate npm version/tag exists.
    - Only secret-name presence is checked; no token, workflow log body, registry auth, MCP value, or Bearer value is read or persisted.
    - Git, package, tag, registry, and worktree state are unchanged by the preflight.
  </acceptance_criteria>
  <done>The complete release path is runnable and collision-free, but no one-way action has occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize one exact new immutable patch release</name>
  <read_first>
    .planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
  </read_first>
  <files>None</files>
  <action>Pause after the read-only preflight and request an explicit human decision for the exact helper-derived candidate version. Do not infer authorization from the active phase-completion goal, previous `0.1.6` authorization, prior npm secret configuration, or the recommended option. Only the explicit resume signal for this candidate permits Task 3.</action>
  <decision>Authorize the audited helper to create the exact new patch release commit/tag, push the default branch and tag, let the four-lane release workflow publish it, and record sanitized public evidence?</decision>
  <context>The candidate version, npm tarball, immutable Git tag, GitHub run, and registry publication become permanent external evidence. Existing npm versions/tags cannot be overwritten, and `0.1.6` must remain the historical earlier release rather than being re-described as containing later fixes.</context>
  <options>
    <option id="authorize">
      <name>Authorize exact candidate</name>
      <pros>Publishes the reviewed safe implementation and can close JS-04/JS-07 with matrix-bound public evidence.</pros>
      <cons>The candidate version/tag and release record are permanent.</cons>
    </option>
    <option id="stop">
      <name>Stop before publication</name>
      <pros>Leaves all public npm/tag state unchanged while retaining the pushed, four-lane-green source.</pros>
      <cons>Phase 03.1 remains blocked because public latest still lacks the later fixes.</cons>
    </option>
  </options>
  <verify>
    <automated>npm run release:patch -- --dry-run --json</automated>
  </verify>
  <acceptance_criteria>
    - Execution stops at this blocking-human checkpoint in every mode until the user explicitly authorizes the exact candidate.
    - Any changed candidate, registry/tag collision, source SHA, workflow result, or secret-name status invalidates the preflight and requires a fresh decision.
    - Choosing stop creates no release commit, tag, push, npm version, receipt, or dist-tag change.
  </acceptance_criteria>
  <resume-signal>Reply `authorize &lt;exact-version&gt;` to continue; any other response stops before publication.</resume-signal>
  <done>The one-way release authority is explicit, version-specific, and not inferred.</done>
</task>

<task type="auto">
  <name>Task 3: Publish through the audited helper and record lane-bound exact/latest evidence</name>
  <read_first>
    package.json
    package-lock.json
    src/maintainer/release.cts
    src/maintainer/publish-receipt.cts
    src/smoke/host-smoke.cts
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
  </read_first>
  <reversibility rating="one-way">A published npm version, matching tag, workflow run, and registry evidence cannot be overwritten; correction requires another authorized version.</reversibility>
  <files>package.json, package-lock.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-cursor/.cursor-plugin/plugin.json, .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json</files>
  <action>
    Proceed only after the exact `authorize &lt;version&gt;` response. Snapshot preserved local `.planning`/`.gsd` status/digests, then invoke `npm run release:patch -- --yes --json`; the audited helper alone may change, stage, commit, and tag. Require its previous version, new version, tag, release SHA, and `releasePaths` to match the authorized candidate and exactly seven paths: root package/lock and the five product manifests. Require the release commit parent to equal Plan 32's exact pre-release SHA, all seven versions to agree, and no additional staged/committed path. Never manually create, move, delete, reuse, or force a tag, and never invoke local `npm publish`.

    Push the release commit to `origin/master`, then push only the helper-created matching tag. Discover the unique `Release` workflow push run using the tag and exact release SHA; watch it with exit status and re-query run/jobs. Require exact head SHA/tag/push/success, the four successful required release contract jobs (Ubuntu/Windows × Node 22/24), and a successful dependent publish job. A missing, skipped, cancelled, or failed lane/job stops evidence recording. Confirm the workflow source still has `publish.needs: required-contracts` and that the credential is scoped only to the final publish step; inspect no secret value.

    After Registry convergence, query allow-listed exact-version and latest metadata. Require exact version and latest to equal the new version, registry `gitHead` to equal the release SHA, and bin/engine/repository to match the package contract. Use separate fresh temporary npm caches and user-config boundaries to run required-contract smoke twice: once with `kcoderag-nav@&lt;exact-version&gt;`, once with `kcoderag-nav@latest`; each run must return PASS for Codex, Claude Code, and Cursor with every lifecycle/navigation/MCP/update/uninstall/stub-receipt bit true. Do not substitute authenticated real-host evidence, which remains Phase 6.

    Reduce only the accepted run/job/registry metadata and the two all-true host maps into schema v2 and pipe it to `publish-receipt --record` for `03.1-33-PUBLISH-RECEIPT.json`; immediately verify offline. Commit the receipt separately from the seven-path release commit. Do not persist command output, logs, environment values, headers, npm config, URLs containing credentials, MCP configuration, Bearer values, or token values. Preserve the immutable `03.1-14` v1 receipt unchanged and describe `0.1.6` only as historical evidence.
  </action>
  <verify>
    <automated>node dist/maintainer/publish-receipt.cjs --verify .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json</automated>
  </verify>
  <acceptance_criteria>
    - The helper-derived release commit changes exactly seven version paths and is the direct child of the four-lane-green Plan 32 pre-release SHA; the matching tag points to it immutably.
    - The accepted release run is unique for tag/SHA, all four required matrix jobs succeed, publish succeeds only after their dependency, and registry exact/latest metadata including `gitHead` matches.
    - Fresh exact-version and latest acquisitions each complete install/status/navigation/MCP/update/uninstall for Codex, Claude Code, and Cursor with no FAIL or NOT_RUN evidence.
    - The v2 receipt passes offline verification and contains only allow-listed metadata/booleans; the historical v1 receipt and public release history remain unchanged.
    - No npm version/tag is overwritten and no NPM token, MCP value, Bearer value, raw log, header, or command body is exposed.
  </acceptance_criteria>
  <done>The new public latest is the exact reviewed, Node-only, four-lane-gated implementation and all verifier gaps have immutable sanitized evidence.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Node 22+ unified npx lifecycle with no Python runtime and safe three-host ownership | 31-33 | COVERED | Pack bytes, final source CI, and public exact/latest delivery close the three failed seams. |
| REQ | JS-01 | No Python-running code or runtime compiler in the package | 31 | COVERED | Content-aware real-tarball gate. |
| REQ | JS-02 | Public five-command package and root version source | 33 | COVERED | Audited helper derives the new patch from root package version. |
| REQ | JS-03 | Native one-host adapters and cross-host coexistence | 01-30 preserved; 32-33 smoke | COVERED | Already verifier-PASS; dual public smoke prevents regression. |
| REQ | JS-04 | Public ownership/drift/identity/rollback implementation | 32-33 | COVERED | Final fixed SHA is four-lane green, then becomes public latest. |
| REQ | JS-05 | Advisory/offline hook and bounded refresh | 01-30 preserved; 32-33 smoke | COVERED | Already verifier-PASS and rerun by complete gates. |
| REQ | JS-06 | Deterministic self-contained Python-free pack proof | 31 | COVERED | Allowed CJS negative content fixtures plus real tgz. |
| REQ | JS-07 | Windows/Linux Node 22/24 before matching-tag publication | 32-33 | COVERED | Exact pre-release CI and release matrix, with publish job dependency in evidence. |
| REQ | JS-08 | Honest three-host lifecycle/MCP evidence and docs | 32-33 | COVERED | Fresh exact-version and latest required-contract runs; authenticated real hosts stay Phase 6. |
| RESEARCH | stack | Exact audited dev graph, zero production dependencies, CJS on Node 22+ | 31-33 | COVERED | No package install/change; dependency audit and clean builds rerun. |
| RESEARCH | package | Exact tarball, deterministic generation, no runtime compiler/Python | 31 | COVERED | Gap-targeted byte policy and full gate. |
| RESEARCH | release | Matching tag, four required lanes, sanitized evidence | 32-33 | COVERED | Pre-release and release SHA/run binding plus receipt v2. |
| RESEARCH | security | Opaque MCP values and secret-safe diagnostics | 31-33 | COVERED | Code/path/boolean-only evidence; no credential values. |
| CONTEXT | D-01 | Complete migration; package has no Python-running code | 31 | COVERED | Retired parity route removed and guarded in tgz. |
| CONTEXT | D-02 | TypeScript maintenance source to direct CJS; no runtime compile | 31 | COVERED | Build-to-packed-CJS tracer and unchanged dependency contract. |
| CONTEXT | D-03 | npx replaces marketplace installation | 01-30 preserved; 33 | COVERED | Public npx exact/latest evidence; no marketplace path added. |
| CONTEXT | D-04 | One selected Codex/Claude/Cursor host per invocation | 01-30 preserved; 32-33 | COVERED | Required smoke runs each host separately. |
| CONTEXT | D-05 | Safe verified legacy migration | 01-30 preserved; 33 | COVERED | Public latest is moved to the reviewed fix set; migration vocabulary remains allowed data. |
| CONTEXT | D-06 | Public `kcoderag-nav` contract | 33 | COVERED | Version-specific blocking-human release authorization. |
| CONTEXT | D-07 | Root package version is the only version source | 31,33 | COVERED | Obsolete script retirement plus exact helper seven-path bump. |
| CONTEXT | D-08 | Audited release helper and matching tag workflow | 32-33 | COVERED | Pre-release source CI, explicit authority, helper-only tag, four-lane release. |
| CONTEXT | D-09 | npm latest update semantics | 01-30 preserved; 33 | COVERED | Exact/latest registry and lifecycle verification; no update protocol change. |
| CONTEXT | D-10 | Host-native project paths and honest Cursor boundary | 01-30 preserved; 32-33 | COVERED | Dual public required smoke retains host capability distinctions. |
| CONTEXT | D-11 | cwd/target and confirmation | 01-30 preserved; 32-33 | COVERED | Complete lifecycle smoke reruns public CLI behavior. |
| CONTEXT | D-12 | Structured ownership and drift hard-stop | 01-30 preserved; 33 | COVERED | Final reviewed fixes are published and exercised. |
| CONTEXT | D-13 | Cross-host coexistence; host-local QA/Dev exclusivity | 01-30 preserved; 32-33 | COVERED | Existing tests plus all-three-host lifecycle evidence. |
| CONTEXT | D-14 | Extensible adapter seam; OpenCode deferred | 01-30 preserved | COVERED | No OpenCode implementation added. |
| CONTEXT | D-15 | Node 22 and 24 CI | 32-33 | COVERED | Both ordinary pre-release and tag release evidence require four lanes. |
| CONTEXT | D-16 | Mutations gate Node; deployed hook fails open | 01-30 preserved; 32-33 | COVERED | Complete tests/smoke rerun; no hook behavior redesign. |
| CONTEXT | D-17 | Acquisition failure is zero-write; installed path offline | 01-30 preserved; 32-33 | COVERED | Package acquisition remains before project creation and each run is isolated. |
| CONTEXT | D-18 | One-host atomic transaction and rollback | 01-30 preserved; 33 | COVERED | The public package is bound to the reviewed rollback/identity fix SHA. |
| CONTEXT | Deferred | OpenCode; production identity/HTTPS/credential rotation | — | EXCLUDED | Explicitly deferred; no plan implements or claims these items. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human authorization → release helper | A version-specific decision permits a permanent external release. |
| Git tag/SHA → GitHub release matrix | Immutable source provenance enters four required executable lanes. |
| Required lanes → credential-bearing publish job | npm authority is reachable only after every supported lane succeeds. |
| Registry/package acquisition → public evidence | Exact and latest network artifacts are trusted only after SHA/version/metadata/lifecycle checks. |
| Public evidence → receipt | External results are reduced to closed nonsecret metadata and booleans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-33-01 | Tampering | release commit/tag/version | critical | mitigate | Blocking-human exact-version authority, helper-only seven-path commit/tag, direct-parent and immutable-tag checks; never overwrite/reuse. |
| T-03.1-33-02 | Spoofing | GitHub workflow/registry evidence | critical | mitigate | Unique tag/SHA run, exact four lanes plus publish job, registry `gitHead`/version/latest cross-field validation. |
| T-03.1-33-03 | Information Disclosure | npm credential and MCP config | critical | mitigate | Secret-name-only preflight, workflow-final-step credential scope, fresh isolated acquisition, closed receipt and recursive secret rejection. |
| T-03.1-33-04 | Repudiation | exact/latest host lifecycle evidence | high | mitigate | Two independent fresh required-contract runs with all three hosts and all evidence bits, recorded separately in schema v2. |
| T-03.1-33-05 | Denial of Service | external release/network convergence | medium | mitigate | Fail closed before receipt on missing/failed lanes or metadata; never reinterpret partial/NOT_RUN evidence as success. |
</threat_model>

<verification>
The new `03.1-33-PUBLISH-RECEIPT.json` verifies offline and binds the authorized version/tag/release SHA to all four successful release lanes, dependent publish success, exact/latest registry metadata, and fresh exact/latest three-host lifecycle PASS.
</verification>

<success_criteria>
- Public latest contains the final reviewed safety fixes and no Python-running CJS path.
- The exact publication was gated by Windows/Linux × Node 22/24 and cannot start before those lanes succeed.
- Exact-version and latest public acquisition each pass the complete Codex/Claude/Cursor lifecycle and loopback MCP contract.
- Historical `0.1.6`, all npm versions/tags, and all secret boundaries remain truthful and immutable.
</success_criteria>

## Artifacts this phase produces

- One human-authorized exact seven-path release commit and matching immutable tag.
- One successful tag-triggered release run with four required OS/Node lanes and a dependent publish job.
- One new public immutable patch version and updated npm latest dist-tag; current source yields `0.1.7` unless it legitimately advances before execution.
- Fresh exact-version and latest three-host lifecycle/MCP evidence.
- Sanitized `.planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json`, committed separately from the release commit.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-33-SUMMARY.md` when done.
</output>

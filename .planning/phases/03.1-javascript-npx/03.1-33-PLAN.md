---
phase: 03.1-javascript-npx
plan: 33
type: execute
wave: 29
depends_on: ["03.1-32"]
files_modified:
  - package.json
  - package-lock.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
autonomous: false
gap_closure: true
requirements: [JS-02, JS-04, JS-07, JS-08]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "JS-04: public `kcoderag-nav@latest` resolves to the final reviewed ownership/drift/identity/rollback implementation from the exact release commit, not historical `0.1.6`."
    - "The exact pre-release SHA includes all Plan 31/32 implementation commits and the committed `03.1-32-SUMMARY.md`, is pushed only in Plan 33, and passes one ordinary Windows/Linux × Node 22/24 CI run."
    - "JS-07/D-08/D-15: the credential-bearing publish job starts only after successful Ubuntu/Windows × Node 22/24 required release lanes for the exact matching tag SHA."
    - "D-06/D-07: the audited helper creates a new immutable patch version (`0.1.7` from current `0.1.6`, or the next patch derived from a later valid source state), with package/lock/five manifests as the exact seven-path release commit."
    - "The authorized release cannot start unless a byte-identical and field-identical rerun of the frozen preflight snapshot still proves candidate, SHA/branch, exact CI run/jobs, registry/tag absence, and secret-name presence."
    - "A sanitized v2 receipt binds requested/resolved exact/latest manifests, registry exact/latest and `gitHead`, Release workflow identity/tag ref/head SHA, four lanes/publish, and fresh three-host lifecycle PASS."
    - "No executor action overwrites an npm version or tag, reinterprets `0.1.6`, exposes `NPM_TOKEN`, or persists MCP/Bearer values."
  artifacts:
    - path: "package.json"
      provides: "New root-source package version created only by the audited release helper"
    - path: "package-lock.json + five product manifests"
      provides: "Exact seven-path version-synchronized release write set"
    - path: ".planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json"
      provides: "Sanitized schema-v2 tag/SHA/run/lane/registry/exact/latest three-host evidence"
    - path: ".github/workflows/release.yml"
      provides: "Existing tag-only four-lane gate with publish depending on `required-contracts`"
    - path: ".github/workflows/ci.yml"
      provides: "Existing ordinary four-lane CI exercised on the final summarized pre-release SHA"
  key_links:
    - from: "Plan 32 implementation and SUMMARY commits"
      to: "ordinary CI pre-release SHA"
      via: "Plan 33 Task 1 captures/pushes HEAD only after `03.1-32-SUMMARY.md` is committed and verifies all four exact jobs"
      pattern: "headSha"
    - from: "frozen version-specific preflight snapshot"
      to: "release commit"
      via: "immediate post-checkpoint byte/field replay followed by helper-created direct child of the same pre-release SHA"
      pattern: "release: v"
    - from: "matching immutable tag"
      to: "credential-bearing publish job"
      via: "release workflow `publish.needs: required-contracts` after four successful matrix lanes"
      pattern: "needs: required-contracts"
    - from: "npm exact/latest acquisition"
      to: ".planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json"
      via: "two fresh three-host required-contract runs reduced to allow-listed all-true evidence"
      pattern: "exact_version"
---

<objective>
Push and test the final summarized source, freeze a complete version-specific preflight, then—only after explicit authorization and an identical replay—publish a new immutable patch and capture provenance-bound public evidence.

Purpose: Close the JS-04/JS-07 public-delivery gaps while preserving the historical truth about `0.1.6` and every one-way release control.
Output: One exact summarized pre-release SHA with ordinary four-lane CI, one frozen/replayed authorization snapshot, one exact seven-path release commit, one immutable matching tag, one matrix-gated npm version, fresh dual-specifier three-host evidence, and one sanitized receipt.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
@.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
@src/maintainer/release.cts
@src/maintainer/publish-receipt.cts
@src/smoke/host-smoke.cts
@.github/workflows/release.yml
@tests/maintainer/release.test.cts
@tests/maintainer/release-workflow.test.cts
@tests/maintainer/publish-receipt.test.cts

<interfaces>
- `prepareRelease({ level: "patch", dryRun, yes })` runs the complete local gate, derives the next patch from root `package.json`, commits exactly `RELEASE_OWNED_PATHS`, and creates the matching local tag; it never pushes or publishes.
- `runHostSmoke` accepts only exact `kcoderag-nav@X.Y.Z`, literal `kcoderag-nav@latest`, or the contained local tgz seam; public runs bind requested/expected versions to the acquired manifest and preserve that provenance across synthetic tgz execution.
- Publish-receipt schema v2 requires Release workflow identity/ref/head, exact four-lane/publish results, registry version/`gitHead`, and requested/resolved dual-specifier host evidence; schema v1 remains historical-only.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Push the final summarized source through four-lane CI and freeze its release preflight</name>
  <read_first>
    .planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
    package.json
    package-lock.json
    src/maintainer/release.cts
    tests/maintainer/release.test.cts
    .github/workflows/ci.yml
    tests/maintainer/ci-contract.test.cts
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
    src/maintainer/publish-receipt.cts
    tests/maintainer/publish-receipt.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
  </read_first>
  <precondition>`03.1-32-SUMMARY.md` exists in a committed Plan 32 completion commit; `origin/master` is an ancestor of local `master`; GitHub CLI authentication succeeds; preserved planning/GSD state is distinguishable from tracked/index changes.</precondition>
  <reversibility rating="costly">The default-branch push and ordinary CI record are persistent shared history; recovery is a new audited revert commit, never a force rewrite.</reversibility>
  <files>None (remote default-branch push, external evidence reads, and a private OS-temporary preflight snapshot)</files>
  <action>
    First require `03.1-32-SUMMARY.md` to exist in `HEAD`, confirm `HEAD` contains all Plan 31/32 implementation and SUMMARY commits, and assert the current branch is `master`, `origin/master` is an ancestor, the index has no staged entries, and no unexpected tracked change exists beyond the preserved planning/GSD baseline. Run the complete local gate. Capture this HEAD as the 40-character `preReleaseSha`, push only `master` to `origin/master` without force, and create no tag/version/publication. Discover the ordinary workflow only as `CI` from `.github/workflows/ci.yml`, push event, and exact `preReleaseSha`; require one unambiguous completed/success run and exact successful `Required contracts` jobs for Ubuntu Node 22/24 and Windows Node 22/24. Reconfirm local HEAD, `origin/master`, and run `headSha` are identical. The Plan 33 task itself creates no repository commit, so the tested SHA remains the release commit's prospective parent.

    On that exact four-lane-green SHA, run release/workflow/receipt/smoke/pack contract tests and `npm run release:patch -- --dry-run --json`. Parse only safe fields. The candidate is `0.1.7` while root remains `0.1.6`; if the committed source legitimately has a later version, use only the helper-derived next patch. Require the dry-run release paths to be exactly package/lock plus five product manifests. Query allow-listed public npm metadata and local/remote tags without mutation: public latest equals the current root version, the exact candidate version is absent, and the candidate tag is absent both locally and remotely. Check GitHub authentication and only the presence of repository secret name `NPM_TOKEN`, never its value.

    Serialize a canonical, stable-key-order frozen snapshot under `os.tmpdir()` with filename `kcoderag-nav-03.1-33-preflight-{preReleaseSha}.json`, outside the repository, and compute its SHA-256. Create it with user-only permissions. Include exactly: candidate version/tag and seven release paths; branch; `preReleaseSha`; local/origin SHA; ordinary workflow name/path/run ID/event/head SHA/conclusion; the four exact job names/conclusions; current root/public-latest versions; booleans proving candidate npm version absence, local tag absence, remote tag absence, and `NPM_TOKEN` name presence. Retain the exact bytes, digest, and private path across the checkpoint; include no secret value, logs, URLs, headers, MCP/Bearer material, or raw command bodies. The active goal and prior secret setup are readiness only, not release authorization.
  </action>
  <verify>
    <automated>npm run ci:local; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run release:patch -- --dry-run --json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $sha = (git rev-parse HEAD).Trim(); if ((git rev-parse origin/master).Trim() -ne $sha) { throw 'origin SHA mismatch' }; $runs = @(gh run list --workflow ci.yml --event push --commit $sha --json databaseId,headSha,status,conclusion | ConvertFrom-Json | Where-Object { $_.headSha -eq $sha -and $_.status -eq 'completed' -and $_.conclusion -eq 'success' }); if ($runs.Count -ne 1) { throw 'expected one successful CI run' }; $run = gh run view $runs[0].databaseId --json headSha,event,conclusion,jobs | ConvertFrom-Json; if ($run.headSha -ne $sha -or $run.event -ne 'push' -or $run.conclusion -ne 'success') { throw 'CI provenance mismatch' }; $required = @('Required contracts (ubuntu-latest, Node 22)','Required contracts (ubuntu-latest, Node 24)','Required contracts (windows-latest, Node 22)','Required contracts (windows-latest, Node 24)'); foreach ($name in $required) { if (@($run.jobs | Where-Object { $_.name -eq $name -and $_.conclusion -eq 'success' }).Count -ne 1) { throw "missing successful lane: $name" } }</automated>
  </verify>
  <acceptance_criteria>
    - The exact pre-release SHA contains committed `03.1-32-SUMMARY.md` plus all Plan 31/32 implementation/summary commits, equals `origin/master`, and has one exact four-lane-green ordinary CI run.
    - The helper derives one collision-free next patch and reports exactly package/lock plus the five product manifests; current state yields `0.1.7` unless the source version has legitimately advanced.
    - Public latest still points to the historical current source version before release, and no candidate npm version/tag exists.
    - Only secret-name presence is checked; no token, workflow log body, registry auth, MCP value, or Bearer value is read or persisted.
    - The canonical private snapshot contains the complete allow-listed preflight field set, has a recorded SHA-256, and contains no credential or raw-output data.
    - Apart from the explicit default-branch push/CI run and private temporary snapshot, package, tag, registry, and repository state are unchanged by the preflight.
  </acceptance_criteria>
  <done>The final summarized source SHA is on the default branch, four-lane green, collision-free, and frozen into a version-specific preflight without creating any release state.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize one exact new immutable patch release</name>
  <read_first>
    .planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-14-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
    os.tmpdir()/kcoderag-nav-03.1-33-preflight-{preReleaseSha}.json and Task 1's recorded SHA-256
  </read_first>
  <files>None</files>
  <action>Pause after the frozen preflight and request an explicit human decision for the exact helper-derived candidate version and recorded snapshot digest. Do not infer authorization from the active phase-completion goal, previous `0.1.6` authorization, prior npm secret configuration, or the recommended option. Only the explicit resume signal naming both this candidate and snapshot digest permits Task 3, and Task 3 must replay the entire snapshot before mutation.</action>
  <decision>Authorize the audited helper to create the exact new patch release commit/tag as a direct child of the frozen pre-release SHA, push that release commit and tag, let the four-lane release workflow publish it, and record sanitized public evidence?</decision>
  <context>The candidate version, npm tarball, immutable Git tag, GitHub run, and registry publication become permanent external evidence. Existing npm versions/tags cannot be overwritten, and `0.1.6` must remain the historical earlier release rather than being re-described as containing later fixes.</context>
  <options>
    <option id="authorize">
      <name>Authorize exact candidate</name>
      <pros>Publishes the reviewed safe implementation and can close JS-04/JS-07 with matrix-bound public evidence.</pros>
      <cons>The candidate version/tag and release record are permanent.</cons>
    </option>
    <option id="stop">
      <name>Stop before publication</name>
      <pros>Leaves all public npm/tag state unchanged while retaining the already pushed, four-lane-green source.</pros>
      <cons>Phase 03.1 remains blocked because public latest still lacks the later fixes.</cons>
    </option>
  </options>
  <verify>
    <automated>npm run release:patch -- --dry-run --json</automated>
  </verify>
  <acceptance_criteria>
    - Execution stops at this blocking-human checkpoint in every mode until the user explicitly authorizes the exact candidate.
    - Any changed candidate, registry/tag collision, source SHA, workflow result, or secret-name status invalidates the preflight and requires a fresh decision.
    - Choosing stop creates no release commit/tag, release push, npm version, receipt, or dist-tag change; only Task 1's already authorized default-branch source push/CI remains.
  </acceptance_criteria>
  <resume-signal>Reply `authorize &lt;exact-version&gt; &lt;preflight-sha256&gt;` to continue; any other response stops before publication.</resume-signal>
  <done>The one-way release authority is explicit, version-specific, and not inferred.</done>
</task>

<task type="auto">
  <name>Task 3: Publish through the audited helper and record lane-bound exact/latest evidence</name>
  <read_first>
    package.json
    package-lock.json
    src/maintainer/release.cts
    src/maintainer/publish-receipt.cts
    src/smoke/host-smoke.cts
    .github/workflows/release.yml
    tests/maintainer/release-workflow.test.cts
    .planning/phases/03.1-javascript-npx/03.1-14-PUBLISH-RECEIPT.json
    os.tmpdir()/kcoderag-nav-03.1-33-preflight-{preReleaseSha}.json and Task 1's recorded SHA-256
  </read_first>
  <reversibility rating="one-way">A published npm version, matching tag, workflow run, and registry evidence cannot be overwritten; correction requires another authorized version.</reversibility>
  <files>package.json, package-lock.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-cursor/.cursor-plugin/plugin.json, .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json</files>
  <action>
    Proceed only after the exact `authorize &lt;version&gt; &lt;preflight-sha256&gt;` response matching Task 1. The first post-checkpoint operation, before any release helper call, commit, tag, push, version write, or npm mutation, is a full preflight replay. Re-run the same allow-listed queries and canonical serializer from Task 1 for candidate version/tag/seven paths; branch; local/origin/pre-release SHA; exact ordinary CI workflow name/path/run ID/event/head SHA/conclusion and all four job names/conclusions; current root/public-latest versions; candidate npm absence; local/remote tag absence; and `NPM_TOKEN` name-presence boolean. Verify the original private snapshot file's SHA-256 equals the authorized digest, then require byte-for-byte equality and explicit field-for-field equality between the frozen and replayed snapshots. Any missing file, digest mismatch, changed byte/field, network ambiguity, candidate/latest movement, SHA/branch drift, CI drift, tag/version collision, or secret-name change invalidates the authorization: perform no mutation, return to Task 1, freeze a new snapshot, and require a fresh version-specific human decision.

    Only after that identical replay, snapshot preserved local `.planning`/`.gsd` status/digests and invoke `npm run release:patch -- --yes --json`; the audited helper alone may change, stage, commit, and tag. Require its previous version, new version, tag, release SHA, and `releasePaths` to match the authorized candidate and exactly seven paths: root package/lock and the five product manifests. Require the release commit parent to equal the frozen and replayed four-lane-green `preReleaseSha`, all seven versions to agree, and no additional staged/committed path. Never manually create, move, delete, reuse, or force a tag, and never invoke local `npm publish`.

    Push the release commit to `origin/master`, then push only the helper-created matching tag. Discover the unique workflow by identity `Release`, path `.github/workflows/release.yml`, push event, ref `refs/tags/&lt;tag&gt;`, and exact release SHA; watch it with exit status and re-query run/jobs. Require that exact identity/ref/head SHA/tag/push/success, the four successful required release contract jobs (Ubuntu/Windows × Node 22/24), and a successful dependent publish job. A missing, skipped, cancelled, failed, substituted, or ambiguous lane/job/workflow stops evidence recording. Confirm workflow source still has `publish.needs: required-contracts` and the credential is scoped only to the final publish step; inspect no secret value.

    After Registry convergence, query allow-listed exact-version and latest metadata in one bounded evidence window. Require exact resolved version and latest resolved version to equal the new version, registry `gitHead` to equal release SHA, and bin/engine/repository to match. Use separate fresh temporary npm caches and user-config boundaries to run required-contract smoke twice: exact `kcoderag-nav@&lt;version&gt;` with expected version equal to that X.Y.Z, then `kcoderag-nav@latest` with `--expected-version &lt;version&gt;`. Each result must preserve requested specifier, expected version, acquired manifest name/version and tgz digest across synthetic tgz lifecycle execution, and return PASS for Codex, Claude, and Cursor with all evidence bits true. Re-query registry latest immediately after the latest smoke; if it changed or differs from the acquired manifest/expected version, treat this as a latest race, record nothing, and require a fresh evidence run. Authenticated real-host evidence remains Phase 6.

    Here the acquired manifest is the installed public package manifest read before loopback-source injection, and the recorded tgz digest is the synthetic lifecycle tarball actually executed; retain both identities rather than treating the local tgz path as public provenance.

    Reduce only accepted metadata into schema v2: top-level version/tag/release SHA; registry exact/latest plus `gitHead`; Release workflow name/path/run ID/tag ref/head SHA/event, four lanes, and publish result; exact/latest requested/expected/resolved installed-manifest provenance, synthetic lifecycle tgz digests, and all-true host maps. Pipe it to `publish-receipt --record` for `03.1-33-PUBLISH-RECEIPT.json`, immediately verify offline, and commit the receipt separately from the seven-path release commit. Do not persist the temporary preflight snapshot, command output, logs, environment values, headers, npm config, credential URLs, MCP configuration, Bearer values, or tokens. Preserve immutable `03.1-14` v1 unchanged and describe `0.1.6` only as historical evidence.
  </action>
  <verify>
    <automated>node dist/maintainer/publish-receipt.cjs --verify .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json</automated>
  </verify>
  <acceptance_criteria>
    - Before any mutation, the full post-checkpoint replay is byte-identical and field-identical to the frozen snapshot; every drift path returns to Task 1 and requires fresh authorization.
    - The helper-derived release commit changes exactly seven version paths and is the direct child of the frozen/replayed four-lane-green pre-release SHA containing Plan 32's SUMMARY; the matching tag points to it immutably.
    - The accepted Release workflow identity/path/tag ref/head SHA is unique, all four required matrix jobs succeed, publish succeeds only after their dependency, and registry exact/latest metadata including `gitHead` matches.
    - Fresh exact-version and latest acquisitions bind requested/expected/acquired installed-manifest provenance and synthetic tgz digest, survive lifecycle execution unchanged, and complete all three hosts with no FAIL/NOT_RUN; a latest race cannot produce evidence.
    - The v2 receipt passes offline verification and contains only allow-listed metadata/booleans; the historical v1 receipt and public release history remain unchanged.
    - No npm version/tag is overwritten and no NPM token, MCP value, Bearer value, raw log, header, or command body is exposed.
  </acceptance_criteria>
  <done>The new public latest is the exact reviewed, Node-only, four-lane-gated implementation and all verifier gaps have immutable sanitized evidence.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Node 22+ unified npx lifecycle with no Python runtime and safe three-host ownership | 31-33 | COVERED | Pack bytes, final source CI, and public exact/latest delivery close the three failed seams. |
| REQ | JS-01 | No Python-running code or runtime compiler in the package | 31 | COVERED | Closed tar inventory excludes the maintainer-only parity auditor. |
| REQ | JS-02 | Public five-command package and root version source | 33 | COVERED | Audited helper derives the new patch from root package version. |
| REQ | JS-03 | Native one-host adapters and cross-host coexistence | 01-30 preserved; 32-33 smoke | COVERED | Already verifier-PASS; dual public smoke prevents regression. |
| REQ | JS-04 | Public ownership/drift/identity/rollback implementation | 32-33 | COVERED | Final fixed SHA is four-lane green, then becomes public latest. |
| REQ | JS-05 | Advisory/offline hook and bounded refresh | 01-30 preserved; 32-33 smoke | COVERED | Already verifier-PASS and rerun by complete gates. |
| REQ | JS-06 | Deterministic self-contained Python-free pack proof | 31 | COVERED | Explicit non-publishable artifact plus exact real-tgz membership proof. |
| REQ | JS-07 | Windows/Linux Node 22/24 before matching-tag publication | 32-33 | COVERED | Plan 32 defines receipt contracts; Plan 33 pushes the summarized pre-release SHA and proves both ordinary and release matrices. |
| REQ | JS-08 | Honest three-host lifecycle/MCP evidence and docs | 32-33 | COVERED | Fresh exact-version and latest required-contract runs; authenticated real hosts stay Phase 6. |
| RESEARCH | stack | Exact audited dev graph, zero production dependencies, CJS on Node 22+ | 31-33 | COVERED | No package install/change; dependency audit and clean builds rerun. |
| RESEARCH | package | Exact tarball, deterministic generation, no runtime compiler/Python | 31 | COVERED | Closed inventory excludes the risky maintainer artifact without semantic-parser gaps. |
| RESEARCH | release | Matching tag, four required lanes, sanitized evidence | 32-33 | COVERED | Summarized pre-release SHA, frozen/replayed preflight, and Release identity/ref/SHA receipt binding. |
| RESEARCH | security | Opaque MCP values and secret-safe diagnostics | 31-33 | COVERED | Code/path/boolean-only evidence; no credential values. |
| CONTEXT | D-01 | Complete migration; package has no Python-running code | 31 | COVERED | Retired parity route removed and its maintainer auditor excluded by exact tgz inventory. |
| CONTEXT | D-02 | TypeScript maintenance source to direct CJS; no runtime compile | 31 | COVERED | Build-to-packed-CJS tracer and unchanged dependency contract. |
| CONTEXT | D-03 | npx replaces marketplace installation | 01-30 preserved; 33 | COVERED | Public npx exact/latest evidence; no marketplace path added. |
| CONTEXT | D-04 | One selected Codex/Claude/Cursor host per invocation | 01-30 preserved; 32-33 | COVERED | Required smoke runs each host separately. |
| CONTEXT | D-05 | Safe verified legacy migration | 01-30 preserved; 33 | COVERED | Public latest is moved to the reviewed fix set; migration vocabulary remains allowed data. |
| CONTEXT | D-06 | Public `kcoderag-nav` contract | 33 | COVERED | Version-specific blocking-human release authorization. |
| CONTEXT | D-07 | Root package version is the only version source | 31,33 | COVERED | Obsolete script retirement plus exact helper seven-path bump. |
| CONTEXT | D-08 | Audited release helper and matching tag workflow | 32-33 | COVERED | Summary-inclusive pre-release CI, frozen/replayed authority, helper-only tag, four-lane release. |
| CONTEXT | D-09 | npm latest update semantics | 01-30 preserved; 33 | COVERED | Exact/latest registry and lifecycle verification; no update protocol change. |
| CONTEXT | D-10 | Host-native project paths and honest Cursor boundary | 01-30 preserved; 32-33 | COVERED | Dual public required smoke retains host capability distinctions. |
| CONTEXT | D-11 | cwd/target and confirmation | 01-30 preserved; 32-33 | COVERED | Complete lifecycle smoke reruns public CLI behavior. |
| CONTEXT | D-12 | Structured ownership and drift hard-stop | 01-30 preserved; 33 | COVERED | Final reviewed fixes are published and exercised. |
| CONTEXT | D-13 | Cross-host coexistence; host-local QA/Dev exclusivity | 01-30 preserved; 32-33 | COVERED | Existing tests plus all-three-host lifecycle evidence. |
| CONTEXT | D-14 | Extensible adapter seam; OpenCode deferred | 01-30 preserved | COVERED | No OpenCode implementation added. |
| CONTEXT | D-15 | Node 22 and 24 CI | 33 | COVERED | Plan 33 alone owns ordinary pre-release and tag-release four-lane external evidence after Plan 32's SUMMARY commit. |
| CONTEXT | D-16 | Mutations gate Node; deployed hook fails open | 01-30 preserved; 32-33 | COVERED | Complete tests/smoke rerun; no hook behavior redesign. |
| CONTEXT | D-17 | Acquisition failure is zero-write; installed path offline | 01-30 preserved; 32-33 | COVERED | Package acquisition remains before project creation and each run is isolated. |
| CONTEXT | D-18 | One-host atomic transaction and rollback | 01-30 preserved; 33 | COVERED | The public package is bound to the reviewed rollback/identity fix SHA. |
| CONTEXT | Deferred | OpenCode; production identity/HTTPS/credential rotation | — | EXCLUDED | Explicitly deferred; no plan implements or claims these items. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed Plan 32 SUMMARY → default branch CI | The final evidence-contract implementation and its summary must share the exact pushed/tested pre-release SHA. |
| Human authorization → release helper | A version-specific decision permits a permanent external release. |
| Frozen preflight → post-checkpoint replay | Authorization remains valid only while every safe readiness byte and field is unchanged. |
| Git tag/SHA → GitHub release matrix | Immutable source provenance enters four required executable lanes. |
| Required lanes → credential-bearing publish job | npm authority is reachable only after every supported lane succeeds. |
| Registry/package acquisition → public evidence | Exact and latest network artifacts are trusted only after SHA/version/metadata/lifecycle checks. |
| Public evidence → receipt | External results are reduced to closed nonsecret metadata and booleans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-33-01 | Tampering | release commit/tag/version | critical | mitigate | Summary-inclusive pre-release SHA, byte/field-identical post-checkpoint replay, helper-only seven-path commit/tag, direct-parent and immutable-tag checks. |
| T-03.1-33-02 | Spoofing | GitHub workflow/registry evidence | critical | mitigate | Bind exact CI and Release workflow identities, paths, refs, head SHAs, four lanes/publish, registry `gitHead`/version/latest, and reject ambiguity. |
| T-03.1-33-03 | Information Disclosure | npm credential and MCP config | critical | mitigate | Secret-name-only preflight, workflow-final-step credential scope, fresh isolated acquisition, closed receipt and recursive secret rejection. |
| T-03.1-33-04 | Repudiation | exact/latest host lifecycle evidence | high | mitigate | Bind requested/expected/acquired installed-manifest identity and synthetic lifecycle tgz digest through two fresh three-host runs; re-query latest to reject races. |
| T-03.1-33-05 | Denial of Service | external release/network convergence | medium | mitigate | Fail closed before receipt on missing/failed lanes or metadata; never reinterpret partial/NOT_RUN evidence as success. |
</threat_model>

<verification>
The final summarized pre-release SHA has one exact ordinary four-lane CI run; after an identical frozen-preflight replay, `03.1-33-PUBLISH-RECEIPT.json` verifies offline and binds the authorized version/tag/release SHA to Release workflow identity/ref/head SHA, all four lanes, dependent publish success, registry exact/latest/`gitHead`, and requested/resolved exact/latest three-host PASS.
</verification>

<success_criteria>
- Public latest contains the final reviewed safety fixes and no Python-running CJS path.
- The release commit is the direct child of the ordinary four-lane-green pre-release SHA that includes committed `03.1-32-SUMMARY.md`.
- The exact publication was gated by Windows/Linux × Node 22/24 and cannot start before those lanes succeed.
- Exact-version and latest public acquisition each bind requested/expected/resolved manifest provenance, reject latest races, and pass the complete Codex/Claude/Cursor lifecycle and loopback MCP contract.
- Historical `0.1.6`, all npm versions/tags, and all secret boundaries remain truthful and immutable.
</success_criteria>

## Artifacts this phase produces

- One human-authorized exact seven-path release commit and matching immutable tag.
- One final summarized pre-release source SHA pushed to `origin/master`, with an exact ordinary CI run and four successful required-contract jobs.
- One private frozen preflight snapshot/digest replayed byte-for-byte and field-for-field immediately after authorization; it is not persisted in the repository or receipt.
- `03.1-33-SUMMARY.md` records only the safe pre-release SHA/CI run/job identifiers and preflight digest after all external checks complete; no earlier plan claims that evidence.
- One successful tag-triggered release run with four required OS/Node lanes and a dependent publish job.
- One new public immutable patch version and updated npm latest dist-tag; current source yields `0.1.7` unless it legitimately advances before execution.
- Fresh exact-version and latest three-host lifecycle/MCP evidence.
- Sanitized `.planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json`, committed separately from the release commit.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-33-SUMMARY.md` when done.
</output>

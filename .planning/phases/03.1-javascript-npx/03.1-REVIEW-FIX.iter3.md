---
phase: 03.1-javascript-npx
fixed_at: 2026-08-24T04:44:31.7589382Z
review_path: .planning/phases/03.1-javascript-npx/03.1-REVIEW.md
iteration: 2
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 03.1: Code Review Fix Report

**Fixed at:** 2026-08-24T04:44:31.7589382Z
**Source review:** `.planning/phases/03.1-javascript-npx/03.1-REVIEW.md`
**Iteration:** 2

**Summary:**

- Findings in scope: 4
- Fixed: 4
- Skipped: 0
- Published package: npm `0.1.6` was not changed, republished, pushed, or otherwise mutated.

## Fixed Issues

### CR-01: Filesystem identity checks remain check-then-use and can escape the project or delete replacement data

**Files modified:** `src/core/transaction.cts`, `src/hosts/cursor.cts`, `tests/core/transaction.test.cts`, `tests/hosts/cursor.test.cts`
**Commit:** da555b1
**Applied fix:** Bound reads and staging to no-follow descriptors, revalidated file and ancestor identities immediately before and after pathname operations, and changed replacement/deletion to quarantine the exact verified object so rollback never blindly overwrites replacement data. Recovery files are created through verified directory/file identities. Cursor legacy migration now folds backup payloads into the transaction, quarantines exact verified legacy objects, and refuses restoration through a changed ancestor. Deterministic final-window swap seams prove outside trees and replacement bytes remain untouched. This is filesystem security and rollback logic and requires human verification.

### CR-02: Section-owned JSON updates rewrite and can corrupt unrelated configuration

**Files modified:** `package.json`, `src/core/json-splice.cts`, `src/hosts/claude.cts`, `src/hosts/codex.cts`, `src/hosts/cursor.cts`, `tests/hosts/claude.test.cts`, `tests/hosts/codex.test.cts`, `tests/hosts/cursor.test.cts`
**Commit:** cd7769c
**Applied fix:** Added a dependency-free syntax-aware JSON range scanner/splicer and routed shared MCP/hook object-member and array-item updates/removals through owned lexical ranges only. Whole-document parsing remains validation-only; fatal UTF-8 decoding still fails closed. Lifecycle fixtures cover nested members, CRLF/noncanonical whitespace, escaped strings, and `9007199254740993`, proving exact unowned bytes survive install/update and the exact original document returns after uninstall. This is structured mutation logic and requires human verification.

### WR-01: A successful release commit can still survive an immediate follow-up Git failure

**Files modified:** `src/maintainer/release.cts`, `tests/maintainer/release.test.cts`
**Commit:** d5e4d8a
**Applied fix:** Added a failure seam immediately after `git commit` and before `rev-parse HEAD`. Compensation now discovers a candidate commit only when it is the exact direct child of the original HEAD with the expected subject, seven-path write set, and compatible tag state; ambiguity fails closed instead of resetting guessed history. Regression coverage proves HEAD, files, index, and tag state are restored for commit-discovery, post-commit, and post-tag failures. This is release-state logic and requires human verification.

### WR-02: Expired session-less marker renewal permits multiple 24-hour claims

**Files modified:** `src/hooks/update-check.cts`, `tests/hooks/update-check.test.cts`, `kcoderag-qa/hooks/update-check.cjs`, `kcoderag-dev/hooks/update-check.cjs`
**Commit:** 0881596
**Applied fix:** Replaced read-remove-create renewal with an exclusive generation token derived from the exact observed stale marker, an exact-content recheck, and atomic same-directory replacement. Delayed contenders cannot remove a fresh marker and fail closed if the generation changed; uncertain replacement failures retain the token. Safe token pruning removes only tokens whose paired marker no longer matches. A deterministic nested interleaving reproduces the old double claim and now proves exactly one renewal, while a real Windows filesystem test verifies atomic replacement and cleanup. Generated QA/Dev hooks were regenerated and checked. This is concurrency logic and requires human verification.

## Verification

- Isolated worktree: builds and focused suites passed for all findings: 19/19 transaction/Cursor tests, 24/24 host JSON lifecycle tests, 8/8 release tests, and 12/12 update-check tests. Generated CJS passed syntax checks and `generate:check` reported no drift.
- Main checkout after fast-forward: `npm run ci:local` passed — build and dependency audit passed, 159/159 tests passed, deterministic generation passed, pack audit accepted the exact 60-entry archive, and required-contract smoke passed for Codex, Claude Code, and Cursor.
- Cleanup: `master` fast-forwarded to `0881596`; the isolated worktree, temporary review-fix branch, and recovery sentinel were removed in recovery-protocol order.
- Working tree: only pre-existing/untracked planning and GSD artifacts plus this intentionally uncommitted report remain; no source fix is uncommitted.

---

_Fixed: 2026-08-24T04:44:31.7589382Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 2_

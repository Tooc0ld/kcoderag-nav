---
phase: 03.1-javascript-npx
plan: 13
subsystem: release-automation
tags: [nodejs, npm, git, github-actions, release, receipt, supply-chain]

requires:
  - phase: 03.1-06
    provides: Required audited build, generation, pack, and three-host smoke gates
provides:
  - Exact seven-path patch/minor/major release commit and matching local tag preparation
  - Offline sanitized npm publication receipt recording and verification
  - Tag-only GitHub Actions npm publication after all required gates
affects: [03.1-14, npm-release, github-actions, publication-evidence]

actuals:
  tokens: 10733
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns:
    - Release preparation treats .planning and .gsd as cleanliness-only exceptions while hashing their complete bytes across the operation
    - Publication evidence uses a closed offline schema with cross-field consistency and recursive secret-like value rejection
    - Ordinary CI and tag-only publication are separate workflows with different authority

key-files:
  created:
    - src/maintainer/release.cts
    - tests/maintainer/release.test.cts
    - src/maintainer/publish-receipt.cts
    - tests/maintainer/publish-receipt.test.cts
    - .github/workflows/release.yml
    - tests/maintainer/release-workflow.test.cts
  modified: []

key-decisions:
  - "Release preparation owns exactly package.json, package-lock.json, and the five generated host version manifests; every generated, diff, staged, and committed path set must equal that contract."
  - "Local .planning and .gsd dirtiness may coexist with release preparation, but their complete tree digest must remain unchanged and they are never staged or repaired by the helper."
  - "Publication receipts are offline, exact-schema evidence documents; tag/version, release/workflow SHA, successful push run, registry metadata, and all public npx host evidence must agree."
  - "The release workflow is isolated to v*.*.* tag pushes, uses read-only repository permissions, and exposes NPM_TOKEN only to the final publish step."

patterns-established:
  - "Release transaction: preflight cleanliness and current generation, run required gates, mutate seven owned paths, prove exact write/stage/commit sets, then create the matching local tag."
  - "Publish receipt: record or verify one private JSON document without network, environment, command execution, raw output, or credential-bearing fields."

requirements-completed: [JS-02, JS-07]

coverage:
  - id: D1
    description: "Patch, minor, and major release preparation is dry-run capable and creates only one exact seven-path commit plus the matching immutable local tag."
    requirement: JS-02
    verification:
      - kind: unit
        ref: "tests/maintainer/release.test.cts — exact transaction, dirty surface, drift, rollback, and local-state cases; 5/5 passing"
        status: pass
      - kind: integration
        ref: "npm run release:patch -- --dry-run --json — all 127 Node tests and required generation, pack, and three-host smoke gates pass without mutation"
        status: pass
    human_judgment: false
  - id: D2
    description: "A sanitized offline receipt binds the public package version and tag to the release SHA, successful workflow run, registry latest metadata, and complete host evidence."
    requirement: JS-07
    verification:
      - kind: unit
        ref: "tests/maintainer/publish-receipt.test.cts — schema, mismatch, NOT_RUN, secret, atomic record, CLI, and offline cases; 6/6 passing"
        status: pass
    human_judgment: false
  - id: D3
    description: "Only a matching version-tag workflow can publish, and it reruns every required gate before the credential-scoped npm publish step."
    requirement: JS-07
    verification:
      - kind: unit
        ref: "tests/maintainer/release-workflow.test.cts — trigger, permission, action pin, gate order, tag, token, and CI-isolation cases; 5/5 passing"
        status: pass
      - kind: integration
        ref: "npm publish --dry-run --ignore-scripts — 58-file kcoderag-nav@0.1.4 archive prepared with no publication"
        status: pass
    human_judgment: false

duration: 11min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 13: Audited Release Preparation and Publication Evidence Summary

**An exact seven-file release transaction, closed offline receipt schema, and tag-only gated npm workflow now prepare the first controlled publication without creating remote state.**

## Performance

- **Duration:** 11 min
- **Started:** 2026-08-23T17:12:13Z
- **Completed:** 2026-08-23T17:23:34Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added patch/minor/major release preparation that tolerates existing GSD planning state while proving it unchanged, rejects every other dirty path, and commits/tags exactly seven version-owned files.
- Added a network-free receipt recorder/validator that rejects schema expansion, mismatched run/tag/SHA/version, failed or NOT_RUN evidence, and secret-like values without echoing input.
- Added a minimal-permission `v*.*.*` release workflow that verifies the tag, reruns the audited dependency/build/test/generation/smoke/pack gates, and scopes the npm credential to the final publish step.

## Task Commits

All three tasks followed TDD RED/GREEN order:

1. **Task 1 RED: Specify exact release transaction** - `0553cac` (test)
2. **Task 1 GREEN: Implement exact release transaction** - `f87958b` (feat)
3. **Task 2 RED: Specify sanitized publish receipt** - `515afe4` (test)
4. **Task 2 GREEN: Implement offline receipt validation** - `ec62bea` (feat)
5. **Task 3 RED: Specify tag-only publish workflow** - `1a99f80` (test)
6. **Task 3 GREEN: Add tag-only npm workflow** - `7a1e713` (ci)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `src/maintainer/release.cts` - Dirty-safe preflight, semantic version bump, exact generator/write/stage/commit proof, local tag creation, dry-run, and safe CLI.
- `tests/maintainer/release.test.cts` - Temporary-git release fixtures, build-root ignore boundary, drift/failure matrices, and real-repository snapshot preservation.
- `src/maintainer/publish-receipt.cts` - Exact offline receipt schema, recursive secret rejection, cross-field verification, and private atomic record/verify CLI.
- `tests/maintainer/publish-receipt.test.cts` - Valid receipt plus wrong run/SHA/tag/version/conclusion/evidence/secret/CLI fixtures.
- `.github/workflows/release.yml` - Version-tag-only npm publish workflow with immutable official action pins and minimal permissions.
- `tests/maintainer/release-workflow.test.cts` - Static trigger, permission, gate order, tag check, credential scope, and ordinary-CI isolation contract.

## Decisions Made

- Kept release ownership narrower than generated-product ownership: README and all non-version assets remain untouched because they use `@latest` and do not participate in version bumps.
- Compared generator observations, worktree diff, staged index, and release commit independently; no one source of path evidence is trusted as a substitute for another.
- Allowed `.planning/**` and `.gsd/**` only in the preflight status filter, then hashed both complete trees so a gate or generator cannot silently use that exception as write authority.
- Reused the official action SHA pins already audited by required CI; live official repositories confirmed both pins resolve to the `v4.4.0` tags.
- Kept Plan 13 entirely local/offline with respect to publication: neither the release helper test nor workflow verification pushed a tag or published a package.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Built compiled audit code before running the dependency gate**
- **Found during:** Task 3 (matching-tag workflow)
- **Issue:** The plan prose listed dependency audit before build, but the established Plan 05/06 clean-checkout contract executes `dist/maintainer/dependency-audit.cjs`, which does not exist until TypeScript is built.
- **Fix:** The release workflow installs with scripts disabled, verifies the tag, builds, and then runs the exact dependency audit before tests and all remaining release gates.
- **Files modified:** `.github/workflows/release.yml`, `tests/maintainer/release-workflow.test.cts`
- **Verification:** Release workflow test 5/5 and the complete local release dry-run passed.
- **Committed in:** `7a1e713`

---

**Total deviations:** 1 auto-fixed (1 Rule 3 blocker)
**Impact on plan:** The gate set and publication boundary are unchanged; the ordering makes the compiled audit executable on a clean runner and matches the already approved required CI contract.

## Issues Encountered

- The optional `scripts/ci_monitor.cjs` helper named by the GitHub workflow skill is not present. Current GitHub workflow/event/permission syntax was fetched from official documentation, and the two action pins were verified directly against the official action repositories.
- `npm publish --dry-run` reports that registry login would be required for a real publish, as expected. It exited successfully and created no registry, tag, or remote Git state.

## User Setup Required

None for this plan. Plan 14 will require the controlled npm/GitHub publication authority when it performs the real first release.

## Next Phase Readiness

- Plan 14 can run `release:patch|minor|major -- --yes --json`, push its exact local tag, observe the matching release workflow, validate public npm/npx behavior, and record the sanitized receipt.
- The current package remains `0.1.4`; no local release commit/tag or public npm version was created here.
- No blocker remains for the next Phase 03.1 plan.

## Known Stubs

None.

## Self-Check: PASSED

- All six source, test, and workflow artifacts and this SUMMARY exist.
- Task commits `0553cac`, `f87958b`, `515afe4`, `ec62bea`, `1a99f80`, and `7a1e713` exist with no tracked deletions.
- Fresh final verification passed: release 5/5, receipt 6/6, workflow 5/5, aggregate Node 127/127, deterministic generation, 58-entry pack audit, three-host required smoke, release patch dry-run, and npm publish dry-run.
- The worktree still contains only the pre-existing `.planning`/`.gsd` local state outside committed Plan 13 files; no Bearer, npm token, command output, or credential value was persisted.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

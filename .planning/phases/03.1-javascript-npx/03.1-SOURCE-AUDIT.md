# Phase 03.1 Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan(s) | Status | Evidence boundary |
|---|---|---|---|---|---|
| GOAL | — | Node22+ unified public npx CLI; no Python runtime; safe Codex/Claude/Cursor project lifecycle | 01-11,13-26,29-30 | COVERED | Ignore prerequisite → tracer → shared core/CLI → host/runtime/generator → CI/release → cache cleanup/staged retirement/docs. |
| REQ | JS-01 | TypeScript/CJS full migration and no shipped Python/runtime compiler | 01,03,05,09-11,15-19,23-25,29-30 | COVERED | Python deletion occurs only after Plan15 receipt and exact cache cleanup; Plan19 final post/pack scans. |
| REQ | JS-02 | Public `kcoderag-nav` five-command CLI and package.json version source | 01,13,14,19-21,26 | COVERED | Plan14 must close public 404 before phase completion. |
| REQ | JS-03 | Codex/Claude/Cursor native project adapters, one host per command | 04,07,20,21,26 | COVERED | Cross-host tree-diff tests and npx-only project docs; existing product metadata is check-only in Plan23. |
| REQ | JS-04 | Legacy migration, ownership, pre-write checks, rollback | 01,02,04,07,15 | COVERED | Legacy digest and injection failures remain zero-write. |
| REQ | JS-05 | Advisory CJS hook, async npm latest, 24h/offline fail-open | 03,08,18 | COVERED | Foreground has no network; worker is bounded. |
| REQ | JS-06 | Generator/pre-commit/pack determinism and clean artifact | 05,09-11,15-19,23-25 | COVERED | Temp generator proof precedes guaranteed runtime writes and read-only no-op repository gate; final tarball + retirement audit. |
| REQ | JS-07 | Windows/Linux Node22/24 CI and matching-tag publication | 05,13,14,20 | COVERED | Real first tag/publish is controlled, not a verify command. |
| REQ | JS-08 | Three-host lifecycle/smoke, honest evidence, README + sole guide | 04,06,18,19,21,22,25 | COVERED | Required NOT_RUN is nonzero; sibling guide has machine baseline/receipt. |
| RESEARCH | Package Legitimacy Audit | `typescript`, `@types/node`, transitive `undici-types`; no prod deps | 01 | COVERED | Blocking-human disposition precedes install; exact graph machine audit follows. |
| RESEARCH | Standard Stack | ranges `>=5.7 <7`, `>=22 <23`; lock 6.0.3/22.20.1/6.21.0 | 01 | COVERED | Manifest/lock/npm-ls JSON/integrity/scripts compared. |
| RESEARCH | TypeScript emit | `.cts -> .cjs`, built-ins + node:test, no ts-node/bundler/framework | 01,03,05,09 | COVERED | Dependency/pack audit prevents expansion. |
| RESEARCH | Supply chain | `--ignore-scripts`, exact graph, re-audit on drift | 01,05,15 | COVERED | One reusable `deps:audit` in local/CI/parity gates. |
| RESEARCH | Runtime matrix | Node24 local is insufficient; CI proves Node22 and 24 | 05 | COVERED | Required 2 OS × 2 Node lanes. |
| RESEARCH | npm Registry API | latest metadata, acquisition, immutable publish | 08,13,14 | COVERED | Exact surface remains in COVERAGE.md. |
| CONTEXT | D-01 | Complete migration of hook/update/install/generator/precommit/smoke/tests; no Python | 03,05,08-11,15-19,23-25,30 | COVERED | Bounded cache cleanup and monotonic retirement plans follow parity receipt. |
| CONTEXT | D-02 | TypeScript maintenance and directly runnable CJS | 01,03,10,11,19 | COVERED | No user-side compilation. |
| CONTEXT | D-03 | npx replaces three-host marketplace installation | 04,17,19-21 | COVERED | Root marketplace entries and stale instructions retired. |
| CONTEXT | D-04 | Interactive host or explicit `--host`; one host per call | 04,26 | COVERED | Shared registry and cross-host snapshot. |
| CONTEXT | D-05 | Safe in-place legacy migration preserving environment | 04,07,15,16 | COVERED | Codex/Claude project state and Cursor user-local state require exact digests; Cursor removal additionally requires separate authority and dual rollback. |
| CONTEXT | D-06 | Public unscoped package and standard latest install | 01,14,19,21 | COVERED | Combined one-way/audit checkpoint + actual publication. |
| CONTEXT | D-07 | package.json is sole version source | 09-11,13-14,16,23 | COVERED | Generator/read-only canonical checks/release/pack/state parity. |
| CONTEXT | D-08 | release scripts and matching-tag Actions publish | 13,14 | COVERED | Readiness separate from controlled first release. |
| CONTEXT | D-09 | first relevant tool, npm latest, 24h, fail-open | 08 | COVERED | Foreground cache/session + detached worker. |
| CONTEXT | D-10 | Official project paths and honest project-integration name | 04,07,20,21,23 | COVERED | Cursor Rule explicitly not a hook; existing Cursor assets are canonical read-only checks. |
| CONTEXT | D-11 | cwd default, normalized confirmation, --target, no root search | 01,02 | COVERED | Tracer and target tests. |
| CONTEXT | D-12 | Structured merge, narrow ownership, drift hard stop | 02,04,07 | COVERED | Per-host fixtures. |
| CONTEXT | D-13 | Cross-host coexistence; same-host environment mutex | 04 | COVERED | Full-tree cross-host evidence. |
| CONTEXT | D-14 | Extensible HostAdapter; OpenCode deferred | 04,26 | COVERED | Seam only, no OpenCode id/path/docs. |
| CONTEXT | D-15 | Minimum Node22, CI 22/24 | 01,02,05,19,29 | COVERED | exact engines, pre-install ignore/prewrite gates, matrix, docs. |
| CONTEXT | D-16 | Mutations hard-stop; deployed hook fail-open; read-only diagnostics | 02,03 | COVERED | Separate core and hook tests. |
| CONTEXT | D-17 | Cold acquisition zero-write; installed offline; network fail-open | 08,14 | COVERED | npm acquisition + offline/worker/temp project evidence. |
| CONTEXT | D-18 | Single-host atomic transaction/rollback | 01,02,04,07 | COVERED | state-last and injected failure recovery. |

## Package Legitimacy Audit Disposition

| Package | Accepted range / lock target | Why | Consuming task | Required disposition |
|---|---|---|---|---|
| `typescript` | `>=5.7.0 <7.0.0` / `6.0.3` | `.cts` to `.cjs` compiler only | 03.1-01 Task 2 | Official audit `OK`; included in combined pre-install checkpoint. |
| `@types/node` | `>=22.0.0 <23.0.0` / `22.20.1` | Node22 built-in declarations only | 03.1-01 Task 2 | `SUS:too-new`; blocking-human acceptance required. |
| `undici-types` | transitive `~6.21.0` / `6.21.0` | `@types/node` declaration edge only | 03.1-01 Task 2 | `SUS:too-new`; blocking-human acceptance required; never direct/prod. |

Plan01 Task1 records acceptance of official identity/ownership, exact versions, registry resolutions/integrities/signatures, no lifecycle scripts and the sole transitive edge before any install. Any change to resolution, integrity, scripts, repository ownership or dependency graph invalidates the acceptance and requires re-audit. `dependencies` remains empty. The same task separately preserves the D-06 one-way public-name confirmation.

## Dependency and Wave Audit

- 26 execution waves across 27 plans; every repository-wide build writer is dependency-serialized, same-wave generator plans have disjoint guaranteed-changing write sets, and every plan owns fewer than ten files.
- Plan01 centrally registers every npm script. Plans02/26 split core from CLI; Plans03/08 split hook from update; Plan22 creates docs/sibling audit entries before use.
- Repository-wide build writers are totally ordered `01→02→03→22→08→26→07→09→23→04→05→06→13→15→16`; every `.cts` mutation explicitly runs `build` before its compiled test, and no same-wave source task shares `dist`/`dist-tests` output.
- Plan09 proves changed-subset and full no-op behavior in temp fixtures. Plans10/11 own only guaranteed-changing `runtime-cjs` and `runtime-launcher` leaves; Plan23 read-only checks existing byte-equal registration/metadata/Cursor groups before the first all-product/all-group repository check. Plan19 owns docs and Plan14 owns guaranteed version changes.
- Plan04 migrates exact-owned Cursor user-local state only after independent authority and restores both user/project trees on injected failure. Plan05 snapshots the entire index and never stages, including partial-index fixtures.
- CI/smoke/release readiness precede Plan15 parity receipt. Plan30 then validates and removes exactly five ignored cache roots; retirement advances sequentially through post-source (Plans16/24), post-scripts (17), post-tests (18/25), and final post (19).
- Plan20 scoped project reconciliation precedes Plan19's unique full scan; sibling guide follows; Plan14 is the final controlled public release so the first tarball contains final npx docs and no Python/raw-index runtime.

### npm Script Provider Audit

Plan01 Task2 is the sole `package.json` owner and registers the complete exact command table before any consumer wave. `build=tsc -p tsconfig.json && tsc -p tsconfig.tests.json` is the only repository writer of `dist/`/`dist-tests/`; every `test:*` is a read-only `node --test dist-tests/<explicit-entry>.cjs` command. Maintainer commands are explicit `node dist/...cjs` entries, `generate:check` fixes `--package all --group all --check`, and `ci:local` explicitly orders deps audit → build → tests → generate check → required smoke → pack audit. A script is invoked only in the task that creates its compiled entry or a later dependent wave.

| Script family | Compiled entry/test owner | First invocation |
|---|---|---|
| `build`, `test`, `deps:audit`, `test:dependency-audit`, `test:tracer` | 01 | 01 |
| `test:transaction` | 02 | 02 |
| `test:cli` | 26 | 26 |
| `test:hook` | 03 | 03 |
| `test:launcher`, `test:host:codex`, `test:cross-host`, `test:migration` | 07 | 07 |
| `test:host:claude`, `test:host:cursor` | 04 | 04 |
| `test:update-check` | 08 | 08 |
| `test:generator`, `generate`, `generate:check`; `test:generator:repository` | 09; repository entry added by 23 | 09 temp fixtures; 23 repository-only entry/check |
| `test:precommit`, `test:pack`, `pack:audit`, `test:ci-contract`, `ci:local` | 05 | 05 |
| `test:smoke`, `smoke:required`, `smoke:live` | 06 | 06 |
| `test:release`, `test:publish-receipt`, `test:release-workflow`, `release:patch|minor|major` | 13 | 13 |
| `test:retirement-audit`, `audit:retirement`, `verify:parity-before-retire` | 15 | 15 |
| `test:docs`, `docs:check`, `test:sibling-guide-audit` | 22 | 22 |

### Mode and Flag Provider Audit

| Contract | Implemented/tested by | Consumers ordered after provider |
|---|---|---|
| CLI `--host`, `--environment`, `--target`, `--yes`, `--json`, independent `--allow-legacy-user-removal` and interactive host/legacy confirmation | 26 | 07,04,06,13,14 |
| generator `--package`, exact `--group runtime-cjs|runtime-launcher|runtime-registration|runtime-code|runtime|metadata-config|metadata-guidance|metadata|docs|version|all`, `--check`; test-only roots | 09 | 10-11,23,19,13-14 |
| smoke `--package-spec`, required/live policy | 06 | 13-15 |
| release `--yes`, `--json`, dry-run/strict temp-fixture behavior | 13 | 14 |
| publish receipt `--record`, `--verify` | 13 | 14 |
| retirement `pre`, `post-source`, `post-scripts`, `post-tests`, `post` plus `--verify-pre-receipt` | 15 | 16,24,17,25,19 |
| docs `--policy user-docs|project-instructions|planning|sibling-guide` scoped policies | 22 | 20,19,21 |
| sibling `--capture-baseline`, `--record-receipt`, `--baseline`, `--verify-receipt`, `--verify-summary ... --receipt ...` | 22 | 21 |

Temporal assertions: Plan09 never checks the real product trees; Plan23 is the first real all-product generator gate after Plans10/11 and proves all other live groups are empty-write checks. Plan15 creates/verifies the pre-retirement receipt; Plan30 removes only the five exact ignored cache roots after that final Python invocation and before Plan16 deletes source. No partial-deletion plan invokes final `post`; Plan24 runs `post-source`, Plan17 `post-scripts`, Plan25 `post-tests`, and only Plan19 runs the unique full-repository `post` audit after Plan20 reconciliation.

### Exact Mutable-Set Audit

| Operation | Declared actual writes | Equality evidence |
|---|---|---|
| QA/Dev runtime generation | each Plan10/11 owns exactly three new `runtime-cjs` paths then two changed `runtime-launcher` paths | live-byte classification + Plan09 changed-subset fixtures + per-task observed-write equality |
| Existing registration/metadata/Cursor assets | zero writes; QA/Dev registration+metadata and Cursor metadata are read-only canonical checks | Plan23 before/after bytes/mtime/tree digest equality and empty observed writes |
| README generation | three product READMEs only | Plan19 `docs` group; other bytes unchanged |
| Release bump | package.json, package-lock.json, QA/Dev four host manifests, Cursor manifest; receipt is separate eighth path | Plan13 temp-fixture write/stage/commit equality + Plan14 live equality |
| Cursor legacy migration | project Cursor managed set plus only state-listed exact-owned user files/state | Plan04 authority, digest, environment and dual-rollback fixtures |
| pre-commit | no index writes; check-only by default | Plan05 whole-index bytes + partial-stage blob/working-copy equality |
| npm dependency/build outputs | `.gitignore` changes only; `/node_modules/`, `/dist/`, `/dist-tests/` root rules | Plan29 positive and negative ignore probes; Plan05/13 clean-status assertions |
| Python cache cleanup | only five enumerated `__pycache__` roots after Plan15 | Plan30 canonical-path/reparse/content preflight, exact deletion, root-external status/digest equality |

## Deferred / Excluded

- OpenCode adapter and real-host UI/runtime evidence remain deferred exactly as CONTEXT says. Phase03.1 delivers the adapter seam and loopback contract only; revised Phase6 uses npx for real-host validation.
- Production identity, HTTPS and credential rotation remain Phase8.
- Phase5 hook precision policy is not mixed into behavior-preserving runtime migration.

All GOAL, JS-01..JS-08, RESEARCH constraints, and D-01..D-18 are covered. No source item is missing.

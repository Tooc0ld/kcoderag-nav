---
phase: 03.1-javascript-npx
plan: 24
type: execute
wave: 19
depends_on: ["03.1-16"]
files_modified:
  - kcoderag-dev/hooks/grep_nudge.py
  - kcoderag-dev/hooks/update_check.py
  - kcoderag-dev/hooks/test_grep_nudge.py
  - kcoderag-update.json
autonomous: true
requirements: [JS-01, JS-05, JS-06]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Dev legacy Python and root GitHub-raw update index retire together, completing post-source state."
    - "Only after all source/runtime/index deletions does repository generate:check run again."
  artifacts:
    - {path: "kcoderag-dev/hooks/", provides: "Dev CJS-only hook tree"}
  key_links:
    - {from: "src/hooks/update-worker.cts", to: "npm Registry latest", via: "replacement for removed raw index", pattern: "dist-tags.latest"}
---
<objective>完成剩余 Dev/runtime 与 `kcoderag-update.json` 退役，然后首次进入并验证 post-source 状态。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-16-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 删除 Dev Python 与 GitHub-raw update index</name><files>kcoderag-dev/hooks/grep_nudge.py, kcoderag-dev/hooks/update_check.py, kcoderag-dev/hooks/test_grep_nudge.py, kcoderag-update.json</files><action>精确删除四个 tracked paths。确认 Node generator 不再生产 root update index，CJS worker 只读取 npm Registry latest；用 scoped source/runtime scan拒绝 active raw.githubusercontent、`kcoderag-update.json`、update-index producer/consumer references。不要修改 docs/planning history。</action><verify><automated>npm run test:update-check &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- four paths absent.- shipped/source scope has no GitHub-raw/update-index logic.</acceptance_criteria><done>source/runtime/update-index migration complete。</done></task>
<task type="auto"><name>Task 2: 验证 post-source 并恢复全产品生成门</name><files>None (read-only state and generation checks)</files><action>现在所有 managed source/runtime/index deletions 已完成，运行 `audit:retirement --mode post-source` 和 repository `generate:check`；before/after digests 必须相同。post-source 允许 maintainer scripts/tests 暂存，但不允许上述 source/runtime/index 类残留。</action><verify><automated>npm run audit:retirement -- --mode post-source &amp;&amp; npm run generate:check &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- post-source and all-product generation check pass.- no tree mutation.</acceptance_criteria><done>retirement state advances exactly once to post-source。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-24-01 | Tampering | update source | high | mitigate | exact deletion and scoped raw/index negative gate. |
| T-03.1-24-02 | Denial of Service | Dev runtime | high | mitigate | CJS/update/launcher tests before stage advance. |
</threat_model>
<verification>`npm run audit:retirement -- --mode post-source &amp;&amp; npm run generate:check`</verification>
<success_criteria>- Dev and raw update-index remnants are gone.- post-source state/generation gate passes only after full source deletion.</success_criteria>
## Artifacts this phase produces
- Removed Dev Python runtime/tests and root `kcoderag-update.json`; post-source receipt in summary.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-24-SUMMARY.md` when done.</output>

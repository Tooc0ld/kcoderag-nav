---
phase: 03.1-javascript-npx
plan: 26
type: execute
wave: 7
depends_on: ["03.1-08"]
files_modified:
  - src/hosts/host-adapter.cts
  - src/cli/commands.cts
  - src/bin/kcoderag-nav.cts
  - src/tracer/codex-install.cts
  - tests/cli/commands.test.cts
autonomous: true
requirements: [JS-02, JS-03, JS-04]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-04/D-14: one selected HostAdapter per command; OpenCode remains an unregistered seam."
    - "D-11/D-15/D-16: CLI confirmation/runtime/JSON behavior uses Plan02 core without host-path branches."
  artifacts:
    - {path: "src/hosts/host-adapter.cts", provides: "pure host adapter interface", exports: ["HostAdapter"]}
    - {path: "src/cli/commands.cts", provides: "five-command controller", exports: ["executeCommand"]}
  key_links:
    - {from: "src/cli/commands.cts", to: "src/core/transaction.cts", via: "adapter DesiredState", pattern: "applyTransaction"}
---
<objective>在 Plan02 核心上抽取 HostAdapter 和五命令控制器，并创建 Plan01 已注册的 compiled CLI test entry。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-02-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 提炼纯 HostAdapter 与 CLI controller</name><files>src/hosts/host-adapter.cts, src/cli/commands.cts, src/bin/kcoderag-nav.cts, src/tracer/codex-install.cts</files><action>定义 `detect/renderInstall/renderUninstall/status` 纯接口；CLI 处理 install/status/doctor/update/uninstall、interactive codex/claude/cursor、JSON 下显式 host/`--yes`、runtime gate 与安全输出。加入单独的 `--allow-legacy-user-removal` authority bit：它不能由 `--yes` 推导，非 Cursor legacy 路径拒绝该 flag，实际删除仍由 adapter 在 exact ownership preflight 后决定。将 tracer 接入共享 transaction 后删除重复实现，但保持 Plan01 flags/schema。</action><verify><automated>npm run build</automated></verify><acceptance_criteria>- core 不按 host 分支，adapter 不写盘。- 无 host JSON mutation 零写入。- legacy user-removal authority 独立、显式且不可被通用确认隐含。</acceptance_criteria><done>D-04/D-05/D-14 的通用接线完成。</done></task>
<task type="auto" tdd="true"><name>Task 2: 创建 compiled CLI decision tests</name><files>tests/cli/commands.test.cts</files><action>覆盖五命令、interactive/JSON、取消、cwd/target、Node&lt;22 mutating rejection、status/doctor只读、stable stdout/stderr/exit codes，以及 `--yes` 不等于 legacy removal authority、显式 flag 只向 Cursor adapter 传递。tsconfig 将测试编译到 Plan01 `test:cli` 指向的 CJS entry。</action><verify><automated>npm run build &amp;&amp; npm run test:cli &amp;&amp; npm run test:tracer</automated></verify><acceptance_criteria>- `test:cli` entry 存在且全 cases 实际运行。- 输出不含 MCP 值。- implicit/host-mismatched legacy authority fixtures 非零且不调用 mutation。</acceptance_criteria><done>Plan02/26 shared CLI behavior 可由后续 adapters 复用。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-26-01 | Elevation of Privilege | host dispatch | high | mitigate | exact registry selection and one adapter transaction. |
| T-03.1-26-02 | Information Disclosure | CLI output | high | mitigate | code/path-only JSON and synthetic fixtures. |
</threat_model>
<verification>`npm run build &amp;&amp; npm run test:cli &amp;&amp; npm run test:tracer`</verification>
<success_criteria>- HostAdapter/CLI compiled entries exist and execute through Plan02 core.</success_criteria>
## Artifacts this phase produces
- New symbols: `HostAdapter`, `executeCommand`; compiled `test:cli` entry.
- Public commands/flags completed here: `install`, `status`, `doctor`, `update`, `uninstall`, interactive host selection, `--host`, `--environment`, `--target`, `--yes`, `--json`, `--allow-legacy-user-removal`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-26-SUMMARY.md` when done.</output>

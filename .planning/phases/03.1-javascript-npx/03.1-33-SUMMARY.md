---
phase: 03.1-javascript-npx
plan: 33
subsystem: public-release
tags: [nodejs, npm, github-actions, provenance, supply-chain, three-host-smoke]

requires:
  - phase: 03.1-31
    provides: Closed Node-only npm tarball inventory that excludes the repository-only retirement auditor
  - phase: 03.1-32
    provides: Exact/latest acquisition provenance and closed schema-v2 release receipt contracts
  - phase: 03.1-14
    provides: Immutable historical 0.1.6 release evidence retained without reinterpretation
provides:
  - Public kcoderag-nav 0.1.7 and npm latest bound to the final reviewed release commit
  - Four-lane Ubuntu/Windows Node 22/24 Release workflow evidence with dependent publish success
  - Sanitized schema-v2 exact/latest registry and three-host lifecycle evidence
affects: [phase-verification, public-npx-install, release-provenance, phase-06-real-host-evidence]

actuals:
  tokens: 1756
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - A one-way release is authorized by exact candidate plus frozen snapshot digest and starts only after byte-identical replay
    - Public evidence binds immutable tag, release SHA, workflow identity, registry gitHead, and dual-specifier lifecycle provenance

key-files:
  created:
    - .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
  modified:
    - package.json
    - package-lock.json
    - kcoderag-qa/.codex-plugin/plugin.json
    - kcoderag-qa/.claude-plugin/plugin.json
    - kcoderag-dev/.codex-plugin/plugin.json
    - kcoderag-dev/.claude-plugin/plugin.json
    - kcoderag-cursor/.cursor-plugin/plugin.json

key-decisions:
  - "Authorized only candidate 0.1.7 against frozen preflight SHA-256 7d33e72da9a60ab4c4b1abd78cfb6b3ff6122edeacb61cb3adbbe55cce15105c; the complete replay was byte-identical and field-identical before mutation."
  - "Used only the audited patch helper to create release commit af38e03a3243777c1c00bdc5fb0c5a9f347780b6 and immutable tag v0.1.7 as a direct child of pre-release SHA 8b4f0e5dbdda3f8d9825a68eedde69f9527c1e15."
  - "Accepted only Release run 32715408403 because its workflow path, tag ref, head SHA, push event, four required lanes, dependent publish job, and success conclusion matched the authorized release."
  - "Kept 0.1.6 and its schema-v1 receipt historical and unchanged; 0.1.7 receives a separate schema-v2 receipt."

patterns-established:
  - "Frozen replay gate: any candidate, SHA, CI, registry, tag, workflow-state, or secret-name drift revokes release authority before the helper runs."
  - "Dual public smoke evidence: exact and literal latest each acquire independently, retain installed-manifest identity plus synthetic lifecycle tgz digest, and require all three hosts with every evidence bit true."

requirements-completed: [JS-02, JS-04, JS-07, JS-08]

coverage:
  - id: D1
    description: "The final summarized source passed ordinary four-lane CI, replayed the authorized preflight byte-for-byte, and became an exact helper-owned seven-path release commit and matching immutable tag."
    requirement: JS-02
    verification:
      - kind: integration
        ref: "CI run 32708593380 — exact pre-release SHA and Ubuntu/Windows Node 22/24 required lanes succeeded"
        status: pass
      - kind: integration
        ref: "npm run release:patch -- --dry-run --json followed by authorized --yes --json — candidate 0.1.7, seven paths, direct parent, and matching v0.1.7 tag"
        status: pass
    human_judgment: false
  - id: D2
    description: "Release workflow 32715408403 gated publication on all four supported OS/Node lanes, and registry exact/latest metadata converged on release gitHead af38e03a."
    requirement: JS-07
    verification:
      - kind: integration
        ref: "GitHub Actions Release run 32715408403 — Release workflow, v0.1.7, exact head SHA, four lanes and publish all completed/success"
        status: pass
      - kind: integration
        ref: "npm exact/latest metadata window — version 0.1.7, gitHead af38e03a3243777c1c00bdc5fb0c5a9f347780b6, expected bin/engine/repository"
        status: pass
    human_judgment: false
  - id: D3
    description: "Fresh exact-version and literal-latest acquisitions each passed complete Codex, Claude Code, and Cursor required-contract lifecycles with closed schema-v2 offline evidence."
    requirement: JS-04
    verification:
      - kind: e2e
        ref: "host-smoke required-contract for kcoderag-nav@0.1.7 — three hosts PASS, 11/11 evidence, digest 1a235c6f0a6eaae736f1e220593072539159da659407ced35d6fbc20959d4b74"
        status: pass
      - kind: e2e
        ref: "host-smoke required-contract for kcoderag-nav@latest — three hosts PASS, 11/11 evidence, digest 7d7c929948dd58c000be6d864b7b7c95539665094f2ed815d78a9fd33a2ad0b7, no latest race"
        status: pass
      - kind: integration
        ref: "node dist/maintainer/publish-receipt.cjs --verify 03.1-33-PUBLISH-RECEIPT.json"
        status: pass
    human_judgment: false

duration: 1h 38m
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 33: Matrix-Gated Public 0.1.7 Release Summary

**Public `kcoderag-nav@latest` is now immutable release `0.1.7`, bound from the final reviewed pre-release SHA through four required OS/Node lanes to registry `gitHead` and fresh exact/latest three-host lifecycle evidence.**

## Performance

- **Duration:** 1h 38m
- **Started:** 2026-08-24T08:45:09Z
- **Completed:** 2026-08-24T10:22:38Z
- **Tasks:** 3
- **Files modified:** 8 execution artifacts

## Accomplishments

- Froze and replayed the complete version-specific preflight for candidate `0.1.7`; authorized and replayed snapshot bytes both hash to `7d33e72da9a60ab4c4b1abd78cfb6b3ff6122edeacb61cb3adbbe55cce15105c`.
- Created release commit `af38e03a3243777c1c00bdc5fb0c5a9f347780b6` with exactly the root package/lock and five product manifests, then pushed its immutable `v0.1.7` tag.
- Accepted Release run `32715408403` only after all Ubuntu/Windows Node 22/24 lanes and the dependent publish job completed successfully.
- Confirmed npm exact and latest both resolve to `0.1.7` with `gitHead` equal to the release commit and expected bin, Node engine, and repository metadata.
- Ran isolated public exact/latest required-contract smokes; Codex, Claude Code, and Cursor each passed all eleven lifecycle/navigation/MCP evidence checks.
- Recorded and offline-verified a closed schema-v2 receipt while leaving historical `0.1.6` evidence byte-identical.

## Task Commits

1. **Task 1: Push final summarized source through four-lane CI and freeze release preflight** — verification/remote evidence only; no repository commit.
2. **Task 2: Authorize one exact new immutable patch release** — no commit; the user explicitly authorized `0.1.7` with the exact frozen snapshot digest.
3. **Task 3: Publish and record lane-bound exact/latest evidence**
   - `af38e03` — audited helper-created seven-path `release: v0.1.7` commit and matching tag
   - `0bb3c30` — sanitized schema-v2 registry/workflow/dual-smoke receipt

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `package.json`, `package-lock.json`, and the five QA/Dev/Cursor product manifests — exact helper-owned version synchronization to `0.1.7`.
- `.planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json` — closed release/workflow/registry/exact/latest three-host evidence.

## Decisions Made

- Treated the user's authority as candidate-and-digest specific. The release helper did not run until the replay recreated the frozen snapshot exactly.
- Kept helper, workflow, and evidence authorities separate: the helper alone created commit/tag; GitHub Actions alone published; the receipt only reduced accepted public metadata.
- Retained `0.1.6` and its schema-v1 receipt as immutable historical evidence rather than attributing later fixes to it.
- Preserved authenticated real-host evidence as Phase 6 work; this plan records required loopback lifecycle/MCP evidence only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - CLI compatibility] Reduced GitHub job evidence through the REST metadata endpoint**
- **Found during:** Task 3 Release workflow verification
- **Issue:** The installed `gh workflow view` did not support the planned JSON flag, and an initial `gh run view` job query expanded nested nonsecret step metadata beyond the intended reduced fields.
- **Fix:** Re-queried workflow/run/jobs through `gh api` and accepted only workflow identity/path/state, tag/SHA/event/status/conclusion, the four required job conclusions, and publish conclusion. No logs or secret values were read or persisted.
- **Files modified:** None
- **Verification:** The reduced result contained exactly Release run `32715408403` and five completed/success required jobs; the closed receipt verifier rejected extra fields by construction.
- **Committed in:** No source commit; evidence-only correction before receipt creation.

---

**Total deviations:** 1 auto-fixed (1 Rule 3 CLI compatibility correction).
**Impact on plan:** Release provenance and secret boundaries remained intact; accepted and persisted evidence is the exact closed allow-list required by schema v2.

## Authentication Gates

- GitHub CLI authentication was already valid from Task 1. Repository secret readiness was rechecked by the exact name `NPM_TOKEN` only; no value was read.

## Issues Encountered

- The first combined replay command was rejected by command policy before execution. The same fail-closed replay was split into smaller read-only queries and then matched the frozen bytes exactly before any mutation.
- A post-helper PowerShell path-comparison expression reported a false mismatch after the helper had succeeded. Independent read-only checks confirmed the direct parent, exact seven paths, aligned versions, clean index, and matching tag without rerunning or altering the release.
- Two PowerShell-generated receipt candidates were rejected as `invalid_receipt_schema` before any receipt file was written. A canonical Node JSON producer was piped into the audited recorder, after which offline verification passed.
- A final verification attempt launched exact and latest public smokes concurrently and both returned nonzero. Sequential reruns immediately passed for all three hosts, confirming the parallel acquisition attempt—not the package contract—was the transient failure mode.

## User Setup Required

None. `kcoderag-nav@0.1.7` is public and `npx kcoderag-nav@latest install` resolves without private npm credentials.

## Next Phase Readiness

- Phase 03.1 public-delivery gaps are closed with immutable registry, tag, workflow-lane, and dual public-package evidence.
- The phase verifier can now re-evaluate JS-01 through JS-08 against public `0.1.7` rather than historical `0.1.6`.
- Authenticated real Codex, Claude Code, and Cursor evidence remains explicitly owned by Phase 6.

## Known Stubs

None.

## Self-Check: PASSED

- Release commit `af38e03a3243777c1c00bdc5fb0c5a9f347780b6` exists, is the direct child of pre-release SHA `8b4f0e5dbdda3f8d9825a68eedde69f9527c1e15`, changes exactly seven declared version paths, and is the local/remote target of `v0.1.7`.
- Ordinary CI run `32708593380` remains completed/success for the exact pre-release SHA with all four required lanes; Release run `32715408403` remains completed/success for the exact tag/release SHA with four required lanes plus publish.
- Fresh registry queries report exact and latest `0.1.7`, release `gitHead`, and expected bin/engine/repository metadata.
- Fresh sequential exact-version and latest required-contract smokes each passed Codex, Claude Code, and Cursor with all eleven evidence fields true; the immediate latest re-query remained race-free.
- `03.1-33-PUBLISH-RECEIPT.json` exists and passes the offline schema-v2 verifier; historical schema-v1 receipt object `515da8382823bfc6035c3eb930272f582d02f3f1` is unchanged.
- Coverage classification reports 3/3 deliverables fully auto-covered and passing. Modified execution artifacts contain no incomplete stub or skipped-test marker.
- STATE, ROADMAP, and REQUIREMENTS remain untouched for the orchestrator's post-verifier phase-completion boundary.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

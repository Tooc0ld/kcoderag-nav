---
phase: 03.1-javascript-npx
plan: 06
subsystem: host-smoke-ci
tags: [nodejs, npm-exec, mcp, loopback, codex, claude-code, cursor, github-actions]

requires:
  - phase: 03.1-04
    provides: Complete Codex, Claude Code, and Cursor project-native adapters and lifecycle
  - phase: 03.1-05
    provides: Required Windows/Linux Node 22/24 CI matrix and exact script-policy audit
provides:
  - Honest PASS/FAIL/NOT_RUN smoke verdicts with eleven explicit evidence bits per host
  - Metadata-only loopback MCP initialize/tools-list/tools-call receipts
  - Real temporary npm-exec install/status/update/uninstall coverage for all three hosts
  - Required smoke enforcement in local CI and every Windows/Linux Node 22/24 lane
affects: [03.1-13, 03.1-14, release, npm-publish, real-host-evidence]

actuals:
  tokens: 13289
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - Package acquisition and synthetic loopback preparation finish before any host target exists
    - Required smoke passes only when every lifecycle, navigation, MCP, and receipt bit is true
    - Optional live smoke preserves NOT_RUN as a distinct non-PASS outcome

key-files:
  created:
    - src/smoke/host-smoke.cts
    - src/smoke/stub-mcp-server.cts
    - tests/smoke/host-smoke.test.cts
  modified:
    - .github/workflows/ci.yml
    - package.json
    - src/maintainer/dependency-audit.cts
    - tests/maintainer/ci-contract.test.cts
    - tests/maintainer/dependency-audit.test.cts

key-decisions:
  - "Required contract smoke acquires and invokes a real temporary npm package, then proves eleven evidence bits for Codex, Claude Code, and Cursor before reporting PASS."
  - "The loopback server records only path, method, tool name, and request id; headers, arguments, configuration, and credentials never enter receipts or result JSON."
  - "Optional authenticated smoke is isolated behind workflow_dispatch plus a self-hosted runner variable and cannot replace required contract smoke."
  - "Package acquisition completes before creating host project roots, so an npm or network failure cannot partially install a target."

patterns-established:
  - "Smoke verdict: required FAIL and NOT_RUN exit nonzero; optional NOT_RUN exits zero; every incomplete executed run is FAIL."
  - "MCP proof: initialize, tools/list, tools/call, expected search_code registration, and a metadata-only stub receipt are independent required bits."
  - "CI proof: smoke:required is part of ci:local and the shared required matrix job, without continue-on-error or artifact uploads."

requirements-completed: [JS-01, JS-08]

coverage:
  - id: D1
    description: "Codex, Claude Code, and Cursor required contract smoke performs real npm package acquisition and complete project lifecycle with honest evidence."
    requirement: JS-08
    verification:
      - kind: unit
        ref: "tests/smoke/host-smoke.test.cts — evidence matrix, optional tri-state, receipt redaction, acquisition-first cases; 4/4 passing"
        status: pass
      - kind: e2e
        ref: "npm run smoke:required — all three hosts PASS with 11/11 evidence bits"
        status: pass
    human_judgment: false
  - id: D2
    description: "The Node smoke path drives a bounded loopback MCP server through initialize, tools/list, and search_code tools/call without exposing request or credential data."
    requirement: JS-01
    verification:
      - kind: integration
        ref: "tests/smoke/host-smoke.test.cts — real HTTP round trip and exact metadata-only receipt schema"
        status: pass
      - kind: other
        ref: "npm run ci:local — complete Node suite 111/111, generation check, 56-entry pack audit, final smoke PASS"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every required Windows/Linux Node 22/24 lane runs smoke:required, while optional live smoke remains explicitly gated and non-authoritative."
    requirement: JS-08
    verification:
      - kind: unit
        ref: "tests/maintainer/ci-contract.test.cts — required order and isolated live gate; 5/5 passing"
        status: pass
      - kind: integration
        ref: "npm run deps:audit && npm run test:dependency-audit — exact ci:local and smoke script policy; 7/7 passing"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-24
status: complete
---

# Phase 03.1 Plan 06: Honest Three-Host Smoke and Required CI Summary

**Real npm-exec lifecycle smoke now requires complete navigation and loopback MCP receipts for Codex, Claude Code, and Cursor in every required CI lane.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-24T01:00:10+08:00
- **Completed:** 2026-08-24T01:08:32+08:00
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Added a Node smoke runner that acquires a real local tgz or exact public package with npm, synthesizes only temporary loopback connection inputs, and runs install/status/update/uninstall through `npm exec` in fresh projects.
- Added a bounded localhost MCP stub whose initialize, tools/list, and tools/call receipts contain metadata only; required PASS also needs the expected tool name plus Codex/Claude hook or Cursor Rule evidence.
- Restored `smoke:required` to `ci:local`, the exact dependency-audit policy, and all four Windows/Linux Node 22/24 required lanes; optional authenticated smoke is a separate explicitly gated self-hosted job.

## Task Commits

Task 1 followed TDD RED/GREEN order; Task 2 was committed atomically with its policy tests:

1. **Task 1 RED: Specify honest host smoke evidence** - `35f42af` (test)
2. **Task 1 GREEN: Implement honest three-host smoke runner** - `ab9ebd0` (feat)
3. **Task 2: Require honest host smoke in every lane** - `57ae43a` (ci)

**Plan metadata:** committed separately after this SUMMARY and state are verified.

## Files Created/Modified

- `src/smoke/host-smoke.cts` - Three-state evaluator, real package acquisition, npm-exec lifecycle driver, host navigation proof, optional live runner, and safe aggregate output.
- `src/smoke/stub-mcp-server.cts` - Loopback-only bounded HTTP MCP service and metadata-only JSONL receipt reader.
- `tests/smoke/host-smoke.test.cts` - Evidence-bit matrix, optional verdict semantics, actual MCP round trip, receipt redaction, and acquisition-before-target proof.
- `.github/workflows/ci.yml` - Required contract smoke in the four-lane matrix plus separately gated optional authenticated smoke.
- `package.json` - Canonical mode names and required smoke restored to `ci:local`.
- `src/maintainer/dependency-audit.cts` - Exact local-CI and smoke script policy.
- `tests/maintainer/ci-contract.test.cts` - Required smoke order, strict failure policy, and optional self-hosted gate contract.
- `tests/maintainer/dependency-audit.test.cts` - Negative fixtures for removed or swapped smoke policies.

## Decisions Made

- Used a repacked temporary copy of the acquired npm package with synthetic loopback configuration. The smoke never reads or prints production MCP values, while the CLI and packaged runtime bytes remain the actual acquired implementation.
- Kept every evidence bit independent. A tool name, launcher success, or natural-language claim cannot stand in for a real MCP request receipt.
- Made Cursor required proof use its real Rule/skill/MCP contract rather than pretending Cursor has a PreToolUse hook; optional live Cursor remains honestly NOT_RUN because no supported headless host command exists.
- Kept optional authenticated execution non-required and operator-controlled; required loopback smoke remains deterministic and credentials-free.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reconciled the deferred Plan 05 local-CI policy when the runner became available**
- **Found during:** Task 2 (required CI integration)
- **Issue:** Plan 05 intentionally removed the not-yet-implemented smoke command from `ci:local` and the dependency-audit exact policy so that its own verification could run.
- **Fix:** Added `smoke:required` back to `ci:local`, synchronized the exact dependency audit, and added negative policy fixtures as explicitly required by the Plan 05 handoff.
- **Files modified:** `package.json`, `src/maintainer/dependency-audit.cts`, `tests/maintainer/dependency-audit.test.cts`
- **Verification:** `npm run deps:audit`, 7/7 dependency-audit tests, and the full `npm run ci:local` all passed.
- **Committed in:** `57ae43a`

---

**Total deviations:** 1 auto-fixed (1 Rule 3 blocker)
**Impact on plan:** The fix closes the intentionally deferred Plan 05 seam and makes required local/remote CI policy complete; it adds no publication authority or external state change.

## Issues Encountered

- The repository still has no optional `scripts/ci_monitor.cjs` helper named by the GitHub workflow skill. Workflow changes were grounded in current official GitHub Actions syntax, and the local CI contract tests enforce the resulting job/matrix/gate structure.

## User Setup Required

None - required smoke is offline and credential-free. Optional authenticated live smoke runs only when an operator explicitly enables the self-hosted workflow-dispatch gate on a runner that already has host authentication.

## Next Phase Readiness

- Plan 03.1-13 can reuse `smoke:required` in release preparation without treating optional live evidence as authoritative.
- Plan 03.1-14 can pass `--package-spec kcoderag-nav@X.Y.Z` to verify the exact public release after publication.
- Phase 6 still owns real-host evidence policy; Cursor optional live remains an explicit NOT_RUN until a supported headless execution path exists.

## Known Stubs

None. Cursor optional-live `headless_host_unsupported` is an intentional honest verdict, not a passing placeholder; required Cursor contract evidence is complete.

## Self-Check: PASSED

- All eight production/test/config artifacts and this SUMMARY exist.
- Task commits `35f42af`, `ab9ebd0`, and `57ae43a` exist with no file deletions.
- Fresh overall verification passed: smoke tests 4/4, complete Node tests 111/111, deterministic generation, 56-entry real npm tgz audit, and three-host required smoke with every evidence bit true.

---
*Phase: 03.1-javascript-npx*
*Completed: 2026-08-24*

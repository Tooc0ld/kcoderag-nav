---
phase: 03.1-javascript-npx
plan: 23
type: execute
wave: 7
depends_on: ["03.1-12", "03.1-27", "03.1-28"]
files_modified:
  - tests/generator/repository-generation.test.cts
autonomous: true
requirements: [JS-01, JS-06]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Real repository generate:check runs only after QA, Dev and Cursor writers complete."
    - "All three product trees are deterministic, version-consistent and self-contained together."
  artifacts:
    - {path: "tests/generator/repository-generation.test.cts", provides: "post-generation repository integration gate"}
  key_links:
    - {from: "src/generator/index.cts", to: "kcoderag-qa|dev|cursor", via: "all-product read-only check", pattern: "checkGenerated"}
---
<objective>在三份真实生成树全部更新后，创建并运行唯一的 repository-wide generation integration gate。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-12-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-27-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-28-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 创建真实仓库生成整合测试</name><files>tests/generator/repository-generation.test.cts</files><action>测试读取三个完成后的 product trees，断言 package version、环境 namespace、CJS/launcher/MCP/skill/Rule/manifest 自包含关系与无 unresolved token；before/after tree digest 证明 check-only。不得重写生成物。</action><verify><automated>npm run build &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- missing/stale asset fixture fails.- check test does not mutate repository.</acceptance_criteria><done>真实三产品关系有 compiled integration test。</done></task>
<task type="auto"><name>Task 2: 执行 all-product repository check</name><files>None (read-only check of completed generated trees)</files><action>现在才运行 `generate:check`；先记录三个 tree digest，执行后比较不变，并再次跑 repository integration test。任何 drift 非零且不自动 generate。</action><verify><automated>npm run generate:check &amp;&amp; npm run test:generator</automated></verify><acceptance_criteria>- repository all-product check passes only after Plans10-12/27-28.- no generated file changes.</acceptance_criteria><done>JS-06 real repository determinism gate closed without premature failure。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-23-01 | Repudiation | generated parity | medium | mitigate | compiled relation assertions and digest before/after. |
| T-03.1-23-02 | Tampering | check mode | high | mitigate | read-only test and full tree digest. |
</threat_model>
<verification>`npm run generate:check &amp;&amp; npm run test:generator`</verification>
<success_criteria>- Full repository generation check runs at the correct dependency point and is read-only.</success_criteria>
## Artifacts this phase produces
- New compiled repository generation test entry; all-product `generate:check` receipt in summary.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-23-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 23
type: execute
wave: 11
depends_on: ["03.1-10", "03.1-11"]
files_modified:
  - tests/generator/repository-generation.test.cts
autonomous: true
requirements: [JS-01, JS-06]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Real repository generate:check runs only after QA/Dev guaranteed-changing runtime writers complete."
    - "已 byte-equal 的 QA/Dev hook registration、QA/Dev metadata 与全部 Cursor assets 只做 check-only；observed write set 必须为空。"
    - "All three product trees are deterministic, version-consistent and self-contained together."
  artifacts:
    - {path: "tests/generator/repository-generation.test.cts", provides: "post-generation repository integration gate"}
  key_links:
    - {from: "src/generator/index.cts", to: "kcoderag-qa|dev|cursor", via: "all-product read-only check", pattern: "checkGenerated"}
---
<objective>在 QA/Dev 真实 runtime 写入后，建立唯一 repository gate：对已存在 byte-equal 资产只读校验，再运行 all-product/all-group check。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-10-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-11-SUMMARY.md
@kcoderag-qa/hooks/hooks.json
@kcoderag-dev/hooks/hooks.json
@kcoderag-cursor/.cursor-plugin/plugin.json</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 创建真实仓库生成整合测试</name><files>tests/generator/repository-generation.test.cts</files><action>测试读取三个 product trees，断言 package version、环境 namespace、QA/Dev CJS+launcher 与既有 registration/MCP/skill/agent、Cursor manifest/MCP/Rule/skill、三 README 的自包含关系与无 unresolved token；before/after tree digest 证明 check-only。不得重写生成物。Plan01 的 `test:generator:repository` 只运行本文件，避免 Plan09 在此文件创建前调用 stale/missing compiled entry。</action><verify><automated>npm run build &amp;&amp; npm run test:generator:repository</automated></verify><acceptance_criteria>- missing/stale asset fixture fails.- compiled repository entry实际运行且不 mutate repository.</acceptance_criteria><done>真实三产品关系有独立 compiled integration test。</done></task>
<task type="auto"><name>Task 2: 只读校验 live byte-equal asset groups</name><files>None (read-only checks of existing generated assets)</files><action>在写前 live-byte audit 已确认以下资产与 canonical render 应保持 byte-equal：QA/Dev `runtime-registration`、QA/Dev `metadata-config`/`metadata-guidance`、Cursor `metadata-config`/`metadata-guidance`。逐组保存 path bytes/mtime/tree digest，分别运行 `generate --check`；observed writes 必须为空，前后证据 exact equal。不得先运行 write-mode 来制造变更，也不把这些路径列入任何计划的 files_modified；任何 drift 直接失败并由执行者审查原因。</action><verify><automated>npm run generate -- --package qa --group runtime-registration --check &amp;&amp; npm run generate -- --package dev --group runtime-registration --check &amp;&amp; npm run generate -- --package qa --group metadata --check &amp;&amp; npm run generate -- --package dev --group metadata --check &amp;&amp; npm run generate -- --package cursor --group metadata --check</automated></verify><acceptance_criteria>- all listed groups pass with empty observed write set and unchanged bytes/mtime/digests.- no no-op asset is claimed as a write by Plans10/11/23.</acceptance_criteria><done>live no-op assets are proven canonical without undeclared writes。</done></task>
<task type="auto"><name>Task 3: 执行 all-product/all-group repository check</name><files>None (read-only check of completed generated trees)</files><action>现在才运行 Plan01 固定的 `generate:check=node dist/generator/index.cjs --package all --group all --check`；先记录三个 tree digests，执行后比较不变，并运行独立 repository integration test。任何 drift 非零且不自动 generate。</action><verify><automated>npm run generate:check &amp;&amp; npm run test:generator:repository</automated></verify><acceptance_criteria>- all-product/all-group check只在 Plans10/11 后执行。- no generated file changes.</acceptance_criteria><done>JS-06 real repository determinism gate closed without premature/no-op writes。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-23-01 | Repudiation | generated parity | medium | mitigate | compiled relation assertions and digest before/after. |
| T-03.1-23-02 | Tampering | check mode | high | mitigate | read-only test and full tree digest. |
</threat_model>
<verification>`npm run generate:check &amp;&amp; npm run test:generator:repository`，且所有 declared no-op group 前后 digests 相同。</verification>
<success_criteria>- Full repository generation check runs at the correct dependency point and is read-only.</success_criteria>
## Artifacts this phase produces
- New compiled repository generation test entry; all-product `generate:check` receipt in summary.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-23-SUMMARY.md` when done.</output>

---
phase: 03.1-javascript-npx
plan: 17
type: execute
wave: 19
depends_on: ["03.1-24"]
files_modified:
  - scripts/manage_project_install.py
  - scripts/manage_cursor_local_install.py
  - scripts/generate_plugins.py
  - scripts/update_plugin.py
  - scripts/pre_commit_generate.py
  - scripts/run_host_smoke.py
  - scripts/__init__.py
autonomous: true
requirements: [JS-01, JS-02, JS-03, JS-06, JS-08]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-01/D-03: parity gate 后所有维护者安装、Cursor、生成、更新、pre-commit、smoke 入口不再依赖 Python。"
    - "每组只删除已由明确 npm command 与 Node test 替代的 tracked 文件。"
  artifacts:
    - {path: "src/cli/commands.cts", provides: "项目安装/更新替代入口"}
    - {path: "src/generator/index.cts", provides: "生成器替代入口"}
    - {path: "src/smoke/host-smoke.cts", provides: "smoke 替代入口"}
  key_links:
    - {from: "package.json scripts", to: "dist/**/*.cjs", via: "Node maintenance commands", pattern: "dist/"}
---
<objective>按职责分组删除七个已被 Node 命令替代的 Python maintainer scripts。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-24-SUMMARY.md</context>
<tasks>
<task type="auto"><name>Task 1: 退役安装、Cursor 与生成 Python 入口</name><files>scripts/manage_project_install.py, scripts/manage_cursor_local_install.py, scripts/generate_plugins.py</files><action>确认 Plan15 summary 的对应 CLI/generator suites 已运行，再只删除三文件。扫描 package/hooks/docs 当前执行入口，确保不再引用它们；旧行为仍由 Node lifecycle/generator tests 覆盖。</action><verify><automated>npm run test:cli &amp;&amp; npm run test:cross-host &amp;&amp; npm run generate:check</automated></verify><acceptance_criteria>- 三文件无调用引用。- 三宿主 lifecycle/generator 全绿。</acceptance_criteria><done>项目/Cursor/生成维护入口只剩 Node。</done></task>
<task type="auto"><name>Task 2: 退役更新、pre-commit 与 smoke Python 入口</name><files>scripts/update_plugin.py, scripts/pre_commit_generate.py, scripts/run_host_smoke.py</files><action>确认 npm update、Node pre-commit 和 smoke required 入口已接线后只删除三文件；更新 `.githooks`/CI 引用应已在 Plan05/06 切换，若仍有引用则停止而非隐式改其它文件。</action><verify><automated>npm run test:update-check &amp;&amp; npm run test:precommit &amp;&amp; npm run smoke:required</automated></verify><acceptance_criteria>- 当前 launcher/CI/hook 不引用被删路径。- required smoke 实际 PASS。</acceptance_criteria><done>更新/提交/smoke 维护入口只剩 Node。</done></task>
<task type="auto"><name>Task 3: 删除空 Python scripts package marker</name><files>scripts/__init__.py</files><action>在 `rg` 证明 scripts 下没有需保留的 Python importer 后删除 `__init__.py`；运行 post inventory 子检查，禁止递归删除 scripts 目录或无关资产。</action><verify><automated>npm run audit:retirement -- --mode post-scripts</automated></verify><acceptance_criteria>- scripts 下无 `.py`/Python invocation。- 其它脚本资产不变。</acceptance_criteria><done>维护脚本层完成 Python 退役。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-17-01 | Denial of Service | maintainer commands | high | mitigate | 每个删除前确认 Node replacement suite 与引用扫描。 |
| T-03.1-17-02 | Tampering | dirty worktree | high | mitigate | 精确 tracked path 删除，不 stage/revert 无关改动。 |
</threat_model>
<verification>`npm run test:cli &amp;&amp; npm run generate:check &amp;&amp; npm run smoke:required`</verification>
<success_criteria>- 七个 Python maintainer entry 已移除。- npm/Node replacements 全部可运行。</success_criteria>
## Artifacts this phase produces
- Removed files: seven explicitly listed `scripts/*.py` files.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-17-SUMMARY.md` when done.</output>

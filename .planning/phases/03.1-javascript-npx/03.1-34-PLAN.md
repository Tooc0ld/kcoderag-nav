---
phase: 03.1-javascript-npx
plan: 34
type: execute
wave: 30
depends_on: ["03.1-33"]
files_modified:
  - package.json
  - package-lock.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - .planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json
autonomous: false
gap_closure: true
requirements: [JS-02, JS-04, JS-07, JS-08]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "T-03.1-33-02/T-03.1-33-04: public `kcoderag-nav@0.1.8` and `@latest` are the exact clean-reviewed hardened source, with official-Registry identity and per-command anti-TOCTOU controls proven by fresh schema-v3 evidence."
    - "The committed pre-release SHA contains Plan 34, every post-0.1.7 source fix, the final clean REVIEW/FIX artifacts, and the blocking SECURITY report; that exact SHA is pushed to `origin/master` and passes ordinary Ubuntu/Windows x Node 22/24 CI before authorization."
    - "Before the human checkpoint, a private canonical preflight freezes candidate `0.1.8`, source/CI/history/worktree/current-Registry facts, exact seven release paths, candidate npm/tag absence, and `NPM_TOKEN` name presence into bytes with a SHA-256 digest; no version, tag, or publication mutation occurs."
    - "Only the exact response `authorize 0.1.8 <preflight-sha256>` permits release; every other response or any byte/field drift stops before mutation and requires a newly frozen preflight and authorization."
    - "D-06/D-07/D-08: the existing audited release helper alone creates a direct-child seven-path `0.1.8` commit and immutable `v0.1.8` tag; the tag-triggered Release workflow completes all four required lanes before its publish job."
    - "D-15: both the ordinary pre-release CI and tag Release workflow pass Ubuntu/Windows x Node 22/24 on the exact bound SHAs."
    - "Historical `v0.1.7` and `03.1-33-PUBLISH-RECEIPT.json` remain byte-identical and truthfully schema v2; Plan 34 writes a separate `03.1-34-PUBLISH-RECEIPT.json` using schema v3."
    - "D-01/D-02/D-03/D-04/D-05/D-09/D-10/D-11/D-12/D-13/D-14/D-16/D-17/D-18: the public exact/latest package preserves the completed Node-only, one-host, advisory/fail-open, ownership, drift, rollback, update, and host-native contracts through the complete local gate and fresh three-host lifecycle smoke."
    - "No credential value, MCP/Bearer value, npm configuration, raw command body, log body, temporary absolute path, or partial/NOT_RUN evidence enters output, commits, receipts, or summaries."
  artifacts:
    - path: "package.json"
      provides: "Root-only public version source advanced to 0.1.8 solely by the audited helper"
    - path: ".planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json"
      provides: "Closed schema-v3 release/workflow/Registry/artifact/exact/latest three-host evidence"
    - path: ".planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json"
      provides: "Immutable historical schema-v2 evidence for 0.1.7"
    - path: ".github/workflows/release.yml"
      provides: "Existing tag-only four-lane gate with publish depending on required contracts"
    - path: "src/smoke/host-smoke.cts"
      provides: "Official-Registry acquisition, SRI/SHA binding, and exclusive per-command verified tarball copies"
    - path: "src/maintainer/publish-receipt.cts"
      provides: "Backward-compatible v1/v2 verification and closed schema-v3 public artifact identity"
  key_links:
    - from: "committed Plan 34 + hardened source/review/security evidence"
      to: "ordinary CI"
      via: "one exact pushed pre-release SHA and four successful required-contract jobs"
      pattern: "headSha"
    - from: "frozen preflight digest and exact authorization"
      to: "audited release helper"
      via: "byte-for-byte and field-for-field replay before the first mutation"
      pattern: "authorize 0.1.8"
    - from: "immutable v0.1.8 tag"
      to: "npm publication"
      via: "Release required-contracts matrix followed by the dependent publish job"
      pattern: "needs: required-contracts"
    - from: "official Registry exact/latest tarballs"
      to: "schema-v3 receipt"
      via: "canonical URL + SRI + SHA-256/SHA-512 identity, exclusive invocation copies, manifest/version/gitHead convergence, and all-true three-host evidence"
      pattern: "publicRegistryArtifact"
---

<objective>
Close only security threats T-03.1-33-02 and T-03.1-33-04 by publishing the current clean-reviewed hardened source as immutable patch `0.1.8` and recording fresh schema-v3 public evidence.

Purpose: Move the already-reviewed Registry identity and per-command anti-replacement controls into the bytes users actually install, without rewriting or reinterpreting historical `0.1.7` evidence.
Output: One committed/pushed four-lane-green pre-release SHA, one private frozen/replayed authorization snapshot, one helper-owned seven-path `0.1.8` release commit and tag, one matrix-gated npm publication, fresh exact/latest three-host evidence, and one separate offline-verifiable schema-v3 receipt.

No version path, tag, npm version, dist-tag, or publish job may be created before the exact blocking authorization in Task 2.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-RESEARCH.md
@.planning/phases/03.1-javascript-npx/03.1-31-PLAN.md
@.planning/phases/03.1-javascript-npx/03.1-31-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-32-PLAN.md
@.planning/phases/03.1-javascript-npx/03.1-32-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-33-PLAN.md
@.planning/phases/03.1-javascript-npx/03.1-33-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
@.planning/phases/03.1-javascript-npx/03.1-REVIEW.md
@.planning/phases/03.1-javascript-npx/03.1-REVIEW-FIX.md
@.planning/phases/03.1-javascript-npx/03.1-SECURITY.md
@.planning/phases/03.1-javascript-npx/03.1-VERIFICATION.md
@src/maintainer/release.cts
@src/maintainer/publish-receipt.cts
@src/smoke/host-smoke.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml
@tests/maintainer/release.test.cts
@tests/maintainer/release-workflow.test.cts
@tests/maintainer/publish-receipt.test.cts
@tests/smoke/host-smoke.test.cts

<interfaces>
- `prepareRelease({ level: "patch", dryRun, yes })` validates the complete local gate, derives the next root-manifest patch, owns exactly `RELEASE_OWNED_PATHS`, commits those seven paths, and creates the matching local tag; it never pushes or publishes.
- `runHostSmoke({ mode: "required-contract", packageSpec, expectedVersion })` accepts exact or literal latest public requests, strips inherited npm controls, pins the official Registry, verifies canonical tarball URL plus SRI/SHA identity, and invokes each lifecycle command through a fresh exclusive digest-named copy with before/after checks.
- `PackageProvenance.publicRegistryArtifact` contains only `{ registry, resolvedTarballUrl, distIntegrity, artifactSha256, artifactSha512 }`; `lifecycleTarballSha256` independently identifies the loopback-instrumented package actually used by host lifecycle commands.
- `verifyPublishReceipt` preserves immutable schemas 1 and 2 and requires schema 3 to cross-bind version/tag/release SHA, Registry exact/latest/gitHead, Release workflow/ref/head/four lanes/publish, matching exact/latest public Registry artifact identity, independent lifecycle digests, and all-true host evidence.
- `.github/workflows/ci.yml` tests ordinary pushes on Ubuntu/Windows x Node 22/24; `.github/workflows/release.yml` permits publication only from a matching `vX.Y.Z` tag after the same four required lanes, with `NPM_TOKEN` scoped to the final publish step.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Bind the complete hardened source to ordinary four-lane CI and a frozen 0.1.8 preflight</name>
  <read_first>
    AGENTS.md
    .planning/phases/03.1-javascript-npx/03.1-34-PLAN.md
    .planning/phases/03.1-javascript-npx/03.1-REVIEW.md
    .planning/phases/03.1-javascript-npx/03.1-REVIEW-FIX.md
    .planning/phases/03.1-javascript-npx/03.1-SECURITY.md
    .planning/phases/03.1-javascript-npx/03.1-33-SUMMARY.md
    .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
    package.json
    package-lock.json
    src/maintainer/release.cts
    src/maintainer/publish-receipt.cts
    src/smoke/host-smoke.cts
    .github/workflows/ci.yml
    .github/workflows/release.yml
  </read_first>
  <precondition>`03.1-34-PLAN.md`, the final clean REVIEW/FIX artifacts, the current blocking SECURITY report, and every hardened source/test fix are committed in local `master`; `origin/master` is an ancestor; GitHub CLI authentication succeeds; the existing dirty/untracked `.planning`/`.gsd` baseline is distinguishable and contains no unexpected non-planning tracked change.</precondition>
  <reversibility rating="costly">Pushing the complete source/evidence SHA to the shared default branch creates persistent history and CI records; correction must use a new commit rather than rewriting the branch.</reversibility>
  <files>None (read-only validation, one non-force default-branch push, remote CI observation, and a private OS-temporary preflight snapshot)</files>
  <action>
    Confirm `HEAD` contains this Plan 34, every source/test commit after immutable `v0.1.7`, `03.1-REVIEW.md` with `status: clean`, `03.1-REVIEW-FIX.md` with `status: all_fixed`, and `03.1-SECURITY.md` whose only blocking open IDs are T-03.1-33-02 and T-03.1-33-04. Require branch `master`, an empty index, `origin/master` as an ancestor, and no tracked worktree change outside the preserved `.planning`/`.gsd` baseline. Capture a canonical path/status-only worktree manifest, its entry count and SHA-256, plus the existing release helper's `.planning`/`.gsd` byte digest; never capture file contents or credential-bearing configuration.

    Run `npm run ci:local` from the exact HEAD and require every test/generator/real-pack/three-host result to pass without skipped, todo, FAIL, or NOT_RUN evidence. Capture that 40-character HEAD as `preReleaseSha`; push only `master` to `origin/master` without force and create no version change or tag. Locate exactly one completed/success ordinary `CI` push run from `.github/workflows/ci.yml` for that SHA. Require `preReleaseSha`, local HEAD, `origin/master`, run `headSha`, and each exact successful job—`Required contracts (ubuntu-latest, Node 22)`, Ubuntu Node 24, Windows Node 22, and Windows Node 24—to agree.

    On the unchanged SHA, rerun the focused release, workflow, receipt, smoke, and pack contract tests, then run `npm run release:patch -- --dry-run --json`. Require current version `0.1.7`, exact candidate `0.1.8`, tag `v0.1.8`, and exactly the root package/lock plus five product manifest paths. Query only allow-listed public npm/Git metadata: official Registry `latest` remains `0.1.7` with its current `gitHead`; exact `0.1.8` is absent; `v0.1.8` is absent locally and remotely. Require immutable `v0.1.7` local/remote tag target equality and capture both the Git blob OID and SHA-256 of `03.1-33-PUBLISH-RECEIPT.json`. Check GitHub repository secret names only and record `NPM_TOKEN` presence as a boolean; never request, print, or persist its value.

    Serialize one canonical stable-key-order preflight object to a user-only file under `os.tmpdir()` named `kcoderag-nav-03.1-34-preflight-{preReleaseSha}.json`, then compute its SHA-256. Its exact closed fields are: schema identifier; candidate version/tag/seven release paths; branch and pre-release/local/origin SHAs; source-evidence blob OIDs for Plan 34, REVIEW, REVIEW-FIX, and SECURITY plus their safe status/open-ID assertions; ordinary CI workflow name/path/run ID/event/head/conclusion and four exact job conclusions; current root version and official Registry latest/version/gitHead; booleans for candidate npm absence, local tag absence, remote tag absence, and `NPM_TOKEN` name presence; canonical worktree entry count/status digest and local `.planning`/`.gsd` digest; historical `v0.1.7` local/remote target plus Plan 33 receipt blob OID/SHA-256. Retain the exact bytes, private path, and digest across Task 2. No version file, tag, npm version, dist-tag, or publish workflow is mutated before authorization.
  </action>
  <verify>
    <automated>npm run ci:local; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $releaseLines = @(npm run release:patch -- --dry-run --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $release = ($releaseLines | Where-Object { $_.Trim().Length -gt 0 } | Select-Object -Last 1) | ConvertFrom-Json; $expectedPaths = @('package.json','package-lock.json','kcoderag-qa/.codex-plugin/plugin.json','kcoderag-qa/.claude-plugin/plugin.json','kcoderag-dev/.codex-plugin/plugin.json','kcoderag-dev/.claude-plugin/plugin.json','kcoderag-cursor/.cursor-plugin/plugin.json') | Sort-Object; $actualPaths = @($release.releasePaths | Sort-Object); if ($release.ok -ne $true -or $release.dryRun -ne $true -or $release.previousVersion -ne '0.1.7' -or $release.version -ne '0.1.8' -or $release.tag -ne 'v0.1.8' -or @(Compare-Object -ReferenceObject $expectedPaths -DifferenceObject $actualPaths).Count -ne 0) { throw 'release dry-run contract mismatch' }; $sha = (git rev-parse HEAD).Trim(); if ($sha -notmatch '^[0-9a-f]{40}$' -or (git branch --show-current).Trim() -ne 'master' -or (git rev-parse origin/master).Trim() -ne $sha) { throw 'source SHA mismatch' }; $runs = @(gh run list --workflow ci.yml --event push --commit $sha --json databaseId,headSha,status,conclusion | ConvertFrom-Json | Where-Object { $_.headSha -eq $sha -and $_.status -eq 'completed' -and $_.conclusion -eq 'success' }); if ($runs.Count -ne 1) { throw 'expected one successful CI run' }; $run = gh run view $runs[0].databaseId --json headSha,event,conclusion,jobs | ConvertFrom-Json; if ($run.headSha -ne $sha -or $run.event -ne 'push' -or $run.conclusion -ne 'success') { throw 'CI provenance mismatch' }; $required = @('Required contracts (ubuntu-latest, Node 22)','Required contracts (ubuntu-latest, Node 24)','Required contracts (windows-latest, Node 22)','Required contracts (windows-latest, Node 24)'); foreach ($name in $required) { if (@($run.jobs | Where-Object { $_.name -eq $name -and $_.conclusion -eq 'success' }).Count -ne 1) { throw "missing successful lane: $name" } }</automated>
  </verify>
  <acceptance_criteria>
    - The exact committed/pushed `preReleaseSha` contains Plan 34, all hardened source/test fixes, clean REVIEW/FIX, and the blocking SECURITY report; local, origin, and ordinary CI head SHAs match.
    - One unambiguous ordinary CI run for that SHA has all four required Ubuntu/Windows Node 22/24 jobs completed successfully.
    - The audited helper dry-run derives exactly `0.1.8`, `v0.1.8`, and the seven release-owned paths while leaving current root at `0.1.7`.
    - Official Registry latest and `gitHead`, candidate npm/tag absence, immutable `v0.1.7`/Plan 33 receipt identity, worktree/local-state baselines, and secret-name presence are all frozen in a closed canonical snapshot.
    - The private snapshot digest is computed over exact bytes and no secret value, MCP/Bearer material, raw output/log, npm config, file content, or absolute temporary path enters its fields.
    - No version, tag, npm, dist-tag, receipt, or publish mutation occurs in Task 1.
  </acceptance_criteria>
  <done>The complete hardened source is committed, pushed, four-lane green, collision-free, historically anchored, and frozen into one exact 0.1.8 authorization preflight.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize only the frozen 0.1.8 release</name>
  <read_first>
    .planning/phases/03.1-javascript-npx/03.1-34-PLAN.md
    .planning/phases/03.1-javascript-npx/03.1-SECURITY.md
    os.tmpdir()/kcoderag-nav-03.1-34-preflight-{preReleaseSha}.json and Task 1's recorded SHA-256
  </read_first>
  <files>None</files>
  <action>Pause after Task 1 and present the exact candidate `0.1.8`, exact pre-release SHA, exact ordinary CI run/four-lane result, and exact frozen preflight SHA-256 using safe metadata only. Do not infer authority from prior `0.1.7` authorization, npm token setup, the phase-completion request, auto-advance settings, or any approval-like wording. Only one byte-exact response format naming both `0.1.8` and Task 1's digest permits Task 3.</action>
  <decision>Authorize the audited helper to create and push immutable `0.1.8`/`v0.1.8`, allow the four-lane Release workflow to publish it, and record a separate schema-v3 public receipt?</decision>
  <context>The npm version, Git tag, release commit, GitHub workflow record, and public artifact identities are permanent. The authorization is valid only for the frozen source/readiness bytes and cannot authorize a changed candidate or replay.</context>
  <options>
    <option id="authorize">
      <name>Authorize exact 0.1.8</name>
      <pros>Moves the reviewed Registry identity and anti-TOCTOU controls into public latest and can close both blocking threats.</pros>
      <cons>The exact version/tag/publication cannot be overwritten or removed from history.</cons>
    </option>
    <option id="stop">
      <name>Stop before release</name>
      <pros>Leaves all version, tag, npm, dist-tag, and receipt state unchanged while retaining the pushed four-lane-green source.</pros>
      <cons>T-03.1-33-02 and T-03.1-33-04 remain open because public latest stays on 0.1.7.</cons>
    </option>
  </options>
  <verify>
    <automated>$releaseLines = @(npm run release:patch -- --dry-run --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $release = ($releaseLines | Where-Object { $_.Trim().Length -gt 0 } | Select-Object -Last 1) | ConvertFrom-Json; $expectedPaths = @('package.json','package-lock.json','kcoderag-qa/.codex-plugin/plugin.json','kcoderag-qa/.claude-plugin/plugin.json','kcoderag-dev/.codex-plugin/plugin.json','kcoderag-dev/.claude-plugin/plugin.json','kcoderag-cursor/.cursor-plugin/plugin.json') | Sort-Object; $actualPaths = @($release.releasePaths | Sort-Object); if ($release.ok -ne $true -or $release.dryRun -ne $true -or $release.previousVersion -ne '0.1.7' -or $release.version -ne '0.1.8' -or $release.tag -ne 'v0.1.8' -or @(Compare-Object -ReferenceObject $expectedPaths -DifferenceObject $actualPaths).Count -ne 0) { throw 'authorized candidate drift' }</automated>
  </verify>
  <acceptance_criteria>
    - The only continue signal is exactly `authorize 0.1.8 &lt;preflight-sha256&gt;`, with the actual Task 1 digest substituted and no extra text.
    - Any other response stops execution before Task 3 and creates no release commit/tag, release push, npm version, dist-tag change, or Plan 34 receipt.
    - Any changed source SHA, candidate, seven-path set, CI identity/result, Registry latest, tag/version collision, history identity, worktree baseline, or secret-name status invalidates the snapshot and requires Task 1 plus a new authorization.
  </acceptance_criteria>
  <resume-signal>Reply exactly `authorize 0.1.8 &lt;preflight-sha256&gt;`; any other response stops before publication.</resume-signal>
  <done>The permanent 0.1.8 release is authorized only by its exact version and frozen readiness digest.</done>
</task>

<task type="auto">
  <name>Task 3: Replay, publish 0.1.8 through four lanes, and record schema-v3 public evidence</name>
  <read_first>
    package.json
    package-lock.json
    src/maintainer/release.cts
    src/maintainer/publish-receipt.cts
    src/smoke/host-smoke.cts
    .github/workflows/release.yml
    tests/maintainer/release.test.cts
    tests/maintainer/release-workflow.test.cts
    tests/maintainer/publish-receipt.test.cts
    tests/smoke/host-smoke.test.cts
    .planning/phases/03.1-javascript-npx/03.1-33-PUBLISH-RECEIPT.json
    os.tmpdir()/kcoderag-nav-03.1-34-preflight-{preReleaseSha}.json and Task 1's recorded SHA-256
  </read_first>
  <precondition>The user supplied exactly `authorize 0.1.8 &lt;preflight-sha256&gt;` for Task 1's retained private snapshot, and no operation has consumed or altered release state since that checkpoint.</precondition>
  <reversibility rating="one-way">Published npm `0.1.8`, immutable tag `v0.1.8`, its release commit, workflow record, and Registry artifact cannot be overwritten; correction requires another separately authorized version.</reversibility>
  <files>package.json, package-lock.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-cursor/.cursor-plugin/plugin.json, .planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json</files>
  <action>
    Before the first release-helper call, version write, commit, tag, push, or npm mutation, recompute every Task 1 allow-listed field with the same canonical serializer. Verify the retained snapshot file hashes to the exact authorized digest, then require byte-for-byte equality and explicit field-for-field equality between frozen and replayed snapshots, including source-evidence blob OIDs, local/origin/pre-release SHA, exact ordinary CI run/four jobs, current Registry latest/gitHead, candidate version/tag absence, immutable `v0.1.7` and Plan 33 receipt identities, worktree/local-state baselines, seven paths, and `NPM_TOKEN` presence boolean. Any mismatch, missing snapshot, ambiguity, network failure, race, collision, or status drift performs no mutation, returns to Task 1, and requires a new exact authorization.

    After the identical replay only, invoke `npm run release:patch -- --yes --json`; no manual version edit, commit, or tag command may substitute for the audited helper. Require previous/new versions `0.1.7`/`0.1.8`, tag `v0.1.8`, exactly seven release paths, one release commit whose parent is the authorized `preReleaseSha`, and all seven version fields equal `0.1.8`. Require an empty index, no other committed path, and unchanged canonical worktree/local-state baselines. Reconfirm local/remote `v0.1.7` targets and the Plan 33 receipt Git blob/SHA-256 before and after the helper.

    Push the release commit to `origin/master`, then push only the helper-created `v0.1.8` tag without force. Discover exactly one tag-triggered workflow by name `Release`, path `.github/workflows/release.yml`, event `push`, ref `refs/tags/v0.1.8`, and head SHA equal to the helper release commit. Watch and re-query it. Require all four exact `Required release contracts` Ubuntu/Windows Node 22/24 jobs to complete successfully and the dependent `Verify and publish matching npm tag` job to succeed. Confirm from workflow source that publish still depends on `required-contracts` and `NPM_TOKEN` remains scoped only to the final publish step; inspect no secret value. Missing, skipped, cancelled, failed, substituted, or ambiguous evidence stops receipt creation.

    After official Registry convergence, query exact `kcoderag-nav@0.1.8` and literal `@latest` metadata through isolated empty user/global npm configs pinned to `https://registry.npmjs.org/`. In one bounded evidence window require both versions `0.1.8`, registry `gitHead` equal to the release commit, and expected bin/engine/repository. Run the current hardened required-contract harness sequentially with separate fresh temporary roots/caches/configs: first exact `kcoderag-nav@0.1.8`, then `kcoderag-nav@latest --expected-version 0.1.8`. Both must return PASS for Codex, Claude, and Cursor with every evidence bit true. Exact/latest `publicRegistryArtifact` must match on official Registry origin, canonical tarball URL, canonical SRI, artifact SHA-256, and artifact SHA-512; each independent loopback lifecycle digest must validate; every Registry install and host install/status/update/uninstall command must traverse a fresh exclusive digest-named copy whose original and invocation bytes pass checks before and after execution. Any `artifact_integrity_failed`, FAIL, NOT_RUN, source/version mismatch, SRI/hash mismatch, or public-artifact mismatch records no receipt.

    Immediately re-query Registry latest after the latest smoke and require it still equals `0.1.8` with the same release `gitHead` and public artifact identity; a latest race invalidates the evidence window and requires fresh exact/latest runs. Reduce only the accepted allow-listed metadata and booleans into schema 3 and pipe it through `publish-receipt --record` to the new `03.1-34-PUBLISH-RECEIPT.json`; verify the saved file offline and commit it separately from the seven-path release commit. Keep `03.1-33-PUBLISH-RECEIPT.json` byte-identical and schema 2. Persist no preflight file, raw npm/GitHub output, log, environment value, npm config, credential URL, MCP/Bearer value, token, or temporary absolute path. After the receipt commit, create `03.1-34-SUMMARY.md` with only safe SHA/run/job/version/digest evidence; the orchestrator then refreshes SECURITY and runs final phase verification.
  </action>
  <verify>
    <automated>node dist/maintainer/publish-receipt.cjs --verify .planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node dist/smoke/host-smoke.cjs --mode required-contract --package-spec kcoderag-nav@0.1.8 --expected-version 0.1.8; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node dist/smoke/host-smoke.cjs --mode required-contract --package-spec kcoderag-nav@latest --expected-version 0.1.8; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $receipt = Get-Content -LiteralPath '.planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json' -Raw | ConvertFrom-Json; if ($receipt.schema_version -ne 3 -or $receipt.version -ne '0.1.8') { throw 'receipt identity mismatch' }; $userConfig = [System.IO.Path]::GetTempFileName(); $globalConfig = [System.IO.Path]::GetTempFileName(); try { $exactText = [string]::Join("`n", @(npm view kcoderag-nav@0.1.8 version gitHead dist.tarball dist.integrity bin.kcoderag-nav engines.node repository.url --json --registry=https://registry.npmjs.org/ --userconfig=$userConfig --globalconfig=$globalConfig)); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $latestText = [string]::Join("`n", @(npm view kcoderag-nav@latest version gitHead dist.tarball dist.integrity bin.kcoderag-nav engines.node repository.url --json --registry=https://registry.npmjs.org/ --userconfig=$userConfig --globalconfig=$globalConfig)); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $exact = $exactText | ConvertFrom-Json; $latest = $latestText | ConvertFrom-Json; $expectedUrl = 'https://registry.npmjs.org/kcoderag-nav/-/kcoderag-nav-0.1.8.tgz'; $releaseSha = $receipt.release_commit_sha; function Assert-RegistryMetadata($metadata, $artifact, $label) { if ($metadata.version -ne '0.1.8' -or $metadata.gitHead -ne $releaseSha -or $metadata.'dist.tarball' -ne $expectedUrl -or $metadata.'dist.integrity' -ne $artifact.distIntegrity -or $metadata.'dist.tarball' -ne $artifact.resolvedTarballUrl -or $metadata.'bin.kcoderag-nav' -ne 'dist/bin/kcoderag-nav.cjs' -or $metadata.'engines.node' -ne '>=22' -or $metadata.'repository.url' -ne 'git+https://github.com/Tooc0ld/kcoderag-nav.git') { throw "$label official Registry mismatch" } }; Assert-RegistryMetadata $exact $receipt.lifecycle.exact_version.publicRegistryArtifact 'exact'; Assert-RegistryMetadata $latest $receipt.lifecycle.latest.publicRegistryArtifact 'latest' } finally { [System.IO.File]::Delete($userConfig); [System.IO.File]::Delete($globalConfig) }</automated>
  </verify>
  <acceptance_criteria>
    - The authorized preflight is replayed byte-for-byte and field-for-field before mutation; every drift path returns to Task 1 without consuming the authority.
    - The helper alone creates a direct-child release commit changing exactly the seven version paths, all at `0.1.8`, and immutable `v0.1.8` points to it; no earlier version/tag/receipt changes.
    - The unique Release run matches workflow/path/ref/head/event, all four supported OS/Node lanes succeed, and the publish job succeeds only after them.
    - Registry exact/latest version and `gitHead` converge on `0.1.8` and the release SHA; exact/latest independently acquired public bytes have identical official Registry URL/SRI/SHA-256/SHA-512 identity.
    - Fresh exact and latest runs each pass complete Codex/Claude/Cursor lifecycle/MCP evidence through exclusive per-command verified copies, and the immediate latest re-query detects no race.
    - `03.1-34-PUBLISH-RECEIPT.json` is schema 3, passes offline verification, contains only closed metadata/booleans, and remains separate from immutable schema-v2 `03.1-33-PUBLISH-RECEIPT.json`.
    - `03.1-34-SUMMARY.md` truthfully records safe evidence; SECURITY and final VERIFICATION remain orchestrator-owned follow-up artifacts.
  </acceptance_criteria>
  <done>Public exact/latest 0.1.8 contain the clean-reviewed hardened controls, and fresh schema-v3 evidence closes T-03.1-33-02 and T-03.1-33-04 without rewriting history.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Node 22+ unified npx lifecycle with no Python runtime and safe Codex/Claude/Cursor project integration | 01-33 preserved; 34 | COVERED | Plan 34 republishes the complete reviewed source and reruns all local/public lifecycle gates. |
| REQ | JS-01 | Direct CJS with no Python-running public code/runtime compiler | 31 preserved; 34 regression | COVERED | Closed 59-member pack gate reruns before both CI and release. |
| REQ | JS-02 | Public five-command package; root manifest is the version source | 34 | COVERED | Existing helper derives and synchronizes 0.1.8 from the root across exactly seven paths. |
| REQ | JS-03 | Three native adapters, one selected host, cross-host coexistence | 01-33 preserved; 34 smoke | COVERED | Exact/latest required smoke runs each host through the published package. |
| REQ | JS-04 | Ownership, drift, identity fencing, and one-host rollback in public bytes | 34 | COVERED | 0.1.8 is the exact clean-reviewed hardened source; public lifecycle smoke exercises it. |
| REQ | JS-05 | Advisory/offline hook and bounded detached latest refresh | 01-33 preserved; 34 regression | COVERED | Full local gate plus public host smoke rerun; no hook source change. |
| REQ | JS-06 | Deterministic, version-aligned, self-contained pack proof | 31 preserved; 34 regression | COVERED | Helper and release lanes rerun generator check and real closed-inventory pack audit. |
| REQ | JS-07 | Ordinary test-only and tag-release Ubuntu/Windows Node 22/24 gating | 34 | COVERED | Exact pre-release and release SHAs each require all four lanes; publish depends on release lanes. |
| REQ | JS-08 | Honest three-host lifecycle/MCP evidence and synchronized npx docs | 34 | COVERED | Fresh exact/latest loopback evidence is schema-v3 bound; authenticated real hosts remain Phase 6. |
| RESEARCH | stack | Audited dev-only dependency graph, zero runtime dependencies, direct CJS on Node 22+ | 01-33 preserved; 34 gates | COVERED | No dependency change; dependency audit/build rerun locally and in both matrices. |
| RESEARCH | package | Deterministic exact tarball with no Python/runtime compiler | 31 preserved; 34 gates | COVERED | Real pack audit precedes authorization and publication. |
| RESEARCH | security | Exact npm provenance, integrity, safe evidence, and no credential leakage | 34 | COVERED | Official Registry URL/SRI/SHA binding, anti-TOCTOU copies, closed receipt, and presence-only token handling. |
| CONTEXT | D-01 | Complete migration; no Python-running published code | 31 preserved; 34 regression | COVERED | Node-only exact pack proof reruns on 0.1.8 source/tag. |
| CONTEXT | D-02 | TypeScript maintenance source to direct CJS; no runtime compile | 01-33 preserved; 34 regression | COVERED | Build/package gates rerun; no runtime compiler added. |
| CONTEXT | D-03 | npx replaces marketplace installation | 34 | COVERED | Public exact/latest npx package is the only release evidence surface. |
| CONTEXT | D-04 | One selected Codex/Claude/Cursor host per invocation | 01-33 preserved; 34 smoke | COVERED | Host harness invokes each selected adapter separately. |
| CONTEXT | D-05 | Safe verified legacy migration | 01-33 preserved; 34 smoke | COVERED | Reviewed public lifecycle bytes and full regression gate preserve migration semantics. |
| CONTEXT | D-06 | Public unscoped `kcoderag-nav` contract | 34 | COVERED | Exact 0.1.8 authorization and official public Registry evidence. |
| CONTEXT | D-07 | Root package version is the only version source | 34 | COVERED | Helper-only exact seven-path synchronization. |
| CONTEXT | D-08 | Helper-created tag and tag-only gated publish | 34 | COVERED | Frozen authorization, direct-child release, four lanes, dependent publish. |
| CONTEXT | D-09 | npm latest update semantics | 01-33 preserved; 34 | COVERED | Registry latest is frozen/replayed and re-queried after smoke to reject races. |
| CONTEXT | D-10 | Host-native project paths and honest Cursor boundary | 01-33 preserved; 34 smoke | COVERED | Cursor Rule/skill/MCP remains distinct from Codex/Claude hooks. |
| CONTEXT | D-11 | Explicit cwd/target and confirmation | 01-33 preserved; 34 smoke | COVERED | Complete public lifecycle behavior reruns; no target policy change. |
| CONTEXT | D-12 | Structured ownership, unrelated config preservation, drift hard-stop | 01-33 preserved; 34 smoke | COVERED | Public 0.1.8 contains final reviewed ownership/identity fixes. |
| CONTEXT | D-13 | Cross-host coexistence and host-local QA/Dev exclusivity | 01-33 preserved; 34 gates | COVERED | Full tests and three-host smoke rerun without changing environment policy. |
| CONTEXT | D-14 | Extensible adapter seam; OpenCode deferred | 01-33 preserved | COVERED | Plan 34 adds no host behavior and makes no OpenCode claim. |
| CONTEXT | D-15 | Node 22 and 24 on Windows/Linux | 34 | COVERED | Both ordinary and release workflows require all four lanes. |
| CONTEXT | D-16 | Mutation runtime gate; deployed hook fail-open | 01-33 preserved; 34 regression | COVERED | Complete gate and public hook evidence rerun without redesign. |
| CONTEXT | D-17 | Acquisition failure is zero-write; installed hook offline | 34 | COVERED | Public acquisition precedes project creation, is isolated, integrity-bound, and fail-closed. |
| CONTEXT | D-18 | One-host atomic transaction and rollback | 01-33 preserved; 34 smoke | COVERED | Exact/latest public lifecycle runs the reviewed transaction implementation for each host. |
| CONTEXT | Deferred | OpenCode; production identity/HTTPS/credential rotation | — | EXCLUDED | Explicitly deferred; Plan 34 changes neither scope. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hardened reviewed source/evidence → origin CI | Plan 34, fixes, REVIEW/FIX, and SECURITY must share the exact pushed/tested pre-release SHA. |
| Human authorization → release helper | Only an exact candidate-and-digest response can permit permanent release mutation. |
| Frozen preflight → post-checkpoint replay | Authorization remains valid only while every canonical safe byte and field is unchanged. |
| Immutable 0.1.7 history → new 0.1.8 history | The new release must add evidence without altering an existing tag, version, or receipt. |
| Release tag/SHA → GitHub matrix → npm | Public credentials become reachable only after four required supported-runtime lanes pass for the exact tag SHA. |
| Official Registry metadata/tarball → verified public artifact | Network-controlled metadata and bytes are executable only after origin, canonical URL, SRI, SHA, and installed-manifest checks. |
| Verified artifact → per-command lifecycle copy | Every npm invocation gets an exclusive content-addressed copy checked before and after use. |
| Public/workflow/lifecycle evidence → schema-v3 receipt | Only cross-bound closed metadata and all-true booleans may persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-34-01 | Spoofing | human release authorization | critical | mitigate | Freeze a closed canonical preflight, require exact `authorize 0.1.8 <digest>`, and replay bytes plus fields before any mutation. |
| T-03.1-34-02 | Tampering | source/CI/release SHA chain | critical | mitigate | Require committed Plan/review/security evidence in one pre-release SHA, exact ordinary CI lanes, helper direct parent, immutable tag, and matching Release head SHA. |
| T-03.1-34-03 | Spoofing | Registry/source identity | critical | mitigate | Pin the official Registry and canonical tarball URL; bind SRI, SHA-256/SHA-512, installed manifest, registry `gitHead`, tag, and release SHA across exact/latest. |
| T-03.1-34-04 | Tampering | lifecycle tarball TOCTOU | high | mitigate | Use fresh exclusive digest-named copies for Registry install and every host command, check original/copy bytes before and after, and fail the whole evidence run on replacement. |
| T-03.1-34-05 | Repudiation | schema-v3 receipt | high | mitigate | Closed offline schema cross-binds workflow lanes/publish, Registry identity, independent lifecycle digests, and all host evidence; commit the verified receipt separately. |
| T-03.1-34-06 | Tampering | immutable 0.1.7 history | high | mitigate | Freeze and replay v0.1.7 local/remote tag target and Plan 33 receipt blob/SHA; verify both unchanged after helper, publish, and receipt commit. |
| T-03.1-34-07 | Information Disclosure | npm/MCP credentials and external output | critical | mitigate | Inspect only secret-name presence, isolate npm configs, persist closed metadata, and reject secret-like keys/values with bounded scanning and stable codes. |
| T-03.1-34-08 | Denial of Service | partial/ambiguous external evidence | high | mitigate | Treat missing, duplicate, skipped, cancelled, FAIL, NOT_RUN, race, network ambiguity, or hash mismatch as a hard stop before receipt creation. |
| T-03.1-34-09 | Elevation of Privilege | release/worktree mutation scope | high | mitigate | Replay canonical worktree/local-state digests; require helper-only seven paths, clean index, no force push, and no mutation of unrelated dirty/untracked work. |
</threat_model>

<verification>
The exact committed/pushed pre-release SHA passes ordinary four-lane CI; after exact version-and-digest authorization and identical preflight replay, immutable `v0.1.8` passes the tag Release four-lane gate and publish job. Registry exact/latest/gitHead and fresh hardened exact/latest three-host runs converge on the same official artifact identity, and `03.1-34-PUBLISH-RECEIPT.json` verifies offline as schema 3 while the 0.1.7 tag/receipt remain unchanged.
</verification>

<success_criteria>
- T-03.1-33-02 is closable because public 0.1.8 Registry bytes, `gitHead`, release/tag SHA, official tarball URL, SRI, SHA-256/SHA-512, workflow identity, four lanes, and publish result are one cross-bound identity.
- T-03.1-33-04 is closable because exact/latest public smokes execute each npm lifecycle command from an exclusive verified copy and record fresh all-true three-host evidence, not historical 0.1.7 claims.
- No release mutation occurs before the exact checkpoint response and byte/field-identical replay.
- Immutable 0.1.7 history remains separate and unchanged; the new schema-v3 receipt belongs only to 0.1.8.
- Plan 34 SUMMARY is complete and safe; the orchestrator can then update SECURITY to zero blocking threats and run final phase verification.
</success_criteria>

## Artifacts this phase produces

- One Plan-34-inclusive hardened pre-release SHA pushed without force, plus one exact ordinary CI run with four required lanes.
- One private canonical 0.1.8 preflight snapshot/digest and an exact blocking human authorization; the snapshot is never committed.
- One helper-created seven-path `release: v0.1.8` commit and matching immutable tag.
- One successful tag-triggered Release run with four required OS/Node lanes and dependent npm publish.
- Fresh exact-version and latest official-Registry three-host lifecycle/MCP evidence using the hardened harness.
- One new sanitized schema-v3 `.planning/phases/03.1-javascript-npx/03.1-34-PUBLISH-RECEIPT.json`; historical Plan 33 schema-v2 evidence stays unchanged.
- `.planning/phases/03.1-javascript-npx/03.1-34-SUMMARY.md` with safe evidence for the orchestrator's SECURITY refresh and final verifier.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-34-SUMMARY.md` when done.
</output>

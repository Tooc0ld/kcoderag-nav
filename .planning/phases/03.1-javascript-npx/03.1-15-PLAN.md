---
phase: 03.1-javascript-npx
plan: 15
type: execute
wave: 16
depends_on: ["03.1-13"]
files_modified:
  - src/maintainer/retirement-audit.cts
  - tests/maintainer/retirement-audit.test.cts
  - .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json
autonomous: true
requirements: [JS-01, JS-04, JS-05, JS-06, JS-08]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-05: explicit monotonic modes pre → post-source → post-scripts → post-tests → post are implemented and tested before deletion."
    - "Pre-retirement full parity runs once before deletion and persists exact schema `kcoderag-nav/pre-retirement-receipt@1`; Plan30 consumes the same names/types/canonical hashes without inference."
    - "`pre_cache_inventory` has exact roots/counts 1/2/2/6/14, total25, sorted unique relative `.pyc` paths and lowercase SHA-256; missing/extra/empty/duplicate/unsorted/mismatch is invalid."
    - "Receipt freezes a valid repo HEAD, canonical authorized-set/receipt hashes, filtered unrelated status bytes+paths, and a nonempty sorted root-external digest list before deletion."
    - "Inventory includes kcoderag-update.json and all GitHub-raw/update-index references, which final post mode forbids."
  artifacts:
    - {path: "src/maintainer/retirement-audit.cts", provides: "retirement state machine, canonical serializer and strict receipt verifier", exports: ["auditRetirement", "verifyPreReceipt", "canonicalJson", "hashAuthorizedSet"]}
    - {path: "tests/maintainer/retirement-audit.test.cts", provides: "exact producer-schema, canonical-hash and malformed-field matrix"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json", provides: "versioned sanitized parity/cache authorization producer receipt"}
  key_links:
    - {from: "pre parity suites", to: "pre-retirement receipt", via: "exact versioned schema and canonical SHA-256", pattern: "verifyPreReceipt"}
    - {from: "pre_cache_inventory", to: "Plan30 authorization", via: "identical field names/types and canonicalJson", pattern: "authorized_set_sha256"}
---
<objective>在任何删除前实现完整退役状态机，并运行全 parity 生成可供后续阶段验证的去敏 receipt。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 实现 versioned receipt producer/verifier 与单调退役状态机</name><files>src/maintainer/retirement-audit.cts, tests/maintainer/retirement-audit.test.cts</files><behavior>- schema_version 必须精确为 `kcoderag-nav/pre-retirement-receipt@1`。- producer/verifier 对 cache inventory、HEAD、status、root-external digests 和两种 hash 使用同一 schema/canonicalizer。- undefined/missing/empty/duplicate/unsorted/extra/mismatch/invalid fields all fail closed。</behavior><action>保留 explicit tracked inventory 与 `pre|post-source|post-scripts|post-tests|post` 单调 state machine，并新增唯一 producer schema。`pre_cache_inventory` 必须精确为 `{roots:[{path,count}],root_counts:{"plugin-src/hooks/__pycache__":1,"kcoderag-qa/hooks/__pycache__":2,"kcoderag-dev/hooks/__pycache__":2,"scripts/__pycache__":6,"tests/__pycache__":14},total:25,files:[{path,sha256}]}`；`roots` 按上述固定顺序、`files` 按规范 POSIX repo-relative path code-point 升序且无重复，path 必须位于对应 exact root、扩展名只允许 `.pyc`，SHA-256 必须为非空小写 64-hex。

定义 `canonicalJson(value)`：递归按 Unicode code-point 升序排列所有 object keys；array 保持已经验证的规定顺序；拒绝 undefined、稀疏 array、NaN/Infinity 和未知 schema keys；使用无空格的 JSON UTF-8、无 BOM、无尾随换行。`authorized_set_sha256=sha256(UTF8(canonicalJson(pre_cache_inventory)))`。`receipt_sha256=sha256(UTF8(canonicalJson(topLevelReceiptWithoutReceiptSha256)))`，明确排除且只排除 `receipt_sha256` 字段，避免 self-hash 歧义。`schema_version` 固定为上述字符串，`repo_head` 为冻结 current HEAD 的小写 40-hex。

定义 `unrelated_status_before` exact type `{format:"git-status-porcelain-v1-z",bytes_base64,sha256,entries:[{code,path}]}`：排除且只排除 Plan15/30 receipts 与 SUMMARY 后，把 `git status --porcelain=v1 -z` 解析成无重复 code+normalized-relative-path records、按 `code+" "+path` code-point 排序，再编码为 `code SP path NUL` canonical bytes；`bytes_base64` 是该 bytes 的 base64，`sha256` 是其小写 SHA-256，`entries` 与解码 bytes 双向一致。定义 `root_external_digests_before` 为五个 cache parent 内、排除 exact cache roots 后所有 regular nonsymlink files 的非空、按 path 排序、无重复 `{path,sha256}` 列表。

Top-level receipt 使用 exact allow-list：`schema_version,repo_head,tracked_inventory_sha256,suites,generated_sha256,pack_sha256,timestamp,pre_cache_inventory,authorized_set_sha256,unrelated_status_before,root_external_digests_before,receipt_sha256`。实现 `--verify-pre-receipt` 同时校验 schema、current/frozen HEAD、两 hash、status bytes/hash/entries、root-external list 及 required suite bits；cache state 只接受 receipt 完全相等或五根全部不存在，partial/替换/额外失败。tests 对每个 required top-level/nested field逐项删除、置空、undefined、额外 key、错误 type/hash/head；对 roots/files/status/external lists 测试 duplicate、unsorted、missing、extra、count/total/path/root/digest mismatch、uppercase/short digest、`.pyo`、symlink/reparse、partial-clean，全部必须非零。</action><verify><automated>npm run build &amp;&amp; npm run test:retirement-audit</automated></verify><acceptance_criteria>- compiled tests prove exact schema version、root object/counts/total25、sorted files and canonical serialization/hash formulas.- every required field/list/hash absent, empty, duplicated, unsorted, mismatched, extra or invalid fails.- tracked inventory still includes kcoderag-update.json/raw logic; only exact pre-cache or all-clean states pass。</acceptance_criteria><done>Plan15 produces exactly the schema Plan30 consumes, with no optional/undefined trust fields。</done></task>
<task type="auto"><name>Task 2: 运行全 parity 并原子持久化 exact producer receipt</name><read_first>- `src/maintainer/retirement-audit.cts` schema constants、canonicalJson/hashAuthorizedSet and verifyPreReceipt tests
- Plan30 exact consumer contract in `03.1-30-PLAN.md`
- Current five cache roots and adjacent regular files; never log their bytes</read_first><files>.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</files><action>执行 `verify:parity-before-retire` 的所有 required suites；任何 missing/NOT_RUN/empty selection失败，且这是最后一次 Python invocation。suite 全绿后重新冻结 current `repo_head`；按 Task1 exact types 采集 `pre_cache_inventory`，硬要求 root counts 1/2/2/6/14 与 total25；采集 filtered `unrelated_status_before` canonical bytes/entries 和非空 `root_external_digests_before`。先计算 authorized-set hash，再组装 exact allow-listed receipt，最后按 omit-only-self 公式计算 receipt hash并原子写文件。

写后从磁盘重新 parse：要求 schema_version exact、repo_head 等于 current HEAD、所有 required fields defined/nonempty、两 hash recompute equal、status base64→bytes→entries→hash闭环、external list 非空排序且磁盘摘要相等、pre-cache receipt/disk path+SHA/counts 双向相等；再运行 `--verify-pre-receipt`。任一失败删除无效临时 receipt并停止，不留下可被 Plan30接受的部分证据；receipt 不含命令输出、配置值、cache bytes或凭据。</action><verify><automated>npm run verify:parity-before-retire &amp;&amp; npm run test:retirement-audit &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</automated></verify><acceptance_criteria>- exact versioned receipt exists only after every required suite passes and validates against current HEAD/disk.- pre_cache_inventory has exact roots array/root_counts/total25/sorted files; receipt/authorized/status/external hashes all recompute.- no later plan invokes Python; worktree changes are limited to final parity caches and this receipt。</acceptance_criteria><done>Plan30 receives a complete, immutable and cryptographically self-consistent producer receipt。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-15-01 | Repudiation | parity authorization | high | mitigate | exact schema version、frozen HEAD、canonical authorized-set/receipt hashes and strict offline verification. |
| T-03.1-15-02 | Tampering | retirement stage | high | mitigate | explicit monotonic state machine and exact paths. |
| T-03.1-15-03 | Tampering | cache/status/external inventories | high | mitigate | exact counts/types/order、path containment、byte/hash closure and malformed-field tests. |
| T-03.1-15-04 | Information Disclosure | receipts | high | mitigate | allow-listed paths/booleans/hashes/status metadata only; no file/config/cache bytes. |
</threat_model>
<verification>`npm run test:retirement-audit &amp;&amp; npm run verify:parity-before-retire &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json`; malformed schema/canonicalization corpus must all fail.</verification>
<success_criteria>- All retirement modes and exact producer schema are tested.- A valid, frozen-HEAD, canonically hashed pre receipt exists before any deletion.</success_criteria>
## Artifacts this phase produces
- Retirement state machine modes/flags and strict schema/canonicalization tests.
- `kcoderag-nav/pre-retirement-receipt@1` producer receipt with exact `pre_cache_inventory`, HEAD, status/external evidence and canonical hashes.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md` when done.</output>

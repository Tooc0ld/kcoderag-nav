---
phase: 03.1-javascript-npx
plan: 15
type: execute
wave: 10
depends_on: ["03.1-13"]
files_modified:
  - src/maintainer/retirement-audit.cts
  - tests/maintainer/retirement-audit.test.cts
  - src/generator/index.cts
  - package.json
autonomous: true
requirements: [JS-01, JS-04, JS-06, JS-08]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-05: Python source/runtime/test 删除前，Node 对等测试、legacy migration、三宿主 smoke 和生成/pack 检查必须全绿。"
    - "预退役 gate 复用 exact dependency audit，并生成可读的 owned Python inventory；失败禁止任何删除。"
  artifacts:
    - {path: "src/maintainer/retirement-audit.cts", provides: "Python 退役前后可复用的零残留/对等门禁", exports: ["auditRetirement"]}
  key_links:
    - {from: "src/maintainer/retirement-audit.cts", to: "src/maintainer/dependency-audit.cts", via: "exact graph first", pattern: "deps:audit"}
    - {from: "src/maintainer/retirement-audit.cts", to: "Python inventory", via: "explicit allow-listed retirement manifest", pattern: "retirement"}
---
<objective>在删除任何 Python 文件前建立一次完整、机器可执行且可在删除后复用的 parity/retirement gate。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 定义明确 Python inventory 与零残留审计</name>
  <files>src/maintainer/retirement-audit.cts, tests/maintainer/retirement-audit.test.cts</files>
  <behavior>- pre 模式要求 inventory 中旧 Python 全部存在且每项对应 Node parity evidence。- post 模式要求 inventory 全部消失，并扫描 packed/generated/user docs 无 Python runtime/commands。- 未列出的新增 `.py/.pyc/__pycache__` 或 Python launcher reference 都失败。</behavior>
  <action>从当前 tracked runtime、scripts、tests 和 generated trees建立显式相对路径/类别 inventory，但只存路径不存配置内容。实现 `auditRetirement(pre|post)`，先跑 deps audit，再校验每类对应 npm test/script evidence。扫描范围过滤 `.git/.planning/.gsd/node_modules`，但不能过滤发布 allow-list 或 README/AGENTS 用户指令。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; npm run audit:retirement -- --mode pre</automated></verify>
  <acceptance_criteria>- pre/post 与 surprise Python/reference fixtures 精确失败。- inventory 只覆盖本 phase owned artifacts，不匹配无关用户文件。</acceptance_criteria><done>删除范围、前置证据与最终零残留定义可机器执行。</done>
</task>
<task type="auto">
  <name>Task 2: 执行不可跳过的预退役全套 parity</name>
  <files>src/generator/index.cts, package.json</files>
  <action>添加 `verify:parity-before-retire`，依次执行 `deps:audit`、build、全部 Node tests、Python-vs-CJS hook corpus、legacy migration、三宿主 lifecycle、required smoke、generate check、pack audit 和 retirement pre audit。只有所有子项实际运行且通过才 exit0；任一 NOT_RUN/缺脚本/空测试选择都失败。将成功命令与摘要写入 03.1-15-SUMMARY，不记录输入值。</action>
  <verify><automated>npm run verify:parity-before-retire</automated></verify>
  <acceptance_criteria>- 缺任一 suite 的 fixture 非零。- 成功后 generated tree/worktree 不产生意外修改。</acceptance_criteria><done>Python 删除获得明确、不可跳过的 parity 许可。</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-15-01 | Repudiation | retirement evidence | high | mitigate | 明确 suite/inventory、NOT_RUN 非零、summary 记录命令摘要。 |
| T-03.1-15-02 | Tampering | deletion scope | high | mitigate | explicit tracked path inventory，无 glob 删除。 |
| T-03.1-15-SC | Tampering | dependency graph | high | mitigate | gate 首项复用 exact `deps:audit`。 |
</threat_model>
<verification>`npm run verify:parity-before-retire`</verification>
<success_criteria>- 所有 Node parity/e2e/pack/generation 证据实际运行。- Python inventory 有界且删除尚未发生。</success_criteria>
## Artifacts this phase produces
- New files/symbols: `retirement-audit.cts`, `auditRetirement`, retirement audit tests.
- New scripts: `audit:retirement`, `verify:parity-before-retire`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md` when done.</output>

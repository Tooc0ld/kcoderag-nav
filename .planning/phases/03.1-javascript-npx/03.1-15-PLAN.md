---
phase: 03.1-javascript-npx
plan: 15
type: execute
wave: 16
depends_on: ["03.1-13"]
files_modified:
  - src/maintainer/retirement-audit.cts
  - tests/maintainer/retirement-audit.test.cts
  - src/maintainer/cache-cleanup.cts
  - tests/maintainer/cache-cleanup.test.cts
  - .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json
autonomous: true
requirements: [JS-01, JS-04, JS-05, JS-06, JS-08]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-01/D-05: explicit monotonic modes pre → post-source → post-scripts → post-tests → post are implemented and tested before deletion."
    - "The receipt freezes an immutable production-source baseline commit; later planning/receipt commits are allowed only when the baseline remains an ancestor and the committed production inventory/diff is unchanged."
    - "One exported implementation owns canonical JSON, code-point path ordering/validation, production inventory hashing, receipt verification, and Plan30 cleanup authorization."
    - "`pre_cache_inventory` has exact roots/counts 2/2/2/6/14, total26, sorted unique relative `.pyc` paths and lowercase SHA-256; missing/extra/empty/duplicate/unsorted/mismatch is invalid."
    - "The cleanup CLI is proven on isolated five-root fixtures before Plan30 can invoke it; every rejected fixture performs zero unlink/rmdir operations and writes no success receipt."
    - "Inventory includes kcoderag-update.json and all GitHub-raw/update-index references, which final post mode forbids."
  artifacts:
    - {path: "src/maintainer/retirement-audit.cts", provides: "retirement state machine, shared canonical/path functions, production-baseline inventory and strict receipt verifier", exports: ["auditRetirement", "verifyPreReceipt", "canonicalJson", "compareCodePointPaths", "validateSortedUniquePaths", "buildTrackedProductionInventory", "hashTrackedProductionInventory", "verifyProductionBaseline", "hashAuthorizedSet"]}
    - {path: "tests/maintainer/retirement-audit.test.cts", provides: "exact producer-schema, canonical-path/hash, lineage and production-drift matrix"}
    - {path: "src/maintainer/cache-cleanup.cts", provides: "tested authorize/execute/verify cleanup module and CLI", exports: ["validateProducerReceipt", "validateAuthorizationReceipt", "buildCleanupPlan", "executeCleanupPlan", "verifyCleanupReceipt", "main"]}
    - {path: "tests/maintainer/cache-cleanup.test.cts", provides: "five-root malformed authorization matrix and exact 26-unlink/5-rmdir success fixture"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json", provides: "versioned sanitized parity/cache authorization producer receipt with immutable production baseline"}
  key_links:
    - {from: "repo_head + tracked_production_inventory", to: "Plan30 authorize/execute/verify", via: "verifyProductionBaseline ancestor, inventory-hash and filtered-diff checks", pattern: "tracked_production_inventory_sha256"}
    - {from: "pre_cache_inventory", to: "cache-cleanup CLI", via: "shared canonicalJson and code-point path validator", pattern: "authorized_set_sha256"}
---
<objective>在任何删除前实现完整退役与 cleanup 授权链，运行全 parity，并生成不受后续规划提交影响的生产源码基线 receipt。</objective>
<execution_context>@gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-13-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 实现共享 canonical、生产基线与 versioned receipt verifier</name><files>src/maintainer/retirement-audit.cts, tests/maintainer/retirement-audit.test.cts</files><behavior>- `canonicalJson` recursively code-point sorts object keys and rejects non-canonical values.- one shared comparator validates every path-bearing sorted list.- a planning-only descendant of the frozen baseline passes; non-ancestor or any committed production drift fails.- every missing/empty/duplicate/unsorted/extra/mismatched receipt field fails closed.</behavior><action>保留 explicit tracked inventory 与 `pre|post-source|post-scripts|post-tests|post` 单调 state machine，并实现唯一 `kcoderag-nav/pre-retirement-receipt@1` producer/verifier contract。

导出 `compareCodePointPaths(a,b)`，按 `Array.from` 后的 Unicode scalar value 逐项数值比较，较短相同前缀优先；导出 `validateSortedUniquePaths`，要求 normalized POSIX repo-relative path、无反斜杠/NUL/`.`/`..`/absolute/case-fold collision，并使用该 comparator 验证严格升序和唯一性。导出 `canonicalJson(value)`：递归用同一 code-point comparator 排 object keys；array 保持已经由 schema/path validator 验证的规定顺序；拒绝 undefined、稀疏 array、NaN/Infinity、非 plain object 和未知 schema keys；输出无空格 UTF-8 JSON、无 BOM/尾随换行。所有 root_counts、files、status/external/inventory records 和 receipt hashes只能调用这些共享函数；禁止依赖 object insertion order 或 locale-sensitive ordering。

定义 `tracked_production_inventory` 为从 `git ls-tree -r -z <repo_head>` 得到的所有 tracked production records `{path,mode,blob_oid,sha256}`，按共享 path comparator 严格排序。每条 `sha256` 来自 baseline blob bytes。排除规则只允许 `.planning/**`、`.gsd/**`，以及显式 receipt/SUMMARY allow-list `03.1-15-PRE-RETIREMENT-RECEIPT.json`、`03.1-15-SUMMARY.md`、`03.1-30-CACHE-AUTH-RECEIPT.json`、`03.1-30-CACHE-CLEANUP-RECEIPT.json`、`03.1-30-SUMMARY.md`；不得排除任何 `src/**`、`tests/**`、package/generated/plugin/docs production path。`tracked_production_inventory_sha256=sha256(UTF8(canonicalJson(tracked_production_inventory)))`。

导出 `verifyProductionBaseline(receipt,currentHead)`：验证 `repo_head` 是 nonempty lowercase 40-hex commit，执行 `git merge-base --is-ancestor repo_head currentHead`；从 current HEAD tree 按完全相同规则重建 production inventory并要求 records与hash exact equal；解析 `git diff --name-only -z repo_head..currentHead --`，过滤只允许上述 planning/receipt prefixes/paths，任何其它 path 都失败。它不得要求 current HEAD 等于 baseline，因此后续 GSD planning/receipt commits可以作为 descendant；测试必须覆盖 planning-only descendant PASS、production descendant FAIL、non-ancestor FAIL、path/mode/blob/digest/inventory hash drift FAIL。

`pre_cache_inventory` exact shape 为 `{roots:[{path,count}],root_counts:{"plugin-src/hooks/__pycache__":2,"kcoderag-qa/hooks/__pycache__":2,"kcoderag-dev/hooks/__pycache__":2,"scripts/__pycache__":6,"tests/__pycache__":14},total:26,files:[{path,sha256}]}`；roots 固定顺序，files 使用共享 validator，path 限于 exact roots 且仅 `.pyc`，SHA-256 为 lowercase 64-hex。`authorized_set_sha256=sha256(UTF8(canonicalJson(pre_cache_inventory)))`；`receipt_sha256=sha256(UTF8(canonicalJson(topLevelReceiptWithoutReceiptSha256)))`，只排除 self field。

保留 exact `unrelated_status_before` 和 nonempty sorted `root_external_digests_before` contract。Top-level exact allow-list 改为 `schema_version,repo_head,tracked_production_inventory,tracked_production_inventory_sha256,suites,generated_sha256,pack_sha256,timestamp,pre_cache_inventory,authorized_set_sha256,unrelated_status_before,root_external_digests_before,receipt_sha256`。`--verify-pre-receipt` 必须调用 `verifyProductionBaseline`，再验证 exact schema/hash/status/external/suite/cache state；cache 只接受 receipt exact match 或五根全部不存在，partial/替换/额外失败。</action><verify><automated>npm run build &amp;&amp; npm run test:retirement-audit</automated></verify><acceptance_criteria>- shared canonicalJson recursively code-point sorts keys, shared path comparator/validator governs every sorted path list, and tests reject locale/insertion-order dependence.- exact receipt fields include the full tracked production inventory and its canonical hash.- baseline ancestor plus planning-only descendant passes without HEAD equality; non-ancestor, committed production diff or inventory drift fails.- all schema/cache/status/external malformed cases fail closed.</acceptance_criteria><done>Plan15 owns a single canonical implementation and a production baseline contract that survives only allowed planning descendants。</done></task>

<task type="auto" tdd="true"><name>Task 2: 实现并隔离验证 cleanup authorization/execute/verify CLI</name><files>src/maintainer/cache-cleanup.cts, tests/maintainer/cache-cleanup.test.cts</files><behavior>- validation builds an explicit 26-unlink/5-rmdir plan before any mutation.- every malformed or drifted fixture returns nonzero with unlink_count=0, rmdir_count=0, no success receipt and byte-identical fixture.- success performs exactly 26 explicit unlinks followed by five empty nonrecursive rmdirs.- authorize/execute/verify all enforce the production baseline through the shared verifier.</behavior><action>实现 `src/maintainer/cache-cleanup.cts`，只从 retirement-audit 导入并调用 `canonicalJson`、`compareCodePointPaths`、`validateSortedUniquePaths`、`verifyProductionBaseline`、`verifyPreReceipt` 和 hash helpers，不另写 serializer、root_counts/files ordering 或 path comparator。导出 `validateProducerReceipt`、`validateAuthorizationReceipt`、`buildCleanupPlan`、`executeCleanupPlan`、`verifyCleanupReceipt`、`main`；提供 compiled CLI commands `authorize --producer <path> --authorization <path>`、`execute --producer <path> --authorization <path> --cleanup-receipt <path>`、`verify --producer <path> --authorization <path> --cleanup-receipt <path>`。

所有 commands 先验证 producer schema/self hash/authorized hash/production baseline，再验证 root counts 2/2/2/6/14、total26、sorted unique path+SHA、status/external fields、resolved containment、regular nonsymlink `.pyc` files、无 `.pyo`/extra/child directory/reparse escape。Authorization exact allow-list 为 `schema_version,source_schema_version,repo_head,tracked_production_inventory,tracked_production_inventory_sha256,plan15_receipt_sha256,authorized_set_sha256,pre_cache_inventory,unrelated_status_before,root_external_digests_before,receipt_sha256`；cleanup exact allow-list 为 `schema_version,repo_head,tracked_production_inventory_sha256,plan15_receipt_sha256,authorization_receipt_sha256,authorized_set_sha256,pre_cache_inventory,observed_deletions,deletion_set_equal,removed_roots,unrelated_status_before,unrelated_status_after,root_external_digests_before,root_external_digests_after,receipt_sha256`。两者都用 shared `canonicalJson` 的 omit-only-self hash，并用 shared code-point comparator/validator 处理 root_counts、files、observed_deletions、removed_roots及所有 path lists；禁止以 `JSON.stringify` insertion-order equality 或 `localeCompare` 实现契约比较。

`buildCleanupPlan` 只从 frozen inventory 生成26个 explicit unlink targets和固定 five exact roots 的 rmdir targets；完成所有验证前不可调用 mutation adapter或写 success receipt。`executeCleanupPlan` 逐项重新 lstat/realpath/hash后 unlink，只有 observed set exact equal后才检查五 root empty并逐个非递归 rmdir；receipt只在全部成功后原子写。`verifyCleanupReceipt` 必须内部调用 shared `verifyPreReceipt` 接受 all-roots-absent state，使 Plan30 live task不需要调用任何第二套 verifier。

测试使用 temp git repo + 五 root fixture与注入式 fs mutation adapter。Malformed matrix 必须逐项覆盖 missing、empty、invalid hash/path/count、duplicate、unsorted、extra、producer/authorization/disk mismatch、receipt/authorized hash mismatch、symlink/reparse escape、baseline non-ancestor、current committed production inventory drift；还要覆盖 unknown/undefined fields和 partial cache state。每个 negative case在执行前后对 fixture 全树 path/type/bytes SHA-256 manifest exact equal，断言 exit nonzero、adapter `unlink_count=0`、`rmdir_count=0`、authorization/cleanup success receipt均不存在。Success fixture断言 exact frozen 26 paths各 unlink一次、五 exact roots各 empty nonrecursive rmdir一次、无其它 mutation，并验证 sanitized cleanup receipt。先写失败测试，再实现；修改 `.cts` 后显式 `npm run build` 才运行 compiled tests。</action><verify><automated>npm run build &amp;&amp; node --test dist-tests/maintainer/cache-cleanup.test.cjs</automated></verify><acceptance_criteria>- malformed matrix contains every named case and proves zero mutation/no receipt/byte-identical fixture.- success evidence is exactly 26 explicit unlink calls and five exact nonrecursive rmdir calls.- every CLI subcommand calls shared baseline/canonical/path validation; no duplicated ordering or serialization implementation exists.- build precedes compiled test execution.</acceptance_criteria><done>Plan30 receives one fully tested CLI whose validation phase is mutation-free and whose execution set is explicit。</done></task>

<task type="auto"><name>Task 3: 运行全 parity 并原子持久化 immutable production-baseline receipt</name><precondition>Task 1 and Task 2 production source/tests are committed, `git rev-parse HEAD` names that commit, and no later production commit exists.</precondition><read_first>- `src/maintainer/retirement-audit.cts` exact schema/canonical/path/baseline exports
- `src/maintainer/cache-cleanup.cts` producer consumer contract
- Plan30 exact consumer contract in `03.1-30-PLAN.md`
- Current five cache roots and adjacent regular files; never log their bytes</read_first><files>.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</files><action>先执行 build、两套 compiled tests和 `verify:parity-before-retire` 所有 required suites；missing/NOT_RUN/empty selection均失败，且这是最后一次 Python invocation。suite 全绿后，把当前已包含两个 modules/tests的 commit冻结为 immutable `repo_head`；从该 commit tree采集 exact `tracked_production_inventory`并计算 `tracked_production_inventory_sha256`。按 Task1 schema采集 exact `pre_cache_inventory`、filtered `unrelated_status_before` canonical bytes/entries和 nonempty `root_external_digests_before`，计算 authorized-set hash，再按 exact allow-list/self-omit formula原子写 receipt。

写后从磁盘重新 parse并调用 `--verify-pre-receipt`：baseline必须是 current HEAD 的 ancestor（此刻可相等，之后不要求相等），current HEAD production inventory/hash与 baseline exact equal，filtered baseline..HEAD diff无 production path；所有 required fields defined/nonempty，receipt/authorized hashes重算相等，status/external/cache闭环。任一失败删除无效临时 receipt并停止；receipt不含命令输出、配置值、cache bytes或凭据。</action><verify><automated>npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; node --test dist-tests/maintainer/cache-cleanup.test.cjs &amp;&amp; npm run verify:parity-before-retire &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</automated></verify><acceptance_criteria>- exact receipt exists only after both modules are compiled/tested and every parity suite passes.- repo_head names the production-source baseline commit; full sorted production inventory/hash recomputes, and verifier accepts later planning-only descendants without current-HEAD equality.- cache roots/counts 2/2/2/6/14/total26 and all receipt/status/external hashes recompute.- no later plan invokes Python.</acceptance_criteria><done>Plan30 receives a complete, immutable and cryptographically self-consistent producer receipt plus a tested cleanup CLI。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git commit/receipt → cleanup CLI | Untrusted lineage, path and hash evidence must validate before filesystem mutation. |
| Receipt inventory → local filesystem | Frozen relative paths cross into destructive unlink/rmdir operations. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-15-01 | Repudiation | parity authorization | high | mitigate | exact schema, immutable ancestor baseline, canonical inventory/authorized/receipt hashes and strict offline verification. |
| T-03.1-15-02 | Tampering | production baseline | high | mitigate | full tracked tree inventory plus filtered baseline..HEAD diff; planning descendants allowed, production changes rejected. |
| T-03.1-15-03 | Elevation of Privilege | cleanup paths | high | mitigate | validated explicit unlink/rmdir plan, containment and symlink/reparse rejection before mutation. |
| T-03.1-15-04 | Tampering | cache/status/external inventories | high | mitigate | exact counts/types/code-point order, byte/hash closure and malformed fixture matrix. |
| T-03.1-15-05 | Information Disclosure | receipts | high | mitigate | allow-listed paths/booleans/hashes/status metadata only; no file/config/cache bytes or credentials. |
</threat_model>
<verification>`npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; node --test dist-tests/maintainer/cache-cleanup.test.cjs &amp;&amp; npm run verify:parity-before-retire &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json`; malformed receipt/baseline/cleanup fixtures must all fail with zero mutations.</verification>
<success_criteria>- Shared canonical/path/baseline functions and all retirement modes are tested.- Cleanup CLI passes the exact negative matrix and 26/5 success fixture before live use.- A valid immutable production-baseline receipt exists before any deletion.</success_criteria>
## Artifacts this phase produces
- Shared `canonicalJson`, code-point path comparator/validator, tracked production inventory/hash and baseline lineage verifier.
- Tested cache-cleanup authorize/execute/verify module and compiled CLI contract.
- `kcoderag-nav/pre-retirement-receipt@1` with exact cache inventory, immutable production baseline inventory/hash, status/external evidence and canonical hashes.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md` when done.</output>

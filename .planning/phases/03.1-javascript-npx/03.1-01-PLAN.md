---
phase: 03.1-javascript-npx
plan: 01
type: execute
wave: 2
depends_on: ["03.1-29"]
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.tests.json
  - src/maintainer/dependency-audit.cts
  - tests/maintainer/dependency-audit.test.cts
  - src/bin/kcoderag-nav.cts
  - src/tracer/codex-install.cts
  - tests/tracer/codex-install.test.cts
autonomous: false
requirements: [JS-01, JS-02, JS-03, JS-04]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "RESEARCH/D-06: 在任何 npm install 或公共命令实现前，人工同时确认 SUS exact graph 与 one-way 公共包名契约。"
    - "D-02/D-15: 仅用受审计的 TypeScript 开发图生成 Node 22+ 可直接执行的 CJS，production dependencies 为空，engines.node 精确为 >=22。"
    - "npm `files` 是逐路径发布 allow-list；源码、tests、planning、dist-tests、node_modules 与 Python 不可进入 tarball。"
    - "D-11/D-18: Codex QA tracer 在确认绝对目标后以单宿主事务写入，失败恢复原树。"
  artifacts:
    - {path: "src/maintainer/dependency-audit.cts", provides: "依赖图、脚本、resolution 与 integrity 的机器审计", exports: ["auditDependencyGraph"]}
    - {path: "src/bin/kcoderag-nav.cts", provides: "公共 CJS CLI 入口", exports: ["main"]}
    - {path: "tests/tracer/codex-install.test.cts", provides: "npx 到临时 Codex 项目的纵切回归"}
  key_links:
    - {from: "package-lock.json", to: "src/maintainer/dependency-audit.cts", via: "锁图与 npm ls JSON 三方比对", pattern: "auditDependencyGraph"}
    - {from: "package.json", to: "dist/bin/kcoderag-nav.cjs", via: "bin.kcoderag-nav", pattern: "dist/bin/kcoderag-nav\\.cjs"}
---

<objective>
先通过依赖合法性与 D-06 双重不可逆门禁，再建立可复用的机器供应链审计并跑通 Codex QA 的 production-quality npx tracer。

Purpose: 在扩展三宿主前证明公共入口、受审计构建图、目标边界与回滚架构真实可行。
Output: npm/TypeScript 骨架、依赖审计器、公共 CLI 与 Codex QA 端到端 tracer。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-RESEARCH.md
@.planning/phases/03.1-javascript-npx/COVERAGE.md
@.planning/phases/03.1-javascript-npx/03.1-29-SUMMARY.md
@scripts/manage_project_install.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: 接受 exact npm 图并最终确认 kcoderag-nav 公共契约</name>
  <decision>在任何 package-manager install 前记录两项人工 disposition：接受 `typescript >=5.7.0 &lt;7.0.0`/lock `6.0.3`、`@types/node &gt;=22.0.0 &lt;23.0.0`/lock `22.20.1`、sole transitive `undici-types ~6.21.0`/lock `6.21.0` 的 exact graph；确认 D-06 未 scoped 包名 `kcoderag-nav` 与 `npx kcoderag-nav@latest install`。</decision>
  <context>03.1-RESEARCH.md 的官方 audit verdict 确认 identity、repository/publisher、registry integrity/signature 与无 lifecycle scripts；GSD seam 对后两项保留 `SUS:too-new`，所以仍由人显式接受。确认只覆盖上述 ranges、resolution、integrity、ownership、scripts 与唯一 transitive edge；任一值、依赖边或生命周期脚本变化都重新审计。D-06 发布后是 one-way 外部契约。</context>
  <options>
    <option id="accept-audit-and-name"><name>接受 exact graph 并确认 kcoderag-nav（推荐）</name><pros>与官方审计、锁目标和 D-06 一致。</pros><cons>图漂移需重审；首次发布后改名成本高。</cons></option>
    <option id="stop-and-reaudit"><name>停止并重新审计/讨论</name><pros>不在未接受警告或名称下形成安装与发布契约。</pros><cons>Phase 03.1 暂停，后续安装任务不得执行。</cons></option>
  </options>
  <resume-signal>仅 `accept-audit-and-name` 可继续，并在 03.1-01-SUMMARY.md 记录 exact graph、无 lifecycle scripts、integrity 审计已接受及再审条件；否则停止。</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 锁定受批准工具链并注册完整 npm 命令表</name>
  <reversibility rating="one-way">本任务首次写入 D-06 公共 `kcoderag-nav` name/bin；发布后撤销需要兼容包与迁移。</reversibility>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.tests.json</files>
  <behavior>
    - dependencies 为空，只有已接受 ranges/lock targets；安装禁用 lifecycle scripts；`engines.node` 精确为 `>=22`。
    - tsconfig.json 把 `src/**/*.cts` 编译到 `dist/**/*.cjs`；tsconfig.tests.json 把 `tests/**/*.cts` 编译到 `dist-tests/**/*.cjs`，不需要运行时编译。
    - `files` 逐项允许 `dist/` 和三产品所需的确切生成文件；不使用会吞入 Python/source/test/planning 的产品目录通配。
    - 所有后续 verify 使用的 npm script 在本任务一次注册，后续计划不得争用 package.json；只有 `build` 写仓库 `dist/`/`dist-tests/`，所有 `test:*` 只读 compiled CJS。
  </behavior>
  <action>以 `npm install --save-dev ... --ignore-scripts` 创建锁文件。`tsconfig.json` 仅编译 `src/**` 到 `dist/**`，`tsconfig.tests.json` 仅编译 `tests/**` 到 `dist-tests/**`。设置 `engines.node` 精确为 `>=22`，公共 bin 固定为 `dist/bin/kcoderag-nav.cjs`。设置 exact `files` 数组：`dist/`；QA/Dev 各自的 `.codex.mcp.json`、`.mcp.json`、`.codex-plugin/plugin.json`、`.claude-plugin/plugin.json`、`agents/kcode-explorer.md`、`hooks/grep-nudge.cjs`、`hooks/update-check.cjs`、`hooks/update-worker.cjs`、`hooks/run_hook.cmd`、`hooks/run_hook.sh`、`hooks/hooks.json`、`skills/code-lookup-discipline/SKILL.md`、`README.md`；Cursor 的 `.cursor-plugin/plugin.json`、`mcp.json`、`rules/kcoderag-navigation.mdc`、`skills/code-lookup-discipline/SKILL.md`、`README.md`。仅再接受 npm 自动包含的根 `package.json` 与 `README.md`，不列产品目录或源码目录通配。

注册以下 exact command map（`&amp;&amp;` 表示 package.json 中的 shell chaining）：`build=tsc -p tsconfig.json &amp;&amp; tsc -p tsconfig.tests.json`，且它是唯一写仓库 `dist/`/`dist-tests/` 的 script；`test=node --test dist-tests`；`deps:audit=node dist/maintainer/dependency-audit.cjs`；所有 compiled tests 分别为 `test:dependency-audit=node --test dist-tests/maintainer/dependency-audit.test.cjs`、`test:tracer=node --test dist-tests/tracer/codex-install.test.cjs`、`test:transaction=node --test dist-tests/core/transaction.test.cjs`、`test:cli=node --test dist-tests/cli/commands.test.cjs`、`test:hook=node --test dist-tests/hooks/grep-nudge.test.cjs`、`test:launcher=node --test dist-tests/hooks/launcher.test.cjs`、`test:host:codex=node --test dist-tests/hosts/codex.test.cjs`、`test:host:claude=node --test dist-tests/hosts/claude.test.cjs`、`test:host:cursor=node --test dist-tests/hosts/cursor.test.cjs`、`test:cross-host=node --test dist-tests/hosts/cross-host.test.cjs`、`test:migration=node --test dist-tests/migration/legacy-state.test.cjs`、`test:update-check=node --test dist-tests/hooks/update-check.test.cjs`、`test:generator=node --test dist-tests/generator/generation.test.cjs`、`test:generator:repository=node --test dist-tests/generator/repository-generation.test.cjs`、`test:precommit=node --test dist-tests/maintainer/pre-commit.test.cjs`、`test:pack=node --test dist-tests/maintainer/pack-audit.test.cjs`、`test:ci-contract=node --test dist-tests/maintainer/ci-contract.test.cjs`、`test:smoke=node --test dist-tests/smoke/host-smoke.test.cjs`、`test:release=node --test dist-tests/maintainer/release.test.cjs`、`test:publish-receipt=node --test dist-tests/maintainer/publish-receipt.test.cjs`、`test:release-workflow=node --test dist-tests/maintainer/release-workflow.test.cjs`、`test:retirement-audit=node --test dist-tests/maintainer/retirement-audit.test.cjs`、`test:docs=node --test dist-tests/maintainer/docs-check.test.cjs`、`test:sibling-guide-audit=node --test dist-tests/maintainer/sibling-guide-audit.test.cjs`。

维护命令固定为 `generate=node dist/generator/index.cjs`、`generate:check=node dist/generator/index.cjs --package all --group all --check`、`pack:audit=node dist/maintainer/pack-audit.cjs`、`smoke:required=node dist/smoke/host-smoke.cjs --mode required`、`smoke:live=node dist/smoke/host-smoke.cjs --mode live`、`audit:retirement=node dist/maintainer/retirement-audit.cjs`、`verify:parity-before-retire=node dist/maintainer/retirement-audit.cjs --run-parity --mode pre --receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json`、`docs:check=node dist/maintainer/docs-check.cjs`、`release:patch=node dist/maintainer/release.cjs patch`、`release:minor=node dist/maintainer/release.cjs minor`、`release:major=node dist/maintainer/release.cjs major`。`ci:local` 精确组合 `npm run deps:audit &amp;&amp; npm run build &amp;&amp; npm test &amp;&amp; npm run generate:check &amp;&amp; npm run smoke:required &amp;&amp; npm run pack:audit`。未来入口此时可尚未生成，但 path/flag/owner 已固定；静态自审拒绝任何 script 引用 `.cts`、Python、ts-node，且拒绝除 `build` 外的 script 含 `tsc`、写 `dist` 或隐式调用 `build`。</action>
  <verify><automated>npm ci --ignore-scripts &amp;&amp; node -e "const p=require('./package.json');if(Object.keys(p.dependencies||{}).length||p.engines?.node!=='&gt;=22')process.exit(1);const names='build test deps:audit test:dependency-audit test:tracer test:transaction test:cli test:hook test:launcher test:host:codex test:host:claude test:host:cursor test:cross-host test:migration test:update-check test:generator test:generator:repository generate generate:check test:precommit test:pack pack:audit test:ci-contract ci:local test:smoke smoke:required smoke:live test:release test:publish-receipt test:release-workflow test:retirement-audit audit:retirement verify:parity-before-retire test:docs docs:check test:sibling-guide-audit release:patch release:minor release:major'.split(' ');if(names.some(n=&gt;!p.scripts[n]))process.exit(1);if(Object.entries(p.scripts).some(([n,v])=&gt;n!=='build'&amp;&amp;(/\btsc\b/.test(v)||/npm run build/.test(v))))process.exit(1)"</automated></verify>
  <acceptance_criteria>
    - package.json 没有 production dependency、runtime compiler、CLI parser、bundler或测试框架。
    - 后续所有计划引用的 npm script 已注册且指向 compiled CJS；本任务不运行尚未创建的入口。
  </acceptance_criteria>
  <done>工具链、唯一 script registry 与 D-06 package/bin 契约先于所有后续执行入口存在。</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: 跑通 Codex QA 的 npx 安装纵切路径</name>
  <files>src/maintainer/dependency-audit.cts, tests/maintainer/dependency-audit.test.cts, src/bin/kcoderag-nav.cts, src/tracer/codex-install.cts, tests/tracer/codex-install.test.cts</files>
  <behavior>
    - `install --host codex --environment qa --target <tmp> --yes --json` 写出 MCP、hook、skill 和版本化 state，并只输出安全 JSON。
    - 默认 cwd；交互写前展示规范化绝对路径，拒绝确认、Node&lt;22、路径逃逸、漂移或注入失败均零写入。
    - 构建输出为直接运行的 `.cjs`，不加载 TypeScript/Python。
  </behavior>
  <action>先写 dependency audit 与端到端红色测试。实现 `auditDependencyGraph`，同时解析 package.json、lock v3 与 `npm ls --all --json`，拒绝额外 direct/transitive、父边、overrides/resolutions、lifecycle scripts、resolution/integrity 或 approved exact graph 漂移；然后实现 `main`/`runCli`/`installCodexQa` 纵切，先运行依赖/运行时/路径/确认/冲突校验，再完整 staging、原子替换、state-last 和恢复。凭据源作为不透明字节，只断言摘要与路径。保留 `--host`、`--environment`、`--target`、`--yes`、`--json` 契约。</action>
  <verify><automated>npm run build &amp;&amp; npm run deps:audit &amp;&amp; npm run test:dependency-audit &amp;&amp; npm run test:tracer</automated></verify>
  <acceptance_criteria>
    - `dist/bin/kcoderag-nav.cjs` 在 Node 22+ 直接执行；package bin 指向它。
    - 成功树只含预期受管内容；取消、拒绝和每个失败点前后树快照相同。
    - stdout/stderr/test snapshot 不含 MCP 配置值或凭据。
  </acceptance_criteria>
  <done>D-02、D-06、D-11、D-15、D-18 的第一条真实用户路径端到端通过。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-01-SC | Tampering | npm dependency graph | high | mitigate | blocking-human disposition、exact lock/integrity/edge/script audit、`--ignore-scripts` 与漂移重审。 |
| T-03.1-01-01 | Tampering | target transaction | high | mitigate | 规范路径、拒绝 symlink/root escape、state-last 原子提交与全量恢复。 |
| T-03.1-01-02 | Information Disclosure | CLI diagnostics | high | mitigate | 不透明凭据输入与安全 code/path 输出。 |
</threat_model>

<verification>
`npm ci --ignore-scripts &amp;&amp; npm run deps:audit &amp;&amp; npm run build &amp;&amp; npm run test:dependency-audit &amp;&amp; npm run test:tracer`
</verification>

<success_criteria>
- exact graph 与 one-way 名称都有人类明确接受且留痕。
- 机器审计可拒绝额外包、脚本及 resolution/integrity 漂移。
- Codex QA tracer 端到端可运行、可回滚且无敏感输出。
</success_criteria>

## Artifacts this phase produces

- New files: `package.json`, `package-lock.json`, `tsconfig.json`, `tsconfig.tests.json`, dependency audit source/test, CLI/tracer source/test.
- New symbols: `auditDependencyGraph`, `main`, `runCli`, `installCodexQa`.
- New CLI command/flags: `install`, `--host`, `--environment`, `--target`, `--yes`, `--json`; package script `deps:audit`.

<output>Create `.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md` when done.</output>

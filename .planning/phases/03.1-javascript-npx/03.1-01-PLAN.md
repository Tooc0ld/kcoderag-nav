---
phase: 03.1-javascript-npx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - src/bin/kcoderag-nav.cts
  - src/tracer/codex-install.cts
  - tests/tracer/codex-install.test.cts
autonomous: false
requirements: [JS-01, JS-02, JS-03, JS-04]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-06: 用户确认公共包名 `kcoderag-nav` 与 `npx kcoderag-nav@latest install` 成为外部安装契约后，执行器才实现该入口。"
    - "D-02/D-15: 仓库具有 Node.js 22+ 可构建的 TypeScript 工具链，运行产物是直接执行的 `.cjs`，不需要 ts-node 或用户侧编译。"
    - "RESEARCH: 只有 `typescript` 与 `@types/node` 两个 direct devDependencies；lock targets 为 6.0.3、22.20.1 与传递的 undici-types 6.21.0，production dependencies 为空。"
    - "D-11: 不传 `--target` 时使用 cwd，任何写入前展示规范化绝对目标并确认；自动化可显式使用 `--yes`。"
    - "D-18: Codex QA tracer 在完整写前校验后以单宿主事务写入，注入失败会恢复目标项目原状。"
  artifacts:
    - path: "package.json"
      provides: "公共 npm 包元数据、Node engine、bin、files allow-list 与构建/测试脚本"
      contains: "kcoderag-nav"
    - path: "src/bin/kcoderag-nav.cts"
      provides: "编译为 dist/bin/kcoderag-nav.cjs 的 CLI 入口"
      exports: ["main"]
    - path: "src/tracer/codex-install.cts"
      provides: "Codex QA 安装的首条生产纵切路径"
      exports: ["runCli", "installCodexQa"]
    - path: "tests/tracer/codex-install.test.cts"
      provides: "从 CLI 参数到临时项目受管文件的端到端回归"
  key_links:
    - from: "package.json"
      to: "dist/bin/kcoderag-nav.cjs"
      via: "bin.kcoderag-nav"
      pattern: "dist/bin/kcoderag-nav\\.cjs"
    - from: "src/bin/kcoderag-nav.cts"
      to: "src/tracer/codex-install.cts"
      via: "runCli 调用"
      pattern: "runCli"
    - from: "src/tracer/codex-install.cts"
      to: ".codex/ and .agents/skills/"
      via: "规范化路径检查后的原子写入计划"
      pattern: "installCodexQa"
---

<objective>
跑通一条可保留的端到端纵切路径：用本地打包的公共 CLI 入口把 QA 集成安装到临时 Codex 项目，并证明 Node 运行时检查、目标确认、项目边界和回滚同时生效。

Purpose: 尽早验证 `npx -> bin CJS -> CLI -> Codex 项目文件 -> install-state` 的整体架构，避免在三宿主扩展后才发现包入口或事务模型不可用。
Output: npm/TypeScript 构建骨架、公共 CLI、Codex QA tracer 与端到端测试。
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@.planning/phases/03.1-javascript-npx/03.1-PATTERNS.md
@.planning/phases/03.1-javascript-npx/03.1-RESEARCH.md
@scripts/manage_project_install.py
@plugin-src/environments.json

<interfaces>
The tracer must establish these durable public contracts for later plans:
- `main(argv?: string[]): Promise<number>` owns process-boundary exit behavior.
- `runCli(argv, io, runtime): Promise<CliResult>` returns stable data; it never prints sensitive source values.
- `installCodexQa(request, dependencies): Promise<InstallResult>` accepts an explicit normalized target and a canonical QA input provider.
- Mutating flags introduced here: `--host codex`, `--environment qa`, `--target <path>`, `--yes`, `--json`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认公共 npm 包名与标准 npx 命令</name>
  <decision>D-06 one-way 门：确认发布未 scoped 公共包 `kcoderag-nav`，标准命令为 `npx kcoderag-nav@latest install`。</decision>
  <context>包名和命令一旦被发布并写入用户文档就是外部契约；以后改名需要兼容包、迁移说明和已安装项目更新路径。用户在 discuss-phase 已选择该名称，本门禁在首次实现前保留最终确认。</context>
  <options>
    <option id="confirm-kcoderag-nav">
      <name>确认 kcoderag-nav（推荐）</name>
      <pros>与 D-06、现有项目命名和计划的更新提示一致。</pros>
      <cons>首次公开发布后改名成本高。</cons>
    </option>
    <option id="stop-before-contract">
      <name>停止并重新讨论包名</name>
      <pros>在形成外部契约前仍可改名。</pros>
      <cons>Phase 03.1 暂停，后续计划不得执行。</cons>
    </option>
  </options>
  <resume-signal>选择 confirm-kcoderag-nav 继续，或 stop-before-contract 停止。</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: 跑通 Codex QA 的本地 npm 包安装纵切链路</name>
  <reversibility rating="one-way">D-06 的公共包名和 bin 命令被文档及已安装项目引用后，撤销需要兼容发布与用户迁移。</reversibility>
  <read_first>
    - `.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md` D-02、D-06、D-11、D-15、D-18
    - `.planning/phases/03.1-javascript-npx/03.1-RESEARCH.md` Standard Stack、Package Legitimacy Audit、Supply-chain conclusion 与 Validation Architecture
    - `scripts/manage_project_install.py` 的 `_safe_target`、`_assert_managed_path`、`_desired_install`、`_apply_transaction`、`main`
    - `plugin-src/environments.json` 只读取环境键与非敏感结构；MCP 凭据源只作为不透明字节输入，不写入日志或测试快照
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, src/bin/kcoderag-nav.cts, src/tracer/codex-install.cts, tests/tracer/codex-install.test.cts</files>
  <behavior>
    - `install --host codex --environment qa --target <tmp> --yes --json` 在 Node 22+ 下写出 Codex QA 的 MCP、hook、skill 与版本化 install-state，并只输出一个无敏感值的 JSON 结果。
    - 不传 target 时选择 cwd；交互模式在写入前显示规范化绝对路径并等待确认，拒绝确认时零写入。
    - 目标逃逸、符号链接祖先、预写冲突、Node 版本不足或注入的替换失败都不会留下部分安装；失败对象只包含稳定 code 与安全相对路径。
    - 编译输出是 `.cjs`，用户路径不调用 ts-node、tsc 或任何运行时编译器。
  </behavior>
  <action>
    先写失败的端到端测试，再创建根 `package.json`、锁文件和 TypeScript 配置。package name 固定为 `kcoderag-nav`，`engines.node` 要求 22+，`bin.kcoderag-nav` 指向编译后的 CJS。按官方审计接受的 direct devDependency ranges 固定 `typescript >=5.7.0 <7.0.0` 与 `@types/node >=22.0.0 <23.0.0`，使用 `--ignore-scripts` 生成锁文件；审计日 lock targets 必须精确解析为 `typescript@6.0.3`、`@types/node@22.20.1` 和唯一传递项 `undici-types@6.21.0`。`dependencies` 必须为空，不直接声明 `undici-types`，也不增加 CLI/test/bundler/runtime package。使用 `.cts` 让 TypeScript 直接产出 `.cjs`，files allow-list 只暴露运行时 CJS 与后续宿主资产。

    在源码/摘要中保留审计警告：GSD bare-name seam 将 `@types/node` 与 `undici-types` 标成 `SUS: too-new`，但 exact-version 官方 npm metadata、DefinitelyTyped/Node.js ownership、无 lifecycle scripts、完整性与签名已支持本计划的版本范围和 lock targets；该警告是审计证据，不是重复执行 checkpoint。若实际 resolution、integrity、lifecycle scripts、repository ownership 或依赖图与上述快照不同，则在安装前停止并重新审计。

    实现 bin 与 Codex QA tracer：解析显式宿主/环境/目标参数，默认 cwd 和 QA；在任何项目写入前完成 Node 版本、目标规范化、确认、路径边界、受管冲突与不透明 QA 源加载。构建完整 desired-state，先暂存所有字节、再原子替换、最后写 install-state；注入任一步失败时恢复所有本宿主原始字节。不得读取或输出 Bearer/URL/header 内容，测试使用合成 MCP 文档并只断言结构、摘要和路径。

    保持外部 CLI 契约可由后续共享模块重构而不改变：stdout 在 `--json` 下只有一个 schema-versioned JSON；人工提示与拒绝走 stderr；退出码区分成功、用户取消、写前拒绝和事务失败。
  </action>
  <verify>
    <automated>npm ci --ignore-scripts &amp;&amp; npm ls --all &amp;&amp; npm run build &amp;&amp; npm run test:tracer</automated>
  </verify>
  <acceptance_criteria>
    - 构建产物包含 `dist/bin/kcoderag-nav.cjs`，执行它不加载 TypeScript 或 Python。
    - package-lock 精确锁定 `typescript@6.0.3`、`@types/node@22.20.1`、`undici-types@6.21.0`，dependencies 为空且 npm ls 没有其它包。
    - 临时 Codex 项目获得且仅获得预期 `.codex/`、`.agents/skills/` 受管内容，install-state 版本等于 package.json 版本。
    - 取消、Node 不足、路径逃逸和注入失败的树快照在调用前后完全一致。
    - 测试输出、JSON 结果和错误消息不包含 MCP 配置值或凭据值。
  </acceptance_criteria>
  <done>本地构建的 `kcoderag-nav` CJS CLI 可以安全安装 Codex QA，并以端到端自动化证明 D-02、D-06、D-11、D-15、D-18 的第一条真实路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm package metadata -> build | 第三方开发包进入构建环境，必须先经过合法性审计并锁定完整性。 |
| CLI args/cwd -> project filesystem | 用户路径、现有配置和符号链接均不可信，写入只能落在 adapter 声明的项目根内。 |
| credential-bearing canonical input -> diagnostics | MCP 配置可被复制到受管文件，但内容不能进入 stdout、stderr、测试或计划制品。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01-SC | Tampering | typescript / @types/node supply chain | high | mitigate | 官方 exact-version 审计、固定 direct ranges/lock targets、无 lifecycle scripts、`npm ci --ignore-scripts`、npm ls graph 与 lock integrity 校验；图或元数据漂移时重新审计。 |
| T-03.1-01-01 | Tampering | target path and transaction | high | mitigate | 拒绝绝对相对项、`..`、符号链接祖先和根外解析；完整 staging 后原子提交并回滚。 |
| T-03.1-01-02 | Information Disclosure | CLI diagnostics | high | mitigate | 结构化安全错误仅含 code/path；合成测试源；不打印文件、URL、header 或凭据值。 |
| T-03.1-01-03 | Denial of Service | unsupported runtime | medium | mitigate | Node 22+ 在写前硬停止，避免半安装；退出码与 JSON 原因稳定。 |
</threat_model>

<verification>
- `npm ci --ignore-scripts` 必须从已审计锁文件安装；`npm ls --all` 只能出现两个 direct dev packages 与唯一 transitive `undici-types@6.21.0`。
- `npm run build` 必须只生成可直接执行的 CJS。
- `npm run test:tracer` 必须覆盖成功、取消、路径拒绝、Node 版本拒绝和事务回滚。
</verification>

<success_criteria>
- 公共命令契约经过 one-way checkpoint 明确确认。
- 一条 Codex QA 安装路径从 bin 到项目状态端到端可运行。
- 所有写入均发生在确认后的目标内，失败时零残留且无敏感输出。
</success_criteria>

## Artifacts this phase produces

- New files: `package.json`, `package-lock.json`, `tsconfig.json`, `src/bin/kcoderag-nav.cts`, `src/tracer/codex-install.cts`, `tests/tracer/codex-install.test.cts`.
- New symbols: `main`, `runCli`, `installCodexQa`, plus the tracer's stable JSON result/error types.
- New CLI command/flags: `install`, `--host codex`, `--environment qa`, `--target <path>`, `--yes`, `--json`.
- Built artifacts: `dist/bin/kcoderag-nav.cjs` and the tracer's compiled runtime module.

<output>
Create `.planning/phases/03.1-javascript-npx/03.1-01-SUMMARY.md` when done.
</output>

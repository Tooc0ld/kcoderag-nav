---
phase: 03.1-javascript-npx
plan: 30
type: execute
wave: 17
depends_on: ["03.1-15"]
files_modified:
  - plugin-src/hooks/__pycache__/
  - kcoderag-qa/hooks/__pycache__/
  - kcoderag-dev/hooks/__pycache__/
  - scripts/__pycache__/
  - tests/__pycache__/
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json
autonomous: true
requirements: [JS-01, JS-05]
estimate: {tokens: 12000, raw_tokens: 12000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "删除前先验证 exact `kcoderag-nav/pre-retirement-receipt@1`；schema/HEAD/receipt hash/authorized-set hash/status/root-external fields 任一 undefined、空、无效或不一致都零删除。"
    - "Plan15、Plan30 authorization 与磁盘共享同一 `pre_cache_inventory` 类型；五根计数精确为 1/2/2/6/14、总数25、排序相对路径+SHA-256 三方相等。"
    - "只有冻结在 sanitized authorization receipt 中的 25 个显式文件可被逐项删除；observed deletion set 必须与授权集完全相等后，才可移除五个已证实为空的 exact root。"
    - "Cleanup receipt 必须证明 frozen HEAD、pre/post unrelated canonical bytes+paths 与非空 root-external digest list 精确相等；任何 cache 或旁路漂移硬停止。"
  artifacts:
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json", provides: "`kcoderag-nav/cache-cleanup-authorization@1` frozen copy of Plan15 contract and 25-file authority"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json", provides: "`kcoderag-nav/cache-cleanup-receipt@1` set/status/external equality evidence"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md", provides: "sanitized cache cleanup summary"}
  key_links:
    - {from: "Plan15 pre-retirement receipt", to: "03.1-30-CACHE-AUTH-RECEIPT.json", via: "identical pre_cache_inventory/status/external fields plus recomputed canonical hashes", pattern: "authorized_set_sha256"}
    - {from: "03.1-30-CACHE-AUTH-RECEIPT.json", to: "03.1-30-CACHE-CLEANUP-RECEIPT.json", via: "explicit-file unlink observations and set equality before rmdir", pattern: "observed_deletions"}
---
<objective>在最后一次 Python parity 与第一项 tracked Python 删除之间，安全清除五个精确 ignored cache root，不触碰其它目录。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json
@.gitignore</context>
<tasks>
<task type="auto"><name>Task 1: 验证 Plan15 receipt 并冻结 exact 25-file 删除授权</name><read_first>
- `.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json` 的 exact `kcoderag-nav/pre-retirement-receipt@1` schema 与所有 required fields
- `src/maintainer/retirement-audit.cts`/compiled CJS exports `canonicalJson`、`hashAuthorizedSet`、`verifyPreReceipt`；Plan30 不得另定义序列化规则
- `.gitignore` 中五 root 的 ignored boundary；不得由 ignore 状态推导 ownership
</read_first><files>.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json (create; five cache roots are read-only)</files><action>第一步必须运行 Plan15 `--verify-pre-receipt` 且在其成功前不创建 receipt、不删除任何文件。要求 `schema_version === "kcoderag-nav/pre-retirement-receipt@1"`；所有 exact allow-listed fields defined；`repo_head` 是非空小写 40-hex且等于 `git rev-parse HEAD`。使用 Plan15 导出的同一 `canonicalJson`：重算 `receipt_sha256=sha256(canonicalJson(receipt without only receipt_sha256))` 和 `authorized_set_sha256=sha256(canonicalJson(pre_cache_inventory))`，要求两者为非空小写64-hex且相等。`pre_cache_inventory` exact shape 为 `{roots:[{path,count}],root_counts:{"plugin-src/hooks/__pycache__":1,"kcoderag-qa/hooks/__pycache__":2,"kcoderag-dev/hooks/__pycache__":2,"scripts/__pycache__":6,"tests/__pycache__":14},total:25,files:[{path,sha256}]}`；roots使用相同固定顺序，files sorted/unique。

强制验证 `unrelated_status_before` exact fields/types：format literal、defined `bytes_base64`、valid nonempty lowercase SHA-256、sorted unique entries；decode bytes 后重算 hash并按 Plan15 canonical records重建 entries，任一 byte/path mismatch失败。`root_external_digests_before` 必须 defined、非空、sorted unique `{path,sha256}`，每个 SHA valid且与当前五 parent 的 root-external files相等。任何 undefined/missing/empty/extra field、invalid hash/head/schema 或 frozen HEAD drift 都零删除且不写 authorization receipt。

逐 root 进行写前 preflight：仓库/root/parent 的 canonical resolved absolute path 必须与固定相对路径精确拼接并保持 containment；root、每个 dirent 与每个文件都拒绝 symlink/reparse/junction，禁止子目录；只允许 regular `.pyc`，发现任一 `.pyo` 或其它扩展立即失败。构造 receipt inventory map 与磁盘 inventory map，逐项重算 SHA-256，要求 root counts 为 1/2/2/6/14、总数 25，relative-path key set 与 digest value 双向 exact equal；任何 extra/missing path、重复/case-fold collision、摘要漂移或 path outside exact roots 都零删除且不写 authorization receipt。

全部通过后才原子写 exact authorization schema：`schema_version="kcoderag-nav/cache-cleanup-authorization@1"`、`source_schema_version="kcoderag-nav/pre-retirement-receipt@1"`、`repo_head`、`plan15_receipt_sha256`（复制已重算成功的 Plan15 field，不是 raw-file hash）、`authorized_set_sha256`、完整同型 `pre_cache_inventory`、完整同型 `unrelated_status_before`、完整同型非空 `root_external_digests_before`、`receipt_sha256`。前三个复杂字段必须从 Plan15 deep-copy 且 canonicalJson byte-equal，不改名为 authorized_files/root_counts 等平行类型。Authorization `receipt_sha256` 使用同一 omit-only-self canonical formula。Top-level exact allow-list之外、undefined 或空 trust field 均失败。不得记录 cache bytes/命令输出/环境变量/凭据。写后再次验证 Plan15→authorization→磁盘三方 equality；Task1 结束时25文件仍存在且 SHA不变。</action><verify><automated>node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json &amp;&amp; node -e "const fs=require('node:fs'),c=require('node:crypto'),cp=require('node:child_process'),lib=require('./dist/maintainer/retirement-audit.cjs'),d='.planning/phases/03.1-javascript-npx/',p=JSON.parse(fs.readFileSync(d+'03.1-15-PRE-RETIREMENT-RECEIPT.json','utf8')),a=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json','utf8')),hex=x=&gt;/^[a-f0-9]{64}$/.test(x),hash=x=&gt;c.createHash('sha256').update(Buffer.from(lib.canonicalJson(x),'utf8')).digest('hex'),omit=o=&gt;{const{receipt_sha256,...r}=o;return r},keys=(o,x)=&gt;JSON.stringify(Object.keys(o).sort())===JSON.stringify([...x].sort()),head=cp.execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();if(p.schema_version!=='kcoderag-nav/pre-retirement-receipt@1'||a.schema_version!=='kcoderag-nav/cache-cleanup-authorization@1'||a.source_schema_version!==p.schema_version||!/^[a-f0-9]{40}$/.test(p.repo_head)||p.repo_head!==head||a.repo_head!==head)process.exit(1);if(!hex(p.receipt_sha256)||hash(omit(p))!==p.receipt_sha256||a.plan15_receipt_sha256!==p.receipt_sha256||!hex(p.authorized_set_sha256)||hash(p.pre_cache_inventory)!==p.authorized_set_sha256||a.authorized_set_sha256!==p.authorized_set_sha256)process.exit(1);if(!keys(a,['schema_version','source_schema_version','repo_head','plan15_receipt_sha256','authorized_set_sha256','pre_cache_inventory','unrelated_status_before','root_external_digests_before','receipt_sha256'])||!hex(a.receipt_sha256)||hash(omit(a))!==a.receipt_sha256)process.exit(1);if(lib.canonicalJson(a.pre_cache_inventory)!==lib.canonicalJson(p.pre_cache_inventory)||lib.canonicalJson(a.unrelated_status_before)!==lib.canonicalJson(p.unrelated_status_before)||lib.canonicalJson(a.root_external_digests_before)!==lib.canonicalJson(p.root_external_digests_before)||!Array.isArray(a.root_external_digests_before)||!a.root_external_digests_before.length)process.exit(1);const s=p.unrelated_status_before;if(!keys(s,['format','bytes_base64','sha256','entries'])||s.format!=='git-status-porcelain-v1-z'||typeof s.bytes_base64!=='string'||!hex(s.sha256)||c.createHash('sha256').update(Buffer.from(s.bytes_base64,'base64')).digest('hex')!==s.sha256||!Array.isArray(s.entries))process.exit(1)" &amp;&amp; node -e "const fs=require('node:fs'),p=require('node:path'),c=require('node:crypto'),d='.planning/phases/03.1-javascript-npx/',a=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json','utf8')),i=a.pre_cache_inventory,w=[['plugin-src/hooks/__pycache__',1],['kcoderag-qa/hooks/__pycache__',2],['kcoderag-dev/hooks/__pycache__',2],['scripts/__pycache__',6],['tests/__pycache__',14]],seen=new Map();if(i.total!==25||i.files?.length!==25||JSON.stringify(i.roots)!==JSON.stringify(w.map(([path,count])=&gt;({path,count})))||JSON.stringify(i.root_counts)!==JSON.stringify(Object.fromEntries(w)))process.exit(1);for(const x of i.files){if(!/^[a-f0-9]{64}$/.test(x.sha256)||seen.has(x.path)||p.posix.normalize(x.path)!==x.path||p.isAbsolute(x.path)||p.extname(x.path)!=='.pyc')process.exit(1);const root=w.find(([r])=&gt;x.path.startsWith(r+'/'));if(!root)process.exit(1);const s=fs.lstatSync(x.path),h=c.createHash('sha256').update(fs.readFileSync(x.path)).digest('hex');if(!s.isFile()||s.isSymbolicLink()||h!==x.sha256)process.exit(1);seen.set(x.path,root[0])}if([...seen.keys()].some((x,n,z)=&gt;n&amp;&amp;z[n-1].localeCompare(x)&gt;=0)||w.some(([r,n])=&gt;[...seen.values()].filter(v=&gt;v===r).length!==n))process.exit(1)"</automated></verify><acceptance_criteria>- exact Plan15 schema verifies before any Plan30 write/delete; frozen valid HEAD and both Plan15 hashes recompute.- authorization uses exact version/allow-list and deep-copies the same inventory/status/external types; its self hash recomputes and no field is undefined/empty.- roots/counts 1/2/2/6/14/total25 and every relative path/SHA-256 equal Plan15, authorization and disk; any cache/status/external/schema/hash drift refuses with zero deletes.</acceptance_criteria><done>删除 authority 被冻结为一个完整、同型、canonically hashed 25-file allow-list。</done></task>
<task type="auto"><name>Task 2: 逐文件执行冻结授权并在 set equality 后移除空 roots</name><read_first>
- `.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json` exact authorization schema、自身 receipt hash、同型 `pre_cache_inventory/unrelated_status_before/root_external_digests_before`
- `.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json`；只在 cleanup receipt equality 通过后验证 all-roots-absent state
- Task1 的 exact canonical/root/reparse checks；Task2 必须逐文件重复，不能信任旧 path handle
</read_first><files>plugin-src/hooks/__pycache__/, kcoderag-qa/hooks/__pycache__/, kcoderag-dev/hooks/__pycache__/, scripts/__pycache__/, tests/__pycache__/, .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json</files><action>删除前重新运行 Task1 全验证：Plan15 schema_version/receipt_sha256/authorized_set_sha256、authorization schema_version/receipt_sha256、两者 frozen repo_head 与 current HEAD、同型 inventory/status/external fields 均必须 defined、非空且 canonical equal。重新从磁盘验证25个 path/SHA、status canonical bytes+entries/hash 和非空 root-external digests；任一变化零删除。把 `authorization.pre_cache_inventory.files` 的已验证排序25-path固定为唯一 delete loop input，不 glob、不重新扫描追加、不递归、不删除 parent。逐项 unlink 前再次 lstat/realpath/regular-file/`.pyc`/SHA-256，只删除该显式 path并记录成功 relative path。中途错误停止且不得声称PASS；incomplete receipt 如实记录，重试只能用同一 authorized_set_sha256/列表。

只有当 observed deletion set 与 frozen 25-path set 双向 exact equal、每项已不存在且没有未授权删除时，才逐 root 再做 canonical resolved parent containment 与 no-symlink/reparse 验证，确认 `readdir` 为空，然后用非递归 exact `rmdir` 移除这五个目录。禁止 wildcard、`rm -r`/`Remove-Item -Recurse`、动态 root、父目录删除或环境变量目标。

只在 deletion-set equality 和五 root rmdir 成功后原子写 exact cleanup schema：`schema_version="kcoderag-nav/cache-cleanup-receipt@1"`、`repo_head`、`plan15_receipt_sha256`、`authorization_receipt_sha256`（等于 authorization 的 canonical receipt_sha256）、`authorized_set_sha256`、同型 `pre_cache_inventory`、sorted unique `observed_deletions`、`deletion_set_equal=true`、固定顺序 `removed_roots`、同型 `unrelated_status_before`/`unrelated_status_after`、同型非空 `root_external_digests_before`/`root_external_digests_after`、omit-only-self canonical `receipt_sha256`。Before fields deep-copy authorization；after fields用同一 capture/type/canonicalization重新采集。Pre/post status decoded canonical bytes、entries、hash 必须逐字节/逐路径相等；pre/post external lists必须 defined、非空并 canonical exact equal。Top-level unknown/undefined/empty trust field失败。先离线验证 cleanup 全契约，再允许 Plan15 verifier接受 five-roots-absent state。</action><verify><automated>node -e "const fs=require('node:fs'),c=require('node:crypto'),cp=require('node:child_process'),lib=require('./dist/maintainer/retirement-audit.cjs'),d='.planning/phases/03.1-javascript-npx/',p=JSON.parse(fs.readFileSync(d+'03.1-15-PRE-RETIREMENT-RECEIPT.json','utf8')),a=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json','utf8')),r=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-CLEANUP-RECEIPT.json','utf8')),hex=x=&gt;/^[a-f0-9]{64}$/.test(x),hash=x=&gt;c.createHash('sha256').update(Buffer.from(lib.canonicalJson(x),'utf8')).digest('hex'),omit=o=&gt;{const{receipt_sha256,...q}=o;return q},keys=(o,x)=&gt;JSON.stringify(Object.keys(o).sort())===JSON.stringify([...x].sort()),head=cp.execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim(),status=s=&gt;keys(s,['format','bytes_base64','sha256','entries'])&amp;&amp;s.format==='git-status-porcelain-v1-z'&amp;&amp;typeof s.bytes_base64==='string'&amp;&amp;hex(s.sha256)&amp;&amp;c.createHash('sha256').update(Buffer.from(s.bytes_base64,'base64')).digest('hex')===s.sha256&amp;&amp;Array.isArray(s.entries);if(p.schema_version!=='kcoderag-nav/pre-retirement-receipt@1'||a.schema_version!=='kcoderag-nav/cache-cleanup-authorization@1'||r.schema_version!=='kcoderag-nav/cache-cleanup-receipt@1'||!/^[a-f0-9]{40}$/.test(head)||p.repo_head!==head||a.repo_head!==head||r.repo_head!==head)process.exit(1);if(!hex(p.receipt_sha256)||hash(omit(p))!==p.receipt_sha256||a.plan15_receipt_sha256!==p.receipt_sha256||r.plan15_receipt_sha256!==p.receipt_sha256||!hex(a.receipt_sha256)||hash(omit(a))!==a.receipt_sha256||r.authorization_receipt_sha256!==a.receipt_sha256||!hex(r.receipt_sha256)||hash(omit(r))!==r.receipt_sha256)process.exit(1);if(!hex(p.authorized_set_sha256)||hash(p.pre_cache_inventory)!==p.authorized_set_sha256||a.authorized_set_sha256!==p.authorized_set_sha256||r.authorized_set_sha256!==p.authorized_set_sha256)process.exit(1);if(!keys(r,['schema_version','repo_head','plan15_receipt_sha256','authorization_receipt_sha256','authorized_set_sha256','pre_cache_inventory','observed_deletions','deletion_set_equal','removed_roots','unrelated_status_before','unrelated_status_after','root_external_digests_before','root_external_digests_after','receipt_sha256']))process.exit(1);if(lib.canonicalJson(r.pre_cache_inventory)!==lib.canonicalJson(a.pre_cache_inventory)||!status(r.unrelated_status_before)||!status(r.unrelated_status_after)||lib.canonicalJson(r.unrelated_status_before)!==lib.canonicalJson(a.unrelated_status_before)||r.unrelated_status_before.bytes_base64!==r.unrelated_status_after.bytes_base64||lib.canonicalJson(r.unrelated_status_before.entries)!==lib.canonicalJson(r.unrelated_status_after.entries)||r.unrelated_status_before.sha256!==r.unrelated_status_after.sha256)process.exit(1);if(!Array.isArray(r.root_external_digests_before)||!r.root_external_digests_before.length||!Array.isArray(r.root_external_digests_after)||!r.root_external_digests_after.length||lib.canonicalJson(r.root_external_digests_before)!==lib.canonicalJson(a.root_external_digests_before)||lib.canonicalJson(r.root_external_digests_before)!==lib.canonicalJson(r.root_external_digests_after))process.exit(1);const paths=r.pre_cache_inventory.files.map(x=&gt;x.path),obs=r.observed_deletions,roots=r.pre_cache_inventory.roots.map(x=&gt;x.path);if(paths.length!==25||obs.length!==25||paths.some((x,i)=&gt;x!==obs[i])||r.deletion_set_equal!==true||roots.length!==5||roots.some((x,i)=&gt;r.removed_roots[i]!==x)||roots.some(x=&gt;fs.existsSync(x)))process.exit(1)" &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</automated></verify><acceptance_criteria>- exact schema versions, frozen valid HEAD, Plan15/authorization/cleanup self hashes and authorized-set hash all recompute and match; undefined/missing/empty/extra trust fields fail.- only frozen 25 explicit paths are unlinked; observed set exact equals sorted inventory before five empty roots are non-recursively removed.- status before/after fields are present and exact byte/path/hash equal; root-external before/after are present, nonempty, same type and exact equal before Plan15 accepts all-clean.</acceptance_criteria><done>25-file cleanup is cryptographically linked to Plan15 and proves no unrelated status or adjacent-file drift。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-30-01 | Elevation of Privilege | cache deletion paths | high | mitigate | Plan15 receipt first, frozen 25-file allow-list, per-unlink path/hash/containment checks, exact non-recursive rmdir only after empty. |
| T-03.1-30-02 | Tampering | cache inventory | high | mitigate | exact 1/2/2/6/14 counts, total25, bidirectional relative-path/SHA-256 equality; reject extra/missing/`.pyo`/case/digest drift. |
| T-03.1-30-03 | Tampering | cross-plan receipt contract | high | mitigate | exact three schema versions, frozen HEAD, shared canonicalJson, recomputed self/authorized hashes and exact field allow-lists. |
| T-03.1-30-04 | Tampering | unrelated worktree | high | mitigate | pre/post canonical status bytes+entries+hash and nonempty root-external digest equality, with only owned receipts/SUMMARY excluded. |
| T-03.1-30-05 | Repudiation | cleanup evidence | high | mitigate | canonically hashed authorization/cleanup receipts; observed deletion set must equal frozen set before roots-absent state is accepted. |
</threat_model>
<verification>Exact producer/authorization/cleanup schema versions、frozen HEAD、canonical receipt/authorized hashes、same-type inventory/status/external fields全部闭环；observed set exact equal、五根随后不存在、pre/post status bytes/paths与external digests exact equal，最后才验证 Plan15 all-clean state。</verification>
<success_criteria>- Plan15→Plan30 shared contract has no missing/undefined/renamed fields and every hash recomputes.- exact counts 1/2/2/6/14/total25 and three-way path/SHA equality precede deletion.- only frozen25 files are deleted; set equality precedes empty-root removal and unrelated evidence remains exact。</success_criteria>
## Artifacts this phase produces
- New exact-version sanitized authorization/cleanup receipts reusing Plan15 inventory/status/external types and canonical hashing.
- Removal of exactly 25 frozen `.pyc` paths followed by five exact empty `__pycache__` roots; summarized without cache bytes in `03.1-30-SUMMARY.md`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md` when done.</output>

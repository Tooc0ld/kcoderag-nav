---
phase: 03.1-javascript-npx
plan: 30
type: execute
wave: 17
depends_on: ["03.1-15"]
files_modified:
  - plugin-src/hooks/__pycache__/
  - kcoderag-qa/hooks/__pycache__/
  - kcoderag-dev/hooks/__pycache__/
  - scripts/__pycache__/
  - tests/__pycache__/
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json
autonomous: true
requirements: [JS-01, JS-05]
estimate: {tokens: 12000, raw_tokens: 12000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "删除前先验证 Plan15 pre-cache receipt；五根计数必须精确为 1/2/2/6/14、总数 25，磁盘相对路径+SHA-256 清单与 receipt 双向相等。"
    - "只有冻结在 sanitized authorization receipt 中的 25 个显式文件可被逐项删除；observed deletion set 必须与授权集完全相等后，才可移除五个已证实为空的 exact root。"
    - "任何 extra/missing `.pyc`、任一 `.pyo`、摘要漂移、root/path 逃逸、symlink/reparse 或 unrelated status bytes/paths 变化都会硬停止。"
  artifacts:
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json", provides: "validated frozen 25-file relative-path/SHA-256 authorization"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json", provides: "observed deletion set equality and exact empty-root removal evidence"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md", provides: "sanitized cache cleanup summary"}
  key_links:
    - {from: "Plan15 pre-retirement receipt", to: "03.1-30-CACHE-AUTH-RECEIPT.json", via: "exact root counts plus relative-path/SHA-256 inventory equality", pattern: "authorized_files"}
    - {from: "03.1-30-CACHE-AUTH-RECEIPT.json", to: "03.1-30-CACHE-CLEANUP-RECEIPT.json", via: "explicit-file unlink observations and set equality before rmdir", pattern: "observed_deletions"}
---
<objective>在最后一次 Python parity 与第一项 tracked Python 删除之间，安全清除五个精确 ignored cache root，不触碰其它目录。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json
@.gitignore</context>
<tasks>
<task type="auto"><name>Task 1: 验证 Plan15 receipt 并冻结 exact 25-file 删除授权</name><read_first>
- `.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json` 的 pre-cache per-file relative path/SHA-256 inventory、root counts 与 tracked inventory hash
- `src/maintainer/retirement-audit.cts`/`dist/maintainer/retirement-audit.cjs` 的 `--verify-pre-receipt` contract
- `.gitignore` 中五 root 的 ignored boundary；不得由 ignore 状态推导 ownership
</read_first><files>.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json (create; five cache roots are read-only)</files><action>第一步必须运行 Plan15 `--verify-pre-receipt` 且在其成功前不创建 receipt、不删除任何文件。固定 Plan15 receipt schema seam 为 `pre_cache_inventory: {root_counts,total,files:[{path,sha256}]}`。固定唯一 root→count map：`plugin-src/hooks/__pycache__=1`、`kcoderag-qa/hooks/__pycache__=2`、`kcoderag-dev/hooks/__pycache__=2`、`scripts/__pycache__=6`、`tests/__pycache__=14`，总计恰好 25。要求 Plan15 receipt 的 `pre_cache_inventory` 含这五个且仅这五个 roots，并为每个文件给出规范 POSIX relative path 与小写 64-hex SHA-256。

逐 root 进行写前 preflight：仓库/root/parent 的 canonical resolved absolute path 必须与固定相对路径精确拼接并保持 containment；root、每个 dirent 与每个文件都拒绝 symlink/reparse/junction，禁止子目录；只允许 regular `.pyc`，发现任一 `.pyo` 或其它扩展立即失败。构造 receipt inventory map 与磁盘 inventory map，逐项重算 SHA-256，要求 root counts 为 1/2/2/6/14、总数 25，relative-path key set 与 digest value 双向 exact equal；任何 extra/missing path、重复/case-fold collision、摘要漂移或 path outside exact roots 都零删除且不写 authorization receipt。

全部通过后才以原子 create 写 `03.1-30-CACHE-AUTH-RECEIPT.json`：schema/version、Plan15 receipt SHA-256、current HEAD、固定 root/count map、total=25、按相对路径排序的 25 个 `{path,sha256}`、授权集 canonical hash、以及排除两个 Plan30 owned receipts/SUMMARY 后的 raw `git status --porcelain=v1 -z` SHA-256 和精确 code+path list。不得记录文件 bytes、命令输出、环境变量或凭据。写后再次从 Plan15 receipt、authorization receipt 与磁盘做同一三方双向 equality；Task1 结束时 25 个文件全部仍存在且 SHA 不变。</action><verify><automated>node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json &amp;&amp; node -e "const fs=require('node:fs'),c=require('node:crypto'),d='.planning/phases/03.1-javascript-npx/',raw=fs.readFileSync(d+'03.1-15-PRE-RETIREMENT-RECEIPT.json'),pre=JSON.parse(raw).pre_cache_inventory,a=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json','utf8')),sort=x=&gt;[...x].sort((u,v)=&gt;u.path.localeCompare(v.path));if(!pre||pre.total!==25||a.total!==25||JSON.stringify(Object.entries(pre.root_counts||{}).sort())!==JSON.stringify(Object.entries(a.root_counts||{}).sort())||JSON.stringify(sort(pre.files||[]))!==JSON.stringify(sort(a.authorized_files||[])))process.exit(1);if(a.plan15_receipt_sha256!==c.createHash('sha256').update(raw).digest('hex'))process.exit(1)" &amp;&amp; node -e "const fs=require('node:fs'),p=require('node:path'),c=require('node:crypto');const f='.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json',a=JSON.parse(fs.readFileSync(f,'utf8')),want=new Map([['plugin-src/hooks/__pycache__',1],['kcoderag-qa/hooks/__pycache__',2],['kcoderag-dev/hooks/__pycache__',2],['scripts/__pycache__',6],['tests/__pycache__',14]]);if(a.total!==25||a.authorized_files?.length!==25||want.size!==Object.keys(a.root_counts||{}).length)process.exit(1);const seen=new Map();for(const x of a.authorized_files){if(!/^[a-f0-9]{64}$/.test(x.sha256)||seen.has(x.path))process.exit(1);const root=[...want.keys()].find(r=&gt;x.path.startsWith(r+'/'));if(!root||p.posix.normalize(x.path)!==x.path||p.isAbsolute(x.path))process.exit(1);const s=fs.lstatSync(x.path);if(!s.isFile()||s.isSymbolicLink()||p.extname(x.path)!=='.pyc')process.exit(1);const h=c.createHash('sha256').update(fs.readFileSync(x.path)).digest('hex');if(h!==x.sha256)process.exit(1);seen.set(x.path,root)}for(const[r,n]of want){if(a.root_counts[r]!==n||[...seen.values()].filter(v=&gt;v===r).length!==n)process.exit(1)}"</automated></verify><acceptance_criteria>- Plan15 receipt verifies before any Plan30 write/delete.- exact five roots/counts 1/2/2/6/14, total 25, and every relative path/SHA-256 are bidirectionally equal between Plan15 receipt, disk and authorization receipt.- extra/missing `.pyc`, any `.pyo`, digest/case/path/root/reparse drift fails with zero deletes; unrelated status bytes/paths baseline is frozen.</acceptance_criteria><done>删除 authority 被冻结为一个可复验、去敏、精确 25-file allow-list。</done></task>
<task type="auto"><name>Task 2: 逐文件执行冻结授权并在 set equality 后移除空 roots</name><read_first>
- `.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json` 的 frozen `authorized_files`、root counts、Plan15 hash 与 unrelated status baseline
- `.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json`；只在 cleanup receipt equality 通过后验证 all-roots-absent state
- Task1 的 exact canonical/root/reparse checks；Task2 必须逐文件重复，不能信任旧 path handle
</read_first><files>plugin-src/hooks/__pycache__/, kcoderag-qa/hooks/__pycache__/, kcoderag-dev/hooks/__pycache__/, scripts/__pycache__/, tests/__pycache__/, .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json</files><action>删除前重新验证 authorization receipt 自身 hash、Plan15 receipt hash/current HEAD、固定 counts/total、全部 25 个当前 relative path/SHA-256、canonical containment 与 symlink/reparse 状态；任一变化零删除。把 authorization receipt 中排序后的 25 个 path 固定为唯一 delete loop input，不 glob、不重新扫描后追加、不递归、不删除 parent。对每一项在 unlink 前再次 lstat/realpath/regular-file/`.pyc`/SHA-256 校验，然后只 unlink 该显式 path；记录每个成功 unlink 的 relative path。若中途系统错误，停止且不得声称 PASS；cleanup receipt 必须如实标记 incomplete，后续只允许用同一 frozen list 重试 remaining entries，不扩大授权集。

只有当 observed deletion set 与 frozen 25-path set 双向 exact equal、每项已不存在且没有未授权删除时，才逐 root 再做 canonical resolved parent containment 与 no-symlink/reparse 验证，确认 `readdir` 为空，然后用非递归 exact `rmdir` 移除这五个目录。禁止 wildcard、`rm -r`/`Remove-Item -Recurse`、动态 root、父目录删除或环境变量目标。

原子写 sanitized `03.1-30-CACHE-CLEANUP-RECEIPT.json`：authorization receipt SHA-256、Plan15 receipt SHA-256、authorized set hash、排序 observed deletions、`deletion_set_equal=true`、五个 exact removed roots、pre/post unrelated raw-status hash+code/path list equality和 root-external digest equality；不记录 cache bytes。先离线验证 cleanup receipt 的 observed/authorized set equality 与 unrelated baseline equality，再允许调用 Plan15 `--verify-pre-receipt` 接受 five-roots-absent state。Plan30 owned receipts/SUMMARY 是 status 比较的唯一排除项。</action><verify><automated>node -e "const fs=require('node:fs'),c=require('node:crypto');const d='.planning/phases/03.1-javascript-npx/',a=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json','utf8')),r=JSON.parse(fs.readFileSync(d+'03.1-30-CACHE-CLEANUP-RECEIPT.json','utf8')),paths=a.authorized_files.map(x=&gt;x.path).sort(),obs=[...r.observed_deletions].sort(),roots=['plugin-src/hooks/__pycache__','kcoderag-qa/hooks/__pycache__','kcoderag-dev/hooks/__pycache__','scripts/__pycache__','tests/__pycache__'];if(paths.length!==25||obs.length!==25||paths.some((x,i)=&gt;x!==obs[i])||r.deletion_set_equal!==true)process.exit(1);if(roots.some(x=&gt;fs.existsSync(x))||roots.some((x,i)=&gt;r.removed_roots?.[i]!==x))process.exit(1);if(r.authorization_receipt_sha256!==c.createHash('sha256').update(fs.readFileSync(d+'03.1-30-CACHE-AUTH-RECEIPT.json')).digest('hex'))process.exit(1);if(r.unrelated_status_before_sha256!==r.unrelated_status_after_sha256||JSON.stringify(r.unrelated_status_before)!==JSON.stringify(r.unrelated_status_after))process.exit(1)" &amp;&amp; node dist/maintainer/retirement-audit.cjs --verify-pre-receipt .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json</automated></verify><acceptance_criteria>- only the frozen 25 explicit paths are unlinked; observed deletion set exactly equals authorization set before any root removal.- five exact roots are removed only when empty and after repeated containment/reparse checks; no recursive/wildcard/parent delete occurs.- cleanup receipt proves set equality and unrelated status bytes/paths/root-external digests unchanged before Plan15 verifier accepts roots absent.</acceptance_criteria><done>retirement inventory 的 25 个 authorized pyc 和五个空 root 被精确清除，且删除证据不可把 partial/extra deletion 伪装为成功。</done></task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-30-01 | Elevation of Privilege | cache deletion paths | high | mitigate | Plan15 receipt first, frozen 25-file allow-list, per-unlink path/hash/containment checks, exact non-recursive rmdir only after empty. |
| T-03.1-30-02 | Tampering | cache inventory | high | mitigate | exact 1/2/2/6/14 counts, total25, bidirectional relative-path/SHA-256 equality; reject extra/missing/`.pyo`/case/digest drift. |
| T-03.1-30-03 | Tampering | unrelated worktree | high | mitigate | raw status hash+code/path and root-external digest equality, with only owned receipts/SUMMARY excluded. |
| T-03.1-30-04 | Repudiation | cleanup evidence | high | mitigate | authorization and cleanup receipts; observed deletion set must equal frozen set before roots-absent state is accepted. |
</threat_model>
<verification>Plan15 pre-cache receipt、25-file authorization receipt 与 cleanup receipt 形成闭环；observed deletion set exact equal、五根随后不存在、unrelated status bytes/paths/digests unchanged，最后才验证 Plan15 all-clean state。</verification>
<success_criteria>- exact counts 1/2/2/6/14 与 total25 在删除前得到 receipt/disk equality。- 只删除 frozen 25 files；set equality 后才移除五个空 root。- tracked retirement 尚未开始且无任何旁路删除。</success_criteria>
## Artifacts this phase produces
- New sanitized `03.1-30-CACHE-AUTH-RECEIPT.json` and `03.1-30-CACHE-CLEANUP-RECEIPT.json`.
- Removal of exactly 25 frozen `.pyc` paths followed by five exact empty `__pycache__` roots; summarized without cache bytes in `03.1-30-SUMMARY.md`.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md` when done.</output>

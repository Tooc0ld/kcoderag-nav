---
phase: 03.1-javascript-npx
plan: 30
type: execute
wave: 17
depends_on: ["03.1-15"]
files_modified:
  - plugin-src/hooks/__pycache__/
  - kcoderag-qa/hooks/__pycache__/
  - kcoderag-dev/hooks/__pycache__/
  - scripts/__pycache__/
  - tests/__pycache__/
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json
  - .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json
autonomous: true
requirements: [JS-01, JS-05]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Plan30 only invokes the compiled cleanup CLI after rebuilding and rerunning its isolated malformed/success fixture suite."
    - "The frozen repo_head is an immutable production baseline ancestor, not an equality check against current HEAD; later planning/receipt commits pass only when production inventory and filtered diff remain unchanged."
    - "Plan15, authorization and disk share exact cache roots/counts 1/2/2/6/14, total25 and sorted path+SHA authority through the shared canonical/path implementation."
    - "Any invalid receipt, lineage, production drift, cache/status/external drift or reparse escape stops before unlink/rmdir and creates no success receipt."
    - "Exactly 25 frozen files are unlinked before exactly five verified-empty roots are removed nonrecursively; cleanup evidence preserves unrelated status and root-external digests."
  artifacts:
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json", provides: "`kcoderag-nav/cache-cleanup-authorization@1` frozen authority linked to Plan15 production baseline"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json", provides: "`kcoderag-nav/cache-cleanup-receipt@1` exact mutation/status/external equality evidence"}
    - {path: ".planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md", provides: "sanitized cache cleanup summary"}
  key_links:
    - {from: "Plan15 receipt", to: "cache-cleanup authorize", via: "verifyProductionBaseline plus shared canonical/path/hash functions", pattern: "tracked_production_inventory_sha256"}
    - {from: "authorization receipt", to: "cleanup receipt", via: "explicit unlink/rmdir plan and observed set equality", pattern: "authorized_set_sha256"}
---
<objective>只通过已编译、已测试的 cleanup CLI 授权并清除五个精确 cache roots，同时允许 GSD 规划提交而拒绝任何生产源码漂移。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03.1-javascript-npx/03.1-15-SUMMARY.md
@.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json
@.gitignore</context>
<tasks>
<task type="auto"><name>Task 1: 重跑 cleanup contract tests 并通过 CLI 冻结授权</name><read_first>
- `src/maintainer/retirement-audit.cts` shared `canonicalJson`、`compareCodePointPaths`、`validateSortedUniquePaths`、`verifyProductionBaseline`
- `src/maintainer/cache-cleanup.cts` tested `authorize|execute|verify` CLI contract
- `.planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json` exact producer schema
</read_first><files>.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json (create; five cache roots are read-only)</files><action>先运行 `npm run build`，再运行 compiled retirement-audit 和 cache-cleanup tests；任何失败停止且不创建 authorization receipt。随后只调用 `node dist/maintainer/cache-cleanup.cjs authorize --producer ... --authorization ...`，不得在计划执行中另写 inline receipt/path/delete logic。

CLI 必须验证 producer `schema_version`、self/authorized hashes、所有 required fields和 exact allow-list；`repo_head` 是 immutable production baseline，运行 `git merge-base --is-ancestor repo_head HEAD`，从 current HEAD重建 tracked production inventory并用共享 `canonicalJson`/path comparator exact compare `tracked_production_inventory` 和 `tracked_production_inventory_sha256`，再确认 filtered `git diff --name-only -z repo_head..HEAD --` 除 `.planning/**`、`.gsd/**`和 exact receipt/SUMMARY allow-list外为空。root_counts 与 files 必须直接调用 Plan15 exported canonical/path functions；禁止用 `JSON.stringify` insertion-order equality 或 `localeCompare`。不得要求 current HEAD 等于 baseline；baseline non-ancestor、任何 production path/mode/blob/digest/hash drift都失败。

CLI 继续验证 exact `pre_cache_inventory` shape/counts/total25、磁盘 path+SHA双向 equality、status canonical bytes+entries/hash、nonempty root-external digests、resolved containment以及 no symlink/reparse/`.pyo`/extra。它必须先构造完整 explicit 25-unlink/5-rmdir plan但此命令不执行 plan。验证全部通过后才原子写 authorization exact fields：`schema_version,source_schema_version,repo_head,tracked_production_inventory,tracked_production_inventory_sha256,plan15_receipt_sha256,authorized_set_sha256,pre_cache_inventory,unrelated_status_before,root_external_digests_before,receipt_sha256`；复杂字段deep-copy producer并由共享 canonical functions重验。Task结束时25文件和五 roots均保持原字节。</action><verify><automated>npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; node --test dist-tests/maintainer/cache-cleanup.test.cjs &amp;&amp; node dist/maintainer/cache-cleanup.cjs authorize --producer .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json --authorization .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json</automated></verify><acceptance_criteria>- build precedes both compiled test suites and CLI authorization.- authorization accepts a descendant HEAD only when frozen production inventory/hash and filtered baseline diff are unchanged; non-ancestor/production drift fails.- CLI uses the exported canonical/path/baseline functions for root_counts/files and receipts; no local serializer or locale-sensitive/path insertion-order comparison is introduced.- authorization is written only after full mutation-free validation and all 25 cache files remain exact.</acceptance_criteria><done>Live deletion has one tested, canonically linked and production-baseline-safe authorization receipt。</done></task>

<task type="auto"><name>Task 2: 只调用 tested CLI 执行 exact cleanup 并验证 receipt</name><read_first>
- `.planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json` exact authorization and immutable baseline evidence
- `src/maintainer/cache-cleanup.cts` tested execute/verify contract; no inline deletion implementation is permitted
- Plan15 receipt all-roots-absent verification contract
</read_first><files>plugin-src/hooks/__pycache__/, kcoderag-qa/hooks/__pycache__/, kcoderag-dev/hooks/__pycache__/, scripts/__pycache__/, tests/__pycache__/, .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json</files><action>只调用 compiled CLI `execute`，传入 producer、authorization和cleanup receipt exact paths。CLI 在任何 mutation 前重新完成 Task1全部验证：baseline ancestor、current committed production inventory/hash、filtered baseline diff、producer/auth schemas/hashes、cache disk、unrelated status和root-external evidence必须完全一致；验证期间构造 frozen explicit plan，任何失败必须 nonzero、unlink/rmdir counts都为0且无cleanup success receipt。

验证成功后，CLI只以 frozen plan中的25个显式 path为输入；每次 unlink 前重验 lstat/realpath/regular-file/`.pyc`/SHA/containment。observed deletions与授权 sorted set exact equal后，才逐个确认五 fixed roots empty/no reparse并执行五次非递归 exact rmdir。禁止 wildcard、recursive removal、动态 root、parent deletion或环境变量目标。

CLI 只在25/5全部成功后原子写 `kcoderag-nav/cache-cleanup-receipt@1`，包含 `repo_head,tracked_production_inventory_sha256,plan15_receipt_sha256,authorization_receipt_sha256,authorized_set_sha256,pre_cache_inventory,observed_deletions,deletion_set_equal,removed_roots,unrelated_status_before,unrelated_status_after,root_external_digests_before,root_external_digests_after,receipt_sha256`。随后只调用同一 CLI `verify`；它必须内部调用 shared `verifyPreReceipt` 接受 all-roots-absent state，并验证 shared canonical hashes、baseline descendant with no production drift、observed set exact equality、五 roots absent、pre/post unrelated status bytes/paths/hash exact equal、nonempty root-external digests exact equal。中途系统错误可返回失败但不得写 success receipt或声称PASS。</action><verify><automated>node dist/maintainer/cache-cleanup.cjs execute --producer .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json --authorization .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json --cleanup-receipt .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json &amp;&amp; node dist/maintainer/cache-cleanup.cjs verify --producer .planning/phases/03.1-javascript-npx/03.1-15-PRE-RETIREMENT-RECEIPT.json --authorization .planning/phases/03.1-javascript-npx/03.1-30-CACHE-AUTH-RECEIPT.json --cleanup-receipt .planning/phases/03.1-javascript-npx/03.1-30-CACHE-CLEANUP-RECEIPT.json</automated></verify><acceptance_criteria>- live task contains no independent serialization/path/deletion implementation and invokes only the tested CLI.- exact 25 explicit unlinks precede exact five empty nonrecursive rmdirs; observed and authorized sets match.- receipt verification proves baseline ancestry without current-HEAD equality, unchanged committed production inventory/diff, exact hashes, roots absent, and unrelated status/external evidence unchanged.- no success receipt/PASS exists for incomplete execution.</acceptance_criteria><done>Exact cache cleanup is linked to the immutable production baseline and performed only by the fixture-proven CLI。</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Planning descendant HEAD → production baseline | Allowed GSD commits must not mask a production-source change. |
| Authorization receipt → filesystem mutation | Untrusted paths and hashes become destructive operations only after full validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-30-01 | Elevation of Privilege | cache deletion paths | high | mitigate | tested explicit 25/5 plan, per-path containment/hash/reparse checks and nonrecursive rmdir. |
| T-03.1-30-02 | Tampering | production baseline | high | mitigate | ancestor check plus canonical full-tree inventory/hash and filtered baseline diff; no HEAD equality shortcut. |
| T-03.1-30-03 | Tampering | cross-plan receipt contract | high | mitigate | shared canonical/path implementation and exact producer/authorization/cleanup schemas and hashes. |
| T-03.1-30-04 | Tampering | unrelated worktree | high | mitigate | pre/post canonical status bytes+entries+hash and nonempty root-external digest equality. |
| T-03.1-30-05 | Repudiation | cleanup evidence | high | mitigate | observed deletion set must equal frozen set before roots-absent success receipt is accepted. |
</threat_model>
<verification>`npm run build &amp;&amp; npm run test:retirement-audit &amp;&amp; node --test dist-tests/maintainer/cache-cleanup.test.cjs` passes before the compiled authorize/execute/verify sequence; final Plan15 verification accepts planning descendants only with exact production baseline inventory/diff equality.</verification>
<success_criteria>- Plan30 contains no untested inline cleanup implementation.- immutable baseline ancestry, full production inventory/hash and filtered diff all verify across GSD receipt commits.- exact 25/5 cleanup and unrelated evidence equality are recorded by canonically hashed receipts.</success_criteria>
## Artifacts this phase produces
- `kcoderag-nav/cache-cleanup-authorization@1` linked to the Plan15 immutable production inventory.
- `kcoderag-nav/cache-cleanup-receipt@1` proving exact 25 explicit unlinks, five nonrecursive rmdirs and unchanged unrelated evidence.
- Removal of only the five authorized cache roots.
<output>Create `.planning/phases/03.1-javascript-npx/03.1-30-SUMMARY.md` when done.</output>

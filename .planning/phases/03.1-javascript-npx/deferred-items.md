# Deferred Items

- [open] GitHub Release run `32682791252` succeeded, but GitHub annotated the pinned
  `actions/checkout` and `actions/setup-node` revisions as Node.js 20 actions being
  forced onto Node.js 24. Re-audit and update the immutable official action pins in a
  future workflow-maintenance plan; this warning did not affect the verified `v0.1.6`
  publication.

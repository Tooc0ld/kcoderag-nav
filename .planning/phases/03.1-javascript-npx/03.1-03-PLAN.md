---
phase: 03.1-javascript-npx
plan: 03
type: execute
wave: 3
depends_on: ["03.1-02"]
files_modified:
  - src/hooks/grep-nudge.cts
  - tests/hooks/grep-nudge.test.cts
  - plugin-src/hooks/hooks.json
  - plugin-src/hooks/run_hook.cmd
  - plugin-src/hooks/run_hook.sh
  - tests/hooks/launcher.test.cts
autonomous: true
requirements: [JS-01, JS-05]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-02: Hook 规范实现改为 TypeScript 并编译为可直接运行的 CJS。"
    - "D-16: 任何 malformed payload、Node 缺失/过旧或 launcher 异常都 advisory fail-open、exit 0。"
  artifacts:
    - {path: "src/hooks/grep-nudge.cts", provides: "跨宿主搜索 nudge CJS 源", exports: ["hookOutput", "main"]}
    - {path: "plugin-src/hooks/hooks.json", provides: "Claude/Codex hook 事件与 launcher 注册"}
  key_links:
    - {from: "plugin-src/hooks/hooks.json", to: "plugin-src/hooks/run_hook.cmd", via: "Windows launcher", pattern: "run_hook.cmd"}
    - {from: "plugin-src/hooks/run_hook.sh", to: "grep-nudge.cjs", via: "部署后相对路径", pattern: "grep-nudge.cjs"}
---

<objective>行为等价地迁移 advisory hook 与 Windows/Linux launcher，更新检查和生成器分别留给后续窄计划。</objective>
<execution_context>@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/03.1-javascript-npx/03.1-CONTEXT.md
@plugin-src/hooks/grep_nudge.py
@plugin-src/hooks/hooks.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 迁移搜索分类与协议边界</name>
  <files>src/hooks/grep-nudge.cts, tests/hooks/grep-nudge.test.cts</files>
  <behavior>
    - Python corpus 的 Grep/Glob/Bash/shell 变体逐例输出等价；单文件、精确文本、日志和局部搜索保持静默。
    - 空输入、超长输入、畸形 JSON、未知工具或内部异常均 exit 0 且不输出阻断结果。
    - stdout 只允许 hook 协议 JSON；无提醒时为空。
  </behavior>
  <action>先把现有表驱动 case 作为 Node `node:test` 红色测试，再逐纯函数迁移解析、反引号/引号、Windows/Unix shell normalization 与 nudge JSON。保持本阶段仅行为迁移，不提前实现 Phase5 的新误报策略。入口顶层捕获全部异常并返回 0，不记录 payload。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:hook</automated></verify>
  <acceptance_criteria>- 全部现有行为 case 等价；故障与畸形输入均 fail-open。- 测试中无真实 MCP 值。</acceptance_criteria>
  <done>D-01、D-02、D-16 的 hook runtime parity 建立。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 切换双平台 launcher 到部署内 CJS</name>
  <files>plugin-src/hooks/hooks.json, plugin-src/hooks/run_hook.cmd, plugin-src/hooks/run_hook.sh, tests/hooks/launcher.test.cts</files>
  <behavior>
    - Windows launcher 能处理空格/Unicode 项目路径；POSIX launcher 不依赖 cwd。
    - Node 不存在、版本损坏、脚本缺失或返回异常时 launcher 仍 exit 0、stderr/stdout 静默。
    - hooks.json 只挂载 Codex Bash 和 Claude Grep/Glob/Bash 需要的 PreToolUse matcher。
  </behavior>
  <action>用 launcher 自身目录解析编译后的 `grep-nudge.cjs`，不依赖全局插件根或当前目录。先做 Node 22+ 快速探测，任何错误吞掉并成功返回；不得联网。写真实 subprocess 测试覆盖带空格路径、嵌套 cwd、缺失 Node 模拟和协议 stdout。</action>
  <verify><automated>npm run build &amp;&amp; npm run test:launcher</automated></verify>
  <acceptance_criteria>- Windows/Linux launcher 都有可执行测试。- deployed hook 没有 Python 命令或网络路径。</acceptance_criteria>
  <done>hook 的宿主启动链在 Node 异常时仍不阻断原工具。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-03-01 | Denial of Service | PreToolUse hook | high | mitigate | 有界输入、无网络、顶层 catch、launcher 永远 fail-open。 |
| T-03.1-03-02 | Information Disclosure | hook payload | high | mitigate | 不记录原始输入/凭据，stdout 仅协议对象。 |
| T-03.1-03-03 | Tampering | relative launcher path | medium | mitigate | 以脚本自身目录定位受管 CJS，不信任 cwd。 |
</threat_model>
<verification>`npm run build &amp;&amp; npm run test:hook &amp;&amp; npm run test:launcher`</verification>
<success_criteria>- hook behavior parity 与 fail-open 均通过。- launcher 不依赖 Python、cwd 或网络。</success_criteria>

## Artifacts this phase produces

- New files: hook TypeScript source and Node tests; CJS-oriented Windows/POSIX launchers.
- New symbols: `hookOutput`, `main`.

<output>Create `.planning/phases/03.1-javascript-npx/03.1-03-SUMMARY.md` when done.</output>

---
phase: 04.2-public-debranding
plan: "43"
type: execute
wave: 43
depends_on: ["04.2-32"]
files_modified:
  - .planning/phases/04.2-public-debranding/04.2-REVIEW.md
autonomous: true
requirements: [BRAND-01, BRAND-03]
gap_closure: true
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-14：当前不可变Git subject的全部tracked path/blob为零finding，review证据不再用原始身份拼写描述四个retired deletion targets。"
    - "BRAND-01边界覆盖空tracked surface、畸形Git inventory以及大小写、Unicode、全半角和常见分隔归一化变体。"
    - "D-05/D-06/D-07/D-08与BRAND-03：review意义、126项scope、clean verdict、27条R/S规则、E01–E15、无兼容alias、fail-open及五宿主行为均未改变。"
  artifacts:
    - path: ".planning/phases/04.2-public-debranding/04.2-REVIEW.md"
      provides: "identity-clean review evidence with four neutral retired-path tokens"
  key_links:
    - from: ".planning/phases/04.2-public-debranding/04.2-REVIEW.md"
      to: "src/maintainer/brand-audit.cts"
      via: "immutable full-subject path/blob scan after the exact evidence edit is committed"
---

<objective>
Neutralize the four review-evidence self-findings and prove the complete post-output Git subject is clean without changing review meaning or behavior evidence.

Purpose: close the first verifier gap before any new candidate is frozen.
Output: one evidence-only commit, focused edge proofs, full regression evidence, and a neutral-by-construction Summary.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-VERIFICATION.md
@.planning/phases/04.2-public-debranding/04.2-REVIEW.md
@.planning/phases/04.2-public-debranding/04.2-REVIEW-FIX.md
@src/maintainer/brand-audit.cts
@tests/maintainer/brand-audit.test.cts
</context>

<scope>
In scope: the four exact review-list entries identified by verification, immutable Git scanning, scanner edge backstops, and proportional behavior regression. Non-goals: product/source changes, scanner policy changes, history rewriting, candidate creation, remote refs, package publication, registry activity, or edits to user-owned dirty/untracked paths.
</scope>

<tasks>

<task type="tracer">
  <name>Task 1: Replace four self-finding review entries with neutral evidence tokens</name>
  <files>.planning/phases/04.2-public-debranding/04.2-REVIEW.md</files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-VERIFICATION.md — authoritative four line numbers and evidence-preservation requirement
    - .planning/phases/04.2-public-debranding/04.2-REVIEW.md — exact tracked evidence to edit
    - src/maintainer/scrub-baseline.cts — dirty-work overlap and exact-stage precedent
  </read_first>
  <action>Capture a private path+hunk baseline and require an empty index. Edit only the four verified list entries at lines 21, 41, 50, and 100, replacing them one-for-one with `retired-path-token:generated-cursor-skill`, `retired-path-token:generated-qa-skill`, `retired-path-token:canonical-skill-source`, and `retired-path-token:hook-test-source`. Add one neutral note explaining that these tokens retain the four intentional retirement-deletion scope entries without materializing retired identity text. Preserve `files_reviewed: 126`, zero findings, all resolved-finding explanations, endpoint conclusions, test outcomes, timestamps, and review status. Do not re-embed any matched spelling, excerpt, renamed path, compatibility alias, or scanner family payload. Stage only this file and stop on dirty overlap.</action>
  <acceptance_criteria>
    - Exactly four neutral retired-path tokens replace the four verifier-identified entries; review count and clean verdict remain unchanged.
    - A direct path/content call to the compiled scanner returns zero findings for the edited file before commit.
    - The task changes no product, generated, test, receipt, config, state, or unrelated planning file.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('node:fs');const a=require('./dist/maintainer/brand-audit.cjs');const p='.planning/phases/04.2-public-debranding/04.2-REVIEW.md';const t=fs.readFileSync(p,'utf8');if((t.match(/retired-path-token:/g)||[]).length!==4)process.exit(1);const x=a.scanBrandText(p,{scope:'git_path',exactPath:p});const y=a.scanBrandText(t,{scope:'git_content',exactPath:p});if(x.findingCount+y.findingCount!==0)process.exit(1)" &amp;&amp; git diff --check -- .planning/phases/04.2-public-debranding/04.2-REVIEW.md</automated>
  </verify>
  <done>The review retains the same 126-item scope and clean meaning while the four self-findings are removed by neutral tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Prove immutable zero-hit, scanner edges, and behavior parity</name>
  <files></files>
  <read_first>
    - src/maintainer/brand-audit.cts — immutable Git inventory and normalization contract
    - tests/maintainer/brand-audit.test.cts — encoding, normalization, malformed-input, secret-safe, Git and tar cases
    - tests/skills/code-style-correction.test.cts — R01–R19 and S01–S08 structure
    - tests/skills/code-style-correction.behavior.test.cts — E01–E15 behavior oracle
    - .planning/debug/resolved/phase-04-2-final-regression.md — accepted full-regression precedent
  </read_first>
  <action>After Task 1 is committed, run the live immutable Git gate on HEAD and require findingCount zero. Run the focused scanner suite and an explicit zero-entry fake Git-tree oracle: an empty tracked surface must return a valid zero-count result without invoking blob batch reads, while malformed/non-blob/oversized surfaces continue to fail closed in the focused suite. The same suite must cover the closed family under case, Unicode normalization, width, whitespace, dash, and underscore transforms without exposing raw family text. Run full current CI so the neutral-only capability set, lack of compatibility aliases, invalid retired state, 27 rule IDs, E01–E15, hook fail-open, composition, rollback, and five-host packaged behavior remain unchanged. Preserve the existing dirty/untracked baseline and empty index.</action>
  <acceptance_criteria>
    - `audit:brand` over committed HEAD reports zero findings across every tracked path and blob.
    - Empty tracked input returns schema-valid zero counts; degenerate Git records fail closed; Unicode/common-separator variants are caught by named scanner tests.
    - Full CI passes with no compatibility alias or behavior-parity regression under BRAND-03.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:brand-audit &amp;&amp; node -e "const a=require('./dist/maintainer/brand-audit.cjs');const s='0'.repeat(40),t='1'.repeat(40);const run=(_r,x)=&gt;{if(x[0]==='rev-parse')return Buffer.from((x[x.length-1].endsWith('^{tree}')?t:s)+'\n');if(x[0]==='ls-tree')return Buffer.alloc(0);throw Error('unexpected_git_call')};const v=a.scanGitTree({root:process.cwd(),subject:s},{runGit:run});if(v.scannedCount!==0||v.findingCount!==0||v.findings.length!==0)process.exit(1)" &amp;&amp; npm run audit:brand -- git --subject HEAD &amp;&amp; npm run ci:local &amp;&amp; git diff --cached --exit-code &amp;&amp; git diff --check</automated>
  </verify>
  <done>The committed subject is zero-hit, edge behavior is explicit, and neutralization has no functional or compatibility-alias regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked review prose → immutable scanner | evidence text can recreate a prohibited family or leak a raw match |
| dirty worktree → evidence commit | unrelated user bytes must not enter the exact edit |
| Git inventory → PASS verdict | empty, malformed, Unicode or oversized input can spoof completeness |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-43-01 | Tampering/Repudiation | review evidence meaning | high | mitigate | four one-for-one neutral tokens, fixed count and immutable post-commit scan |
| T-04.2-43-02 | Information disclosure | scanner diagnostics | high | mitigate | metadata-only family/path tokens; no raw match, excerpt, config or secret output |
| T-04.2-43-03 | Tampering | Unicode/representation bypass | high | mitigate | shared normalization suite plus live full-subject audit |
| T-04.2-43-04 | Spoofing/Denial of service | empty or malformed Git surface | high | mitigate | explicit empty oracle and fail-closed malformed/resource tests |
| T-04.2-43-05 | Tampering | dirty-work absorption | high | mitigate | private baseline, overlap stop, empty index and exact staging |
| T-04.2-43-06 | Tampering | unintended external mutation | high | mitigate | no remote, tag, release, publication or registry command in this plan |
</threat_model>

<verification>
The plan is not complete until the Task 1 commit and the neutral-by-construction 04.2-43-SUMMARY.md are committed, then `npm run audit:brand -- git --subject HEAD` passes again so the Summary itself is inside the proof surface.
</verification>

<success_criteria>
- The four verifier-reported review findings are gone with evidence meaning and cardinality preserved.
- Current committed HEAD, including this plan and its Summary, is zero-hit.
- BRAND-01 edge backstops and BRAND-03 behavior/no-alias proof are explicit and green.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-43-SUMMARY.md` using neutral current identity and metadata only. Do not include retired spellings, source excerpts, exact matched paths, machine/user paths, or secret-bearing output. Commit the Summary before Plan 44 begins.
</output>

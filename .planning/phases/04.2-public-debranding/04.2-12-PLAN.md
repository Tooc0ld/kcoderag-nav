---
phase: 04.2-public-debranding
plan: "12"
type: execute
wave: 13
depends_on: ["04.2-11"]
files_modified:
  - "src/maintainer/scrub-baseline.cts"
  - "tests/maintainer/scrub-baseline.test.cts"
  - "package.json"
autonomous: true
requirements: [BRAND-01, BRAND-03]
risk_level: high
reversibility: reversible
must_haves:
  truths:
    - "每个后续explicit scrub task在编辑前捕获private dirty path+hunk baseline。"
    - "目标与pre-existing user hunks重叠时动态停到blocking checkpoint；无自动overwrite/stage/absorb/revert。"
    - "每批只stage显式paths并在commit后证明unrelated baseline不变。"
  artifacts:
    - path: "src/maintainer/scrub-baseline.cts"
      provides: "private dirty-hunk capture/preservation helper"
    - path: "tests/maintainer/scrub-baseline.test.cts"
      provides: "overlap/checkpoint and baseline canaries"
  key_links:
    - from: "explicit scrub paths"
      to: "captureScrubBaseline"
      via: "private diff hash/hunk ranges"
    - from: "post-commit check"
      to: "unrelated dirty baseline"
      via: "exact preservation"
  prohibitions:
    - "BRAND-03：scrub不得吸收、覆盖、回退或提交任何pre-existing unrelated hunk。"
  flagged_assumptions:
    - requirement: BRAND-01
      category: unclassified
      statement: "Exact scrub paths由plans固定；scanner仅验证结果，不动态扩大任一batch。"
---

<objective>
**As a** dirty worktree的维护者，**I want to** 每批中性化都保留已有工作，**so that** 全仓scrub不会吞掉用户hunks。

Purpose: 为所有explicit scrub计划建立可执行dirty-baseline contract。
Output: scrub baseline helper、overlap dynamic checkpoint、package scripts。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md

No external API integration: Phase 04.2 renames and audits existing local/package projections without adding an API capability surface.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 捕获private path+hunk baseline并检测overlap</name>
  <files>src/maintainer/scrub-baseline.cts, tests/maintainer/scrub-baseline.test.cts</files>
  <read_first>
    - src/maintainer/pre-commit.cts — Git subprocess/index preservation analog
    - src/core/transaction.cts — no-overwrite safety analog
    - current git status — dirty-work constraint
  </read_first>
  <behavior>
    - captureScrubBaseline receives explicit paths and stores binary diff only in private temp.
    - Public result contains opaque pathTokens/hunk ranges/digests, no file contents/exact hit paths.
    - Any planned-hunk overlap yields requiresCheckpoint true before edit.
  </behavior>
  <action>新增`ScrubBaselineError/Result,captureScrubBaseline,assertScrubBaselinePreserved`。用direct git argv取得porcelain-v2/diff，仅private temp保存binary diff；public metadata使用pathToken、hunk ranges、SHA。planned path有pre-existing hunks时不自动授权，返回`scrub_overlap_requires_checkpoint`；调用者必须动态创建blocking decision让用户在review panel处理。</action>
  <acceptance_criteria>
    - clean target可继续；overlap在任何write/stage前停止。
    - secret/raw diff不进入stdout/stderr/JSON/log。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; node --test --test-name-pattern="capture|overlap|private" dist-tests/maintainer/scrub-baseline.test.cjs</automated></verify>
  <done>scrub前置baseline可安全执行。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 限制 staging/commit并复验baseline</name>
  <files>src/maintainer/scrub-baseline.cts, tests/maintainer/scrub-baseline.test.cts, package.json</files>
  <read_first>
    - Task1 helper/result
    - package.json — maintainer script convention
  </read_first>
  <action>实现post-edit assertion：仅explicit paths允许产生新diff/stage；所有unrelated path/hunk digest保持；commit后验证explicit batch已进入HEAD且unrelated working/index状态与baseline一致。添加`test:scrub-baseline`；helper本身不commit或编辑。</action>
  <acceptance_criteria>
    - staged unrelated path、lost user hunk、unexpected rename均stable fail。
    - scripts只运行tests，不进入ordinary precommit live gate。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:scrub-baseline</automated></verify>
  <done>后续每个batch可证明只提交自己的显式hunks。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable subject/npm output → candidate artifact | package prediction and actual bytes are untrusted bounded subprocess results |
| artifact/workflow receipt → readiness verdict | provenance, lane and support metadata become formal assurance |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-12-01 | Tampering/Elevation | traversal/link/special package or workflow artifact | high | mitigate | private destinations, realpath containment, regular-file tar policy and safe artifact extraction |
| T-04.2-12-02 | Denial of service | oversized/binary/archive/output | high | mitigate | fixed limits, bounded JSON/output/time and fail-closed unsupported content |
| T-04.2-12-03 | Tampering | Unicode/path prediction bypass | high | mitigate | same Plan06 matcher scans dry-run paths and actual member paths/bodies |
| T-04.2-12-04 | Spoofing/Information disclosure | npm/Git/workflow output injection | high | mitigate | shell-false argv, exact JSON schemas, no raw logs/config/credentials in evidence |
| T-04.2-12-05 | Tampering/Repudiation | dry-run/pack/smoke/lane TOCTOU | high | mitigate | exact invocation counts and SHA/memberCount revalidation at every consumer/lane |
| T-04.2-12-06 | Information disclosure | match/path/secret/receipt leakage | high | mitigate | safe tokens, metadata-only receipts and canary traversal |
| T-04.2-12-07 | Tampering/Repudiation | external release mutation | high | mitigate | no commit/tag/publish/dist-tag/registry package acquisition/refetch implementation; NOT_RUN_BY_SCOPE |
</threat_model>

## Artifacts this phase produces

- New symbols: `ScrubBaselineError`, `ScrubBaselineResult`, `captureScrubBaseline`, `assertScrubBaselinePreserved`.
- Script: `test:scrub-baseline`; no public CLI/state/receipt field.
- Private temp holds raw diff only for the task lifetime; public output uses tokens/digests.

<verification>
Quick gate: dirty overlap and unrelated-hunk fixtures

Plan gate: `npm run build && npm run test:scrub-baseline`.
</verification>

<success_criteria>
- Every scrub batch can fail before overwriting user work.
- Only explicit batch paths may be staged and committed.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-12-SUMMARY.md` when done using neutral current terminology.
</output>

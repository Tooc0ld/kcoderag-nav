---
phase: 04.2-public-debranding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/capabilities/contracts.cts"
  - "src/capabilities/registry.cts"
  - "src/capabilities/jx3-style-nudge.cts → src/capabilities/code-style-nudge.cts"
  - "src/core/state.cts"
  - "src/hosts/host-version-support.cts"
  - "src/hosts/claude.cts"
  - "src/hooks/jx3-style-nudge.cts → src/hooks/code-style-nudge.cts"
  - "plugin-src/capabilities/jx3-style-nudge/skill/** → plugin-src/capabilities/code-style-nudge/skill/**"
  - "src/generator/index.cts"
  - "package.json"
  - "tests/hosts/claude.test.cts"
autonomous: false
requirements: [BRAND-01, BRAND-03]
risk_level: high
reversibility: one-way
must_haves:
  truths:
    - "D-05/D-08：new-only capability/Skill identity 从 CLI selection 贯穿 provider、Claude managed Skill/handler、state 与 actual tgz。"
    - "D-10/D-17：exact Claude 2.1.241 eligible event 在完整性校验后 claim marker，并输出既有有界 advisory context。"
    - "冻结 Claude receipt path、bytes、digest 与 PASS 结论不变；旧状态无 alias/migration 且 mutation spy 为零。"
  artifacts:
    - path: "src/capabilities/code-style-nudge.cts"
      provides: "new-only receipt-gated provider"
    - path: "src/hooks/code-style-nudge.cts"
      provides: "minimal neutral eligible-event/integrity runtime"
    - path: "tests/hosts/claude.test.cts"
      provides: "actual-tgz end-to-end tracer"
  key_links:
    - from: "CLI selection"
      to: "src/capabilities/registry.cts"
      via: "exact code-style-nudge lookup"
    - from: "src/hosts/claude.cts"
      to: "src/hooks/code-style-nudge.cts"
      via: "managed Skill/handler path"
    - from: "integrity verifier"
      to: "once marker"
      via: "success must precede claim"
  prohibitions:
    - "BRAND-03：tracer 不得以 alias、迁移或省略 hook/integrity/event/tgz 任何一段来缩短路径。"
  flagged_assumptions:
    - requirement: BRAND-03
      category: unclassified
      statement: "BRAND-03 unclassified edge 仍需后续完整回归与 D-12 人工语义 verdict；tracer 只关闭一条真实 Claude path。"
---

<objective>
**As a** 公共 CLI 维护者，**I want to** 先证明 Claude Code 2.1.241 的中性 capability 从选择到实际 tgz 内运行时完整贯通，**so that** 扩展建立在已执行的 production path 上。

Purpose: 按 D-05/D-07/D-08/D-10/D-17 交付真正闭合的 tracer，并在 one-way 修改前停靠确认。
Output: new-only provider/state/Claude projection、minimal hook rename、actual-tgz eligible-event test。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md

No external API integration: Phase 04.2 renames and audits existing local/package projections without adding an API capability surface.

Assumption delta: noun `Host/HostId`; decision `no-change`; rationale: Phase 04.2 preserves the existing five-host union and only includes ZCode in the same packaged acceptance matrix.

Schema gate: no ORM/schema file is in scope.
</context>

<tasks>
<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: 确认 D-05 one-way public identity</name>
  <files>none</files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-CONTEXT.md — D-05 one-way decision
    - .planning/REQUIREMENTS.md — BRAND-01/BRAND-03
  </read_first>
  <action>展示 exact `code-style-nudge` / `code-style-correction`、0.3.0 public surfaces、no-alias/no-migration boundary 与 undo cost；只接受 `proceed` 或停止，不重开名称选择。</action>
  <decision>是否按D-05确认不可逆的唯一public identity为code-style-nudge/code-style-correction，并接受不提供alias、migration、adoption或旧state cleanup。</decision>
  <options>
    <option id="proceed">确认locked identity与one-way代价，继续执行tracer。</option>
    <option id="stop">停止Phase 04.2，不修改任何public identity。</option>
  </options>
  <acceptance_criteria>
    - 维护者明确回复 `proceed` 后才修改 public identity 文件。
    - checkpoint 记录不含旧匹配原词或配置正文。
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('.planning/phases/04.2-public-debranding/04.2-01-PLAN.md','utf8');if(!s.includes('&lt;task type=&quot;tracer&quot;'))process.exit(1)"</automated>
    <human-check>确认 D-05 仍为权威 one-way 决定。</human-check>
  </verify>
  <resume-signal>proceed</resume-signal>
  <done>D-05 在任何 public contract 修改前得到确认。</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: 从 selection 到 actual tgz advisory event 闭合 Claude tracer</name>
  <reversibility rating="one-way">D-05 发布后进入公共 CLI/state/path 契约。</reversibility>
  <files>src/capabilities/contracts.cts, src/capabilities/registry.cts, src/capabilities/jx3-style-nudge.cts → src/capabilities/code-style-nudge.cts, src/core/state.cts, src/hosts/host-version-support.cts, src/hosts/claude.cts, src/hooks/jx3-style-nudge.cts → src/hooks/code-style-nudge.cts, plugin-src/capabilities/jx3-style-nudge/skill/** → plugin-src/capabilities/code-style-nudge/skill/**, src/generator/index.cts, package.json, tests/hosts/claude.test.cts</files>
  <read_first>
    - src/capabilities/contracts.cts, src/capabilities/registry.cts, src/capabilities/jx3-style-nudge.cts — current closed provider path
    - src/core/state.cts, src/hosts/host-version-support.cts, src/hosts/claude.cts — state/support/managed projection
    - src/hooks/jx3-style-nudge.cts — current integrity-before-marker flow and 600-char bound
    - tests/hosts/claude.test.cts — current actual-package integration helper
    - src/generator/index.cts, package.json — actual package membership
  </read_first>
  <behavior>
    - Selecting the new ID on exact Claude 2.1.241 renders the neutral managed Skill and handler.
    - The installed handler from one actual private tgz receives one eligible C/C++/Lua write event, verifies state/composite/all-file digests before marker claim, and returns bounded advisory context.
    - Former state and unsupported versions stop before render/transaction with a byte-identical target.
  </behavior>
  <action>先写名为 `neutral Claude tracer from actual tgz` 的失败集成测试，再做 closed rename。Tracer 自身必须包含 minimal `src/hooks/code-style-nudge.cts`、integrity-before-`claimNudgeOnce` ordering、eligible event 与 bounded response；不得把 hook rename留给后续。Canonical Skill 五文件只做 path/frontmatter identity move，正文优化后置。测试在private temp执行 actual `npm pack --ignore-scripts --json --pack-destination <dir>`，校验返回tgz realpath，使用该tgz的compiled CLI安装 Claude target，再执行安装后的 handler；断言 selection→provider→Skill/handler→event 全链闭合、receipt digest不变、marker只在integrity PASS后出现。保留 shared transaction为唯一写边界，不加入旧ID parser/alias/migration/cleanup。Dispatcher exhaustive cases、hook negative unit matrix及provider/registry expansion tests分别由Plans02/03承担，不进入tracer文件预算。</action>
  <acceptance_criteria>
    - 精确 tracer test 通过并确实执行 tgz 内 handler，不是 source import shortcut。
    - advisory context非空且不超过既有600字符；marker claim spy发生在所有digest验证之后。
    - frozen Claude receipt bytes/SHA/host/version/PASS保持原样。
    - actual tgz path在private destination内并由test清理；不触碰registry。
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; node --test --test-name-pattern="neutral Claude tracer from actual tgz" dist-tests/hosts/claude.test.cjs</automated>
  </verify>
  <done>一条 production-quality Claude 2.1.241 中性路径已从 public selection 运行到 actual-tgz advisory output。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted project/Git/npm/tar input → maintainer/runtime code | paths, object bytes, archive metadata and subprocess output cross a validation boundary |
| canonical/package bytes → host projection/evidence | bytes and support claims become managed files or readiness facts |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-01-01 | Tampering/Elevation | path/tar traversal and link/special entries | high | mitigate | exact bounded inventories, containment/no-follow checks, no extraction, focused adversarial tests |
| T-04.2-01-02 | Denial of service | oversized/binary/blob/member input | high | mitigate | fixed byte/count/path/output/time limits and fail-closed unsupported input |
| T-04.2-01-03 | Tampering | Unicode/case/separator bypass | high | mitigate | one NFKC closed matcher and synthesized variant/negative-control tests |
| T-04.2-01-04 | Spoofing/Information disclosure | subprocess/output injection | high | mitigate | direct argv, shell false, bounded machine parsing and no raw stdout/stderr reflection |
| T-04.2-01-05 | Tampering/Repudiation | scan/pack/smoke TOCTOU | high | mitigate | immutable subject/OIDs and one rehashed SHA/member-count-bound tgz |
| T-04.2-01-06 | Information disclosure | matches, credentials, config and receipts | high | mitigate | metadata-only schemas, opaque values and raw-match/secret canaries |
| T-04.2-01-07 | Tampering/Repudiation | unintended release mutation | high | mitigate | no commit/tag/publish/dist-tag/registry-refetch branch; exact NOT_RUN_BY_SCOPE evidence |
</threat_model>

## Artifacts this phase produces

- New/renamed files: `src/capabilities/code-style-nudge.cts`, `src/hooks/code-style-nudge.cts`, `tests/hooks/code-style-nudge.test.cts`, canonical `plugin-src/capabilities/code-style-nudge/skill/**`.
- New/renamed symbols: `codeStyleNudgeCapabilityProvider`, `CODE_STYLE_NUDGE`, `evaluateCodeStyleIntegrity`, `codeStyleContribution`, `HostVersionSupportResult.codeStyleNudge`.
- Public/state surface: `CapabilityId`, contributor/section IDs, marker capability and `--capability` are new-only; no schema version or migration field.
- CLI/scripts: existing build/tests/package entry; no release command.

<verification>
Quick gate: the single actual-tgz Claude tracer test

Plan gate: `npm run build && npm run test:capabilities && npm run test:host:claude && npm run test:capability-hooks`.
</verification>

<success_criteria>
- Tracer closes selection, provider, projection, minimal runtime, integrity, marker, eligible event and bounded context.
- No alias/migration/cleanup exists and receipt evidence is byte-stable.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-01-SUMMARY.md` when done; use neutral current terminology and omit source excerpts.
</output>

---
phase: 04.2-public-debranding
plan: "02"
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - "src/hooks/code-style-nudge.cts"
  - "src/hooks/once-marker.cts"
  - "src/hooks/pre-tool-dispatcher.cts"
  - "src/hooks/session-cleanup.cts"
  - "src/hosts/claude.cts"
  - "tests/hooks/jx3-style-nudge.test.cts → tests/hooks/code-style-nudge.test.cts"
  - "tests/hooks/once-marker.test.cts"
  - "tests/hooks/pre-tool-dispatcher.test.cts"
  - "tests/hooks/session-cleanup.test.cts"
autonomous: true
requirements: [BRAND-03]
risk_level: high
reversibility: reversible
must_haves:
  truths:
    - "D-10：完整 malformed/oversized/drift/concurrency/dispatcher matrix保持first-relevant、integrity-before-marker与fail-open。"
    - "旧 OS cache marker 不读、不复制、不删；D-19 manual reset边界不变。"
  artifacts:
    - path: "src/hooks/code-style-nudge.cts"
      provides: "hardened bounded hook runtime"
    - path: "tests/hooks/code-style-nudge.test.cts"
      provides: "full renamed hook regression"
  key_links:
    - from: "src/hooks/code-style-nudge.cts"
      to: "src/hooks/once-marker.cts"
      via: "integrity then atomic claim"
    - from: "src/hooks/pre-tool-dispatcher.cts"
      to: "navigation contribution"
      via: "per-contributor isolation"
  prohibitions:
    - "BRAND-03：不得借改名改变首次相关写入、fail-open、marker上限、dispatcher顺序或SessionEnd清理权。"
  flagged_assumptions:
    - requirement: BRAND-03
      category: unclassified
      statement: "BRAND-03 unclassified edge由这里的自动回归与Plan04人工Skill verdict共同关闭。"
---

<objective>
**As a** 宿主用户，**I want to** 改名后的提示在异常和并发下仍低打扰且不阻断，**so that** identity变化不改变运行时风险。

Purpose: 把 tracer 之外的 runtime hardening 独立完成。
Output: 完整 hook/marker/dispatcher/session 回归。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md

No external API integration: Phase 04.2 renames and audits existing local/package projections without adding an API capability surface.

Assumption delta: noun `Host/HostId`; decision `no-change`; rationale: Phase 04.2 preserves the existing five-host union and only includes ZCode in the same packaged acceptance matrix.

Schema gate: no ORM/schema file is in scope.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 扩展 malformed、drift 与 fail-open coverage</name>
  <files>src/hooks/code-style-nudge.cts, src/hosts/claude.cts, tests/hooks/jx3-style-nudge.test.cts → tests/hooks/code-style-nudge.test.cts</files>
  <read_first>
    - src/hooks/code-style-nudge.cts — Plan01 minimal path
    - src/hosts/claude.cts — launcher/integrity inputs
    - tests/hooks/jx3-style-nudge.test.cts — existing regression source; read before its exact Git move
  </read_first>
  <behavior>
    - Irrelevant/read-only/rename/delete/malformed/oversized/unstable-session/drifted events emit nothing and do not consume marker.
    - Parser/filesystem/hash/cache exceptions return undefined, empty stdout and exit 0.
  </behavior>
  <action>先将`tests/hooks/jx3-style-nudge.test.cts`以exact Git move改名为`tests/hooks/code-style-nudge.test.cts`并同步imports/descriptions，不复制或保留dual test；随后在新路径补齐 D-10 锁定的 extension/mutation bounds、state/composite/all-managed-file digest failure、malformed/oversized input和catch-to-undefined行为。保持Tracer中的eligible path不变。Diagnostics不得包含payload/config/stdout/stderr。</action>
  <acceptance_criteria>
    - 每个negative case marker inventory不变，随后第一个valid event仍可提示。
    - 所有异常fail-open且不会压制原工具调用。
    - Git inventory只有new test path，旧test path不存在。
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; node --test --test-name-pattern="malformed|oversized|drift|fail-open" dist-tests/hooks/code-style-nudge.test.cjs</automated>
  </verify>
  <done>negative/failure runtime matrix完整。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 保持 once concurrency、dispatcher isolation 与 disabled cleanup</name>
  <files>src/hooks/once-marker.cts, src/hooks/pre-tool-dispatcher.cts, src/hooks/session-cleanup.cts, tests/hooks/once-marker.test.cts, tests/hooks/pre-tool-dispatcher.test.cts, tests/hooks/session-cleanup.test.cts</files>
  <read_first>
    - src/hooks/once-marker.cts — atomic claim/1024 bound
    - src/hooks/pre-tool-dispatcher.cts — isolation/order/aggregate cap
    - src/hooks/session-cleanup.cts — D-19 disabled cleanup
    - corresponding tests — existing executable contracts
  </read_first>
  <action>只将caller identity切到new-only；保留stable session三元组、并发atomic once、1024 inventory bound、600 aggregate bound、navigation/style独立catch，以及receipt-unproven SessionEnd cleanup disabled。Former marker namespace保持不可达residue。</action>
  <acceptance_criteria>
    - 并发eligible events恰好一个claim；overflow确定性淘汰且不泄露session。
    - style异常不抑制navigation；cleanup无删除调用。
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:capability-hooks</automated>
  </verify>
  <done>runtime hardening保持完整且只改变identity。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted project/Git/npm/tar input → maintainer/runtime code | paths, object bytes, archive metadata and subprocess output cross a validation boundary |
| canonical/package bytes → host projection/evidence | bytes and support claims become managed files or readiness facts |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-02-01 | Tampering/Elevation | path/tar traversal and link/special entries | high | mitigate | exact bounded inventories, containment/no-follow checks, no extraction, focused adversarial tests |
| T-04.2-02-02 | Denial of service | oversized/binary/blob/member input | high | mitigate | fixed byte/count/path/output/time limits and fail-closed unsupported input |
| T-04.2-02-03 | Tampering | Unicode/case/separator bypass | high | mitigate | one NFKC closed matcher and synthesized variant/negative-control tests |
| T-04.2-02-04 | Spoofing/Information disclosure | subprocess/output injection | high | mitigate | direct argv, shell false, bounded machine parsing and no raw stdout/stderr reflection |
| T-04.2-02-05 | Tampering/Repudiation | scan/pack/smoke TOCTOU | high | mitigate | immutable subject/OIDs and one rehashed SHA/member-count-bound tgz |
| T-04.2-02-06 | Information disclosure | matches, credentials, config and receipts | high | mitigate | metadata-only schemas, opaque values and raw-match/secret canaries |
| T-04.2-02-07 | Tampering/Repudiation | unintended release mutation | high | mitigate | no commit/tag/publish/dist-tag/registry-refetch branch; exact NOT_RUN_BY_SCOPE evidence |
</threat_model>

## Artifacts this phase produces

- Modified runtime symbols retain Plan01 names; no additional public API.
- Tests cover malformed, drift, fail-open, concurrency, isolation, bounds and disabled cleanup.
- State/receipt fields unchanged; no cleanup or migration surface.

<verification>
Quick gate: focused renamed hook negative cases

Plan gate: `npm run build && npm run test:capability-hooks`.
</verification>

<success_criteria>
- First relevant reminder and fail-open semantics match the frozen behavior.
- Marker/dispatcher/session behavior remains bounded and isolated.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-02-SUMMARY.md` when done; use neutral current terminology and omit source excerpts.
</output>

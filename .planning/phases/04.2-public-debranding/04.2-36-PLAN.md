---
phase: 04.2-public-debranding
plan: "36"
type: execute
wave: 34
depends_on: ["04.2-30"]
files_modified:
  - ".github/workflows/readiness.yml"
  - ".github/actions/readiness-upload/action.yml"
  - ".github/actions/readiness-upload/index.cjs"
  - "tests/maintainer/readiness-workflow.test.cts"
  - "tests/maintainer/github-artifact-upload.test.cts"
autonomous: true
gap_closure: true
requirements: [BRAND-02, BRAND-04]
risk_level: high
reversibility: costly
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-16、BRAND-02：package producer必须在GitHub Actions JavaScript action handler内运行，使runner注入job-scoped artifact runtime inputs；shell ScriptHandler不再承担direct uploader入口。"
    - "现有uploader的artifact_auth_invalid、可信results origin、bounded responses、same-process private lease、create/upload/finalize和best-effort partial cleanup契约保持严格，不通过生产workflow伪造环境变量。"
    - "D-16、D-17：producer仍只创建一个actual 0.3.0 tgz，先完成pack/tar/five-host required-contract smoke，再由同一process消费lease上传；四lane及packaged-only边界不变。"
    - "外部actions继续固定到完整commit OID、permissions保持contents:read；不增加依赖、candidate registry acquisition、显式dispatch、push、tag、publish、release或dist-tag动作。"
    - "失败candidate f387da46d5ebcbeaeb2a1aa0218623a2860422c3及run 33161895992永久退役，receipt集合保持zero；本计划不重跑、不推送、不把该run转为证据。"
  artifacts:
    - path: ".github/actions/readiness-upload/action.yml"
      provides: "candidate-owned Node action handler declaration and closed output inventory"
    - path: ".github/actions/readiness-upload/index.cjs"
      provides: "same-process entrypoint into the existing package-upload producer"
    - path: ".github/workflows/readiness.yml"
      provides: "package job wired to the local JavaScript action rather than a shell run step"
    - path: "tests/maintainer/readiness-workflow.test.cts"
      provides: "exact missing-runtime RED oracle and runner-handler semantic regression"
    - path: "tests/maintainer/github-artifact-upload.test.cts"
      provides: "production process-env auth resolution proof without weakened validation"
  key_links:
    - from: ".github/workflows/readiness.yml package upload step"
      to: ".github/actions/readiness-upload/action.yml"
      via: "candidate-local uses path resolved as a JavaScript action handler"
    - from: ".github/actions/readiness-upload/index.cjs"
      to: "src/maintainer/readiness-workflow.cts main(['package-upload'])"
      via: "one in-process call; no subprocess or artifact-path reconstruction"
    - from: "GitHub Runner Node action handler"
      to: "src/maintainer/github-artifact-upload.cts"
      via: "runner-injected ACTIONS_RESULTS_URL and ACTIONS_RUNTIME_TOKEN read by the existing strict adapter"
  prohibitions:
    - "不得修改或放宽github-artifact-upload的token、scope、origin、response、timeout或metadata校验来隐藏handler错误。"
    - "不得在workflow中手工构造、转发、打印或持久化artifact runtime URL/token。"
    - "不得用独立upload-artifact step、临时路径、metadata或第二次pack重建candidate tgz。"
    - "不得执行远端重跑、ref mutation、tag、publish、registry_refetch、npm view、remote packageSpec、release或dist-tag mutation。"
---

<objective>
修复真实run 33161895992暴露的GitHub Actions handler边界，使direct artifact uploader在runner真正注入私有artifact inputs的JavaScript action context中执行。

Purpose: 关闭本地synthetic auth测试未覆盖、shell ScriptHandler必然产生artifact_auth_invalid的workflow缺口，同时保持one-actual-tgz与全部安全门禁。
Output: candidate-owned local JavaScript action、rewired readiness package step、精确RED/GREEN handler回归和production env contract测试。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/readiness-package-job-fail.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-30-SUMMARY.md
@.github/workflows/readiness.yml
@src/maintainer/readiness-workflow.cts
@src/maintainer/github-artifact-upload.cts
@tests/maintainer/readiness-workflow.test.cts
@tests/maintainer/github-artifact-upload.test.cts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN 将package producer迁入Node action handler</name>
  <precondition>Commit f387da46d5ebcbeaeb2a1aa0218623a2860422c3 resolves locally, run 33161895992 remains failed with zero receipts, and `.planning/debug/readiness-package-job-fail.md` records artifact_auth_invalid before network; otherwise stop and re-diagnose without editing.</precondition>
  <reversibility rating="costly">该handler决定runner是否向direct uploader提供job-scoped私有输入；错误回退会使正式readiness在网络前确定性失败。</reversibility>
  <files>tests/maintainer/readiness-workflow.test.cts, .github/workflows/readiness.yml, .github/actions/readiness-upload/action.yml, .github/actions/readiness-upload/index.cjs</files>
  <read_first>
    - .planning/debug/readiness-package-job-fail.md — confirmed ScriptHandler/NodeScriptActionHandler boundary and exact reproduction
    - .github/workflows/readiness.yml — current package step, exact push authority, pinned actions, outputs and four lanes
    - src/maintainer/readiness-workflow.cts — assertWorkflowProvenance, packageAndUpload, appendOutput and exported main
    - src/maintainer/github-artifact-upload.cts — existing process-env auth lookup and same-process lease consumer; read-only in this task
    - tests/maintainer/readiness-workflow.test.cts — current workflow structure and lane receipt assertions
  </read_first>
  <behavior>
    - Exact oracle: with a valid push event/ref/head/workflowCommit tuple, a writable empty GITHUB_OUTPUT file, and both artifact runtime variables absent, the compiled package-upload command exits 1, emits stable reason artifact_auth_invalid, leaves GITHUB_OUTPUT at zero bytes, and makes no artifact service request.
    - RED: the current package shell run fails the new handler classification test because it resolves to ScriptHandler rather than a local JavaScript action handler.
    - GREEN: the package upload step resolves through `uses: ./.github/actions/readiness-upload`; its manifest declares `runs.using: node24`, `runs.main: index.cjs`, and exactly the five existing package outputs.
    - GREEN: the local CJS entrypoint invokes the existing exported workflow main once with package-upload in the same process and propagates its exit code; it does not spawn npm/node, recreate a tgz, read an artifact path, or implement upload logic.
    - Workflow contract: exact branch-push provenance, package-job outputs, four lane jobs, final verifier, external full-OID pins and contents:read permissions remain unchanged.
  </behavior>
  <action>先只修改readiness workflow测试：加入完整missing-runtime可执行oracle，并让package-step handler测试要求candidate-local JavaScript action metadata/entrypoint。运行聚焦测试，确认oracle精确得到exit 1、artifact_auth_invalid、GITHUB_OUTPUT零bytes，同时handler test在现有shell step上失败；提交RED。随后新增最小local action目录：manifest只声明名称、描述、五个既有outputs及node24/index.cjs；entrypoint只require已由前一步`npm run build`生成的`dist/maintainer/readiness-workflow.cjs`并在同一process调用`main(["package-upload"])`。将workflow package upload step从shell command替换为该local `uses`，保留id、READINESS_WORKFLOW_COMMIT、job outputs、先checkout/setup-node/npm ci/build的顺序，以及其余jobs字节语义。不得在manifest/entrypoint/workflow复制认证值、lease、pack、smoke或upload协议。运行聚焦测试形成GREEN提交。</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test --test-name-pattern="missing artifact runtime|JavaScript action handler|one package job" dist-tests/maintainer/readiness-workflow.test.cjs</automated>
  </verify>
  <done>旧shell上下文的精确失败被锁定，正式package producer通过candidate-owned Node action handler同进程进入现有strict uploader。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定production env认证路径与完整readiness回归</name>
  <files>tests/maintainer/github-artifact-upload.test.cts, tests/maintainer/readiness-workflow.test.cts</files>
  <read_first>
    - src/maintainer/github-artifact-upload.cts — options override versus process.env resolution order and pre-consumption auth validation
    - tests/maintainer/github-artifact-upload.test.cts — synthetic option-injection tests and private lease fixture
    - .github/actions/readiness-upload/action.yml — Task 1 handler declaration
    - .github/actions/readiness-upload/index.cjs — Task 1 in-process entrypoint
    - package.json — existing build/test/readiness commands; no dependency or script addition is expected
  </read_first>
  <behavior>
    - With valid runner-shaped values supplied only through process.env and a test fetcher, uploadCandidateArtifactFromLease performs create/raw PUT/finalize once and returns the same metadata-only receipt.
    - With those process values absent and no option overrides, upload fails before consuming the lease or calling fetch; stable auth validation remains unchanged.
    - The workflow/action inventory contains no unpinned external action, extra permission, second pack/uploader, explicit dispatch, candidate registry acquisition or release mutation.
    - Full local tests, docs/local-guide checks, retirement audit, immutable Git brand audit and diff hygiene pass without a remote action.
  </behavior>
  <action>扩展uploader测试类型和fixture，使一个用例只通过临时process.env提供runner-shaped inputs并验证现有create/upload/finalize顺序，另一个用例删除这些inputs且不传options overrides，证明fetch未调用且lease未消费；每个测试必须在finally中精确恢复原环境，禁止打印值。同步强化workflow test对local action output集合、same-process调用、无subprocess/无路径重建、external action full-OID pins及minimal permissions的断言。若production-env用例已由现有实现直接通过，不修改uploader source；任何失败先证明是实现缺口才允许停下重新规划，不能放宽auth校验。完成后运行全部本地门禁；不调用gh、git push或任何remote workflow API。</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:github-artifact-upload &amp;&amp; npm run test:readiness-workflow &amp;&amp; npm run ci:local &amp;&amp; npm run docs:check &amp;&amp; npm run guide:check &amp;&amp; npm run audit:retirement &amp;&amp; npm run audit:brand -- git --subject HEAD &amp;&amp; git diff --check</automated>
  </verify>
  <done>本地回归同时证明runner env生产读取路径、strict missing-auth失败和JavaScript handler wiring；没有远端动作或伪造receipt。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Runner handler → local action process | private job-scoped artifact inputs are injected only for supported action handlers |
| candidate-local action → compiled maintainer runtime | a tiny checked-in entrypoint selects the exact producer command |
| private tgz lease → artifact service | one in-memory/handle-bound artifact crosses authenticated create/upload/finalize calls |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-36-01 | Spoofing/Elevation | runtime artifact authentication | high | mitigate | Node action handler supplies private values; existing token scope and trusted-origin validation remains unchanged |
| T-04.2-36-02 | Tampering | local action entrypoint substitution | high | mitigate | candidate-owned Git blob, fixed relative compiled entrypoint, handler semantic tests and workflowBlobOid receipt binding |
| T-04.2-36-03 | Tampering/Repudiation | second pack or path reconstruction | high | mitigate | entrypoint calls package-upload once in-process; lease consumer count and no-subprocess assertions |
| T-04.2-36-04 | Information disclosure | token/URL/log leakage | high | mitigate | no workflow forwarding or printing; existing metadata-only errors and receipt schema |
| T-04.2-36-05 | Denial of service | control response or upload stall | high | mitigate | existing response byte bounds, timeout and abort behavior |
| T-04.2-36-06 | Tampering | external action or permission drift | high | mitigate | full-OID pin assertions and top-level contents:read-only gate |
| T-04.2-36-07 | Tampering | unintended release/ref mutation | high | mitigate | local-only gates; no remote command, tag, publish, release, registry candidate read or dist-tag surface |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | exact 0.3.0 neutral readiness against one actual tgz | Task 1 repairs only the hosted handler needed to upload the already-audited artifact |
| REQ | BRAND-02 tarball gate cannot be bypassed | same packageAndUpload owner, lease and pack/tar/smoke ordering remain exact |
| REQ | BRAND-04 frozen-subject five-host readiness-only evidence | Task 2 preserves four-lane workflow and prohibits release mutations; Plan 37 refreezes before reuse |
| CONTEXT | D-13 layered Git/tgz audit | no change to source snapshot or actual tgz validators |
| CONTEXT | D-14 secret-safe diagnostics | stable artifact_auth_invalid and metadata-only tests; no runtime value output |
| CONTEXT | D-15 formal-readiness-only hard gate | handler remains exclusive to readiness workflow, not ordinary CI/pre-commit |
| CONTEXT | D-16 one tgz, immutable candidate, no release action | same-process local action preserves the one-lease producer; Plan 37 creates a new candidate |
| CONTEXT | D-17 five-host packaged lanes and ZCode boundary | lane matrix and receipt shape are unchanged |
| RESEARCH/DEBUG | ScriptHandler omits private inputs; Node action handler injects them | Task 1 selects the runner handler matching the uploader contract and Task 2 covers production env lookup |

Source audit result: COVERED. Phase 05–08 work, publication, registry verification, ZCode true-host admission and any rerun of the retired candidate remain out of scope.

<verification>
Focused RED/GREEN: exact missing-runtime oracle plus Node action handler classification and production env adapter tests.

Plan gate: full local CI/docs/guide/retirement/immutable-brand gates pass; no GitHub run is triggered.
</verification>

<success_criteria>
- Missing runtime inputs under shell semantics remain a deterministic, zero-output-file artifact_auth_invalid oracle.
- The production workflow reaches the existing uploader only through a candidate-owned Node action handler.
- One actual tgz, same-process lease consumption, metadata-only outputs, external action pins and minimal permissions remain enforced.
- f387da46d5ebcbeaeb2a1aa0218623a2860422c3 and run 33161895992 remain retired with zero receipts.
- No source implementation outside the five declared files and no external state is changed.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-36-SUMMARY.md. Record RED/GREEN evidence, exact handler design, unchanged uploader auth boundary, full local gates, and retirement of candidate f387da46d5ebcbeaeb2a1aa0218623a2860422c3/run 33161895992; do not claim a new candidate, platform PASS or remote action.
</output>

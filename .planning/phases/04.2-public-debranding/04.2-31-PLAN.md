---
phase: 04.2-public-debranding
plan: "31"
type: execute
wave: 41
depends_on: ["04.2-42"]
files_modified: []
autonomous: true
requirements: [BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: costly
must_haves:
  truths:
    - "D-16：正式readiness必须由实际Windows/Linux × Node 22/24 workflow lanes返回四份metadata-only receipts；缺一即NOT_RUN/blocked。"
    - "D-17：四个lanes复用producer上传的同一actual tgz SHA/memberCount；五宿主navigation PASS，style仅Claude Code 2.1.241 eligible，其余四宿主零写host_version_unsupported。"
    - "workflow只允许lockfile dependency acquisition；不得从registry获取candidate、npm view、remote packageSpec、refetch、tag、publish或dist-tag mutation。"
    - "用户已将本Phase专用readiness ref的窄操作权委托给agent：Plan42新candidate存在后，只可把refs/heads/readiness/04.2-candidate从精确retired OID 6c851a7f7795679fe4269223d328400df2984f8c plain non-force fast-forward到该candidate；remote OID、strict ancestry、branch或non-force条件不符时写前BLOCKED。"
    - "Task 2开始前必须把HEAD、index entries、cached/tracked hunks、tracked path inventory与现有untracked inventory记录到仓库外private baseline；四lane下载后逐字段零delta，不能用全局clean-worktree假设覆盖既有dirty work。"
  artifacts:
    - path: "GitHub Actions run artifacts"
      provides: "four metadata-only platform lane receipts for the frozen candidate"
  key_links:
    - from: "Plan 42 candidateSubject"
      to: "readiness.yml producer and four matrix lanes"
      via: "delegated exact non-force fast-forward push event plus uploaded actual tgz SHA/memberCount"
  prohibitions:
    - "不得用readiness.yml静态检查或本机模拟替代四个平台lane receipts。"
    - "不得把ZCode packaged lane写成true-host version/trust/admission PASS。"
    - "不得执行tag、publish、registry_refetch、npm view、candidate remote packageSpec或dist-tag mutation。"
    - "不得把delegated candidate ref权限扩张为force/delete/alternate branch/default-branch merge/tag/release/publish或dist-tag授权。"
  flagged_assumptions:
    - requirement: BRAND-02
      category: unclassified
      statement: "外部workflow服务与仓库授权可用；不可用时保持blocking checkpoint而非伪造PASS。"
---

<objective>
**As a** release maintainer，**I want to** 真实运行四个OS/Node lanes并收集同一tgz的receipts，**so that** platformLanes: PASS来自执行证据而不是YAML静态推断。

Purpose: 完成readiness-only外部平台证据闭环。
Output: 四份metadata-only lane receipts供final validator消费。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-11-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-38-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-42-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 验证delegated exact candidate ref权限与写前条件</name>
  <files></files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-42-SUMMARY.md — sole post-raw-artifact-download-fix frozen candidateSubject
    - .github/workflows/readiness.yml at candidateSubject — workflow that must exist on the dispatched ref
    - git remote configuration names only — identify approved origin without printing URL
  </read_first>
  <action>应用用户在本Phase内授予的delegated narrow authority，不再请求新的人工checkpoint或特定口令。只从04.2-42-SUMMARY读取完整new candidateSubject，并将权限严格解释为：目标只能是`refs/heads/readiness/04.2-candidate`，expected predecessor只能是6c851a7f7795679fe4269223d328400df2984f8c，操作只能是一次plain non-force fast-forward，或remote已精确等于new candidate时幂等继续。先只读证明new candidate是expected predecessor的strict descendant，包含readiness workflow、local action、corrected uploader、exact raw-artifact downloader pin及其oracle blobs；要求remote ref恰为expected predecessor或已精确等于new candidate，且`gh auth status`成功。absent、第三OID、non-ancestor、认证不可用或blob缺失均在push前BLOCKED。该委托不授权force、force-with-lease、delete、alternate branch、merge、default-branch mutation、tag、release、publish、registry refetch或dist-tag。</action>
  <acceptance_criteria>
    - agent仅使用已委托的专用branch plain non-force fast-forward权限；不再等待人工口令，且不把委托扩张到force/delete/alternate branch/default merge或任何release mutation。
    - local candidate是6c851a7f7795679fe4269223d328400df2984f8c的strict descendant commit，且Git blobs包含.github/workflows/readiness.yml、两个local action文件、corrected uploader source、raw-artifact downloader pin及specified-oracle tests。
    - remote ref只读preflight结果只能是精确6c851a7f7795679fe4269223d328400df2984f8c或精确new candidate OID；其他状态在写前BLOCKED。
  </acceptance_criteria>
  <verify><automated>git cat-file -e &lt;new-candidate-sha&gt;^{commit} &amp;&amp; git merge-base --is-ancestor 6c851a7f7795679fe4269223d328400df2984f8c &lt;new-candidate-sha&gt; &amp;&amp; git cat-file -e &lt;new-candidate-sha&gt;:.github/workflows/readiness.yml &amp;&amp; git cat-file -e &lt;new-candidate-sha&gt;:.github/actions/readiness-upload/action.yml &amp;&amp; git cat-file -e &lt;new-candidate-sha&gt;:.github/actions/readiness-upload/index.cjs &amp;&amp; git cat-file -e &lt;new-candidate-sha&gt;:src/maintainer/github-artifact-upload.cts &amp;&amp; git cat-file -e &lt;new-candidate-sha&gt;:tests/maintainer/readiness-workflow.test.cts &amp;&amp; gh auth status</automated></verify>
  <what-built>Plan42冻结的post-raw-artifact-download-fix candidate与exact candidate-branch push workflow；尚未执行新的远端写。</what-built>
  <how-to-verify>核对new candidate完整SHA、expected predecessor、strict-descendant proof、专用branch名、plain non-force fast-forward边界及明确排除的force/delete/alternate branch/default merge/tag/release/publish/dist-tag动作。</how-to-verify>
  <done>delegated narrow authority与所有写前条件已验证；只有精确专用branch的ordinary fast-forward可进入Task2。</done>
</task>

<task type="auto">
  <name>Task 2: 安全推送exact ref、Dispatch并完成真实四平台workflow</name>
  <files></files>
  <read_first>
    - .github/workflows/readiness.yml — producer, artifact upload, matrix lanes and forbidden-network controls
    - src/maintainer/release-readiness.cts — exactly-one dry-run/actual-pack authority
    - src/maintainer/readiness-workflow.cts — receipt schema and final lane ingestion
    - src/maintainer/github-artifact-upload.cts — same-process private lease upload/finalize protocol
    - .planning/phases/04.2-public-debranding/04.2-42-SUMMARY.md — exact post-raw-artifact-download-fix candidateSubject and six-candidate/five-run retirement boundary
  </read_first>
  <action>仅在Task1通过delegated authority与全部preflight后，先在OS private temp且仓库目录之外以`flag: wx`创建pre-Task-2 baseline JSON；它保存HEAD OID，并对`git ls-files --stage -z`的index entries、`git diff --cached --binary --no-ext-diff`的cached hunks、`git ls-files -z`的tracked path inventory、`git diff --binary --no-ext-diff`的tracked worktree hunks和`git ls-files --others --exclude-standard -z`的现有untracked inventory分别计算SHA-256。baseline不读取文件内容、不写仓库且不打印路径；private evidence directory也必须位于仓库外。随后重新读取目标ref：若精确等于retired 6c851a7f7795679fe4269223d328400df2984f8c，先再次证明其为new candidate祖先，再执行一次plain non-force `git push origin &lt;new-candidate-sha&gt;:refs/heads/readiness/04.2-candidate`；若已精确等于new candidate则不重复写并进入幂等run关联；absent或任何第三OID均BLOCKED，禁止force、force-with-lease、delete、merge或替代branch。该push本身是唯一workflow trigger，不调用显式dispatch API。push后`git ls-remote`必须精确等于new candidate；记录push前时间与SHA，用`gh run list --workflow readiness.yml --branch readiness/04.2-candidate --event push --commit &lt;new-candidate-sha&gt;`定位唯一新run，明确排除failed runs 33161895992、33177297004、33187499766、33192180167、33242048873及任何headSha为六个retired candidates之一的run。run必须event=push、headBranch精确、headSha=candidateSubject；receipts必须绑定精确triggerRef、workflowCommit和candidate-owned workflowBlobOid。任何缺失/多义/OID mismatch均BLOCKED/NOT_RUN。producer通过candidate-owned Node action handler在同一process执行一次dry-run、一次actual pack、全部local consumers及strict uploader CreateArtifact、deterministic block stage、commit block list、FinalizeArtifact后dispose；四lane必须用candidate固定的official raw-artifact downloader按artifact ID取得同一tgz，再验证artifactId/SHA/memberCount并运行五宿主smoke。允许lockfile npm ci；拒绝candidate registry acquisition/refetch、npm view、remote packageSpec、tag、release、publish与dist-tag mutation。只下载metadata-only receipts，不保存raw logs/remote URL/credentials。receipts落盘后重新计算baseline全部六个字段；HEAD/index/cached hunks/tracked paths/tracked worktree hunks/untracked inventory任一delta均BLOCKED，保留既有dirty hunk但绝不要求全局clean。</action>
  <instructions>先验证Task1的delegated narrow authority和完整new candidate SHA；remote等于精确6c851a7f7795679fe4269223d328400df2984f8c时执行plain non-force fast-forward，等于new candidate时按幂等恢复，其他状态停止。新push自动触发；不得调用显式dispatch。用git ls-remote和gh run list/view关联push前时间、event、branch、headSha与candidate，并排除runs 33161895992、33177297004、33187499766、33192180167和33242048873，再等待四lane。权限、ancestry、OID、run关联或lane任一失败时保持BLOCKED并提供safe run id，不手工制造evidence。</instructions>
  <acceptance_criteria>
    - workflow producer记录dryRunCount=1、actualPackCount=1，四lane receipt的candidateSubject、artifactSha256、memberCount完全一致。
    - receipts集合恰为windows/node22、windows/node24、linux/node22、linux/node24，且每份实际执行五宿主packaged smoke。
    - 每lane navigation五宿主PASS；Claude 2.1.241 style eligible并保留冻结receipt bytes/digest；Codex/Cursor/OpenCode/ZCode style为zero-write host_version_unsupported。
    - ZCode receipt明确packagedOnly:true、trueHostAcceptance:NOT_RUN_BY_SCOPE。
    - 任一lane缺失/失败时platformLanes保持NOT_RUN或blocked，不产生PASS receipt。
    - remote dispatch ref从精确retired OID 6c851a7f7795679fe4269223d328400df2984f8c plain non-force fast-forward后等于candidateSubject，或已等于candidate并幂等恢复；无force/delete/alternate branch/default merge/tag/release/publish/dist-tag调用。
    - run及四receipts的event=push、ref/branch精确候选branch、headSha/workflowCommit=candidateSubject、workflowBlobOid匹配candidate Git blob。
    - run id不等于33161895992、33177297004、33187499766、33192180167或33242048873，candidate/head/workflow不等于六个retired candidates中的任何一个；run 33242048873的单个artifact与全部五个retired runs的zero receipts均未被复用。
    - Task 2结束时HEAD、index entries、cached hunks、tracked path inventory、tracked worktree hunks与原有untracked inventory逐字段等于private pre-Task-2 baseline；仓库外receipt/baseline文件不进入inventory。
  </acceptance_criteria>
  <verify><automated>node -e "const{execFileSync}=require('node:child_process');const want='&lt;candidate-sha&gt;';const out=execFileSync('git',['ls-remote','--exit-code','origin','refs/heads/readiness/04.2-candidate'],{encoding:'utf8'}).trim();if(out.split(/\s+/)[0]!==want)process.exit(1)" &amp;&amp; gh run view &lt;run-id&gt; --json conclusion,event,headBranch,headSha,jobs,workflowName &amp;&amp; npm run build &amp;&amp; node dist/maintainer/readiness-workflow.cjs verify-lanes --candidate-subject &lt;candidate-sha&gt; --expected-event push --expected-ref refs/heads/readiness/04.2-candidate --evidence-dir &lt;private-evidence-dir&gt;</automated></verify>
  <verification>向executor提供run id、candidate SHA与private evidence directory；verify-lanes必须返回四lane完整且同一artifact identity。</verification>
  <what-built>Plan11建立、Plan36修复handler、Plan38修复block transport、c372e54修复ProtoJSON request wire、f00a545修复ProtoJSON response aliases、20378c5修复raw artifact downloads的readiness producer/matrix workflow，以及Plan42冻结的new candidate subject。</what-built>
  <how-to-verify>确认delegated plain non-force push自动产生唯一push run；等待四个Windows/Linux × Node 22/24 jobs完成；下载lane receipts并运行带expected-event/ref的verify-lanes。不要手工编辑receipt。</how-to-verify>
  <resume-signal>提供GitHub Actions run id、candidate SHA和本地private evidence directory；四lane未齐时不要恢复。</resume-signal>
  <done>四份同tgz metadata-only lane receipts已由实际workflow产生并下载。</done>
</task>

<task type="auto">
  <name>Task 3: 阻塞验证四lane receipt集合</name>
  <files></files>
  <read_first>
    - src/maintainer/readiness-workflow.cts — lane set and safe receipt parser
    - src/maintainer/readiness-seal.cts — candidate/tgz binding
    - private evidence directory from Task 2 — metadata-only inputs
    - .planning/phases/04.2-public-debranding/04.2-42-SUMMARY.md — frozen candidate identity
  </read_first>
  <action>以strict schema读取恰好四份receipt，拒绝duplicate/missing/unknown lane、非PASS workflow conclusion、candidateSubject/SHA/memberCount漂移、额外正文字段、原始paths或secret canary。额外要求每份triggerEvent=push、triggerRef=refs/heads/readiness/04.2-candidate、headSha=workflowCommit=candidateSubject，并用Git object plumbing核对workflowBlobOid属于candidate内readiness.yml；确认五宿主矩阵与D-17边界。只有集合完整一致才在内存返回platformLanes PASS给Plan32；失败保持NOT_RUN/blocked。最后读取Task2 private baseline并以相同argv、NUL/binary输出和SHA-256算法重算HEAD、index entries、cached hunks、tracked paths、tracked worktree hunks与untracked inventory；任何字段变化都拒绝PASS，baseline comparison本身不得更新index或读取untracked内容。</action>
  <acceptance_criteria>
    - verifier只接受四个精确lane identity且共享candidateSubject/artifactSha256/memberCount。
    - receipt输出只有stable codes/counts/digests/host outcome，无raw path、匹配词、snippet或credential/config value。
    - exact pre/post baseline证明Task 2未改变HEAD、index entries、cached/tracked hunks、tracked path inventory或原有untracked inventory；已有dirty work允许存在但必须byte-identical。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:readiness-workflow &amp;&amp; node dist/maintainer/readiness-workflow.cjs verify-lanes --candidate-subject &lt;candidate-sha&gt; --evidence-dir &lt;private-evidence-dir&gt; &amp;&amp; node -e "const{execFileSync}=require('node:child_process');const{createHash}=require('node:crypto');const{readFileSync}=require('node:fs');const run=a=&gt;execFileSync('git',a,{encoding:null,maxBuffer:67108864});const h=b=&gt;createHash('sha256').update(b).digest('hex');const actual={head:run(['rev-parse','HEAD']).toString('utf8').trim(),index:h(run(['ls-files','--stage','-z'])),cachedHunks:h(run(['diff','--cached','--binary','--no-ext-diff'])),trackedPaths:h(run(['ls-files','-z'])),trackedHunks:h(run(['diff','--binary','--no-ext-diff'])),untracked:h(run(['ls-files','--others','--exclude-standard','-z']))};const expected=JSON.parse(readFileSync(process.argv[1],'utf8'));if(JSON.stringify(actual)!==JSON.stringify(expected))process.exit(1)" &lt;private-pre-task-2-baseline&gt;</automated></verify>
  <done>platformLanes PASS已由实际四lane receipts阻塞验证并私下传递。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub workflow/runner → local validator | external metadata and artifacts are untrusted |
| producer tgz → four OS/Node lanes | artifact transfer may be substituted or truncated |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-31-01 | Tampering/Elevation | tar traversal/link/special entries | high | mitigate | producer tar audit plus each-lane safe extraction policy |
| T-04.2-31-02 | Denial of service | oversized/binary artifact | high | mitigate | byte/member/depth limits before read/extract |
| T-04.2-31-03 | Tampering | Unicode bypass | high | mitigate | same NFKC/case/separator matcher in producer |
| T-04.2-31-04 | Spoofing/Information disclosure | subprocess/log injection | high | mitigate | argv invocation, schema parsing, metadata-only receipts |
| T-04.2-31-05 | Tampering/Repudiation | pack/upload/smoke TOCTOU | high | mitigate | artifact SHA/memberCount checked in all four lanes |
| T-04.2-31-06 | Information disclosure | secret/config disclosure | high | mitigate | canary tests and rejection of non-schema receipt keys |
| T-04.2-31-07 | Tampering | unintended ref/release/network mutation | high | mitigate | blocking user authorization, non-force dedicated branch, exact ls-remote OID gate; no tag/release/default merge/publish/dist-tag; only lockfile npm ci allowed |
</threat_model>

## Artifacts this phase produces

- Four untracked downloaded platform receipt files with fields schemaVersion, os, nodeMajor, candidateSubject, artifactSha256, memberCount, dryRunCount, actualPackCount, workflowConclusion, hostOutcomes, packagedOnly and trueHostAcceptance.
- No new repository file, runtime symbol, CLI surface, state field or tracked receipt is created in this plan.
- Delegated external effect: after Plan42 freezes the exact candidate and all preconditions pass, plain non-force fast-forward the existing readiness/04.2-candidate branch only from retired 6c851a7f7795679fe4269223d328400df2984f8c to its exact new descendant; an already-equal ref is idempotent, while absent/third-OID/non-FF states block. No force/delete/alternate branch and no broader release authority.

<verification>
Quick gate: verify-lanes against downloaded evidence.

Plan gate: actual workflow conclusion plus strict four-lane validator; static YAML tests alone are insufficient.
</verification>

<success_criteria>
- Four real lanes used one exact uploaded tgz.
- platformLanes cannot become PASS from missing, simulated or static evidence.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-31-SUMMARY.md neutral-by-construction as a documentation-only child: current identity and metadata only; do not embed source excerpts, raw external logs, machine/user paths or forbidden spellings.
</output>

---
phase: 04.2-public-debranding
plan: "31"
type: execute
wave: 34
depends_on: ["04.2-30"]
files_modified: []
autonomous: false
requirements: [BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: reversible
must_haves:
  truths:
    - "D-16：正式readiness必须由实际Windows/Linux × Node 22/24 workflow lanes返回四份metadata-only receipts；缺一即NOT_RUN/blocked。"
    - "D-17：四个lanes复用producer上传的同一actual tgz SHA/memberCount；五宿主navigation PASS，style仅Claude Code 2.1.241 eligible，其余四宿主零写host_version_unsupported。"
    - "workflow只允许lockfile dependency acquisition；不得从registry获取candidate、npm view、remote packageSpec、refetch、tag、publish或dist-tag mutation。"
    - "执行期只有在独立用户授权checkpoint后，才可将exact candidate commit推送到专用dispatch branch；remote OID不等时BLOCKED且绝不force。"
  artifacts:
    - path: "GitHub Actions run artifacts"
      provides: "four metadata-only platform lane receipts for the frozen candidate"
  key_links:
    - from: "Plan 30 candidateSubject"
      to: "readiness.yml producer and four matrix lanes"
      via: "authorized exact-branch push event plus uploaded actual tgz SHA/memberCount"
  prohibitions:
    - "不得用readiness.yml静态检查或本机模拟替代四个平台lane receipts。"
    - "不得把ZCode packaged lane写成true-host version/trust/admission PASS。"
    - "不得执行tag、publish、registry_refetch、npm view、candidate remote packageSpec或dist-tag mutation。"
    - "不得把candidate ref push授权扩张为tag、release、default-branch merge、publish或dist-tag授权。"
  flagged_assumptions:
    - requirement: BRAND-02
      category: unclassified
      statement: "外部workflow服务与仓库授权可用；不可用时保持blocking checkpoint而非伪造PASS。"
---

<objective>
**As a** release maintainer，**I want to** 真实运行四个OS/Node lanes并收集同一tgz的receipts，**so that** platformLanes: PASS来自执行证据而不是YAML静态推断。

Purpose: 完成readiness-only外部平台证据闭环。
Output: 四份metadata-only lane receipts供final validator消费。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-11-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-30-SUMMARY.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 授权exact candidate远端dispatch ref写入</name>
  <files></files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-30-SUMMARY.md — exact frozen candidateSubject
    - .github/workflows/readiness.yml at candidateSubject — workflow that must exist on the dispatched ref
    - git remote configuration names only — identify approved origin without printing URL
  </read_first>
  <action>这是新的执行期外部写授权边界。checkpoint展示candidateSubject、目标`refs/heads/readiness/04.2-candidate`、将执行的一次non-force branch push及其可见远端影响；在用户明确回复`AUTHORIZE_READINESS_REF_PUSH`之前只允许`git cat-file`、`git ls-remote`与`gh auth status`只读检查，禁止push/merge/tag/release/publish/dist-tag。若用户拒绝、未授权、远端认证不可用、candidate不含readiness.yml，或目标ref已指向不同OID，则overall readiness保持BLOCKED、platformLanes=NOT_RUN，不dispatch也不伪造PASS。</action>
  <acceptance_criteria>
    - 用户明确授权仅限exact candidate到专用branch，不包含default-branch merge或任何release mutation。
    - local candidate是commit且其Git blob包含.github/workflows/readiness.yml。
    - remote ref只读preflight结果为absent或精确candidate OID；其他状态BLOCKED。
  </acceptance_criteria>
  <verify><automated>git cat-file -e &lt;candidate-sha&gt;^{commit} &amp;&amp; git cat-file -e &lt;candidate-sha&gt;:.github/workflows/readiness.yml &amp;&amp; gh auth status</automated></verify>
  <what-built>Plan30冻结的candidate与Plan11 exact candidate-branch push workflow；尚未执行任何远端写。</what-built>
  <how-to-verify>核对candidate SHA、专用branch名、non-force push边界及明确排除的tag/release/default merge/publish/dist-tag动作。</how-to-verify>
  <resume-signal>仅接受AUTHORIZE_READINESS_REF_PUSH；其他回复均保持BLOCKED/NOT_RUN。</resume-signal>
  <done>用户已对exact candidate的单一dispatch branch写入给出窄授权。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 安全推送exact ref、Dispatch并完成真实四平台workflow</name>
  <files></files>
  <read_first>
    - .github/workflows/readiness.yml — producer, artifact upload, matrix lanes and forbidden-network controls
    - src/maintainer/release-readiness.cts — exactly-one dry-run/actual-pack authority
    - src/maintainer/readiness-workflow.cts — receipt schema and final lane ingestion
    - src/maintainer/github-artifact-upload.cts — same-process private lease upload/finalize protocol
    - .planning/phases/04.2-public-debranding/04.2-30-SUMMARY.md — exact frozen candidateSubject
  </read_first>
  <action>仅在Task1收到精确授权后，读取目标`refs/heads/readiness/04.2-candidate`的`git ls-remote`结果：absent时执行一次non-force `git push origin &lt;candidate-sha&gt;:refs/heads/readiness/04.2-candidate`，已等于candidate时不重复写；若指向其他OID则BLOCKED，禁止force、delete或merge。该push本身是唯一workflow trigger，不调用任何显式dispatch API。push后再次`git ls-remote`并要求remote OID精确等于candidateSubject；记录push前时间与candidate SHA，再用`gh run list --workflow readiness.yml --branch readiness/04.2-candidate --event push --commit &lt;candidate-sha&gt;`定位由该push产生的唯一run。run必须event=push、headBranch=readiness/04.2-candidate、headSha=candidateSubject；receipts还必须证明triggerRef精确、workflowCommit=candidateSubject，且workflowBlobOid等于candidate commit内readiness.yml blob。任何缺失/多义/OID mismatch均BLOCKED/NOT_RUN。producer在同一Node process执行一次dry-run、一次actual pack、全部local consumers及`uploadCandidateArtifactFromLease`，finalize成功后dispose；不得独立upload step或从metadata/path重建tgz。四lane验证artifactId/SHA/memberCount后运行五宿主smoke。允许lockfile npm ci；拒绝candidate registry acquisition/refetch、npm view、remote packageSpec、tag、release、publish与dist-tag mutation。下载metadata-only receipts，不保存raw logs/remote URL/credentials。</action>
  <instructions>先验证Task1授权信号；执行non-force exact branch push或确认same-OID ref。新push自动触发；不得调用显式dispatch。用git ls-remote和gh run list/view关联push时间、event、branch、headSha与candidate，再等待四lane。权限、OID、run关联或lane任一失败时保持BLOCKED并提供run id，不手工制造evidence。</instructions>
  <acceptance_criteria>
    - workflow producer记录dryRunCount=1、actualPackCount=1，四lane receipt的candidateSubject、artifactSha256、memberCount完全一致。
    - receipts集合恰为windows/node22、windows/node24、linux/node22、linux/node24，且每份实际执行五宿主packaged smoke。
    - 每lane navigation五宿主PASS；Claude 2.1.241 style eligible并保留冻结receipt bytes/digest；Codex/Cursor/OpenCode/ZCode style为zero-write host_version_unsupported。
    - ZCode receipt明确packagedOnly:true、trueHostAcceptance:NOT_RUN_BY_PHASE_06。
    - 任一lane缺失/失败时platformLanes保持NOT_RUN或blocked，不产生PASS receipt。
    - remote dispatch ref OID精确等于candidateSubject；无force/default merge/tag/release/publish/dist-tag调用。
    - run及四receipts的event=push、ref/branch精确候选branch、headSha/workflowCommit=candidateSubject、workflowBlobOid匹配candidate Git blob。
  </acceptance_criteria>
  <verify><automated>node -e "const{execFileSync}=require('node:child_process');const want='&lt;candidate-sha&gt;';const out=execFileSync('git',['ls-remote','--exit-code','origin','refs/heads/readiness/04.2-candidate'],{encoding:'utf8'}).trim();if(out.split(/\s+/)[0]!==want)process.exit(1)" &amp;&amp; gh run view &lt;run-id&gt; --json conclusion,event,headBranch,headSha,jobs,workflowName &amp;&amp; npm run build &amp;&amp; node dist/maintainer/readiness-workflow.cjs verify-lanes --candidate-subject &lt;candidate-sha&gt; --expected-event push --expected-ref refs/heads/readiness/04.2-candidate --evidence-dir &lt;private-evidence-dir&gt;</automated></verify>
  <verification>向executor提供run id、candidate SHA与private evidence directory；verify-lanes必须返回四lane完整且同一artifact identity。</verification>
  <what-built>Plan 11建立的readiness producer/matrix workflow与Plan 30冻结的candidate subject。</what-built>
  <how-to-verify>确认authorized push自动产生唯一push run；等待四个Windows/Linux × Node 22/24 jobs完成；下载lane receipts并运行带expected-event/ref的verify-lanes。不要手工编辑receipt。</how-to-verify>
  <resume-signal>提供GitHub Actions run id、candidate SHA和本地private evidence directory；四lane未齐时不要恢复。</resume-signal>
  <done>四份同tgz metadata-only lane receipts已由实际workflow产生并下载。</done>
</task>

<task type="auto">
  <name>Task 3: 阻塞验证四lane receipt集合</name>
  <files></files>
  <read_first>
    - src/maintainer/readiness-workflow.cts — lane set and safe receipt parser
    - src/maintainer/readiness-seal.cts — candidate/tgz binding
    - private evidence directory from Task 2 — metadata-only inputs
    - .planning/phases/04.2-public-debranding/04.2-30-SUMMARY.md — frozen candidate identity
  </read_first>
  <action>以strict schema读取恰好四份receipt，拒绝duplicate/missing/unknown lane、非PASS workflow conclusion、candidateSubject/SHA/memberCount漂移、额外正文字段、原始paths或secret canary。额外要求每份triggerEvent=push、triggerRef=refs/heads/readiness/04.2-candidate、headSha=workflowCommit=candidateSubject，并用Git object plumbing核对workflowBlobOid属于candidate内readiness.yml；确认五宿主矩阵与D-17边界。只有集合完整一致才在内存返回platformLanes PASS给Plan32；失败保持NOT_RUN/blocked。</action>
  <acceptance_criteria>
    - verifier只接受四个精确lane identity且共享candidateSubject/artifactSha256/memberCount。
    - receipt输出只有stable codes/counts/digests/host outcome，无raw path、匹配词、snippet或credential/config value。
    - no-write check证明Task 2未改变Git/index/worktree。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:readiness-workflow &amp;&amp; node dist/maintainer/readiness-workflow.cjs verify-lanes --candidate-subject &lt;candidate-sha&gt; --evidence-dir &lt;private-evidence-dir&gt; &amp;&amp; git diff --check</automated></verify>
  <done>platformLanes PASS已由实际四lane receipts阻塞验证并私下传递。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub workflow/runner → local validator | external metadata and artifacts are untrusted |
| producer tgz → four OS/Node lanes | artifact transfer may be substituted or truncated |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-31-01 | Tampering/Elevation | tar traversal/link/special entries | high | mitigate | producer tar audit plus each-lane safe extraction policy |
| T-04.2-31-02 | Denial of service | oversized/binary artifact | high | mitigate | byte/member/depth limits before read/extract |
| T-04.2-31-03 | Tampering | Unicode bypass | high | mitigate | same NFKC/case/separator matcher in producer |
| T-04.2-31-04 | Spoofing/Information disclosure | subprocess/log injection | high | mitigate | argv invocation, schema parsing, metadata-only receipts |
| T-04.2-31-05 | Tampering/Repudiation | pack/upload/smoke TOCTOU | high | mitigate | artifact SHA/memberCount checked in all four lanes |
| T-04.2-31-06 | Information disclosure | secret/config disclosure | high | mitigate | canary tests and rejection of non-schema receipt keys |
| T-04.2-31-07 | Tampering | unintended ref/release/network mutation | high | mitigate | blocking user authorization, non-force dedicated branch, exact ls-remote OID gate; no tag/release/default merge/publish/dist-tag; only lockfile npm ci allowed |
</threat_model>

## Artifacts this phase produces

- Four untracked downloaded platform receipt files with fields schemaVersion, os, nodeMajor, candidateSubject, artifactSha256, memberCount, dryRunCount, actualPackCount, workflowConclusion, hostOutcomes, packagedOnly and trueHostAcceptance.
- No new repository file, runtime symbol, CLI surface, state field or tracked receipt is created in this plan.
- Authorized external effect: create the dedicated readiness/04.2-candidate branch only when absent, pointing exactly at candidateSubject; no force/update of a different OID and no broader release authority.

<verification>
Quick gate: verify-lanes against downloaded evidence.

Plan gate: actual workflow conclusion plus strict four-lane validator; static YAML tests alone are insufficient.
</verification>

<success_criteria>
- Four real lanes used one exact uploaded tgz.
- platformLanes cannot become PASS from missing, simulated or static evidence.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-31-SUMMARY.md neutral-by-construction as a documentation-only child: current identity and metadata only; do not embed source excerpts, raw external logs, machine/user paths or forbidden spellings.
</output>

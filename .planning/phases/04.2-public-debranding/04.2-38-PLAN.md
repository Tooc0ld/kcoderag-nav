---
phase: 04.2-public-debranding
plan: "38"
type: tdd
wave: 36
depends_on: ["04.2-37"]
files_modified:
  - "src/maintainer/github-artifact-upload.cts"
  - "tests/maintainer/github-artifact-upload.test.cts"
  - "src/maintainer/readiness-workflow.cts"
  - "tests/maintainer/readiness-workflow.test.cts"
autonomous: true
requirements: [BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: costly
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-16、BRAND-02：candidate-owned local action继续在同一Node process消费唯一private tgz lease；Actions artifact v7控制面保持CreateArtifact/FinalizeArtifact原shape，数据面改为确定性Azure Put Block序列和一次Put Block List commit。"
    - "旧的一次性bare blob PUT不再是可接受请求；每个block ID、分块边界、stage顺序与commit block-list顺序都由exact tests锁定，全部stage bytes拼接后必须与leased tgz逐byte相同。"
    - "data-plane retries、timeouts、总bytes、block bytes、block count、control response与URL长度全部有固定上限；只重试幂等stage/commit的network、timeout、408、429和5xx结果，尝试次数有界；每个stage/commit response body均立即、去敏、有界地cancel且绝不解析。"
    - "Create成功后的任一stage、commit或Finalize失败都先best-effort DeleteArtifact再返回；lease在成功或失败后均dispose且不可复用，Finalize严格晚于成功commit。"
    - "失败诊断只公开稳定reason、封闭stage和status class；不得序列化signed URL、query/token、headers、body、Azure error text、response text或runner backend IDs。"
    - "runtime dependencies保持zero；实现只使用Node.js 22+ built-ins和现有fetch，不修改local action、workflow topology、one-tgz owner、五宿主lanes或release boundary。"
  artifacts:
    - path: "src/maintainer/github-artifact-upload.cts"
      provides: "official-compatible deterministic Azure block staging, block-list commit, bounded retry and safe failure metadata"
    - path: "tests/maintainer/github-artifact-upload.test.cts"
      provides: "RED/GREEN transport, byte identity, cleanup, retry, lease and leakage contract tests"
    - path: "src/maintainer/readiness-workflow.cts"
      provides: "closed secret-safe stage/status-class projection for local-action failure output"
    - path: "tests/maintainer/readiness-workflow.test.cts"
      provides: "end-to-end failure JSON oracle that never reflects artifact service secrets"
  key_links:
    - from: ".github/actions/readiness-upload/index.cjs"
      to: "src/maintainer/readiness-workflow.cts package-upload"
      via: "unchanged one in-process main invocation"
    - from: "src/maintainer/readiness-workflow.cts packageAndUpload"
      to: "src/maintainer/github-artifact-upload.cts uploadCandidateArtifactFromLease"
      via: "one private CandidatePackageArtifactLease consumer"
    - from: "CreateArtifact signedUploadUrl"
      to: "Azure block staging and commit"
      via: "preserved signed query plus comp=block/blockid and comp=blocklist requests"
  prohibitions:
    - "不得增加@actions/artifact、Azure SDK或任何runtime/dev package来绕过zero-dependency合同。"
    - "不得退回bare Put Blob、第二次pack、repo extraction、artifact path reconstruction或独立upload-artifact step。"
    - "不得放宽results origin、JWT scope、signed blob origin、control response、artifact metadata或SHA-256验证。"
    - "不得修改.github/actions/readiness-upload、readiness.yml、candidate ref、receipt或任何remote/release状态。"
    - "不得执行push、workflow rerun、tag、publish、registry_refetch、npm view、remote packageSpec、release或dist-tag mutation。"
---

<objective>
以测试先行方式把Actions artifact数据面替换为官方兼容的确定性Azure block stage/commit transport，并让下一次真实run在失败时留下严格去敏的stage/status-class诊断。

Purpose: 关闭run 33177297004暴露的live protocol缺口，同时保留one-actual-tgz、same-process lease、零依赖和readiness-only边界。
Output: 分块transport、bounded retry/cleanup、safe diagnostics及拒绝旧bare PUT的RED/GREEN合同。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/readiness-action-upload-fail.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-36-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-37-SUMMARY.md
@src/maintainer/github-artifact-upload.cts
@tests/maintainer/github-artifact-upload.test.cts
@src/maintainer/readiness-workflow.cts
@tests/maintainer/readiness-workflow.test.cts
@.github/actions/readiness-upload/action.yml
@.github/actions/readiness-upload/index.cjs
@.github/workflows/readiness.yml

<interfaces>
`uploadCandidateArtifactFromLease(lease, options)` is the sole data-plane entry and returns only artifactId/name/sha256/memberCount/size. `releaseReadiness.withCandidatePackageBytes` grants the workflow-upload consumer one callback over exact tgz bytes and disposes the lease after callback settlement. `readinessWorkflow.main(["package-upload"])` converts recognized maintainer errors into one JSON stdout value and exit code 1. Keep these public shapes except for adding closed optional stage/statusClass failure metadata.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN 贯通single-block stage、block-list commit与safe failure output</name>
  <precondition>Run 33177297004 remains failed with zero artifacts/receipts, remote refs/heads/readiness/04.2-candidate still resolves to 6b3d1bcd304dc6cd66b44938098e081a8693ead3, and `.planning/debug/readiness-action-upload-fail.md` remains the diagnosed root-cause record; verify read-only and do not rerun.</precondition>
  <reversibility rating="costly">该wire sequence决定GitHub-hosted runner能否持久化唯一candidate artifact；回退到invented request会再次跳过全部四lane。</reversibility>
  <files>tests/maintainer/github-artifact-upload.test.cts, tests/maintainer/readiness-workflow.test.cts, src/maintainer/github-artifact-upload.cts, src/maintainer/readiness-workflow.cts</files>
  <read_first>
    - .planning/debug/readiness-action-upload-fail.md — live failure, eliminated hypotheses and official v7 control/data-plane differential
    - src/maintainer/github-artifact-upload.cts — current CreateArtifact, bare PUT, FinalizeArtifact, cleanup and lease boundary
    - tests/maintainer/github-artifact-upload.test.cts — permissive mock that currently admits the invalid transport
    - src/maintainer/readiness-workflow.cts — current stable-reason-only error projection
    - tests/maintainer/readiness-workflow.test.cts — executable package-upload stdout and no-output-file oracle
  </read_first>
  <behavior>
    - RED: replace the happy-path mock with a strict request oracle that rejects any signed-blob PUT lacking `comp=block` or `comp=blocklist`; current code fails before FinalizeArtifact.
    - GREEN sequence: CreateArtifact v7, one Put Block carrying the exact lease bytes, one Put Block List whose sole ordered ID equals the staged ID, then FinalizeArtifact with the existing string size and wrapped sha256 hash.
    - Query/header contract: clone the trusted signed URL, preserve every signed query item, append only the operation parameters, use a fixed supported `x-ms-version`, exact data/content lengths, octet-stream for blocks and XML for block-list commit; redirects remain errors.
    - Deterministic ID contract: derive fixed-width ASCII indices starting at zero, encode them as base64 with equal decoded length, use no randomness/time/path/content/token, and commit IDs in ascending byte-offset order.
    - Byte contract: the concatenation of staged block bodies equals the private leased tgz Buffer exactly; no extraction, repo write, second pack or body serialization occurs.
    - Failure contract: a synthetic commit 503 produces artifact_upload_failed plus stage=commit_block_list and statusClass=5xx, calls DeleteArtifact, omits FinalizeArtifact and disposes the lease.
    - Workflow projection: package-upload failure stdout contains only schemaVersion/status/reason/stage/statusClass and never any injected URL, signature, header, request/response body, Azure text, runtime token or backend ID canary.
  </behavior>
  <action>先只改两份测试建立RED：strict fetch oracle必须解析method、trusted host和query，分别识别Create、stage、commit、Finalize、Delete；它对旧bare PUT直接抛出unexpected_request，并检查commit XML只引用已stage的deterministic ID。另用workflow main的注入fixture制造commit 503及包含secret canary的响应，要求stdout只有封闭failure metadata。运行聚焦测试并提交预期RED。随后在uploader内把单次blob PUT替换为顺序block staging与commit-block-list helper：固定block size/总bytes/block count/service-version上限，所有block ID等长且由index确定，使用URL API保留SAS query并追加operation query，commit XML由内部ID列表生成。保持CreateArtifact与FinalizeArtifact v7 request body逐字段不变、digest/size/memberCount receipt不变、origin/token/signed URL校验不变。扩展GitHubArtifactUploadError为封闭stage/statusClass元数据，并在readiness workflow只投影allow-listed字段；未知异常仍折叠为readiness_workflow_failed。不得读取或转发响应正文来生成诊断。运行同一聚焦命令形成GREEN提交。</action>
  <acceptance_criteria>
    - 旧bare PUT在RED oracle中失败，GREEN request sequence精确为create、stage_block、commit_block_list、finalize。
    - staged bytes逐byte等于lease bytes，commit只引用实际stage的deterministic block IDs且Finalize只在commit成功后发生。
    - commit 5xx路径调用DeleteArtifact、不调用Finalize、lease不可复用，failure JSON只含封闭安全字段。
    - CreateArtifact与FinalizeArtifact v7 bodies及成功receipt shape保持现有精确合同。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; node --test --test-name-pattern="block stage|block list|safe upload failure" dist-tests/maintainer/github-artifact-upload.test.cjs dist-tests/maintainer/readiness-workflow.test.cjs</automated></verify>
  <done>最薄single-block真实协议路径从private lease贯通到Finalize，且失败从uploader到action stdout保留stage/status class而不泄密。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展multi-block顺序、bounded retries与全阶段cleanup矩阵</name>
  <files>tests/maintainer/github-artifact-upload.test.cts, src/maintainer/github-artifact-upload.cts</files>
  <read_first>
    - src/maintainer/github-artifact-upload.cts — Task 1 block planner, request builder and error metadata seams
    - tests/maintainer/github-artifact-upload.test.cts — Task 1 strict transport oracle and reusable lease fixture
    - src/maintainer/release-readiness.cts — lease byte/consumer/disposal and candidate artifact resource bounds
  </read_first>
  <behavior>
    - A deterministic multi-block fixture crosses at least three block boundaries; observed IDs are unique/equal-length/ascending, request bodies concatenate to the exact leased bytes, and commit XML preserves the same order.
    - Stage and commit retry only network, timeout, HTTP 408, 429, and 5xx outcomes; each operation has a fixed maximum attempt count, non-retryable 3xx/other 4xx stops immediately, and no retry changes block ID or body bytes.
    - Every Put Block and Put Block List response, including success, retryable status and terminal error, calls `response.body.cancel()` immediately after status classification without `text()`, `json()`, `arrayBuffer()` or stream reads; cancellation is raced against a fixed cleanup deadline, cancellation failure is swallowed, and the next retry/final failure does not wait without bound.
    - Non-empty response-stream fixtures cover a retryable stage 503 followed by success and a terminal commit 4xx: each stream is cancelled exactly once before retry/return, no canary chunk is pulled or reflected, and the retry retains the exact block ID/body.
    - Timeout uses AbortController per attempt; retry and timeout settings accept only bounded safe integers and cannot multiply attempts, bytes, blocks or elapsed work beyond declared ceilings.
    - Oversized byte length, block count overflow, malformed signed query, duplicate/invalid block plan and unexpected response class fail before Finalize with stable safe metadata.
    - After Create succeeds, final failure at every staged block position, commit, or Finalize attempts DeleteArtifact exactly once best-effort; cleanup failure cannot replace the original reason/stage/statusClass.
    - Success and every failure consume/dispose the lease exactly once; no second consumer can observe bytes, and tests never write leased bytes to the repository or temp extraction tree.
    - Serialization canaries in signed URL, token, headers, block bytes, XML-adjacent response text and Azure error text never appear in error message, JSON projection or receipt.
  </behavior>
  <action>先用table-driven tests补齐三类RED矩阵：至少三block的ID/order/byte equality；retryable与non-retryable network/status outcomes及attempt ceiling；Create后每个失败阶段的Delete/Finalize/lease/disclosure结果。retry矩阵必须额外提供真正non-empty `ReadableStream` response：stage 503后成功、terminal commit 4xx，并断言每个body在下一attempt或返回前恰好cancel一次、pull/read次数为零、canary不进入任何error/JSON/receipt；成功stage/commit的non-empty body也必须cancel。测试必须以mock fetch记录结构化method/query/header/body metadata但不把secret值放进断言失败消息。再补实现：以固定常量切分Buffer并在开始网络前验证总bytes/block count；每次stage复用完全相同URL/block ID/body，commit复用完全相同ordered XML；只对显式retryable类别使用有界attempt loop和每attempt AbortController，其他结果立即失败。每次data-plane fetch一取得Response并完成封闭status classification，就必须在任何retry、success progression或error return前调用`response.body?.cancel()`，不得调用text/json/arrayBuffer或读取stream；以固定cleanup deadline与cancel Promise竞速，timeout/cancel rejection只安全吞掉且不能替换原始结果，确保连接清理不会无界延长attempt。DeleteArtifact继续best-effort且最多一次，原始安全诊断优先。不要并发stage，避免order和resource预算多义；不要给Create/Finalize增加可能重复控制面副作用的自动重试。完成后运行uploader/workflow/full local gates，确认dependencies仍为空、action/workflow blobs未改且没有远端动作。</action>
  <acceptance_criteria>
    - multi-block test证明deterministic IDs/order和exact leased byte equality，裸PUT或commit-before-stage无法通过。
    - retry矩阵证明类别、attempt ceiling、per-attempt timeout和body/ID稳定；non-retryable响应零重试。
    - retryable、terminal error与success的non-empty data-plane response streams均在不解析、不pull、不泄露canary的前提下恰好cancel一次，且cancel cleanup有固定deadline。
    - 每个Create后失败阶段都恰好一次cleanup、零Finalize或仅在commit后Finalize，并保持原错误元数据。
    - package.json production dependencies仍为空，local action/workflow和one-tgz owner无差异。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:github-artifact-upload &amp;&amp; npm run test:readiness-workflow &amp;&amp; node -e "const p=require('./package.json');if(!p.dependencies||Object.keys(p.dependencies).length!==0)process.exit(1)" &amp;&amp; git diff --check</automated></verify>
  <done>block transport在成功、重试、timeout、资源拒绝和全部失败stage上都有可执行合同，且没有扩张dependency、artifact ownership或release surface。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub artifact control plane → candidate local action | runtime token, backend IDs and signed URL are untrusted secrets |
| signed Azure URL → block data plane | query capabilities and HTTP outcomes are untrusted and must not enter diagnostics |
| private candidate lease → staged blocks | exact tgz bytes must cross once without extraction, substitution or reuse |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-38-01 | Spoofing/Elevation | token scope, results origin and signed blob origin | high | mitigate | preserve exact JWT scope and HTTPS host allow-lists before lease consumption |
| T-04.2-38-02 | Tampering/Repudiation | block ID/order/body substitution | high | mitigate | deterministic equal-length IDs, sequential stage, exact byte equality and ordered block-list tests |
| T-04.2-38-03 | Denial of service | bytes, blocks, retries and stalled requests | high | mitigate | fixed total/block/count/attempt/time limits with per-attempt AbortController |
| T-04.2-38-04 | Information disclosure/Denial of service | URL/token/header/body/Azure response reflection or retained response streams | high | mitigate | closed stage/status-class enums; every data-plane body is immediately cancelled without parsing under a fixed cleanup deadline; non-empty stream retry/error canaries |
| T-04.2-38-05 | Tampering | partial artifact finalized or retained | high | mitigate | Finalize only after commit; once-only best-effort DeleteArtifact on every post-create failure |
| T-04.2-38-06 | Tampering/Repudiation | lease reuse or alternate artifact bytes | high | mitigate | same-process withCandidatePackageBytes consumer, digest recheck and post-settlement disposed lease tests |
| T-04.2-38-07 | Tampering | supply-chain or release-surface expansion | high | mitigate | Node built-ins only, empty dependencies assertion, unchanged action/workflow and no remote/release commands |
</threat_model>

## Artifacts this phase produces

- Updated `src/maintainer/github-artifact-upload.cts` and `src/maintainer/readiness-workflow.cts`; compiled CJS is build output, not an independently edited source.
- Updated focused contract tests for block transport and secret-safe failure projection.
- No tgz, artifact, receipt, candidate, ref, run, tag, release or registry state is produced by this plan.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | exact neutral 0.3.0 readiness from one actual tgz | Plan 38 repairs only the failed artifact transport; Plan 39 refreezes the full candidate before any external run |
| REQ | BRAND-01 immutable Git zero-hit | product prose/identity is unchanged; Plan 39 reruns whole-subject zero-hit over this implementation and Summary |
| REQ | BRAND-02 one exact tarball and non-bypassable pack evidence | both tasks consume only the existing private lease bytes and prohibit repack/path reconstruction |
| REQ | BRAND-03 behavior/five-host equivalence | action/workflow/five-host lanes remain unchanged; exact bytes delivered to them are tested |
| REQ | BRAND-04 frozen subject and readiness-only evidence | no remote/release action here; Plan 39 creates a new subject and Plan 31 retains actual lanes |
| CONTEXT | D-01–D-15 identity, Skill, audit and secret-safe gates | existing completed Plans 01–35 remain historical evidence; this correction changes no identity/Skill/scanner semantics and strengthens D-14 diagnostics |
| CONTEXT | D-16 one tgz, immutable candidate, no release action | same-process lease and no external actions are explicit invariants; new freeze is Plan 39 |
| CONTEXT | D-17 five-host packaged/ZCode boundary | existing four-lane topology and packaged-only ZCode receipt remain untouched |
| CONTEXT | D-18 repository-owned guide | no guide or sibling repository access occurs |
| RESEARCH | one-artifact-many-validators, Node built-ins, bounded safe I/O | block transport preserves the single lease and adds explicit byte/retry/time/resource bounds |
| DEBUG | official control shapes matched; bare PUT diverged from block stage/commit and hid failure stage | Task 1 replaces the exact divergent seam and Task 2 rejects the permissive mock class |

Source audit result: COVERED. Publication, registry verification, ZCode true-host admission, Phase 05 precision and Phase 07/08 work remain out of scope.

<verification>
Focused RED/GREEN: strict block transport and safe failure JSON oracles.

Plan gate after Task 2: `npm run ci:local`, `npm run docs:check`, `npm run guide:check`, `npm run audit:retirement`, `npm run audit:brand -- git --subject HEAD`, `git diff --check`; no remote action.
</verification>

<success_criteria>
- Official-compatible block stage/commit transport sends the exact leased tgz bytes and rejects the old bare PUT contract.
- Retries, timeouts, bytes, blocks and cleanup are bounded and deterministic.
- Failure output identifies only stage/status class and leaks no URL/token/header/body/service text.
- CreateArtifact/FinalizeArtifact v7 shapes, same-process lease, one-tgz owner, zero dependencies, local action, four lanes and readiness-only scope remain intact.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-38-SUMMARY.md. Record RED/GREEN commits, exact request-sequence contracts, bounds, safe diagnostic fields and all local gates; retire candidate 6b3d1bcd304dc6cd66b44938098e081a8693ead3 and runs 33161895992/33177297004 with zero receipts, and do not claim a new candidate or platform PASS.
</output>

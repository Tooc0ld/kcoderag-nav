---
phase: 04.2-public-debranding
plan: "07"
type: execute
wave: 8
depends_on: ["04.2-06"]
files_modified:
  - "src/maintainer/brand-audit.cts"
  - "src/maintainer/tar-archive.cts"
  - "src/maintainer/pack-audit.cts"
  - "tests/maintainer/brand-audit.test.cts"
  - "tests/maintainer/tar-archive.test.cts"
  - "tests/maintainer/pack-audit.test.cts"
  - "package.json"
autonomous: true
requirements: [BRAND-01, BRAND-02]
risk_level: high
reversibility: reversible
must_haves:
  truths:
    - "D-13：immutable Git paths/blobs与actual tgz member paths/bodies调用Plan06同一matcher。"
    - "BRAND-02：audit:brand为mandatory compiled CLI，exact args/JSON/exit contract在下游前直接测试。"
    - "tar parser拒绝traversal/link/special/duplicate/truncation/resource ambiguity且不extract。"
  artifacts:
    - path: "src/maintainer/tar-archive.cts"
      provides: "bounded non-extracting tar parser"
    - path: "src/maintainer/brand-audit.cts"
      provides: "Git/tar adapters and CLI main"
    - path: "package.json"
      provides: "mandatory audit:brand/test scripts"
  key_links:
    - from: "scanGitTree"
      to: "git ls-tree/cat-file"
      via: "one immutable subject/OID stream"
    - from: "scanTarball"
      to: "readTarArchive"
      via: "every member name/body"
    - from: "audit:brand"
      to: "dist/maintainer/brand-audit.cjs"
      via: "single safe JSON CLI"
  prohibitions:
    - "BRAND-02：不得把worktree grep、dry-run manifest或binary skip当作Git/tgz authority。"
  flagged_assumptions:
    - requirement: BRAND-02
      category: unclassified
      statement: "Unclassified BRAND-02 row明确由dry-run预测inventory加actual tgz authority的双层证明关闭；本计划提供适配器，Plan09编排计数。"
---

<objective>
**As a** release verifier，**I want to** 通过一个mandatory CLI扫描immutable Git与tgz，**so that** 下游门禁调用真实且安全的公共contract。

Purpose: 实现Git/tar inventory adapters、shared parser与required CLI。
Output: scanGitTree/scanTarball/readTarArchive与audit:brand scripts。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md


No external API integration: Phase 04.2 renames and audits existing local/package projections without adding an API capability surface.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 immutable Git adapter 与 mandatory CLI</name>
  <files>src/maintainer/brand-audit.cts, tests/maintainer/brand-audit.test.cts, package.json</files>
  <read_first>
    - Plan06 brand-audit core/tests
    - src/maintainer/retirement-audit.cts — ls-tree analog
    - src/maintainer/pre-commit.cts — bounded subprocess pattern
    - package.json — exact current script/compiled file surfaces
  </read_first>
  <behavior>
    - scanGitTree reads one resolved subject using ls-tree -r -z --full-tree and one cat-file --batch.
    - CLI syntax is audit:brand -- git --subject SHA with optional repeated --include paths; stdout one safe JSON, finding/error nonzero.
  </behavior>
  <action>实现mode/type/OID/path Buffer解析、blob batch验证、limits/timeouts与worktree-independent proof。CLI `main(argv)` exact result fields `schemaVersion,scope,subject,tree,scannedCount,findingCount,findings`;不输出exact path。package `audit:brand`固定为`node dist/maintainer/brand-audit.cjs`，`test:brand-audit`固定compiled test。测试必须direct spawn compiled CLI而非只调用function。</action>
  <acceptance_criteria>
    - dirty index/worktree/untracked canary不改变同subject result。
    - symlink/submodule/special/malformed/oversize fail closed且输出safe。
    - direct CLI test验证args、one JSON、exit codes、raw-path/secret absence。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:brand-audit &amp;&amp; node --test --test-name-pattern="CLI emits one safe JSON document" dist-tests/maintainer/brand-audit.test.cjs</automated></verify>
  <done>mandatory audit:brand在任何下游调用前有直接compiled contract proof。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 shared tar parser 与 actual tgz adapter</name>
  <files>src/maintainer/tar-archive.cts, src/maintainer/pack-audit.cts, src/maintainer/brand-audit.cts, tests/maintainer/tar-archive.test.cts, tests/maintainer/pack-audit.test.cts, tests/maintainer/brand-audit.test.cts, package.json</files>
  <read_first>
    - src/maintainer/pack-audit.cts — private tar reader/inventory analog
    - Plan06 matcher and Task1 Git adapter
    - tests/maintainer/pack-audit.test.cts — malformed archive fixtures
  </read_first>
  <behavior>
    - readTarArchive never extracts and accepts only regular files/directories under package/.
    - scanTarball verifies expected SHA then scans every path/body; unsupported content fails closed.
  </behavior>
  <action>提取`TarArchiveError/Limits/Entry/readTarArchive`，验证gzip cap、header/checksum/type/size/padding/root/safe path/duplicates；拒绝links/devices/FIFO/PAX ambiguity。pack-audit复用shared parser。添加`test:tar-archive`，ordinary CI只通过npm test运行unit tests，不调用live audit。</action>
  <acceptance_criteria>
    - traversal、absolute、duplicate、link/special、truncation、checksum与limit fixtures全部stable fail。
    - pack audit与brand scan无第二tar parser/shell tar。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:tar-archive &amp;&amp; npm run test:brand-audit &amp;&amp; npm run test:pack</automated></verify>
  <done>Git/tgz共享matcher且tgz由一个安全parser覆盖。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical/Git/npm/tar bytes → parser/generator | untrusted paths, encodings, archive entries and output cross into trusted metadata |
| trusted metadata → package/host/evidence | a false or leaking verdict can become public assurance |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-07-01 | Tampering/Elevation | path/tar traversal, links and special entries | high | mitigate | component containment, no-follow, regular-file allow-list, no extraction and adversarial tests |
| T-04.2-07-02 | Denial of service | oversized/binary/blob/member input | high | mitigate | fixed archive/blob/member/count/path/output/time limits and fail-closed decoding |
| T-04.2-07-03 | Tampering | Unicode/case/separator bypass | high | mitigate | shared NFKC/lower/separator core with synthesized variants and negative controls |
| T-04.2-07-04 | Spoofing/Information disclosure | subprocess/output injection | high | mitigate | direct argv, shell false, bounded JSON/binary protocols, no raw output reflection |
| T-04.2-07-05 | Tampering/Repudiation | scan/pack/smoke TOCTOU | high | mitigate | immutable OIDs and SHA/member-count-bound artifact reverified at every consumer |
| T-04.2-07-06 | Information disclosure | raw match, exact path, config or secret | high | mitigate | placeholder/token diagnostics, private remediation callback and secret canaries |
| T-04.2-07-07 | Tampering | external release mutation | high | mitigate | no commit/tag/publish/dist-tag/registry acquisition/refetch branch |
</threat_model>

## Artifacts this phase produces

- New symbols: `scanGitTree`, `scanTarball`, `main`, `TarArchiveError`, `TarArchiveLimits`, `TarArchiveEntry`, `readTarArchive`.
- Mandatory scripts: `audit:brand`, `test:brand-audit`, `test:tar-archive`; compiled entry `dist/maintainer/brand-audit.cjs` added to package files if shipped.
- Git/tar results retain only safe metadata; no state fields.

<verification>
Quick gate: direct compiled CLI plus tar adversarial suites

Plan gate: `npm run build && npm run test:brand-audit && npm run test:tar-archive && npm run test:pack`.
</verification>

<success_criteria>
- Mandatory CLI contract is executable and correctly ordered.
- Git and actual tgz paths/bodies use one matcher and safe diagnostics.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-07-SUMMARY.md` when done using neutral current terminology.
</output>

---
phase: 04.2-public-debranding
plan: "14"
type: execute
wave: 15
depends_on: ["04.2-13"]
files_modified:
  - "AGENTS.md"
  - "README.md"
  - ".planning/PROJECT.md"
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/codebase/ARCHITECTURE.md"
  - ".planning/codebase/CONCERNS.md"
  - ".planning/codebase/CONVENTIONS.md"
  - ".planning/codebase/INTEGRATIONS.md"
  - ".planning/codebase/STACK.md"
  - ".planning/codebase/STRUCTURE.md"
  - ".planning/codebase/TESTING.md"
autonomous: true
requirements: [BRAND-01, BRAND-03, BRAND-04]
risk_level: high
reversibility: costly
must_haves:
  truths:
    - "D-02/D-14：本计划只处理显式路径清单；每批均从不可变提交验证零命中，且公开输出不包含原始路径或匹配文本。"
    - "每批编辑前记录私有脏路径与 hunk 基线；重叠则进入阻断检查点，未重叠才精确暂存并提交。"
    - "root contracts继续声明五宿主、local guide、0.3.0 readiness-only与Phase05–08边界。"
  artifacts:
    - path: ".planning/codebase/"
      provides: "current neutral architecture maps"
  key_links:
    - from: "explicit path list"
      to: "captureScrubBaseline/assertScrubBaselinePreserved"
      via: "pre-edit private baseline and post-commit preservation"
    - from: "batch commit"
      to: "audit:brand git --subject HEAD --include"
      via: "immutable scoped zero-hit"
  prohibitions:
    - "不得用scanner动态返回path扩大batch，也不得覆盖、吸收、回退或提交pre-existing unrelated hunks。"
  flagged_assumptions:
    - requirement: BRAND-01
      category: encoding
      statement: "文件内容相等/长度由Plan06 strict decoder与NFKC matcher定义；本批不自创文本等价规则。"
---

<objective>
**As a** dirty worktree维护者，**I want to** 分批中性化root contracts and codebase maps，**so that** zero-hit收敛可审计且不损伤已有工作。

Purpose: 修正stale maps并中性化root authority。
Output: neutral current project/contracts/maps。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: root contracts and codebase maps batch 1</name>
  <files>AGENTS.md, README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - each exact file listed in this task — read current bytes and preserve non-matching semantics
    - src/maintainer/scrub-baseline.cts — private dirty-hunk baseline/checkpoint contract
    - src/maintainer/brand-audit.cts — immutable scanner and safe diagnostics
    - .planning/phases/04.2-public-debranding/04.2-CONTEXT.md — D-01–D-18
    - current source/adapters/tests — authority for stale map corrections
  </read_first>
  <action>在任何编辑前调用 `captureScrubBaseline`记录本task显式paths的private path+hunk baseline；若任一target与pre-existing user hunk重叠，必须在write/stage前动态发出blocking checkpoint并由用户在review panel处理，禁止overwrite/absorb/revert。只对列出的exact files做中性语义改写，保持D-03允许identity、历史事实、R/S/E结论、receipt digest与phase boundaries；不用global blind replacement，不编辑untracked drafts。只stage本task paths，创建一个普通task commit，随后用immutable HEAD scoped audit与`assertScrubBaselinePreserved`验证。</action>
  <acceptance_criteria>
    - 本task explicit destination paths在committed HEAD为zero findings/zero unsupported。
    - unrelated dirty path+hunk/index baseline逐项不变；无未列出的staged path。
    - docs描述与live five-host architecture一致，不恢复Python/marketplace/migration。
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:scrub-baseline &amp;&amp; npm run audit:brand -- git --subject HEAD --include "AGENTS.md" --include "README.md" --include ".planning/PROJECT.md" --include ".planning/REQUIREMENTS.md" --include ".planning/ROADMAP.md" --include ".planning/STATE.md" --include ".planning/codebase/ARCHITECTURE.md" &amp;&amp; git diff --check</automated>
  </verify>
  <done>root contracts/maps batch 1已独立提交且未触碰用户工作。</done>
</task>

<task type="auto">
  <name>Task 2: root contracts and codebase maps batch 2</name>
  <files>.planning/codebase/CONCERNS.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - each exact file listed in this task — read current bytes and preserve non-matching semantics
    - src/maintainer/scrub-baseline.cts — private dirty-hunk baseline/checkpoint contract
    - src/maintainer/brand-audit.cts — immutable scanner and safe diagnostics
    - .planning/phases/04.2-public-debranding/04.2-CONTEXT.md — D-01–D-18
    - current source/adapters/tests — authority for stale map corrections
  </read_first>
  <action>在任何编辑前调用 `captureScrubBaseline`记录本task显式paths的private path+hunk baseline；若任一target与pre-existing user hunk重叠，必须在write/stage前动态发出blocking checkpoint并由用户在review panel处理，禁止overwrite/absorb/revert。只对列出的exact files做中性语义改写，保持D-03允许identity、历史事实、R/S/E结论、receipt digest与phase boundaries；不用global blind replacement，不编辑untracked drafts。只stage本task paths，创建一个普通task commit，随后用immutable HEAD scoped audit与`assertScrubBaselinePreserved`验证。</action>
  <acceptance_criteria>
    - 本task explicit destination paths在committed HEAD为zero findings/zero unsupported。
    - unrelated dirty path+hunk/index baseline逐项不变；无未列出的staged path。
    - docs描述与live five-host architecture一致，不恢复Python/marketplace/migration。
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:scrub-baseline &amp;&amp; npm run audit:brand -- git --subject HEAD --include ".planning/codebase/CONCERNS.md" --include ".planning/codebase/CONVENTIONS.md" --include ".planning/codebase/INTEGRATIONS.md" --include ".planning/codebase/STACK.md" --include ".planning/codebase/STRUCTURE.md" --include ".planning/codebase/TESTING.md" &amp;&amp; git diff --check</automated>
  </verify>
  <done>root contracts/maps batch 2已独立提交且未触碰用户工作。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty worktree → explicit scrub edits | user hunks and matched paths coexist |
| Git object/path/content → scanner diagnostics | path, Unicode, binary and secret-bearing content is untrusted |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-14-01 | Tampering/Elevation | path traversal/symlink/special target | high | mitigate | exact listed paths, Git mode validation, no-follow and no dynamic inventory expansion |
| T-04.2-14-02 | Denial of service | oversized/binary input | high | mitigate | Plan06 limits/strict decoder and fail-closed scoped audit |
| T-04.2-14-03 | Tampering | Unicode bypass | high | mitigate | same NFKC closed matcher after each commit |
| T-04.2-14-04 | Spoofing/Information disclosure | subprocess/output injection | high | mitigate | direct argv and safe token diagnostics; raw diff stays private |
| T-04.2-14-05 | Tampering/Repudiation | edit/scan TOCTOU | high | mitigate | task commit first, then scan immutable HEAD OIDs |
| T-04.2-14-06 | Information disclosure | exact path/match/secret | high | mitigate | no raw serialized findings and private remediation channel |
| T-04.2-14-07 | Tampering | release mutation | high | mitigate | ordinary scoped commits only; no tag/publish/dist-tag/registry actions |
</threat_model>

## Artifacts this phase produces

- Modified/moved exact paths are only the frontmatter inventory above; no new runtime symbol, CLI, state or receipt field.
- Each task creates one ordinary commit and a private ephemeral baseline; no tracked baseline artifact.
- Historical/current semantics remain, expressed with neutral current terminology.

<verification>
Quick gate: per-task scoped immutable audit.

Plan gate: `npm run build && npm run test:scrub-baseline && npm run test:brand-audit && git diff --check`.
</verification>

<success_criteria>
- All explicit paths are neutral on immutable HEAD.
- User dirty work is preserved exactly.
- No external release or registry action occurs.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-14-SUMMARY.md` using neutral current terminology only.
</output>

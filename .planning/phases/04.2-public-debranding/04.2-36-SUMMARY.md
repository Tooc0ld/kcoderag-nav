---
phase: 04.2-public-debranding
plan: "36"
subsystem: release-assurance
tags: [github-actions, node24, artifact-upload, readiness, tdd]

requires:
  - phase: 04.2-30
    provides: frozen candidate workflow and one-tgz readiness producer
  - phase: 04.2-35
    provides: source/generated package identity repair used by the retired candidate
provides:
  - candidate-local Node 24 action handler for the existing package-upload producer
  - exact missing-runtime zero-output executable oracle for shell-handler regression
  - process-env artifact authentication and no-consume failure regressions
affects: [04.2-37, readiness-workflow, github-artifact-upload]

actuals:
  tokens: 3724
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - GitHub private artifact runtime inputs are consumed only inside a local JavaScript action handler
    - the action entrypoint delegates once in-process to the existing compiled readiness producer

key-files:
  created:
    - .github/actions/readiness-upload/action.yml
    - .github/actions/readiness-upload/index.cjs
    - .planning/phases/04.2-public-debranding/04.2-36-SUMMARY.md
  modified:
    - .github/workflows/readiness.yml
    - tests/maintainer/readiness-workflow.test.cts
    - tests/maintainer/github-artifact-upload.test.cts

key-decisions:
  - "Run package-upload through a candidate-owned node24 action so the runner supplies job-scoped artifact inputs without workflow forwarding."
  - "Keep authentication, lease consumption, create/upload/finalize, pack, smoke, and output behavior in the existing compiled maintainer runtime."
  - "Retire candidate f387da46d5ebcbeaeb2a1aa0218623a2860422c3 and failed run 33161895992 permanently with zero receipts; Plan 37 must freeze a new subject."

patterns-established:
  - "Thin action adapter: manifest owns handler selection and output declaration; index.cjs owns only one in-process main([\"package-upload\"]) call."
  - "Auth-boundary oracle: missing runner inputs must fail with artifact_auth_invalid before HTTPS fetch, lease consumption, or GITHUB_OUTPUT mutation."

requirements-completed: [BRAND-02, BRAND-04]

coverage:
  - id: D1
    description: "The package producer executes in a candidate-local Node action handler while preserving one-tgz and four-lane workflow topology."
    requirement: BRAND-02
    verification:
      - kind: integration
        ref: "tests/maintainer/readiness-workflow.test.cts#package producer resolves through a candidate-local JavaScript action handler"
        status: pass
      - kind: integration
        ref: "npm run ci:local"
        status: pass
    human_judgment: false
  - id: D2
    description: "Runner authentication is read from process.env, and absent inputs fail before network, lease consumption, or workflow output."
    requirement: BRAND-04
    verification:
      - kind: unit
        ref: "tests/maintainer/github-artifact-upload.test.cts#process env authentication and missing authentication cases"
        status: pass
      - kind: integration
        ref: "tests/maintainer/readiness-workflow.test.cts#missing artifact runtime inputs fail before a request or workflow output is produced"
        status: pass
    human_judgment: false

duration: 18min
completed: 2026-08-28
status: complete
---

# Phase 04.2 Plan 36: Readiness Node Action Handler Summary

**The exact readiness package producer now runs inside a candidate-local Node 24 action handler that receives runner artifact inputs while preserving the existing strict uploader and one-tgz workflow.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-08-28T10:31:14Z
- **Completed:** 2026-08-28T10:49:33Z
- **Tasks:** 2
- **Files modified:** 5 workflow/action/test files plus this Summary

## Accomplishments

- Replaced the package job's shell `run:` uploader invocation with one local `uses: ./.github/actions/readiness-upload` step while retaining its ID, provenance environment, upstream build order, five job outputs, four lanes, and final verifier.
- Added a closed action manifest with exactly five outputs and `runs.using: node24`; its CJS entrypoint requires the existing compiled readiness module, calls `main(["package-upload"])` exactly once in-process, and propagates the returned exit status.
- Locked the real shell-handler failure as an executable oracle: valid push provenance plus absent artifact runtime inputs exits 1 with `artifact_auth_invalid`, writes zero output bytes, and performs no HTTPS artifact request.
- Proved the existing uploader reads runner-shaped authentication from `process.env`, preserves create/raw-PUT/finalize order, and rejects missing authentication before fetch or lease consumption without changing uploader source.

## Task Commits

1. **Task 1 RED: Expose the shell handler artifact-auth gap** — `4630fbd` (test)
2. **Task 1 GREEN: Run the producer in a local Node action** — `2d17b3d` (fix)
3. **Task 2: Lock runner auth and workflow inventory** — `cb0f0ca` (test)

## RED/GREEN Evidence

- **Task 1 RED:** the exact missing-runtime executable oracle passed, while both new local-handler assertions failed against the existing shell step. Focused result: 1/3 pass and 2/3 expected failures. Test-only commit: `4630fbd`.
- **Task 1 GREEN:** the same build and focused command passed 3/3 after adding only the local manifest, thin entrypoint, and package-step `uses` wiring. Implementation commit: `2d17b3d`.
- **Task 2 characterization:** both process-env behaviors passed directly against the existing strict uploader, as the plan anticipated; no artificial failing expectation was introduced and `src/maintainer/github-artifact-upload.cts` remained unchanged. The strengthened uploader suite passed 5/5 and workflow suite passed 8/8. Test commit: `cb0f0ca`.

## Verification Evidence

- Fresh focused verification passed: uploader 5/5 and readiness workflow 8/8.
- `npm run ci:local`: aggregate tests 413/413, deterministic generation clean, pack audit 17/17, and required five-host smoke 12/12.
- `npm run docs:check`: six canonical documents passed.
- `npm run guide:check`: the repository-owned guide passed all 13 required topics.
- `npm run audit:retirement`: post mode passed with zero script, source, and test remnants.
- `npm run audit:brand -- git --subject HEAD`: immutable source/test subject `cb0f0ca4d2a9aef6c73a74f7099f0ec7fbf18120` scanned 488 Git blobs with zero findings.
- `git diff --check` passed. No `gh`, push, dispatch, registry, tag, publish, release, or dist-tag command was called.

## Decisions Made

- The workflow selects the runner's Node action handler through a checked-in relative `uses` path; it never constructs, forwards, prints, or persists the private artifact URL or token.
- The action entrypoint contains no subprocess, path reconstruction, tgz handling, pack/smoke logic, uploader implementation, or authentication mapping. All producer behavior remains in `dist/maintainer/readiness-workflow.cjs`.
- External actions retain their exact full commit OIDs, top-level permissions remain exactly `contents: read`, and the five package outputs remain unchanged.
- Candidate `f387da46d5ebcbeaeb2a1aa0218623a2860422c3` and run `33161895992` remain failed, retired, and zero-receipt evidence. This plan does not authorize their rerun or reuse.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first RED-only request sentinel intercepted the local packaged-host smoke's own fetch path and produced `platform_smoke_failed`. Before the RED commit, the sentinel was narrowed to record HTTPS artifact-service traffic while allowing loopback smoke; the exact oracle then produced the required `artifact_auth_invalid` result.

## Known Stubs

None. Empty arrays/objects found by the mechanical scan are bounded test accumulators and process-state fixtures, not deferred product behavior.

## User Setup Required

None - no external service configuration required.

## Threat Surface Review

- The planned GitHub Runner handler trust boundary changed; no new endpoint, credential forwarding path, package dependency, permission, release surface, or schema was added.
- Existing trusted results-origin, token scope, bounded response, timeout, same-process lease, create/upload/finalize, and best-effort partial-cleanup checks are unchanged.
- The action handler is candidate-owned and its exact workflow blob remains bound by the existing lane receipts; a future readiness run must use a newly frozen candidate.

## Next Phase Readiness

- Plan 37 can now freeze a new post-handler candidate and must not reuse or rerun the retired f387/run 33161895992 pair.
- Platform lanes remain unclaimed here. This plan produced no receipt, remote run, ref mutation, tag, release, publish, or registry evidence.

## Self-Check: PASSED

- Confirmed this Summary exists and commits `4630fbd`, `2d17b3d`, and `cb0f0ca` resolve.
- Confirmed the plan diff contains exactly the five declared workflow/action/test files and no tracked deletion.
- Confirmed `src/maintainer/github-artifact-upload.cts`, Plans 31/32, package/pack/tar/receipt sources, and unrelated dirty/untracked work are unchanged.
- Confirmed coverage metadata classifies both deliverables as fully automated PASS, with no stub, skipped test, unrun verification, or unplanned threat surface.

---
*Phase: 04.2-public-debranding*
*Completed: 2026-08-28*

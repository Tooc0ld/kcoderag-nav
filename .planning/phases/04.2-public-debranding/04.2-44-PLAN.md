---
phase: 04.2-public-debranding
plan: "44"
type: execute
wave: 44
depends_on: ["04.2-43"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: costly
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "After the 04.2-43 Summary commit, one distinct immutable candidate contains the current fixes and passes the tracked zero-hit gate before any remote mutation."
    - "BRAND-02: the dry-run package inventory and one actual 0.3.0 tgz are checked by the same case, Unicode, separator, and representation-normalizing policy and both are zero-hit."
    - "BRAND-03: source and the exact tgz preserve five-host behavior and parity with no compatibility aliases; every projected endpoint is exactly /mcp and mcp is never followed by a slash."
    - "The dedicated remote ref moves by one plain non-force fast-forward from exactly 0bf617b7becf65b3d336027d895183ced313ad4c, then one Windows/Linux Node 22/24 four-lane run consumes the same tgz."
    - "BRAND-04: candidate, tree, workflow, tgz, artifact, and lane identities are recorded as secret-free metadata; tag, publish, refetch, unpublish, release, dist-tag, and history-rewrite actions do not occur."
  artifacts:
    - path: "new immutable Git candidate"
      provides: "post-fix 0.3.0 source authority"
    - path: "one exact 0.3.0 tgz"
      provides: "single package authority shared by local and hosted checks"
    - path: ".planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md"
      provides: "candidate, ref transition, run, artifact, lane, and gate metadata for Plan 45"
  key_links:
    - from: "04.2-43-SUMMARY.md"
      to: "new immutable candidate"
      via: "tree-identical candidate freeze after the zero-hit fix is committed"
    - from: "dry-run package inventory"
      to: "one actual tgz"
      via: "same normalized identity policy and exact member-set comparison"
    - from: "refs/heads/readiness/04.2-candidate"
      to: "hosted readiness run"
      via: "expected-predecessor plain fast-forward and exact candidate workflow"
    - from: "producer artifact"
      to: "four hosted lanes"
      via: "one artifact ID, one tgz digest, no lane-local repack"
---

<objective>
Freeze and fully exercise one post-fix immutable 0.3.0 candidate, one exact tgz, and one current four-lane hosted run.

Purpose: close BRAND-02 package parity, BRAND-03 behavior/no-alias parity, and the external evidence portion of BRAND-04 without broadening release authority.
Output: immutable candidate, exact tgz evidence, dedicated-ref transition, four hosted lane results, and 04.2-44-SUMMARY.md.
</objective>

<scope>
In scope: exact candidate freeze; all current local source, brand, package, full-CI, docs, local-guide, security, pack, five-host, and endpoint gates; one tgz; one exact-ref fast-forward; one Windows/Linux Node 22/24 readiness run; metadata-only evidence.

Non-goals: no tag, publish, registry refetch, unpublish, release, dist-tag mutation, force push, alternate ref, history rewrite, package rebuild in hosted lanes, compatibility alias, Phase 05-08 claim, or true-host admission claim.
</scope>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-43-SUMMARY.md
@.planning/phases/04.2-public-debranding/COVERAGE.md
@package.json
@src/maintainer/release-readiness.cts
@src/maintainer/brand-audit.cts
@src/maintainer/readiness-seal.cts
@src/core/mcp-endpoint.cts
@.github/workflows/readiness.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the post-fix immutable candidate without absorbing dirty work</name>
  <precondition>04.2-43-SUMMARY.md is committed; the index is empty; read-only ls-remote reports refs/heads/readiness/04.2-candidate exactly at 0bf617b7becf65b3d336027d895183ced313ad4c; the user-confirmed continuation remains the sole authority for the later narrow ref update.</precondition>
  <reversibility rating="costly">Once the candidate drives hosted evidence, its subject and tree become immutable receipt identities.</reversibility>
  <files></files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-43-SUMMARY.md — zero-hit fix commit and private dirty-state handoff
    - src/maintainer/scrub-baseline.cts — path and hunk baseline contract
    - src/maintainer/release-readiness.cts — exact candidate product snapshot contract
    - .github/workflows/readiness.yml — candidate-owned workflow identity
  </read_first>
  <action>Capture private path, hunk, untracked, and index baselines; require no staged entry and stop on overlap. Prove the Plan 43 Summary commit is HEAD, the expected predecessor is an ancestor, and the tracked candidate surface is zero-hit. Create one allow-empty, tree-identical candidate commit with no file staging. Verify its parent is the Summary commit, its tree equals its parent tree, it is a distinct strict descendant of the expected predecessor, and every current source/generated/workflow blob is addressable from the candidate. Restore neither files nor index because no user bytes may be touched; compare the private baseline byte-for-byte after freeze.</action>
  <acceptance_criteria>
    - Candidate parent is the committed Plan 43 Summary, candidate tree equals parent tree, and candidate OID differs from the expected predecessor.
    - The index stays empty and dirty/untracked path plus hunk baselines are identical before and after candidate creation.
    - Any predecessor mismatch, brand finding, staged entry, overlap, missing blob, or baseline drift blocks candidate use and performs no remote action.
  </acceptance_criteria>
  <verify><automated>git diff --cached --exit-code &amp;&amp; git merge-base --is-ancestor 0bf617b7becf65b3d336027d895183ced313ad4c &lt;candidate-sha&gt; &amp;&amp; npm run audit:brand -- git --subject &lt;candidate-sha&gt; &amp;&amp; node -e "const{execFileSync}=require('node:child_process');const s=process.argv[1],g=x=&gt;execFileSync('git',['rev-parse',x],{encoding:'utf8'}).trim();if(g(s+'^{tree}')!==g(s+'^1^{tree}'))process.exit(1)" &lt;candidate-sha&gt;</automated></verify>
  <done>One clean tree-identical post-fix candidate is frozen and the user's pre-existing worktree/index state is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove source, dry-run inventory, and one actual tgz with current gates</name>
  <files></files>
  <read_first>
    - package.json — current gate commands and 0.3.0 pack configuration
    - src/maintainer/brand-audit.cts — one policy for Git, dry-run inventory, and tar members/bodies
    - src/maintainer/release-readiness.cts — source/generated/package identity partition
    - src/core/mcp-endpoint.cts — exact /mcp normalization
    - .planning/phases/04.2-public-debranding/COVERAGE.md — five-host and explicit opt-out boundary
  </read_first>
  <action>Use a private temporary checkout of the exact candidate and a private pack destination. Run the candidate-declared current source, zero-hit, deterministic generation, dependency/package, full CI, docs, repository-local experience-guide ownership, security, pack, five-host lifecycle/smoke, readiness, and endpoint regression gates. Capture `npm pack --dry-run --json` as one private JSON document, require exactly one package record with a non-empty files array, and pass every normalized dry-run path through the compiled shared `scanBrandText` policy with `tar_path` scope. Create exactly one actual 0.3.0 tgz with `npm pack --json --pack-destination`, capture that command's files inventory, pass its paths through the same shared policy, and run the shared tar audit over actual member names and bodies. Sort both JSON path arrays and fail on any count or exact-path mismatch before accepting the tgz; require zero case, Unicode, separator, width, or encoding-variant findings on both representations. Prove source and tgz contain only the canonical capability names with no aliases, all five host projections preserve behavior, and every MCP endpoint is exactly `/mcp`; reject any occurrence where `mcp` is immediately followed by `/`, including generated configuration and tar members. Record command versions, candidate/tree/product/workflow/blob IDs, tgz filename, digest, size, member digest, and gate outcomes without file bodies, URLs, headers, tokens, or credentials.</action>
  <acceptance_criteria>
    - All current local source/brand/package/full-CI/docs/local-guide/security/pack/five-host/readiness gates pass against the exact candidate.
    - BRAND-02 dry-run JSON and the actual tgz pack JSON are audited by the same shared path policy; sorted counts and exact paths are equal, while the tar audit additionally proves actual member names and bodies are zero-hit for Unicode and separator variants.
    - The automated inventory comparison exits nonzero on a missing, extra, renamed, duplicate-count, or reordered-after-normalization mismatch; exactly one dry-run record and one actual tgz are accepted.
    - BRAND-03 source/tgz five-host projections have behavior parity, no aliases, exact `/mcp`, and no `mcp` followed by `/`.
    - Only one tgz is created; no registry, remote ref, workflow, tag, release, or publication action occurs in this task.
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run generate:check &amp;&amp; npm run audit:brand -- git --subject &lt;candidate-sha&gt; &amp;&amp; npm test &amp;&amp; node --test dist-tests/core/mcp-endpoint.test.cjs &amp;&amp; npm run test:readiness-workflow &amp;&amp; npm run test:readiness-seal &amp;&amp; npm run test:dependency-audit &amp;&amp; npm pack --dry-run --json &gt; &lt;dry-run-json&gt; &amp;&amp; npm pack --json --pack-destination &lt;private-pack-dir&gt; &gt; &lt;actual-pack-json&gt; &amp;&amp; node -e "const fs=require('node:fs'),a=require('./dist/maintainer/brand-audit.cjs');const inv=p=&gt;{const j=JSON.parse(fs.readFileSync(p,'utf8'));if(!Array.isArray(j)||j.length!==1||!Array.isArray(j[0].files)||j[0].files.length===0)process.exit(1);return j[0].files.map(x=&gt;x.path).sort()};const d=inv(process.argv[1]),t=inv(process.argv[2]);if(d.length!==t.length||d.some((p,i)=&gt;p!==t[i]))process.exit(1);for(const p of d){const v=a.scanBrandText(p,{scope:'tar_path',exactPath:p});if(v.findingCount!==0)process.exit(1)}" &lt;dry-run-json&gt; &lt;actual-pack-json&gt; &amp;&amp; npm run audit:brand -- tar --archive &lt;exact-tgz&gt;</automated></verify>
  <done>The exact candidate and one tgz pass the complete current local gate set, same-policy package audit, five-host parity, and endpoint invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Fast-forward the exact ref once and obtain four-lane evidence</name>
  <precondition>Tasks 1-2 pass; the exact candidate and tgz identities are fixed; a fresh read-only remote check still reports the expected predecessor; user confirmation authorizes only this scoped continuation.</precondition>
  <reversibility rating="costly">The plain push and hosted run create durable remote evidence, but are limited to the dedicated readiness ref and candidate.</reversibility>
  <files></files>
  <read_first>
    - .github/workflows/readiness.yml at the candidate — exact producer and four-lane topology
    - src/maintainer/release-readiness.cts — run, artifact, member, and lane validation
    - .planning/phases/04.2-public-debranding/04.2-43-SUMMARY.md — expected predecessor and authorization boundary
  </read_first>
  <action>Immediately before mutation, use read-only ls-remote and require the dedicated ref to equal 0bf617b7becf65b3d336027d895183ced313ad4c; prove the candidate is its strict descendant. Push only `<candidate-sha>:refs/heads/readiness/04.2-candidate` with ordinary non-force semantics and no other refspec. Resolve the single run whose head SHA and workflow blob equal the candidate. Require one producer artifact containing the exact locally verified tgz—same filename, bytes, digest, size, and member digest—and four successful consumers: Windows Node 22, Windows Node 24, Linux Node 22, Linux Node 24. Each lane must download by artifact identity, never repack, and report source/package/brand/five-host/endpoint results tied to that tgz. Write 04.2-44-SUMMARY.md with secret-free immutable metadata and explicit `NOT_RUN_BY_SCOPE` entries for tag, publish, registry refetch, unpublish, release, dist-tag, and history rewrite. Any ref race, run ambiguity, artifact substitution, lane omission, credential-shaped output, or unavailable metadata hard-blocks the Summary PASS claim.</action>
  <acceptance_criteria>
    - Exactly one dedicated ref changes by a plain non-force fast-forward from the expected predecessor to the candidate; no other ref changes.
    - One candidate-owned run and artifact match the exact local tgz, and all four required OS/Node lanes pass without repacking.
    - Summary metadata is sufficient for receipt construction, secret-free, and marks every prohibited external action `NOT_RUN_BY_SCOPE`.
  </acceptance_criteria>
  <verify><automated>git merge-base --is-ancestor 0bf617b7becf65b3d336027d895183ced313ad4c &lt;candidate-sha&gt; &amp;&amp; git ls-remote --refs origin refs/heads/readiness/04.2-candidate &amp;&amp; npm run build &amp;&amp; npm run test:readiness-workflow</automated></verify>
  <done>The exact candidate is the sole dedicated-ref head and one exact tgz has current PASS evidence in all four hosted lanes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty work/index → candidate | unrelated user bytes must not enter immutable release authority |
| candidate/dry-run → actual tgz | package members and normalized representations must not drift |
| local tgz → producer artifact → four lanes | artifact substitution or lane-local repack must be rejected |
| local expected ref → remote update | only the exact dedicated predecessor may fast-forward |
| hosted metadata → Summary | credential-shaped or incomplete evidence is untrusted |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-44-01 | Tampering | dirty/index absorption | high | mitigate | private exact baseline, empty-index gate, tree-identical candidate |
| T-04.2-44-02 | Tampering | representation or package substitution | high | mitigate | same normalization policy, exact inventories, tgz/member digests |
| T-04.2-44-03 | Spoofing/Tampering | remote ref integrity | high | mitigate | exact predecessor, ancestry proof, one plain non-force refspec |
| T-04.2-44-04 | Spoofing | run/artifact/lane substitution | high | mitigate | bind subject, tree, workflow blob, artifact ID, tgz digest, and four lane tuples |
| T-04.2-44-05 | Information disclosure | hosted logs and evidence | high | mitigate | metadata allowlist; reject URLs, headers, tokens, configuration bodies, and credential-shaped values |
| T-04.2-44-06 | Elevation/Tampering | release-scope expansion | high | mitigate | explicit scoped authorization and `NOT_RUN_BY_SCOPE` non-actions |
</threat_model>

<verification>
Candidate and tgz pass current local gates; the exact ref transition, run, artifact, and four lane tuples are independently resolvable; private dirty-state baseline remains unchanged.
</verification>

<success_criteria>
- BRAND-02 same-policy dry-run and actual-tgz audits are zero-hit with identical intended inventories.
- BRAND-03 behavior, five-host parity, no-alias, and exact `/mcp` invariants pass in source and the exact tgz.
- The dedicated ref fast-forwards once from the exact expected predecessor and one tgz passes Windows/Linux Node 22/24.
- Secret-free Summary metadata records all immutable identities and keeps external release actions `NOT_RUN_BY_SCOPE`.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md with candidate/tree/product/workflow/blob identities; dry-run and tgz inventories/digests; exact predecessor/ref transition; run/artifact/member/four-lane tuples; endpoint and gate outcomes; dirty-baseline proof; and scoped non-actions for Plan 45.
</output>

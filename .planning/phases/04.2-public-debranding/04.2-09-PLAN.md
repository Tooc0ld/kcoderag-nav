---
phase: 04.2-public-debranding
plan: "09"
type: execute
wave: 10
depends_on: ["04.2-08"]
files_modified:
  - "src/maintainer/release-readiness.cts"
  - "tests/maintainer/release-readiness.test.cts"
  - "src/maintainer/pack-audit.cts"
  - "tests/maintainer/pack-audit.test.cts"
  - "src/smoke/host-smoke.cts"
  - "tests/smoke/host-smoke.test.cts"
autonomous: true
requirements: [BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: reversible
must_haves:
  truths:
    - "RESOLVED：release-readiness.cts独占dry-run与one actual tgz生命周期；所有消费者只收injected artifact。"
    - "BRAND-02：formal invocation dryRunCount=1、actualPackCount=1；dry-run每个path与actual每个member path/body使用同一matcher。"
    - "D-17：one actual tgz被pack audit、tar scan、five-host packaged smoke复用；ZCode仅packaged。"
  artifacts:
    - path: "src/maintainer/release-readiness.cts"
      provides: "one-dry-run/one-actual-pack orchestrator and artifact owner"
    - path: "src/maintainer/pack-audit.cts"
      provides: "injected artifact validator"
    - path: "src/smoke/host-smoke.cts"
      provides: "injected same-tgz five-host smoke"
  key_links:
    - from: "release-readiness.cts"
      to: "npm pack dry-run and actual"
      via: "exact once each"
    - from: "CandidatePackageArtifact private lease"
      to: "pack/brand/smoke"
      via: "same immutable bytes plus metadata SHA/memberCount"
    - from: "runHostSmoke"
      to: "five host rows"
      via: "one provenance object"
  prohibitions:
    - "BRAND-04：任何consumer不得repack、使用remote packageSpec或把packaged ZCode升级成true-host PASS。"
  flagged_assumptions:
    - requirement: BRAND-02
      category: unclassified
      statement: "BRAND-02 unclassified edge由dual inventory与exact invocation counters显式关闭，dry-run仍不替代actual bytes。"
---

<objective>
**As a** readiness维护者，**I want to** 一个owner创建并注入同一actual tgz，**so that** pack、brand和五宿主不会证明不同bytes。

Purpose: 消费RESEARCH的one-tgz RESOLVED seam并加入dry-run predicted inventory。
Output: CandidatePackageArtifact、exact counters、same-provenance five-host smoke。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md

No external API integration: Phase 04.2 renames and audits existing local/package projections without adding an API capability surface.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 由 release-readiness 执行一次 dry-run 与一次 actual pack</name>
  <files>src/maintainer/release-readiness.cts, tests/maintainer/release-readiness.test.cts, src/maintainer/pack-audit.cts, tests/maintainer/pack-audit.test.cts</files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-RESEARCH.md — RESOLVED orchestrator ownership
    - src/maintainer/pack-audit.cts — existing pack/temp/parser seams
    - src/maintainer/brand-audit.cts — predicted path and actual tar scanners
    - tests/maintainer/pack-audit.test.cts — npm/tar spies
  </read_first>
  <behavior>
    - One invocation performs exactly one bounded npm pack --dry-run --ignore-scripts --json and scans every returned files[].path.
    - It then performs exactly one actual pack, validates private filename, snapshots bounded immutable bytes and returns one process-local leased artifact.
  </behavior>
  <action>新增`CandidatePackageArtifact`、`CandidatePackageArtifactLease`、`createCandidatePackageArtifact`与`withCandidatePackageBytes`于`release-readiness.cts`。先direct argv执行一次dry-run，bounded parse exact name/version/files array并将每个path送入Plan06 matcher；再在同private destination direct argv执行一次actual pack，验证JSON filename containment、realpath、SHA-256/memberCount/version，并在pack完成后立即读取一次bounded immutable tgz bytes。公开`CandidatePackageArtifact`只含name/version/sha256/memberCount/counters；private class slots持有canonicalTgzPath、open no-follow handle与immutable bytes，`toJSON`及structured serialization永不暴露它们。lease覆盖pack audit、tar scan、five-host smoke与Plan11 workflow upload全部消费者，最后一个consumer完成后一次dispose/cleanup；提前dispose、path swap或consumer越界fail closed。pack-audit改为通过`withCandidatePackageBytes(lease, consumer)`读取相同snapshot，formal path无internal pack。</action>
  <acceptance_criteria>
    - spies exact counters为1/1；dry-run零/重复/malformed path fail closed。
    - actual artifact public JSON无path/bytes/handle；private lease向全部消费者提供同一immutable byte identity。
    - dry-run与actual paths均用same matcher，但actual member bodies仍是content authority。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; node --test --test-name-pattern="dry-run count|actual pack count|injected artifact" dist-tests/maintainer/release-readiness.test.cjs</automated></verify>
  <done>dual inventory与one actual artifact seam成立。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将同一 artifact 注入 pack、tar 与 five-host smoke</name>
  <files>src/maintainer/pack-audit.cts, tests/maintainer/pack-audit.test.cts, src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts, src/maintainer/release-readiness.cts, tests/maintainer/release-readiness.test.cts</files>
  <read_first>
    - Task1 CandidatePackageArtifact contract
    - src/smoke/host-smoke.cts and tests — current packageSpec/provenance/five-host matrix
    - src/maintainer/brand-audit.cts — scanTarball
  </read_first>
  <behavior>
    - Pack audit, tar scan and all five hosts observe exact same immutable bytes/SHA/memberCount.
    - Navigation passes five rows; style only Claude 2.1.241 supported, other four zero-write unsupported.
  </behavior>
  <action>让`auditPackArtifact`、tar scanner与`runHostSmoke`只接受active `CandidatePackageArtifactLease`，通过`withCandidatePackageBytes`消费同一snapshot并核对metadata SHA/memberCount，绕过public Registry acquisition；不得从公开receipt重构path、重新pack或二次读取可替换文件。Plan11 workflow uploader也必须在同一lease生命周期内上传这些immutable bytes。一个run按codex/claude/cursor/opencode/zcode顺序，ZCode `layer:"packaged"`且无version/trust/admission PASS。Receipt只含metadata。</action>
  <acceptance_criteria>
    - consumer spies identity-equal且无repack；mutation后下一consumer fail closed。
    - all rows same provenance；four unsupported rows完整tree zero-write/navigationPreserved。
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:pack &amp;&amp; npm run test:brand-audit &amp;&amp; npm run test:smoke &amp;&amp; node --test dist-tests/maintainer/release-readiness.test.cjs</automated></verify>
  <done>同一actual tgz成为全部local/package validators的唯一authority。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable subject/npm output → candidate artifact | package prediction and actual bytes are untrusted bounded subprocess results |
| artifact/workflow receipt → readiness verdict | provenance, lane and support metadata become formal assurance |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-09-01 | Tampering/Elevation | traversal/link/special package or workflow artifact | high | mitigate | private destinations, realpath containment, regular-file tar policy and safe artifact extraction |
| T-04.2-09-02 | Denial of service | oversized/binary/archive/output | high | mitigate | fixed limits, bounded JSON/output/time and fail-closed unsupported content |
| T-04.2-09-03 | Tampering | Unicode/path prediction bypass | high | mitigate | same Plan06 matcher scans dry-run paths and actual member paths/bodies |
| T-04.2-09-04 | Spoofing/Information disclosure | npm/Git/workflow output injection | high | mitigate | shell-false argv, exact JSON schemas, no raw logs/config/credentials in evidence |
| T-04.2-09-05 | Tampering/Repudiation | dry-run/pack/smoke/lane TOCTOU | high | mitigate | exact invocation counts and SHA/memberCount revalidation at every consumer/lane |
| T-04.2-09-06 | Information disclosure | match/path/secret/receipt leakage | high | mitigate | safe tokens, metadata-only receipts and canary traversal |
| T-04.2-09-07 | Tampering/Repudiation | external release mutation | high | mitigate | no commit/tag/publish/dist-tag/registry package acquisition/refetch implementation; NOT_RUN_BY_SCOPE |
</threat_model>

## Artifacts this phase produces

- `release-readiness.cts` owns `CandidatePackageArtifact`, `CandidatePackageArtifactLease`, `createCandidatePackageArtifact`, `withCandidatePackageBytes`, counters and private cleanup.
- `pack-audit.cts` adds `auditPackArtifact`; `host-smoke.cts` adds injected artifact/provenance options.
- Public artifact metadata: name/version/sha256/memberCount/dryRunCount/actualPackCount; private class slots hold non-serializable canonicalTgzPath/open handle/immutable bytes until all consumers and workflow upload finish.
- No public lifecycle or release mutation surface.

<verification>
Quick gate: exact counter and injected-provenance tests

Plan gate: `npm run build && node --test dist-tests/maintainer/release-readiness.test.cjs && npm run test:pack && npm run test:brand-audit && npm run test:smoke`.
</verification>

<success_criteria>
- Formal artifact creation is one dry-run plus one actual pack.
- One actual tgz is reused by every required consumer.
</success_criteria>

<output>
Create `.planning/phases/04.2-public-debranding/04.2-09-SUMMARY.md` when done using neutral current terminology.
</output>

---
phase: 04.2-public-debranding
plan: "35"
type: execute
wave: 32
depends_on: ["04.2-34"]
files_modified:
  - "src/maintainer/release-readiness.cts"
  - "tests/maintainer/release-readiness.test.cts"
  - "src/maintainer/readiness-seal.cts"
  - "tests/maintainer/readiness-seal.test.cts"
autonomous: true
gap_closure: true
requirements: [BRAND-01, BRAND-02, BRAND-04]
risk_level: high
reversibility: costly
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-13、D-16：immutable Git candidate证明受跟踪的源码产品身份和完整package allow-list；由clean build生成且按设计不入Git的dist/**成员不再被错误要求为Git blobs。"
    - "D-13、BRAND-02：deterministic built output的成员名与bytes仍只由同一个actual tgz及现有exact pack/tar gates证明；本修复不跳过、不替代或缩窄archive inventory。"
    - "D-16：two-subject seal分别比较tracked source identities与declared generated-output inventory；任一source blob缺失/篡改或generated-output allow-list漂移都阻塞。"
    - "版本必须保持exact 0.3.0；one-actual-tgz、zero-hit、五宿主、semantic review、四lane和readiness-only边界全部保持。"
    - "旧candidate bb72868b10a2cb8e58e4e497961141c97c2aacb7因已知snapshot契约失败永久退役；Plan 30必须在本计划全部提交后冻结新的candidate，禁止复用旧OID。"
  artifacts:
    - path: "src/maintainer/release-readiness.cts"
      provides: "explicit immutable source-identity and generated-output inventory snapshot contract"
    - path: "src/maintainer/readiness-seal.cts"
      provides: "two-subject comparison over both snapshot partitions"
    - path: "tests/maintainer/release-readiness.test.cts"
      provides: "RED/GREEN proof for a valid candidate whose deterministic dist outputs are absent from Git"
    - path: "tests/maintainer/readiness-seal.test.cts"
      provides: "negative proof for source identity and generated-output inventory drift"
  key_links:
    - from: "candidate package.json files allow-list"
      to: "immutable Git source snapshot"
      via: "exact partition: package.json plus every non-dist declared member must be a Git blob"
    - from: "candidate package.json dist/** declarations"
      to: "one actual tgz"
      via: "exact declared generated path inventory retained in snapshot; existing pack/tar gates validate members and bytes"
    - from: "Plan 35 GREEN commits"
      to: "Plan 30 candidate freeze"
      via: "04.2-30 depends_on 04.2-35 and freezes a new post-fix immutable OID"
  prohibitions:
    - "不得把任意缺失package member解释为generated output；只有规范化后的dist/**声明进入generated partition。"
    - "不得改package.json files allow-list、pack-audit、tar parser、artifact lease、receipt schema或release workflow来规避失败。"
    - "不得把worktree dist bytes或dry-run manifest当作immutable Git source identity。"
    - "不得执行push、tag、publish、registry_refetch、npm view、remote packageSpec、release或dist-tag mutation。"
---

<objective>
修复 valid 0.3.0 candidate 的 readiness snapshot/seal 契约，使 Git 只证明不可变源码产品身份与精确声明，而 actual tgz 继续证明 deterministic built/archive inventory。

Purpose: 关闭 Plan 30 source-only checkpoint 暴露的 `package_inventory_invalid` 真缺口，同时不削弱 D-13/D-16 的 immutable candidate、one-actual-tgz 和 two-subject assurance。
Output: RED/GREEN 回归、显式 source/generated snapshot 分区、严格 final seal，以及供 Plan 30 重新冻结 candidate 的全门禁通过状态。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-CONTEXT.md
@.planning/phases/04.2-public-debranding/04.2-RESEARCH.md
@.planning/phases/04.2-public-debranding/04.2-PATTERNS.md
@.planning/phases/04.2-public-debranding/04.2-30-PLAN.md
@.planning/phases/04.2-public-debranding/04.2-34-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN 分离Git source identities与declared generated outputs</name>
  <precondition>Commit bb72868b10a2cb8e58e4e497961141c97c2aacb7 resolves locally and the current compiled readPackageProductSnapshot reports package_inventory_invalid for it; if either fact is false, stop and re-diagnose before editing.</precondition>
  <reversibility rating="costly">该snapshot digest被readiness receipt和final seal共同消费；回退会再次使合法clean-build candidate无法进入平台证据阶段。</reversibility>
  <files>src/maintainer/release-readiness.cts, tests/maintainer/release-readiness.test.cts</files>
  <read_first>
    - src/maintainer/release-readiness.cts — PackageProductSnapshot, readPackageProductSnapshot, runReleaseReadiness, validProductPath and immutable Git blob helpers
    - tests/maintainer/release-readiness.test.cts — readinessFixture, readinessOptions and current exact-0.3.0/semantic-review fixtures
    - package.json — read-only authority for the current exact files allow-list; 46 dist/** declarations are clean-build outputs and the remaining declarations are tracked blobs
    - src/maintainer/pack-audit.cts — read-only existing actual archive equality contract; do not edit it
    - commit bb72868b10a2cb8e58e4e497961141c97c2aacb7 — retired reproduction subject, never a future candidate
  </read_first>
  <behavior>
    - RED 1: a committed exact-0.3.0 fixture whose package allow-list declares dist/bin/kcoderag-nav.cjs but whose Git tree intentionally omits dist/** reproduces the current package_inventory_invalid failure.
    - GREEN 1: the same fixture reaches the existing BLOCKED-until-platform-lanes result and returns a deterministic source/generated snapshot without reading worktree dist bytes.
    - Negative 1: a declared non-dist package member missing from the immutable subject fails with package_inventory_invalid.
    - Negative 2: malformed, duplicate, traversal, absolute or non-dist-as-generated declarations remain rejected; version and local-guide requirements remain exact.
    - Contract: generatedPaths contains the exact sorted dist/** declarations, sourcePaths contains package.json plus every declared non-dist member, and oids contains every and only sourcePaths entry.
  </behavior>
  <action>先只改测试fixture/断言：让合法candidate模拟真实仓库拓扑（`package.json.files`声明编译输出，但Git subject没有该生成目录），运行聚焦测试并确认它在当前实现上以`package_inventory_invalid`失败，形成RED提交。随后修改`PackageProductSnapshot`与`readPackageProductSnapshot`：保留exact name/version/files/path/duplicate/local-guide校验；将规范化后的`dist/**`声明闭集记录为generatedPaths，将`package.json`加所有非dist声明记录为sourcePaths；所有sourcePaths必须从指定immutable subject解析为blob并进入OID/body digest，任何缺失均失败。snapshot digest必须域分隔地绑定完整声明顺序无关集合、source/generated分类、source OID与body digest，不能读取working tree或为缺失source提供fallback。`runReleaseReadiness`继续消费该snapshot，artifact SHA/memberCount、semantic review、checks、platform lanes和externalActions契约不变。不得修改`package.json`、pack/tar实现或receipt schema来使测试通过。完成GREEN提交后运行聚焦测试。</action>
  <verify>
    <automated>npm run build &amp;&amp; node --test --test-name-pattern="source product snapshot|exact 0.3.0 local readiness|semantic PASS" dist-tests/maintainer/release-readiness.test.cjs</automated>
  </verify>
  <done>合法clean-build candidate不再因46个untracked dist outputs被拒绝；每个tracked source identity和完整generated declaration inventory仍被immutable subject精确绑定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 强化two-subject seal并运行完整readiness回归</name>
  <reversibility rating="costly">seal是Phase 04.2完成声明的最后阻塞门；比较分区不完整会允许documentation child掩盖产品漂移。</reversibility>
  <files>src/maintainer/readiness-seal.cts, tests/maintainer/readiness-seal.test.cts</files>
  <read_first>
    - src/maintainer/readiness-seal.cts — runReadinessSeal candidate/final product comparison and final-child scope gate
    - tests/maintainer/readiness-seal.test.cts — documentation-child fixture and current product/local-guide drift cases
    - src/maintainer/release-readiness.cts — Task 1 PackageProductSnapshot sourcePaths/generatedPaths/oids contract
    - .planning/phases/04.2-public-debranding/04.2-30-PLAN.md — new-candidate freeze and source-only preflight contract after this gap plan
    - .planning/phases/04.2-public-debranding/04.2-31-PLAN.md — exact push authorization checkpoint that must remain unchanged
    - .planning/phases/04.2-public-debranding/04.2-32-PLAN.md — final receipt and post-output seal contract
  </read_first>
  <behavior>
    - A candidate/final pair with declared dist/** absent from both Git trees passes when the only child change is under .planning/.
    - Removing a required source identity from either immutable subject fails closed; changing a tracked source blob between candidate and final returns candidate_product_drift.
    - Adding, removing, renaming or reclassifying one declared generated-output path between candidate and final returns candidate_product_drift even when no generated blob exists in either Git tree.
    - A non-planning child change remains final_child_scope_invalid; receipt digest mismatch and final zero-hit failures remain blocking.
  </behavior>
  <action>先扩展seal fixtures，让dist output只存在于package allow-list而不进入Git，并添加source missing/tamper及generated-output inventory drift负例；确认至少一个新负例在未强化seal比较前失败。随后更新`runReadinessSeal`，显式要求candidate/final snapshot的digest、完整declared paths、sourcePaths、generatedPaths及每个source OID完全相等；只迭代sourcePaths读取OID，禁止依赖generated path的undefined OID相等。保留`changedPaths`仅允许`.planning/` documentation child、final whole-Git zero-hit、optional receipt digest绑定和no-write输出。最后运行focused tests与全部本地门禁，确认现有one-actual-tgz/pack/tar/smoke代码无需修改且Plan31的`AUTHORIZE_READINESS_REF_PUSH` checkpoint文本、权限边界和readiness-only约束未变。Plan35完成后明确记录bb72868b10a2cb8e58e4e497961141c97c2aacb7为retired failed input；不得把它写入任何新receipt或交给Plan31，执行流回到Plan30冻结新candidate。</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:release-readiness &amp;&amp; npm run test:readiness-seal &amp;&amp; npm run ci:local &amp;&amp; npm run docs:check &amp;&amp; npm run guide:check &amp;&amp; npm run audit:retirement &amp;&amp; npm run audit:brand -- git --subject HEAD &amp;&amp; git diff --check</automated>
  </verify>
  <done>source identity和generated-output declaration任一漂移均被seal阻塞，完整本地readiness门禁通过；旧candidate已退役，下一步只能由Plan30冻结新的post-fix candidate。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable package.json → snapshot partition | untrusted declared paths determine which identities must exist in Git versus the archive |
| immutable Git blobs → source digest | missing, substituted, oversized or malformed object data can spoof candidate identity |
| candidate subject → final documentation child | later commits may alter source identities or generated declarations while claiming documentation-only scope |
| actual tgz metadata → readiness evidence | archive identity must remain owned by the existing one-artifact lease and exact pack/tar validators |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-35-01 | Tampering/Elevation | source/generated path classification | high | mitigate | only normalized dist/** declarations may omit Git blobs; every non-dist declaration and package.json must resolve to an immutable blob |
| T-04.2-35-02 | Tampering/Repudiation | generated inventory drift | high | mitigate | digest and seal compare exact sorted declared/generated path sets independently of blob presence |
| T-04.2-35-03 | Tampering | source identity substitution | high | mitigate | bind source path, Git OID and SHA-256 body digest from the immutable subject and compare candidate/final OIDs |
| T-04.2-35-04 | Spoofing/Information disclosure | Git subprocess failure/output | high | mitigate | existing argv-only bounded Git helpers and stable path-free error codes; never serialize object bytes or command output |
| T-04.2-35-05 | Denial of service | oversized object or path inventory | high | mitigate | preserve bounded Git output, JSON size and validated finite allow-list parsing |
| T-04.2-35-06 | Tampering/Repudiation | source/archive authority confusion | high | mitigate | Git snapshot proves source identities/declarations; unchanged actual-tgz pack/tar gates prove generated member names and bytes |
| T-04.2-35-07 | Tampering | unintended release mutation | high | mitigate | no package/release/workflow edits and no push/tag/publish/refetch commands; external actions remain NOT_RUN_BY_SCOPE |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | exact 0.3.0 neutral candidate readiness against one actual tgz | Plan 35 repairs the candidate snapshot; Plans 30–32 retain freeze, platform and final evidence ownership |
| REQ | BRAND-01 current immutable source remains fully auditable | sourcePaths require exact Git blobs and final whole-tree zero-hit remains unchanged |
| REQ | BRAND-02 dry-run/actual archive inventory remains exact | generatedPaths preserve the allow-list while existing one-actual-tgz pack/tar gates remain authoritative |
| REQ | BRAND-03 behavior equivalence remains intact | no runtime/capability/Skill/host files change; full suite and five-host smoke gate remain required |
| REQ | BRAND-04 exact 0.3.0 readiness-only, no history/release mutation | old candidate is retired, Plan30 freezes a new OID, and external actions stay NOT_RUN_BY_SCOPE |
| CONTEXT | D-13 immutable Git plus actual tgz layered proof | explicit source/generated partition with no authority overlap |
| CONTEXT | D-14 secret-safe diagnostics | existing stable error codes and bounded metadata-only output retained |
| CONTEXT | D-15 formal-readiness-only hard audit | no ordinary CI workflow or pre-commit integration added |
| CONTEXT | D-16 frozen candidate, one tgz, two subjects, no release action | Plan35 → Plan30 → Plan31 → Plan32 dependency chain preserves the complete contract |
| CONTEXT | D-17 five-host packaged matrix and ZCode Phase 06 boundary | no smoke/host change; full existing gates remain required |
| CONTEXT | D-18 local guide authority | local guide remains a mandatory tracked source identity and package declaration |
| RESEARCH | immutable candidate source plus deterministic built/archive inventory | Task1 implements the missing authority separation without changing actual-tgz ownership |
| PATTERNS | stable errors, immutable Git objects, one artifact/many validators | both tasks reuse existing helpers and keep pack/tar/smoke seams unchanged |

Source audit result: COVERED. Deferred Phase 05–08 work, publication, registry verification and ZCode true-host admission remain out of scope.

<verification>
Focused RED/GREEN: release-readiness and readiness-seal compiled tests prove the exact topology and drift cases.

Plan gate: ci:local, docs/local-guide/retirement checks, immutable Git brand audit and diff hygiene all pass without registry or release mutation.
</verification>

<success_criteria>
- A valid candidate with 46 declared deterministic dist outputs absent from Git passes source snapshot preflight.
- Every non-dist declared member remains a required immutable Git blob; missing/tampered source identities fail closed.
- Exact generated-output declarations are independently digest-bound and seal-compared; inventory drift fails closed.
- Actual built bytes remain proven only through the unchanged one-actual-tgz pack/tar/smoke chain.
- bb72868b10a2cb8e58e4e497961141c97c2aacb7 is retired and cannot reach Plan31; Plan30 freezes a new candidate after Plan35.
- Exact 0.3.0, zero-hit, semantic review, four platform lanes, five-host packaged evidence, readiness-only and no-publish boundaries are unchanged.
</success_criteria>

<output>
Create .planning/phases/04.2-public-debranding/04.2-35-SUMMARY.md, then resume 04.2-30. The summary must record RED/GREEN evidence, the source/generated partition, the retired candidate OID and the requirement that Plan30 freeze a new post-fix candidate; it must not claim platform PASS or perform any external action.
</output>

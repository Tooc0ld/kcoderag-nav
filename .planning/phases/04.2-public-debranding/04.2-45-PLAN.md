---
phase: 04.2-public-debranding
plan: "45"
type: execute
wave: 45
depends_on: ["04.2-44"]
files_modified:
  - .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json
autonomous: true
gap_closure: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04]
risk_level: high
reversibility: reversible
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "The stale receipt bound to run 33257947461 and artifact 9716391301 cannot attest the current fixes and is replaced, not amended or treated as cumulative evidence."
    - "BRAND-04: the replacement receipt binds the exact post-fix candidate, tree, product snapshot, workflow blob, one tgz, artifact member, and all four Windows/Linux Node 22/24 lane results."
    - "The final HEAD retains zero findings, all regressions/generation/docs/local-guide/security and exact `/mcp` checks pass, and `mcp` is never followed by a slash."
    - "After 04.2-45-SUMMARY.md and any allowed GSD tracking child are committed, the final phase seal is no-write: HEAD, tree, status, refs, staged, dirty, and untracked baselines are identical before and after; stale, missing, unavailable, ambiguous, or child-scope metadata blocks completion."
    - "Tag, publish, registry refetch, unpublish, release, dist-tag, and history rewrite remain `NOT_RUN_BY_SCOPE`."
  artifacts:
    - path: ".planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json"
      provides: "current metadata-only immutable candidate and four-lane attestation"
    - path: ".planning/phases/04.2-public-debranding/04.2-45-SUMMARY.md"
      provides: "final no-write seal evidence and phase handoff"
  key_links:
    - from: ".planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md"
      to: "replacement readiness receipt"
      via: "independent exact-subject/tree/workflow/tgz/artifact/lane resolution"
    - from: "replacement readiness receipt"
      to: "current HEAD"
      via: "allowed documentation-child scope and product-snapshot equality"
    - from: "current HEAD"
      to: "final phase verdict"
      via: "zero-hit, regression, endpoint, and no-write seal gates"
---

<objective>
Replace the stale readiness receipt with current immutable evidence and close Phase 04.2 through a final no-write seal.

Purpose: make BRAND-04 attest the post-fix candidate and exact tgz instead of an older run, while preserving the external-action boundary.
Output: replacement 04.2-READINESS-RECEIPT.json and final 04.2-45-SUMMARY.md.
</objective>

<scope>
In scope: independently resolve candidate/run/artifact/lane metadata; replace the stale metadata-only receipt; validate subject/tree/workflow/blob/artifact/member/five-host identities; run final HEAD gates and a binary-safe no-write seal.

Non-goals: no new candidate, ref update, workflow dispatch, artifact upload, tgz rebuild, tag, publish, registry refetch, unpublish, release, dist-tag mutation, history rewrite, Phase 05-08 claim, or modification of the repository-local experience guide.
</scope>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md
@.planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json
@src/maintainer/release-readiness.cts
@src/maintainer/readiness-seal.cts
@src/maintainer/brand-audit.cts
@src/core/mcp-endpoint.cts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Resolve the current candidate, artifact, member, and four-lane evidence independently</name>
  <files></files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md — claimed candidate, run, artifact, tgz, and lane tuple handoff
    - src/maintainer/release-readiness.cts — independent evidence normalization and validation
    - .github/workflows/readiness.yml at the candidate — workflow and producer/consumer authority
    - .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json — stale schema shape only, not evidence authority
  </read_first>
  <action>Treat every Summary and stale-receipt field as an untrusted claim. Resolve the dedicated remote head, candidate commit/tree, exact workflow blob, product snapshot, run head/workflow identity, producer artifact, tgz filename/digest/size/member digest, and four lane tuples independently. Require exactly Windows Node 22, Windows Node 24, Linux Node 22, and Linux Node 24, each PASS against the same artifact member with no repack. Require source and tgz five-host outcomes and exact endpoint results to match Plan 44. Do not read or record artifact bodies, URLs, headers, authorization values, or log payloads. If any metadata is stale, unavailable, ambiguous, substituted, or not bound to the exact candidate, stop before editing the receipt.</action>
  <acceptance_criteria>
    - Independently resolved candidate, tree, product, workflow blob, run, artifact, tgz member, and four lane tuples exactly match Plan 44's claims.
    - The run contains one producer package authority and exactly four required successful consumer lanes; no missing or extra tuple can satisfy the gate.
    - Stale run 33257947461 and artifact 9716391301 are explicitly excluded from current authority.
  </acceptance_criteria>
  <verify><automated>git cat-file -e &lt;candidate-sha&gt;^{commit} &amp;&amp; git rev-parse &lt;candidate-sha&gt;^{tree} &amp;&amp; git ls-remote --refs origin refs/heads/readiness/04.2-candidate &amp;&amp; npm run build &amp;&amp; npm run test:readiness-workflow</automated></verify>
  <done>One complete, current, independently resolved evidence set is ready for receipt construction; stale identities have no authority.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stale receipt with current metadata-only identities</name>
  <files>.planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json</files>
  <read_first>
    - src/maintainer/release-readiness.cts — receipt schema, canonical ordering, and metadata allowlist
    - src/maintainer/readiness-seal.cts — strict receipt and child-scope checks
    - .planning/phases/04.2-public-debranding/04.2-44-SUMMARY.md — independently confirmed values from Task 1
    - .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json — file to replace atomically
  </read_first>
  <action>Construct a complete replacement receipt from Task 1's independently confirmed values. Bind the exact candidate subject/tree/product digest, candidate-owned workflow and relevant blob IDs, 0.3.0 tgz filename/digest/size/member digest, current run/artifact IDs, and the four exact lane tuples plus five-host outcomes. Preserve external actions as `NOT_RUN_BY_SCOPE` and keep the receipt metadata-only and canonically ordered. Replace the stale file atomically; do not carry forward its old run/artifact as current evidence. Run receipt/schema, readiness, child-scope, secret-shape, and seal-focused tests before staging only this exact path. Reject unknown fields, missing identities, stale values, duplicate lanes, child-subject substitution, unsafe strings, or any staged path outside the receipt.</action>
  <acceptance_criteria>
    - Receipt contains only the current exact evidence set and four required lanes; old run/artifact IDs are absent as authorities.
    - Candidate versus final-HEAD child scope is accepted only when product identities match and intervening changes are allowed planning/receipt outputs.
    - Receipt is secret-free, deterministic, schema-valid, and every prohibited external action is exactly `NOT_RUN_BY_SCOPE`.
    - The staging area contains only the receipt path.
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run test:readiness-seal &amp;&amp; npm run test:readiness-workflow &amp;&amp; git diff --cached --name-only -- .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json</automated></verify>
  <done>The stale receipt is replaced by one current, strict, secret-free candidate/tgz/four-lane receipt with scoped non-actions preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Commit all outputs, then run the final HEAD no-write phase seal</name>
  <files>.planning/phases/04.2-public-debranding/04.2-45-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04.2-public-debranding/04.2-32-PLAN.md — established post-output private-baseline and fixed-point contract
    - src/maintainer/readiness-seal.cts — final subject, child scope, receipt, and no-write contract
    - src/maintainer/brand-audit.cts — final tracked HEAD zero-hit gate
    - src/core/mcp-endpoint.cts — exact `/mcp` invariant
    - .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json — current attestation input
    - docs/MCP_QA_EXPERIENCE_GUIDE.md — repository-local ownership gate input, read-only
  </read_first>
  <action>After the receipt commit, run the full regression, deterministic generator, docs, repository-local guide ownership, security, dependency, package/readiness, five-host, and endpoint checks needed to prepare the handoff; require endpoint output exactly `/mcp` and reject `mcp` immediately followed by `/` in current tracked or package evidence. Create 04.2-45-SUMMARY.md with the independently resolved identities, gate outcomes, stale-evidence exclusion, scoped non-actions, and this exact post-output verification contract, then commit the Summary and any executor-produced GSD tracking child allowed by the execution workflow. Only after every tracked output is committed, allocate an OS-private baseline file outside the repository with exclusive creation and mode 0600. Hash the exact Plan 32/PATTERNS surface plus explicit fixed-point identities: HEAD OID, HEAD tree OID, porcelain-v2 status bytes, staged index bytes, cached hunks, tracked paths, tracked worktree hunks, untracked inventory, and the complete refname/OID inventory. Run only the live current-HEAD zero-hit command and `npm run seal:04.2 -- --candidate &lt;candidate-sha&gt; --final HEAD --receipt .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json`; the seal must bind exact candidate/tree/product/workflow/tgz/artifact/member/four-lane identities, allowed documentation/GSD child scope only, and every external action `NOT_RUN_BY_SCOPE`. Recompute the same fields with identical argv and compare byte-for-byte. Stale or unavailable metadata, wrong child scope, any HEAD/tree/status/ref delta, any repository write, skipped required gate, or mismatched non-action hard-blocks completion. Nothing may create, edit, stage, commit, tag, update a ref, or emit a repository artifact after the post-seal comparison begins.</action>
  <acceptance_criteria>
    - Full regression, generation, docs/local-guide, security, package/readiness, five-host, and exact `/mcp` gates pass before the final Summary commit.
    - 04.2-45-SUMMARY.md and any allowed GSD tracking child are committed before the private post-output baseline is captured; no tracked output is promised or created after the seal.
    - Current committed HEAD zero-hit and the final seal accept the replacement receipt and exact allowed child scope, while rejecting stale/unavailable/substituted identities.
    - Pre/post seal HEAD OID, tree OID, porcelain status, refs, index, cached/tracked hunks, tracked paths, and untracked inventory are identical.
    - Final Summary keeps tag, publish, refetch, unpublish, release, dist-tag, and history rewrite `NOT_RUN_BY_SCOPE`.
  </acceptance_criteria>
  <verify><automated>npm run build &amp;&amp; npm run generate:check &amp;&amp; npm test &amp;&amp; node --test dist-tests/core/mcp-endpoint.test.cjs &amp;&amp; npm run test:dependency-audit &amp;&amp; npm run test:readiness-workflow &amp;&amp; npm run test:readiness-seal &amp;&amp; git cat-file -e HEAD:.planning/phases/04.2-public-debranding/04.2-45-SUMMARY.md &amp;&amp; node -e "const{execFileSync}=require('node:child_process');const{createHash}=require('node:crypto');const{writeFileSync}=require('node:fs');const run=a=&gt;execFileSync('git',a,{encoding:null,maxBuffer:67108864});const h=b=&gt;createHash('sha256').update(b).digest('hex');const snap={head:run(['rev-parse','HEAD']).toString('utf8').trim(),tree:run(['rev-parse','HEAD^{tree}']).toString('utf8').trim(),status:h(run(['status','--porcelain=v2','-z','--untracked-files=all'])),index:h(run(['ls-files','--stage','-z'])),cachedHunks:h(run(['diff','--cached','--binary','--no-ext-diff'])),trackedPaths:h(run(['ls-files','-z'])),trackedHunks:h(run(['diff','--binary','--no-ext-diff'])),untracked:h(run(['ls-files','--others','--exclude-standard','-z'])),refs:h(run(['for-each-ref','--format=%(refname)%00%(objectname)']))};writeFileSync(process.argv[1],JSON.stringify(snap)+'\n',{flag:'wx',mode:0o600})" &lt;private-pre-seal-baseline&gt; &amp;&amp; npm run audit:brand -- git --subject HEAD &amp;&amp; npm run seal:04.2 -- --candidate &lt;candidate-sha&gt; --final HEAD --receipt .planning/phases/04.2-public-debranding/04.2-READINESS-RECEIPT.json &amp;&amp; node -e "const{execFileSync}=require('node:child_process');const{createHash}=require('node:crypto');const{readFileSync}=require('node:fs');const run=a=&gt;execFileSync('git',a,{encoding:null,maxBuffer:67108864});const h=b=&gt;createHash('sha256').update(b).digest('hex');const actual={head:run(['rev-parse','HEAD']).toString('utf8').trim(),tree:run(['rev-parse','HEAD^{tree}']).toString('utf8').trim(),status:h(run(['status','--porcelain=v2','-z','--untracked-files=all'])),index:h(run(['ls-files','--stage','-z'])),cachedHunks:h(run(['diff','--cached','--binary','--no-ext-diff'])),trackedPaths:h(run(['ls-files','-z'])),trackedHunks:h(run(['diff','--binary','--no-ext-diff'])),untracked:h(run(['ls-files','--others','--exclude-standard','-z'])),refs:h(run(['for-each-ref','--format=%(refname)%00%(objectname)']))};if(JSON.stringify(actual)!==readFileSync(process.argv[1],'utf8').trim())process.exit(1)" &lt;private-pre-seal-baseline&gt;</automated></verify>
  <done>All outputs are already committed; current HEAD is zero-hit, the replacement receipt attests exact four-lane evidence, and the final post-output comparison proves no HEAD, tree, status, ref, index, tracked, or untracked mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Summary/remote metadata → replacement receipt | claims are untrusted until independently resolved |
| candidate → final documentation child | only allowed child scope may share product identity |
| stale receipt → current phase verdict | older evidence must never be promoted |
| final seal → repository | validation must not mutate tracked, staged, dirty, or untracked state |

## STRIDE Threat Register (ASVS L1, block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-45-01 | Spoofing/Repudiation | receipt staleness | high | mitigate | independently resolve every immutable identity and exclude old authorities |
| T-04.2-45-02 | Tampering | artifact/member/lane substitution | high | mitigate | exact run, artifact, tgz member, digest, size, and four-lane tuple binding |
| T-04.2-45-03 | Elevation/Tampering | child-scope expansion | high | mitigate | product snapshot equality plus allowlisted documentation-child paths |
| T-04.2-45-04 | Tampering/Denial of service | unavailable or ambiguous metadata | high | mitigate | fail closed before receipt edit or completion claim |
| T-04.2-45-05 | Information disclosure | receipt/log fields | high | mitigate | metadata allowlist and secret-shape rejection |
| T-04.2-45-06 | Tampering | final-seal writes | high | mitigate | binary-safe pre/post HEAD, index, dirty, untracked, and digest baseline equality |
| T-04.2-45-07 | Elevation | external action scope | high | mitigate | exact `NOT_RUN_BY_SCOPE` assertions and no remote mutation in this plan |
</threat_model>

<verification>
The replacement receipt is independently bound to the post-fix candidate and one exact tgz; final HEAD passes all current gates; the final seal leaves the complete repository baseline unchanged.
</verification>

<success_criteria>
- BRAND-04 immutable candidate/tree/workflow/tgz/artifact/member and four hosted lane identities are current and exact.
- The stale run/artifact cannot attest current fixes and are absent from current authority.
- Final HEAD passes zero-hit, regression, generator, docs/local-guide, security, package/five-host, and endpoint gates.
- The final seal accepts only allowed child scope, performs no writes, and hard-blocks stale or unavailable metadata.
- External release actions remain `NOT_RUN_BY_SCOPE`.
</success_criteria>

<output>
Create and commit .planning/phases/04.2-public-debranding/04.2-45-SUMMARY.md after the receipt and preparatory gates but before the final seal. Record current immutable identities, four lane tuples, preparatory command outcomes, stale-evidence exclusion, scoped non-actions, and the exact post-output baseline/seal contract; include no URLs, headers, tokens, credentials, logs, or configuration bodies. Commit any allowed GSD tracking child before baseline capture. The live seal PASS is reported only by the executor/verifier response; no file, commit, tracking update, or other repository write is created after the seal.
</output>

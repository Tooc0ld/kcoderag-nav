---
phase: 04-deployment-reliability
plan: 10
type: execute
wave: 8
depends_on: ["04-09", "04-14", "04-17"]
files_modified:
  - package.json
  - kcoderag-dev/.codex.mcp.json
  - kcoderag-dev/.mcp.json
  - kcoderag-dev/.claude-plugin/plugin.json
  - kcoderag-dev/.codex-plugin/plugin.json
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: npm's public allow-list and compatibility discovery surfaces expose no Dev product after QA-only public docs are already synchronized."
    - "D-02: retiring Dev package/manifest entries does not remove source-owned legacy Dev decoding for confirmed migration/uninstall."
    - "D-04: no package or compatibility manifest can select the retired Dev product."
  artifacts:
    - path: "package.json"
      provides: "QA/Cursor-only npm public files allow-list"
    - path: "kcoderag-dev/.codex.mcp.json"
      provides: "Retired Dev Codex MCP projection that must be absent"
    - path: "kcoderag-dev/.mcp.json"
      provides: "Retired Dev Claude MCP projection that must be absent"
    - path: "kcoderag-dev/.claude-plugin/plugin.json"
      provides: "Retired Claude compatibility manifest that must be absent"
    - path: "kcoderag-dev/.codex-plugin/plugin.json"
      provides: "Retired Codex compatibility manifest that must be absent"
  key_links:
    - from: "package.json files"
      to: "Plan 11 pack audit"
      via: "closed QA/Cursor-only public inventory"
      pattern: "kcoderag-qa|kcoderag-cursor"
    - from: "Plan 14 public docs"
      to: "Dev entry-point deletion"
      via: "users receive QA-only transition guidance before discovery surfaces disappear"
      pattern: "QA-only|legacy"
---

<objective>
Retire Dev from the npm allow-list and host compatibility discovery only after the public README and authoritative guide explain the QA-only transition.

Purpose: Make public entry-point retirement separately auditable and preserve the locked user-communication order.
Output: QA/Cursor-only package inventory and deletion of four Dev config/manifest entries.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-09-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-14-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-17-SUMMARY.md
@package.json
@src/core/state.cts
@src/generator/index.cts

<interfaces>
- Plan 14 has already documented QA-only public behavior and exact legacy migration/uninstall in both authoritative public entry documents.
- Plans 09 and 17 provide complete Cursor and QA replacement products.
- Plan 18 removes the remaining executable/content Dev tree only after these public entry points are retired.
- D-02 legacy compatibility remains in core/host source/tests and is not owned by `kcoderag-dev/`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Remove Dev from npm's public allow-list</name>
  <read_first>
    package.json
    README.md
    ../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md
    .planning/phases/04-deployment-reliability/04-14-SUMMARY.md
  </read_first>
  <reversibility rating="one-way">D-01 retires the public Dev package surface for immutable 0.2.0; restoration requires a new public product decision.</reversibility>
  <files>package.json</files>
  <action>
    Assert Plan 14's QA-only public documentation is present, then remove the `kcoderag-dev` member from the exact npm `files` allow-list. Preserve the public bin, compiled runtime, QA/Cursor assets, Node engine, repository metadata, scripts, and zero runtime dependencies. Do not add a replacement Dev alias, environment selector, or root marketplace catalog.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const p=require('./package.json'); if(p.files.some(x=&gt;x.includes('kcoderag-dev'))) throw new Error('Dev remains public'); for(const x of ['kcoderag-qa','kcoderag-cursor','dist']) if(!p.files.some(v=&gt;v.includes(x))) throw new Error('required member missing '+x)"</automated>
  </verify>
  <acceptance_criteria>
    - npm's public allow-list has no Dev member.
    - QA/Cursor/bin/runtime package entries remain present.
    - Public QA-only transition guidance existed before this deletion executes.
  </acceptance_criteria>
  <done>The npm package entry surface is QA/Cursor-only without weakening current runtime distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Delete all Dev host discovery manifests</name>
  <read_first>
    kcoderag-dev/.codex.mcp.json
    kcoderag-dev/.mcp.json
    kcoderag-dev/.claude-plugin/plugin.json
    kcoderag-dev/.codex-plugin/plugin.json
    tests/migration/legacy-state.test.cts
  </read_first>
  <files>kcoderag-dev/.codex.mcp.json, kcoderag-dev/.mcp.json, kcoderag-dev/.claude-plugin/plugin.json, kcoderag-dev/.codex-plugin/plugin.json</files>
  <action>
    Delete the two Dev MCP projections and two compatibility manifests so no supported host can discover a Dev product from repository/package metadata. Per D-02, keep all historical recognition in source-owned exact path/digest decoders; do not copy, redirect, or alias these manifests to QA. Verification may inspect only filenames and metadata, never MCP values.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:migration &amp;&amp; node -e "const fs=require('node:fs'); for(const f of ['kcoderag-dev/.codex.mcp.json','kcoderag-dev/.mcp.json','kcoderag-dev/.claude-plugin/plugin.json','kcoderag-dev/.codex-plugin/plugin.json']) if(fs.existsSync(f)) throw new Error('retired entry remains '+f)"</automated>
  </verify>
  <acceptance_criteria>
    - All four Dev config/manifest entry files are absent.
    - Legacy Dev decode/migration tests remain independent of deleted generated bytes.
    - QA, Cursor, unrelated repository, and sibling-repository files are untouched by this task.
  </acceptance_criteria>
  <done>No package or compatibility discovery surface can select Dev, while exact historical decoding remains available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs → product deletion | Transition guidance must exist before public Dev discovery disappears. |
| Repository tree → npm public allow-list | A forgotten member could remain installable. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Repudiation | transition order | high | mitigate | Depend on Plan 14 and assert both authoritative QA-only documents before deleting entries. |
| T-04-10-02 | Tampering | package inventory | high | mitigate | Enforce an exact QA/Cursor allow-list and explicit absence of four Dev discovery files. |
| T-04-10-03 | Denial of Service | legacy migration | high | mitigate | Preserve and run source-owned legacy decoder tests independent of generated files. |
| T-04-10-04 | Information Disclosure | retired MCP metadata | high | mitigate | Verify only path absence and never read or report connection values. |
</threat_model>

<verification>
QA-only docs precondition, npm allow-list checks, exact four-file absence, and legacy migration tests prove safe package/manifest retirement.
</verification>

<success_criteria>
- This plan owns exactly five concrete files.
- QA-only README and sibling guide were synchronized before it executes.
- Dev has no npm/config/manifest discovery surface; runtime/content deletion remains Plan 18.
</success_criteria>

## Artifacts this phase produces

- QA/Cursor-only `package.json.files` inventory.
- Deletion of four Dev MCP/compatibility entry files.

<output>
Create `.planning/phases/04-deployment-reliability/04-10-SUMMARY.md` when done.
</output>

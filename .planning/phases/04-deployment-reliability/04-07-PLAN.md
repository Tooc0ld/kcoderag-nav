---
phase: 04-deployment-reliability
plan: 07
type: execute
wave: 4
depends_on: ["04-02", "04-14"]
files_modified:
  - src/generator/index.cts
  - plugin-src/environments.json
  - plugin-src/environments/dev.mcp.json
  - plugin-src/routing.json
  - plugin-src/cursor/rules/kcoderag-navigation.mdc
  - tests/generator/generation.test.cts
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: canonical product selection accepts only QA and Cursor projection; Dev cannot be selected, rendered, or retained as a public generator input."
    - "D-01: routing and Cursor canonical text describe QA only without changing Phase 05 lookup precision or claiming Cursor Hook equivalence."
    - "D-04: Dev selection is always unsupported and the full-repository `all/all --check` treats retired Dev presence as blocking drift before 0.2.0."
  artifacts:
    - path: "src/generator/index.cts"
      provides: "QA/Cursor-only deterministic product map and retired-product refusal"
      exports: ["Product", "ASSET_GROUP_PATHS", "checkGenerated", "generatePackage"]
    - path: "plugin-src/environments.json"
      provides: "single canonical QA environment metadata record"
    - path: "plugin-src/routing.json"
      provides: "QA-only navigation policy source"
    - path: "tests/generator/generation.test.cts"
      provides: "focused canonical QA/Cursor selection and deterministic-byte proof"
  key_links:
    - from: "plugin-src/environments.json"
      to: "src/generator/index.cts"
      via: "closed one-QA-record input validation and deterministic rendering"
      pattern: "PRODUCTS|PRODUCT_DIRECTORIES"
    - from: "src/generator/index.cts"
      to: "Plans 08-10 and 17-19 product/document trees"
      via: "QA and Cursor product-specific generation plus retired Dev output rejection"
      pattern: "generatePackage|checkGenerated"
---

<objective>
Remove Dev from the canonical generator/input model before regenerating any public product tree.

Purpose: Give QA regeneration, Cursor regeneration, and Dev retirement one small deterministic source contract instead of mixing canonical and 31 generated files in one plan.
Output: QA/Cursor-only generator types, inputs, routing text, and focused canonical-generation tests.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-PATTERNS.md
@.planning/phases/04-deployment-reliability/04-02-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-14-SUMMARY.md
@src/generator/index.cts
@plugin-src/environments.json
@plugin-src/routing.json
@plugin-src/cursor/rules/kcoderag-navigation.mdc

<interfaces>
- Plan 02 establishes the QA-only public/controller contract; this plan is the corresponding canonical generated-product boundary.
- `generatePackage` and `checkGenerated` render sorted bytes from canonical templates; generated trees remain products and are owned by Plans 08-10 and 17-19.
- Legacy Dev decoding remains in core/host migration code and must not import or require a generated Dev product.
- Connection bytes are opaque input and may not enter diagnostics, snapshots, or receipts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Close canonical generation around QA and Cursor</name>
  <read_first>
    src/generator/index.cts
    plugin-src/environments.json
    plugin-src/environments/dev.mcp.json
    plugin-src/routing.json
    tests/generator/generation.test.cts
  </read_first>
  <reversibility rating="one-way">D-01 removes a published product and its canonical inputs; restoring it after 0.2.0 requires a new public contract and migration.</reversibility>
  <files>src/generator/index.cts, plugin-src/environments.json, plugin-src/environments/dev.mcp.json, plugin-src/routing.json, tests/generator/generation.test.cts</files>
  <behavior>
    - Product selection accepts QA, Cursor, or all-applicable only and rejects Dev as unsupported.
    - Environment metadata requires exactly one QA record and does not read a Dev connection source.
    - Routing output is QA-only and contains no switching/fallback branch.
    - Any requested Dev product fails; full-repository `all/all --check` reports an existing retired Dev product as a stable path-only refusal while targeted QA/Cursor generation remains usable for ordered replacement.
  </behavior>
  <action>
    Per D-01, remove Dev from `Product`, environment IDs, product directories, asset-group maps, generator CLI choices, replacements, and validation. Delete `plugin-src/environments/dev.mcp.json` only after its focused test proves no production reader remains. Rename shared QA/Dev constants to QA-specific ownership and preserve code-unit sorting, atomic generation, group compatibility, version alignment, and opaque QA connection bytes. Reject every explicit Dev selection. Make the full-repository `--package all --group all --check` detect the retired directory, while targeted QA/Cursor generation/check ignores unrelated product roots so Plans 08/09 can establish replacements before Plan 10 deletes Dev. Scope retirement checks to active public generator/output regions so historical migration identifiers remain valid data. Do not edit any generated product tree in this task.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:generator</automated>
  </verify>
  <acceptance_criteria>
    - Types and CLI cannot select Dev; canonical environment input contains exactly QA.
    - QA/Cursor render requests are byte-deterministic in temporary outputs.
    - Existing/requested Dev output fails with stable code/path-only diagnostics.
    - Legacy state/migration tests remain independent of deleted generator input.
  </acceptance_criteria>
  <done>The canonical generator can produce only QA and Cursor assets and actively rejects public Dev output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make canonical routing and Cursor policy QA-only</name>
  <read_first>
    plugin-src/routing.json
    plugin-src/cursor/rules/kcoderag-navigation.mdc
    tests/generator/generation.test.cts
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>plugin-src/routing.json, plugin-src/cursor/rules/kcoderag-navigation.mdc, tests/generator/generation.test.cts</files>
  <behavior>
    - Generated QA skill/agent/routing content has no public Dev choice or fallback.
    - Cursor policy retains Rule/skill/MCP semantics and never claims PreToolUse equivalence.
    - Keyword/context/call-chain guidance remains unchanged; Phase 05 precision work is not absorbed.
  </behavior>
  <action>
    Remove only public Dev/environment-switching language from canonical routing and Cursor Rule inputs. Preserve the current advisory/fail-open wording, Cursor capability distinction, and existing query-method guidance exactly outside environment identity. Add generator tests that compare the normalized QA-only outputs and reject public Dev command/path wording in active regions without broad-grepping legacy planning/migration data.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:generator</automated>
  </verify>
  <acceptance_criteria>
    - Canonical routing and Cursor Rule are QA-only.
    - No Hook-equivalence, semantic/hybrid, or precision-scope change is introduced.
    - Focused active-region checks do not reject legitimate historical Dev migration fixtures.
  </acceptance_criteria>
  <done>All downstream generated navigation products consume one honest QA-only policy source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical templates/config → generated products | Opaque connection values and product identity become installable bytes. |
| Retired product identifiers → compatibility history | Public retirement checks must not destroy legacy decode evidence. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Tampering | product selection | high | mitigate | Close generator enums/input/output sets and reject every active Dev selection or directory. |
| T-04-07-02 | Information Disclosure | generator diagnostics | high | mitigate | Treat connection bytes as opaque and emit only stable codes, safe paths, sizes, and digests. |
| T-04-07-03 | Spoofing | Cursor capability text | medium | mitigate | Preserve Rule/skill/MCP wording and reject any generated Hook-equivalence claim. |
| T-04-07-04 | Denial of Service | retirement scan | low | accept | Region-scope checks to public generator/output surfaces so migration history remains usable. |
</threat_model>

<verification>
Focused generator tests prove closed QA/Cursor selection, deterministic temporary output, opaque values, and QA-only routing before repository products are regenerated.
</verification>

<success_criteria>
- Canonical generation has six explicitly owned files, below the 15-file plan ceiling.
- QA/Cursor regeneration plans can run from one stable canonical contract.
- Dev remains available only to host/core legacy decoding, never to the public generator.
</success_criteria>

## Artifacts this phase produces

- QA/Cursor-only `Product`, environment, group, and generator CLI contracts.
- Deleted canonical `plugin-src/environments/dev.mcp.json`.
- QA-only `plugin-src/routing.json` and Cursor Rule source.
- Canonical generation/retired-product regression tests.

<output>
Create `.planning/phases/04-deployment-reliability/04-07-SUMMARY.md` when done.
</output>

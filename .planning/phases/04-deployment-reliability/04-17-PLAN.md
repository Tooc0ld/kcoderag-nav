---
phase: 04-deployment-reliability
plan: 17
type: execute
wave: 7
depends_on: ["04-07", "04-08"]
files_modified:
  - kcoderag-qa/.codex.mcp.json
  - kcoderag-qa/.mcp.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/hooks/hooks.json
  - kcoderag-qa/agents/kcode-explorer.md
  - kcoderag-qa/skills/code-lookup-discipline/SKILL.md
  - tests/generator/qa-product.test.cts
autonomous: true
requirements: [DEP-01, DEP-02]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: generated QA metadata and guidance expose QA only and are version-aligned with the root package."
    - "DEP-02/D-05-D-07: Codex/Claude registration reaches the self-contained Plan 08 launchers without embedding a project root."
    - "D-04: the complete non-document QA product has a closed deterministic twelve-file inventory."
  artifacts:
    - path: "kcoderag-qa/hooks/hooks.json"
      provides: "Codex/Claude advisory Hook registration for shipped launchers"
    - path: "kcoderag-qa/.codex-plugin/plugin.json"
      provides: "QA-only Codex compatibility metadata"
    - path: "kcoderag-qa/.claude-plugin/plugin.json"
      provides: "QA-only Claude compatibility metadata"
    - path: "tests/generator/qa-product.test.cts"
      provides: "Closed QA product inventory and deterministic-byte regression"
  key_links:
    - from: "kcoderag-qa/hooks/hooks.json"
      to: "kcoderag-qa/hooks/run_hook.cmd and run_hook.sh"
      via: "host registration invokes Plan 08 shipped launchers"
      pattern: "run_hook"
    - from: "qa-product.test.cts"
      to: "Plans 08 and 17 output"
      via: "exact twelve-file non-document set and fresh-render hashes"
      pattern: "kcoderag-qa"
---

<objective>
Regenerate QA host registration, MCP metadata, and guidance after the executable runtime is fixed, then close the complete non-document QA inventory.

Purpose: Keep registration/guidance ownership disjoint from runtime bytes while proving the two slices form one self-contained QA product.
Output: Five host/config files, two guidance files, and a focused product-inventory test.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-07-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-08-SUMMARY.md
@src/generator/index.cts
@tests/generator/generation.test.cts

<interfaces>
- Plan 08 supplies the exact executable paths this plan registers.
- Plan 07 owns all canonical inputs and rejects retired Dev output.
- This plan excludes `kcoderag-qa/README.md`; early public contract sync is Plan 14 and generated documentation is Plan 19.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Regenerate QA registration and opaque MCP metadata</name>
  <read_first>
    src/generator/index.cts
    kcoderag-qa/.codex.mcp.json
    kcoderag-qa/.mcp.json
    kcoderag-qa/.codex-plugin/plugin.json
    kcoderag-qa/.claude-plugin/plugin.json
    kcoderag-qa/hooks/hooks.json
  </read_first>
  <files>kcoderag-qa/.codex.mcp.json, kcoderag-qa/.mcp.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-qa/hooks/hooks.json</files>
  <action>
    Per D-01/D-04, generate QA Codex/Claude MCP projections, compatibility manifests, and Hook registration from canonical inputs. Keep connection values opaque, propagate the exact root package version, register only Plan 08 project-contained launchers, and preserve advisory/fail-open matching. Do not restore a root marketplace catalog or treat compatibility manifests as installation sources. Validate references by safe relative path only; never snapshot or print MCP values.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-registration &amp;&amp; node dist/generator/index.cjs --package qa --group metadata-config &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-registration --check &amp;&amp; node dist/generator/index.cjs --package qa --group metadata-config --check</automated>
  </verify>
  <acceptance_criteria>
    - QA manifests and MCP/Hook registration share exact root version identity.
    - Every registration path resolves to a Plan 08 file inside `kcoderag-qa`.
    - No connection/config value enters diagnostics, snapshots, or generated evidence.
  </acceptance_criteria>
  <done>QA host metadata reaches the real generated runtime and exposes QA only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate guidance and close the QA product inventory</name>
  <read_first>
    kcoderag-qa/agents/kcode-explorer.md
    kcoderag-qa/skills/code-lookup-discipline/SKILL.md
    tests/generator/qa-product.test.cts
    src/generator/index.cts
  </read_first>
  <files>kcoderag-qa/agents/kcode-explorer.md, kcoderag-qa/skills/code-lookup-discipline/SKILL.md, tests/generator/qa-product.test.cts</files>
  <behavior>
    - The QA non-document product inventory is exactly the twelve files owned by Plans 08 and 17.
    - Agent and skill route to QA only and preserve the Phase 05 lookup-precision boundary.
    - Missing, extra, retired-Dev-referencing, dangling, or nondeterministic members fail.
  </behavior>
  <action>
    Generate the QA agent and shared skill from canonical routing. Build the inventory test around an explicit relative-path set spanning Plans 08 and 17, fresh temporary generation, hash equality, version equality, self-contained reference resolution, and scoped retired-Dev rejection. Keep README out of this non-document inventory and leave final repository/package enforcement to Plan 11.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package qa --group metadata-guidance &amp;&amp; node dist/generator/index.cjs --package qa --group metadata-guidance --check &amp;&amp; node --test dist-tests/generator/qa-product.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All twelve non-document QA files equal fresh canonical output.
    - QA guidance contains no public environment selection/fallback and retains deferred precision boundaries.
    - Extra, missing, dangling, or Dev-dependent members fail with metadata-only diagnostics.
  </acceptance_criteria>
  <done>The split QA runtime and registration/guidance slices converge into one closed deterministic product.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Opaque MCP input → host metadata | Embedded connection values must never enter diagnostics/evidence. |
| Registration → generated runtime | A stale path could bypass or break the fail-open launcher. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17-01 | Tampering | QA product inventory | high | mitigate | Exact path set, reference resolution, and fresh-render hashes reject drift. |
| T-04-17-02 | Spoofing | compatibility metadata | high | mitigate | Require QA identity and exact root version in every manifest. |
| T-04-17-03 | Information Disclosure | MCP config | critical | mitigate | Treat values as opaque and assert only paths, sizes, versions, hashes, and booleans. |
| T-04-17-04 | Denial of Service | Hook registration | medium | mitigate | Register only tested Plan 08 fail-open launchers with project-relative paths. |
</threat_model>

<verification>
Fresh registration/config/guidance generation plus exact twelve-file inventory proves a deterministic QA-only product connected to the Plan 08 runtime.
</verification>

<success_criteria>
- This plan owns exactly eight concrete files.
- QA registration/guidance is deterministic and self-contained with Plan 08.
- README remains separately owned by Plans 14 and 19.
</success_criteria>

## Artifacts this phase produces

- Five QA MCP/manifest/Hook-registration files.
- QA agent and `code-lookup-discipline` skill.
- `tests/generator/qa-product.test.cts` spanning the split product.

<output>
Create `.planning/phases/04-deployment-reliability/04-17-SUMMARY.md` when done.
</output>

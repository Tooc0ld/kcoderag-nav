---
phase: 04-deployment-reliability
plan: 12
type: execute
wave: 11
depends_on: ["04-06", "04-11"]
files_modified:
  - package.json
  - src/maintainer/release.cts
  - tests/maintainer/release.test.cts
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04: the release helper derives exact minor 0.2.0/v0.2.0 from 0.1.8 and owns only package, lock, two QA manifests, and one Cursor manifest."
    - "D-04: dry-run runs every configured gate before mutation and reports the exact five-path write-set without changing repository/index/tag state."
    - "D-20: dirty production paths, failed gates, tag collision, unexpected path, or injected commit failure roll back locally and never push/publish/unpublish."
  artifacts:
    - path: "src/maintainer/release.cts"
      provides: "Five-path QA/Cursor release preparation and exact minor dry-run"
      exports: ["VERSION_MANIFEST_PATHS", "RELEASE_OWNED_PATHS", "prepareRelease"]
    - path: "tests/maintainer/release.test.cts"
      provides: "Exact version/write-set/gate/rollback/tag-collision contract"
    - path: "package.json"
      provides: "Maintainer release command wired to compiled Node CJS"
  key_links:
    - from: "prepareRelease({level:'minor', dryRun:true})"
      to: "Plan 15 audited publication"
      via: "exact 0.1.8→0.2.0 derivation and identical five-path write-set"
      pattern: "RELEASE_OWNED_PATHS|0.2.0"
    - from: "src/maintainer/release.cts"
      to: "npm run ci:local"
      via: "automatic gates finish before any version write/commit/tag"
      pattern: "runGates"
---

<objective>
Make local 0.2.0 release preparation exact and rollback-safe, separately from pack, smoke, CI, and pre-commit implementation.

Purpose: Keep permanent version/tag mechanics small enough to audit before Plan 15 invokes them.
Output: A five-path release helper, exact dry-run contract, and exhaustive local failure tests.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-06-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-11-SUMMARY.md
@package.json
@src/maintainer/release.cts
@tests/maintainer/release.test.cts

<interfaces>
- Plan 11 owns final real-pack enforcement and the evidence validators; this plan does not edit those files.
- `prepareRelease` may create a local commit/tag only after gates; it never pushes or publishes.
- Root `package.json` remains the sole version source; `package-lock.json` and three QA/Cursor compatibility manifests are derived version destinations.
- No human release checkpoint is added because D-04 already authorizes exact 0.2.0 after automatic gates.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Derive exact five-path 0.2.0 release state without mutation</name>
  <read_first>
    package.json
    src/maintainer/release.cts
    tests/maintainer/release.test.cts
    kcoderag-qa/.codex-plugin/plugin.json
    kcoderag-qa/.claude-plugin/plugin.json
    kcoderag-cursor/.cursor-plugin/plugin.json
  </read_first>
  <reversibility rating="one-way">D-04 permanently publishes the five-path 0.2.0 identity; dry-run and exact path gates must be correct before Plan 15.</reversibility>
  <files>src/maintainer/release.cts, tests/maintainer/release.test.cts, package.json</files>
  <behavior>
    - Minor dry-run from root 0.1.8 yields 0.2.0 and v0.2.0.
    - Owned paths are exactly `package.json`, `package-lock.json`, QA Codex manifest, QA Claude manifest, and Cursor manifest.
    - Dry-run runs configured automatic gates, emits one closed JSON result, and leaves files/index/refs/tags byte-identical.
    - Any extra/missing/Dev manifest path or version disagreement fails before mutation.
  </behavior>
  <action>
    Remove every Dev manifest/path from version propagation and release ownership. Define and export an immutable sorted five-path set and derive all new versions from root `package.json`. Keep exact semver minor derivation, local gate ordering, one JSON receipt, path-only diagnostics, and no push/publish side effects. Update the package script only if needed to invoke compiled CJS deterministically. Add fixture-based dry-run tests with injected successful/failing gates that snapshot worktree/index/refs/tags and assert exact 0.1.8→0.2.0 identity. Do not run the real full-gate dry-run against the incomplete pre-documentation branch; Plan 15 performs that exact command on the final implementation subject after Plans 13-14.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:release</automated>
  </verify>
  <acceptance_criteria>
    - Fixture-backed dry-run reports exactly previous 0.1.8, next 0.2.0, tag v0.2.0, and five owned paths while proving gate invocation order.
    - No repository/index/ref/tag byte changes occur.
    - Any Dev/extra/missing path or manifest version disagreement is a stable pre-write refusal.
  </acceptance_criteria>
  <done>The exact permanent release shape is reproducible without changing shared state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove local release commit/tag rollback and isolation</name>
  <read_first>
    src/maintainer/release.cts
    tests/maintainer/release.test.cts
    src/core/transaction.cts
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>src/maintainer/release.cts, tests/maintainer/release.test.cts</files>
  <behavior>
    - Successful non-dry local preparation changes/stages/commits exactly five version paths and creates one matching local tag.
    - Dirty owned path, unrelated staged change, gate failure, tag collision, commit/tag failure, or write failure stops or restores the pre-run local state.
    - Existing unrelated dirty/untracked planning and sibling-repository work is never staged, reset, committed, or deleted.
    - Helper never runs network push, npm publish, unpublish, or dist-tag mutation.
  </behavior>
  <action>
    Preserve the existing injected runner/failure-point architecture and strengthen exact staging, commit-parent, tag-target, rollback, and external-side-effect assertions for the five-path contract. Snapshot owned files, index entries, HEAD, tags, and known unrelated dirty/untracked baseline; restore only helper-owned temporary changes on failure. Keep permanent push/publish in Plan 15 and fix-forward policy in D-20.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:release</automated>
  </verify>
  <acceptance_criteria>
    - Success fixture creates exactly one five-path commit and matching tag.
    - Every injected failure returns to identical local bytes/index/HEAD/tags.
    - No external network/publication command is possible from this helper.
  </acceptance_criteria>
  <done>Local release preparation is exact, isolated, and recoverable before irreversible publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source/generated version state → release commit | Only five derived version paths may enter the release commit. |
| Git worktree/index → local tag | Existing user work must not be staged, reset, or attached to the release. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12-01 | Tampering | release write-set | critical | mitigate | Exact immutable five-path allow-list, manifest/version equality, and staging/commit diff tests. |
| T-04-12-02 | Elevation of Privilege | Git staging | high | mitigate | Snapshot index/user baseline, stage only owned paths, and restore helper-owned changes on every injected failure. |
| T-04-12-03 | Repudiation | local tag identity | high | mitigate | Require tag v0.2.0 target equals the exact generated release commit and reject collisions. |
| T-04-12-04 | Information Disclosure | helper diagnostics | high | mitigate | Emit only versions, safe paths, SHAs/digests, booleans, and stable error codes. |
</threat_model>

<verification>
Fixture-backed exact minor dry-run and complete release-helper tests pass with five paths, no Dev manifest, no mutation on dry-run, and complete local rollback/isolation; Plan 15 owns the first real full-gate dry-run on the final subject.
</verification>

<success_criteria>
- Pack/repository concerns stay in Plan 11; smoke/CI/pre-commit stay in Plan 13.
- Release helper owns three source/test/config files and an exact five-path runtime write-set.
- No release approval checkpoint or external publication side effect exists here.
</success_criteria>

## Artifacts this phase produces

- QA/Cursor-only `VERSION_MANIFEST_PATHS` and `RELEASE_OWNED_PATHS`.
- Exact 0.1.8→0.2.0 minor dry-run JSON contract.
- Local release commit/tag rollback and isolation tests.

<output>
Create `.planning/phases/04-deployment-reliability/04-12-SUMMARY.md` when done.
</output>

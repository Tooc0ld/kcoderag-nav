---
phase: 04-deployment-reliability
plan: 09
type: execute
wave: 5
depends_on: ["04-07"]
files_modified:
  - kcoderag-cursor/mcp.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - kcoderag-cursor/rules/kcoderag-navigation.mdc
  - kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
  - tests/generator/cursor-product.test.cts
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: the generated Cursor product projects QA only and remains self-contained/version-aligned."
    - "D-01: Cursor retains its honest Rule/skill/MCP capability boundary and never claims Codex/Claude PreToolUse behavior."
    - "D-04: a fresh canonical render and exact inventory test reject Dev wording, missing/extra members, or nondeterministic Cursor bytes."
  artifacts:
    - path: "kcoderag-cursor/mcp.json"
      provides: "QA-only Cursor MCP projection"
    - path: "kcoderag-cursor/rules/kcoderag-navigation.mdc"
      provides: "Always-on QA navigation Rule"
    - path: "kcoderag-cursor/skills/code-lookup-discipline/SKILL.md"
      provides: "Shared QA lookup discipline for Cursor"
    - path: "tests/generator/cursor-product.test.cts"
      provides: "Closed four-file non-document Cursor product proof"
  key_links:
    - from: "plugin-src/cursor/rules/kcoderag-navigation.mdc"
      to: "kcoderag-cursor/rules/kcoderag-navigation.mdc"
      via: "deterministic Cursor product rendering"
      pattern: "QA|Rule"
    - from: "kcoderag-cursor/.cursor-plugin/plugin.json"
      to: "kcoderag-cursor/mcp.json"
      via: "version-aligned compatibility metadata references the shipped QA MCP product"
      pattern: "version"
---

<objective>
Regenerate and close the Cursor QA product independently of the QA Hook product and retired Dev tree.

Purpose: Preserve Cursor's distinct capability model while keeping generated ownership small and disjoint.
Output: Four non-document Cursor product files and an exact product test.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-07-SUMMARY.md
@src/generator/index.cts
@plugin-src/cursor/rules/kcoderag-navigation.mdc

<interfaces>
- Plan 07 owns canonical QA/Cursor product selection and Cursor Rule input.
- Cursor has Rule/skill/MCP, not a generated PreToolUse launcher; no compatibility claim may blur that boundary.
- Plan 14 owns `kcoderag-cursor/README.md`; this plan owns every other Cursor product file.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Regenerate the complete Cursor QA navigation path</name>
  <read_first>
    src/generator/index.cts
    plugin-src/cursor/rules/kcoderag-navigation.mdc
    kcoderag-cursor/mcp.json
    kcoderag-cursor/.cursor-plugin/plugin.json
    kcoderag-cursor/rules/kcoderag-navigation.mdc
    kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
  </read_first>
  <files>kcoderag-cursor/mcp.json, kcoderag-cursor/.cursor-plugin/plugin.json, kcoderag-cursor/rules/kcoderag-navigation.mdc, kcoderag-cursor/skills/code-lookup-discipline/SKILL.md</files>
  <action>
    Run the compiled canonical generator for Cursor non-document groups. Preserve the QA MCP identity, always-on Rule, shared skill, compatibility version, project-relative/self-contained references, and no-Hook capability boundary. Keep connection values opaque and do not add Dev selection, semantic/hybrid claims, or Phase 05 precision changes. Check the same product a second time without writes.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package cursor --group metadata-config &amp;&amp; node dist/generator/index.cjs --package cursor --group metadata-guidance &amp;&amp; node dist/generator/index.cjs --package cursor --group metadata-config --check &amp;&amp; node dist/generator/index.cjs --package cursor --group metadata-guidance --check</automated>
  </verify>
  <acceptance_criteria>
    - All four Cursor files equal fresh canonical output and share exact version identity.
    - Rule/skill/MCP references resolve inside the product.
    - No Hook or public Dev behavior is claimed.
  </acceptance_criteria>
  <done>The Cursor QA navigation path is complete and honest from MCP registration through Rule and skill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close and verify the Cursor non-document product</name>
  <read_first>
    tests/generator/generation.test.cts
    kcoderag-cursor/.cursor-plugin/plugin.json
    kcoderag-cursor/rules/kcoderag-navigation.mdc
    kcoderag-cursor/skills/code-lookup-discipline/SKILL.md
  </read_first>
  <files>tests/generator/cursor-product.test.cts</files>
  <behavior>
    - Cursor non-document inventory is exactly the four files in plan frontmatter.
    - Fresh temporary generation has identical normalized bytes and version identity.
    - Dev wording/path, Hook-equivalence, missing/extra member, or secret-bearing evidence fails.
  </behavior>
  <action>
    Add `cursor-product.test.cts` with an exact relative-member set, fresh-temporary-render hash comparison, version/reference checks, and active-region QA-only/Rule-boundary assertions. Exclude `kcoderag-cursor/README.md` because Plan 14 owns documentation generation. Report only member names, versions, sizes, hashes, and boolean failures.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node --test dist-tests/generator/cursor-product.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Exact inventory and fresh-render comparison pass.
    - Dev and Hook-equivalence negative fixtures fail deterministically.
    - No MCP/config value enters test output.
  </acceptance_criteria>
  <done>The Cursor product is independently closed without overlapping QA or Dev generated ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical Cursor inputs → installed Rule/skill/MCP | Generated guidance and MCP identity control host behavior. |
| Cursor capability → public claims | Rule behavior must not be spoofed as a Hook event. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Tampering | Cursor product inventory | high | mitigate | Exact member set and fresh-render hashes reject hand edits, extras, and omissions. |
| T-04-09-02 | Spoofing | capability wording | high | mitigate | Active-region tests require Rule/skill/MCP wording and reject PreToolUse equivalence. |
| T-04-09-03 | Information Disclosure | MCP evidence | critical | mitigate | Keep values opaque and emit metadata/hashes/booleans only. |
</threat_model>

<verification>
Cursor canonical generation, exact inventory, fresh-render identity, and capability-boundary tests pass without touching QA or Dev product files.
</verification>

<success_criteria>
- This plan owns five concrete files.
- Cursor remains QA-only and self-contained.
- QA Hook and Dev retirement outputs are disjoint from this plan.
</success_criteria>

## Artifacts this phase produces

- Regenerated Cursor QA MCP, compatibility manifest, Rule, and skill.
- `tests/generator/cursor-product.test.cts`.

<output>
Create `.planning/phases/04-deployment-reliability/04-09-SUMMARY.md` when done.
</output>

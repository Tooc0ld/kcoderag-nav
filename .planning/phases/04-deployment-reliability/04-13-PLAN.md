---
phase: 04-deployment-reliability
plan: 13
type: execute
wave: 12
depends_on: ["04-12"]
files_modified:
  - src/smoke/host-smoke.cts
  - tests/smoke/host-smoke.test.cts
  - src/maintainer/pre-commit.cts
  - tests/maintainer/pre-commit.test.cts
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - tests/maintainer/ci-contract.test.cts
  - tests/maintainer/release-workflow.test.cts
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01/D-04: required smoke acquires a real package, installs QA on all three hosts, and contains no public Dev selector/product evidence."
    - "DEP-02/D-05-D-07: installed Codex/Claude commands run from root and a Unicode/space deep child; moved-copy behavior remains required automated launcher evidence."
    - "DEP-03/D-09-D-16: smoke proves status/doctor/source-conflict/preinstall/uninstall semantics with metadata-only receipts and no real user-config mutation."
    - "D-04: ordinary CI and release workflows require exactly Linux/Windows × Node 22/24 lanes; publish receives NPM_TOKEN only after all four release lanes succeed."
    - "D-04: pre-commit and CI contracts fail on QA/Cursor generation, pack, smoke, source, or retired-product drift without staging/resetting user work; the full clean local gate runs after Plan 19 generated-doc/audit work in Plan 15."
  artifacts:
    - path: "src/smoke/host-smoke.cts"
      provides: "Three-host QA lifecycle and installed root/deep Hook contract smoke"
    - path: "src/maintainer/pre-commit.cts"
      provides: "Index-immutable QA/Cursor canonical staging gate"
    - path: ".github/workflows/ci.yml"
      provides: "Ordinary exact four-lane CI contract used on the final evidence commit"
    - path: ".github/workflows/release.yml"
      provides: "Tag-only exact four-lane release gate followed by npm publish"
  key_links:
    - from: "src/smoke/host-smoke.cts"
      to: "dist/bin/kcoderag-nav.cjs"
      via: "real temporary package acquisition and selected-host project lifecycle"
      pattern: "required-contract"
    - from: ".github/workflows/ci.yml"
      to: "src/maintainer/pre-release-evidence.cts"
      via: "validator expects the exact four ordinary CI jobs for the final evidence commit SHA"
      pattern: "ubuntu|windows|22|24"
---

<objective>
Align smoke, pre-commit, ordinary CI, and release CI with the QA-only/root-Hook/source-diagnostic contract, separately from release version mechanics.

Purpose: Produce the automatic cross-platform evidence that Plan 15 must observe on the final evidence commit and release tag.
Output: Expanded three-host package smoke, QA/Cursor pre-commit gate, and exact four-lane CI/release workflow contracts.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-11-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-12-SUMMARY.md
@src/smoke/host-smoke.cts
@src/maintainer/pre-commit.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- `runHostSmoke({mode:"required-contract", packageSpec, expectedVersion})` acquires a real package and emits only closed PASS/FAIL/NOT_RUN metadata.
- Plan 11 provides final pack enforcement and pre-release/head validators; Plan 12 provides exact local 0.2.0 preparation.
- Ordinary CI and tag Release are distinct workflows, each with the same required `{ubuntu,windows} × {22,24}` test lanes; only Release owns publish.
- No required contract may be converted to NOT_RUN; Phase 06 authenticated real MCP query evidence remains outside this phase.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Expand real-package smoke through lifecycle and installed Hook evidence</name>
  <read_first>
    src/smoke/host-smoke.cts
    tests/smoke/host-smoke.test.cts
    src/cli/commands.cts
    tests/hooks/launcher.test.cts
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts</files>
  <behavior>
    - Required smoke installs/updates/statuses/doctors/uninstalls QA for Codex, Claude, and Cursor with no environment selector.
    - Codex/Claude execute the actual installed registered Hook command from project root and Unicode/space deep child and record same-project boolean/digest evidence.
    - Synthetic selected-host active conflict blocks install/update before writes while uninstall remains available for an undrifted project.
    - Cursor remains Rule/skill/MCP-only and every required contract is PASS or FAIL, never NOT_RUN.
  </behavior>
  <action>
    Update the loopback smoke harness for QA-only CLI/state/findings. Acquire a real packed/public candidate in an isolated temp npm environment, run all five commands per host, and inject synthetic selected-host source inventories/runners instead of touching real user roots. For Codex/Claude, extract only the installed command metadata needed to invoke from root/deep and persist booleans/digests rather than command/config/path bodies. Preserve official Registry/pack provenance controls, loopback MCP request metadata allow-list, Cursor's honest boundary, and Phase 06 exclusion.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:smoke &amp;&amp; npm run smoke:required</automated>
  </verify>
  <acceptance_criteria>
    - Three-host QA lifecycle, source gates, and uninstall exception pass from a real acquired package.
    - Root/deep installed Hook evidence is true for Codex/Claude with same-project fingerprints.
    - Cursor records Rule/skill/MCP evidence only.
    - No required bit is NOT_RUN and no config/command/absolute path/process body enters the receipt.
  </acceptance_criteria>
  <done>The actual package delivers the complete Phase 04 host lifecycle contract in smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make pre-commit reject partial QA/Cursor or retired Dev staging</name>
  <read_first>
    src/maintainer/pre-commit.cts
    tests/maintainer/pre-commit.test.cts
    .githooks/pre-commit
    src/generator/index.cts
    package.json
  </read_first>
  <files>src/maintainer/pre-commit.cts, tests/maintainer/pre-commit.test.cts</files>
  <behavior>
    - Relevant canonical/QA/Cursor staging runs build/generation checks and requires complete coupled groups.
    - Any staged retired Dev path is an explicit failure.
    - Unrelated staging is a fast no-op; alternate-index execution never stages, resets, or edits worktree/index bytes.
  </behavior>
  <action>
    Update canonical path groups and partial-staging rules to the new Plan 07-10 ownership map. Add exact fixtures for QA-only atomic groups, Cursor-only groups, canonical input changes, deleted Dev paths, unrelated staging, and pre-existing dirty/untracked work. Preserve alternate-index blob-OID snapshots and check-only generator behavior.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:precommit</automated>
  </verify>
  <acceptance_criteria>
    - Relevant complete staging passes and partial/retired staging fails.
    - Unrelated staging remains a fast no-op.
    - Index/worktree OIDs/bytes are identical before/after every check.
  </acceptance_criteria>
  <done>Local commits cannot reintroduce stale generated or Dev public state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close ordinary and release four-lane CI contracts</name>
  <read_first>
    .github/workflows/ci.yml
    .github/workflows/release.yml
    tests/maintainer/ci-contract.test.cts
    tests/maintainer/release-workflow.test.cts
    src/maintainer/pre-release-evidence.cts
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, tests/maintainer/ci-contract.test.cts, tests/maintainer/release-workflow.test.cts</files>
  <behavior>
    - Ordinary CI has exactly four required test identities: Ubuntu/Windows × Node 22/24, each running the complete required contract.
    - Tag Release has the same four required lanes and publish depends on all four successes.
    - NPM_TOKEN is available only to publish; ordinary CI and required lanes have no publish permission/secret.
    - Focused workflow/smoke/pre-commit/pack suites pass before Plan 19 generated docs/audits; Plan 15 runs the complete `ci:local` on the final documented implementation subject.
  </behavior>
  <action>
    Update workflow matrices/names only where required to give the validator a stable exact lane tuple and head-SHA contract. Keep ordinary CI non-publishing and tag Release publish dependent on all four required jobs, with minimal permissions and NPM_TOKEN scoped to the publish step. Extend workflow tests for exact job sets, no substitute/extra success, head/ref binding, required smoke, and secret boundary. Run focused workflow, smoke, pre-commit, and real-pack gates here. Do not claim a complete `ci:local` result before Plan 19 finishes generated documentation and docs audits; Plan 15 must run the complete gate on the frozen final implementation subject.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:ci-contract &amp;&amp; npm run test:release-workflow &amp;&amp; npm run test:smoke &amp;&amp; npm run test:precommit &amp;&amp; npm run pack:audit</automated>
  </verify>
  <acceptance_criteria>
    - Both workflows expose exact stable four-lane identities and required-contract commands.
    - Publish cannot run before all four tag lanes succeed and is the only token-bearing step.
    - Focused workflow/smoke/pre-commit/pack gates pass; full `ci:local` remains an explicit Plan 15 prerequisite after docs.
  </acceptance_criteria>
  <done>Local, ordinary remote, and tag-release assurance share one exact Phase 04 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packaged CLI → temporary smoke hosts | Real executable bytes process synthetic project/source state. |
| Git index → pre-commit | Checks must observe staged bytes without modifying user work. |
| GitHub commit/tag → CI/publish | Exact jobs and head/ref identity gate npm credentials and publication. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13-01 | Information Disclosure | smoke receipts | critical | mitigate | Persist closed metadata/booleans/digests and reject config, command, path, header, token, and subprocess bodies. |
| T-04-13-02 | Elevation of Privilege | pre-commit index handling | high | mitigate | Alternate index, exact OID snapshots, check-only commands, and no stage/reset mutation. |
| T-04-13-03 | Spoofing | CI lane identity | high | mitigate | Stable exact OS/Node job tuple and matching head/ref assertions in workflow and validator tests. |
| T-04-13-04 | Elevation of Privilege | npm token | critical | mitigate | Minimal workflow permissions and publish-step-only token after all four required lanes. |
</threat_model>

<verification>
Focused smoke/pre-commit/workflow/pack suites pass with QA-only products, root/deep Hooks, source diagnostics, exact four-lane identities, and no secret leakage; Plan 15 owns the final complete `ci:local` run after documentation.
</verification>

<success_criteria>
- Pack/release mechanics are not mixed into this plan's eight-file ownership.
- Ordinary CI can later be machine-validated on the evidence commit.
- Release publish remains fully automatic after gates and needs no human approval checkpoint.
</success_criteria>

## Artifacts this phase produces

- Expanded three-host `required-contract` smoke and tests.
- QA/Cursor/retired-Dev index-immutable pre-commit policy.
- Exact ordinary and tag-release four-lane workflow contracts.
- Focused smoke/pre-commit/workflow/pack gate evidence ready for Plan 15's final complete `ci:local` run.

<output>
Create `.planning/phases/04-deployment-reliability/04-13-SUMMARY.md` when done.
</output>

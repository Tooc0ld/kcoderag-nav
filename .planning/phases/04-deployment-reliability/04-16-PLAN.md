---
phase: 04-deployment-reliability
plan: 16
type: execute
wave: 15
depends_on: ["04-15"]
files_modified:
  - REDACTED_HISTORICAL_HEAD_PROJECT/.codex/
  - REDACTED_HISTORICAL_USER_CONFIG/.codex/
  - .planning/phases/04-deployment-reliability/04-16-HEAD-ACCEPTANCE.json
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-17/D-18: exact public 0.2.0 doctor proves Head managedDriftCount zero and one exact capability-gated degraded-owned `kcoderag-nav` marketplace cleanup fingerprint before any write, despite the known pre-cleanup list failures."
    - "D-03: the previously locked cleanup authorization is exercised non-interactively only with `--allow-owned-source-cleanup` plus the frozen SHA-256 fingerprint; any plan drift or ambiguous source stops without mutation."
    - "DEP-01/D-17-D-19: exact public 0.2.0 performs the approved native cleanup and one atomic Head QA update, then status/doctor are healthy with no active duplicate."
    - "DEP-02/D-19: root and Unicode/space deep-child installed Hook executions select the same nearest Head state and launcher fingerprints."
    - "DEP-03/D-19: only declared Head Codex managed paths and the frozen cleanup identity change; all unrelated project/host/user/sibling digests remain unchanged."
    - "D-20: failed Head migration rolls back project writes; any published defect preserves immutable 0.2.0 and becomes a 0.2.1 fix-forward."
  artifacts:
    - path: "REDACTED_HISTORICAL_HEAD_PROJECT/.codex/kcoderag-nav/install-state.json"
      provides: "Current QA-only Codex managed state installed from exact public 0.2.0"
    - path: ".planning/phases/04-deployment-reliability/04-16-HEAD-ACCEPTANCE.json"
      provides: "Closed exact artifact, zero-drift, cleanup fingerprint, health, root/deep, and unchanged-scope evidence"
    - path: ".planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json"
      provides: "Validated public 0.2.0 identity prerequisite"
  key_links:
    - from: "04-15-PUBLISH-RECEIPT.json"
      to: "npx kcoderag-nav@0.2.0"
      via: "exact version, official Registry artifact identity, gitHead/SRI/tar hash, and release SHA"
      pattern: "0.2.0"
    - from: "Head doctor finding"
      to: "Codex native cleanup"
      via: "exact KCodeRag-owned identity, supported capability, fixed argv, frozen SHA-256 fingerprint, and explicit automation authority"
      pattern: "cleanupFingerprint"
    - from: "Head root/deep cwd"
      to: "Head install-state and launcher"
      via: "installed nearest-state bootstrap with identical state/launcher digests"
      pattern: "nearestStateDigest|launcherDigest"
    - from: "04-16-HEAD-ACCEPTANCE.json"
      to: "src/maintainer/head-acceptance.cts"
      via: "closed pre-publication validator cross-checks receipt/artifact/cleanup/health/root-deep/scope evidence"
      pattern: "validateHeadAcceptance"
---

<objective>
Migrate the real, redacted `REDACTED_HISTORICAL_HEAD_PROJECT` Codex project to exact public QA-only 0.2.0 using the previously authorized, fingerprint-bound owned cleanup, then validate a closed acceptance receipt.

Purpose: Turn public artifact guarantees into the actual DEP-01/02/03 deployed outcome without a redundant approval pause or any broad/ambiguous cleanup.
Output: Exact native cleanup, atomic Head QA update, healthy status/doctor, root/deep nearest-state proof, unchanged-scope proof, and machine-validated Head acceptance.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-11-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-15-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json
@src/maintainer/head-acceptance.cts
@src/cli/commands.cts
@src/hosts/codex.cts
@src/core/transaction.cts

<interfaces>
- `npx --yes kcoderag-nav@0.2.0 <command> --host codex --target REDACTED_HISTORICAL_HEAD_PROJECT --json` is the only accepted Head lifecycle entry; latest and local tgz are invalid deployment evidence.
- `REDACTED_HISTORICAL_HEAD_PROJECT` and `REDACTED_HISTORICAL_USER_CONFIG` are neutral redactions for the exact project and user roots used by this completed historical acceptance; they do not describe a new Phase 04.2 action.
- `doctor` is read-only and returns stable safe finding/capability/cleanup-plan metadata. Only the exact known KCodeRag-owned source with supported observed capability may become cleanup-eligible.
- The actual Head pre-state is the Plan 05 degraded-owned-registration case: both Codex JSON list commands fail because marketplace `kcoderag-nav` resolves to the recognized local legacy repository source. This single closed case is reachable; other list errors remain manual-only.
- The locked D-03/D-17 discussion is prior authorization for that one exact owned cleanup. The runtime expression of authority is both `--allow-owned-source-cleanup` and its freshly frozen `--cleanup-fingerprint`; general `--yes`, release authority, and legacy migration authority are insufficient.
- `applyTransaction` owns all Head project writes. Native user cleanup is separately fingerprint-authorized, followed by a complete re-scan before project mutation.
- Plan 11 created and tested `validateHeadAcceptance` before Plan 15 publication; this plan cannot weaken its schema after seeing results.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove zero Head drift and freeze the exact cleanup fingerprint read-only</name>
  <precondition>Public exact/latest 0.2.0, v0.2.0, release SHA, artifact integrity/tar hash, and `04-15-PUBLISH-RECEIPT.json` converge and verify offline.</precondition>
  <read_first>
    .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
    src/maintainer/head-acceptance.cts
    src/cli/commands.cts
    src/hosts/codex.cts
  </read_first>
  <files>None (read-only official Registry/Head/Codex diagnosis and private temporary baseline)</files>
  <action>
    Verify the publication receipt offline, query official Registry exact 0.2.0 through isolated empty npm config, and require gitHead/SRI/tar hash/artifact identity equality. Run exact public `doctor --host codex --target REDACTED_HISTORICAL_HEAD_PROJECT --json` twice with no mutation flags. Require both observations to report the same host/target/project state, `managedDriftCount: 0`, no symlink/special/ambiguous/raw MCP/manual Hook finding, and exactly one cleanup-eligible degraded-owned registration with the closed safe shape: selected host `codex`, source type `owned_marketplace_registration`, exact marketplace name `kcoderag-nav`, normalized source path equal to the recognized local kcoderag-nav legacy repository, recognized legacy provenance ID, both pre-cleanup list failures attributed only to that registration, `codex-cli` at or above the Plan 05 floor, confirmed `codex plugin marketplace remove --help` schema, fixed argv `codex plugin marketplace remove kcoderag-nav --json`, 5-second timeout, and canonical `sha256:<64 lowercase hex>` cleanup fingerprint. Never require a successful pre-cleanup list inventory for this one degraded route. If name/path/provenance/failure attribution differs, another KCodeRag/unrelated affected source exists, capability is absent/old/failing, or the record is malformed/ambiguous, hard-stop manual-only before native/project mutation.

    Freeze the exact fingerprint plus safe identity/capability fields as the expected cleanup plan and independently capture a private closed baseline: selected managed relative paths/types/modes/digests; unrelated Head paths; Claude/Cursor project trees; sibling projects; unrelated Codex user config; public artifact identity; and declared changed-scope fingerprint. Never persist file/config/command/process/credential bodies or absolute temporary paths. A second doctor result and every frozen digest must equal the first; this exact equality is the Task 2 automation authority boundary.
  </action>
  <verify>
    <automated>$aLines=@(npx --yes kcoderag-nav@0.2.0 doctor --host codex --target 'REDACTED_HISTORICAL_HEAD_PROJECT' --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $a=($aLines | Where-Object {$_.Trim()} | Select-Object -Last 1) | ConvertFrom-Json; $bLines=@(npx --yes kcoderag-nav@0.2.0 doctor --host codex --target 'REDACTED_HISTORICAL_HEAD_PROJECT' --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $b=($bLines | Where-Object {$_.Trim()} | Select-Object -Last 1) | ConvertFrom-Json; $af=@($a.findings | Where-Object {$_.cleanupEligible -eq $true}); $bf=@($b.findings | Where-Object {$_.cleanupEligible -eq $true}); if ($a.managedDriftCount -ne 0 -or $b.managedDriftCount -ne 0 -or $af.Count -ne 1 -or $bf.Count -ne 1 -or $af[0].sourceType -ne 'owned_marketplace_registration' -or $af[0].cleanupFingerprint -notmatch '^sha256:[0-9a-f]{64}$' -or $af[0].cleanupFingerprint -ne $bf[0].cleanupFingerprint) { throw 'Head degraded-owned zero-drift/fingerprint mismatch' }</automated>
  </verify>
  <acceptance_criteria>
    - Publication receipt and official exact artifact identity match before Head inspection.
    - Both exact public doctors report managed drift count zero and the identical expected degraded-owned cleanup plan/fingerprint.
    - Host/name/normalized path/provenance/list-failure attribution/capability/schema/argv/timeout are exact; other-name/path, multiple-source, unrelated-list-error, malformed, or ambiguous cases stop unchanged.
    - Frozen baseline contains safe paths/digests/booleans only and proves every unrelated scope before mutation.
  </acceptance_criteria>
  <done>The exact public artifact, zero-drift Head project, one previously authorized owned cleanup fingerprint, and unchanged-scope baseline are frozen without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Execute only the frozen cleanup authority and atomic Head QA update</name>
  <precondition>Task 1's second doctor, public receipt/artifact identity, managed digests, capability, safe source identity, argv/scope/timeout, and cleanup fingerprint still exactly match the frozen baseline.</precondition>
  <read_first>
    Task 1 private frozen baseline and cleanup fingerprint
    .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json
    src/cli/commands.cts
    src/hosts/codex.cts
    src/core/transaction.cts
  </read_first>
  <files>REDACTED_HISTORICAL_HEAD_PROJECT/.codex/, REDACTED_HISTORICAL_USER_CONFIG/.codex/</files>
  <action>
    Replay every Task 1 public/project/source/capability/digest field immediately. Exercise the prior locked D-03/D-17 authorization by invoking exact public 0.2.0 `update --host codex --target REDACTED_HISTORICAL_HEAD_PROJECT --yes --allow-owned-source-cleanup --cleanup-fingerprint <frozen-sha256>`; the two cleanup arguments are the explicit non-interactive authorization and cannot be inferred from `--yes`. Do not add legacy Dev migration authority unless the frozen state is the exact legacy Dev case already covered by D-02 and the dedicated flag is separately present; for the expected Head QA state it must be absent.

    Require the public runtime to execute only fixed native argv `codex plugin marketplace remove kcoderag-nav --json` within five seconds; never delete or edit Codex config directly. Immediately after native success require complete `codex plugin list --json` and `codex plugin marketplace list --json` calls to both succeed and prove exact absence of `kcoderag-nav` plus no other blocking selected-host source before rendering. Only then allow exactly one `applyTransaction` state-last project update to QA/0.2.0. Do not use force, a variable marketplace name/path, global reinstall, latest, or local pack. On native timeout/failure/ambiguous result, continued list failure, or residual conflict stop before project writes and report truthful user-state metadata without raw output. On project failure require exact project rollback; if public 0.2.0 is defective, preserve release objects and open 0.2.1 fix-forward work.
  </action>
  <verify>
    <automated>if ($env:KCODERAG_NAV_CLEANUP_FINGERPRINT -notmatch '^sha256:[0-9a-f]{64}$') { throw 'missing frozen cleanup fingerprint' }; npx --yes kcoderag-nav@0.2.0 update --host codex --target 'REDACTED_HISTORICAL_HEAD_PROJECT' --yes --allow-owned-source-cleanup --cleanup-fingerprint $env:KCODERAG_NAV_CLEANUP_FINGERPRINT --json</automated>
  </verify>
  <acceptance_criteria>
    - Runtime fingerprint exactly equals Task 1; any source/capability/project/public drift stops before native cleanup.
    - Native cleanup is exactly `codex plugin marketplace remove kcoderag-nav --json`; post-removal plugin and marketplace inventories both succeed and prove exact absence before any project transaction.
    - Head project update is one QA/0.2.0 state-last transaction with exact rollback on injected/operational failure.
    - No direct, force, broad, latest, local-pack, or unrelated-host mutation occurs.
  </acceptance_criteria>
  <done>The prior cleanup authorization is consumed only for its exact fingerprint and Head moves atomically to public QA 0.2.0.</done>
</task>

<task type="auto">
  <name>Task 3: Record and machine-validate complete Head acceptance</name>
  <precondition>Task 2 succeeded; exact public receipt/artifact and frozen before-scope baseline remain available; no post-update command has changed project or user state.</precondition>
  <read_first>
    .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json
    Task 1 private baseline and Task 2 result/fingerprint
    src/maintainer/head-acceptance.cts
    tests/hooks/launcher.test.cts
    src/core/state.cts
  </read_first>
  <files>.planning/phases/04-deployment-reliability/04-16-HEAD-ACCEPTANCE.json</files>
  <action>
    Run exact 0.2.0 status and doctor and require QA/version 0.2.0, `status: healthy`, `ok: true`, zero managed drift, zero active conflict, and no cleanup-eligible duplicate; record informational residue only when D-09 marks it non-blocking. Extract the installed Codex Hook command without persisting its body and execute a safe eligible input from Head root and a Unicode/space deep child. Require both valid advisory protocol classes plus identical nearest-state and launcher digests and no outer fallback.

    Compare the frozen before/after metadata: changed scope equals only declared Head Codex managed paths plus the exact frozen cleanup identity; unrelated Head paths, Claude/Cursor project trees, sibling projects, and unrelated Codex user config have identical digests/true unchanged flags. Write `04-16-HEAD-ACCEPTANCE.json` using the Plan 11 closed schema with exact public receipt/artifact/release identity, initial zero drift, degraded-owned pre-observation code, exact safe host/name/path-provenance fingerprint inputs, frozen/replayed cleanup fingerprint, fixed native-command result code, successful post-removal plugin/marketplace inventory schema codes and exact absence, final health, root/deep state/launcher fingerprints, changed-scope fingerprint, and unrelated-scope digest booleans. Invoke `head-acceptance.cjs` against both acceptance and publication receipts; it must reject any missing/mismatched/extra/sensitive field. Commit the validated acceptance receipt separately without modifying 0.2.0/tag/latest.
  </action>
  <verify>
    <automated>node dist/maintainer/head-acceptance.cjs --verify .planning/phases/04-deployment-reliability/04-16-HEAD-ACCEPTANCE.json --publish-receipt .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json</automated>
  </verify>
  <acceptance_criteria>
    - Final exact status/doctor are healthy QA/0.2.0 with zero drift/conflict/duplicate.
    - Root/deep installed Hooks select identical nearest Head state/launcher fingerprints and valid protocol class.
    - Frozen and replayed cleanup fingerprints match; changed scope equals only declared Head Codex paths plus that source.
    - Receipt proves the known degraded pre-state transitioned through the one fixed native command to successful complete post-removal inventories with exact absence.
    - Every unrelated digest is unchanged and publication receipt/artifact identity matches acceptance exactly.
    - Closed validator passes; receipt contains no secret/config/command/log/temp-path body.
  </acceptance_criteria>
  <done>Head runs exact public QA-only 0.2.0 healthily from root/deep with no duplicate or unrelated mutation, backed by pre-defined machine validation.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Head loads current QA from root/deep cwd and diagnoses conflicts before writes | 01-19 | COVERED | Canon, implementation, split products/docs, artifact assurance, release, and real Head acceptance are planned. |
| REQ | DEP-01 | Head current managed QA, healthy, no user-level duplicate | 01-03, 07-19 | COVERED | QA-only source/products/retirement/docs/release plus exact Head health and cleanup proof. |
| REQ | DEP-02 | Root/deep Hook resolves one managed launcher | 01-04, 08, 13-17, 19 | COVERED | State/root discovery, split runtime/registration, smoke/docs, and Head proof. |
| REQ | DEP-03 | Three-host read-only source diagnosis without secret exposure/ambiguous auto-delete | 01-06, 11, 13-16, 19 | COVERED | Closed findings/capability/fingerprint model, validators, smoke/docs, and real source cleanup. |
| RESEARCH | — | No RESEARCH.md; established Phase 03.1 patterns and 04-PATTERNS.md suffice | 01-19 | COVERED | No new dependency; native commands are preflighted/versioned instead of assumed. |
| CONTEXT | D-01 | Public QA-only; Dev removed from CLI/assets/docs | 01-03, 07-19 | COVERED | Legacy Dev remains decode input only; public docs precede deletion. |
| CONTEXT | D-02 | Exact atomic legacy Dev-to-QA migration | 01-03, 10, 14, 18-19 | COVERED | Dedicated authority, exact ownership, rollback, and compatibility independent of generated Dev. |
| CONTEXT | D-03 | Confirmed native cleanup for exact owned source only | 01, 05-06, 14, 16, 19 | COVERED | Normal/degraded capability plus fingerprint authority; ambiguous/shared/incomplete sources manual-only. |
| CONTEXT | D-04 | Immutable 0.2.0 after gates, no release checkpoint | 01, 07, 10-19 | COVERED | Subject/evidence split, local/ordinary/tag/public automatic gates. |
| CONTEXT | D-05 | Upward nearest selected-host state lookup | 01, 04, 08, 13-17, 19 | COVERED | Actual registered command tested, documented, and deployed. |
| CONTEXT | D-06 | Damaged nearest boundary silently stops | 01, 04, 14, 19 | COVERED | Outer fallback unreachable after first state pathname. |
| CONTEXT | D-07 | Move/copy/rename/drive-safe project | 01-02, 04, 08, 13-14, 19 | COVERED | Relative state plus moved-copy tests/smoke/docs. |
| CONTEXT | D-08 | Reject root/home/global; no VCS requirement | 01-02, 14, 19 | COVERED | Exact target tests/canon/docs. |
| CONTEXT | D-09 | Active/owned/residue source tiers | 01, 05-06, 13-14, 19 | COVERED | Closed schema and all-host fixtures/docs. |
| CONTEXT | D-10 | Selected-host scan only; cross-host coexistence | 01, 03, 06, 13 | COVERED | Call-count/sibling-digest/lifecycle tests. |
| CONTEXT | D-11 | Raw/manual/ambiguous hard-stop without value inspection | 01, 05-06, 11, 14, 19 | COVERED | Sentinel tests, closed validators, and manual-only guidance. |
| CONTEXT | D-12 | install/update full gate; uninstall project-only; reads read-only | 01, 03, 05-06, 13 | COVERED | Controller/all-host/smoke coverage. |
| CONTEXT | D-13 | Fast status, deep doctor, mutation self-gate | 01, 05-06, 13-14, 19 | COVERED | Scan modes, call counts, smoke, docs. |
| CONTEXT | D-14 | Stable actionable findings; no doctor fix | 01, 05-06, 14, 19 | COVERED | Closed schema, capability/fixed plan, retired fix-like flags. |
| CONTEXT | D-15 | source_conflict and ok false | 01, 05-06, 13 | COVERED | State/controller/smoke assertions. |
| CONTEXT | D-16 | not_installed doctor readiness | 01, 05-06, 13-14, 19 | COVERED | Preinstall fixtures and user guidance. |
| CONTEXT | D-17 | Exact public Head sequence | 01, 11, 15-16 | COVERED | Closed validator, public receipt, exact package lifecycle. |
| CONTEXT | D-18 | Head drift hard-stop, no force workaround | 01, 03, 11, 16 | COVERED | Task 1 zero drift/fingerprint plus transaction-only update. |
| CONTEXT | D-19 | Artifact/health/duplicates/root-deep/unchanged acceptance | 01, 04, 11, 13, 16 | COVERED | Validator fixed before publish; receipt covers every predicate. |
| CONTEXT | D-20 | Rollback plus immutable 0.2.0/0.2.1 fix-forward | 01, 03, 11-12, 15-16 | COVERED | No unpublish/tag/latest rollback path. |
| CONTEXT | Deferred | Phase 05 Hook precision | — | EXCLUDED | Matcher/nudge precision is preserved. |
| CONTEXT | Deferred | Phase 06 real MCP registration/query evidence | — | EXCLUDED | Smoke/Head evidence stops at lifecycle and Hook/Rule contracts. |
| CONTEXT | Deferred | Phase 07 GSD hooks/runtime | — | EXCLUDED | No global GSD configuration is changed. |
| CONTEXT | Deferred | Phase 08 identity/HTTPS/token rotation | — | EXCLUDED | Current QA credential risk remains accepted and secret-safe. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public npm artifact → real Head execution | Network-acquired executable bytes must match verified exact 0.2.0 provenance. |
| Head/user observations → cleanup authority | Only exact capability-verified owned metadata and its fingerprint may authorize mutation. |
| Native user cleanup → project transaction | Cleanup is external/fingerprint-bound; project update remains atomic. |
| Real project/user state → acceptance receipt | Only closed safe metadata/digests/booleans may persist. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16-01 | Spoofing | exact public package | critical | mitigate | Verify receipt, Registry, gitHead, SRI, tar hash, version, tag, and release SHA before Head commands. |
| T-04-16-02 | Elevation of Privilege | user-source cleanup | critical | mitigate | Require zero drift, exact supported capability/inventory/argv/scope, frozen fingerprint, explicit paired flags, and replay. |
| T-04-16-03 | Tampering | Head migration | high | mitigate | Refuse any drift, use one state-last transaction, and compare declared/unchanged scope digests. |
| T-04-16-04 | Information Disclosure | real config/evidence | critical | mitigate | Never persist config/process bodies; closed validator accepts only allow-listed codes, digests, counts, and booleans. |
| T-04-16-05 | Repudiation | deployed acceptance | high | mitigate | Pre-defined validator binds receipt/artifact, initial drift, cleanup fingerprint, final health, root/deep, and scope evidence. |
| T-04-16-06 | Spoofing | degraded Head source | critical | mitigate | Bind exact Codex/name/normalized known path/provenance/list-failure attribution and require successful post-removal inventories before project writes. |
| T-04-16-SC | Tampering | npx exact acquisition | high | mitigate | Execute only verified owned official `kcoderag-nav@0.2.0` matching publication receipt and immutable release provenance. |
</threat_model>

<verification>
Two exact public doctors freeze zero drift and the one exact degraded-owned cleanup fingerprint; paired automation flags consume prior authorization; fixed native removal must restore complete inventories before exact update yields healthy/no-duplicate/root-deep/scope evidence; the pre-defined validator accepts the closed receipt against publication identity.
</verification>

<success_criteria>
- DEP-01 is true on real Head with exact public QA-only 0.2.0 and no active duplicate.
- DEP-02 is true for the installed Head command from root and a deep child with the same nearest state/launcher.
- DEP-03 is true for the real stale source, capability/fingerprint authority, manual-only fallbacks, and unchanged unrelated scope.
- Public 0.2.0 remains immutable and any defect follows 0.2.1 fix-forward.
</success_criteria>

## Artifacts this phase produces

- Private frozen metadata-only Head/public/source baseline and exact SHA-256 cleanup-plan fingerprint.
- Explicit non-interactive consumption of the prior D-03 authorization for that fingerprint only.
- Exact public `kcoderag-nav@0.2.0` Head QA update through one project transaction.
- Healthy exact status/doctor plus same-nearest-state root/deep Hook evidence.
- `.planning/phases/04-deployment-reliability/04-16-HEAD-ACCEPTANCE.json` validated by `validateHeadAcceptance`.

<output>
Create `.planning/phases/04-deployment-reliability/04-16-SUMMARY.md` when done.
</output>

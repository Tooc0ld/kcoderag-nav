---
phase: 04-deployment-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - AGENTS.md
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01/D-02: the active project canon defines QA as the only public environment and Dev solely as exact legacy decode input for migration/uninstall."
    - "DEP-02/D-05/D-06/D-07/D-08: the canon requires nearest-state upward Hook discovery, damaged-boundary fail-open, moved-project support, and project-only targets without a VCS prerequisite."
    - "DEP-03/D-03/D-09-D-16: the canon requires selected-host secret-safe diagnosis, explicit fingerprint-bound owned-source cleanup authority, and manual-only handling for raw/manual/ambiguous sources."
    - "D-04/D-17-D-20: the roadmap preserves automatic release gates, exact 0.2.0 Head acceptance, immutable publication, and 0.2.1 fix-forward without another release approval."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Superseding QA-only product definition and lifecycle constraints"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Concrete classified DEP-01/DEP-02/DEP-03 acceptance predicates"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 04 goal, plan sequence, success criteria, and deferred-phase fences"
    - path: "AGENTS.md"
      provides: "Managed project/stack/architecture instructions regenerated from canonical planning sources"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "AGENTS.md"
      via: "GSD managed project/stack/architecture blocks expose the same QA-only contract before implementation starts"
      pattern: "GSD:project-start|QA-only"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "DEP-01/02/03 predicates map to the Phase 04 outcome and plans"
      pattern: "DEP-01|DEP-02|DEP-03"
---

<objective>
**As a** maintainer executing Phase 04, **I want to** establish the QA-only deployment contract in canonical planning and managed agent instructions first, **so that** every implementation plan works from one non-stale source of truth.

Move the canonical product/requirement/roadmap/managed-AGENTS transition ahead of all production changes.

Purpose: Resolve the spec-less DEP edges and prevent later code, generated assets, diagnostics, release, and Head deployment from implementing the superseded QA/Dev contract.
Output: QA-only PROJECT/REQUIREMENTS/ROADMAP plus matching managed AGENTS sections.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-PATTERNS.md

<interfaces>
- D-01 through D-20 in `04-CONTEXT.md` are locked and are the precedence source for this transition.
- Phase 1-3/03.1 completion history remains factual; Phase 04/0.2.0 explicitly supersedes the public Dev contract rather than rewriting history.
- Managed `AGENTS.md` blocks derive from planning/codebase sources; unrelated user-authored regions remain untouched.
- Root README and the authoritative sibling guide are synchronized immediately in Plan 14 before any public Dev CLI/generator/asset/docs contract removal; generated README/audit tooling follows in Plan 19.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the QA-only canonical contract end to end</name>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    AGENTS.md
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <reversibility rating="one-way">D-01/D-04 define the permanent 0.2.0 public product transition; later reversal requires a new published contract and migration.</reversibility>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, AGENTS.md</files>
  <action>
    Per D-01/D-02, update the active product definition, core value, constraints, decisions, Phase 04 goal, and requirement mapping so new/public install, update, generated products, docs, and release are QA-only while exact legacy Dev remains decode-only migration/uninstall input. Convert each previously unclassified DEP-01/DEP-02/DEP-03 edge into concrete observable predicates: exact public 0.2.0 plus Head healthy/no duplicates; nearest/root/deep/moved Hook behavior with damaged-nearest fail-open; and selected-host status/doctor/source tiers with secret safety and no automatic ambiguous cleanup. Per D-03, define cleanup as an independent explicit authority bound to a frozen source-plan fingerprint, including a non-interactive automation flag path; general confirmation, release authorization, and legacy migration authority cannot substitute for it. Preserve completed historical records and the Phase 05-08 fences.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const paths=['.planning/PROJECT.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md','AGENTS.md']; const s=paths.map(p=>fs.readFileSync(p,'utf8')).join('\n'); for(const id of ['DEP-01','DEP-02','DEP-03','D-01','D-20']) if(!s.includes(id)) throw new Error('missing '+id); if(!s.includes('QA')) throw new Error('missing QA contract')"</automated>
  </verify>
  <acceptance_criteria>
    - Current product and Phase 04 requirement text exposes QA only as a public environment and clearly isolates legacy Dev compatibility.
    - DEP-01, DEP-02, and DEP-03 each contain closed, testable acceptance predicates instead of an unclassified descriptor.
    - D-03 cleanup authorization is explicitly separate, fingerprint-bound, and usable non-interactively only through an exact automation authority.
    - D-04 and D-17-D-20 preserve automatic release gates, exact Head proof, immutable release identity, and fix-forward recovery.
  </acceptance_criteria>
  <done>All later plans can consume one canonical QA-only, project-scoped, diagnosable deployment contract.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate managed AGENTS context and audit scope fences</name>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    AGENTS.md
    .planning/phases/04-deployment-reliability/04-PATTERNS.md
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Regenerate or scoped-edit only the managed project, stack, conventions, and architecture blocks so they state Node 22+/CJS, npm CLI project-only lifecycle, public QA-only behavior, legacy decode boundary, nearest Hook discovery, selected-host source diagnosis, transaction ownership, immutable 0.2.0 release gates, and sibling-guide ownership. Preserve unrelated AGENTS content byte-for-byte. Explicitly keep Phase 05 Hook precision, Phase 06 authenticated real MCP queries, Phase 07 global GSD hook work, Phase 08 identity/HTTPS/token rotation, and OpenCode behavior outside this phase.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('node:fs'); const a=fs.readFileSync('AGENTS.md','utf8'); for(const marker of ['GSD:project-start','GSD:stack-start','GSD:architecture-start','QA']) if(!a.includes(marker)) throw new Error('managed AGENTS missing '+marker); const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); for(const id of ['DEP-01','DEP-02','DEP-03']) if(!r.includes(id)) throw new Error('missing '+id)"</automated>
  </verify>
  <acceptance_criteria>
    - Managed AGENTS blocks match PROJECT/REQUIREMENTS/ROADMAP and D-01 through D-20.
    - No unrelated AGENTS region changes.
    - Deferred Phase 05-08/OpenCode scope remains pending and unclaimed.
    - README and sibling guide remain untouched here and are ordered by Plan 14 before every public Dev contract removal.
  </acceptance_criteria>
  <done>The executor-facing contract is synchronized before any Phase 04 production implementation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locked context → canonical planning | D-01 through D-20 must not be weakened or silently reinterpreted. |
| Canonical planning → managed AGENTS | Executors consume managed instructions before touching production code. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Tampering | locked decisions | high | mitigate | Cite D-01 through D-20 in concrete predicates and preserve deferred fences before implementation plans run. |
| T-04-01-02 | Spoofing | managed AGENTS contract | high | mitigate | Regenerate only managed blocks from canonical sources and verify required identifiers across all four artifacts. |
| T-04-01-03 | Repudiation | historical roadmap | medium | mitigate | Preserve completed historical records and record Phase 04 as the explicit superseding transition. |
| T-04-01-04 | Information Disclosure | planning examples | high | mitigate | Describe credential handling abstractly and never copy MCP values, headers, tokens, or config bodies into canon. |
</threat_model>

<verification>
Canonical planning and managed AGENTS agree on QA-only public behavior, exact legacy compatibility, source-cleanup authority, root discovery, release/Head acceptance, and deferred boundaries before any implementation plan can start.
</verification>

<success_criteria>
- DEP-01/DEP-02/DEP-03 are concrete and classified.
- All implementation plans depend directly or transitively on this Wave 1 contract.
- Public README/sibling-guide work is explicitly ordered immediately after this canonical contract and before all Dev CLI/generator/asset/docs removal; generated docs/audits remain later.
</success_criteria>

## Artifacts this phase produces

- QA-only `.planning/PROJECT.md` product and constraint contract.
- Concrete DEP-01/DEP-02/DEP-03 predicates in `.planning/REQUIREMENTS.md`.
- Phase 04 plan/success/deferred mapping in `.planning/ROADMAP.md`.
- Synchronized managed project/stack/conventions/architecture blocks in `AGENTS.md`.

<output>
Create `.planning/phases/04-deployment-reliability/04-01-SUMMARY.md` when done.
</output>

---
phase: 04-deployment-reliability
plan: 08
type: execute
wave: 6
depends_on: ["04-04", "04-07"]
files_modified:
  - kcoderag-qa/hooks/grep-nudge.cjs
  - kcoderag-qa/hooks/run_hook.cmd
  - kcoderag-qa/hooks/run_hook.sh
  - kcoderag-qa/hooks/update-check.cjs
  - kcoderag-qa/hooks/update-worker.cjs
autonomous: true
requirements: [DEP-01, DEP-02]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: the generated QA Hook runtime is self-contained, version-aligned, and has no public Dev path or selection."
    - "DEP-02/D-05-D-07: generated Codex/Claude launchers perform upward nearest-state discovery and silently fail open at a damaged nearest boundary."
    - "D-04: fresh canonical runtime generation is byte-deterministic on Node 22/24 and Windows/Linux."
  artifacts:
    - path: "kcoderag-qa/hooks/grep-nudge.cjs"
      provides: "Self-contained advisory Hook runtime"
    - path: "kcoderag-qa/hooks/run_hook.cmd"
      provides: "Windows nearest-state fail-open launcher"
    - path: "kcoderag-qa/hooks/run_hook.sh"
      provides: "POSIX nearest-state fail-open launcher"
    - path: "kcoderag-qa/hooks/update-check.cjs"
      provides: "Foreground local-cache-only update awareness"
    - path: "kcoderag-qa/hooks/update-worker.cjs"
      provides: "Detached bounded npm refresh worker"
  key_links:
    - from: "src/generator/index.cts"
      to: "kcoderag-qa/hooks/"
      via: "compiled canonical QA runtime projection"
      pattern: "runtime-code"
    - from: "run_hook.cmd/run_hook.sh"
      to: "grep-nudge.cjs"
      via: "nearest install-state selects its sibling runtime"
      pattern: "install-state|grep-nudge"
---

<objective>
Regenerate only the executable QA Hook runtime from Plan 07 canonical inputs and prove the root/deep path is self-contained.

Purpose: Give runtime generation disjoint ownership from QA registration/guidance and keep this plan to five concrete files.
Output: QA advisory CJS, update runtime, and Windows/POSIX launchers.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-04-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-07-SUMMARY.md
@src/generator/index.cts
@tests/hooks/launcher.test.cts

<interfaces>
- Plan 04 owns canonical root discovery and Hook tests; Plan 07 projects it through explicit generator groups.
- This plan owns exactly the five executable QA Hook files; Plan 17 owns QA registrations, MCP metadata, agent, skill, and product inventory.
- Generated runtime is directly executable CJS/launchers on Node 22+ with no Python, Git checkout, runtime compiler, or public Dev dependency.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Regenerate one complete QA Hook runtime path</name>
  <read_first>
    src/generator/index.cts
    kcoderag-qa/hooks/grep-nudge.cjs
    kcoderag-qa/hooks/update-check.cjs
    kcoderag-qa/hooks/update-worker.cjs
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>kcoderag-qa/hooks/grep-nudge.cjs, kcoderag-qa/hooks/update-check.cjs, kcoderag-qa/hooks/update-worker.cjs</files>
  <action>
    Per D-01/D-04, regenerate the QA advisory runtime and asynchronous update modules from the canonical `runtime-code` input. Preserve bounded parsing, empty-output exit-zero failures, nearest-state-selected runtime behavior, foreground local-cache-only lookup, and detached bounded npm worker behavior. Treat generated bytes as products and never hand-edit them. Assert the output contains no Python, Git checkout, runtime TypeScript, public Dev selector, or absolute project root.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-code &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-code --check &amp;&amp; npm run test:hooks</automated>
  </verify>
  <acceptance_criteria>
    - All three runtime files equal a fresh check-only render.
    - Eligible input emits only a valid advisory response; malformed, oversized, or update failures emit nothing and exit zero.
    - Foreground execution performs no network request and contains no retired runtime dependency.
  </acceptance_criteria>
  <done>The canonical implementation reaches a real self-contained QA Hook runtime without registration-layer coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Windows and POSIX nearest-state launchers</name>
  <read_first>
    src/generator/index.cts
    kcoderag-qa/hooks/run_hook.cmd
    kcoderag-qa/hooks/run_hook.sh
    tests/hooks/launcher.test.cts
  </read_first>
  <files>kcoderag-qa/hooks/run_hook.cmd, kcoderag-qa/hooks/run_hook.sh</files>
  <action>
    Regenerate both launchers from the canonical launcher group. Per D-05-D-07, start at event cwd, walk upward, stop at the nearest selected-host state pathname, fail open without outer fallback if that nearest boundary is damaged, and resolve runtime siblings from state-relative paths so move/copy/rename and Unicode/space directories remain usable. Preserve Windows/POSIX quoting and suppress operational stderr/stdout contamination.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/generator/index.cjs --package qa --group runtime-code --check &amp;&amp; npm run test:launcher</automated>
  </verify>
  <acceptance_criteria>
    - Windows and POSIX bytes match canonical output.
    - Root, deep Unicode/space, moved-copy, and damaged-nearest fixtures pass with no absolute install path.
    - Missing Node/runtime/state and malformed input remain silent exit-zero.
  </acceptance_criteria>
  <done>Generated QA launchers select exactly one nearest managed runtime and always fail open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical generator → executable QA files | Generated CJS and launchers execute inside user projects. |
| Event cwd/state → runtime selection | Untrusted paths must not escape the nearest managed boundary. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Tampering | generated QA runtime | high | mitigate | Fresh-render checks reject missing or hand-edited executable bytes. |
| T-04-08-02 | Elevation of Privilege | launcher traversal | high | mitigate | Resolve only validated state-relative siblings at the nearest boundary and test traversal/symlink failures. |
| T-04-08-03 | Information Disclosure | Hook output | high | mitigate | Emit only closed advisory JSON or empty stdout; suppress runtime/network diagnostics. |
| T-04-08-04 | Denial of Service | update awareness | medium | mitigate | Foreground reads bounded cache only and spawns a detached, timeout-bounded worker. |
</threat_model>

<verification>
Fresh runtime generation/check plus Hook and launcher suites prove deterministic, self-contained, nearest-state, fail-open execution without touching QA registration, Cursor, or Dev trees.
</verification>

<success_criteria>
- This plan owns exactly five concrete files.
- QA runtime and both launchers are deterministic and self-contained.
- Registration/guidance ownership is deferred to Plan 17.
</success_criteria>

## Artifacts this phase produces

- `kcoderag-qa/hooks/grep-nudge.cjs`.
- `kcoderag-qa/hooks/update-check.cjs` and `update-worker.cjs`.
- `kcoderag-qa/hooks/run_hook.cmd` and `run_hook.sh`.

<output>
Create `.planning/phases/04-deployment-reliability/04-08-SUMMARY.md` when done.
</output>

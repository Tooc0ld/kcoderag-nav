---
phase: 04-deployment-reliability
plan: 15
type: execute
wave: 14
depends_on: ["04-19"]
files_modified:
  - .planning/phases/04-deployment-reliability/04-REVIEW.md
  - .planning/phases/04-deployment-reliability/04-SECURITY.md
  - .planning/phases/04-deployment-reliability/04-PRE-RELEASE-VERIFICATION.md
  - package.json
  - package-lock.json
  - kcoderag-qa/.codex-plugin/plugin.json
  - kcoderag-qa/.claude-plugin/plugin.json
  - kcoderag-cursor/.cursor-plugin/plugin.json
  - .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04: REVIEW/SECURITY/PRE-RELEASE verdicts bind one immutable implementation subject SHA/tree and are committed in a separate evidence-only child; no artifact binds to its own commit."
    - "D-04: the final evidence commit is origin/master and passes exactly ordinary Ubuntu/Windows × Node 22/24 CI before version mutation."
    - "D-04: the audited helper alone creates the direct-child five-path 0.2.0 commit/tag after exact dry-run and complete replayed gates; no human release checkpoint is added."
    - "DEP-01/D-17: official Registry exact/latest, gitHead, SRI/tar hash, tag Release lanes, and fresh QA-only three-host smoke converge in `04-15-PUBLISH-RECEIPT.json`."
    - "D-20: after publication, 0.2.0/tag/latest are immutable; defects become 0.2.1 without unpublish, force push, tag replacement, or dist-tag rollback."
  artifacts:
    - path: ".planning/phases/04-deployment-reliability/04-REVIEW.md"
      provides: "CLEAN code-review verdict bound to the implementation subject SHA/tree"
    - path: ".planning/phases/04-deployment-reliability/04-SECURITY.md"
      provides: "SECURED ASVS-L1 verdict bound to the same subject with zero open high/critical threats"
    - path: ".planning/phases/04-deployment-reliability/04-PRE-RELEASE-VERIFICATION.md"
      provides: "PASS DEP/D-01-D-16 verdict bound to the same subject"
    - path: "package.json"
      provides: "Root-only public version advanced from 0.1.8 to 0.2.0 by the audited helper"
    - path: ".planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json"
      provides: "Closed public tag/workflow/Registry/artifact/exact/latest QA-only evidence"
  key_links:
    - from: "immutable implementation subject SHA/tree"
      to: "evidence-only commit"
      via: "three bound verdict files, direct-parent relation, and exact three-path diff"
      pattern: "subjectSha|subjectTree|evidenceCommit"
    - from: "final evidence commit"
      to: "ordinary CI"
      via: "origin/master/head and exact four required successful job identities"
      pattern: "ubuntu|windows|22|24"
    - from: "v0.2.0 tag"
      to: "npm kcoderag-nav@0.2.0"
      via: "tag Release four lanes, dependent publish, Registry identity, and required smoke"
      pattern: "04-15-PUBLISH-RECEIPT.json"
---

<objective>
Publish immutable `kcoderag-nav@0.2.0` from an independently reviewed implementation subject, with evidence in a separate commit and ordinary final-evidence CI before version/tag mutation.

Purpose: Eliminate self-attesting review evidence while retaining every automatic local, remote, pack, smoke, release, and public-artifact gate.
Output: Subject-bound verdicts, an evidence-only four-lane-green commit, five-path 0.2.0 release/tag, successful publication, and a closed public receipt.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-11-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-12-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-13-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-14-SUMMARY.md
@.planning/phases/04-deployment-reliability/04-19-SUMMARY.md
@src/maintainer/pre-release-evidence.cts
@src/maintainer/release.cts
@src/maintainer/publish-receipt.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- Plan 11 provides `validatePreReleaseEvidence`; Plan 12 proves exact five-path release preparation; Plan 13 fixes exact ordinary/tag four-lane job identities; Plan 14 establishes early public docs; Plan 19 completes generated docs and audits.
- The implementation subject is frozen before any evidence artifact exists. Its Git tree contains all production/source/test/generated/docs changes to be reviewed and published.
- The evidence commit's direct parent is the subject and its only changed paths are the three evidence files. Ordinary CI runs on that evidence commit, whose production/package tree equals the subject.
- `prepareRelease` creates a local commit/tag only and never pushes/publishes. The tag-only Release workflow owns npm publication after four required lanes.
- D-04 authorizes exact 0.2.0 after automatic gates; REVERSIBILITY_GATES=false means no release approval checkpoint.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the implementation subject, commit evidence separately, and pass ordinary CI</name>
  <precondition>All implementation plans 01-14 and 17-19 plus their summaries are committed on local master; the index is empty; origin/master is an ancestor; GitHub CLI/npm authentication are available without printing credentials; no 0.2.0 package or v0.2.0 tag exists.</precondition>
  <read_first>
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
    .planning/phases/04-deployment-reliability/04-14-SUMMARY.md
    .planning/phases/04-deployment-reliability/04-19-SUMMARY.md
    src/maintainer/pre-release-evidence.cts
    .github/workflows/ci.yml
    package.json
  </read_first>
  <reversibility rating="costly">Pushing subject-bound evidence creates shared audit/CI history; correction creates a new subject/evidence pair without rewriting history.</reversibility>
  <files>.planning/phases/04-deployment-reliability/04-REVIEW.md, .planning/phases/04-deployment-reliability/04-SECURITY.md, .planning/phases/04-deployment-reliability/04-PRE-RELEASE-VERIFICATION.md</files>
  <action>
    Require no unexpected tracked/index change and distinguish/preserve the known unrelated dirty/untracked planning/GSD baseline. Freeze current HEAD as `subjectSha` and `git rev-parse HEAD^{tree}` as `subjectTree`; record them outside the repository while the audits run. Run the configured Phase 04 code review over the subject tree and create `04-REVIEW.md` with exact `CLEAN`, subject SHA/tree, covered range, and zero open findings. Run the ASVS-L1 security auditor and create `04-SECURITY.md` with exact `SECURED`, identical subject binding, and zero open high/critical threats. Run goal-backward verification and create `04-PRE-RELEASE-VERIFICATION.md` with exact `PASS`, identical subject binding, DEP-01/02/03 and D-01 through D-16 coverage. Any finding/gap fixes production first, commits a new implementation subject, discards stale uncommitted evidence, and reruns all three audits; never waive or edit a verdict to pass.

    Commit exactly those three evidence paths in one evidence commit whose direct parent is `subjectSha`; require `git diff --name-only subjectSha..evidenceCommit` to equal the closed three-path set and `git rev-parse evidenceCommit^{tree}` to differ only by those evidence files. Run `npm run ci:local` and exact 0.2.0 dry-run, then push master without force. Wait for the unique ordinary CI run whose event/ref/head is the evidence commit and require exactly successful `{ubuntu,windows} × {22,24}` required jobs. Require local HEAD and origin/master equal the evidence commit. Feed only normalized job IDs/OS/Node/head/conclusions to `pre-release-evidence.cjs`; require it to validate verdicts, common subject, direct-parent/evidence-only delta, final ref/head, and exact jobs. Confirm official Registry latest remains 0.1.8 and local/remote v0.2.0 plus npm 0.2.0 remain absent. Inspect only NPM_TOKEN name-presence boolean.
  </action>
  <verify>
    <automated>npm run ci:local; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $lines=@(npm run release:minor -- --dry-run --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $r=($lines | Where-Object {$_.Trim()} | Select-Object -Last 1) | ConvertFrom-Json; if ($r.previousVersion -ne '0.1.8' -or $r.version -ne '0.2.0' -or $r.tag -ne 'v0.2.0' -or @($r.releasePaths).Count -ne 5) { throw 'dry-run mismatch' }; node dist/maintainer/pre-release-evidence.cjs --verify --review .planning/phases/04-deployment-reliability/04-REVIEW.md --security .planning/phases/04-deployment-reliability/04-SECURITY.md --verification .planning/phases/04-deployment-reliability/04-PRE-RELEASE-VERIFICATION.md --from-git --require-remote origin/master --require-ci-evidence $env:KCODERAG_NAV_CI_EVIDENCE</automated>
  </verify>
  <acceptance_criteria>
    - All three verdict files bind the same immutable subject SHA/tree and contain clean/secured/pass closed verdicts.
    - Evidence commit parent is subject and its diff contains exactly the three evidence paths; no evidence artifact names itself as subject.
    - Local HEAD, origin/master, ordinary CI head, and exactly four successful job tuples equal the evidence commit.
    - Local full gate and exact five-path 0.2.0 dry-run pass; no version/tag/npm mutation occurs.
    - Any audit/subject/evidence/ref/job drift hard-stops and requires a fresh subject/evidence pair.
  </acceptance_criteria>
  <done>An independently attested implementation subject is carried by one evidence-only, ordinary-four-lane-green final pre-release commit.</done>
</task>

<task type="auto">
  <name>Task 2: Create only the direct-child five-path 0.2.0 commit and tag</name>
  <precondition>Task 1 machine validation still passes; final evidence commit remains local HEAD/origin/master; official 0.2.0 and local/remote v0.2.0 are absent; repository baseline is unchanged.</precondition>
  <read_first>
    package.json
    package-lock.json
    src/maintainer/release.cts
    .planning/phases/04-deployment-reliability/04-PRE-RELEASE-VERIFICATION.md
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <reversibility rating="one-way">D-04/D-20 make the 0.2.0 release commit/tag permanent; correction after publication is 0.2.1 fix-forward.</reversibility>
  <files>package.json, package-lock.json, kcoderag-qa/.codex-plugin/plugin.json, kcoderag-qa/.claude-plugin/plugin.json, kcoderag-cursor/.cursor-plugin/plugin.json</files>
  <action>
    Replay Task 1's validator, remote/head/jobs, Registry/tag absence, worktree/index baseline, full local gate, and exact dry-run. Invoke only `npm run release:minor -- --yes --json`; do not hand-edit versions or create a manual commit/tag. Require the new release commit's direct parent to be the final evidence commit, its diff exactly the five Plan 12 paths, every version exactly 0.2.0, local v0.2.0 targeting that commit, an empty index, and byte-identical unrelated planning/user/sibling state. Keep all earlier tags/versions/receipts immutable.
  </action>
  <verify>
    <automated>$parent=(git rev-parse HEAD).Trim(); $lines=@(npm run release:minor -- --yes --json); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $r=($lines | Where-Object {$_.Trim()} | Select-Object -Last 1) | ConvertFrom-Json; if ($r.ok -ne $true -or $r.previousVersion -ne '0.1.8' -or $r.version -ne '0.2.0' -or $r.tag -ne 'v0.2.0' -or @($r.releasePaths).Count -ne 5) { throw 'release mismatch' }; if ((git rev-parse HEAD^).Trim() -ne $parent -or (git rev-parse v0.2.0).Trim() -ne (git rev-parse HEAD).Trim()) { throw 'release parent/tag mismatch' }</automated>
  </verify>
  <acceptance_criteria>
    - Helper alone creates one direct-child five-path release commit and matching local tag.
    - All five version fields equal 0.2.0 and no other path enters the commit.
    - Unrelated baseline and historical public/local objects remain unchanged.
    - Any replay/helper failure preserves the pre-release state without a partial release.
  </acceptance_criteria>
  <done>Local immutable v0.2.0 is the exact five-path child of the validated evidence commit.</done>
</task>

<task type="auto">
  <name>Task 3: Publish through tag four-lane gates and record exact public evidence</name>
  <precondition>Local v0.2.0/HEAD identify Task 2 release commit; origin/master remains its parent evidence commit; npm 0.2.0 and remote v0.2.0 are absent.</precondition>
  <read_first>
    .github/workflows/release.yml
    src/maintainer/publish-receipt.cts
    src/smoke/host-smoke.cts
    package.json
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>.planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json</files>
  <action>
    Push the release commit to origin/master and push only v0.2.0 without force. Discover/watch the unique tag-triggered Release run for `refs/tags/v0.2.0` and the exact release SHA; require exact successful Ubuntu/Windows × Node 22/24 jobs plus the dependent publish job. After bounded Registry convergence, query exact 0.2.0 and latest through isolated empty npm configs pinned to the official Registry; require version, gitHead, bin, engine, repository, canonical tarball URL, SRI, and tar hashes to agree. Run fresh exact `kcoderag-nav@0.2.0` and latest-expected-0.2.0 required-contract smokes; all QA-only three-host lifecycle evidence must PASS. Write only closed metadata/booleans/digests to `04-15-PUBLISH-RECEIPT.json`, including implementation subject, evidence commit, release commit/tag, exact job identities, Registry identity, artifact identity, and smoke identity; verify offline and commit the receipt separately. If anything fails after publication, preserve npm/tag/latest and open a 0.2.1 fix-forward gap per D-20.
  </action>
  <verify>
    <automated>node dist/maintainer/publish-receipt.cjs --verify .planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node dist/smoke/host-smoke.cjs --mode required-contract --package-spec kcoderag-nav@0.2.0 --expected-version 0.2.0; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node dist/smoke/host-smoke.cjs --mode required-contract --package-spec kcoderag-nav@latest --expected-version 0.2.0</automated>
  </verify>
  <acceptance_criteria>
    - Unique Release event/ref/head matches v0.2.0 and exact four required jobs plus publish succeed.
    - Official Registry exact/latest/gitHead/SRI/tar hash converge on the release commit/artifact.
    - Fresh exact/latest smokes are all PASS and contain no Dev public path or required NOT_RUN.
    - Receipt verifies offline, preserves subject/evidence/release distinctions, and contains no secret/config/log/process/temp-path value.
    - D-20 remains fix-forward only; no immutable public object is removed or rewritten.
  </acceptance_criteria>
  <done>Public exact/latest 0.2.0 is immutable, four-lane-green, QA-only, and backed by a closed publication receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Implementation subject → evidence commit | Review artifacts must attest to their parent subject, not themselves. |
| Evidence commit → ordinary CI | Exact final pre-release ref/head and four job identities must converge. |
| Release helper → commit/tag | Permanent version history is created from one exact five-path write-set. |
| Tag SHA → Release workflow → npm | Publication authority is reachable only after four required jobs. |
| Official Registry/artifact → receipt/smoke | Network-provided bytes and metadata must match immutable provenance. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15-01 | Spoofing | evidence subject | critical | mitigate | Freeze subject SHA/tree first; require identical verdict bindings and a direct-child three-path evidence-only commit. |
| T-04-15-02 | Tampering | final ordinary CI | critical | mitigate | Require local/remote evidence head and exact successful four-lane job tuples through the machine validator. |
| T-04-15-03 | Elevation of Privilege | release write-set | high | mitigate | Use only audited helper, exact five paths, clean index, no force, and replay all gates before mutation. |
| T-04-15-04 | Spoofing | Registry artifact | critical | mitigate | Pin official Registry and bind exact/latest, gitHead, canonical URL, SRI, tar hash, tag jobs, and fresh smoke. |
| T-04-15-05 | Information Disclosure | credentials/evidence | critical | mitigate | Inspect secret-name presence only; isolate npm configs; persist closed metadata and reject secret-like values recursively. |
| T-04-15-SC | Tampering | public npm acquisition | high | mitigate | Verify owned exact 0.2.0 tag/gitHead/SRI/tar hash and workflow provenance before executing fresh smokes. |
</threat_model>

<verification>
The implementation subject has independent bound verdicts; its evidence-only child passes exact ordinary four-lane CI; the helper creates five-path v0.2.0; tag Release publishes after four lanes; Registry exact/latest and fresh package smokes converge in the offline-valid receipt.
</verification>

<success_criteria>
- Evidence never self-binds and any production fix creates a fresh subject/evidence pair.
- Final evidence commit, remote head, and ordinary CI job set are machine validated before version mutation.
- Public 0.2.0 is exact, immutable, QA-only, and authorized without a redundant human release checkpoint.
</success_criteria>

## Artifacts this phase produces

- Immutable implementation subject SHA/tree.
- Three subject-bound verdict files in one evidence-only child commit.
- Exact successful ordinary four-lane CI on the evidence commit.
- One helper-created five-path 0.2.0 release commit/tag.
- One successful tag Release with four required jobs and dependent publish.
- `.planning/phases/04-deployment-reliability/04-15-PUBLISH-RECEIPT.json`.

<output>
Create `.planning/phases/04-deployment-reliability/04-15-SUMMARY.md` when done.
</output>

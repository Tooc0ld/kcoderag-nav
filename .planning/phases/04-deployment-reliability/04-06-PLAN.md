---
phase: 04-deployment-reliability
plan: 06
type: execute
wave: 7
depends_on: ["04-05"]
files_modified:
  - src/hosts/claude.cts
  - src/hosts/cursor.cts
  - src/hosts/user-sources.cts
  - src/hosts/index.cts
  - src/cli/commands.cts
  - tests/hosts/claude.test.cts
  - tests/hosts/cursor.test.cts
  - tests/hosts/cross-host.test.cts
  - tests/cli/commands.test.cts
autonomous: true
requirements: [DEP-03]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "DEP-03/D-09/D-11: Claude Code and Cursor classify the same source tiers without reading, comparing, logging, or snapshotting MCP values."
    - "D-03/D-14: Claude uses exact scoped plugin uninstall after a versioned read-only capability preflight; marketplace removal additionally requires complete exclusive ownership, while absent/ambiguous capability and Cursor stay manual-only."
    - "D-10: each command scans only its selected host; active sources in other hosts do not block install/update/uninstall."
    - "D-12/D-13: all three hosts share fast status, deep doctor, full install/update gates, and project-only uninstall semantics."
    - "D-14/D-16: preinstall doctor output is stable, actionable, read-only, and consistent across hosts without a fix option."
  artifacts:
    - path: "src/hosts/claude.cts"
      provides: "Claude selected-host user-source scanner and native uninstall plans"
    - path: "src/hosts/cursor.cts"
      provides: "Cursor selected-host source scanner with honest cleanup capability boundary"
    - path: "tests/hosts/cross-host.test.cts"
      provides: "cross-host scan isolation and source-gate coexistence proof"
  key_links:
    - from: "src/hosts/index.cts"
      to: "selected HostAdapter.scanUserSources"
      via: "fixed one-host registry dispatch"
      pattern: "HOST_ADAPTERS"
    - from: "Claude/Cursor findings"
      to: "src/cli/commands.cts"
      via: "the shared fast/deep/gate orchestration and truthful JSON formatter"
      pattern: "source_conflict"
---

<objective>
Expand the source-diagnostic and cleanup-authority contract to Claude Code and Cursor, then prove one-host scanning and cross-host coexistence end to end.

Purpose: Complete DEP-03 without letting host-specific APIs leak into core mutation logic or permitting ambiguous cleanup.
Output: Three-host source diagnostics, honest native cleanup commands, and selected-host isolation regressions.
</objective>

<execution_context>
@C:/Users/kingsoft/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/kingsoft/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-PATTERNS.md
@.planning/phases/04-deployment-reliability/04-05-SUMMARY.md
@src/hosts/user-sources.cts
@src/hosts/claude.cts
@src/hosts/cursor.cts
@src/hosts/index.cts

<interfaces>
- Plan 05 provides `SourceFinding`, three `SourceScanMode` values, fixed-argv cleanup plans, and the controller's independent cleanup authority.
- The observed supported Claude baseline is 2.1.241. Its read-only plugin inventory is a top-level JSON array whose objects contain `id`, `version`, `scope`, `enabled`, `installPath`, `installedAt`, and `lastUpdated`; marketplace inventory is a top-level JSON array whose objects contain `name`, `source`, `repo`, and `installLocation`.
- The only preferred plugin cleanup argv is `claude plugin uninstall PLUGIN@MARKETPLACE --scope user|project|local` with a 5-second timeout. Marketplace removal is broader and is eligible only after complete inventories prove the marketplace plus every affected plugin exclusively KCodeRag-owned.
- Official help/docs guide capability discovery only; observed version and fixture-backed exact JSON/list parsing are mandatory before any destructive command. Absent, old, failing, incomplete, or ambiguous capability is manual-cleanup-only.
- Cursor has no assumed equivalent CLI. A filesystem legacy record is not automatically a host-native plugin uninstall; when no verified native capability exists, report manual cleanup only.
- One selected adapter per command remains non-negotiable; source enumeration must not probe another host's user roots.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Claude Code source diagnosis and native owned-plugin cleanup</name>
  <read_first>
    src/hosts/claude.cts
    src/hosts/user-sources.cts
    tests/hosts/claude.test.cts
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>src/hosts/claude.cts, src/hosts/user-sources.cts, tests/hosts/claude.test.cts</files>
  <behavior>
    - Fast scan finds active Claude duplicate plugin/marketplace/raw MCP/manual Hook conflicts; deep scan also reports cache/disabled residue.
    - Exact owned plugin identity exposes `claude plugin uninstall PLUGIN@MARKETPLACE --scope SCOPE`; marketplace removal is eligible only under complete exclusive ownership.
    - Ambiguous/manual/raw sources are never cleanup-eligible and block install/update before project render.
    - Timeout/non-zero/parse failure remains code/path-only and project bytes stay unchanged.
  </behavior>
  <action>
    Implement Claude user-source enumeration using bounded project-independent host metadata and injected non-shell native CLI calls. Within a 5-second timeout per call, run `claude --version`; verify `claude plugin list --help`, `claude plugin marketplace list --help`, `claude plugin uninstall --help`, and `claude plugin marketplace remove --help` expose JSON inventory, `PLUGIN@MARKETPLACE`, and `--scope user|project|local`; then run the two JSON inventories. Require version 2.1.241 or newer and the exact fixture-backed top-level arrays/field types declared in this plan before any cleanup becomes eligible. Classify by plugin/marketplace identity, KCodeRag MCP keys, hook signatures, enabled state, and safe locations only; never read or compare URL/header/Bearer values or persist raw CLI bodies.

    Prefer exact argv `claude plugin uninstall PLUGIN@MARKETPLACE --scope SCOPE`, with scope taken from the validated `user|project|local` inventory item. Consider `claude plugin marketplace remove MARKETPLACE --scope SCOPE` only when the marketplace inventory is complete and the complete plugin inventory proves both the registration and every affected plugin are exclusively KCodeRag-owned; a shared marketplace, missing scope, incomplete inventory, unrecognized version/schema, or unavailable command is a manual-cleanup-only hard stop. Reuse Plan 05's canonical cleanup fingerprint, explicit authority, timeout, and re-scan flow. Treat official docs as guidance rather than runtime proof. Add fixtures for every exact shape, unknown fields/types, active/disabled/cache/raw/manual sources, shared marketplace, owned cleanup success/failure, and preinstall readiness.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:claude</automated>
  </verify>
  <acceptance_criteria>
    - Claude active duplicates produce source_conflict/ok-false and block project mutations before render.
    - Only exact owned plugin records with supported observed capability expose scoped native uninstall; marketplace removal additionally proves complete exclusive ownership.
    - Old/absent/failing/ambiguous capability and incomplete inventory produce manual-cleanup-only findings and no subprocess mutation.
    - Deep informational residue does not block installation.
    - Sentinel secrets and subprocess bodies are absent from all outputs and errors.
  </acceptance_criteria>
  <done>Claude Code has the same safe diagnostic tiers and independently authorized native cleanup behavior as Codex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Cursor diagnosis without inventing an unsafe cleanup equivalent</name>
  <read_first>
    src/hosts/cursor.cts
    src/hosts/user-sources.cts
    tests/hosts/cursor.test.cts
    .planning/phases/04-deployment-reliability/04-PATTERNS.md
  </read_first>
  <files>src/hosts/cursor.cts, src/hosts/user-sources.cts, tests/hosts/cursor.test.cts</files>
  <behavior>
    - Cursor fast/deep scans classify active project-external plugin/MCP/Rule/manual residue by safe metadata only.
    - Existing exact filesystem legacy ownership may be diagnosed, but automatic cleanup is offered only if a verified Cursor native uninstall capability exists.
    - Raw MCP/Rule/manual/ambiguous records hard-stop install/update and remain manual cleanup only.
    - Cursor project QA status/doctor semantics match other hosts without claiming a Hook capability.
  </behavior>
  <action>
    Refactor the existing Cursor legacy observation so source diagnosis does not call its credential-comparison helper. Treat safe plugin names, state/digest ownership, enabled registration, Rule/skill paths, and MCP keys as metadata; if ownership cannot be proven without sensitive values, emit ambiguous conflict. Preserve the existing separately authorized project-plus-user legacy migration only for its exact already-managed filesystem contract. Do not label that flow a native plugin cleanup or output a copyable automatic command unless a real Cursor host command is verified. Add sentinel-secret and manual-only regressions.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:host:cursor</automated>
  </verify>
  <acceptance_criteria>
    - Cursor detects active conflicting sources and informational residue without inspecting MCP values.
    - No fabricated native cleanup command is emitted.
    - Exact legacy managed migration remains authority-bound and compensating; ambiguous sources stay manual.
    - Cursor continues to expose Rule/skill/MCP semantics only.
  </acceptance_criteria>
  <done>Cursor participates honestly in source diagnosis while retaining its distinct capability and ownership boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove selected-host scanning, cleanup authority, and cross-host coexistence</name>
  <read_first>
    src/hosts/index.cts
    src/cli/commands.cts
    tests/hosts/cross-host.test.cts
    tests/cli/commands.test.cts
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>src/hosts/index.cts, src/cli/commands.cts, tests/hosts/cross-host.test.cts, tests/cli/commands.test.cts</files>
  <behavior>
    - A Codex command does not enumerate Claude/Cursor user roots, and equivalent isolation holds for each host.
    - An active source in host A does not block installing/updating host B in the same project.
    - A selected-host active source blocks before selected project writes, while selected-host uninstall remains available if its project state is undrifted.
    - Cleanup fingerprint authority for one host/capability/argv/scope cannot be replayed against another host or combined with legacy Dev migration authority.
  </behavior>
  <action>
    Add injected scanners/runners with call counters to the fixed adapter registry and controller tests. Exercise all host pairs with simultaneous project QA installations and user-source fixtures. Assert exact read-only version/list/preflight argv, 5-second timeout, inventory schema IDs, cleanup argv/scope, scanner invocation sets, transaction call counts, sibling project/user tree digests, and independent fingerprint authority validation. Keep output ordering deterministic across hosts but never aggregate multiple-host findings into one command. Add a complete no-secret scan over human JSON/error output using sentinel config/process values.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:cross-host &amp;&amp; npm run test:cli</automated>
  </verify>
  <acceptance_criteria>
    - Each command invokes exactly one selected-host scanner and never touches sibling host roots.
    - Cross-host QA project installations and unrelated user sources coexist as D-10 requires.
    - Install/update/uninstall scan behavior matches D-12 exactly.
    - Authority/capability/argv/scope mix-ups and sentinel leakage fail deterministic tests.
  </acceptance_criteria>
  <done>DEP-03 is complete across all hosts with one-host isolation, truthful cleanup capabilities, and no secret exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-specific user stores → shared finding schema | Different host formats may contain credentials and ambiguous ownership. |
| Selected adapter registry → host roots | A command must not scan or mutate another host. |
| Verified native command → external host process | Cleanup affects user-level host state outside the project transaction. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure | Claude/Cursor scanners | critical | mitigate | Use keys/names/paths/state metadata only and reject config/process bodies from findings. |
| T-04-06-02 | Elevation of Privilege | cross-host scan dispatch | high | mitigate | Fixed one-host registry, injected call-count tests, and sibling-tree digest assertions. |
| T-04-06-03 | Tampering | host-native cleanup | high | mitigate | Exact owned identity, fixed argv, independent authority, bounded execution, and post-cleanup re-scan. |
| T-04-06-04 | Spoofing | Cursor cleanup capability | medium | mitigate | Emit manual-only findings unless an actual verified host-native uninstall command exists. |
</threat_model>

<verification>
Claude, Cursor, cross-host, and CLI suites pass the full source tier, native cleanup, manual-only, one-host scan, uninstall exception, and secret-safety matrices.
</verification>

<success_criteria>
- DEP-03 applies consistently to Codex, Claude Code, and Cursor while respecting host capability differences.
- No command scans or blocks on a non-selected host.
- Automatic cleanup remains restricted to exact owned sources with verified native commands.
</success_criteria>

## Artifacts this phase produces

- Claude Code and Cursor `scanUserSources` implementations.
- Versioned Claude capability preflight, exact scoped native cleanup plans, exclusive marketplace-removal proof, and honest Cursor manual-only findings where native uninstall is unavailable.
- Three-host cross-source isolation and authority regression coverage.

<output>
Create `.planning/phases/04-deployment-reliability/04-06-SUMMARY.md` when done.
</output>

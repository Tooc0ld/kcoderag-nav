---
phase: 04-deployment-reliability
plan: 14
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
  - ../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01/D-02: immediately after Plan 01 and before any public Dev CLI, generator, asset, or documentation contract is removed, root README and the authoritative sibling guide describe QA as the only public environment and Dev only as exact migration/uninstall input."
    - "DEP-02/D-05-D-08: both documents explain project-only targets, upward nearest-state lookup, damaged-nearest fail-open, moves/copies, and forbidden global roots."
    - "DEP-03/D-03/D-09-D-16: both documents distinguish status, doctor, and mutation self-gate plus exact fingerprint-bound native cleanup and manual-only ambiguous sources."
    - "D-17-D-20: both documents state the exact 0.2.0 Head/public/fix-forward contract without claiming Phase 06 real MCP query evidence."
    - "The sibling KCodeRag guide remains the sole `MCP_QA_EXPERIENCE_GUIDE.md`; this repository has no copy."
  artifacts:
    - path: "README.md"
      provides: "Early public QA-only five-command lifecycle and transition guide"
    - path: "../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md"
      provides: "Sole authoritative Codex/Claude/Cursor QA experience guide"
  key_links:
    - from: "README.md"
      to: "Plan 10/18 Dev retirement"
      via: "public migration/uninstall instructions precede all Dev deletion"
      pattern: "QA|legacy Dev"
    - from: "04-CONTEXT.md D-01-D-20"
      to: "both public documents"
      via: "locked command, cleanup, target, Hook, release, and evidence boundaries"
      pattern: "status|doctor|cleanup-fingerprint"
---

<objective>
Synchronize the two authoritative public entry documents to the locked QA-only contract immediately after canonical planning and before any public Dev CLI, generator, package, manifest, runtime, or guidance contract is removed.

Purpose: Ensure users have migration, cleanup, diagnosis, and recovery instructions throughout the irreversible public transition.
Output: Updated root README and sole sibling KCodeRag QA experience guide.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-01-SUMMARY.md
@README.md
@../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md

<interfaces>
- Plan 01 and locked D-01-D-20 are sufficient sources for this public transition contract; later implementation must conform to these documents and Plan 19 audits it against compiled behavior.
- Plans 02 and 07 depend directly on this plan, so neither the public CLI nor canonical generator can remove Dev selection/input before both documents are synchronized; every asset/runtime deletion remains downstream of them.
- `MCP_QA_EXPERIENCE_GUIDE.md` is owned only by `D:/AIProgram/KCodeRag`; never create a local copy.
- Generated README templates/products and automated document audits remain Plan 19 ownership.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Publish the QA-only lifecycle and transition in the root README</name>
  <read_first>
    README.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
    .planning/phases/04-deployment-reliability/04-01-SUMMARY.md
  </read_first>
  <files>README.md</files>
  <action>
    Per Plan 01 and locked D-01-D-20, document the target public contract before code removal begins: `npx kcoderag-nav@latest install --host codex|claude|cursor`, project-only exact targets, Node 22+, all five commands, QA as the only public environment, status versus doctor, source tiers, exact legacy Dev migration, and independently authorized cleanup using `--allow-owned-source-cleanup --cleanup-fingerprint sha256:...`. Document the Codex exact native plugin/remove route and its capability-gated known degraded marketplace-registration route with fixed marketplace name; document Claude's exact scoped native uninstall preference and exclusive-ownership condition for marketplace removal. State that absent/ambiguous capability, raw MCP, manual Hook, other path/name, multiple source, or incomplete post-scan is manual-only. Explain nearest-state root/deep/move and silent damaged-nearest fail-open; keep Cursor Rule/skill/MCP distinct and avoid semantic/hybrid/authenticated-query claims. Plan 19 must later fail if implementation diverges from this early public contract.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('node:fs'); const s=fs.readFileSync('README.md','utf8'); for(const x of ['npx kcoderag-nav@latest','--host codex','--host claude','--host cursor','status','doctor','--cleanup-fingerprint','QA']) if(!s.includes(x)) throw new Error('README missing '+x); if(fs.existsSync('MCP_QA_EXPERIENCE_GUIDE.md')) throw new Error('local guide copy forbidden')"</automated>
  </verify>
  <acceptance_criteria>
    - Active install/update examples are QA-only and project-scoped for all three supported hosts.
    - Legacy Dev appears only in exact migration/uninstall compatibility guidance.
    - Cleanup requires exact supported native capability, frozen fingerprint, and post-removal rescan; ambiguous cases remain manual-only.
    - Root/deep/moved/damaged-nearest and Cursor capability differences are accurate.
  </acceptance_criteria>
  <done>The repository's primary public document communicates the complete QA-only transition before Dev deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize only the authoritative KCodeRag experience guide</name>
  <read_first>
    ../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md
    README.md
    .planning/phases/04-deployment-reliability/04-CONTEXT.md
  </read_first>
  <files>../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md</files>
  <action>
    Update only `D:/AIProgram/KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md` to match the compiled public lifecycle, all three supported hosts, exact normal/degraded Codex and scoped Claude cleanup boundaries, status/doctor differences, legacy migration, nearest-state behavior, and exact 0.2.0 recovery/fix-forward contract. Explain that Phase 04 evidence proves lifecycle and Hook/Rule contracts but Phase 06 owns authenticated real MCP query evidence. Do not list unrelated clients, create a nav-repository copy, include MCP URLs/headers/Bearers, or modify unrelated sibling work.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const p='../KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md'; if(!fs.existsSync(p)) throw new Error('authoritative guide missing'); const s=fs.readFileSync(p,'utf8'); for(const x of ['Codex','Claude','Cursor','QA','status','doctor']) if(!s.includes(x)) throw new Error('guide missing '+x); if(fs.existsSync('MCP_QA_EXPERIENCE_GUIDE.md')) throw new Error('local guide copy forbidden')"</automated>
  </verify>
  <acceptance_criteria>
    - The sibling guide is the sole authoritative copy and covers Codex, Claude Code, and Cursor.
    - Its QA-only migration/cleanup/root/release guidance agrees with root README and locked decisions.
    - No unrelated sibling file, client, secret, or Phase 06 query claim is added.
  </acceptance_criteria>
  <done>Both authoritative public entry documents are synchronized before any Dev asset disappears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locked contract → public instructions | Stale guidance could direct unsupported or destructive commands. |
| kcoderag-nav → sibling KCodeRag guide | Cross-repository edit must remain one-file and secret-safe. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14-01 | Tampering | transition guidance | high | mitigate | Make Plans 02/07 depend directly on this plan so all CLI/generator/asset/docs removal remains downstream. |
| T-04-14-02 | Elevation of Privilege | cleanup instructions | critical | mitigate | Document only fixed native argv, exact scope/capability, paired fingerprint authority, and manual-only ambiguity. |
| T-04-14-03 | Information Disclosure | public/sibling docs | critical | mitigate | Prohibit URL/header/token/config bodies and use metadata-only examples. |
| T-04-14-04 | Repudiation | sibling ownership | high | mitigate | Modify only the authoritative sibling guide and require no local copy. |
</threat_model>

<verification>
Focused root/sibling checks prove QA-only command and cleanup guidance exists in both authoritative documents before any dependent Dev-contract-removal plan can execute.
</verification>

<success_criteria>
- This plan owns exactly two concrete files across the two authorized repositories.
- Plans 02 and 07 cannot execute before it; therefore no downstream Dev CLI/generator/asset/docs removal can execute before it.
- Generated docs and audit tooling remain disjoint Plan 19 work.
</success_criteria>

## Artifacts this phase produces

- QA-only root `README.md` public transition contract.
- Updated sole `D:/AIProgram/KCodeRag/MCP_QA_EXPERIENCE_GUIDE.md`.

<output>
Create `.planning/phases/04-deployment-reliability/04-14-SUMMARY.md` when done.
</output>

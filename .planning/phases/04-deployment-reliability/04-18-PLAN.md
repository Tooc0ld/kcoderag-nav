---
phase: 04-deployment-reliability
plan: 18
type: execute
wave: 9
depends_on: ["04-10"]
files_modified:
  - kcoderag-dev/README.md
  - kcoderag-dev/agents/kcode-explorer.md
  - kcoderag-dev/skills/code-lookup-discipline/SKILL.md
  - kcoderag-dev/hooks/grep-nudge.cjs
  - kcoderag-dev/hooks/hooks.json
  - kcoderag-dev/hooks/run_hook.cmd
  - kcoderag-dev/hooks/run_hook.sh
  - kcoderag-dev/hooks/update-check.cjs
  - kcoderag-dev/hooks/update-worker.cjs
autonomous: true
requirements: [DEP-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "DEP-01/D-01: after public docs and discovery surfaces transition to QA, no executable, registration, guidance, or README remains under `kcoderag-dev`."
    - "D-02: complete generated-tree deletion leaves exact source-owned legacy Dev migration/uninstall decoding operational."
    - "D-04: canonical generation treats all Dev output as forbidden rather than required missing output."
  artifacts:
    - path: "kcoderag-dev/hooks/"
      provides: "Retired executable and registration tree that must be absent"
    - path: "kcoderag-dev/agents/kcode-explorer.md"
      provides: "Retired Dev agent that must be absent"
    - path: "kcoderag-dev/skills/code-lookup-discipline/SKILL.md"
      provides: "Retired Dev skill that must be absent"
    - path: "kcoderag-dev/README.md"
      provides: "Retired Dev product documentation that must be absent"
  key_links:
    - from: "Plan 10 package/manifest retirement"
      to: "complete kcoderag-dev directory removal"
      via: "entry surfaces disappear before executable/content bytes"
      pattern: "kcoderag-dev"
    - from: "src/core/state.cts and src/hosts/*.cts"
      to: "legacy Dev migration"
      via: "historical path/digest constants remain source-owned"
      pattern: "parseLegacyInstallState"
---

<objective>
Delete the remaining executable and content Dev product tree after public guidance and package/manifest retirement are complete.

Purpose: Finish QA-only public-product retirement without coupling deletion to the package or documentation changes.
Output: Absence of all nine remaining `kcoderag-dev` files and the directory itself.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-deployment-reliability/04-CONTEXT.md
@.planning/phases/04-deployment-reliability/04-10-SUMMARY.md
@src/core/state.cts
@src/generator/index.cts
@tests/migration/legacy-state.test.cts

<interfaces>
- Plan 10 has already removed Dev from npm and deleted its MCP/compatibility entries after Plan 14 public guidance.
- This plan owns every remaining path under `kcoderag-dev/`; no other plan writes these nine files.
- Legacy Dev recognition is an exact decoder contract in current core/host source and tests, never a dependency on this generated tree.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Delete the retired Dev executable runtime</name>
  <read_first>
    kcoderag-dev/hooks/grep-nudge.cjs
    kcoderag-dev/hooks/update-check.cjs
    kcoderag-dev/hooks/update-worker.cjs
    src/generator/index.cts
  </read_first>
  <files>kcoderag-dev/hooks/grep-nudge.cjs, kcoderag-dev/hooks/update-check.cjs, kcoderag-dev/hooks/update-worker.cjs</files>
  <action>
    Delete the Dev Hook implementation and both update modules. Per D-01/D-04, do not copy, redirect, alias, or shim these paths to QA; the current adapter must render the new QA product during an authorized exact migration. Use the generator check to prove every Dev runtime-code output is forbidden.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('node:fs'); for(const f of ['kcoderag-dev/hooks/grep-nudge.cjs','kcoderag-dev/hooks/update-check.cjs','kcoderag-dev/hooks/update-worker.cjs']) if(fs.existsSync(f)) throw new Error('retired runtime remains '+f)" &amp;&amp; npm run generate:check</automated>
  </verify>
  <acceptance_criteria>
    - No Dev CJS advisory/update runtime remains.
    - Generator check treats runtime-code absence as correct and refuses attempted Dev generation.
    - QA runtime and registration bytes remain unchanged.
  </acceptance_criteria>
  <done>The repository contains no executable Dev CJS Hook/update runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the retired Dev registration and launchers</name>
  <read_first>
    kcoderag-dev/hooks/hooks.json
    kcoderag-dev/hooks/run_hook.cmd
    kcoderag-dev/hooks/run_hook.sh
    src/generator/index.cts
  </read_first>
  <files>kcoderag-dev/hooks/hooks.json, kcoderag-dev/hooks/run_hook.cmd, kcoderag-dev/hooks/run_hook.sh</files>
  <action>
    Delete the Dev Hook registration and Windows/POSIX launchers. Do not leave aliases or compatibility shims to the QA launcher; exact legacy migration renders the current QA desired state only through the selected host adapter. Require all three paths absent and canonical generation still closed around QA/Cursor.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('node:fs'); for(const f of ['kcoderag-dev/hooks/hooks.json','kcoderag-dev/hooks/run_hook.cmd','kcoderag-dev/hooks/run_hook.sh']) if(fs.existsSync(f)) throw new Error('retired registration remains '+f)" &amp;&amp; npm run generate:check</automated>
  </verify>
  <acceptance_criteria>
    - No Dev Hook registration or platform launcher remains.
    - QA registration and launchers remain byte-identical.
    - No compatibility alias or public Dev command is introduced.
  </acceptance_criteria>
  <done>No host can register or launch the retired Dev Hook product.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Dev guidance and prove exact legacy compatibility</name>
  <read_first>
    kcoderag-dev/agents/kcode-explorer.md
    kcoderag-dev/skills/code-lookup-discipline/SKILL.md
    kcoderag-dev/README.md
    tests/migration/legacy-state.test.cts
    src/core/state.cts
  </read_first>
  <files>kcoderag-dev/agents/kcode-explorer.md, kcoderag-dev/skills/code-lookup-discipline/SKILL.md, kcoderag-dev/README.md</files>
  <action>
    Delete the retired Dev agent, skill, and README, then require the complete `kcoderag-dev` directory to disappear. Per D-02, run exact migration fixtures proving historical Dev ownership is reconstructed from explicit current-source schema/path/digest facts and requires dedicated migration authority. Inspect only path names, digests, and stable codes; never reveal historical MCP values.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:migration &amp;&amp; npm run generate:check &amp;&amp; node -e "const fs=require('node:fs'); if(fs.existsSync('kcoderag-dev')) throw new Error('retired Dev tree remains')"</automated>
  </verify>
  <acceptance_criteria>
    - The complete `kcoderag-dev` directory is absent.
    - Legacy Dev decode/migration tests still pass with exact ownership and dedicated authority.
    - No QA/Cursor, public docs, unrelated repository, or sibling-repository file changes occur here.
  </acceptance_criteria>
  <done>The full Dev product tree is gone while historical migration/uninstall decoding remains exact and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated Dev bytes → repository deletion | Exact enumeration must avoid unrelated paths. |
| Deleted product → legacy migration | Compatibility cannot depend on absent public bytes. |

## STRIDE Threat Register (ASVS Level 1; blocking threshold high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18-01 | Tampering | deletion inventory | high | mitigate | Enumerate exactly nine files and require only the bounded `kcoderag-dev` directory to become absent. |
| T-04-18-02 | Denial of Service | legacy migration | high | mitigate | Run exact source-owned decoder/migration tests after generated-tree deletion. |
| T-04-18-03 | Information Disclosure | historical config | high | mitigate | Assert only paths, digests, codes, and booleans; never read or output MCP values. |
</threat_model>

<verification>
Explicit nine-file absence, complete directory absence, generator refusal, and migration tests prove complete Dev content retirement without compatibility regression.
</verification>

<success_criteria>
- This plan owns exactly nine concrete files.
- `kcoderag-dev/` is absent after execution.
- Exact legacy decode remains confined to current source/tests.
</success_criteria>

## Artifacts this phase produces

- Deletion of the six-file Dev Hook/runtime tree.
- Deletion of Dev agent, skill, and README.
- Passing legacy decode/migration evidence independent of generated assets.

<output>
Create `.planning/phases/04-deployment-reliability/04-18-SUMMARY.md` when done.
</output>

---
phase: 01-qa-repeatable-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-qa-repeatable-package/01-01-SUMMARY.md
  - .planning/phases/01-qa-repeatable-package/01-VERIFICATION.md
  - .planning/phases/01-qa-repeatable-package/01-VALIDATION.md
autonomous: true
requirements:
  - PKG-01
  - PKG-03
  - PKG-04
  - PKG-05
  - HOOK-01
  - HOOK-03
  - HOOK-04
  - HOOK-05
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "QA is the documented default and can be installed, inspected, updated, and uninstalled as a self-contained project package."
    - "One canonical source deterministically generates independent QA and Dev packages without generated drift."
    - "Structural lookup receives an advisory while malformed, exact-text, log, and single-file searches remain fail-open or silent."
    - "Generated manifests, MCP documents, hook launchers, skills, and package-local tests are valid without a canonical parent checkout."
  artifacts:
    - path: "plugin-src/"
      provides: "Canonical package, hook, skill, routing, and version inputs"
    - path: "scripts/generate_plugins.py"
      provides: "Deterministic write/check generation contract"
    - path: "scripts/manage_project_install.py"
      provides: "Default QA project lifecycle"
    - path: "tests/test_generation.py"
      provides: "Generated package and manifest regression evidence"
    - path: "kcoderag-qa/hooks/test_grep_nudge.py"
      provides: "Generated QA hook behavioral regression"
  key_links:
    - from: "plugin-src/"
      to: "kcoderag-qa/ and kcoderag-dev/"
      via: "scripts/generate_plugins.py"
      pattern: "--write|--check"
    - from: "scripts/manage_project_install.py"
      to: "target .codex/ and .agents/"
      via: "default QA render and ownership state"
      pattern: "install|status|update|uninstall"
---

<objective>
Retrospectively verify the already-implemented Phase 1 MVP from quick-task commits and current
tests. Do not recreate package or hook implementation. Produce canonical phase evidence only
when every mapped requirement has current automated or artifact evidence.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Map existing implementation to Phase 1 requirements</name>
  <files>.planning/quick/, plugin-src/, scripts/generate_plugins.py, scripts/manage_project_install.py, tests/</files>
  <action>Cross-reference all 15 Phase 1 requirement IDs against quick summaries, current source, generated QA/Dev packages, and named regression tests. Treat nested-subdirectory hook roots, refined false-positive policy, and real-host installation as Phase 4–6 hardening rather than silently expanding the original MVP.</action>
  <verify>python scripts/generate_plugins.py --check</verify>
  <done>Every Phase 1 requirement has specific current evidence and no original MVP blocker remains.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run package and hook gates and record canonical evidence</name>
  <files>.planning/phases/01-qa-repeatable-package/01-01-SUMMARY.md, .planning/phases/01-qa-repeatable-package/01-VERIFICATION.md, .planning/phases/01-qa-repeatable-package/01-VALIDATION.md</files>
  <action>Run the full unittest suite, generated QA/Dev hook regressions, generator check, pre-commit, and diff check. Record requirement coverage, deferred hardening, and Nyquist mappings without modifying implementation files.</action>
  <verify>python -m unittest discover -s tests -p "test_*.py" -q</verify>
  <done>Phase 1 verification is passed, validation is compliant, and all 15 requirements are ready to mark complete.</done>
</task>

</tasks>

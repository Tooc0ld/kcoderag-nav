---
phase: 04.1-code-style
plan: "05"
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02", "04.1-03"]
files_modified:
  - src/hooks/pre-tool-dispatcher.cts
  - src/hooks/code-style-nudge.cts
  - src/hooks/grep-nudge.cts
  - src/hooks/once-marker.cts
  - src/hooks/session-cleanup.cts
  - tests/hooks/pre-tool-dispatcher.test.cts
  - tests/hooks/code-style-nudge.test.cts
  - tests/hooks/grep-nudge.test.cts
  - tests/hooks/once-marker.test.cts
  - tests/hooks/session-cleanup.test.cts
  - plugin-src/hooks/hooks.json
  - plugin-src/hooks/run_hook.cmd
  - plugin-src/hooks/run_hook.sh
  - tests/hooks/launcher.test.cts
autonomous: true
requirements: [STYLE-01, TEST-10]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-09/D-16/D-17: one dispatcher parses bounded host events once; code-style triggers only for structured content creation/modification of exact C/C++/Lua extensions, including bounded native Codex patch headers, never ordinary shell or source-code semantics."
    - "D-13/D-14/D-15: output is one constant Skill reminder, and full Skill tree plus handler/dispatcher managed digests must match before any nudge; drift is silent."
    - "D-18-D-20: only stable IDs form hashed zero-byte once claims; fixture-proven SessionEnd may clean its own lane, otherwise live claims are never auto-deleted; saturation/errors suppress output fail-open."
  artifacts:
    - {path: "src/hooks/pre-tool-dispatcher.cts", provides: "single bounded contributor dispatcher"}
    - {path: "src/hooks/code-style-nudge.cts", provides: "structured-write classifier and constant reminder"}
    - {path: "src/hooks/once-marker.cts", provides: "stable-session once claim"}
    - {path: "src/hooks/session-cleanup.cts", provides: "receipt-gated safe cleanup handler"}
  key_links:
    - {from: "src/hooks/code-style-nudge.cts", to: "plugin-src/capabilities/code-style-nudge/skill/**", via: "complete managed digest verification"}
    - {from: "src/hooks/session-cleanup.cts", to: "fixtures/host-delivery/*.json", via: "fixture-proven SessionEnd eligibility"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "code-style support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Implement the CJS dispatcher, code-style classifier, integrity gate, stable once marker, and safe cleanup semantics (D-09-D-20).
Purpose: Produce one accurate low-noise reminder without scanning, blocking, leaking, or repeating.
Output: Runtime handlers, launchers, marker/cleanup modules, and boundary/concurrency tests.</objective>

<execution_context>@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/phases/04.1-code-style/04.1-CONTEXT.md
@.planning/phases/04.1-code-style/04.1-RESEARCH.md
@src/hooks/grep-nudge.cts
@src/hooks/update-check.cts
@src/hooks/mcp-call-marker.cts
@tests/hooks/grep-nudge.test.cts
@tests/hooks/mcp-call-marker.test.cts
@plugin-src/hooks/hooks.json
@.planning/phases/04.1-code-style/04.1-01-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-02-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-03-SUMMARY.md
</context>

<planner_flagged_assumptions>
- STYLE-01 was unclassified. Acceptance boundary is the exact structured-tool/extension/patch, complete-tree integrity, constant nudge, stable-session once, cleanup, and fail-open contract below; no deterministic audit or scan certification is implied.
</planner_flagged_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Dispatch one eligible structured write and one bounded Codex patch</name>
  <read_first>src/hooks/grep-nudge.cts, tests/hooks/grep-nudge.test.cts, src/capabilities/registry.cts, plugin-src/capabilities/code-style-nudge/skill/SKILL.md</read_first>
  <files>src/hooks/pre-tool-dispatcher.cts, src/hooks/code-style-nudge.cts, src/hooks/grep-nudge.cts, tests/hooks/pre-tool-dispatcher.test.cts, tests/hooks/code-style-nudge.test.cts, tests/hooks/grep-nudge.test.cts</files>
  <behavior>
    - D-16: extensions are exactly `.c,.cc,.cpp,.cxx,.h,.hh,.hpp,.hxx,.inl,.ipp,.lua`, matched case-insensitively with no directory exclusion.
    - D-17: only the frozen structured Write/Edit/MultiEdit/apply_patch content create/modify tool set is eligible; Codex `apply_patch` parsing is limited to bounded native envelope/header target paths for create/delete/rename/multi-file headers, never ordinary shell or source-code body semantics, and only content create/modify yields one nudge per patch.
    - Dispatcher parses once, isolates handlers, caps merged context, and serializes at most one host response.
  </behavior>
  <action>Refactor navigation into a contributor and implement the shared dispatcher plus code-style classifier per D-09, D-16, and D-17. Normalize only the fixed structured Write/Edit/MultiEdit fields and exact case-insensitive extensions. For Codex apply_patch, parse only bounded native envelope/header path records, distinguish create/modify from delete/rename-only, ignore patch body meaning, and coalesce multi-file relevance to one contribution. Never parse generic shell or file contents.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hooks/pre-tool-dispatcher.test.cjs dist-tests/hooks/code-style-nudge.test.cjs dist-tests/hooks/grep-nudge.test.cjs</automated></verify>
  <acceptance_criteria>
    - D-16 tests cover every exact extension, uppercase variants, no directory exclusion, and excluded suffixes.
    - D-17 tests prove fixed structured create/modify triggers; delete/rename/search/shell/unknown do not; apply_patch create/delete/rename/multi-file headers, malformed/oversized envelopes, body decoys, and at-most-one output are covered.
    - One handler failure cannot suppress another valid contribution.</acceptance_criteria>
  <done>Structured writes and native patches produce exactly one bounded reminder candidate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce stable-session once claims and safe cleanup</name>
  <read_first>src/hooks/update-check.cts, src/hooks/mcp-call-marker.cts, tests/hooks/mcp-call-marker.test.cts, fixtures/host-delivery/claude-2.1.241.json</read_first>
  <files>src/hooks/once-marker.cts, src/hooks/session-cleanup.cts, src/hooks/code-style-nudge.cts, tests/hooks/once-marker.test.cts, tests/hooks/session-cleanup.test.cts, tests/hooks/code-style-nudge.test.cts</files>
  <behavior>
    - D-18: stable identity is accepted only from `session_id`, `thread_id`, or `conversation_id`; cwd/PID/random/time fallback is forbidden and absence is silent; the key hashes host + managed root + capability + stable ID under OS cache `kcoderag-nav/nudges`, with no raw input and zero-byte files.
    - D-19: only a receipt-proven SessionEnd event may delete that host/session's exact owned claim; other hosts never auto-delete live claims, 1024-capacity saturation suppresses new nudges, and cleanup errors fail open.
    - D-20: atomic `wx` makes one concurrent winner and every marker/cleanup failure exits 0 with empty output.
  </behavior>
  <action>Implement stable-ID-only once claims and a separate internal SessionEnd cleanup handler. Bind automatic cleanup eligibility to fixture receipt evidence; do not assume unsupported event availability. Never arbitrarily evict live markers. At capacity 1024 or on permission/race/cleanup error, suppress output and preserve host operation. Expose no sixth public CLI command: manual cleanup is documentation-only and status/doctor remain read-only.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hooks/once-marker.test.cjs dist-tests/hooks/session-cleanup.test.cjs dist-tests/hooks/code-style-nudge.test.cjs</automated></verify>
  <acceptance_criteria>
    - D-18 absent/unstable IDs produce no marker or output.
    - D-19 SessionEnd deletes only the exact proved lane; unsupported hosts retain live claims; saturation and cleanup failures stay silent.
    - Concurrent contenders yield one zero-byte hashed marker and one output; raw root/ID/tool/code never appears.</acceptance_criteria>
  <done>Once semantics remain correct under races, capacity, lifecycle, and failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate the nudge on complete managed-tree integrity and fail-open launchers</name>
  <read_first>plugin-src/hooks/hooks.json, plugin-src/hooks/run_hook.cmd, plugin-src/hooks/run_hook.sh, tests/hooks/launcher.test.cts, plugin-src/capabilities/code-style-nudge/skill/SKILL.md</read_first>
  <files>src/hooks/pre-tool-dispatcher.cts, src/hooks/code-style-nudge.cts, plugin-src/hooks/hooks.json, plugin-src/hooks/run_hook.cmd, plugin-src/hooks/run_hook.sh, tests/hooks/pre-tool-dispatcher.test.cts, tests/hooks/code-style-nudge.test.cts, tests/hooks/launcher.test.cts</files>
  <behavior>
    - D-13/D-14: context is a constant request to load `$code-style-correction`, honor user/project precedence, and review changed regions; it contains no path/code/full rule/scan claim.
    - D-15: before claiming once, verify managed digests for SKILL.md, all four references, code-style handler, and dispatcher; any missing/edited file or extra override source makes the hook silent.
    - Hook config launches one dispatcher; malformed/oversized input, missing Node/runtime, damaged state, output failure, and handler exception all exit 0.
  </behavior>
  <action>Register one shared dispatcher and preserve Windows/POSIX fail-open launcher behavior. Implement complete capability integrity verification before marker creation. Add one fixture for each missing/edited reference, missing/edited SKILL/handler/dispatcher, and an extra override source; every fixture must yield silent hook output and an integrity finding consumed by Plan 06 status/doctor. Add no Python, network, runtime TypeScript, second capability hook, or PostToolUse scanner.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/hooks/pre-tool-dispatcher.test.cjs dist-tests/hooks/code-style-nudge.test.cjs dist-tests/hooks/launcher.test.cjs</automated></verify>
  <acceptance_criteria>
    - D-15 independently covers SKILL.md, each of four references, handler, dispatcher, and extra override drift.
    - Drift occurs before marker creation and produces empty hook stdout.
    - Constant nudge and every launcher/error boundary remain protocol-valid and nonblocking.</acceptance_criteria>
  <done>The runtime prompts only from one complete, exact managed capability tree.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-05-01 | Injection/Disclosure | hook/patch payload | critical | mitigate | bounded structured fields/header-only parsing, constant output, body/shell decoy tests |
| T-04.1-05-02 | Tampering | managed Skill/runtime | high | mitigate | complete digest set and extra-override drift rejection |
| T-04.1-05-03 | Repudiation/DoS | marker/cleanup | high | mitigate | hash-only `wx`, receipt-gated SessionEnd, no eviction, fail-open tests |
</threat_model>
<verification>Run dispatcher/classifier/marker/cleanup/launcher suites, including every D-15-D-20 fixture.</verification>
<success_criteria>One complete managed code-style tree yields one safe reminder per stable session; every unsupported/error/drift path is silent.</success_criteria>

## Artifacts this phase produces
- Shared dispatcher and code-style/navigation contributors
- Stable once-marker and receipt-gated SessionEnd cleanup handler
- Full integrity, patch-envelope, race, capacity, and fail-open tests

<output>Create `.planning/phases/04.1-code-style/04.1-05-SUMMARY.md`.</output>

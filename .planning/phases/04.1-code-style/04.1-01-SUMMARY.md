---
phase: 04.1-code-style
plan: "01"
subsystem: host-delivery-evidence
tags: [native-hosts, claude-code, enterprise-launcher, codex, cursor, opencode, tdd, secret-safe-receipts]
requires: []
provides:
  - Required real Claude Code 2.1.241 native pre-write PASS receipt captured through the authorized enterprise launcher 1.2.1
  - Closed metadata-only host delivery receipt schema with exact fixture and provenance digests
  - Conservative exact-version code-style support table containing only validated digest-bound PASS evidence
  - Real bounded Codex, Cursor, and OpenCode probes with truthful unsupported verdicts
affects: [04.1-capability-platform, code-style-nudge, host-projection, host-version-gating]
actuals:
  tokens: 14604
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [local npm pack fixture, fixed native argv, closed receipt schema, exact receipt digest binding, fail-closed support claim]
key-files:
  created:
    - src/fixtures/host-delivery.cts
    - src/hosts/host-version-support.cts
    - tests/fixtures/host-delivery.test.cts
    - fixtures/host-delivery/claude-2.1.241.json
    - fixtures/host-delivery/codex-0.146.1.json
    - fixtures/host-delivery/cursor-3.17.8.json
    - fixtures/host-delivery/opencode-1.18.23.json
  modified: []
key-decisions:
  - "Use the user-authorized enterprise launcher 1.2.1 for the real Claude session while binding host eligibility to the separately verified Claude Code 2.1.241 baseline."
  - "Enable code-style only for exact host/version rows whose checked-in closed receipt is PASS and matches its frozen canonical digest; keep navigation independently eligible."
  - "Record Codex, Cursor, and OpenCode as unsupported when model-visible pre-write context cannot be directly proved, even when native install and invocation succeed."
patterns-established:
  - "Evidence boundary: raw prompts, subprocess streams, configuration values, project paths, and session identifiers remain transient and never enter receipts."
  - "Native boundary: every capture uses a fresh absent target, locally packed public CLI install, strict version parse, fixed noninteractive argv, hidden Windows execution, and output/time limits."
requirements-completed: [PLAT-03, TEST-10]
coverage:
  - id: D1
    description: "Claude Code 2.1.241 delivers one model-visible sentinel before the first relevant write and remains nonblocking across valid, empty, malformed, nonzero, and timeout hook cases."
    requirement: PLAT-03
    verification:
      - kind: native
        ref: "fixtures/host-delivery/claude-2.1.241.json"
        status: pass
      - kind: unit
        ref: "tests/fixtures/host-delivery.test.cts#closed receipt schema accepts only complete native Claude PASS evidence"
        status: pass
    human_judgment: false
  - id: D2
    description: "All four fixed host versions have bounded closed receipts; unproved native lanes remain unsupported rather than inferred PASS."
    requirement: TEST-10
    verification:
      - kind: unit
        ref: "tests/fixtures/host-delivery.test.cts#every real host probe emits one closed receipt without inferring unsupported PASS claims"
        status: pass
    human_judgment: false
  - id: D3
    description: "code-style support is exact-version and frozen-receipt-digest bound while navigation stays independently enabled."
    requirement: PLAT-03
    verification:
      - kind: unit
        ref: "tests/fixtures/host-delivery.test.cts#code-style support rows are exact and exist only for frozen PASS receipts"
        status: pass
    human_judgment: false
duration: 76 min
completed: 2026-08-26
status: complete
---

# Phase 04.1 Plan 01: Native Host Delivery Evidence Summary

**Real Claude 2.1.241 pre-write delivery proof with secret-safe closed receipts and conservative exact-version gating for four hosts**

## Performance

- **Duration:** 76 min
- **Started:** 2026-08-26T11:53:07Z
- **Completed:** 2026-08-26T13:08:38Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Captured a real Claude Code 2.1.241 session through the authorized enterprise launcher 1.2.1 and proved a stable `session_id`, one model-visible sentinel, structured C++ create/modify plus later Lua write, and nonblocking valid/empty/malformed/nonzero/timeout hook behavior.
- Added a closed receipt parser and verifier that accepts only exact keys, bounded canonical metadata, valid digests/timestamps, and complete observations for PASS; raw session values, transcripts, paths, prompts, configuration, URLs, and credentials are excluded.
- Ran real locally packed project installs and bounded native invocations for Codex 0.146.1, Cursor 3.17.8, and OpenCode 1.18.23. Their unproved native pre-write delivery remains explicitly unsupported rather than inferred from Rule, Skill, configuration, or post-event presence.
- Bound the sole enabled code-style row to the exact Claude version and frozen receipt digest while preserving navigation eligibility for every host/version result.

## Native Evidence Outcomes

| Host baseline | Native install | Verdict | Stable reason |
| --- | --- | --- | --- |
| Claude Code 2.1.241 via enterprise launcher 1.2.1 | proved | PASS | `verified` |
| Codex 0.146.1 | proved | UNSUPPORTED | `native_context_unproved` |
| Cursor 3.17.8 | proved | UNSUPPORTED | `headless_host_unsupported` |
| OpenCode 1.18.23 | proved | UNSUPPORTED | `native_context_unproved` |

Only the closed receipts contain capture evidence. No raw subprocess output, session value, prompt, project path, configuration body, or credential was persisted.

## Task Commits

Each TDD gate was committed atomically:

1. **Task 1 RED: Claude native delivery contract** - `ded9932` (test)
2. **Task 1 GREEN: Claude native PASS capture and exact support row** - `600e7b3` (feat)
3. **Task 2 RED: cross-host closed receipt contract** - `8a449ec` (test)
4. **Task 2 GREEN: real bounded cross-host probes and receipts** - `9b8e054` (feat)

**Plan metadata:** committed with this summary and GSD tracking update.

## Files Created/Modified

- `src/fixtures/host-delivery.cts` - Real-host capture/verify CLI, strict closed receipt validation, bounded subprocess handling, native fixture probes, cleanup, and secret-safe failure codes.
- `src/hosts/host-version-support.cts` - Exact support rows validated against checked-in PASS receipt digests.
- `tests/fixtures/host-delivery.test.cts` - Closed-schema, canary, non-PASS, exact-version, digest-binding, four-receipt, and independent-navigation tests.
- `fixtures/host-delivery/claude-2.1.241.json` - Required real native PASS evidence.
- `fixtures/host-delivery/codex-0.146.1.json` - Real install/invocation with unproved context recorded as unsupported.
- `fixtures/host-delivery/cursor-3.17.8.json` - Real install/capability probe with no reliable headless writer recorded as unsupported.
- `fixtures/host-delivery/opencode-1.18.23.json` - Real install/invocation with no directly proved pre-write context recorded as unsupported.

## Decisions Made

- The user-authorized `enterprise-launcher` enterprise command is the real Claude launcher for this fixture; eligibility remains bound to the independently verified `claude --version` baseline of 2.1.241, while the fixture digest also binds enterprise launcher 1.2.1 provenance.
- `UNSUPPORTED` is an evidence boundary, not a product failure: navigation stays available, but code-style delivery remains disabled unless the exact host/version receives a complete native PASS receipt.
- Cursor Rule/Skill configuration and OpenCode post-execution plugin events cannot substitute for model-visible native pre-write context.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added the native stream requirement `--verbose`**

- **Found during:** Task 1 GREEN
- **Issue:** Claude Code-compatible launcher 2.1.241 rejects print mode with `--output-format stream-json` unless `--verbose` is also present, so the plan's literal argv could not start the required structured capture.
- **Fix:** Added only `--verbose`; all other required headless flags and native project hook loading remained unchanged.
- **Files modified:** `src/fixtures/host-delivery.cts`
- **Committed in:** `600e7b3`

### User-Authorized Launcher Clarification

- The plan named the public `claude` launcher literally. The user explicitly designated enterprise `enterprise-launcher` 1.2.1 as the equivalent authenticated Claude Code launcher and authorized that literal substitution after the official launcher lacked usable authentication.
- The capture still independently requires `claude --version` to equal 2.1.241, uses the same required headless semantics, preserves project-native settings/hook loading, and stores neither launcher output nor authentication/session data.

---

**Total deviations:** 1 auto-fixed blocking issue and 1 explicit user-authorized launcher clarification.
**Impact on plan:** No evidence criterion was weakened. The added flag is required for structured native output, and the enterprise wrapper retained the exact Claude host baseline and safety boundaries.

## Authentication Gates

- **Task 1:** The official `claude` launcher did not provide a usable authenticated session. The gate was resolved by the user's explicit authorization of authenticated enterprise launcher enterprise launcher 1.2.1; no official Claude authentication was required or modified.

## Issues Encountered

- Codex's real bounded session did not expose the complete write/session/model-context invariants, so its receipt remains `UNSUPPORTED/native_context_unproved`.
- Cursor 3.17.8 exposed no reliable fixed noninteractive writer path; its real capability probe is `UNSUPPORTED/headless_host_unsupported`.
- OpenCode's real bounded session did not directly prove a model-visible pre-write context path; its receipt remains `UNSUPPORTED/native_context_unproved`.

## TDD Gate Compliance

- Task 1 RED `ded9932` preceded GREEN `600e7b3`.
- Task 2 RED `8a449ec` preceded GREEN `9b8e054`.
- Final verification rebuilt both TypeScript targets, reran the exact real Claude capture with `--require-pass`, passed 6/6 fixture tests, and independently verified all four closed receipts.

## User Setup Required

None - the authorized launcher and fixed native baselines were available for execution.

## Next Phase Readiness

- Claude Code 2.1.241 is the sole evidence-backed code-style delivery baseline and is ready for capability composition/projection.
- Codex, Cursor, and OpenCode navigation can remain independent, but code-style projection must stay disabled until an exact-version native PASS receipt exists.
- Phase 06 real authenticated MCP-query evidence remains intentionally out of scope.

## Self-Check: PASSED

All seven implementation/test/evidence files and this summary were found. All four TDD
task commits exist, the stub scan found no placeholder/TODO/FIXME patterns, the exact
Claude native capture passed, all four receipts verified, and the fixture suite passed 6/6.

---
*Phase: 04.1-code-style*
*Completed: 2026-08-26*

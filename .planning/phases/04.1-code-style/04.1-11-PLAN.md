---
phase: 04.1-code-style
plan: "11"
type: execute
wave: 7
depends_on: ["04.1-07", "04.1-09", "04.1-10"]
files_modified:
  - src/smoke/host-smoke.cts
  - tests/smoke/host-smoke.test.cts
  - src/maintainer/pre-release-evidence.cts
  - tests/maintainer/pre-release-evidence.test.cts
  - src/maintainer/release.cts
  - tests/maintainer/release.test.cts
  - tests/maintainer/release-workflow.test.cts
  - tests/maintainer/ci-contract.test.cts
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, LEG-01, STYLE-01, TEST-10]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Pack-acquired smoke proves both additive orders, no-op repeat, failed second add preservation, update, source-conflicted uninstall zero-write, partial uninstall, final uninstall, drift, and rollback on receipt-supported lanes; unsupported lanes prove navigation succeeds and code-style is rejected before write."
    - "Required native evidence includes mandatory current Claude PASS and exact receipt binding for every other enabled code-style row; UNSUPPORTED/FAIL is never promoted to PASS or NOT_RUN success."
    - "Windows/Linux × Node 22/24 CI and release evidence close all six requirements/D-01-D-28, run generation/pack/security checks, and explicitly run `npm run audit:retirement`."
  artifacts:
    - {path: "src/smoke/host-smoke.cts", provides: "pack-acquired four-host capability lifecycle matrix"}
    - {path: "src/maintainer/pre-release-evidence.cts", provides: "closed requirement/decision/receipt/CI evidence"}
    - {path: ".github/workflows/ci.yml", provides: "four OS/runtime lanes"}
  key_links:
    - {from: "fixtures/host-delivery/*.json", to: "src/maintainer/pre-release-evidence.cts", via: "exact enabled-row receipt coverage"}
    - {from: ".github/workflows/ci.yml", to: "npm run audit:retirement", via: "explicit retirement gate in every required lane"}
  prohibitions:
    - {statement: "A selected capability set must not produce partial project mutation when any contributor or preflight fails.", status: unverified, flagged: true}
    - {statement: "code-style support must not be claimed through instruction-only, toast, Skill-only, post-event, or inferred-version substitutes for native model-visible pre-write context.", status: unverified, flagged: true}
    - {statement: "Legacy or manual MCP, plugin, Python hook, and ambiguous multi-source installations must not be migrated, adopted, deleted, or granted cleanup authority.", status: unverified, flagged: true}
---

<objective>Close TEST-10 with pack-acquired lifecycle, native receipts, four-lane CI, and release evidence.
Purpose: Prove the shipped artifact rather than source-only or generated-file-only behavior.
Output: Full smoke matrix, strict pre-release evidence, CI/release contracts, and direct retirement audit.</objective>

<execution_context>@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-code-style/04.1-CONTEXT.md
@src/smoke/host-smoke.cts
@tests/smoke/host-smoke.test.cts
@src/maintainer/pre-release-evidence.cts
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.planning/phases/04.1-code-style/04.1-07-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-09-SUMMARY.md
@.planning/phases/04.1-code-style/04.1-10-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove the packed additive capability lifecycle end to end</name>
  <read_first>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts, package.json, fixtures/host-delivery/claude-2.1.241.json, tests/cli/commands.test.cts</read_first>
  <files>src/smoke/host-smoke.cts, tests/smoke/host-smoke.test.cts</files>
  <behavior>
    - Acquire a real temporary tarball; for receipt-supported lanes run navigation→code-style and code-style→navigation additive installs, duplicate no-op, failed second add, update, source-conflicted uninstall, and partial/final uninstall.
    - For unsupported lanes, install navigation successfully, then select code-style and require a stable `host_version_unsupported` refusal before render/transaction with zero writes and unchanged healthy navigation.
    - Verify exact native assets, capability status, one transaction evidence, unrelated tree fingerprints, and original capability preservation.
    - Run real first-write native evidence only for receipt-supported lanes; current Claude is always in that set because Plan 01 requires PASS.
  </behavior>
  <action>Extend the isolated public-package harness and metadata-only loopback receipts. Branch every host by the frozen receipt table: run the full dual-order code-style lifecycle only for PASS-supported lanes; for UNSUPPORTED/FAIL lanes prove navigation install/status remains healthy and code-style selection returns the stable unsupported code before rendering/transaction with an identical before/after tree. Claude must take the supported branch because Plan 01 is require-pass. Across applicable branches include the common install/update/uninstall source gate, concurrent loser/injected rollback, D-15 asset/reference/override drift, D-18 missing stable ID, D-19 marker saturation/SessionEnd eligibility, and D-17 patch envelope cases through installed CJS. Keep PASS/FAIL/NOT_RUN semantics honest and never capture config/body/token/path/session/code.</action>
  <verify><automated>npm run build &amp;&amp; npm run test:smoke &amp;&amp; npm run smoke:required</automated></verify>
  <acceptance_criteria>
    - Every supported lane proves both additive orders and failed second add retains the original capability exactly.
    - Every unsupported lane proves navigation success plus stable code-style write-before refusal, zero writes, and unchanged navigation health.
    - Uninstall manual-source conflict is zero-write.
    - Claude native lane is PASS; every other enabled lane has exact PASS receipt, and unsupported lanes remain explicit.
    - All required evidence dimensions are PASS; NOT_RUN cannot satisfy required scope.</acceptance_criteria>
  <done>The actual npm artifact passes the complete capability lifecycle and native evidence matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind release evidence to all requirements, decisions, receipts, and CI lanes</name>
  <read_first>src/maintainer/pre-release-evidence.cts, tests/maintainer/pre-release-evidence.test.cts, src/maintainer/release.cts, tests/maintainer/release.test.cts, tests/maintainer/release-workflow.test.cts, tests/maintainer/ci-contract.test.cts, .github/workflows/ci.yml, .github/workflows/release.yml</read_first>
  <files>src/maintainer/pre-release-evidence.cts, tests/maintainer/pre-release-evidence.test.cts, src/maintainer/release.cts, tests/maintainer/release.test.cts, tests/maintainer/release-workflow.test.cts, tests/maintainer/ci-contract.test.cts, .github/workflows/ci.yml, .github/workflows/release.yml</files>
  <behavior>
    - Closed evidence requires all six requirement IDs, D-01..D-28, exact support receipts, mandatory Claude PASS, generated QA/Cursor checks, pack/smoke/docs/security results, and four CI lanes.
    - Windows/Linux Node 22/24 lanes run `npm run audit:retirement` explicitly in addition to local CI aggregation.
    - Missing/extra/stale/failed/NOT_RUN/secret-bearing evidence fails with a stable safe code.
  </behavior>
  <action>Extend immutable-subject/head-bound pre-release and release contracts for Phase 04.1. Add exact receipt/support digests and capability lifecycle evidence. Update CI/release workflows and tests to run build, full tests, generation check, pack audit, required smoke, secret scans, and a direct `npm run audit:retirement` command. Preserve immutable publish/fix-forward policy; validate readiness only and do not publish in this plan.</action>
  <verify><automated>npm run build &amp;&amp; node --test dist-tests/maintainer/pre-release-evidence.test.cjs &amp;&amp; npm run test:ci-contract &amp;&amp; npm run test:release &amp;&amp; npm run test:release-workflow &amp;&amp; npm run audit:retirement</automated></verify>
  <acceptance_criteria>
    - Every requirement/decision/receipt/generated product/CI lane is exact and closed.
    - Direct retirement audit succeeds and CI/release contract tests fail when it is removed or bypassed.
    - Secret canaries/raw evidence values never enter output or receipts.</acceptance_criteria>
  <done>Release readiness is bound to the complete Phase 04.1 evidence subject.</done>
</task>
</tasks>

<threat_model>
ASVS Level 1; blocking threshold: high.
| Threat ID | STRIDE | Surface | Severity | Disposition | Control |
|---|---|---|---|---|---|
| T-04.1-11-01 | Spoofing/Repudiation | smoke/receipt evidence | high | mitigate | packed artifact, exact receipt digests, mandatory Claude PASS, closed schema |
| T-04.1-11-02 | Tampering | CI/release lanes | high | mitigate | exact four-lane contract and direct retirement audit |
| T-04.1-11-03 | Information Disclosure | evidence/logs | critical | mitigate | metadata-only fields, secret/raw-value rejection |
</threat_model>
<verification>Run required smoke, pre-release, CI, release, and direct retirement audit commands.</verification>
<success_criteria>TEST-10 and release readiness are complete on packed bytes and all required platforms.</success_criteria>

## Artifacts this phase produces
- Pack-acquired four-host capability smoke matrix
- Closed support/requirement/decision pre-release evidence
- Windows/Linux Node 22/24 CI and release contracts with direct retirement audit

<output>Create `.planning/phases/04.1-code-style/04.1-11-SUMMARY.md`.</output>

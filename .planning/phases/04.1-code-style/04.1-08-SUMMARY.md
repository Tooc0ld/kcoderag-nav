---
phase: 04.1-code-style
plan: 08
subsystem: distribution
tags: [capability-projection, generator, npm-pack, retirement, pre-commit, node-cjs]

requires:
  - phase: 04.1-code-style/07
    provides: additive capability lifecycle, exact host support gates, and source-conflict mutation boundary
provides:
  - deterministic capability-by-host product projection from canonical source
  - closed zero-runtime-dependency npm archive policy for capability runtime and Skill assets
  - active legacy, cleanup, Dev, Python, runtime-source, scanner/SVN, and marketplace retirement guards
  - complete capability-aware pre-commit verification sequence
affects: [04.1-09, 04.1-10, 04.1-11, release, generated-products]

actuals:
  tokens: 20412
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns:
    - capability selection maps to explicit immutable host asset groups
    - package and generated-tree integrity are separate fail-closed gates
    - retirement scans active executable surfaces while allowing explicit historical regions

key-files:
  created: []
  modified:
    - src/generator/index.cts
    - package.json
    - src/maintainer/pack-audit.cts
    - src/maintainer/dependency-audit.cts
    - src/maintainer/retirement-audit.cts
    - src/maintainer/pre-commit.cts

key-decisions:
  - "Repository QA/Cursor trees remain generated outputs: Plan 08 proves canonical projection in isolated roots and leaves materialization to Plans 09/10."
  - "Pack audit validates the exact temporary tgz independently; repository generation drift remains a separate required pre-commit and CI gate."
  - "Retirement policy rejects executable authority and active instructions by stable class, while explicitly historical/legacy Markdown regions remain auditable records."

patterns-established:
  - "Capability projection: canonical capability IDs select deterministic runtime, registration, metadata, guidance, and documentation groups."
  - "Closed package policy: every published member is explicitly declared, and forbidden runtime/source/secret classes fail before release."
  - "Current pre-commit policy: build, capability, Skill, hook, conflict, generator, retirement, then read-only repository generation check."

requirements-completed: [PLAT-01, PLAT-03, LEG-01, STYLE-01, TEST-10]

coverage:
  - id: D1
    description: Deterministic capability-aware QA and Cursor projections render and rollback entirely in isolated roots.
    requirement: PLAT-01
    verification:
      - kind: integration
        ref: "npm run test:generator (12/12) and npm run test:generator:repository (4/4)"
        status: pass
    human_judgment: false
  - id: D2
    description: The public npm archive remains Node 22+ CJS, dependency-free at runtime, and closed around exact capability assets.
    requirement: PLAT-03
    verification:
      - kind: integration
        ref: "npm run test:dependency-audit (8/8), npm run test:pack (14/14), npm run pack:audit"
        status: pass
    human_judgment: false
  - id: D3
    description: Retired compatibility surfaces cannot re-enter active source or bypass the capability-aware pre-commit checks.
    requirement: LEG-01
    verification:
      - kind: unit
        ref: "npm run test:retirement-audit (10/10), npm run test:precommit (9/9), npm run audit:retirement"
        status: pass
    human_judgment: false

duration: 30min
completed: 2026-08-27
status: complete
---

# Phase 04.1 Plan 08: Capability Generation and Distribution Policy Summary

**Deterministic capability-by-host projection with an exact Node/CJS npm archive, active retirement guards, and a capability-aware pre-commit chain**

## Performance

- **Duration:** 30 min
- **Started:** 2026-08-26T16:33:03Z
- **Completed:** 2026-08-26T17:02:19Z
- **Tasks:** 3
- **Files modified:** 12

## Accomplishments

- Replaced product-wide generation branching with canonical capability selection and explicit runtime, registration, metadata, guidance, documentation, and union projections; repeatable selections canonicalize and incompatible host projections refuse before writes.
- Closed the public package around exact capability registry/runtime/Skill members while retaining Node.js 22+, CommonJS user paths, zero runtime dependencies, immutable version propagation, and real temporary-tgz verification.
- Extended retirement and pre-commit policy so active legacy migration/cleanup authority, Dev, Python, runtime TypeScript, scanner/SVN workflows, root marketplaces, and omitted current checks fail with stable safe codes.

## Task Commits

Each TDD task was committed as a RED/GREEN pair:

1. **Task 1: Render one complete capability-aware host product in an isolated root** - `323fe13` (RED), `c2f5680` (GREEN)
2. **Task 2: Close package, pack, and dependency policy** - `fb162b7` (RED), `22cdf7f` (GREEN)
3. **Task 3: Enforce retirement and pre-commit policy** - `728e8a4` (RED), `ab7ee81` (GREEN)

## Files Created/Modified

- `src/generator/index.cts` - Canonical capability selection, host projection groups, deterministic isolated generation, and strict CLI selection.
- `tests/generator/generation.test.cts` - Capability projection, selection, no-op, drift, incompatibility, and rollback coverage.
- `tests/generator/repository-generation.test.cts` - Isolated repository-source projection without writing checked-in product trees.
- `package.json` - Exact published Skill assets and current capability/hook/manual-conflict test scripts.
- `src/maintainer/pack-audit.cts` - Closed archive inventory for the capability runtime and canonical code-style Skill tree.
- `tests/maintainer/pack-audit.test.cts` - Missing, extra, forbidden, secret, version, and real-pack policy coverage.
- `src/maintainer/dependency-audit.cts` - Exact current scripts, zero runtime dependencies, and removed migration-suite enforcement.
- `tests/maintainer/dependency-audit.test.cts` - Current script and obsolete migration rejection coverage.
- `src/maintainer/retirement-audit.cts` - Active retired-surface scanner plus zero-argument final audit mode.
- `tests/maintainer/retirement-audit.test.cts` - Every retired class, historical-region allowance, and compiled CLI coverage.
- `src/maintainer/pre-commit.cts` - Ordered build/capability/Skill/hook/conflict/generator/retirement checks with index preservation.
- `tests/maintainer/pre-commit.test.cts` - Required-check failure codes, ordering, removal of migration, and no-mutation guarantees.

## Decisions Made

- Checked-in `kcoderag-qa/**` and `kcoderag-cursor/**` remain output-only and were not changed; isolated roots prove Plan 08 generator correctness before Plans 09/10 materialize each tree.
- Pack audit owns exact archive acquisition and inventory integrity, while `generate:check` remains a distinct required pre-commit/CI control. This lets package policy be tested without weakening either boundary or crossing generated-output ownership.
- Historical records are allowed only beneath explicit history/legacy/migration/retirement headings or `.planning`; active instructions and executable declarations remain fail-closed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Separated archive validation from cross-plan repository generation drift**

- **Found during:** Task 2 (Close package, pack, and dependency policy)
- **Issue:** The pack auditor invoked repository `generate:check`, so Plan 09/10-owned output drift prevented validating Plan 08's independent archive policy.
- **Fix:** Kept generation as a required pre-commit/CI gate and made pack audit validate dependencies plus the real temporary tgz and exact archive inventory directly.
- **Files modified:** `src/maintainer/pack-audit.cts`, `tests/maintainer/pack-audit.test.cts`
- **Verification:** `npm run test:pack` 14/14 and `npm run pack:audit` returned 72 exact entries.
- **Committed in:** `22cdf7f`

---

**Total deviations:** 1 auto-fixed (1 blocking integration issue)
**Impact on plan:** The fix preserves both fail-closed controls and avoids taking ownership of generated products assigned to Plans 09/10.

## Issues Encountered

- Normal source commits reached the repository generation gate and stopped on the known Plan 09/10 output gap. After fresh scoped verification, each affected commit was path-staged and made with narrowly authorized hook bypass; no generated tree or unrelated dirt was staged.
- Full `npm test` completed with 317/320 passing. The three failures are exclusively generated-output ownership: two Cursor product tests still expect the old four-file closed inventory, and one QA product digest test sees the old `hooks/hooks.json` bytes. `generate:check` reports the corresponding QA/Cursor Skill/runtime materialization set (15 paths), all delegated to Plans 09/10.

## TDD Gate Compliance

- Task 1: RED `323fe13` precedes GREEN `c2f5680`.
- Task 2: RED `fb162b7` precedes GREEN `22cdf7f`.
- Task 3: RED `728e8a4` precedes GREEN `ab7ee81`.

## Known Stubs

None. The only `placeholder` matches are pack-audit rejection codes and their negative tests.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 09 can materialize the QA dispatcher/code-style runtime and canonical Skill tree from the now-proven generator contract.
- Plan 10 can materialize the Cursor canonical Skill tree and update its closed product inventory.
- After those output-owned commits, the repository-wide generation check and the remaining three full-suite tests should converge without changing Plan 08 source policy.

## Self-Check: PASSED

All 12 modified implementation/test files, this summary, and the six RED/GREEN task commits were verified on disk and in Git history.

---
*Phase: 04.1-code-style*
*Completed: 2026-08-27*

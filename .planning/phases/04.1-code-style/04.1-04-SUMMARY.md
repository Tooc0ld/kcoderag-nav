---
phase: 04.1-code-style
plan: "04"
subsystem: capability-lifecycle-core
tags: [typescript, node, capability-composition, atomic-transaction, ownership, legacy-retirement]
dependency_graph:
  requires:
    - 04.1-03 synthetic capability contracts and frozen support receipts
  provides:
    - exact contributor-aware current install state
    - deterministic whole-set capability composer
    - metadata-only selected-host source findings
    - current-schema-only adapter observation shells
  affects:
    - 04.1-06 native host capability projections
    - 04.1-07 capability-native CLI orchestration
    - package inventory for dist/capabilities/compose.cjs
tech_stack:
  added: []
  patterns:
    - canonical capability ordering with composite ownership digests
    - one immutable desired state per selected capability set
    - state-last transaction commit with complete rollback
    - metadata-only conflict observation without config-body reads
key_files:
  created:
    - src/capabilities/compose.cts
    - tests/capabilities/compose.test.cts
  modified:
    - src/core/contracts.cts
    - src/core/state.cts
    - src/hosts/host-adapter.cts
    - src/hosts/user-sources.cts
    - src/hosts/codex.cts
    - src/hosts/claude.cts
    - src/hosts/cursor.cts
    - src/hosts/opencode.cts
    - tests/core/transaction.test.cts
    - tests/hosts/user-sources.test.cts
  deleted:
    - tests/migration/legacy-state.test.cts
decisions:
  - "Install state is one exact capability graph; retired environment-shaped and Python records are invalid inputs, not migration candidates."
  - "Shared-file ownership is bound to canonical contributor IDs and one composite digest; originals restore only after the final contributor is removed."
  - "Manual or retired host sources expose frozen path/code metadata only and never cleanup commands, fingerprints, callbacks, or inferred ownership."
metrics:
  duration: 30m
  completed: 2026-08-26
status: complete
actuals:
  tokens: 102971
  tasks: 2
  commits: 5
---

# Phase 04.1 Plan 04: Capability Composition and Legacy Authority Retirement Summary

Contributor-aware capability state and deterministic set composition now feed one state-last atomic transaction, while unpublished QA/Dev/Python migration and executable cleanup authority are absent from the compiled runtime.

## Performance

- **Duration:** 30 minutes
- **Started:** 2026-08-26T13:52:00Z
- **Completed:** 2026-08-26T14:22:00Z
- **Tasks:** 2
- **Files changed:** 13

## Accomplishments

- Composed two synthetic capability contributions into one immutable desired state with stable capability ordering, contributor-aware files/sections, and a composite digest.
- Proved collision, path-escape, incomplete-set, drift, staging, commit, and rollback failures produce zero partial selected-set mutation.
- Proved one-contributor recomposition preserves remaining ownership and unrelated bytes, while final removal restores the exact original shared bytes.
- Replaced the environment-shaped state decoder with one exact capability schema; retired Node/Python state shapes are rejected as `invalid_state`.
- Deleted Codex, Claude, Cursor, and OpenCode migration/owned-cleanup branches and reduced source reporting to frozen, sorted, secret-safe metadata.

## Task Commits

1. **Task 1 RED: capability composition tracer** - `3283aed`
2. **Task 1 GREEN: atomic contributor-aware composition** - `1b00331`
3. **Task 2 RED: retired state and cleanup authority boundary** - `5993668`
4. **Task 2 GREEN: current-only state consumers and metadata findings** - `b8c15a5`

## Verification

- `npm run build` — PASS.
- `node --test dist-tests/capabilities/compose.test.cjs dist-tests/core/transaction.test.cjs dist-tests/hosts/user-sources.test.cjs` — PASS, 23/23.
- Runtime export audit — PASS: state exports contain no legacy/capability-transition decoder; source helpers expose no cleanup factory/executor; all four adapter objects expose neither cleanup nor migration callbacks.
- Retired-symbol scan across current core/host source and compiled runtime — PASS.
- `npm test` — integration gate intentionally not green at this wave boundary: 253 passed, 72 failed, 1 skipped out of 326. Failures are confined to later-wave host projection/CLI/launcher/smoke suites plus the undeclared compiled composer inventory.

## TDD Gate Compliance

- RED commits precede both GREEN commits.
- Task 1 RED failed on the missing composer module before implementation.
- Task 2 RED failed on the old state decoder and executable cleanup runtime exports before removal.

## Known Stubs

| File | Line | Intentional boundary | Resolving plan |
|---|---:|---|---|
| `src/hosts/codex.cts` | 196 | Native capability projection returns `capability_projection_required`. | 04.1-06 |
| `src/hosts/claude.cts` | 175 | Native capability projection returns `capability_projection_required`. | 04.1-06 |
| `src/hosts/cursor.cts` | 171 | Native capability projection returns `capability_projection_required`. | 04.1-06 |
| `src/hosts/opencode.cts` | 171 | Native capability projection returns `capability_projection_required`. | 04.1-06 |

These placeholders do not prevent this plan's synthetic composition/state/transaction goal; they deliberately prevent the old product-shaped adapters from mutating with the new schema before Plan 06 supplies complete host projections.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Preserved buildability across the Plan 07 CLI ownership boundary**

- **Found during:** Task 2 GREEN
- **Issue:** `src/cli/commands.cts` still imports retired cleanup types and reads retired observation fields, but this plan is explicitly restricted to plan-declared files and Plan 07 owns CLI replacement.
- **Fix:** Retained compile-only TypeScript seams in contracts while emitting no runtime cleanup fields, plans, factories, callbacks, or adapter authority.
- **Files modified:** `src/core/contracts.cts`, `src/hosts/host-adapter.cts`, `src/hosts/user-sources.cts`
- **Commit:** `b8c15a5`

**2. [Rule 3 - Blocking] Removed stale compiled migration test before the full gate**

- **Found during:** Overall verification
- **Issue:** TypeScript does not clean deleted outputs, so the ignored `dist-tests/migration/legacy-state.test.cjs` remained discoverable after deleting its canonical source.
- **Fix:** Removed that exact generated file after validating it was inside the workspace; no tracked package file was changed.
- **Files modified:** ignored build output only
- **Commit:** not applicable

## Deferred Integration Gaps

- Plan 06 must replace `capability_projection_required` in `src/hosts/codex.cts`, `src/hosts/claude.cts`, `src/hosts/cursor.cts`, and `src/hosts/opencode.cts`; current full-suite failures occur in `tests/hooks/launcher.test.cts`, the four `tests/hosts/*.test.cts` adapter suites, `tests/hosts/cross-host.test.cts`, and `tests/smoke/host-smoke.test.cts`.
- Plan 07 must remove the old cleanup/migration controller flow in `src/cli/commands.cts` and then delete the compile-only seams from `src/core/contracts.cts`, `src/hosts/host-adapter.cts`, and `src/hosts/user-sources.cts`; current CLI integration failures are in `tests/cli/commands.test.cts`.
- Wave integration must declare the newly compiled `dist/capabilities/compose.cjs` package member. `tests/maintainer/pack-audit.test.cts` currently reports `files_policy_invalid`; package/pack files were outside this plan and were intentionally not changed.

## Threat Flags

No unplanned network, authentication, schema-at-trust-boundary, or write surface was introduced. The new filesystem composition path remains behind existing validated project roots and the single transaction boundary.

## Self-Check: PASSED

All declared current files and the summary exist, the obsolete migration suite is absent, and commits `3283aed`, `1b00331`, `5993668`, and `b8c15a5` are present in repository history.
